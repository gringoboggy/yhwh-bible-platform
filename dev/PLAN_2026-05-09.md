# Master plan — Ethiopian Tewahedo Bible publishing platform

**Date:** 2026-05-09 — full restructure after the v1.0 candidate
became shippable. Replaces `dev/PLAN_2026-05-08.md` (now in
`dev/archive/`).

**Status:** active master sequence. Companion docs: every
`dev/SCOPE_*.md` and the still-canonical
`dev/CLAUDE_PROJECT_RULES.md`.

**Why this restructure:** the 2026-05-08 plan was structured around
the path TO v1.0. Every phase from Tier A through Tier C shipped
plus several from Tier D got promoted forward. The v1.0 corpus
floor was crossed by 26K notes. The plan is no longer a road; it's
an inventory plus a sequence of post-candidate options. The
restructure surfaces:

  - **What's already shipped** (so future Claude doesn't re-scope it)
  - **Explicit `Depends:` and `Unblocks:` per open phase** (was
    implicit prose; the linter can verify the graph now)
  - **File-overlap clusters** (phases touching the same files are
    bundled — bandwidth-aware per §3.6)
  - **`[USER]` markers** so user-side phases don't compete for my
    session bandwidth
  - **Track grouping** (Release / Short / Medium / Long / Hardening
    / User-side / Parked) instead of Tier A/B/C — surfaces the
    right next move at any session-bandwidth budget
  - **Pre-session ordering recommendation** in §6 — "if the next
    session is X minutes / focused on Y, do Z"

---

## 0. Bootstrap pointer

Future Claude sessions read, in order:

```
1. dev/CLAUDE_PROJECT_RULES.md   rules + §9 mental models
2. dev/SESSION_STATE.md          live snapshot
3. dev/PLAN_2026-05-09.md        THIS FILE — sequence forward
```

Optional follow-ons: `dev/IN_FLIGHT.md` (open task tracker) and
`dev/CHANGELOG.md` (chronological history).

---

## 1. North star — unchanged

```
v1.0 candidate criteria:    ALL MET (51,394 notes; θ.2 + χ.1 + ψ.8
                            + ψ.10/12/13/14/15/17/18 + ω.8/9/10
                            + ξ.1/2/4 + corpus ≥ 25K)

Corpus depth (Ethiopian flagship):
  TARGET:  35,000-40,000 notes
  TODAY:   51,394 (147% of original target)

Buyer demo arc:             SHIPPABLE
Visual QA pass:              PENDING (user-side, browser + e-reader)
Binary build:                INFRASTRUCTURE READY (PyInstaller +
                            PyWebView wired; user runs build per
                            platform; signing licenses optional)
```

The buyer demo unchanged from CLAUDE_PROJECT_RULES.md §1: open
/wizard, click through ~8 cards, click BUILD, an EPUB downloads
with that publisher's imprint, ISBN, copyright, theme, only their
picked notes, and verse popups in the languages they configured.

---

## 2. Status snapshot

```
13 consoles · 971 tests · 10/10 linter · 5 editions · 51,394 notes

Phases shipped this session arc (excerpt):
  ψ.18 + ψ.18.1   matrix-totals sidebar + per-chapter drilldown
  ψ.15            editor-console polish (8 of 13 consoles unified)
  χ.7 + χ.6+      Nave's OCR + Hebrew re-promote (corpus +35K)
  χ.1             Strong's Greek (+7,399 lang-greek notes)
  θ.1-θ.4         desktop binary chain (launcher / shell / appcast / installers)
  ψ.13/14/17      design system + buyer-arc polish + reader EPUB
  ω.5             paths-resolver foundation (+ ω.5.1-3 migrations)
  τ.1             WEB translation infrastructure
  χ-AI-xrefs      LLM-backed thematic xref detector (paid; user-side)
  χ.0             Kenyon textual-criticism ingest
```

For the full phase ledger see §7 (Phase ledger) below.

---

## 3. Track structure

Six tracks. Each open phase belongs to exactly one. Tracks order
work by how it competes for bandwidth, not by Greek-letter family.

| Track | Description | Session cost |
|---|---|---|
| **RELEASE** | One-time motion to declare v1.0 shipped | 0.5-1 session |
| **SHORT** | 1-session phases; bandwidth-cheap; bundle by file overlap | 1 each |
| **MEDIUM** | 1-2 sessions; needs a spec read or spec write | 1-2 each |
| **LONG** | Multi-session; strategic features needing infrastructure | 2+ |
| **HARDENING** | Audit-style; runs alongside any track | 0.5-1 each |
| **USER-SIDE** | No my-session bandwidth; user runs externally | 0 from me |
| **PARKED** | Design call needed before implementing | gated |

A phase with multiple sub-phases (e.g. ψ.7-A + ψ.7-B) lives on the
same track unless one explicitly waits.

---

## 4. RELEASE TRACK — declare v1.0 shipped

### v1.0.0 — Visual QA + binary build + tag

**Status:** open
**Depends:** all v1.0 candidate criteria (✓ met — see north star §1)
**Unblocks:** v1.x release cadence
**Effort:** 0.5-1 session (mostly user-side)
**Track:** RELEASE
**Files:** dev/VERSION (touch), git tag

User runs:

    pip install pyinstaller pywebview
    pyinstaller dev/launcher.spec     # → dist/YHWH.{exe,app}
    ./dev/build_dmg.sh                # macOS DMG (or)
    dev\build_msi.cmd                 # Windows installer (or)
    ./dev/build_appimage.sh           # Linux AppImage

Plus visual walkthrough: open browser to each of 13 consoles + a
freshly-built EPUB in Apple Books / Calibre / Kobo / Kindle. Sign
off or file specific tweaks (which become v1.0.1 patch fixes).

Shippable WITHOUT signing licenses; signed builds are a separate
opt-in (Apple Dev ID $99/yr; Authenticode $200-400/yr; Linux
unsigned).

---

## 5. OPEN PHASES — by track

### 5.1 SHORT TRACK

#### ψ.7-A — Four built-in editions

**Status:** open
**Depends:** —
**Unblocks:** ψ.7-B (templates need example seed editions)
**Effort:** ~1 session, LOW risk (data-only)
**Files:** content/editions.yaml; tests/test_scripts.py
**Cluster:** EDITIONS

Adds 4 built-in editions to the existing 5, lifting the dropdown to
9 traditions:

| New edition | Canon | Distinctive lens |
|---|---|---|
| `eastern-orthodox` | orthodox (78b, **already defined, currently unused**) | LXX-leaning; Patristic / Byzantine commentary; foregrounds liturgy-byzantine |
| `anglican-bcp` | catholic (76b; Apocrypha as deuterocanonical) | BCP-style apparatus; lectionary integration |
| `lutheran-confessional` | protestant (66b) | Reformation-era commentary foregrounded; Apocrypha as separate section |
| `coptic-orthodox` | ethiopian (87b — Coptic shares ~78 with Ethiopian) | Coptic patristic + ascetic-monastic emphasis |

Pattern from CLAUDE_PROJECT_RULES.md §9 "Add a new edition feature":
schema additive, defaults preserve back-compat, build-pipeline
no-op when fields unset. Each new edition adds ~30 YAML lines. No
Python changes needed — the loader already iterates editions
generically.

Tests cover: each new edition loads, has valid canon ref, kind
filter set yields >0 notes per the matrix, build_one() produces a
non-empty EPUB.

Spec: `dev/SCOPE_2026-05-09-addendum-edition-templates.md`
(written alongside this PLAN refresh).

#### ψ.7-B — Edition template starter packs

**Status:** open
**Depends:** ψ.7-A
**Unblocks:** v1.x edition expansion (any new edition can land as a
template first, then be promoted to built-in)
**Effort:** ~1 session, LOW risk
**Files:** new content/edition_templates/*.yaml (5-7 files);
scripts/web.py (api_edition_templates_list, api_create_edition_from_template);
scripts/templates/wizard.py (Start-from-template button on step 1);
tests/test_scripts.py
**Cluster:** EDITIONS

Folder of partial-edition YAML records that the wizard offers as
starting points. Templates ride the existing ν.4 cloning machinery.

Templates to ship:

  1. `monastic-daily-office` — daily office reading order, low-density apparatus
  2. `school-friendly-nrsv` — large fonts, simple kinds, no Hebrew/Greek popups
  3. `children` — large fonts, illustrations slot, simplified commentary
  4. `family-devotional` — Q&A style, mid-density, family-reading rhythm
  5. `scholarly-academic-with-apparatus` — mirrors the existing built-in for clone-and-tweak
  6. `anglican-bcp` (template mirror of ψ.7-A's built-in)
  7. `lutheran-confessional` (template mirror)

Pattern: each YAML is a partial `editions.yaml`-style record with a
`template_id` instead of `id`, plus a `description` field shown in
the wizard picker. The /api/editions/from-template endpoint
deep-copies the template + applies user-supplied id/title overrides
+ runs ν.4 clone validation.

#### ψ.16 — Status-dashboard polish

**Status:** open
**Depends:** ψ.15 (✓ shipped — 5 of 8 templates already use _design)
**Unblocks:** ψ.13.5 (the f-string sweep needs every template using
the substitution markers first)
**Effort:** ~1 session, LOW risk
**Files:** scripts/templates/{audit,preflight,ops,diff,apihelp,index}.py;
tests/test_scripts.py
**Cluster:** TEMPLATES

Apply HEADER_NAV_LINKS + BUYER_ARC_POLISH_CSS to the remaining 5
status/dashboard consoles. Same pattern as ψ.14 (compare/wizard/export)
and ψ.15 (customize/publisher/covers/matrix/sources). Lands all 13
consoles on a single source of truth for the cross-link nav and
buyer-arc polish CSS.

Note: /index (note editor) is the largest template and may need
extra care for the brand strong + heavy nav baseline. /apihelp is
trivial.

#### ν.2.8 + ψ.11 — UX polish duo

**Status:** open (ν.2.9 ✓ shipped 2026-05-08)
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session combined (each is short)
**Files:** scripts/templates/customize.py, wizard.py; small CSS
additions in _design.py
**Cluster:** UX-MICRO

Two small visual fixes from the 2026-05-08 audit (ν.2.9 already
shipped its pending-save chip):

  - **ν.2.8** customize console — visual sections via `<details>` /
    `<section>` boundaries instead of one undifferentiated grid
  - **ψ.11** wizard — reversibility hints + better field-grouping
    rhythm in the branding step

Bundle as one PR; the file overlap is high (customize.py +
wizard.py) and the changes are CSS-only / small markup tweaks.

#### ψ.13.5 — F-string sweep across 8 templates

**Status:** open
**Depends:** ψ.16 (all 13 templates need substitution markers
before the sweep so the diff is uniform)
**Unblocks:** future template authors who'd benefit from real
interpolation instead of `.replace()`
**Effort:** ~1 session, LOW-MED risk (8-template diff)
**Files:** scripts/templates/{compare,wizard,export,customize,publisher,covers,matrix,sources,audit,preflight,ops,diff,apihelp,index}.py
(after ψ.16)
**Cluster:** TEMPLATES

Convert the 13 ψ.13/14/15/16 consumers from `r"""..."""` raw
templates + `.replace()` substitutions to f-string interpolation.
Single focused diff; defers regression risk to one file at a time
within the PR. The §6.2 cross-link linter already imports each
module post-substitution, so it's the load-bearing safety net.

#### ψ.18.2 — Per-chapter granularity for non-top-5 books

**Status:** open
**Depends:** ψ.18 + ψ.18.1 (✓ shipped)
**Unblocks:** —
**Effort:** ~1 session, LOW risk
**Files:** scripts/core/matrix.py; scripts/web.py (api_matrix);
scripts/templates/matrix.py; tests/test_scripts.py
**Cluster:** MATRIX-SIDEBAR

ψ.18.1 caps the chapter drilldown at top-5 books per kind ("+ N
more books" italic line for the rest). For kinds spanning many
books (e.g. xref-citation across 60+ books), the long tail is
hidden. ψ.18.2 makes the "+ N more books" expandable — click to
reveal the full per-chapter breakdown.

Tradeoff: at full corpus this could render hundreds of rows; gate
behind an explicit user click + lazy-load pattern.

#### ψ.26 — Matrix bulk operations (multi-select + drag-select + bulk-apply)

**Status:** open
**Depends:** ψ.12 (✓ shipped — DOM patching infra) + ψ.18.1 (✓
shipped — drilldown surface for bulk previews)
**Unblocks:** operator productivity at 9-edition scale (toggling
the same kind across 9 editions is currently 9 clicks)
**Effort:** ~1 session, LOW-MED risk
**Files:** scripts/templates/matrix.py (selection-state model +
bulk-apply UI); tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Three bulk-op flows:

  1. **Shift+click** to range-select consecutive kinds in a
     category; toggle once → applies to all selected
  2. **Drag-select** across a row (one kind, multiple editions)
     to bulk-toggle that kind everywhere
  3. **Apply-to-all** button per kind — "enable comm-patristic
     across all 9 editions" via one click

Selection state is client-side only (LOCAL_ENABLED-style); save
flow unchanged. Indeterminate-state checkboxes for category
parents already exist post-ψ.12.

#### ψ.27 — Matrix scenarios + import/export YAML

**Status:** open
**Depends:** ψ.7-B (✓ once shipped — same template-yaml pattern)
**Unblocks:** sharing matrix configurations across machines /
publishers; /api/scenarios for backup
**Effort:** ~1 session, LOW risk
**Files:** new content/matrix_scenarios/*.yaml (5-7 named
scenarios: minimal, devotional, academic, family, scholarly,
mvp-launch, full-corpus); scripts/web.py (api_matrix_scenarios_list,
api_matrix_export_yaml, api_matrix_import_yaml);
scripts/templates/matrix.py (Scenarios picker UI);
tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Today /matrix has minimal scenario load infrastructure (one path).
ψ.27 promotes it to a first-class feature: named scenarios,
import/export YAML for portability. Scenarios apply across
editions or to one edition.

Initial built-in scenarios:

| Scenario | Use case | Default kinds |
|---|---|---|
| `minimal` | reader's edition | text + xref-citation only |
| `devotional` | family reading | comm-* + dev-application + dev-prayer |
| `academic` | scholar / seminary | full apparatus, all comm-*, all hist-* |
| `scholarly` | seminary library | every kind, every category, mvp+phase2+phase3 |
| `mvp-launch` | initial retail SKU | mvp-phase only |
| `full-corpus` | superset (current scholarly-academic edition) | every kind |

#### ψ.28 — Matrix search-and-filter for kinds

**Status:** open
**Depends:** —
**Unblocks:** scaling beyond 60 kinds (today's matrix scrolls;
search is faster)
**Effort:** ~1 session, LOW risk
**Files:** scripts/templates/matrix.py (search input + visibility
filter on kind rows); tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Type-ahead filter that hides non-matching kind rows in real time.
Matches on kind code, label, category, or symbol. `/` keyboard
shortcut focuses the input. Empty query restores all kinds.

Trivial UI but high productivity multiplier — finding a specific
kind among 60+ today requires Cmd+F or scroll-and-scan.

#### ψ.29 — Matrix undo/redo + keyboard help overlay

**Status:** open
**Depends:** ψ.12 (✓ shipped — incremental DOM patching enables
clean undo without full rerender)
**Unblocks:** confident editing at scale (less fear of misclick)
**Effort:** ~1 session, LOW-MED risk
**Files:** scripts/templates/matrix.py (undo stack + Cmd+Z/Y
handler + `?` help modal); tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Undo/redo stack of toggle ops bounded at 50 entries. Cmd+Z /
Ctrl+Z = undo last toggle; Cmd+Shift+Z / Ctrl+Y = redo. Reset
button still works as the global "discard all unsaved".

Plus `?` keyboard shortcut shows a help modal listing every
shortcut: Tab navigation, Space to toggle, Cmd+Z, /, Esc, Cmd+S
to save. Documents what already exists post-ψ.12 + new ψ.26-29
additions.

#### ψ.30 — Matrix accessibility + mobile responsive

**Status:** open
**Depends:** —
**Unblocks:** non-mouse users; tablet workflows
**Effort:** ~1-2 sessions, LOW-MED risk
**Files:** scripts/templates/matrix.py (ARIA labels + aria-live
for save status + responsive breakpoints); tests/test_scripts.py
(structural ARIA presence checks)
**Cluster:** MATRIX-EDIT

  - **Accessibility**: ARIA roles on grid + cells + live regions
    for save status; proper label associations on every checkbox;
    skip-link to main content; ensure focus rings visible (✓
    already from ψ.14 BUYER_ARC_POLISH_CSS once ψ.16 lands).
  - **Mobile responsive**: at narrow viewports the grid switches
    to a per-edition tabbed view (one edition's kind list at a
    time) with a horizontal scroll for category jumps. Pinches /
    swipes work via standard touch events.

Pairs with ψ.16 (status-dashboard polish) since ψ.16's
HEADER_NAV_LINKS substitution is the natural place to add the
skip-link.

#### ψ.31 — Matrix per-book overrides UI integration

**Status:** open
**Depends:** ν.2.7-A + ν.2.7-B (✓ shipped — popup_languages_per_book
encoder/decoder); ψ.8.4 (✓ shipped — traditions_per_book pattern)
**Unblocks:** unified editor experience (today /matrix and
/customize per-book sections feel disconnected)
**Effort:** ~1-2 sessions, MED risk
**Files:** scripts/templates/matrix.py (per-book drilldown panel);
scripts/web.py (api_matrix surfaces per-book toggle state);
scripts/core/matrix.py (per_book_overrides field on Matrix);
tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Today the matrix shows per-book counts (post ψ.18) but not
per-book OVERRIDES (which kinds are toggled differently for
specific books). Per-book overrides exist for popup_languages
and traditions but aren't visible in the matrix. ψ.31 adds a
fourth dimension: clicking a kind cell offers "override for
specific books" sub-panel mirroring /customize's per-book matrix
shape.

The fourth-dimension data (ed × kind × book × override-state) is
small (override is rare) so it doesn't blow up payload like the
parked full-corpus per-chapter expansion.

#### ψ.32 — Matrix compare-editions side-by-side view

**Status:** open
**Depends:** —
**Unblocks:** retail decision-making ("we already ship X, would
adding Y duplicate effort?")
**Effort:** ~1-2 sessions, MED risk
**Files:** new scripts/templates/_matrix_compare.py (or
extension to matrix.py); scripts/web.py (api_matrix_compare);
tests/test_scripts.py
**Cluster:** MATRIX-EDIT

Pick 2 editions from the dropdown, see a delta-only matrix:
green rows = enabled in A but not B, red rows = enabled in B
not A, neutral rows = identical. Counts column shows the note-
delta per kind.

Powers ψ.25 (edition diff) at the matrix level — ψ.25 is more
about the DIFF console; ψ.32 is the live in-/matrix comparison
flow.

#### ψ.33 — Matrix print/PDF view + save-diff preview

**Status:** open
**Depends:** ψ.21 (PDF generation infrastructure)
**Unblocks:** editorial review meetings ("here's the matrix to
sign off on"); pre-save confidence ("show me what I'm about to
save")
**Effort:** ~1 session, LOW risk
**Files:** scripts/templates/matrix.py (Print + Save-Preview
buttons); scripts/web.py (api_matrix_print);
tests/test_scripts.py
**Cluster:** MATRIX-VIEW

Two related features bundled:

  1. **Print view**: render the current matrix state (or a
     selected scenario) as a polished PDF for editorial sign-off.
     Each edition gets its own page with full kind list,
     enabled/disabled flags, and counts. Reuses ψ.21
     PDF-generation infrastructure.
  2. **Save-diff preview**: before clicking Save, show a modal:
     "These N kinds will change. K kinds → K' kinds. Click
     Save Anyway or Cancel." Reduces "oops" rate at scale.

#### ψ.20 — Note-density heat-map on /matrix

**Status:** open
**Depends:** ψ.18 + ψ.18.1 (✓ shipped — `per_book` and `per_chapter`
already surface the data)
**Unblocks:** ω.16 (snapshots want a "what changed" view that this
heat-map's density delta will power)
**Effort:** ~1 session, LOW risk
**Files:** scripts/templates/matrix.py (new heat-map sub-panel);
tests/test_scripts.py
**Cluster:** MATRIX-SIDEBAR

Visual signal for corpus density gaps. At 51K notes some books are
dense (Genesis, Psalms, Matthew, John) and some are sparse
(Lamentations, Obadiah, Philemon, 2 John). Today the operator
inspects this with grep on disk; the heat-map turns it into a
glance.

Implementation: per-book × per-chapter coloured grid, palette
green→yellow→red on note count. Hover reveals exact counts. Stays
in the sidebar slot alongside the existing totals + chapter
drilldown panels.

#### ψ.21 — One-click sample export (5-chapter PDF)

**Status:** open
**Depends:** —
**Unblocks:** sales-call workflow ("send this prospect a sample")
**Effort:** ~1 session, LOW-MED risk
**Files:** new scripts/core/sample_pdf.py (PDF generation via
weasyprint or reportlab); scripts/web.py (api_sample_pdf endpoint);
scripts/templates/{customize,wizard,export}.py (Sample button);
tests/test_scripts.py
**Cluster:** PREVIEW

Sales-call affordance — prospects won't install a binary just to
peek at the apparatus. /export gets a "Sample PDF" button that
exports 5 chapters (Gen 1, Ps 1, Mat 1, Jhn 1, Rev 22 — chosen for
narrative + poetry + gospel + epistle + apocalyptic spread) with
full apparatus rendered as a polished PDF.

The PDF is brand-stamped (publisher imprint + ISBN) so a prospect
sees what the final retail product looks like at a glance.

Composes ψ.5 sample-chapter export (✓ shipped) + a PDF
serializer; doesn't build a full EPUB.

#### υ.3 — Search across editions in /sources

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session, LOW risk
**Files:** scripts/web.py (api_search_notes); scripts/templates/sources.py
(search input + result list); tests/test_scripts.py
**Cluster:** SOURCES (new sub-cluster within editor consoles)

Operators currently grep on disk to find notes. /sources adds a
search input that runs api_search_notes(query, edition_id?,
kind?, book?) and returns matches with verse + label + excerpt.
Composes notes_io.load_notes() with simple in-memory substring
matching; cached behind `_files_signature` like the matrix.

Cheap to ship; high productivity multiplier for daily editor work.

#### υ.8 — Verse-of-the-day JSON / RSS feed

**Status:** open
**Depends:** —
**Unblocks:** SEO funnel; church-website embedding
**Effort:** ~1 session, LOW risk
**Files:** scripts/web.py (api_verse_of_day, /api/verse-of-day.rss,
/api/verse-of-day.json); tests/test_scripts.py
**Cluster:** SOURCES

Marketing artifact: pick a verse + its top-rated note(s) by
deterministic-from-date hash, serve as JSON and RSS so external
sites can embed. Lightweight. Could eventually feed a daily
notification on the desktop launcher.

Pairs naturally with ψ.24 (devotional generator) — both pick
verses by calendar but with different output formats.

#### ψ.25 — "Diff between editions" view

**Status:** open
**Depends:** —
**Unblocks:** ω.16 snapshot diffing (which edition changed what
between v1.0 and v1.1)
**Effort:** ~1 session, LOW risk
**Files:** scripts/templates/diff.py (extend existing /diff console);
scripts/web.py (api_edition_diff); tests/test_scripts.py
**Cluster:** TEMPLATES

/diff currently compares git revisions of the corpus. Extend to
also compare two configured editions: "what's in catholic-study but
NOT in evangelical-reformed" — note count delta, kind delta,
canon delta. Powers retail decisions ("we already ship X; would
adding Y duplicate effort?").

### 5.2 MEDIUM TRACK

#### ψ.19 — Reading plans (chronological / one-year / M'Cheyne / lectionary)

**Status:** open
**Depends:** —
**Unblocks:** ψ.24 (devotional generator picks verses by reading
plan); ψ.7-B template starter packs that include a default reading
plan
**Effort:** ~1-2 sessions, LOW-MED risk
**Files:** new scripts/core/reading_plans.py (loader + iterator);
new content/reading_plans/*.yaml (5-7 declarative plans);
scripts/build_edition.py (toc integration); scripts/templates/customize.py
(Reading-plan card); tests/test_scripts.py
**Cluster:** EDITIONS

Every commercial study Bible has reading plans. Ours doesn't yet.
Declarative YAML format: `reading_plans/<id>.yaml` with `name`,
`description`, `entries: [{day: 1, verses: [...]}, ...]`. The
build pipeline emits an extra section in the EPUB ToC linking each
day to its passages.

Initial set:
  - **chronological** — events in approximate historical order
  - **one-year** — read the whole canon in 365 days
  - **mcheyne** — Robert Murray M'Cheyne's 1842 plan (OT once + NT
    + Psalms twice in a year)
  - **lectionary-rcl** — Revised Common Lectionary (Anglican / Lutheran)
  - **lectionary-roman** — Roman Catholic three-year lectionary
  - **monthly-psalms** — psalter through monthly
  - **biblical-feasts** — readings tied to Hebrew calendar feasts

Per-edition opt-in via `enabled_reading_plans: [...]` field on
the edition record. Default = empty (no behavioral change).

#### ω.16 — Edition snapshots (immutable v-tagged retail snapshots)

**Status:** open
**Depends:** ψ.7-B (templates need to be cloneable from snapshots
too); σ.3 (✓ git push) for the snapshot persistence layer
**Unblocks:** retail audit trail; reproducible v1.0 / v1.1 / v1.2
of any given edition
**Effort:** ~1-2 sessions, MED risk
**Files:** new scripts/core/snapshots.py (snapshot encode + decode);
new content/snapshots/<edition>/v<X>/*.yaml; scripts/web.py
(api_snapshot_create, api_snapshot_list, api_snapshot_diff,
api_snapshot_restore); scripts/templates/publisher.py (Snapshots
card); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Shipping v1.0 of an edition to retail needs an audit trail. ω.16
takes a frozen point-in-time of an edition (canon + kinds + theme +
reading plans + cover + ISBN + publisher meta + corpus hash) and
saves it as `content/snapshots/<edition_id>/v<semver>/`. Future
builds reference the snapshot, not the live edition.

Workflow: edit edition freely → "Snapshot v1.0" → live edition
keeps evolving while the v1.0 build remains reproducible. Cloning
a snapshot creates a new edition that forks from it (the natural
v1.1 development path).

Composes ψ.25 (edition diff) for the "what changed" view.

#### π.6 — Cover designer (text + gradient + font)

**Status:** open
**Depends:** π.4 (✓ shipped — covers system handles upload)
**Unblocks:** non-designer buyer onboarding
**Effort:** ~1-2 sessions, LOW risk
**Files:** new scripts/core/cover_design.py (PIL-based generator);
scripts/web.py (api_cover_generate); scripts/templates/covers.py
(Designer panel — title input, gradient pickers, font picker);
tests/test_scripts.py
**Cluster:** COVERS

π.4 covers handles upload of buyer-provided cover images. π.6
generates a cover from text input — title + subtitle + author +
gradient background (preset palettes) + font choice (PD fonts only).
Output drops into the same content/covers/ pipeline as uploads.

Fast onboarding for buyers without graphic-design skills. The
generated cover is a real PNG/JPEG file; nothing locks the buyer
in (they can replace it with an uploaded image at any time).

#### χ.10 — Geographic atlas integration (place-name → coordinates)

**Status:** open
**Depends:** —
**Unblocks:** atlas-aware navigation; map links in popups
**Effort:** ~1-2 sessions, MED risk
**Spec:** TBD (write alongside ship; PD source: openbible.info atlas)
**Files:** new scripts/core/atlas.py (place-name index + coord
lookup); new content/sources/biblical_atlas.json; new
scripts/core/detectors.py:PlaceNameDetector; scripts/run_atlas_at_scale.py;
tests/test_scripts.py
**Cluster:** ATLAS (new)

When a place is mentioned in a verse (Jerusalem, Jericho, Galilee,
etc.), tag the note with the place's coordinates. Apparatus popup
gains an optional "Map" link to OpenStreetMap centered on the
place + a 100km zoom radius.

Source: openbible.info publishes a PD-licensed Bible atlas dataset.
Detector mirrors χ.6 cross-ref pattern — emit a candidate per
recognized place name, batch promote.

Modern feature on a traditional study Bible. Subtle but
differentiating.

#### χ.11 — Calendrical / liturgical-year apparatus

**Status:** open
**Depends:** ψ.7-A (the new anglican-bcp / eastern-orthodox /
coptic-orthodox editions are this phase's natural consumers)
**Unblocks:** richer ψ.19 reading plans (lectionary phases need
calendar)
**Effort:** ~1-2 sessions, MED risk
**Files:** new scripts/core/calendar_liturgy.py; new
content/calendar/feasts.yaml (Roman, Byzantine, Coptic, Ethiopian,
Anglican); scripts/build_edition.py (calendar pass appends Feast
Day notes); tests/test_scripts.py
**Cluster:** LITURGICAL (new)

Marks feast days, fast days, and lectionary positions on relevant
verses. Each note is a `liturgy-*` kind tagged with the tradition.
The four new ψ.7-A editions (eastern-orthodox / anglican-bcp /
coptic-orthodox / lutheran-confessional) are the natural primary
consumers; existing catholic-study + evangelical-reformed get a
muted version.

Pairs with χ.10 atlas — the Holy Land calendar is partly
geographically-anchored (Mount Tabor / Mount Sinai feast days,
etc.).

#### ψ.24 — Daily devotional generator (verse + notes → 30-day PDF)

**Status:** open
**Depends:** ψ.21 (sample PDF infrastructure); ψ.19 (reading plans
provide the per-day verse picker)
**Unblocks:** marketing artifact ("free 30-day devotional with
every Bible")
**Effort:** ~1-2 sessions, LOW risk
**Files:** new scripts/core/devotional.py; scripts/web.py
(api_devotional_generate); scripts/templates/export.py (Devotional
button); tests/test_scripts.py
**Cluster:** PREVIEW

Picks 30 verses (configurable by reading plan or by category) +
top-rated notes per verse + an optional meditation prompt + an
optional verse-of-day theme. Outputs a polished PDF with the
publisher's imprint.

Sales / marketing artifact. Could be embedded in newsletter
funnels — "subscribe to get a free 30-day devotional".

#### τ.12 — Modern critical text (PD/CC-BY when available)

**Status:** open (gated on rights diligence; conservative timing)
**Depends:** τ.7 (Greek NT) + ξ.5 (dependency hygiene including
license tracking)
**Unblocks:** academic-edition depth
**Effort:** ~1 session per source (rights review is the long pole)
**Files:** content/translations/sources/<id>/; standard τ pattern
**Cluster:** TRANSLATIONS

Today the Greek apparatus uses Stephanus / Westcott-Hort (PD).
Modern critical text — Nestle-Aland 28th edition (NA28) — is
copyrighted (Deutsche Bibelgesellschaft). When/if a CC-BY
critical-text option becomes available (e.g. Tyndale House Greek
NT 2017 — currently free for non-commercial; commercial licensing
varies), add it as τ.12 with explicit rights documentation.

Adjacent option: SBLGNT (Logos's PD release) — could ship today
under SBL's terms of use review.

**Status:** open
**Depends:** ψ.7-B (the wizard's Start-from-template button is the
natural entry point for live preview); ψ.8 (✓ tradition stack
should render in preview)
**Unblocks:** the single biggest "wow" buyer moment — non-technical
users see the reading experience without a full build
**Effort:** ~2-3 sessions, MED risk
**Files:** new scripts/templates/_preview.py (iframe wrapper);
scripts/web.py (api_preview endpoint serving one rendered chapter);
scripts/templates/{customize,wizard}.py (iframe slot);
tests/test_scripts.py
**Cluster:** PREVIEW

Inline iframe in /customize and /wizard rendering one chapter as
the reader sees it. Live composition: theme + popup language +
popup translation + tradition stack. Updates on form changes
(debounced 300ms).

Implementation: api_preview(edition_id, book, chapter) calls a
trimmed build_one() with `render_only=True`, returns one HTML
page (no EPUB packaging, no ZIP). The iframe uses srcdoc= so no
new route serves dynamic content from disk.

Schedule once corpus is dense enough that previews aren't empty
(✓ at 51K notes the average chapter has 3-8 notes).

#### ρ.1 — LibriVox audio embed (infrastructure + first edition)

**Status:** open
**Depends:** —
**Unblocks:** ρ.2-5 (audio extensions)
**Effort:** ~1-2 sessions, MED risk
**Spec:** `dev/SCOPE_2026-05-08-addendum-audio-epubs.md`
**Files:** new scripts/core/audio.py (LibriVoxFetcher); new
content/audio/_sets.json; new scripts/fetch_librivox.py; build-pipeline
audio_embed pass in scripts/build_edition.py; scripts/templates/customize.py
(Audio card with size estimator); tests/test_scripts.py
**Cluster:** AUDIO

EPUB 3 native `<audio>` element per chapter. Buyer ticks a box on
/customize, picks a LibriVox voice, build pipeline embeds public-
domain audio. Single-file delivery: study Bible + audiobook in
one .epub.

**Tradeoff:** enabled EPUBs run 1-3 GB; opt-in per edition. The
Audio card on /customize includes a live size estimator that warns
above 500MB.

Sub-phasing per spec:
  - **ρ.1.0 + ρ.1.1**: scripts/core/audio.py + LibriVoxFetcher + _sets.json
  - **ρ.1.2 + ρ.1.3**: build-pipeline pass + audio_embed feature
  - **ρ.1.4**: /customize Audio card + size estimator
  - **ρ.1.5**: per-book overrides (encoder/decoder pair mirroring ν.2.7)

#### χ.2-5 — Commentaries batch

**Status:** open (4 phases bundled)
**Depends:** —
**Unblocks:** Tier B post-v1.0 corpus depth
**Effort:** ~1-2 sessions each
**Files:** scripts/core/sources.py + detectors.py; per-phase
scripts/run_<commentary>_at_scale.py; content/sources/*.json (cached
PD source data); content/notes/<book>.py (promoted notes)
**Cluster:** CORPUS

Each phase mirrors the established χ.6 / χ.7 / χ.1 χ-cluster
pipeline (see CLAUDE_PROJECT_RULES.md §9 "Add a new corpus-growth
phase").

| Phase | Source | Tradition | Expected delta |
|---|---|---|---|
| **χ.2** | Matthew Henry's Commentary | Protestant | ~5-15K notes |
| **χ.3** | Calvin's Commentaries | Reformed | ~5-10K |
| **χ.4** | Catena Aurea | Catholic | ~3-8K |
| **χ.5** | Rashi commentary | Jewish | ~3-7K |

**Risk:** parsing each source dump cleanly. Each yields `comm-*`
notes for a different denominational lens. Once ψ.8 is shipped (✓),
each ingest tags notes with the appropriate tradition automatically.

Execution order recommendation: χ.2 (Matthew Henry — most
voluminous) → χ.4 (Catena — needs Latin translation passes) → χ.3
(Calvin — Reformed) → χ.5 (Rashi — Hebrew script, niche audience).

#### τ.2-11 — PD translation expansion

**Status:** open (10 phases bundled by ingest path)
**Depends:** τ.1 infrastructure (✓ shipped)
**Unblocks:** —
**Effort:** ~1 session each (most); τ.6 Ge'ez = 2-3 sessions
**Spec:** `dev/SCOPE_2026-05-08-addendum-pd-translations.md`
**Files:** content/translations/<id>/sources/, _meta.yaml, .py per
translation
**Cluster:** TRANSLATIONS

Bundle by ingest path:

| Phase | Translation | Path | Synergy |
|---|---|---|---|
| **τ.2** | Douay-Rheims (1899) | ebible.org USFM | Catholic primary |
| **τ.3** | Latin Vulgate (Clementine) | ebible.org | Catholic + popup `latin` slot |
| **τ.4** | Brenton's Septuagint | ebible.org | Orthodox primary |
| **τ.5-A** | JPS 1917 | ebible.org | Jewish primary |
| **τ.5-B** | Westminster Leningrad Codex (Hebrew) | sefaria | Hebrew popup slot |
| **τ.6** | Ge'ez Ethiopian Tewahedo | custom (Unicode/font risk) | **Flagship's native language** |
| **τ.7** | Greek NT (Stephanus / WH) | various | Greek popup slot; χ.1 synergy |
| **τ.8** | Geneva Bible (1599) | ebible.org | Reformed historical |
| **τ.9** | ASV (1901) + YLT (1862) bundle | ebible.org | Academic English |
| **τ.10** | Reina-Valera 1909, Segond 1910, Luther 1545, Synodal 1876, Statenvertaling 1637 | ebible.org | Top-5 non-English audiences |
| **τ.11** | Wycliffe (1395) + Tyndale (1525-1530) | custom (Middle English) | Museum / educational niche |

Most are drop-in via the existing extract_translation.py registry
(τ.1 generalised it). τ.6 Ge'ez has Unicode rendering + font choice
+ sparser source data risk; budget 2-3 sessions.

### 5.3 LONG TRACK

#### θ.3.1 — Native binary auto-update integration

**Status:** open (data plane shipped as θ.3 ✓; native linking pending)
**Depends:** v1.0.0 (so an actual binary exists to update); Apple
Dev ID + Authenticode for signed appcast
**Unblocks:** v1.x silent-update cadence
**Effort:** ~1-2 sessions (mostly user-side after the wiring lands)
**Files:** dev/launcher.spec (Sparkle.framework Tree); scripts/launcher.py
(WinSparkle ctypes init)
**Cluster:** DESKTOP-UPDATE

Wire Sparkle (macOS) and WinSparkle (Windows) into the binary build
chain. Lighter-weight fallback already implemented at infrastructure
level: launcher imports scripts.core.updates, calls fetch_appcast on
startup, surfaces "update available" toast via PyWebView. Native
framework integration is the polished version.

[USER] half: needs binary build pipeline + signing certs in place
before Sparkle's signature verification works.

#### ρ.2-5 — Audio extensions

**Status:** open (4 phases bundled)
**Depends:** ρ.1
**Unblocks:** —
**Effort:** ~1-2 sessions each
**Files:** scripts/core/audio.py extensions; build_edition.py audio
pipeline extensions; scripts/fetch_*.py per audio source
**Cluster:** AUDIO

  - **ρ.2** SMIL synchronized text+audio (highlight-along-with-narration;
    depends on forced-alignment via aeneas)
  - **ρ.3** TTS fallback for PD-audio-sparse books (Enoch, Jubilees,
    Meqabyan, Tewahedo-only material)
  - **ρ.4** Multi-translation audio (audio swaps when popup translation
    toggles)
  - **ρ.5** Per-language audio sets (Spanish, French, etc.) as source
    recordings emerge in LibriVox

ρ.1 alone is sufficient for v1.0+audio; ρ.2-5 are v1.x deepening.

#### ψ.7-C — Edition template marketplace

**Status:** open (speculative; not yet specced)
**Depends:** ψ.7-A + ψ.7-B (✓ once they ship)
**Unblocks:** community contributions
**Effort:** ~2-3 sessions
**Files:** TBD (new console / API / template-import format)
**Cluster:** EDITIONS

Speculative v1.x-Y addition: let users export their custom edition
as a template YAML and share / re-import it. Could become a
GitHub-hosted "edition gallery" with one-click import in the
wizard. Defer scoping until ψ.7-A + ψ.7-B prove the format.

#### χ-AI-notes — AI-augmented note generation (build-time)

**Status:** open (cost-gated, mirrors χ-AI-xrefs)
**Depends:** χ-AI-xrefs (◐ infra shipped) — same Anthropic SDK
plumbing + cost-confirmation guards + caching infrastructure
**Unblocks:** filling sparse-corpus verses for buyer demos
**Effort:** ~2-3 sessions
**Spec:** to write — `dev/SCOPE_2026-05-09-addendum-ai-notes.md`
(mirror of `dev/SCOPE_2026-05-08-addendum-ai-xrefs.md`)
**Files:** scripts/core/sources.py:AnthropicNoteClient; scripts/core/detectors.py:AINoteDetector;
new scripts/run_ai_notes_at_scale.py; tests/test_scripts.py
**Cluster:** CORPUS

Extends the χ-AI-xrefs investment from corpus-time xref proposing
to build-time per-edition note drafting. Publisher with a sparse
verse picks "Generate AI draft note" → first draft appears with
explicit `[Reviewer: AI-generated]` attribution flag. Reviewer
edits; on approve, note enters the corpus tagged with the AI
attribution.

**Cost model:** mirrors χ-AI-xrefs — Haiku 4.5 with 5K-token
cached system prompt, ~$0.002/verse projected. Per-edition opt-in
gated behind `--confirm-cost` ≥ 200 verses.

**Distinct from χ-AI-xrefs:** xrefs propose links between
*existing* verses; AI-notes propose *new note text* for a verse.
Different prompt, different output schema, different reviewer
workflow.

#### ψ.22 — Multi-format export (PDF / MOBI / HTML / TXT alongside EPUB)

**Status:** open
**Depends:** ψ.17 (✓ shipped — reader-EPUB polish provides the
typographic baseline that PDF reuses)
**Unblocks:** Kindle/KDP submission; print prep; accessibility +
academic distribution
**Effort:** ~2-3 sessions (each format = ~0.5-1 session)
**Files:** new scripts/core/export_pdf.py (weasyprint); new
scripts/core/export_mobi.py (calibre-cli wrapper); new
scripts/core/export_plain.py (HTML + TXT serializers); scripts/build_edition.py
(format dispatch); scripts/templates/export.py (format picker);
tests/test_scripts.py
**Cluster:** BUILD-FORMATS (new)

Currently EPUB-only. Add:
  - **PDF** — print-ready (via weasyprint); buyer needs this for
    KDP Print + IngramSpark. Reuses the @page rules ψ.17 added.
  - **MOBI / AZW3** — Kindle / KDP digital. Wraps Calibre's
    `ebook-convert` CLI as a subprocess; dependency is documented
    + warned-on-missing.
  - **HTML** — single-file accessibility-friendly export; useful
    for academic citation + screen-reader workflows.
  - **TXT** — plain UTF-8 with verse markers for grep-friendly
    workflows; useful for Bible-software import paths (Logos /
    Accordance / e-Sword).

Per-edition opt-in via `enabled_formats: [...]` field. Default =
`[epub]` (no behavioral change).

#### ψ.23 — Reverse-interlinear popups (Hebrew/Greek word-by-word with English)

**Status:** open
**Depends:** τ.5-B (Hebrew WLC) + τ.7 (Greek NT) + word-aligned
source data (e.g. Berean Interlinear Bible — PD)
**Unblocks:** academic / language-study buyer market
**Effort:** ~2-3 sessions
**Files:** scripts/core/interlinear.py (alignment loader);
content/translations/<id>/_alignment.py (per-verse word index);
scripts/build_edition.py (popup pass enriches Hebrew/Greek popups
with English alignment); tests/test_scripts.py
**Cluster:** PREVIEW

The Hebrew/Greek popups already display source text + Strong's
linkage. ψ.23 adds word-by-word English alignment so a reader
hovering on "אַהֲבָה" (ahavah) sees both the Hebrew word and its
English translation aligned in the popup.

Source data: Berean Interlinear Bible (BIB) is PD with explicit
word-alignment data. Other PD options: Open English Bible (OEB)
has light alignment.

This is the academic / seminary market's expected feature.

#### θ.5 — Localized UI (Spanish / Portuguese / French / German)

**Status:** open (lifted from "indefinitely deferred" 2026-05-09 per
user direction — was in CLAUDE_PROJECT_RULES §10 "NOT a multi-
language UI")
**Depends:** τ.10 (non-English translations land first so the
buyer demo has Reina-Valera + Segond + Luther backing the
localized UI shells); ξ.5 dependency hygiene (i18n library pick)
**Unblocks:** Spanish / Portuguese / French / German imprints;
expands buyer market beyond English-speaking
**Effort:** ~2-3 sessions per language (Spanish first; others ride
the same scaffold)
**Files:** new scripts/core/i18n.py (string lookup); new
content/i18n/<lang>.yaml (one per language); scripts/templates/_design.py
+ all 13 console templates (string substitution at module load
similar to ψ.13 HEADER_NAV_LINKS pattern); tests/test_scripts.py
**Cluster:** I18N (new)

§10 of the rules doc previously said: "Not a multi-language UI...
Bible content in many languages is the whole point; interface in
many languages is out of scope."

Lifted 2026-05-09 conditional on a real buyer ask. The architecture
is already i18n-friendly (templates use `_design.py` constants;
substitution at module load already exists for nav). Adding a
string-lookup layer that resolves `_("Save")` against
`content/i18n/<lang>.yaml` is a moderate refactor + per-template
sweep.

Order of priority by buyer-market size: Spanish → Portuguese →
French → German. Italian / Russian / Mandarin are post-cloud.

**This phase requires the rules doc §10 to be updated** (lift the
"interface in many languages is out of scope" stance).

### 5.4 HARDENING TRACK

Audit-style phases. Run anytime; not blocking other work.

#### ξ.3 — CSP headers

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session, LOW risk
**Files:** scripts/web.py (response headers); tests/test_scripts.py
**Cluster:** SECURITY

Add Content-Security-Policy headers to every console response.
Default-src 'self'; script-src 'self' 'unsafe-inline'
https://cdn.tailwindcss.com; frame-ancestors 'none'. Tests verify
the headers and that the policy doesn't break Tailwind CDN.

#### ξ.5 — Dependency hygiene

**Status:** open
**Depends:** —
**Unblocks:** v1.0+ supply-chain confidence
**Effort:** ~0.5 session
**Files:** new requirements.txt; new SECURITY.md; pyproject.toml
**Cluster:** SECURITY

Pin every Python dependency. Add SECURITY.md with disclosure
process. Add gitleaks pre-commit hook (alongside lint_rules).

#### ξ.6 — Secrets management

**Status:** open
**Depends:** ξ.5 (gitleaks hook)
**Unblocks:** —
**Effort:** ~0.5 session
**Files:** new .env.example; .gitignore additions; docs in
SECURITY.md
**Cluster:** SECURITY

Document every env-var surface (ANTHROPIC_API_KEY for χ-AI-xrefs,
YHWH_CONTENT_ROOT for ω.5, CODESIGN_IDENTITY for θ.4) in
.env.example. Confirm none are committed. Wire gitleaks scan into
the lint_rules pre-commit hook.

#### ξ.7 — Auth-gate re-evaluation

**Status:** parked (ω.4 was deferred for the same reason)
**Depends:** —
**Unblocks:** multi-user / cloud / reviewer-roles
**Effort:** ~1-2 sessions if reactivated
**Files:** scripts/web.py (mutation routes); new scripts/core/auth.py
**Cluster:** SECURITY

Single-user desktop has no multi-user threat model. Reactivate when
the cloud / reviewer-roles roadmap lands (post-v1.x).

#### ω.11 — Recovery doc + helpers

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session
**Files:** new dev/RECOVERY.md; new scripts/recover.py CLI
**Cluster:** ROBUSTNESS

Document every "I broke something — how do I fix it?" path:
content/notes/ corruption, IN_FLIGHT marker stuck, build pipeline
hangs, lint_rules.py false positives. Wraps existing
notes_io.ensure_backup + atomic_write + git checkout helpers.

#### ω.12 — Crash-safe state (lock files + journals)

**Status:** open
**Depends:** —
**Unblocks:** crash-resilient long-running tasks (χ-AI-xrefs paid
runs, ρ.1 audio fetches)
**Effort:** ~1 session
**Files:** new scripts/core/crashsafe.py; scripts/web.py mutation
routes adopt; tests/test_scripts.py
**Cluster:** ROBUSTNESS

Mutation routes that take >1s acquire a lock file in user_data/
locks/ and journal their progress. Crash recovery on next launcher
boot offers to roll forward / roll back.

#### ω.13 — Performance budgets

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session
**Files:** new tests/test_perf.py; new scripts/perf_budgets.py
**Cluster:** ROBUSTNESS

Pin perf budgets per route: api_matrix < 500ms, /export build per
edition < 90s, /preview render < 1s, full-suite pytest < 90s.
Tests timeout on regression. Budgets re-tuned per release.

#### ξ.8 — Rate limiting on API endpoints

**Status:** open (forward-looking; single-user desktop has no
need today, but cloud era / shared deployment makes it
load-bearing)
**Depends:** —
**Unblocks:** safe cloud deployment; multi-tenant if pursued
**Effort:** ~1 session, LOW risk
**Files:** scripts/web.py (token-bucket middleware); new
scripts/core/rate_limit.py; tests/test_scripts.py
**Cluster:** SECURITY

Per-IP token bucket on `/api/*` mutation endpoints. Default 60
req/min per IP. Configurable via `YHWH_RATE_LIMIT` env var.
Returns 429 + Retry-After on quota exceed. No-op when running
as a localhost-only desktop binary (default).

#### ξ.9 — Subresource integrity for Tailwind CDN

**Status:** open
**Depends:** —
**Unblocks:** CDN-compromise resilience
**Effort:** ~0.5 session, LOW risk
**Files:** scripts/templates/_design.py (SRI hash constant); all
13 console templates (replace bare `<script src="...">` with
`<script src="..." integrity="sha384-..." crossorigin>`); new
scripts/check_sri.py CLI to recompute hashes when CDN version
bumps; tests/test_scripts.py
**Cluster:** SECURITY

Bare `<script src="https://cdn.tailwindcss.com">` doesn't verify
the response. SRI hash lets the browser refuse to execute if
the CDN serves anything else. CDN compromise (rare but real) is
the threat model.

#### ξ.10 — SSRF / outbound URL allowlist

**Status:** open
**Depends:** ω.10 (✓ shipped — scripts.core.http.get is the
single egress point)
**Unblocks:** safer fetch_sources / appcast / future LLM calls
**Effort:** ~1 session, LOW risk
**Files:** scripts/core/http.py (allowlist parameter on get());
scripts/fetch_sources.py + scripts/core/sources.py + scripts/core/updates.py
(declare allowed domains); tests/test_scripts.py
**Cluster:** SECURITY

Today scripts.core.http.get accepts any URL. SSRF defence: pass
an allowlist of trusted domains per call site. Calls that don't
declare an allowlist warn (loudly) and continue (back-compat).
Future cloud-era versions tighten to fail-closed.

Allowed domains today: openscriptures.org, ebible.org,
archive.org, openbible.info, raw.githubusercontent.com (for χ
sources), api.anthropic.com (χ-AI-* clients).

#### ξ.11 — Dependency vulnerability scan (pip-audit)

**Status:** open
**Depends:** ξ.5 (deps hygiene — needs requirements.txt to scan)
**Unblocks:** continuous CVE awareness
**Effort:** ~0.5 session, LOW risk
**Files:** new scripts/audit_deps.py (wraps pip-audit); pre-commit
hook integration; tests/test_scripts.py (smoke that the wrapper
exits cleanly when no vulns)
**Cluster:** SECURITY

`pip-audit` against requirements.txt; fails the pre-commit hook
on any HIGH/CRITICAL CVE. Documented allowlist for known-
acceptable transitives via .audit-waivers.yaml.

#### ξ.12 — SAST static analysis (bandit)

**Status:** open
**Depends:** —
**Unblocks:** automated catch of common Python security
anti-patterns (eval, pickle, hardcoded passwords, weak crypto,
etc.)
**Effort:** ~0.5 session, LOW risk
**Files:** new scripts/audit_sast.py (wraps bandit); pre-commit
integration; tests/test_scripts.py
**Cluster:** SECURITY

Bandit configured to scan scripts/ and tests/. Initial run will
likely flag legitimate uses (ast.literal_eval, subprocess in
build pipeline) — those get an explicit `# nosec: <reason>`
comment per line. Net effect: any NEW unsafe pattern fails CI.

#### ξ.13 — Audit log for mutations

**Status:** open
**Depends:** ω.9 (✓ shipped — atomic writes provide the natural
hook point)
**Unblocks:** retail-grade audit trail; compliance posture for
publishers in regulated industries
**Effort:** ~1 session, LOW risk
**Files:** new scripts/core/audit_log.py (append-only NDJSON in
user_data_dir/audit/); scripts/web.py (decorator on all
api_save_* / api_create_* / api_delete_* routes); tests/test_scripts.py
**Cluster:** SECURITY

Every mutation route appends one line to
`<user_data>/audit/<YYYY-MM>.ndjson`: timestamp + endpoint +
user (placeholder for now since single-user) + before/after diff
hash + result. Read-only API surfaces an /api/audit-log endpoint
+ /audit-log console (extension of existing /audit).

Pairs with ω.16 (edition snapshots) — snapshot creation events
are first-class audit-log entries.

#### ξ.14 — OS keychain for ANTHROPIC_API_KEY

**Status:** open
**Depends:** —
**Unblocks:** secret hygiene for desktop binary distributions
**Effort:** ~1 session, MED risk (cross-platform keychain APIs)
**Files:** new scripts/core/secret_store.py (keyring library
wrapper); scripts/core/sources.py (AnthropicXrefClient +
AnthropicNoteClient read from keychain first, fall back to env
var); tests/test_scripts.py
**Cluster:** SECURITY

Today ANTHROPIC_API_KEY is read from env var only. ψ.14 adds OS
keychain (macOS Keychain / Windows Credential Manager / Linux
Secret Service) via the `keyring` library. Env var still works as
fallback. First-launch flow prompts to save the key in the
keychain after a one-time CLI prompt.

Reduces "I accidentally committed my .env" risk class.

#### ξ.15 — AI-generated content sandboxing

**Status:** open
**Depends:** χ-AI-notes (◯ open) — this phase is its safety
companion
**Unblocks:** safe χ-AI-notes ship
**Effort:** ~1 session, LOW-MED risk
**Files:** new scripts/core/html_sandbox.py (allowlist HTML
sanitizer); scripts/core/detectors.py:AINoteDetector (run
sandbox on output); tests/test_scripts.py covering common XSS
payload classes the AI could emit
**Cluster:** SECURITY

When χ-AI-notes ships, the LLM emits prose for note bodies. ξ.15
sandboxes that output through a strict allowlist (only `<em>`,
`<strong>`, `<a href="...">` with same-origin checks, no
`<script>` ever). Mirrors ξ.4 XSS prevention but specifically for
AI-generated content where the threat model is "the model
hallucinates an unsafe construct" rather than "publisher pastes
malicious HTML".

#### ω.17 — Crash reporting (Sentry-style, opt-in for desktop binary)

**Status:** open
**Depends:** v1.0.0 (need a binary in the wild before reports help)
**Unblocks:** real-world bug visibility
**Effort:** ~1 session, LOW risk
**Files:** new scripts/core/crash_report.py (event submitter);
scripts/launcher.py (opt-in toggle on first run); dev/PRIVACY.md
(crash-report disclosure text)
**Cluster:** ROBUSTNESS

Opt-in crash telemetry for the desktop binary. Captures Python
traceback + OS + Python version + binary build version + (no PII,
no content/ data, no API keys). Pings a self-hosted endpoint or
Sentry if user prefers.

First-launch dialog asks: "Help us improve YHWH by sending
anonymous crash reports? [Yes / No]". Defaults to No. Toggleable in
the desktop launcher's About menu.

Ships with explicit privacy doc in dev/PRIVACY.md.

#### ω.18 — Lint auto-fix mode

**Status:** open
**Depends:** ω.0.1 (✓ shipped — lint_rules.py); ω.15 (✓ shipped
— lint_plan.py)
**Unblocks:** zero-friction drift correction
**Effort:** ~1 session, LOW risk
**Files:** scripts/lint_rules.py + scripts/lint_plan.py (add
`--fix` flag); per-check fixer functions; tests/test_scripts.py
**Cluster:** ROBUSTNESS

`--fix` flag that auto-corrects safe drift:
  - Cross-link missing → insert HEADER_NAV_LINKS marker
  - Encoder canonical-order drift → re-sort
  - SESSION_STATE freshness drift → bump the date stamp
  - Plan-open phase appears shipped → move it to the shipped
    block in §7 ledger
  - Doc cross-reference missing → write a stub addendum

Refuses to fix anything that's structurally ambiguous (untracked
phases, atomic-write violations, plan_depends graph errors) —
those still need human review.

#### ω.19 — Schema validator CLI

**Status:** open
**Depends:** —
**Unblocks:** catch YAML drift before it reaches build
**Effort:** ~1 session, LOW risk
**Files:** new scripts/validate_schemas.py (single CLI for every
YAML); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Schema-validate every YAML against an explicit Pydantic-style
spec: editions.yaml, canons.yaml, kinds.yaml, categories.yaml,
themes.yaml, content/translations/*/_meta.yaml,
content/audio/_sets.json, content/edition_templates/*.yaml,
content/matrix_scenarios/*.yaml, content/reading_plans/*.yaml,
content/calendar/feasts.yaml. Exits non-zero on any violation;
diagnostic reads which file + which field + what was expected.

Composed into pre-commit so a malformed YAML can't reach main.

#### ω.20 — Build cache / incremental rebuild

**Status:** open
**Depends:** —
**Unblocks:** fast iteration on multi-edition builds; ψ.1 live
preview at scale
**Effort:** ~1-2 sessions, MED risk
**Files:** new scripts/core/build_cache.py (hash-based
invalidation); scripts/build_edition.py (skip if cached); new
exports/.cache/ directory; tests/test_scripts.py
**Cluster:** ROBUSTNESS

Cache key: hash(editions.yaml entry + canons.yaml entry + every
note file in canon + kinds.yaml + every translation file
referenced + theme YAML + cover_image bytes). Build skips when
cache hit. Save: 30-90s per untouched edition on
api_build_all_editions; from ~7min → seconds for the common
"only one edition changed" case.

Cache is content-addressable + idempotent; safe to delete to
force rebuild.

#### ω.21 — Watch mode

**Status:** open
**Depends:** ω.20 (build cache makes watch mode bearable)
**Unblocks:** dev-loop iteration speed
**Effort:** ~0.5 session, LOW risk
**Files:** new dev/watch.py (watchdog-based file watcher);
tests/test_scripts.py
**Cluster:** ROBUSTNESS

`python dev/watch.py` watches content/notes, content/translations,
editions.yaml, kinds.yaml, scripts/templates/. On change:
re-runs lint_rules.py and (optionally) rebuilds the active
edition via ω.20's incremental machinery. Notifies via terminal
bell + (on macOS) AppKit notification.

Optional, dev-only; no impact on production.

#### ω.22 — Migration scripts framework

**Status:** open
**Depends:** —
**Unblocks:** safe schema evolution; reproducible upgrades
**Effort:** ~1 session, LOW risk
**Files:** new scripts/migrate.py (versioned migration runner);
new scripts/migrations/<NNNN>_<name>.py per migration; new
content/.migration_state.yaml (ledger of applied); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Formal migration framework: each migration declares
`from_version` / `to_version` / `up()` / `down()` (idempotent +
reversible). The runner reads `.migration_state.yaml`, applies
pending migrations in order, updates state. Backs up via
notes_io.ensure_backup before any write.

Backfills the existing scripts/migrate_to_user_data.py (✓ shipped)
+ scripts/backfill_traditions.py (✓ shipped) into the framework
retroactively as migrations 0001 + 0002.

#### ω.23 — Lint perf profile

**Status:** open
**Depends:** ω.0.1 (✓ shipped) + ω.15 (✓ shipped)
**Unblocks:** keep lint runtime in check as checks grow
**Effort:** ~0.5 session, LOW risk
**Files:** scripts/lint_rules.py + scripts/lint_plan.py
(`--profile` flag printing per-check timing); tests/test_scripts.py
**Cluster:** ROBUSTNESS

`lint_rules.py --profile` reports per-check time + memory + I/O
delta. Helps notice when a new check (e.g. a future ω.20 build-
cache audit) becomes the bottleneck.

Tied to ω.13 (perf budgets) — lint_rules.py has its own perf
budget assertion in test_perf.py once that lands.

#### ω.24 — Interactive prospect REPL

**Status:** open
**Depends:** —
**Unblocks:** non-CLI-power-users contributing notes
**Effort:** ~1 session, LOW risk
**Files:** new scripts/prospect_repl.py (interactive Q&A wizard);
tests/test_scripts.py
**Cluster:** ROBUSTNESS

CLI tool: `python scripts/prospect_repl.py` walks user through
"what book? chapter? verse? kind? label? body?" and creates a
candidate JSON in the project's standard format. Wraps existing
notes_io machinery; gracefully handles abort + resume.

Companion to /sources upload-pre-built-JSON path for users who
prefer terminal over web UI.

#### ω.25 — Bulk rename / refactor tool

**Status:** open
**Depends:** ω.22 (migration framework — bulk renames are a
form of migration)
**Unblocks:** fearless kind-code or category-id refactoring
**Effort:** ~1 session, LOW-MED risk
**Files:** new scripts/refactor.py (atomic rename across
content/ + kinds.yaml + editions.yaml); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Atomic rename of a kind code or category id everywhere it
appears: content/notes/*.py tuples, kinds.yaml entries,
editions.yaml enabled_kinds / disabled_kinds, content/categories.yaml,
content/edition_templates/*.yaml. All-or-nothing via
notes_io.ensure_backup + atomic_write. Generates a migration as
a side-effect (rolls into ω.22 ledger).

Use case: "I shipped kind `comm-evangelical` but want to rename
to `comm-evangelical-broad`". Today this is hand-grep-and-replace
across 87 files. ω.25 makes it one command + tests.

#### ω.26 — Dead code removal sweep

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~1 session, LOW-MED risk
**Files:** new scripts/audit_dead_code.py (wraps vulture +
unused-imports linter); cleanup PR after audit
**Cluster:** ROBUSTNESS

Run vulture + unimports across scripts/ + tests/. Triage:
genuinely-dead → delete; intentionally-public-but-unused →
explicit `# noqa: dead` comment; legitimately-used-but-detected →
fix detector or whitelist. Audit PR plus a recurring lint
addition that flags new dead code.

#### ω.27 — Test fixture consolidation

**Status:** open
**Depends:** —
**Unblocks:** test-suite navigability (today tests/test_scripts.py
is 13K+ lines)
**Effort:** ~1-2 sessions, MED risk (mass file-move with risk
of import drift)
**Files:** split tests/test_scripts.py into tests/test_<area>.py
(test_matrix.py, test_editions.py, test_translations.py,
test_audit.py, test_publisher.py, test_covers.py, test_wizard.py,
test_export.py, test_compare.py, test_diff.py, test_preflight.py,
test_ops.py, test_apihelp.py, test_psi_cluster.py,
test_omega_cluster.py, test_chi_cluster.py, test_xi_cluster.py)
**Cluster:** ROBUSTNESS

Mechanical refactor: move test classes into per-area files.
Imports stay class-level; no test renaming, no test deletion.
Pre-/-post sanity: same test count, same pass/fail counts.

Risk mitigation: do it in 4-5 PRs, each moving a coherent
sub-area at a time. The §6.2 cross-link linter et al.
themselves get tests in tests/test_lint.py.

#### ω.28 — Backup retention policy

**Status:** open
**Depends:** —
**Unblocks:** disk-usage predictability
**Effort:** ~0.5 session, LOW risk
**Files:** scripts/cleanup.py (extend); new
content/.backup_retention.yaml (configurable per-category
retention windows); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Today cleanup.py prunes backups keeping 5 revisions per stem.
Make it configurable per category:
  - `content/notes/*.py` backups: keep 10 revisions
  - `editions.yaml` backups: keep 30 days
  - `epub_working/.backups/`: keep 3 revisions
  - `exports/`: keep 2 builds per edition
  - `content/candidates/`: keep last completed run

Backed by `content/.backup_retention.yaml`; defaults preserve
current behavior.

#### ω.29 — Content directory health checker

**Status:** open
**Depends:** ω.19 (schema validator)
**Unblocks:** continuous content-integrity awareness
**Effort:** ~1 session, LOW risk
**Files:** new scripts/check_content.py; lint_rules.py composes
the check; tests/test_scripts.py
**Cluster:** ROBUSTNESS

Wholesale content/ audit: every notes/*.py parses cleanly via
ast.literal_eval (no exec); every translations/*/_meta.yaml is
valid against schema; every cover_image referenced in editions
exists on disk; every candidate JSON is well-formed; no orphan
files in content/notes/ that aren't in books.yaml. Composed into
preflight + a separate `content_health` check.

#### ω.30 — Cache invalidation audit

**Status:** open
**Depends:** —
**Unblocks:** correctness guarantee on stale data
**Effort:** ~0.5 session, LOW risk
**Files:** new scripts/audit_caches.py (grep all `@lru_cache`
+ trace clear() paths); tests/test_scripts.py
**Cluster:** ROBUSTNESS

Every `@lru_cache` in scripts/ should have a documented clear
path (in compute_matrix, in api_save_*, etc.) and the cache key
should include the file mtime where applicable. Audit reports
caches without clear paths or with weak keys. Cache-related bugs
have a long tail; this is the systemic catcher.

#### ω.31 — Type-checking sweep (mypy or pyright)

**Status:** open
**Depends:** —
**Unblocks:** catch class of bugs the test suite misses
**Effort:** ~1-2 sessions, MED risk (mass annotation + a few
genuine fixes will surface)
**Files:** new mypy.ini / pyrightconfig.json; type annotations
sweep across scripts/core/ and scripts/web.py route adapters;
new scripts/audit_types.py wrapper; tests/test_scripts.py
**Cluster:** ROBUSTNESS

Initial pass with strict-optional + no-untyped-defs. The §9
"pure function + thin route adapter" pattern + the §9 "encoder/
decoder pair" pattern both already type-friendly; new annotations
mostly land on web.py request handlers + match the dict-return
contract.

Fail the pre-commit hook on any new type error in changed files.

#### ω.32 — Docstring coverage

**Status:** open
**Depends:** ω.31
**Unblocks:** —
**Effort:** ~0.5 session, LOW risk
**Files:** new scripts/audit_docstrings.py (wraps interrogate);
docstring sweep on under-documented public functions;
tests/test_scripts.py
**Cluster:** ROBUSTNESS

Pin a minimum docstring coverage % (e.g. 80) on scripts/core/
+ scripts/web.py public functions. Existing project convention
already strong here; this just enforces it.

#### ω.33 — Format consistency (ruff format)

**Status:** open
**Depends:** —
**Unblocks:** —
**Effort:** ~0.5 session, LOW risk (auto-format diff is large
but mechanical)
**Files:** new pyproject.toml [tool.ruff] config; one-shot
format pass; pre-commit hook integration; tests/test_scripts.py
**Cluster:** ROBUSTNESS

Ruff for formatting + linting (replaces black + isort + flake8
in one tool). Single one-shot format pass for the existing
codebase; pre-commit then keeps it consistent.

Risk: the initial diff is large (every file touched). Ship as a
dedicated PR with ZERO logic changes; use `git blame` config
to ignore-revs for that commit so blame stays meaningful.

### 5.5 USER-SIDE TRACK

These don't compete for my session bandwidth. The user runs them
externally; I update SESSION_STATE / corpus counts when they report
results.

| Item | Cost | Outcome | Priority |
|---|---|---|---|
| **AI xrefs paid run** | ~$72 | +5K xref-thematic notes, depth | HIGH (closes the typological / idiomatic gap) |
| **τ.1 WEB user-side fetch** | $0 | +31K verses, modern PD English | MED |
| **Sparkle/WinSparkle linking** | $0 (build infra) | silent auto-update | MED (post-v1.0) |
| **Apple Dev ID** | $99/yr | macOS Gatekeeper passes | LOW (only for signed distribution) |
| **Authenticode cert** | $200-400/yr | Windows SmartScreen passes | LOW (only for signed distribution) |
| **Visual QA passes** (ψ.14/15/16/17) | $0 | sign-off or filed tweaks | HIGH (gates v1.0 ship) |
| **EPUB-reader QA** (Apple Books / Calibre / Kobo / Kindle) | $0 | reader-experience verification | HIGH (gates v1.0 ship) |

### 5.6 PARKED — design calls

Each requires a decision before implementing. They're real, but
small enough that the right answer isn't obvious.

  - **min-confidence calibration mismatch** — at-scale drivers
    default `--min-confidence 0.7`; GreekWordDetector + HebrewWordDetector
    emit at 0.65 floor. Tests pin per-book values. Choosing a side
    shifts the corpus. Resolve when next χ-cluster phase needs to
    re-run at scale.
  - **cleanup.py expansion** — should also prune content/candidates/
    (~1,355 files growing). Currently prunes exports/, epub_working/,
    builds/. Quick to extend; gate is "what's the right retention
    window for un-promoted candidates?"
  - **scaffolder integration test** — running `--apply` against a
    temp dir to catch indent-error class bugs. Worth doing; one
    missing tmp-dir pattern from §9 mental models.
  - **per-chapter as 4th matrix dim** — ψ.18.1 stops at top-5 books
    per kind. Full chapter detail across all books = ~50-100K
    matrix entries. ψ.18.2 above is the user-gated-expand path; a
    fully-loaded fourth dim is a different scope.
  - **multi-user / reviewer roles** — explicitly out of scope until
    cloud-hosted. Reactivate post-v1.x.

### 5.7 INDEFINITELY DEFERRED — with rationale

These will never reach the OPEN list without a scope shift:

  - **ω.4 auth gate on mutations** — single-user desktop has no
    multi-user threat
  - **ψ.5.1 sample-chapter PDF export** — desktop live preview
    obviates
  - **ψ.4 in-browser epubcheck (WASM)** — Java tool inside Pyodide
    is a research project unto itself
  - **φ.7 pre-built EPUB cache** — less leverage in user-data-dir
    model; revisit if cloud era
  - **χ.8 BDB / LSJ lexicons** — digital editions copyright varies;
    license diligence
  - **AI-augmented in-app generation** — the AI xref *detector* is
    a one-time corpus enrichment (χ-AI-xrefs); making the *runtime*
    AI-augmented is a different product

---

## 6. Pre-session ordering — what to ship next, by budget

| If session bandwidth is | And user wants | Ship |
|---|---|---|
| 1 session, low risk | buyer-demo depth | ψ.7-A (4 built-in editions) |
| 1 session, low risk | wizard polish | ψ.7-B (templates) |
| 1 session, low risk | finish design system | ψ.16 (5 status consoles) |
| 1 session, micro polish | UX consistency | ν.2.8 + ψ.11 duo |
| 1 session, structural | template author DX | ψ.13.5 (f-string sweep) |
| 1 session, deep | matrix completeness | ψ.18.2 (chapter expand-all) |
| 1 session, visual | corpus density gaps | ψ.20 (note-density heat-map) |
| 1 session, sales | prospect demo affordance | ψ.21 (sample PDF export) |
| 1 session, productivity | operator search | υ.3 (search-across-editions) |
| 1 session, marketing | external embed | υ.8 (verse-of-day feed) |
| 1 session, retail | edition comparison | ψ.25 (edition diff) |
| 2-3 sessions, big-bet | the "wow" demo upgrade | ψ.1 (live EPUB preview) |
| 1-2 sessions, audio | next biggest differentiator | ρ.1 (LibriVox) |
| 1-2 sessions, corpus | tradition depth | χ.2 (Matthew Henry) |
| 1 session, breadth | translation breadth | τ.2 (Douay-Rheims) |
| 1-2 sessions, devotion | reading-plan apparatus | ψ.19 (reading plans) |
| 1-2 sessions, retail | audit-trail snapshots | ω.16 (edition snapshots) |
| 1-2 sessions, onboarding | non-designer cover | π.6 (cover designer) |
| 1-2 sessions, atlas | geographic apparatus | χ.10 (place-name → coords) |
| 1-2 sessions, calendar | liturgical-year apparatus | χ.11 (feast days) |
| 1-2 sessions, marketing | devotional artifact | ψ.24 (30-day devotional PDF) |
| 1 session, academic | modern critical text | τ.12 (NA28 / SBLGNT) |
| 2-3 sessions, AI | sparse-verse note drafting | χ-AI-notes (build-time AI notes) |
| 2-3 sessions, formats | KDP / print delivery | ψ.22 (PDF + MOBI + HTML + TXT) |
| 2-3 sessions, academic | word-by-word popups | ψ.23 (reverse interlinear) |
| 2-3 sessions, market | non-English imprints | θ.5 (localized UI; lifts §10) |
| 0.5 session, audit | security carry-over | ξ.3 / ξ.5 / ξ.6 |
| 0.5 session, audit | robustness carry-over | ω.11 / ω.12 / ω.13 |
| 0.5 session, audit | crash visibility | ω.17 (opt-in crash reporting) |
| 1 session, matrix | bulk ops at 9-edition scale | ψ.26 (matrix bulk + drag-select) |
| 1 session, matrix | named scenario presets | ψ.27 (matrix scenarios + import/export) |
| 1 session, matrix | scale-friendly browsing | ψ.28 (matrix search/filter) |
| 1 session, matrix | confident editing | ψ.29 (matrix undo/redo + `?` help) |
| 1 session, matrix | retail review | ψ.33 (matrix print/PDF + save-diff preview) |
| 1-2 sessions, matrix | a11y + tablets | ψ.30 (matrix accessibility + mobile) |
| 1-2 sessions, matrix | unified editor | ψ.31 (per-book overrides UI in /matrix) |
| 1-2 sessions, matrix | retail decision | ψ.32 (compare-editions side-by-side) |
| 1 session, security | rate limiting | ξ.8 |
| 0.5 session, security | CDN integrity | ξ.9 (SRI hashes) |
| 1 session, security | egress control | ξ.10 (SSRF allowlist) |
| 0.5 session, security | CVE awareness | ξ.11 (pip-audit) |
| 0.5 session, security | SAST scan | ξ.12 (bandit) |
| 1 session, security | retail audit trail | ξ.13 (audit log) |
| 1 session, security | secret hygiene | ξ.14 (OS keychain) |
| 1 session, security | AI safety | ξ.15 (sandbox χ-AI-notes output) |
| 1 session, tooling | drift auto-fix | ω.18 (lint --fix) |
| 1 session, tooling | YAML safety | ω.19 (schema validator CLI) |
| 1-2 sessions, tooling | iteration speed | ω.20 (build cache) |
| 0.5 session, tooling | dev loop | ω.21 (watch mode) |
| 1 session, tooling | safe schema evolution | ω.22 (migration framework) |
| 0.5 session, tooling | lint perf | ω.23 (lint --profile) |
| 1 session, tooling | non-CLI users | ω.24 (prospect REPL) |
| 1 session, tooling | refactor safety | ω.25 (bulk rename) |
| 1 session, cleanup | dead code | ω.26 (vulture sweep) |
| 1-2 sessions, cleanup | test navigability | ω.27 (test fixture split) |
| 0.5 session, cleanup | disk hygiene | ω.28 (backup retention) |
| 1 session, cleanup | content integrity | ω.29 (content health checker) |
| 0.5 session, cleanup | cache correctness | ω.30 (cache invalidation audit) |
| 1-2 sessions, cleanup | type safety | ω.31 (mypy/pyright sweep) |
| 0.5 session, cleanup | doc hygiene | ω.32 (docstring coverage) |
| 0.5 session, cleanup | format consistency | ω.33 (ruff format) |
| RELEASE motion | declare v1.0 done | v1.0.0 (binary + QA + tag) |

**Recommended next 5-session sequence** (assumes user has not
declared v1.0 yet):

```
Session N+1   ψ.7-A   built-in editions       + ψ.7-A spec doc
Session N+2   ψ.7-B   template starter packs
Session N+3   ψ.16    status-dashboard polish (8 → 13 consoles)
Session N+4   ν.2.8 + ψ.11 duo + ψ.13.5 f-string sweep
Session N+5   v1.0.0 RELEASE motion (visual QA + binary build + tag)
```

After session N+5, v1.0 is shipped. v1.1 starts with **ψ.1 live
EPUB preview** (the highest-leverage demo upgrade) or **ρ.1
LibriVox audio** (the biggest market differentiator after ψ.8) per
user pick.

---

## 7. Phase ledger — every Greek-letter phase, current status

Authoritative phase index. Source: grep CHANGELOG.md +
SCOPE addenda. Status is one of: ✓ shipped, ◐ shipped-infrastructure
(user-side completion remaining), ◯ open, △ parked, ✗ deferred.

### Greek-letter families

| Letter | Theme |
|---|---|
| α | (legacy / pre-snapshot) |
| β | base scaffolding |
| γ | (unused) |
| δ | (unused) |
| ε | (unused) |
| ζ | (unused) |
| η | edition profiles foundation |
| θ | desktop binary chain |
| ι | (sparse) |
| κ | initial buyer demo |
| λ | symbol customization foundation |
| μ | symbol-toggle matrix (λ → μ) |
| ν | customize console + per-edition machinery |
| ξ | security hardening |
| ο | (unused) |
| π | publisher console + cover system |
| ρ | audio-augmented EPUBs |
| σ | persistence + GitHub backup |
| τ | PD translation expansion |
| υ | console surfacings of CLI tools |
| φ | misc foundational |
| χ | corpus + detector expansion |
| ψ | UX / polish / preview / matrix sidebar |
| ω | ops + robustness + meta-tooling |

### Shipped (✓)

```
β.1   β.2   η.1   θ.1   θ.2   θ.3   θ.4   θ.5   θ.6   θ.7
ι.2   κ.1   κ.2
λ.1   λ.2   λ.3   λ.4
μ.0   μ.1   μ.2   μ.2.5   μ.3
ν.1   ν.2   ν.2.5   ν.2.7   ν.2.7-A   ν.2.7-B   ν.2.8   ν.2.9   ν.3   ν.4   ν.5   ν.6   ν.6.1
ξ.1   ξ.2   ξ.4
π.1   π.2   π.4   π.4-A   π.4-B   π.4-C   π.5
σ.1   σ.2   σ.3
τ.1   τ.1.5
υ.1   υ.2   υ.6   υ.7
φ.1
χ.0   χ.0.1   χ.0.2   χ.0.3   χ.0.4   χ.1   χ.6   χ.6.5   χ.7
ψ.2   ψ.3   ψ.4   ψ.5   ψ.6   ψ.7-A   ψ.7-B
ψ.8   ψ.8.0   ψ.8.1   ψ.8.2   ψ.8.2-A   ψ.8.2-B   ψ.8.3   ψ.8.4   ψ.8.5
ψ.10   ψ.11   ψ.12   ψ.12.5   ψ.13   ψ.13.5   ψ.14   ψ.15   ψ.16   ψ.17   ψ.18   ψ.18.1
ω.0   ω.0.1   ω.0.2   ω.0.3   ω.0.4   ω.0.5   ω.0.6   ω.0.7   ω.0.8
ω.1   ω.2   ω.3   ω.5   ω.5.1   ω.5.2   ω.5.3   ω.6   ω.7   ω.8   ω.9   ω.10   ω.14
ψ.1   ψ.1.0   ψ.1.1   ψ.1.2   v1.0.0
```

**v1.0.0** is "prep complete" — version metadata + release notes
shipped this session. The git tag itself is user-side per project
convention (the user controls the published release):

    git tag -a v1.0.0 -m "v1.0.0 — first commercial release candidate"
    git push origin v1.0.0

### Shipped infrastructure, user-side completion remaining (◐)

```
χ-AI-xrefs   API key + paid run + batch promote
τ.1          WEB sources/ extract user-side
ρ.1          (when shipped, LibriVox fetch is user-side)
θ.1-θ.4      binary build + signing (PyInstaller / Inno Setup / hdiutil)
```

### Open (◯)

```
SHORT      ψ.18.2
           ψ.20   ψ.21   υ.3   υ.8   ψ.25
           ψ.26   ψ.27   ψ.28   ψ.29   ψ.33
MEDIUM     ρ.1     χ.2     χ.3     χ.4     χ.5
           τ.2   τ.3   τ.4   τ.5-A   τ.5-B   τ.6   τ.7   τ.8   τ.9   τ.10   τ.11
           ψ.19   ω.16   π.6   χ.10   χ.11   ψ.24   τ.12
           ψ.30   ψ.31   ψ.32
LONG       θ.3.1   ρ.2   ρ.3   ρ.4   ρ.5   ψ.7-C
           χ-AI-notes   ψ.22   ψ.23   θ.5
HARDENING  ξ.3   ξ.5   ξ.6   ω.11   ω.12   ω.13   ω.17
           ξ.8   ξ.9   ξ.10   ξ.11   ξ.12   ξ.13   ξ.14   ξ.15
           ω.18   ω.19   ω.20   ω.21   ω.22   ω.23   ω.24   ω.25
           ω.26   ω.27   ω.28   ω.29   ω.30   ω.31   ω.32   ω.33
```

(v1 release prep ✓ shipped — see ledger above; user-side tag pending.)

### Parked, design-call needed (△)

```
min-confidence calibration mismatch (Greek + Hebrew detectors)
cleanup.py expansion to content/candidates/
scaffolder integration test
ψ.18.2's full per-chapter 4th-dim matrix expansion
ξ.7 auth-gate (re-eval post-cloud-roadmap)
```

### Indefinitely deferred (✗)

```
ω.4   ψ.5.1   ψ.4   φ.7   χ.8
multi-user / reviewer roles
in-app runtime AI generation
```

---

## 8. Cluster matrix — phases that touch the same files

Bundle phases by cluster within a session for bandwidth efficiency.

| Cluster | Phases | Files |
|---|---|---|
| **EDITIONS** | ψ.7-A, ψ.7-B, ψ.7-C, ψ.19 | content/editions.yaml, content/edition_templates/, content/reading_plans/, scripts/web.py (api_*), scripts/templates/wizard.py |
| **TEMPLATES** | ψ.16, ψ.13.5, ψ.25, all 13 console templates | scripts/templates/*.py, scripts/templates/_design.py |
| **MATRIX-VIEW** (visualization, sidebar, export) | ψ.18.2, ψ.20, ψ.33 | scripts/core/matrix.py, scripts/templates/matrix.py (sidebar), scripts/web.py (api_matrix) |
| **MATRIX-EDIT** (interaction flow, bulk ops, scenarios, accessibility) | ψ.26, ψ.27, ψ.28, ψ.29, ψ.30, ψ.31, ψ.32 | scripts/templates/matrix.py (selection state + handlers + ARIA), scripts/core/matrix.py (per-book overrides on Matrix), content/matrix_scenarios/*.yaml (new), scripts/web.py (api_matrix_compare, api_matrix_scenarios_list, api_matrix_export_yaml) |
| **PREVIEW** | ψ.1, ψ.21, ψ.23, ψ.24 | new scripts/templates/_preview.py, scripts/web.py (api_preview / api_sample_pdf / api_devotional_generate), scripts/templates/{customize,wizard,export}.py |
| **AUDIO** | ρ.1, ρ.2, ρ.3, ρ.4, ρ.5 | new scripts/core/audio.py, content/audio/_sets.json, scripts/build_edition.py audio pass |
| **CORPUS** | χ.2, χ.3, χ.4, χ.5, χ-AI-xrefs follow-ups, χ-AI-notes | scripts/core/sources.py, scripts/core/detectors.py, scripts/run_<src>_at_scale.py |
| **ATLAS** | χ.10 | new scripts/core/atlas.py, content/sources/biblical_atlas.json |
| **LITURGICAL** | χ.11 (eastern-orthodox / anglican-bcp / coptic-orthodox / lutheran-confessional from ψ.7-A are primary consumers) | new scripts/core/calendar_liturgy.py, content/calendar/feasts.yaml |
| **TRANSLATIONS** | τ.2-τ.11, τ.12 | scripts/extract_translation.py registry, content/translations/<id>/ |
| **BUILD-FORMATS** | ψ.22 (PDF, MOBI, HTML, TXT) | new scripts/core/export_pdf.py, scripts/core/export_mobi.py, scripts/core/export_plain.py, scripts/build_edition.py format dispatch |
| **COVERS** | π.4 (✓ shipped), π.6 cover designer | scripts/core/covers.py, new scripts/core/cover_design.py, scripts/templates/covers.py |
| **SECURITY** | ξ.3, ξ.5, ξ.6, ξ.7 | scripts/web.py (response headers), new requirements.txt, .env.example, SECURITY.md |
| **ROBUSTNESS** | ω.11, ω.12, ω.13, ω.16 (snapshots), ω.17 (crash reporting) | new scripts/recover.py, new scripts/core/crashsafe.py, new scripts/core/snapshots.py, scripts/perf_budgets.py, new scripts/core/crash_report.py |
| **SOURCES** | υ.3 (search), υ.8 (verse-of-day feed) | scripts/web.py (api_search_notes / api_verse_of_day), scripts/templates/sources.py |
| **I18N** | θ.5 (Spanish / Portuguese / French / German UI) | new scripts/core/i18n.py, content/i18n/<lang>.yaml, all 13 console templates |
| **DESKTOP-UPDATE** | θ.3.1 | dev/launcher.spec, scripts/launcher.py |
| **UX-MICRO** | ν.2.8, ψ.11 | scripts/templates/{customize,wizard}.py, scripts/templates/_design.py |

A new phase that doesn't fit a cluster is a smell — either the
cluster needs widening or the phase is misplaced.

---

## 9. Working agreements — unchanged from PLAN_2026-05-08

  - Saves are explicit, not automatic. Post-σ.3, save = git push.
  - "Continue" / "push" advance to the next phase; never trigger a save.
  - Every phase ship updates: code + tests + CHANGELOG + SESSION_STATE
    + IN_FLIGHT marker + Tier-1 audit.
  - Phases reference scope addenda for spec, not phase letter for
    ordering. Logical sequence > alphabetical.
  - §13 (topic-shift audit) and §14 (state-uncertainty audit)
    protocols apply unconditionally.
  - User preference: most-extensive answer always (memory:
    feedback_extensive_answers.md). Default response depth =
    comprehensive, not terse.

---

## 10. Files for fresh-session bootstrap

```
dev/CLAUDE_PROJECT_RULES.md       canonical conventions + §9 mental models
dev/SESSION_STATE.md              live snapshot of current state
dev/PLAN_2026-05-09.md            this file — sequence forward
```

Optionally:

```
dev/IN_FLIGHT.md                  open-task tracker (active vs idle)
dev/CHANGELOG.md                  chronological history
dev/SCOPE_*.md                    per-feature specs
```

---

## 11. Active scope addenda — index

| Addendum | Phase | Status |
|---|---|---|
| `dev/SCOPE_2026-05-08.md` | base scope | ✓ active |
| `dev/SCOPE_2026-05-08-addendum-cross-denom-compare.md` | ψ.8 | ✓ shipped |
| `dev/SCOPE_2026-05-08-addendum-audio-epubs.md` | ρ.1 | ◯ open |
| `dev/SCOPE_2026-05-08-addendum-pd-translations.md` | τ cluster | ◐ τ.1 done |
| `dev/SCOPE_2026-05-08-addendum-prettification.md` | ψ.13/14/17 | ✓ shipped |
| `dev/SCOPE_2026-05-08-addendum-robustness.md` | ω.8/9/10/11/12/13 | ◐ ω.8/9/10 done |
| `dev/SCOPE_2026-05-08-addendum-security.md` | ξ cluster | ◐ ξ.1/2/4 done |
| `dev/SCOPE_2026-05-08-addendum-kenyon-textcrit.md` | χ.0 | ✓ shipped |
| `dev/SCOPE_2026-05-08-addendum-ai-xrefs.md` | χ-AI-xrefs | ◐ infra done |
| `dev/SCOPE_2026-05-08-addendum-textcrit-deep-dive.md` | χ.0+ | ◯ open |
| `dev/SCOPE_2026-05-07-addendum-popup-languages.md` | ν.2.7 | ✓ shipped |
| `dev/SCOPE_2026-05-07-addendum-covers.md` | π.4 | ✓ shipped |
| `dev/SCOPE_2026-05-07-addendum-tooling-roadmap.md` | χ + υ + φ + ψ | partial |
| `dev/SCOPE_2026-05-07-addendum-ops-and-accelerators.md` | ν.4-5, ψ.*, ω.* | partial; ψ.7 spec lives here |
| `dev/SCOPE_2026-05-09-addendum-edition-templates.md` | ψ.7-A + ψ.7-B | ◯ to write alongside ψ.7 ship |

---

*PLAN coherence enforced by `scripts/lint_plan.py` (added in
ω.15 alongside this restructure). The linter verifies: every
phase mentioned with status `✓ shipped` appears in CHANGELOG;
every phase listed `◯ open` does NOT appear in CHANGELOG with a
ship message; every `Depends:` reference points to a known phase
id. Failing checks block save.*
