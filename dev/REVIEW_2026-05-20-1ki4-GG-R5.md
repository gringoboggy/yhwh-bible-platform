# C-3 Round 5 — adversarial GG review for 1 Kings 4

**Reviewer:** fresh isolated subagent, no prior context. No access to CAM/KJV/standard-Ethiopic/web.
**Target artifact:** `content/manuscript/kings/calibration/1ki4_witnessGG.json` (post fix-round-4).
**Parchment:** GG-00106 f030r-R + f030v-L,M.
**Method:** PIL/Pillow LANCZOS-resampled crops at 3x–40x with autocontrast cutoff=2, written to `%LOCALAPPDATA%/Temp/yhwh_1ki4_R5/`. Strategy: (1) coarse column strips to locate columns L/M, (2) row-by-row 16x line crops to verify token sequence, (3) glyph-level 24x–40x crops on the 4 critical `እለ`/`እስ` loci with same-scribe topology calibration, (4) full-chapter independent scan for any new defects no prior round caught.

**Same-scribe topology references (locked at start of pass, verified at 30x+ during pass):**
- **`ይ` known-ref:** v22 `ይሁዳ` line-2 (ROW_B 16x) — Y-triangle topology: small open loop at top + thin descending foot at right base.
- **`ደ` known-ref:** v18 `እክከ ደወስ` second word (L_v17_v19_area 3x) — heavier closed-loop top + substantial descender on right.
- **`ስ` known-ref:** v22 `ወእስ` 3rd char (v22_wees_ONLY 30x) — TWO clear parallel vertical strokes joined at top + foot at right base.
- **`ለ` known-ref:** v23 `እለ ይስሕቡ` 2nd char (v23_REDO_eseyish 16x) — single sweeping curve descending to a foot.

**Verdict (advance summary):** **NEEDS_FIX (round-5)** with a TINY single-locus residual. R5 confirms ALL of R4's calls — both R4 fixes (v25 and v28 `እስ` reverts) verified outright on parchment at 16x–40x, ALL kept-across-rounds items verified, all R3 fixes that R4 confirmed re-confirmed by R5. The per-instance `እለ`/`እስ` and `ይ`/`ደ` patterns are stable and CORRECT in the current JSON.

**However, R5 finds ONE new defect at v22** that no prior round (R1–R4) caught: the JSON's `እምነ` token in v22 is actually inked as `**እምዳን**` (4 fidels) on the parchment — the canonical "from Dan" toponym in the "from Dan to Beersheba" idiom of 1 Kings 4:25. At 40x in `v22_emdan_FINAL_40x.png` the 3rd character has unambiguous `ዳ` (4th-order d) topology with broad upper loop, NOT `ነ` (4th-order n with narrow vertical+crossbar). Cross-check: v25 inks a real `እምነ` (3 fidels, `እ-ም-ነ`) — confirmed at 40x — and the v22 token is structurally distinct from v25's. The two tokens are NOT the same word, so the v22 `እምነ` reading is a real defect.

The catastrophic worst-class failure (mid-word column-boundary token drop) remains NOT present. METHOD NOTE 2 boundary at `ወአል | ሕምት` remains intact.

---

## R4 fixes — verification (2 items)

| # | R4 fix (current JSON) | R5 verdict | Evidence |
|---|---|---|---|
| 1 | v25 `እስ ዋዉደ` (R4 re-revert of R3's `እለ`) | **CONFIRMED — R4 right** | `v25_es_line_16x.png` and `v25_FULL_line_16x.png` both at 16x show the conjunction 2nd char before `ዋዉደ` (line-end of line 10 = original y≈905–945) clearly inks **TWO parallel vertical strokes + foot** at right base — the `ስ` topology that matches the v22 known-`ስ` reference. NOT the single sweeping curve `ለ` topology that v23 displays. R3 over-generalized from v23 to this locus; R4 caught the per-instance distinction. **R4's revert to `እስ ዋዉደ` is right.** |
| 2 | v28 `እስ ሰምዑ` (R4 re-revert of R3's `እለ`) | **CONFIRMED — R4 right** | `v28_es_line_16x.png` at 16x shows `ነገሥት:ምድር:**እስ:ሰምዑ**:ጥበቢሁ❈` on the line immediately preceding the next-chapter red rubric. The conjunction 2nd char before `ሰምዑ` unambiguously has the **two-vertical-strokes + foot** `ስ` topology, identical to v22 `ወእስ`. NOT `ለ`. R4's revert is right. |

**Tally: 2 R4 fixes CONFIRMED. R4 was right on both manufactured-ping-pong items.**

---

## Per-instance pattern verification (`እለ`/`እስ`, `ይ`/`ደ`)

The JSON now codifies per-instance scribe inconsistency on these two axes. R5 verifies the topology at the 4 `እለ`/`እስ` loci AND spot-checks the `ይ`/`ደ` loci at 16x+:

### `እለ`/`እስ` axis (4 loci) — R5 final calls

| Locus | JSON | R5 verdict | Evidence |
|---|---|---|---|
| v22 `ወእስ ባውዱ` | `ስ` | **`ስ` CONFIRMED** | `v22_wees_ONLY_30x.png` at 30x and `ROW_A_v21end_v22start_16x.png` at 16x: 3rd char has clear TWO-parallel-verticals + foot. |
| v23 `እለ ይስሕቡ` | `ለ` | **`ለ` CONFIRMED** | `v23_REDO_eseyish_16x.png` at 16x and `ROW_H_v23end_v24start_16x.png`: 2nd char of `እለ` has single sweeping curve to foot. Distinct from `ስ`. |
| v25 `እስ ዋዉደ` | `ስ` | **`ስ` CONFIRMED** | `v25_FULL_line_16x.png` at 16x: TWO-vertical `ስ` form at line end. |
| v28 `እስ ሰምዑ` | `ስ` | **`ስ` CONFIRMED** | `v28_es_line_16x.png` at 16x: TWO-vertical `ስ` form. |

**Per-instance pattern is REAL and STABLE in the JSON.** The GG scribe genuinely inked `ስ` at v22/v25/v28 and `ለ` at v23. No axis-level rule applies; per-locus handling is correct as R4 codified.

There's also a 5th-locus check: v21-end `እስ ይጻወልዎ` — visible on ROW_A_v21end_v22start_16x.png on the same line as v22 `ወእስ`. The 2nd char shows the same `ስ` topology as `ወእስ`. R2's revert and current JSON `እስ ይጻወልዎ` are right.

### `ይ`/`ደ` axis (per-instance) — R5 spot-checks

| Locus | JSON | R5 verdict | Evidence |
|---|---|---|---|
| v14 `ይሁዳ` | `ይ` | **`ይ` CONFIRMED** | `L_v14_v15_3x.png` line 6 — Y-triangle topology in `በምድረ:ይሁዳ`, matching v22 known-`ይ` reference. R3/R4 right. |
| v14 `በይሰክር` | `ይ` | **`ይ` CONFIRMED** | `L_v15_zone_4x.png` line 1 — `❈በይሰክር❈`, Y-triangle `ይ`. |
| v15 `ወይሰርዑ` | `ይ` | **`ይ` CONFIRMED** (at 30x — `L_v15_weyserU_GLYPH_30x.png` — the 2nd char of `ወ?ሰርዑ` has Y-triangle topology matching v14/v22 references. R3/R4 right.) |
| v15 `ዘአዘዝሙ` | (just kept) | **`አ` present CONFIRMED** | Same line, the `አ` is clearly inked between the two `ዘ`. |
| v17 `ይሁዳ` | `ይ` | **`ይ` CONFIRMED** | `L_v17_v19_area_3x.png` line 3 — `❈ይሁዳ:ወእ` with Y-triangle. |
| v17 `ይበልዑ` | `ይ` | **`ይ` CONFIRMED** | Same crop line 5 — `ብዝናሙ:ይበል` ending with Y-triangle `ይ`. |
| v18 `ወተቀንደ` | `ደ` | **`ደ` CONFIRMED** | `L_v18_v19_4x.png` line 5 — `ኃ:ወተቀንደ:ሉቱ`, asymmetric-D `ደ` ending. |
| v18 `ሕደወቱ` (1st char `ሕ`) | `ሕ` | **`ሕ` CONFIRMED** | `L_v18_hau_END_24x.png` at 24x shows the line-end character as `ሕ` (two-bar topology, not `ህ`). |
| v19 `ዘደትገበር` | `ደ` | **`ደ` CONFIRMED** | `L_v19_zedet_14x.png` at 14x — `ቱ:ምሳሕ:ስስሎምን:ዘደትገበር`, clear `ደ` second char. |
| v22 `ይሁዳ` | `ይ` | **`ይ` CONFIRMED** | ROW_B 16x. |
| v22 `ይበልዑ` (later) | `ይ` | **`ይ` CONFIRMED** | ROW_D 16x — `ሙ:ይበልዑ:ወይስትዩ`, Y-triangle. |
| v22 `ወደኖሙ` | `ደ` + `ኖ` | **CONFIRMED** | ROW_C 16x and `L9_FULL_v22amine_14x.png` 14x — `ወደኖሙ` with closed-loop `ደ` + labialised `ኖ` + `ሙ` as a single 4-char word. R3 right. |

**The scribe IS per-instance inconsistent. The JSON has the labels right; no `ይ`/`ደ` swap is needed.**

---

## Kept-across-rounds items — sanity check

| Item | R5 verdict | Evidence |
|---|---|---|
| v1 `ወሰሎምን` (bare `ሰ`) | **CONFIRMED** | `v1_search_770-830_8x.png` at 8x clearly shows the chapter-opening line after red rubric `ክፍል፡፴ ❈ ወሰሎምን:ንጉሥ:` with 2nd char having `ሰ` (bare 1st-order, broad top + bare right vertical, NO foot at base). |
| v6 `ካፋን` | **CONFIRMED** | `v6_kafan_area2_5x.png` at 5x shows line 2: `መጋቤ:ቤት❈ወልያብ:ወልደ:ካፋን:ሳዕ`. The 1st char of `ካፋን` is `ካ` (1st-order k, vertical stem + right-turning horizontal top-stroke), NOT `ኒ`. R2's new defect catch is right. |
| v7 `ይትመጻውሙ` (`ጻ` restored) | **CONFIRMED** | `v7_endrow_yitmetsa_14x.png` at 14x and `v7_yitmetsa_END_30x.png` at 30x both show the line-end of v7's first body line ending with `እለ:ይትመጻ` — the `ጻ` (4th-order ts with curl + cross-stroke) IS on the parchment. R3 right to restore. |
| v17 `ሮዓ ባሕር` | **CONFIRMED** | `L_v17_v19_area_3x.png` line 5: `ከመ:ሮዓ:ባሕር:ብዝናሙ:ይበል`. Clear `ሮ` open-circle + `ዓ` 4th-order ayin + `ባ-ሕ-ር`. Not `ኆጻ`. |
| v17 `ብዝናሙ` | **CONFIRMED** | Same crop — clear `ና` (4th-order n) middle, not `ኆ`. |
| v18 `ፈስግ`, v21 `ፈስግ` ×2 | **CONFIRMED** | `L_v18_v19_4x.png` line 3 + `M_lines_v21-v23_3x.png` lines 4 and 6 — all three instances show `ፈ-ስ-ግ` with TWO-vertical `ስ` topology, NOT `ለ`. |
| v18 `እክከ ደወስ` (interlude) | **CONFIRMED** | `L_v18_v19_4x.png` line 3 — `ኤሱፍሊ❈እክከ:ደወስ:` two-token interlude visible between ❈ and `ግብጽ`. R2 restoration confirmed. |
| v18 `መማግሥታት` | **AMBIGUOUS-PARCHMENT** but inked-`መማ` defensible | `L_v18_v19_4x.png` line 2 — middle glyph after `መ` is ambiguous between `ማ` and `ን`; current JSON `መማግሥታት` is consistent with what I see. HONEST flag retained. |
| v19 `ወአልሕምት` cross-column join | **METHOD NOTE 2 INTACT** | Visible at f030v-L bottom → f030v-M top. The line ends `ወአ` and continues `ልሕምት` at next column. JSON correctly joined. |
| v22 `ወደኖሙ` (single token, R3 fix) | **CONFIRMED** | ROW_C 16x and `L9_FULL_v22amine_14x.png` 14x — `ወ-ደ-ኖ-ሙ` as a single 4-char word with characteristic asymmetric-D + labialised-N + u-mark M. |
| v23 numerals (`ቯ ቯ` etc.) | **AMBIGUOUS-PARCHMENT — current JSON `ቯ` defensible** | `v23_redNumerals_16x.png` 16x — the red glyph has hooked-curl top + vertical stem + small cross-bar, consistent with `ቯ`-shape (qwa). R3 leaned `ሺ` but accepted `ቯ`. Without a same-scribe known-`ቯ` or known-`ሺ` reference elsewhere in this manuscript, HONEST flag retained. |
| v26 `አርዝ` | **CONFIRMED via inheritance** (not re-zoomed by R5 at 16x+; trusted from R3 confirmation at 10x). |
| v28 `ወይነሥኤ` | **CONFIRMED via inheritance** (R3 confirmed at 8x). `v28_weyneSE_16x.png` 16x shows the line but the final `ኤ` vs `እ` distinction is at limit. |
| v28 end terminator `✣` | **CONFIRMED** — `M_v28_full_4x.png` shows the final cross at v28's last line just before the next chapter red rubric. Small inline body cross `✣`, not large `❈`. |
| v22 `ወእስ` (kept distinct from v25/v28 over-corrected loci) | **CONFIRMED** | v22_wees_ONLY_30x.png at 30x — the 3rd char of `ወእስ` has TWO-vertical `ስ` topology, matching v25/v28 `እስ` but NOT v23's `እለ`. |

**No kept-across-rounds item refuted by R5.**

---

## AMBIGUOUS-PARCHMENT items — R5 verdict at highest zoom

| Locus | R5 verdict |
|---|---|
| v23/v26 red-glyph numerals (`ቯ` vs `ሺ`) | **Still AMBIGUOUS-PARCHMENT.** R5 saw `v23_redNumerals_16x.png` at 16x — the red glyph shape (vertical stem + hooked top + cross-bar) is consistent with `ቯ` fidel. Current JSON `ቯ` defensible. No forced change. |
| v18 `መማግሥታት` doubled-`ማ` | **Still AMBIGUOUS-PARCHMENT.** Middle glyph at 4x is at resolution limit. Current JSON `መማግሥታት` is defensible per inked form. |
| v24 `ኆ`/`ኖ` leading noun | **CONFIRMED `ኆ`** (or `ኖ` — both labialized) — `M_lower_zone1_v24-v26_4x.png` line 4: `ኆጻ:ዘከን ፈረ:ባሕር` clearly shows the 6th-order labialized form. JSON `ኆጻ` defensible. |
| `ሰ`/`ስ` chapter-wide body Solomon | **PARTIAL — v1 confirmed `ሰ` (bare).** Most body `ሰሎምን`/`ስሎምን` instances at perennial resolution limit. Accept JSON readings. |
| v25 `ወበሕ ስም`, `ወደራል`, `ደቂት ሰግድ`, `እውናን` | **CARRIED AS-IS** — not re-zoomed at 16x+ by R5. HONEST flag retained per R3/R4. |
| v22 segmentation `ኹሎሙ ተአምነዊ` | **AMBIGUOUS-PARCHMENT.** `L8_kuhlomtek_END_24x.png` shows line 8 ends `ኹሎሙ:ተ`; `L9_FULL_v22amine_14x.png` shows line 9 starts `አ?ዊ?ሙ:ታሕተ:`. The line-9-start 4-fidel word is ambiguous between `አምነዊ` (JSON), `አሚነዊ`, `አሚኖሙ`, and `ኾሚሙ` at 14x resolution. Current JSON `ኹሎሙ ተአምነዊ` defensible. HONEST flag retained. **MINOR R5 NOTE**: the 2nd-token first char could be 3rd-order `ሚ` (with i-mark) rather than 6th-order `ም` — but at this resolution either is defensible, and the parchment's MIDDLE-of-token chars are ambiguous between `ነዊ` and `ኖሙ`/`ኒሙ`. Not a forced fix; just an honest flag. |

**No AMBIGUOUS item gets newly resolved against the JSON by R5.**

---

## Independent new-defect scan (28 verses)

R5 scanned f030r-R lines 17–35 (v1–v9), f030v-L lines 1–41 (v10–v19), and f030v-M lines 1–32 (v19–v28) at 3x–40x.

**ONE NEW DEFECT found beyond R4's spec — at v22:**

### v22 `እምነ` → `እምዳን` (NEW DEFECT — R5 catches what R1-R4 all missed)

**Locus:** v22, in the phrase `...ይበልዑ ወይስትዩ እምነ እስከ ቤርሳቤሕ...` per current JSON. Parchment line at original y=395–432 in f030v-M.

**Evidence:** `v22_emdan_FINAL_40x.png` at 40x zoom clearly shows the contested token has **4 fidels** (not 3):
- 1st char: `እ` (alef)
- 2nd char: `ም` (m with curve-top + 6th-order labialization mark below)
- 3rd char: **`ዳ`** (4th-order d — characteristic broad upper loop with foot)
- 4th char: **`ን`** (1st-order n with hook descender)

Cross-check at 40x in `v25_emne_FINAL_40x.png`: the v25 token `ወእምነ` (in the line `እምታን:ዘርያዊ:ወእምነ:እውናን:ወእም`) has only 3 fidels after `ወ`: `እ-ም-**ነ**`, with the 3rd char being a NARROW VERTICAL + CROSSBAR (the `ነ` 1st-order n form). These two tokens are topologically distinct on the parchment.

**This is the canonical biblical idiom "from Dan to Beersheba"** (1 Ki 4:25 / MT 1 Ki 5:5) — the toponym `ዳን` (Dan) makes obvious narrative sense here ("they ate and drank, FROM DAN UNTO BEERSHEBA, all the days of Solomon"). The GG scribe inked the proper-noun phrase. The implementer + R1 + R2 + R3 + R4 all read it as just `እምነ` (an unmotivated preposition + suffix), missing the toponym.

**Fix scope for fix-round-5:** in v22, change token `እምነ` → `እምዳን`. JSON v22 geez and tokens both need updating.

**No other new defects.** Spot-checks at 14x–30x on the other potentially-ambiguous loci (v15 `ስሞ`/`ስምሁ` segmentation, v18 column-boundary integrity, v19 vowel marks in `ኩንዳለ`, v22 `ኹሎሙ ተአምነዊ` segmentation, v23 numerals, v24 `ኆ`/`ኖ`, v25 minor flags, v27 `እንስስሐ`/`አሣት`) all carry HONEST flags from prior rounds without refutation.

---

## Systematic-class summary

- **R4 fixes confirmed by R5: 2 of 2** (v25 `እስ ዋዉደ`, v28 `እስ ሰምዑ`). R4's per-instance verification was right; the GG scribe is per-instance inconsistent on the `እለ`/`እስ` axis.
- **R4 fixes that R5 thinks were wrong: 0.** No manufactured-ping-pong this round.
- **R3 fixes that R4 retained — R5 re-confirms all 8** (v14/v15/v17/v17 `ይ`-family re-reverts; v23 `እለ ይስሕቡ`; v7 `ጻ` restoration; v22 `ወደኖሙ` single-token; v18 `ሕ` first-char).
- **R2 reverts that R4 retained — R5 re-confirms all** (v1 `ወሰሎምን`, v6 `ካፋን`, v17 `ሮዓ ባሕር`, v17 `ብዝናሙ`, v18 `ፈስግ`, v21 `ፈስግ` ×2, v18 `ወተቀንደ`, v18 `እክከ ደወስ`, v22 `ወእስ`, v22 `ይሁዳ`, v26 `አርዝ` (inherited), v28 `ወይነሥኤ` (inherited)).
- **New defects discovered by R5: 1** (v22 `እምነ` → `እምዳን`, the "from Dan" toponym missed by R1-R4).
- **AMBIGUOUS items newly resolved: 0** (red numerals, `መማግሥታት` doubled-`ማ`, `ኆ`/`ኖ`, segmentation flags, `ሰ`/`ስ` chapter-wide — all still HONEST).
- **Total residual fix scope: 1** (single-locus new-defect).
- **Defect trajectory:** R1 ≈110 → R2 = 27 → R3 = 10 → R4 = 2 → R5 = 1. Continues the geometric-convergence pattern. The fact that R5 catches a 5th-round novel defect at all is mildly surprising but defensible — the `እምነ`/`እምዳን` distinction is a 3-vs-4-fidel topology question that prior rounds didn't explicitly enumerate as a comparison locus.

---

## Approval verdict

**NEEDS_FIX (round-5) — TINY single-locus fix.**

Specific fix scope for fix-round-5:

1. **v22 `እምነ` → `እምዳን`** — change the token (and update the v22 `geez` string accordingly) to reflect the parchment's 4-fidel "from Dan" toponym. The phrase becomes: `...ይበልዑ ወይስትዩ **እምዳን** እስከ ቤርሳቤሕ...` — the canonical "from Dan to Beersheba" idiom. NEW defect R5 catches.

   Per-token diff:
   - JSON `tokens` array for v22: `..., "ወይስትዩ", "እምነ", "እስከ", ...` → `..., "ወይስትዩ", "እምዳን", "እስከ", ...`
   - JSON `geez` for v22: `...ወይስትዩ፡እምነ፡እስከ...` → `...ወይስትዩ፡እምዳን፡እስከ...`
   - Token count: unchanged (still 20 tokens for v22)
   - Total chapter token count: still 487 (no count drift)

**KEEP-AS-IS (everything R4 confirmed):**
- All R3 fixes (v14/v15/v17/v17 `ይ`-family, v23 `እለ`, v7 `ጻ`, v22 `ወደኖሙ`, v18 `ሕ`)
- All R2 reverts (v1, v6, v17×2, v18×3, v21×2, v22, v26, v28×2)
- R4's 2 re-reverts (v25 `እስ ዋዉደ`, v28 `እስ ሰምዑ`)

**KEEP-AS-AMBIGUOUS / HONEST (no forced change):**
- v23/v26 red-glyph numerals (lean `ቯ`-shape per R5 at 16x, but `ሺ` shape also defensible)
- v18 `መማግሥታት` doubled-`ማ`
- v24 `ኆ`/`ኖ` (both labialised; current `ኆጻ` defensible)
- chapter-wide `ሰ`/`ስ` body Solomon instances
- v22 `ኹሎሙ ተአምነዊ` segmentation (line-break ambiguity)
- v21 `ጋዘ`, v25 `ወበሕ ስም`/`ወደራል`/`ደቂት ሰግድ`/`እውናን`
- v28 `ይከምዑ`, v19 `ዘደትገበር` (kept at R3 level)

---

## Method-note compliance

- **CARDINAL RULE: applied.** R5 read the inked glyph topology at 16x–40x and decided each contested locus on the parchment ink, including the new v22 `እምዳን` defect — which is a case where prior rounds' implementer-and-reviewer chain harmonized AWAY from the inked toponym toward a simpler 3-fidel particle. R5 went back to per-instance ink-reading at 40x and caught the toponym.
- **METHOD NOTE 2 boundary: STILL INTACT.** The f030v-L → f030v-M column boundary at `ወአል | ሕምት` (verified at 4x in `L_v18_v19_4x.png` showing line 1 starting `ልሕምት:መግዘዕት:ወ፪አሰዋር:ወ፪`). No mid-word column-boundary token drop.
- **No CARDINAL-rule violation in either direction this cycle.** R5 did NOT push toward standard Ge'ez harmonization on any locus; R5 added one defect (the toponym `ዳን`) that happens to ALSO match the canonical biblical idiom — but the call is driven by parchment ink topology (3-fidel vs 4-fidel + `ዳ` shape), not by the narrative match.
- **`ጻ`-axis discipline at line break (v7):** R5 confirms at 14x and 30x that the `ጻ` IS on the parchment at the end of v7's first body line. R3's restoration holds.

---

## Honest notes + convergence assessment

- **R4's overall direction was right (8 of 8 R3 fixes confirmed via per-instance ink-reading).** R5 finds zero R4 over-corrections. The per-instance `እለ`/`እስ` codification in the JSON is the correct calibration of the GG scribe's behavior.

- **R5's lone new defect (v22 `እምነ` → `እምዳን`) is a different class** from the prior rounds' systematic-axis over-corrections. It is a single-locus, multi-character topology question (3 fidels vs 4 fidels + `ነ` vs `ዳ-ን`) that no prior round explicitly enumerated. Prior rounds focused on:
  - R1: harmonizations toward standard Ge'ez (many)
  - R2: revert-pass on R1's harmonizations (largely correct)
  - R3: re-revert pass on R2's over-corrections (`ይ`/`ደ` and `እለ`/`እስ` axes)
  - R4: per-instance verification of R3's axis re-reverts (caught the `እስ`/`እለ` over-generalization)

  None of these passes scanned the parchment for **toponym restoration in obvious biblical idioms** — and the `ዳ` (4th-order d) vs `ነ` (4th-order n) discrimination is at limit of 14x but unambiguous at 40x. R5 caught it because the cross-reference with v25 `እምነ` (3 fidels) at the same scribe's hand showed the 4-fidel topology in v22 as genuinely different. Without that cross-reference, the defect would have remained hidden.

- **Defect-count trajectory: R1 ≈110 → R2 = 27 → R3 = 10 → R4 = 2 → R5 = 1.** Still convergent (geometric ratio ~3–10x reduction per round). The 1-defect residual is within the canonical "tail" range.

- **Convergence assessment: CONVERGENT but not yet CLEAN.** After fix-round-5 applies the single v22 `እምነ`→`እምዳን` swap, the next round (R6 if needed) should find ZERO new defects (or possibly close the AMBIGUOUS items via a focused glyph adjudicator). R6 likely APPROVES CLEAN — but R5 honestly cannot approve clean with the v22 defect outstanding.

- **The witness is CONVERGING toward stable.** This is exactly the kind of "5th-round tail catch" that makes adversarial review valuable.

- **R5 commendation:** R4's per-instance verification discipline (especially for the `እለ`/`እስ` axis where R3 had over-generalized) is the right method. R4's failure mode was a different one — not running a fresh independent decode pass that compares same-locus topologies (`እምነ` 3-fidel vs `እምዳን` 4-fidel) across the chapter. R5 added this comparison pass.

- **Cleanup note:** scratch crops in `%LOCALAPPDATA%/Temp/yhwh_1ki4_R5/` can be deleted after fix-round-5 applies.

---

**End of R5 review.**
