# Session state — current snapshot

> **➤➤➤ 2026-06-11 (LATEST, 🪟 Windows, turn 73 — ★ROUND-6 DEVICE QA INGESTED: K-R5-6 PROVEN ON-DEVICE — the dc:language restore brought EVERY translation script back under Publisher Default, incl. the FIRST-EVER ARABIC popups** (user: "translations are back up … they all work", many random spots, Publisher Default + all 3 pack fonts; screenshots `kobo_img/1..10.png` corroborate — Heb img4, **Ar img8/9**, Aa-sheet img10). Full note `notes/2026-06-11-kobo-round6-device-qa.md` (K-R6-1..5): **K-R6-1 CLOSED** (the user's regression call vindicated end-to-end; EREADERS flipped to PROVEN) · **K-R6-2** gen 1:1 split landed (◈2/◈5/◈8) but only ◈5 opens — **size theory DEAD as sole factor** (4,349✗/2,626✓/2,537✗ stripped; same file, identical markup, links/koboSpans non-monotonic); gen 1:1 = self-landing piece-top so decline≡missed-tap there; 2 decisive user taps queued (font-size-UP re-tap of ◈2/◈8 = geometry test · mid-piece split badges 1sa 16:12 / act 23:6 = decline-vs-deadtap test); NO teleports anywhere + title clamp HOLDS · **K-R6-3** `\n` seps collapsed by the dialog → Mac flips `_VN_SEP_*` to U+2028 · **K-R6-4** "BOOKII" every title page = `.bookpage-eyebrow` small-caps+letter-spacing+italic eats the (markup-present) word space on the kepub engine — PRE-EXISTING (CSS byte-identical v0.1.0/r5/r6); fix = nbsp ×87 base sites ± word-spacing, Apple-unchanged check · **K-R6-5** = K-R5-4 breaks persist (options with Boggy). Board → Mac (U+2028 + eyebrow + hold cap re-tune until the taps). AWAITING USER: the 2 K-R6-2 taps · gen 35:18 ◈4 re-tap · K-R5-4 presentation pick · STK retry verdict. ▶ WIN NEXT: matrix M1 impl. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-11 (🪟 Windows, turn 72 — ✅ ROUND-6 STAGED ON THE KOBO + ★USER STANDING INSTALL GRANT + orphan-vnotes TRIAGE CLOSED.** Pulled Mac's round-6 fix slice → **rebuilt round-6 locally** (eth 26.31 MB + kepub 34.16 MB): ALL K-R2 GATES GREEN ×2 (incl. 4g/4h/4i; same honest 4g warn vnote-1ki-12-24 6,937) + epubcheck 0/0/0/0 + in-zip fix proof (dc:language ×6 **incl. `ar`**; \n-separators 953/953) → **r6 kepub COPIED TO G:\ hash-verified under the persistent filename** `YHWH-Ethiopian-Bible-koboQA-r5.kepub.epub` (Cardo setting survives). **★USER GRANT (standing, both lanes): "you can always install whatever is needed for the project"** → RULES guard #1 REWRITTEN (pre-authorized; declare-in-manifest-first → pin → install; no per-install ask), WIN memory mirrored, board → Mac (its Kindle Previewer = GO). **TRIAGE CLOSED (`notes/2026-06-11-orphan-vnotes-triage.md`): NOT the popup generator** — base = 0 orphans (pairing correct); the **fold/canon-splice passes drop a book's body+markers but leave its vnote asides** (eth artifact: aes 205 + est-10-5 = 206; Mac's 1,598 = +1es/2es at canon scale). ★Kindle = USER-VISIBLE impact (unhide renders orphans as "[no text]" endnote rows ×1,598 + re-inflates the E3013 budget) → fix → MAC with K-KIN (drop vnote asides whose `v-` anchor is absent post-removal + gate 4j; diagnostic `dev/_triage_orphan_vnotes.py`). **★STK round-2 VERDICT (user, live): FAILED after 46 min** (3rd: ~60/~50/46 min — user told macclaude directly; Previewer-oracle bisect = the path). AWAITING USER (round-6 taps): Publisher Default Hebrew+Arabic popup (the K-R5-6 verdict) · gen 1:1 ◈ (cap fix proof) · gen 35:18 re-tap · ◈ glyph under Cardo · 1sa 16:12 / act 23:6 split badges · any title page. ▶ WIN NEXT: matrix M1 impl (fold in the Mac review's 4 blockers: --target-reader+cache-key first). Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-11 (🖥️ Mac, turn 71 — ✅ ROUND-6 FIX SLICE SHIPPED (all 4 assigned items + 2 found-in-flight) + ★item-② ROOT-CAUSED & FIXED + K-KIN round-2 artifact STAGED (user uploading NOW).** TDD throughout (RED 31 → GREEN; new `tests/test_popup_split.py` ×27 + `test_snapshot_block_restore.py` ×3 + splitter/kindle/opf/gate additions). **① K-R4-2 ✅** `apply_badge_markers` splits over-cap popups into units ≤ `note_popup_split_cap` (**default 4,400** stripped; 0=off; one resolver `resolve_note_popup_split_cap` + API validation + /customize number input; `_stripped_len` == the calibration tool's measure BY TEST) — (a) category-boundary packing + (b) depth-0 sentence-boundary body-chunking w/ visible ⋯ marks + (i/k) part headers; single-unit verses byte-identical; conservation guarded per-unit (S2 leaves) + vs-cap-off-run (test). Round-6 kepub: **max verse-notes unit 4,368 (was 19k+)**; 218/66,880 in the proven-pop 3,313–4,498 band. **② K-R5-3 ✅** badge clamp bounds at `<div class="book-title-page"` (regex only — the content-end bp-bound was tried + REVERTED: rev 22:21's spill markers sit PAST bp-87 and must stay in-region; caught by the integration test + new **gate 4i** badge-mode-marker-leak, fires-on-defect proven on the real stale artifact). Artifact: **0 title-piece badges (was 38)**; gate 4h added. **③ K-R5-6 ✅** `patch_opf` target-gates dc:language: kindle = single en-US; ALL other targets restore en-US+hbo+grc+arc+gez **+ ar**; bcp47 pin re-trued. **④ K-R5-7 ✅** `_VN_SEP_*` lead with literal `\n` (U+2028 fallback if device collapses). **⑤ NEW CLASS (round-6 gate catch):** K-R4-2 byte-shift moved a 400KB cut between a spill-duplicate anchor (`v-1en-106-1-x2`) and its aside → 1 PROMOTED noteref; `split_html_document` now CLONES the aside into the referencing piece (`--cNN` id) + retargets the local href (test: TestSpillDuplicateNoterefClone). **⑥ kindle hardening (2nd STK failure forensics — `notes/2026-06-11-kindle-stk-failure-forensics.md`):** `apply_kindle_unhide` strips `hidden=""` from footnote wrappers post-split (3 odd-template `verse-refs-section` pieces held 692k chars of popups under [hidden]); gate 5 extended (zero hidden attrs on kindle artifacts); ranked causes = hidden-counter ambiguity ∥ ~112k-link graph timeout; one-variable bisect ladder written; **Kindle Previewer = the local oracle — NEEDS USER GO (guard #1)**; collateral: **1,598 orphan vnote asides never noteref'd (2es/1es/AddEsther/est) → triage, popup-generator lane**. **ARTIFACTS:** round-6 eth epub+kepub **ALL K-R2 GATES GREEN (incl. new 4g/4h/4i) + epubcheck 0/0/0/0**; K-KIN round-2 `~/Desktop/Ethiopian_Bible_catholic-study_kindle-safe_2026-06-11T024442Z.epub` gates green + 0/0/0/0 — **user uploading to Send-to-Kindle as of session end**. **★item-② ROOT CAUSE (the CI ordering-flake pair):** `restore_snapshot` re-dumped the record from parsed YAML → dropped the in-block comment above catholic-study's `reader_toc_collapsible` — value-identical/BYTE-different; the session-scope guard then fired ONLY when no later byte-restoring test healed the file = order-dependent. FIX: snapshots now freeze the raw `block.yaml` at create; restore splices it back BYTE-EXACT (old snaps fall back to re-dump); `test_restore_round_trips_unchanged_state` hardened (byte-backup + byte-identity assert; the class's false "does NOT mutate editions.yaml" comment corrected). Hunt method: worktree-isolated `YHWH_GUARD_BISECT=1` run + git-diff fingerprint. AWAITING USER: STK verdict (in flight) · Previewer GO · gen 35:18 re-tap · ◈ glyph under Cardo · round-6 device taps (Publisher Default Hebrew+Arabic = the K-R5-6 gate; 1sa 16:12 / act 23:6 split badges; any title page). Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **TWO live Windows lanes (Mac paused; see IN_FLIGHT top).** (1) **BUILD — σ "Edition Cover + Truthful Front Matter"** (spec+plan written; start σ.1 `resolved_note_counts`, the build-accurate counter; then HOLY-BIBLE cover · Your-Edition page · /customize identity · Ge'ez/Amharic covers · live-console reconcile). (2) **CONTENT — Phase D1b PO Esther vision marathon (paused ~p35)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2. Also open: ρ.3 **C3 polish** (folded into σ.6). Background lanes (RULES §2.5): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). (The truth-record rotation gap CLOSED 2026-06-10 turn 69 — the rotator covers ➤➤➤/▶/the board, and `save-all.ps1` rotates on every full save.) Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).
