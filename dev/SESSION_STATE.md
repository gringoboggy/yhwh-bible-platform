# Session state — current snapshot

> **➤➤➤ 2026-06-11 (LATEST, 🪟 Windows, turn 75 — ★MATRIX M1 SHIPPED END-TO-END (all 5 work items) + e2e-PROVEN on real artifacts.** ① **`FORMAT_MATRIX` one-home constant** beside TARGET_READERS (+ `COVER_COLOURS` + `catalog_asset_name` — spec §4.4 naming, v-prefix-normalized; `format_by_id` strict; 18 pins incl. on-disk 25-template parity; "Apple Books"/"Google Play" labels `term-ref-ok`-waived = free-catalog platform labels, not store distribution). ② **The 18 M1 composites COMMITTED** under `content/covers/catalog/` (9 editions × 01_ornate_leafy/02_classical_corner in black; `generate_catalog_composite`/`m1_catalog_plan` in generate_edition_covers.py; 7 pins; both families visually checked) — **CI swaps committed bytes, never composites in-runner** (kills the ubuntu font-divergence class at the root; supersedes the spec's pin-fonts mitigation). ③ **`scripts/swap_epub_cover.py`** (build_epub deterministic-writer rules: mimetype-first STORED, 1980 dates, DEFLATE-9; JPEG magic-byte gate; 7 pins; proven on the round-6 real artifact → epubcheck 0/0/0/0) + **`scripts/build_format_matrix.py` driver** (one job per edition / formats serial; base built ONCE per distinct target via `--target-reader`; gates COMPOSE `scripts/epubcheck.py --require --strict` + `dev/verify_kr2_build.py`; `sums-<ed>.txt` fan-out; `--list-editions` emits the 9 non-standalone ids; 10 pins) + **`.github/workflows/format-matrix.yml`** (plan[tag-validate fail-fast + editions matrix from editions.yaml] → 9-way build [fail-fast:false, own-assets-only uploads] → SHA256SUMS **fan-in sole-writer** [artifact-handoff merge, no per-job self-merge]; concurrency-grouped per tag) — **★e2e PROVEN: catholic-study × everywhere+tablet → 2 swapped catalog assets, epubcheck --strict 0/0/0/0 ×2, ALL K-R2 GATES GREEN ×2** (known-honest 4g vnote-1ki-12-24 warn only). ④ **`scripts/gen_release_catalog.py`** (PAGINATED gh asset listing; **FULL-COUNT column gating** — a colour shows only when EVERY edition has it, partial uploads under-show never 404; never-remove-live **legacy Kobo cell** auto-retires when M3 lights the column; emits catalog.json + a no-JS `<details>` fragment; 13 pins incl. an out-of-repo `-o` regression the live run caught) — proven against the LIVE v0.1.0 release (7 assets → 0 live columns, honest dark state; sim-full-M1 → both columns light correctly). ⑤ **releases.html "Choose your edition & device" band** + build.mjs `{{release_catalog}}` inline (geez-progress pattern) + parchment catalog CSS — **Playwright visual QA on the simulated-live build PASSED** (dark state deploys as a quiet note; catalog appears automatically once assets ship + regen). BONUS: **epubcheck wrapper discovery step 3 = the pip-package jar** (self-shadow-proof PathFinder probe — `scripts/epubcheck.py` shadows the package name on CLI runs; ordered BEFORE the Windows-broken PATH wrapper; TDD pin) + `epubcheck>=5.1,<6` declared in requirements-dev (it was used-but-undeclared). **73/73 arc tests.** Commits dbefafe9→c307e92b. ▶ WIN NEXT: ① milestone 5-leg sync → ② `gh workflow run format-matrix.yml -f tag=v0.1.0` (repo is PUBLIC on GitHub — free minutes; ~18 assets + SHA256SUMS) → ③ on green: regen catalog + rebuild + deploy site (the catalog goes LIVE) → ④ then M2 colour variants / σ lane. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-11 (🖥️ Mac, turn 73 — ★K-KIN ROOT CAUSE FOUND: 3 TOC-HUSK TITLE PAGES, NOT THE LINK GRAPH — the local Previewer oracle is LIVE and NAMED the E-code.** Crash-recovered session: the orphan-vnote slice survived on-disk (tests green) → committed; the pre-crash **Kindle Previewer 3.104.0 install survived** (verified by execution) and became the breakthrough: it **reproduces the STK failure locally in ~15 min** — `E24010 Hyperlink not resolved in toc` ×3 (`bp-45/46/47`) → `E24001 TOC could not be built` → conversion rejected. Those anchors live in **EMPTY HUSK title pieces** (Prayer of Azariah / Susanna / Bel & the Dragon — the demoted Daniel-additions: `apply_appendix_demotion_and_renumber` flips them to `appendix-section`, CSS hides eyebrow/art, and the splitter isolates the ~750-byte title frame in its OWN spine piece; the books' CONTENT is present in the following pieces; nav.xhtml + toc.ncx + the in-book TOC page all point at the husk piece, which the KFX preprocessor refuses as a link target). **Rung-2 DELINK verdict: FAILED on STK (user, live) AND locally with the IDENTICAL E-code — link-graph hypothesis DEAD, oracle fidelity PROVEN both directions.** Shipped: **rung TOCHUSK** in `dev/kindle_bisect.py` (one-variable zip rewrite: 3 husks + opf item/spine itemref/nav li/ncx navPoint/in-book toc-book li ALL removed; tempered navPoint regex — a bare `.*?` swallowed navPoints up to the husk, caught by epubcheck "first playOrder is not 1"; playOrder renumber; hard assert = zero surviving husk refs; **epubcheck 0/0/0/0**; tests ×20) — probe staged `~/Desktop/…_rung-tochusk.epub`. **★TOCHUSK VERDICT (×2 runs, incl. an idle-box rerun): the named TOC errors are ELIMINATED (E24010/E24001 gone, error-count 0) — husk = CONFIRMED blocker #1, fixed by removal — but conversion STILL fails on a GENERIC "Kindle conversion has encountered an internal error" (no E-code, no source file; identical both runs ⇒ not load-induced). The failure is LAYERED: the TOC husks fired first and masked blocker #2.** Next rung = CHAIN delink ON TOP of tochusk (`kindle_bisect.py --rung delink --src ~/Desktop/…_rung-tochusk.epub` → Previewer): PASS ⇒ the link graph WAS the shadowed second cause (sub-bisect it); FAIL ⇒ rung-3 half-spine on the tochusk base. Also shipped: **K-R6-3** `_VN_SEP_*` → U+2028 (TDD; the \n variant collapsed in Kobo's dialog) · **K-R6-4** eyebrow nbsp **AT THE EMITTER** (re-verified WIN's 87-base-sites prescription against real code: the renumber pass REWRITES every eyebrow per build, so base edits would be clobbered; `BOOK <roman>` one-line emitter fix + tests ×3, new `tests/test_eyebrow_renumber.py`) · the `_VNOTE_ASIDE_RE` clobber fixed CONVERGENTLY with WIN (identical rename both lanes; WIN's lint + regression pins kept on merge). Pulled+rebased WIN t73/73b/74 (2 conflict rounds, both resolved combining); **50/50 cross-slice tests green post-rebase** (incl. WIN's 19 target-reader pins). ▶ MAC NEXT: ① tochusk Previewer verdict → ship-fix design (retarget demoted-book TOC entries to the first CONTENT piece — the husk fix class: splice residue organ #2 after orphan vnotes; then kindle-safe rebuild + gates + ONE STK confirmation upload) · ② K-R6-6 chip badges (scouted: emitter `build_edition.py` ~3104 `◈{count}`, `apply_note_popup_style` pattern, `resolve_target_reader` eink gate) · ③ re-run the 2 marker_style integration builds post-merge · ④ hold K-R4-2 cap re-tune until the user's mid-piece taps. AWAITING USER: act 23:6 / 1sa 16:12 mid-piece taps · gen 35:18 re-tap · K-R5-4 pick. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-11 (🪟 Windows, turn 74 — ★MATRIX M1 BLOCKER-#1 SHIPPED + ★BUILD-BREAKING a749e99b REGRESSION CAUGHT & FIXED.** ① **`--target-reader` build flag implemented end-to-end** (spec-review blocker #1 + corollary): `apply_target_override` folds the override into a COPY of the edition record at the top of `build_one` (one-resolver invariant; editions.yaml never mutated; standalone+override = hard error, `--all` skips standalones with a note) · `compute_cache_key(..., target_reader=)` hashes the **RESOLVED** target (override≡stored hits the same entry; normalization = one-time full cache invalidation, conservative-correct) · the same wrong-format class closed in the mtime shortcut (override artifacts named `…_<version>_<target>_<timestamp>.epub`; `is_output_current` target-aware BOTH directions) · sidecar carries `target_reader`. Tests `tests/test_target_reader_override.py` ×19 (TDD). **PROVEN on a real artifact:** catholic-study × eink (23.95 MB, 387s) — filename token + in-zip OPF stamp `yhwh:target-reader="eink"` + sidecar all carry eink; **epubcheck 0/0/0/0**. ② **The first real build post-merge caught a SHOWSTOPPER in Mac's a749e99b orphan-vnote slice:** its new orphan-drop regex was defined as a SECOND module-level `_VNOTE_ASIDE_RE`, silently clobbering the 6-group popup-pass regex → `IndexError: no such group` in EVERY popup-edition build (the arc's own tests stayed green — only the new pass was covered). FIXED forward: renamed `_ORPHAN_VNOTE_ASIDE_RE` + regression pins ×3 (`TestVnoteRegexCollisionRegression`) + **NEW lint check `module_constant_collision`** (ALL_CHECKS 33→34; AST-based, self-referential transform rebinds excluded — the 21 `X_HTML = apply_design_system(X_HTML…)` template sites stay legit; waiver marker `# constant-redef-waived:`; ruff F811 does NOT cover plain variables). ③ Spec REVISED: all 4 Mac review blockers folded into `2026-06-10-website-format-matrix-design.md` (#1 implemented; #2 composite-per-variant via build_epub's deterministic writer; #3 SHA256SUMS fan-in job; #4 per-edition job topology) + MEDs (FORMAT_MATRIX one-home · never-remove-live Kobo cell · catalog full-count gating + pagination · 83-corollary). ▶ WIN NEXT: M1 continue — `FORMAT_MATRIX` constant → ~18 M1 composites → CI workflow + `gen_release_catalog.py` + catalog UI. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **TWO live Windows lanes (Mac paused; see IN_FLIGHT top).** (1) **BUILD — σ "Edition Cover + Truthful Front Matter"** (spec+plan written; start σ.1 `resolved_note_counts`, the build-accurate counter; then HOLY-BIBLE cover · Your-Edition page · /customize identity · Ge'ez/Amharic covers · live-console reconcile). (2) **CONTENT — Phase D1b PO Esther vision marathon (paused ~p35)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2. Also open: ρ.3 **C3 polish** (folded into σ.6). Background lanes (RULES §2.5): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). (The truth-record rotation gap CLOSED 2026-06-10 turn 69 — the rotator covers ➤➤➤/▶/the board, and `save-all.ps1` rotates on every full save.) Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).
