# Session state — current snapshot

> **➤➤➤ 2026-06-14 (LATEST, 🪟 Windows, turn 85 — ★ M4 KINDLE PRODUCTIZED + Node-24 CI bumps; running overnight autonomous).** The device-proven Send-to-Kindle "june10" recipe is now a repeatable build mode (NOT the disproven `--target-reader kindle` variant): NEW `scripts/core/kindle_post.py` (`make_kindle_safe`: strip `display:none`/`visibility:hidden` CSS+inline · drop `.vn-sep` · collapse `dc:language`→single `en-US` · leave `hidden=""` · OCF re-zip; `verify_kindle_safe` gate) + driver `scripts/build_kindle.py` + `build_format_matrix` Kindle column = everywhere base + post-process (`post_process: kindle_safe`; `target_reader` stays `kindle`). **Proven on Windows:** catholic-study = 24.01 MiB, **epubcheck 0/0/0/0**, `verify_kindle_safe` clean, K-R2 green (132,949 `.vn-sep` + 7 CSS hides stripped, 6→1 `dc:language`) — byte-faithful to the Mac's device-confirmed artifact. 15 new unit tests + 147 targeted green; dormant variant / gate-5 / 9-KJV untouched. **Node-24 CI bumps (Grok-delegated, diff-reviewed):** checkout@v5 · setup-python@v6 · setup-java@v5 + `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` across the 3 workflows (Node-20 cutoff 06-16). EREADERS Kindle row+section updated. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-14 (🖥️ Mac, turn 84 — ★★ K-KIN RESOLVED ON THE REAL CHANNEL: Send-to-Kindle ACCEPTS the program Bible).** User-confirmed: `~/Desktop/Ethiopian Bible - Catholic Study (Kindle) june10recipe.epub` **DELIVERED via Send-to-Kindle** (fast upload). It reproduces the proven **June-10 test-2 recipe**: a **standard everywhere build + `display:none` physically stripped + single `en-US`, with NONE of FIXED's extras** (no shipshape compaction, no 189-way file-split, no attr-strip). ⇒ **those KP3-oracle-driven extras were exactly what broke Send-to-Kindle**; the minimal recipe is the answer (24.1 MB, 299 spine, epubcheck 0/0/0/0). **PROVEN RECIPE:** `build_edition.py catholic-study` (standard) → post-process: strip `display:none`/`visibility:hidden` (CSS+inline) · collapse `dc:language`→single `en-US` · leave `hidden=""` · OCF re-zip (mimetype-first stored). **NEXT (fresh session):** productize this as the clean kindle build mode (it's currently standard-build + a deterministic post-process script) → then **M4 Kindle unblocks for real**; plus the v1.0.0 laundry list `notes/2026-06-14-mac-v1.0.0-laundry.md`. **Supersedes turn-83's "M4 BLOCKED".** Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-14 (🪟 Windows, turn 83 — CI health swept · Mac v1.0.0 laundry delivered · decommercialize-tail docs · K-KIN STK verdict [superseded by turn-84]).** Bootstrapped + fixed both CIs. **GitLab:** every per-push `main` pipeline was `ci_quota_exceeded` (monthly CI minutes blown ~Jun 6 — NOT code; jobs never start) → `.gitlab-ci.yml` `workflow:rules` stops per-push pipeline creation (ends the failure-email spam; GitHub Actions stays the real per-push CI; manual/scheduled survive for the quota reset). **GitHub `Tests` red `main` cleared:** Mac's KDP reword pulled (`7bec299b`) + 2 stale `test_scripts.py` pins fixed (`bcp47` = multi-`dc:language` RESTORED for non-kindle, single target-gated to kindle; `enable_ai_notes` imports `EDITABLE_BOOL_FIELDS`) — verified pass; `popup_split` 52/52. **Mac v1.0.0 laundry list:** `notes/2026-06-14-mac-v1.0.0-laundry.md` (5-dim code-verified; Mac's v1.0.0 = finish the Kindle column + tag-time DMG re-notarize + file-disjoint depth). **Decommercialize-tail docs (2nd milestone):** free-public rewrite of `RELEASE_NOTES_v1.0.0.md` + pre-pivot SUPERSEDED banner on `HANDOFF_README_v7.md` (README/VERSION already clean). **K-KIN:** WIN recorded `FIXED.epub` failed Send-to-Kindle → "restart the Kindle arc against the STK oracle (Mac)" — **DONE in turn-84 (RESOLVED).** Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **WIN lane — M1+M2 deployed (90 epubs on v0.1.0); M4 Kindle column PRODUCTIZED (turn-85), running overnight:** the proven recipe is wired into the matrix (`scripts/core/kindle_post` + `scripts/build_kindle.py` + `build_format_matrix` `post_process: kindle_safe`); catholic-study proven (epubcheck 0/0/0/0, `verify_kindle_safe` clean). **NEXT (autonomous):** build all 9 Kindle catalog artifacts via `build_format_matrix --phase M4` + gate each (epubcheck + `verify_kindle_safe` + verify_kr2_build) → `gen_release_catalog.py --tag v0.1.0` → attach to the v0.1.0 release → `node website/build.mjs` → deploy via the publish copy `C:\Users\bogda\Documents\yhwh-site-publish`. ONE Send-to-Kindle device re-confirm on a catalog artifact = the remaining nicety before declaring the column "live." **M3** Kobo column (gated: K-R4-2 cap calibration, user taps pending) · **M5** Play Books (user phone-QA on an M1 artifact). **CONTENT lane:** Phase D1b PO Esther vision marathon (paused ~p35) → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone; then D1b-batch (1ch/2ch/ezr/neh/job) + D2. Background lanes (RULES §2.5): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).
