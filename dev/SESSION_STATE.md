# Session state — current snapshot

> **➤➤➤ 2026-06-12 (LATEST, 🪟 Windows, turn 76 evening close — ★M2 COLOUR CHOICE LIVE + ★KOBO POPUP ROOT CAUSE NAMED + K-KIN user picks + 3 STANDING rules.** ① **M2 SHIPPED + LIVE:** 45 own-design composites (297a0512) · driver variant leg (`cell_asset_names`/`variant_composite_path`, 10 gated assets/edition) · "Other colours" no-JS picker + `.alt-colours` CSS · CI 27383502749 ALL-GREEN (11/11) → 97 release assets → deployed 84a2a1e → **LIVE-VERIFIED** (9 pickers · 90 links · variant HTTP 200); 5-legs `m2-variants-wired` + `m2-live`. ② **Kobo round-6c/6d (user device QA): the decisive taps landed** (1sa 16:12 + act 23:6 units 1+6 TELEPORT/middles open · gen 35:18 single TELEPORTS · gen 1:1 third confirmation) → kepub forensics + a 2-finder+synthesis workflow → **★K-R6-2 ROOT CAUSE (high conf): Nickel popup cap ∈ (8,858..9,273] serialized bytes measured by forward-scan to the next NON-PREFIX-EXTENDING anchor; our split-family HEAD ids are strict prefixes of their siblings (head slice swallows the family; 98/98 heads over cap; tails fail the same group bookkeeping) + no per-unit byte budget (~80 oversized; ~0.6% of 30,344 asides). FIX = 4 legs (prefix-free `-s1` ids · ~8,000 B split driver · prefix-lint + byte-budget gate · 6-spot retap) — full prescription in `notes/2026-06-11-kobo-round6-device-qa.md`. M3's "cap calibration" is RESOLVED by root cause, not tuning.** ▶ WIN next session: legs 1-3 TDD → round-7 build (+ chip badges/U+2028/eyebrow-nbsp already in code) → NEW-filename device load → leg-4 retaps. ③ **K-KIN (Mac lane): vnotegut PASSED ⇒ bytes-driver CONFIRMED; user picks RECORDED on the board:** trim/compact GO · per-reader popup-language CAP (kindle=2, ANY 2 of 4, builder's pick, Heb+Grk ready-made default) · Bible-wide per-language switches (no per-book mixing); Mac's LANGCAP probe (48.7MB) staged = its next oracle read. ④ **Rules/infra:** STANDING auto-pull-on-BEHIND + "pull" command word (RULES) + no-background-runs-at-wrap (user-reinforced: NEVER stop in-flight agents/shells for a fresh session — FINISH them; mirrored + ACKed both lanes) · MCP_TIMEOUT=120000 mirrored into Windows ~/.claude/settings.json (user-approved; effective next start). Round-6c/6d QA + verdict committed (e39bd34b/bcea9b13/b9dfd10f + the verdict commit). Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-11 (🪟 Windows, turn 76 — ★CATALOG REDESIGN: PER-EDITION SIGNATURE COVERS + card-grid Downloads + the band-alt CSS class fix — user-directed ("no '— black'; each edition one of the 5 colours × one of the styles; horizontal cards showing the cover we picked; merge the bands; fix the beige bands").** ① **One-home move:** `EDITION_COVER_TEMPLATES` + `edition_cover_template` → `scripts/core/config.py` (PIL-free home; generate_edition_covers re-exports/delegates, api/covers + the σ.2 pins unaffected); NEW **`build_edition.edition_cover_signature`** = (design, colour) parsed from the edition's recorded/factory stem (typo'd colour raises) — the 9 signatures span 4 designs × all 5 colours (04_minimal_lines unused by the factory map; flag if Boggy wants it represented). ② **FORMAT_MATRIX `cover_design` RETIRED** (covers are the EDITION's — spec **Addendum A**); `build_format_matrix` now COPIES the base build (its own cover is already embedded) to `cell_asset_name` (signature-colour leg) — NO swap for signature assets; `swap_epub_cover` + committed composites = the M2 leg (`catalog_colour_variant_plan` = 9 × own design × 5 colours = 45); the 18 old black format-design composites `git rm`'d. ③ **gen_release_catalog re-keyed:** column live iff EVERY edition's SIGNATURE asset is published; per-cell M2 variant colours (only where that edition's asset exists); `LEGACY_FLAGSHIP_CELL` joins the kepub legacy (never-remove-live covers BOTH flagship files while their columns are dark); renderer = **cover CARD grid** (covers/<id>.jpg + device-label buttons; NO colour-word link text; "Pick your edition" imperative emitted only in the live state); ★**`check_no_withdrawal` guard** — a regen that would darken a previously-live column REFUSES to write without `--allow-withdraw` (never-remove-live made structural). ④ **Website:** `.band-alt` centering FIXED at the class (per-child `margin-inline:auto` was silently cancelled by any child's own margin rule → the user's 5 screenshots of ragged beige bands; now a padding-inline-centered content column, every page aligned), catalog-card CSS, releases.html "Read the Ethiopian Bible"+"Choose your edition & device" MERGED into "Read the Bible — Choose Your Edition", index "Choose your cover" showcase band retired (the catalog cards showcase the REAL covers; Cover-Art source-group repointed; build.mjs STATIC drops showcase). ⑤ **Adversarial review workflow (8 agents, 2 lenses + verify): 6 confirmed findings, ALL addressed** — the no-withdraw guard (#1/#3), the 4 colliding black-signature asset names + partial-run hazard → all-green + `updated_at` freshness protocol spec'd (#2/#4), stale-asset deletion AFTER deploy + sums-prune (sums-membership ≠ provenance — the fan-in merges old lines forward) (#5), gate-aware imperative copy (#6); committed catalog.json/html kept at the LIVE state until the columns light. **Tests:** catalog arc re-pinned 62 (format_matrix 21 · build_format_matrix 10 · gen_release_catalog 25 · catalog_composites 6) + siblings green (covers 11 · cover_templates 31 · swap 7 · test_scripts generator pins 2); ruff clean. **Visual QA (Playwright, sim-full + dark dist):** desktop cards 5+4 with real variety, mobile stacks clean (below-fold blanks = lazy-load artifact, boxes reserved 349×523), dark dist honest (9 cards, flagship+kepub only, 0 apple links, no colour labels). ★**GO-LIVE COMPLETE (same turn):** 5-leg `signature-covers-shipped` (f2250dcd) → CI run 27375212586 **ALL-GREEN** (11/11 jobs, 20:25→20:37Z) → freshness ✓ (the 4 colliding names re-uploaded 20:28/20:31 > dispatch 20:25:20Z; 39 assets total = 25 + 14 new signature names) → catalog regen (guard passed; everywhere+apple lit ×9, 18 signature links) + commit b70817fb → site deployed (`yhwh-site-publish` 9f2332a→bba372f) → **LIVE-VERIFIED on www.yhwhyaway.com/releases.html** (9 cover cards + 18 signature links + covers HTTP 200 + merged band + kepub). **⑤/⑥ CLOSED (user: "delete"):** the 14 stale old-model black assets DELETED from v0.1.0 + 14 SHA256SUMS lines pruned (38→24) + final regen (25 assets, columns lit, catalog.json variant-clean −70 lines; catalog.html byte-identical ⇒ no redeploy) — **MIGRATION COMPLETE**, M2 unblocked. ★User RATIFIED the signature cover map as-is (04_minimal_lines intentionally unused — do not re-flag). Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **WIN lane — M1 catalog go-live IN FLIGHT:** CI run 27364486873 (9 edition jobs + the SHA256SUMS fan-in) building the 18 M1 assets onto v0.1.0; on green → `py -3 scripts/gen_release_catalog.py --tag v0.1.0` → `node website/build.mjs` → deploy via the publish copy `C:\Users\bogda\Documents\yhwh-site-publish` (pull → mirror dist → push; no-reply email). Then the M-ladder: **M2** colour variants (composite-swap ×5 colours; the 225-composite commit-vs-pin decision) · **M3** Kobo column (gated: K-R4-2 cap calibration, user taps pending) · **M4** Kindle column (gated: Mac kindle_safe — husk ship-fix → STK confirmation; the FORMAT_MATRIX row already exists, phase=M4) · **M5** Play Books (user phone-QA on an M1 artifact). **CONTENT lane:** Phase D1b PO Esther vision marathon (paused ~p35) → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone; then D1b-batch (1ch/2ch/ezr/neh/job) + D2. Background lanes (RULES §2.5): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). (σ "Edition Cover + Truthful Front Matter" arc COMPLETE 2026-06-04 — the old "start σ.1" framing here was stale; ρ.3 folded into σ.6.) Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).
