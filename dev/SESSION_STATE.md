# Session state — current snapshot

> **➤➤➤ 2026-05-31 (LATEST) — ✅ MINT CLEANUP (Phases 0–6) COMPLETE · ▶ MINT-7 audit DONE, plan saved, EXECUTION PENDING.** mint-6 polish shipped (5 commits `451ac84c`→`3ed0473b`: .sonar/log sweep + /exec scrub · superpowers INDEX+coherence-lint · /distribution console · audit_caches→preflight · review fixes) + truth-records `ad945f62`; the **save-semantics rule + `save-all.ps1`** shipped (`180e95d1`/`be80ad2e`). **⚠ Save now = full 5-leg sync** (local commit + GitLab + GitHub + E: + F:; RULES §4; one command = `pwsh -File save-all.ps1 -Message "..."`); "continue"/"proceed" still ≠ save, but **"push" IS now a save word**. GitLab `main` is PROTECTED (no force-push — fix forward). All synced at `be80ad2e`. **mint-7** (user: "all of them" — correctness + code-quality + security): a 15-agent audit (`wf_365eda78`) found ~30 issues (9 critical/high verified) → **plan `docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md`** (Phases A–E) + raw findings `docs/superpowers/2026-05-31-mint-7-audit-findings.md`. **▶ NEXT = execute mint-7 Phase A** (★book-code BUGCLUSTER: rebuild `tsk_xrefs.json` canonical [HIGH, ~1,525 TSK xrefs], complete `_normalize_book_code`, **delete** the 56 stale legacy candidate files [don't migrate], meta-test guard) → C security (stored-XSS `sanitize_html`, `auth.json` gitignore) → B at-scale (`ch_count` fix) → D/E. Lint 25✓/1⚠/0✗; 9 KJV editions byte-stable. **[USER]** real-reader eyeball open; Esther p28 paused (parallel lane, entry below).
>
> **➤➤➤ 2026-05-29 (prior) — ▶ PHASE D1b Task 4 — PO Esther p27 (canonical 1:4-tail completes p26 → 1:5–1:8; 1:8 cut after `ፈቃደ` → p28) RECOVERED from TWO consecutive OOM crashes + finished by a CONTROLLER verification pass; accumulator page `27` written (codepoint-clean); committed + E:/F: bundle (user remounted both drives); p28 is the next unit.** Two undocumented crashes since the p26 commit `5a7556f5`, both mid-p27: session `2a3624ca` ran 2 blind transcribers + an adjudicator then crashed (#1); session `2e57d4aa` re-rendered the crops + launched a finishing-adjudicator convergence **workflow** (`wf_fe98cd7b`: 2 recovered blind drafts vs the image + Pereira's French) which **COMPLETED its converged StructuredOutput**, then crashed before the Verify phase ran (#2). **Recovery (NO heavy vision re-run — heavy fan-out is what OOM'd both prior sessions):** recovered the workflow adjudicator's completed reading from its agent transcript, cross-confirmed by the first adjudicator attempt's independent text, then a CONTROLLER re-verification of the two load-bearing cruxes against the staged crops + French: **v7 numeral = printed `፫ ፸`** (own high-zoom confirmed a double-pillar tens form, NOT `፻`; Pereira's French "trente mille"/30,000 divergence FLAGGED, not harmonized — read-the-print) + **v7/v8 boundary = the abundant-wine sentence opens v8** (left-margin "8" beside `ዘሥናይ`; ✻ precedes `ወበዓሉስ`; French v8 "Et le vin était abondant"). Other resolutions: v5 feast word `በዓለ` (ayn-4th); v6 (long, hardest) converged per-glyph + French parallels (parenso=`ዘጸርንስ`, émeraude=`ዘሕብረ መረግድ`, fleurs de rose=`ጽጌ ረዳ`). **1:4 assembled across the p26→p27 break** (`ዕለ`+`ት`=`ዕለት` "days"; day-count `በ፻ወ፹`=180). 3 `uncertain` flags. Accumulator = **pages 24+25+26+27**. **▶ RESUME: p28** (continues 1:8 `…ፈቃደ` tail → 1:9..) — render `render_body_for_vision(doc[28], …, dpi=230, geez_top_fraction≈0.45 — re-check per page)` to OS temp (controller renders; subagents Read the PNG) → 2 blind Opus transcribers + adjudicator + calibration (per `_vision_notes` (h)–(p)); assemble 1:8 + 1:9..; Ethiopic-only codepoint-validate; write accumulator page `'28'`; commit. MAX 1 heavy agent; per-unit commits. **After all PO Esther units:** Task-4 final write `est_patrologia.py` via `po_vision_store.write_po_vision_module(...)` (DROP the p24/p25/p26 partials, use the complete verses in their completing pages) → Task 5 xref sidecar → Task 6 standalone wiring (build 5 books, epubcheck 0/0, 9 editions byte-stable) → Task 7 EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `_vision_notes.md`. **[USER]** real-reader presentation eyeball still open.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused at p28 (see the top journal entries).

## Next

> **Mint cleanup is COMPLETE.** Next critical path = **Phase D1b — PO Esther vision marathon (paused p28)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2 distinctive sources. Background lanes (RULES §2.5, auto-pick when one frees): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
