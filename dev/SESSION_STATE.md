# Session state — current snapshot

> **➤➤➤ 2026-06-04 (LATEST, 🪟 Windows) — ✅ HIERARCHICAL CUSTOMIZATION PHASE A COMPLETE (ρ.3 symbol engine): per-coordinate note-symbol resolution (Bible→book→chapter→individual + force-on), byte-stable, shipped.** Subagent-driven TDD, 6 commits (`8a751bc5`→`ea1f824e`, rebased onto Mac's `2d4927ff`). **New edition fields** (all default-absent ⇒ byte-identical): `note_families_{on,off}_per_book`, `note_families_{on,off}_per_chapter` (signed tokens = category id | kind code), `enabled_note_ids` (force-on; `disabled_note_ids` force-off already shipped ρ.1/ρ.2). **Engine:** `config.enabled_kind_codes_for(edition, all_kinds, book, chapter)` (most-specific-wins: chapter>book, kind>category, OFF>ON; phase/AI hard gates; **fast-path returns the edition-wide result unchanged when no token applies — invariant `enabled_kind_codes_for == enabled_kind_codes` holds for every real edition**) + `build_edition._iter_note_ref_symbols` (91,733 = exact corpus count) + `_symbol_overridden_kinds` + `compute_symbol_disabled_html_ref_ids` (mirrors the tradition filter, short-circuits empty) wired into `build_one` (overridden kinds resolved at ref-id granularity via `disabled_kinds_for_filter = disabled − overridden_kinds`; `enabled_note_ids` force-on subtracted LAST; **`filter_html` unmodified**). **★BYTE-STABILITY PROVEN (ship bar):** gate PASSED 187s; `epub_working/` untouched; nested-anchors clean (0/61); flagship `catholic-study` **epubcheck 0/0/0/0**; affected suites green (enabled_kinds_unified 12 · build_cache 36 · sample_kindfilter_parity 2 · mint11 6); 9 KJV byte-identical. +23 unit tests (`test_hierarchical_symbols.py`) + 2 slow e2e build tests (`test_hierarchical_symbols_build.py`: per-book OFF strips exo xref/keeps gen xref; force-on resurrects one note). **Headless** — UI/API = Phase C. **▶ NEXT: Phase B** (popup-language hierarchy — `_resolve_popup_languages` per-chapter/verse) **→ Phase C** (`/build-my-bible` navigator). Baton: `windows`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-04 (prior, 🖥️ Mac lane) — ★FIRST NOTARIZED macOS BUILD in flight + website overhaul + Geʽez-forward repo metadata, shipped LIVE (user-authorized, file-disjoint from Windows's hierarchical-customize work).** **Apple Developer signing wired** (`dev/launcher.spec`→onedir notarizable `.app` on macOS / onefile on Win-Linux; `dev/YHWH.entitlements`; `build_dmg.sh --entitlements`; `build_desktop.sh`→`.venv` python; PyInstaller declared; `VERSION`→`1.0.0-beta.1`); user made a Developer ID cert (`Bogdan Zorlescu (AAHZNDCGFW)`, +G2 intermediate) + a `yhwh-notary` profile; **`dist/YHWH-1.0.0-beta.1.dmg` built + signed + submitted** (`0c0d10c1`) → **notarization stuck in Apple's queue 90+ min (Apple-side; build verified fine)** → auto-staple + Gatekeeper verify + `SHA256SUMS.txt` pending. **Website LIVE on `yhwh-website`:** first-person mission/About reword, whole-site de-gaud (5-agent critique workflow), "fully customizable" Bible-shaped cascade copy (framed coming), 11→9 editions honesty fix, **Beta→Releases download-hub collapse** (beta redirect, nav drop), "almost here" beta state, wordmark `ኪዳን`→`እግዚአብሔር`, feedback no-phantom-comment-box, footer handles. **Geʽez emphasis:** README + GitHub/GitLab/website descriptions + profile README. Commits `4bf2ebf8`·`846223e7`/`1a9dbd28`/`dedd36d6`·`5aac549e` + profile `2c76649`; site deploys `87b1bb4`→`6ca90da`. ▶ NEXT = notarization done → staple/verify/checksums → public-launch swaps. Full detail in `dev/IN_FLIGHT.md`. Baton: `windows` (file-disjoint; user OK'd the push). TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused at p28 (see the top journal entries).

## Next

> **Mint cleanup is COMPLETE.** Next critical path = **Phase D1b — PO Esther vision marathon (paused p28)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2 distinctive sources. Background lanes (RULES §2.5, auto-pick when one frees): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
