# Session state — current snapshot

> **➤➤➤ 2026-06-04 (LATEST, 🪟 Windows) — ✅ HIERARCHICAL CUSTOMIZATION PHASE C2 COMPLETE — the `/build-my-bible` navigator FRONTEND shipped (byte-neutral).** The 4-level drill-down console: a builder navigates an edition *like a Bible* (Bible→book→chapter→verse) and toggles note-symbol families/kinds + translation-popup languages at every level. Subagent-driven (implementer + 2-stage spec+quality review per task), **8 commits `ce591d93`→`48d0f21f`** (rebased onto Mac's `2064b86d`). **C2-2** console shell + route + §6.2 cross-link (console inventory 18→**19**, `BUILD_MY_BIBLE_HTML`). **C2-3** lazy drill-down JS (clickable breadcrumb, BOOK/CHAPTER caches w/ stale-response guards, spinner, XSS-safe). **C2-4** interactive symbol tri-state (book+chapter; category→kind drill-down) + popup checklists (book/chapter/verse) saving via `PUT /api/edition-meta/<id>` (absolute-replace from a JS `WORK` working copy; **bulk-clears-finer** §3.2; **debounced+serialized autosave** so re-seed can't clobber an in-flight edit) + the C2-1 overrides gap fix (added `popup_languages_per_book`). **C2-5** verse-level note **"ships?" checkbox** + atomic `PUT /api/edition/<id>/note-override` (`default|on|off` → `enabled_note_ids` force-on / `disabled_note_ids` force-off, **mutually exclusive, force-on wins**) + server-computed `ships`/`kind_enabled` read flags. **Bible level READ-ONLY** w/ cross-links to /matrix + /customize (edition-wide already ships there). **★BYTE-NEUTRAL:** entire C2 diff touches NO `build_one`/resolver/`config.py`/`epub_working` — byte-stability gate PASSED + `git diff epub_working` empty ⇒ 9 KJV byte-identical by construction. **★VISUAL QA (live Playwright, 0 console errors):** all 4 levels render+navigate (76 canon books in canonical order, 50 gen chapters, 31 gen-1 verses); book-level xref OFF → persisted `note_families_off_per_book.gen=xref` + reflected as the verse-level xref note unchecked; verse Hebrew popup → persisted `popup_languages_per_verse[gen:1:1]`; `editions.yaml` restored byte-identical post-QA. +tests (console + c2_4 + c2_5). Per-task spec+quality reviewed (all approved after fixes). **▶ NEXT: Phase C3 polish** (/build-tracker resolved-state · website "how you make it yours" copy · reconcile /customize+/sources to surface the new chapter/verse levels). Pulled Mac's website-launch + LICENSE-all-rights-reserved work (rebased). Baton: `windows`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-04 (🖥️ Mac lane) — WEBSITE LAUNCH-SWAPS SHIPPED LIVE (items 3/4/6 + nav fix); #5 email owed to user.** Deployed to GitHub Pages (`yhwh-website` `3d03836`, www.yhwhyaway.com): Giscus comments live (Discussions enabled + giscus GitHub App installed + mapped "Website feedback" discussion #1 + IDs wired in `feedback.html`); Ko-fi (`ko-fi.com/gringoboggy`) + PayPal (`paypal.me/gringoboggy`) donation buttons live (GitHub Sponsors kept pending — `hasSponsorsListing:false`, not yet approved); nav `Support`→`Give`; feedback + customization copy → present-tense. **▶ Owed: #5 email forwarding** for `gringo.boggy@yhwhyaway.com` (passkey-gated Spaceship — user is doing it; mailto works meanwhile). File-disjoint from Windows' ρ.3 Phase C. Baton: `windows`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-04 (prior, 🪟 Windows) — ✅ HIERARCHICAL CUSTOMIZATION PHASE B COMPLETE (ρ.3 popup engine): per-coordinate translation-popup-language resolution (Bible→book→chapter→verse), byte-stable, shipped.** Subagent-driven TDD, 6 commits (`3969e1a6`→`45091d77`). **New fields** (default-absent ⇒ byte-identical): `popup_languages_per_chapter` (`"book:ch=langs"`) + `popup_languages_per_verse` (`"book:ch:vs=langs"`). **Engine:** `_resolve_popup_languages(edition, book, chapter=None, verse=None)` extended to **5-tier most-specific-wins** (verse▸chapter▸book▸default▸DEFAULT_POPUP_WITNESSES; **`is None` cascade** so explicit-empty `"gen:1:1="` = no-popups override; **backward-compat invariant** — 2-arg + no-override-4-arg both identical to before) + `decode/encode_per_{chapter,verse}_languages`; the single call site (`_apply_popup_languages_and_translation`) now threads chapter+verse. **★Stale-cache fix** (found by the byte-stability review): `build_cache._referenced_translations` now walks per-chapter/verse popup langs (was only default/per-book → a per-verse-only translation could serve a stale EPUB; +4 tests, build_cache 36→40). **★BYTE-STABILITY PROVEN (ship bar):** gate PASSED 187s; `epub_working/` untouched; nested-anchors 0/61; flagship epubcheck 0/0/0/0; suites green (popups 12 · symbols 21 · build_cache 40); 9 KJV byte-identical. +23 unit tests + 1 slow e2e build test (`test_hierarchical_popups_build.py`: per-verse override strips Greek from gen 1:1, keeps it in 1:2). Also folded in the 2 Phase-A review nits. **Headless** — UI/API = Phase C. **▶ Phase C1 (API write path) ✅ SHIPPED** (`68f1598b`: the 6 new fields — `note_families_on/off_per_{book,chapter}` + `popup_languages_per_{chapter,verse}` — wired into `api_save_edition_meta` via a DRY `_validate_keyed_list_field` helper + `_KEYED_LIST_FIELDS` table; added to `EDITABLE` + clone-carry; decoded in `api_customize_data`; +56 tests; byte-neutral). **▶ Phase C2-1 (navigator read API) ✅ SHIPPED** (`eabd00bb`: `api_build_my_bible` — lazy 3-level drill-down feed composing `api_customize_data` + `/api/sources` + the resolvers + `canonical_verse_counts`; resolved symbol/popup state per level + notes per verse; 27 tests). UX **locked = drill-down + breadcrumb** (user-chosen); C2 plan written `plans/2026-06-04-hierarchical-customize-phaseC2-navigator.md` (6 sub-tasks). **▶ NEXT: C2-2…C2-6** = the navigator FRONTEND (console shell + cross-link · drill-down JS · symbol/popup toggle controls + save · the verse/note level + `enabled_note_ids` force-on write · byte-backstop/visual-QA) → C3 polish. Baton: `windows`. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **Mint cleanup is COMPLETE.** Next critical path = **Phase D1b — PO Esther vision marathon (paused ~p35)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2 distinctive sources. Background lanes (RULES §2.5, auto-pick when one frees): code-debt tail · doc-coherence currency · **truth-record rotation gap — `rotate_truth_records.py` only archives SESSION_STATE's `➤➤➤` marker, NOT IN_FLIGHT's `▶`, so IN_FLIGHT (~51 KB / 33 entries) can't auto-rotate → soft budget warn; teach the tool IN_FLIGHT's marker (TDD; structure is irregular) + rotate to clear it** · test-coverage growth · Phase-E Clementine (1es/2es). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 19)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
