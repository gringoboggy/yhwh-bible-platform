# Session state — current snapshot

> **➤➤➤ 2026-06-04 (LATEST, 🖥️ Mac lane) — ★FIRST NOTARIZED macOS BUILD in flight + website overhaul + Geʽez-forward repo metadata, shipped LIVE (user-authorized, file-disjoint from Windows's hierarchical-customize work).** **Apple Developer signing wired** (`dev/launcher.spec`→onedir notarizable `.app` on macOS / onefile on Win-Linux; `dev/YHWH.entitlements`; `build_dmg.sh --entitlements`; `build_desktop.sh`→`.venv` python; PyInstaller declared; `VERSION`→`1.0.0-beta.1`); user made a Developer ID cert (`Bogdan Zorlescu (AAHZNDCGFW)`, +G2 intermediate) + a `yhwh-notary` profile; **`dist/YHWH-1.0.0-beta.1.dmg` built + signed + submitted** (`0c0d10c1`) → **notarization stuck in Apple's queue 90+ min (Apple-side; build verified fine)** → auto-staple + Gatekeeper verify + `SHA256SUMS.txt` pending. **Website LIVE on `yhwh-website`:** first-person mission/About reword, whole-site de-gaud (5-agent critique workflow), "fully customizable" Bible-shaped cascade copy (framed coming), 11→9 editions honesty fix, **Beta→Releases download-hub collapse** (beta redirect, nav drop), "almost here" beta state, wordmark `ኪዳን`→`እግዚአብሔር`, feedback no-phantom-comment-box, footer handles. **Geʽez emphasis:** README + GitHub/GitLab/website descriptions + profile README. Commits `4bf2ebf8`·`846223e7`/`1a9dbd28`/`dedd36d6`·`5aac549e` + profile `2c76649`; site deploys `87b1bb4`→`6ca90da`. ▶ NEXT = notarization done → staple/verify/checksums → public-launch swaps. Full detail in `dev/IN_FLIGHT.md`. Baton: `windows` (file-disjoint; user OK'd the push). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-04 (prior, 🪟 Windows, evening) — ☠ VM/CLOUD DROPPED (pod TERMINATED) · ▶▶ HIERARCHICAL EDITION CUSTOMIZATION designed + Phase A planned (the real user #1).** User dropped the cloud-pod approach (*"drop all the vm plans, doing it the way we have been"*) after the Samuel bulk **FAILED** (`1sa2` ran 2h10m → CAM error `rc=2`; chapters 2–51 instant `rc=1` fails; **0 usable output**). Pod `tvdzqrdpyw95bf` **terminated + confirmed gone** (RunPod `podTerminate`); ~$16 credit stays on the account. **Sam/Kings → the LOCAL agent-path marathon (LANE M)**; the 2 cloud docs (`plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md` + `specs/2026-06-02-samkings-cloud-draft-at-scale-design.md`) marked DROPPED. ★**Foreground (user's #1) = HIERARCHICAL EDITION CUSTOMIZATION** — navigate the edition like a Bible (Bible→book→chapter→individual), toggling BOTH note symbols AND translation popups at every level; individual-note on/off already ships (`/sources` ρ.1/ρ.2), Tier-1 edition-wide already ships (`/matrix`). Spec **APPROVED** + **Phase A symbol-engine plan WRITTEN** (`specs/2026-06-04-hierarchical-edition-customization-design.md` + `plans/2026-06-04-hierarchical-customize-phaseA-symbol-engine.md`): per-coordinate resolver `enabled_kind_codes_for` + `compute_symbol_disabled_html_ref_ids` (ref-ids already encode book+chapter+verse → no new strip plumbing) + `enabled_note_ids` force-on; byte-stable; 8 TDD tasks. ▶ NEXT = execute Phase A (subagent-driven) → Phase B popups → Phase C `/build-my-bible` navigator. ★KINGS CAM-foliation bug still stands. Baton: `windows`. TRACKER-STATE active. *(Prior headline below — its "POD LAUNCHED" is now superseded.)*
>
> **➤➤➤ 2026-06-04 (prior, 🪟 Windows) — ☁ POD SAMUEL TRANSCRIPTION BULK LAUNCHED (autonomous, resumable) · website note-removal + creed-reword DEPLOYED + Enforce HTTPS ON · ★KINGS CAM-foliation BUG found · ▶▶ NEXT #1 = build PER-BOOK note selection (before manuscript).** Full active state in `dev/IN_FLIGHT.md`; resume how-to in `.remember/remember.md`. **Pod (the big arc this session):** brought a RunPod pod (`tvdzqrdpyw95bf`, `root@205.196.17.156 -p 11489`) up from scratch — drove **Chrome** (read the live SSH string + started the pod) + stored a RunPod **API key** (`~/.runpod/config.toml`, stop/start without Chrome); installed node + Claude Code; cloned the repo via SSH agent-forwarding (auto-mode blocked the bulk scp — used git-clone-from-GitLab); authed Claude Code to the **Max subscription** (token via clipboard→base64→pod). Proved the **agent path** (`claude -p` vision passes → the deterministic `manuscript_finalize_chapter.py`). ★**Whole-folio 1568px downsample = ILLEGIBLE (3–5px/char); native-res COLUMN TILES (`manuscript_folio_crop.py` 3×3) FIXED it** (GG faithful — Hannah's prayer 1sa2). ★**Pod is RATE-LIMIT-bound, not core-bound** (shared Max-sub limit → concurrency-4 gave garbage; run concurrency-1, slow ~30–50min/pass) → the **$16 credit ≈ 25–30 of 51 chapters** ("run till money runs out"). Bulk runs `nohup` on the pod (`/root/bulk_samuel.log`, output `/root/out/`); **pull+commit from N95, pod never pushes.** **Website (Windows stepped into Mac's lane, user-directed):** removed the per-book-limitation callout + tightened the hero creed (`website/src/index.html`), redeployed to GitHub Pages (`yhwh-website` `54c3544`), **enabled Enforce HTTPS** (cert provisioned), verified live; Mac handoff in `dev/LANE_HANDOFF.md` turn 7 + `website/README.md`. **★KINGS CAM BUG:** the `1ki_b3` CAM crops are physically **1Ki22→2Ki~7**, NOT 1ki13–18 (penned folios right; §17 mapping wrong) → §17 CAM + the prior **7–12 CAM fill are SUSPECT**; GG stands; re-map CAM 1ki from the f126=1ki1 anchor (manifest not corrupted). ⚠ rotate the Claude OAuth token (`/root/.claude_oauth_token`, transcript-echoed). Site commit `ee10b142` (5-leg). Baton: `windows`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-04 (prior, 🖥️ Mac website lane) — ★WEBSITE IS LIVE at www.yhwhyaway.com (on GitHub Pages, NOT Spaceship).** All 5 pages serving (HTTP 200); HTTPS cert auto-provisioning → then enable Enforce HTTPS. **The pivot:** Spaceship cPanel/FTP are unreachable from this Mac (network blocks ports 2083+21; only 443 works — verified via cPanel SSO, the 24h access token, FTP, dashboard File Manager, + a phone-hotspot try, all dead-ended at :2083). So: built `website/dist/` → pushed to NEW public repo **`github.com/gringoboggy/yhwh-website`** (clean, no monorepo history, GH no-reply email) → Pages enabled. The domain's apex `A`/`www CNAME` were **product-locked** by Spaceship hosting (DNS API + Advanced-DNS UI couldn't remove `group:product` records) → freed by **disconnecting the domain from Spaceship web hosting** → then Spaceship **DNS API** (443) set apex `A` → 185.199.108–111.153 + `www CNAME` → `gringoboggy.github.io`. **Email (Spacemail MX/SPF/DKIM/SRV) preserved** (verified; backup `/Volumes/MacHD2/yhwh-dns-backup-*.json`). Earlier this session also shipped the website v2 shell (`4494a129`) + SHA-256 checksum tool `scripts/gen_checksums.py` (`b9b732bc`) + `runpodctl` on the Mac. **DEPLOY NOW = `node website/build.mjs` → push `dist/` to `gringoboggy/yhwh-website`** (memory `reference_spaceship_hosting`/`project_website_launch` rewritten). **▶ NEXT (fresh session, user-chosen): set up Apple Developer signing → get a notarized macOS build (cert).** Then other launch swaps: Enforce HTTPS, downloads-live + first beta build + SHA256SUMS, Giscus go-live, email-forwarding test, donation links. Spaceship web hosting now unused for the site (cancel before Jun 29 or keep; Spacemail separate). Baton: website is the Mac lane (Windows owns P0 Kings — file-disjoint). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior, 🪟 Windows P0) — KINGS 1ki 7–12 folio-mapped (both witnesses, HIGH); 1ki 13–18 GG done + CAM acquired/cropped (CAM pass pending); Samuel transcription (P4) UNBLOCKED + wired. [SESSION PAUSED for a reboot — clean resume.]** Continued P0 Kings on the N95 after Samuel completed. **1ki 7–12 mapped both witnesses, all HIGH** (CAM penned-folio + ምዕ-header confirmed; GG re-cropped-from-source HIGH; LXX-plus flagged: Hadad/Rezon ch11, 12:24a–z) → filled `kings/manifest.yaml` 1ki 7–12; kings has-folios pending 41→**35**. **1ki 13–18: GG mapped HIGH; CAM f135–f141 IIIF-acquired + cropped; the CAM 13–18 vision pass was interrupted by the reboot → RESUME there** (full GG 13–18 table + exact resume pointer in anchor index **§17**). **★P4 Samuel transcription WIRED:** Samuel 100% mapped → paid-API driver `run_manuscript_transcribe_at_scale.py` is cheap + ready (`anthropic` 0.105.2 installed, --dry-run validated, ~$0.15/ch-witness opus); **needs `ANTHROPIC_API_KEY` in `~/.config/yhwh/anthropic.key`** (load at runtime, never print); runs as a separate API-I/O process → parallels N95 Kings-mapping with NO contention (§17). **▶ NEXT (fresh session): (1) CAM 1ki 13–18 pass (crops at `D:\yhwh-folio-crops\1ki_b3\CAM\`) → fill → gate → 1ki 19–22 → 2ki; (2) once the key is dropped in, kick off Samuel transcription in the background.** Baton: `windows`. Pulled Mac's website v2 (`4494a129`/`7f5494b9`). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior, 🖥️ Mac website lane) — WEBSITE v2: static MULTI-PAGE shell BUILT, committed + pushed (`4494a129`).** Full plan via a 12-agent design workflow → user decisions locked. Rebuilt `website/` from the single-page prototype into a **static multi-page** site assembled by a dependency-free `website/build.mjs` (shared `partials/head.html`+`foot.html` → `dist/`; per-page front-matter + `aria-current`; `dist/` gitignored — edit nav/footer in ONE place). **5 pages:** index (migrated; beta CTA + ribbon; `#get-it` rewritten — **fixed the stale "unzip/no setup" copy** that described a non-existent zip; real artifact is an installer), **roadmap** (status-badged dev-stages timeline + fenced "with support, next" group — no dates), **beta** (octagon program icon, honest unsigned-warning steps — macOS notarized/clean, Windows SmartScreen interim — SHA-256 verify, run-from-source), **releases** (auto-updating latest-version card via `latest.php` + static fallback), **feedback** (Giscus comments via GitHub Discussions, lazy-loaded + self-hosted theme, mailto-first). Footer **Connect** row (X·GitHub·GitLab·Email inline SVG) + header **Code** link. `latest.php` = server-side GitHub-releases proxy (visitor IPs never hit api.github.com). `.htaccess` = HTTPS + apex→www + strict CSP (+ giscus CORS / feedback override). `style.css`: `--gold-foot` token, dropped `@font-face local()`, 44px nav targets, new components; WCAG 2.2 AA. **★HOST = Spaceship Web Hosting Essential (cPanel)** — verified on dashboard (trial→CA$5.39/mo Jun 29; 20GB; PHP/Node/cron) → hosts the static site + the PHP feed; NOT Cloudflare/GitHub-Pages (memory `reference_spaceship_hosting`). **Decisions locked:** full clean source at launch (history scrub) · first build **v1.0.0-beta.1** · **notarize macOS now** (Apple Dev paid) · publish **mailto** (forwarding first). Pre-launch placeholders for downloads/comments/version; launch checklist in `website/README.md`. NOT deployed yet (upload `dist/`→`public_html` is a separate step). Combined Windows' 2 Samuel (`f99983a1`) cleanly first. **Baton stays `windows`** (P0 Kings active; website is the disjoint Mac lane). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior) — 🪟 WINDOWS P0: ★2 SAMUEL COMPLETE (1–24, both witnesses) → SAMUEL DONE + new column-crop vision tool.** Resumed P0 on the Windows lane (baton held). ★**Method fix:** whole-folio `Read`s downsample a 7760px CAM / 2081px GG folio into illegibility (the first pass could only read rubric blocks, not names) → built reusable **`scripts/manuscript_folio_crop.py`** (native-res column×row tiles ≤1568px; +5 tests). Mapped all of **2 Samuel 1–24** both witnesses via blind crop-based passes in 4 sub-batches (1–6 · 7–12 · 13–18 · 19–24): **CAM (base) HIGH / name-confirmed** (per-column **ምዕ** chapter-number headers + **ክፍል፡ጾ** rubrics corroborate every onset; penned folios f117–f125 verified; CAM compressed — Samuel ends f125v col M, 1 Kings starts f126r), **GG cross-checks order** (strictly canonical, no transposition; GG names mostly capped by the 2081px source but the 19–24 pass recovered anchor words via higher-zoom re-crops + a red-ከፍል density scan; GG Samuel ends f028v, 1 Kings continuous). CAM 2sa band f118–f125 IIIF-acquired (`acquire_cudl_master.py`, view=215+(folio−106)×2, penned-verified). Filled `samuel/manifest.yaml` 2sa 1–10,12–24 (ch11 stays `calibrated`); anchor index **§15–§16** added. Gate `test_samkings_manifest_complete.py`: **samuel has-folios PASSES (0 pending)**; image-existence GREEN. Commits `03ac235c` (2sa 1–12, rebased onto Mac's `ff9bfe14`) + this commit (2sa 13–24). lint clean; ruff clean; tool tests 5✓. **▶ NEXT: P0 KINGS** (1ki 7–22 + 2ki 1–25; GG 100% on disk `GAPS/2_Kings/GG-00106/`, CAM 1ki ≈ f126+ via CUDL-IIIF) — same crop-based method, MAX-1 heavy vision, sub-batch check-ins, gate per batch. Folio crops cached on `D:\yhwh-folio-crops\`. Baton: `windows`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior) — 🪟 WINDOWS P0: 1 SAMUEL COMPLETE (all 31 ch, both witnesses); folio-index 18–31 added (session's first CUDL-IIIF acquire).** Continued P0 after the 1–17 batch. Two more cross-checked HIGH-confidence vision passes mapped **1sa 18–31** for both witnesses: GG companion (18–31; GG f011v–f017v on disk) + CAM (18–31; f112v–f117r — incl. the **first CUDL-IIIF acquire** of f114r–f117v via `scripts/acquire_cudl_master.py` — ★needs `PYTHONPATH=<repo>` or `scripts.core` import fails). Both strictly canonical, orders matched; CAM penned folio numbers 113–117 confirmed end-to-end. ★Recension: GG (LXX) omits 18:1–5 (ch18 opens at the women's song); CAM (MT-fuller) ch18 opens at 18:1 covenant; **1 Samuel ENDS mid-folio (GG f017v / CAM f117r) with 2 Samuel 1 immediately after** (continuous-folio, both witnesses). Rewrote `samuel/manifest.yaml` 1sa 1–31 (deterministic generator; boundary-generous; status `pending`); anchor index §14 added. Gate `test_samkings_manifest_complete.py`: image-existence GREEN; samuel has-folios now only **2sa pending (23 ch)**. **Committed `ab86dd87`** (also documents Mac's new `brand/` dir in REPO_MAP — it was blocking the `repo_map_complete` lint). Pulled Mac's turn-3 (`678253d1` brand/identity kit — **GitHub Sponsors $1–$5 tiers PUBLISHED** [pending staff review], GitHub/GitLab profiles+avatars, brand assets + `BIOS.md`; `b7f5eed3` lane-handoff + bootup env-health). **Baton TAKEN → `windows`** (Mac idle; website lane at a pause — support-link wiring + Pages deploy pending). **Full 5-leg save this session-end (PUSHED to GitLab+GitHub+E:+F:).** **▶ NEXT (fresh session): P0 2 Samuel (1–10, 12–24)** — GG on disk (f017v–f028); **CAM-IIIF acquire** needed (only f120 local — continue the view sequence past f117, verify penned numbers); same unique-event method, MAX-1 heavy vision, sub-batch check-ins, gate per batch → then 1ki 7–22 → 2ki 1–25. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior) — 🪟 WINDOWS P0: 1 SAMUEL 1–17 DUAL-WITNESS FOLIO MAP COMPLETE (dense-section wall RETIRED) + monetization plan authored.** Resumed P0 Sam/Kings folio-index on the Windows lane (file-disjoint from Mac's website lane). **★Method breakthrough:** the prior "dense-section wall" (KJV-landmark vision gave geometrically-impossible 1sa 7–16 assignments) is RETIRED by **unique-event anchoring** + recension awareness (GG = LXX/Kingdoms-compressed; CAM = MT/printed-fuller) — and I re-verified the wall's "LXX ~45% shorter" assumption = FALSE for the on-disk Swete LXX (it's ~KJV; the real culprit was recurring-phrase anchoring, not versification). Three HIGH-confidence, cross-checked vision passes (CAM keystone 7–17 · GG companion 2–17 · CAM 1–6 re-verify), each onset pinned on a unique event (Kish/donkeys, Nahash's eye-threat, Agag, Jesse/harp, Soko/Goliath…); **penned folio numbers 106/108–112 read directly confirm the view↔folio arithmetic** (f106r=view215). **★Found + corrected a real data bug:** the on-disk CAM filename chapter-labels are shifted ~+3 (the prior session's provisional 1sa 4–11 CAM map was WRONG; the calibration `f111r=1Sam17` label is mislabeled — ch17 onset is f111r-R, but its folio list was already correct). Rewrote `content/manuscript/samuel/manifest.yaml` 1sa 1–17 for BOTH witnesses via a deterministic generator (boundary-generous folio lists onset→next-onset, no tail dropped; status stays `pending`). New reusable `content/manuscript/_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` (method + per-chapter unique-event anchor tables for all 4 books + the §12/§13 confirmed maps + image-availability inventory: **GG 100% on disk all 4 books; CAM partial on disk → CUDL-IIIF for the rest**). Gate `tests/test_samkings_manifest_complete.py`: image-existence GREEN (every referenced file resolves); has-folios failures 43→37 samuel (remaining = 1sa 18–31 + all 2sa + all Kings). **Commits `8b6cb947` (P0 map+index) + `683bf66e` (monetization plan).** **Also authored `plans/2026-06-03-monetization-and-sustainability-plan.md`** (4 revenue layers by speed-to-cash: donations / print-on-demand / hosted open-core builder / commissions+grants; **inviolable: the Word + digital stay free**, revenue only on convenience/artifacts/support/grants) — the Mac website lane is wiring the support links (Ko-fi `ko-fi.com/gringoboggy` live, PayPal handle pending, GH Sponsors after enrollment). Pulled Mac's commits (`746a0546` font self-host + homepage rebuild, `361462ca` README). **Baton TAKEN → `windows`.** **▶ NEXT (fresh session): continue P0 — 1sa 18–31 (GG on disk + the first CAM-IIIF acquire) → 2sa 1–24 → 1ki 7–22 → 2ki 1–25; same unique-event method, MAX-1 heavy vision, sub-batch check-ins + manifest gate per batch. Then P1 pod run waits on the user's pricing-trigger (or grind locally free).** TRACKER-STATE active.
>
> **➤➤➤ 2026-06-03 (prior, Mac) — 🖥️ MAC 2nd-LANE PROVISIONED · baton adopted · WEBSITE prototype built then RE-SCOPED.** Full session on the **Mac (2017 iMac) 2nd lane**. **Bring-up COMPLETE:** uv Py3.14 + deps · **Tesseract via conda-forge/micromamba** (macOS 13 = Homebrew-unsupported → from-source fails; `amh`+`script/Ethiopic` tessdata_best, OCR-proven on the parallel-Bible PDF) · GAPS (697) + OCR/source PDFs placed & verified · **SSH push to GitLab+GitHub wired** (passphrase ed25519 in Keychain; `gh`/`glab` side-loaded; Claude pushes via the launchd agent). **Full suite: 7,385 pass / 12 explained NON-regressions, ZERO real** (5 tau6x1 OCR = Claude-Bash sandbox `/tmp` artifact → pass w/ `TMPDIR` on MacHD2; 3 perf = iMac budget delta; 2 samkings = the P0 WIP gate; 1 hardening backslash = Win path; 1 ruff = transient). **Adopted the lane-handoff baton** (`dev/.lane=mac`, took baton turn 1; SessionStart auto-hook pending user approval — auto-mode blocked the settings edit). **WEBSITE:** 5-agent planning workflow → built a Phase-1 manuscript-reverent prototype (`website/index.html`+`style.css`+9 cover thumbs) → **★USER RE-SCOPE: the site is the YHWH *program*'s (the builder's) HOME PAGE, NOT a Bible-download portal — NO EPUBs/downloads.** Decisions: **showcase + get-the-code** (open-source homepage) · **example-editions gallery** (covers as 'what it builds', no downloads) · keep the manuscript-reverent look → the prototype's download-portal CONTENT must be rebuilt to the program-homepage framing (CSS/shell reusable); this **removes** the old 'export named EPUBs' Phase-0 blocker. Combined Windows' P0 (1sa 7–11 mapping + `fill_manifest_entry.py`) by rebase + **pushed both remotes**. **▶ NEXT (new session): rebuild `website/` content to the program-homepage framing** per the re-scoped `plans/2026-06-03-website-plan.md` (see its ⚠ RE-SCOPE block) → then self-host Noto Ethiopic fonts + 2 missing covers + `build_site.py` generator + GitLab Pages deploy. (Windows owns P0; cloud pod paused.) Full Mac setup + the `TMPDIR`/OCR gotcha = memory `reference_mac_dev_env`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ☁ CLOUD LANE PAUSED (frugal) + CORRECTED PLAN WRITTEN + PHASE-0 GATE STARTED.** Deployed the A5000 pod → verified SSH/env (direct TCP/SCP works; 128-core host, no OOM; Python 3.12 + git present, node absent) → **STOPPED it ($0.00/hr displayed; ~$0.08 used)** at the user's direction: wait for good pricing (user watches), produce a full concrete plan first, user triggers the real feed. **★KEY CORRECTION uncovered:** `run_manuscript_transcribe_at_scale.py` is the **paid-API path** (`ANTHROPIC_API_KEY`, out of budget per [[reference_runpod_cloud_budget]]) — the cloud spec's "just run the existing drivers" is WRONG; the run must use the **AGENT path** (Claude Code on the pod via the Max subscription; vision sub-agents Read crops). Clean seam: `assemble_witness(model_out,…)` is **pure**, so a sub-agent returning the same `model_out` is a drop-in for the API call — everything downstream (`collate_base_structured`/apparatus/store) is unchanged tested code. **(1) New plan** `plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md`: P1 build the agent-path `.claude/workflows/samkings-dualwitness-batch.js` + a converge gate + an assemble-shim (TDD on the calibration chapters, on the N95, free) · P2 corrected bring-up (lean **75 MB tarball** over scp — NO GitHub credential on the pod · setup-token · GAPS) · P3 pilot+measure · P4 bounded run + budget guard; frugal pod lifecycle. **(2) Phase-0 STARTED:** completeness gate `tests/test_samkings_manifest_complete.py` written + **RED** → confirms **93 pending chapters** (samuel 51, kings 42); **GG fully on-disk**, **CAM hires partial** (CUDL-IIIF needed for Sam-pending + 1ki13–22 + all 2ki). **(3) Transfer lesson:** full-repo `git --all` scp to a raw IP trips the auto-mode exfil classifier (correct) → lean tarball + one-time approval. **(4) ✅ P1 BUILT + PROVEN end-to-end on 1ki1 (free, N95):** the agent-path workflow `.claude/workflows/samkings-dualwitness-batch.js` (reads run-config from a JSON file via a bootstrap agent — Workflow `args` don't propagate here; `sequential`=MAX-1) + `scripts/manuscript_{render_crops,assemble_witness,finalize_chapter}.py` + `scripts/core/manuscript_converge.py` (converge + `sanitize_model_out` [strip ❈, clamp uncertain idx, downgrade orphan illegible] + `renumber_verses`); 15 tests, mypy clean. The **1ki1 proof** (`wf_7d11849e-21a`, 4 MAX-1 blind passes, ~1.9h/603k tok) produced faithful diplomatic Ge'ez → after the sanitize/renumber fixes BOTH witnesses validate + collation succeeds (base=CAM, **witness agreement 56.97%=417/732**, base_structured_ok=True). ★Finding: pass-convergence is verse-binary (reads low GG 11%/CAM 2%) vs ~57% token-level → refine the gate to token-level (proposed, non-blocking). Also added `HOW_I_WORK.md` (portable global rules) + the plan's foolproof parallel-operation protocol (single-committer=N95; file-disjoint lanes; shared-quota-bounded). **▶ NEXT: user authenticates (GitHub+GitLab) → pull repo onto the Mac (2nd full lane, its Claude self-provisions) → token-level convergence refinement · P0 folio-mapping. Pod run waits on the user's pricing-trigger.** (Prior headline below.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ☁ RUNPOD CLOUD LANE SCOPED + PLANNED + ACCOUNT PREPPED (the $17 Sam/Kings draft-at-scale program).** This session also: brainstormed→spec'd→planned the cloud lane, AND drove the RunPod console to a deploy-ready state. **(1) Spec** `specs/2026-06-02-samkings-cloud-draft-at-scale-design.md` (3 keystone decisions: draft-at-scale+auto-converge · full dual-witness per chapter · **Approach B** pre-index folios). **(2) P0 plan** `plans/2026-06-02-samkings-folio-index-p0-plan.md` — ★KEY REALIZATION (codebase exploration): the **whole dual-witness pipeline ALREADY EXISTS** (`scripts/core/manuscript_*.py` + `run_manuscript_{transcribe,review,collation}_at_scale.py`), so P0 = **complete the existing `content/manuscript/{samuel,kings}/manifest.yaml`** for ~92 pending chapters (1sa 2,4-16,18-31 · 2sa 1-10,12-24 · 1ki 7-22 · 2ki 1-25) — a vision-data task, NO new code; witnesses = **CAM (Cambridge Add 1570, base) + GG (Gunda Gundē, 2nd)**; CAM hi-res for pending chapters needs CUDL-IIIF acquisition. **(3) Service validated:** 152 manuscript-pipeline tests pass locally (pipeline sound pre-port). **(4) RunPod account READY:** logged in (GitHub OAuth) · $17 · chosen pod **RTX A5000 24GB VRAM/50GB RAM/9 vCPU @ $0.28/hr** (CPU pods >2 vCPU were out-of-capacity; GPU wasted but available; ~60 hrs / ~7 parallel agents) · **SSH key fixed account-wide** (had a stray `❯ ` prompt-prefix that would've broken SSH — caught + fixed + reload-verified clean) · the deploy "override" = the needed TCP-22 SSH port (kept). **NOT deployed** (deploy = the user's $-click; no idle burn). **Runbook** `docs/superpowers/notes/2026-06-02-runpod-bootstrap-runbook.md` makes a fresh session a clean deploy→connect→bootstrap→smoke-test. **▶ NEXT (fresh session, user-initiated): USER clicks Deploy → Claude grabs the SSH string + bootstraps (Claude Code + setup-token + clone + ~1GB GAPS upload) → smoke test (suite + 1 folio-vision, measured $/folio) → then the real run.** (Esther p34 — prior headline; the marathon continues in parallel.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 34 DONE (canonical 3:2-tail + 3:3–3:8; complete 3:2 assembled; 3:8 cut at `…ዘኩሉ ፡ አሕዛብ` → p35).** ★The 2 blind passes (`wf_7c0886f0-9e5`, SEQUENTIAL MAX-1-heavy) DIVERGED **and SHARED a misread** → CONTROLLER adjudication (crisp crux crops + French + apparatus) → **new failure-class (v): two INDEPENDENT passes can converge on the SAME wrong glyph; A=B agreement ≠ proof — verify load-bearing glyphs on a fresh crop.** ★**v6 onset `ወፈቀደ`** ('and he sought [to destroy]') — BOTH passes misread `ፈ`→`ረ`; the crisp crop settled it. ★**3:2 tail** ends in the contrastive enclitic **-ሰ** (`ወመርዶኬዎስሰ` = 'BUT Mordecai alone did not bow to him', cf. 2:20 `ወእስቴርሰ`). ★**v4** carries a genuine 2nd clause (`መርዶኬዎስ ፡ አይሁዳዊ ፡ ውእቱ` 'Mordecai is a Jew') that pass B UNDER-read; v8 pass A read the full last lines (controller-confirmed — NOT an over-read, (q) holds). ★**v7** (faintest): year `በ፲ወ፪`, the lot `ዕፁ` cast for the **14th of Adar** (`ዓሡሩ ፡ ወረቡዑ`; LXX, not the MT's 13th). Story: Mordecai refuses to bow → Haman rages (`ወተምዐ ፡ ጥቀ`) → resolves to destroy ALL the Jews → casts the lot → opens his accusation. Names: Mordecai `መርዶኬዎስ`, king `አርጤርክስርክሴስ`, Haman `ሃማ`. Accumulator pages 24–34; codepoint-clean; 5-leg saved. **▶ NEXT = p35 (banner `III, 9 — B, 1`: completes 3:8 → 3:9–13 → Addition **B** begins — the first interleaved LXX Addition since A).** (p33 — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 33 DONE — ★CHAPTER 3 BEGINS (canonical 2:19-tail + 2:20-23 + 3:1-2; complete 2:19 assembled; 3:2 cut → p34).** ★A CLEAN high-agreement page — both blind passes agreed AND both honored the new **(u)** interpolation discipline (no MT eunuch-names; conspirators stay the unnamed `፪ ኅጽዋን`), so **NO adjudicator needed** (controller-adjudicated via French + 2 crops + apparatus). **★DIVINE NAME `እግዚአብሔር`** (2:20 — first since Addition A). **★Haman enters:** `ሃማ` son of Hammedatha (`ዘአመዳቱ`) the Bougaean (`ብግያዊ`), exalted above the king's friends (3:1). Demonstrative MIXED on one page: `ዝንቱ` (3:1, plain) vs `ለግዝንቱ` (2:23, ግ-onset) per (k). ★The (u) guard VALIDATED — it kept BOTH passes faithful (the positive counterpart to p32's fabrication-catch; the method is now self-reinforcing). Accumulator pages 24–33; codepoint-clean; 5-leg saved. **▶ NEXT = p34 (opens with the 3:2-tail `ወመርዶኬ…` → assemble complete 3:2 = Mordecai's refusal to bow, then onward).** (p32 — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 32 DONE (canonical 2:13-tail + 2:14–19; complete 2:13 assembled; v19 cut → p33).** ★★The adjudicator caught pass A **FABRICATING two MT-plus clauses** absent from this LXX-based Ge'ez — v17 `ወአንገሣ ፡ ህየንተ ፡ አስጢን` ('queen instead of Astin') + v19 'virgins gathered a 2nd time' — both REMOVED (read-the-print over the MT/French → **new failure-class (u)**: a glyph-seeing pass can still *interpolate* standard-text clauses; verify verse ENDINGS, not just glyphs). Confirmed: keeper `ጋይ`/Hegai (not Shaashgaz), Esther's father `አሚናዳብ`, v16 **TWELFTH month `አዳር`/Adar** (not the MT's tenth), king `አርጥርክስርክሴስ`; `ኀበ`→`ነበ` orthography this page. ⚠ CORRECTION to the prior p32-launch note: the Ge'ez does NOT say 'crowned in Vashti's place' — that was pass A's fabrication. Accumulator pages 24–32; codepoint-clean; 5-leg saved. **▶ NEXT = p33 (opens with the v19-tail `ይጸመድ…` → assemble complete 2:19, then onward).** (p31 — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 31 DONE (canonical 2:7-tail + 2:8–13; complete 2:7 ASSEMBLED; v13 cut → p32).** Esther gathered with the maidens to Susa under the eunuch `ጋይ` (Hegai), finds favor, conceals her people, the 12-month beautifying regimen. Mixed-reliability blind passes → a dedicated **ADJUDICATOR** (102 tool-uses, own crops). **★READ-THE-PRINT SAVE:** v12 pass A harmonized 'myrrh' (`ዘከርቤ`) toward Pereira's French, but the printed Ge'ez has **NO** myrrh — it says `ዕፍረት` ('scent/perfume'); the adjudicator restored the print (extends failure-class **(p)**: the editor's French can be *interpretive* — follow the Ge'ez). Also corrected: v8 onset `ወሰበ ፡ አዘዘ` (king ORDERED, not 'heard'), Susa `ሱሳ`, Mordecai `መርዶኬዎስ`, the months in **Ge'ez numerals** `፲ወ፪`/`፮`. Accumulator pages 24–31; codepoint-clean; 5-leg saved. **▶ NEXT = p32 (II,14-19; opens with the v13-tail `…ፈቀ` → assemble complete 2:13, then 2:14-19).** (p30/ch.2-begin — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 30 DONE — CHAPTER 2 BEGINS (canonical 2:1–7-head; v7 continues → p31).** Both blind passes unreliable (pass A GARBLED — skipped v4, Nebuchadnezzar→`ሮቦአምና`; pass B wrong on many interiors) → a dedicated **ADJUDICATOR** convergence (the (t) step, 95 tool-uses, own crops). **★Two names DISTINGUISHED:** the queen **`አስጢን`** (Astin/Vashti, vv1,4 — *both* passes misread Esther) vs **`አስቴር`** (genuinely Esther, Mordecai's reared ward, v7). **★Demonstrative `ግዘንቱ`** (ግ-onset) in the ch.2 head — failure-class **(k)** (p26's ch.1 head was plain `ዝንቱ`). Genealogy with ዘ-connectives `መርዶኬዎስ ፡ ወልደ ፡ ኢያኢሩስ ፡ ዘሴምዩ ፡ ዘቄስዩ ፡ ዘእምነገደ ፡ ብንያም`; `ናቡክድናጾር` (Nebuchadnezzar). Margin 1,2,3,4,5,`6-7`. Accumulator pages 24–30; codepoint-clean; 5-leg saved. **▶ NEXT = p31 (II,8-13; opens with the unnumbered v7-tail → assemble the complete 2:7, then v8-13).** (Esther ch.1 complete — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ ESTHER VISION: PAGE 29 DONE — ★CHAPTER 1 COMPLETE (vv1–22, pages 26–29).** p29 = canonical 1:16-tail + 1:17–22 (Memucan's decree closing ch1; v22 clean, not cut). **★The HARDEST page yet — BOTH blind passes UNRELIABLE** on the faint decree prose (pass B dittographed vv19–21), so the faithful reading needed a CONTROLLER multi-tile read + a dedicated **ADJUDICATOR agent** (rendered its own high-zoom crops; reconciled A/B + Pereira's French + the printed apparatus). Caught: queen `አስጢን` (BOTH passes misread Esther), `መገብተ` governors (both read መንግሥት), v17 onset `ወነገሮሙ`, **a GENUINE doubled `አበየቶ ፡ ለንጉሥ` clause — apparatus-confirmed (M/Q mark it `o`=omitted → the BASE prints it)**, v19/v20 boundary fix, v22 verb `ይፈርህዎሙ`. New `_vision_notes` failure-classes **(s)** heavy-wrap margin-layout (numeral beside the prior verse's wrap-line; onset at the ✻) + **(t)** both-passes-unreliable → adjudicator-convergence + apparatus-arbitrates-apparent-doubling. Codepoint-clean; accumulator pages 24–29; 5-leg saved. **▶ NEXT = p30 (canonical chapter 2 begins).** (New re-verification PROGRAM spec also committed this session — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — 🆕 NEW PROGRAM SCOPED + SPEC COMMITTED: Tewahedo Re-Verification & Triaged Re-Ingest (user-directed).** Use Opus 4.8 + 1M context + ultracode + the high-fidelity transcriptions already made to **(Track 1 / LANE Q)** QA the existing own-vers work (collation 1sa/2sa/1ki · Psalms · Esther · the EN back-translations) to the 4.8 standard, and **(Track 2 / LANE D-scaled)** replace the weak 2026-05-15 OCR bulk via **TRIAGED** own-vers re-ingest — route each book to the cheapest *faithful* method (**parse** clean-Unicode / **printed-vision** critical-ed / **dual-witness** manuscript-only / **qa-in-place**). **Hardware-PORTABLE** (concurrency = a profile knob: `n95` now / `mac` later — the user's possible Apple desktop = a drop-in accelerator that lifts the heavy-vision cap). **Sequencing A (user-picked): finish Esther proof → Track-1 QA in parallel → Track-2 scale.** Honesty contract unchanged (no fabrication/harmonization, apparatus excluded, byte-stable 9 KJV editions). Spec `docs/superpowers/specs/2026-06-02-tewahedo-reverification-and-triaged-reingest-design.md` (committed; INDEX +1 → 46 docs). **▶ NEXT: user reviews the spec → then `writing-plans` for P0 (shared substrate: source-triage recon + hardware-profile config + the reusable Track-1 QA harness).** (Esther p28 done — prior headline.) TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — ▶ PHASE D1b RESUMED — PO Esther vision marathon: PAGE 28 DONE (canonical 1:9–16 + the 1:8 tail).** 2 blind Opus passes (Workflow `wf_d49c0422-6d2`, SEQUENTIAL/MAX-1-heavy) + CONTROLLER crux-crop adjudication vs Pereira's French AND the printed apparatus (MNPQ). **★A 3rd fresh pass (B2) was REJECTED for HARMONIZING to the standard LXX/Hebrew text** — it fabricated cut-off v16 + substituted the standard eunuch names (Mehuman..Carcas) contradicting Pereira's French → **new failure-class `_vision_notes (q)`** (a vision pass that can't resolve the glyphs recites the standard text; detect + reject wholesale) + **`(r)`** (the printed apparatus is a 2nd calibration anchor alongside the French, excluded from the verse). Resolved: **v12/13 boundary** (v13 starts `ወነገሮሙ` — margin 13 + LXX + French; A mis-split); **3 princes** `አርቄስዮስ/ስርስቲዮስ/ማግሌ`; **v10 prints 8 `ለ`-tokens vs 7 French/LXX names** (8th `ወለዢ` flagged, not harmonized); **name1 printed `ገማ`** (read-the-print vs apparatus/French `ሐማ`). Accumulator pages 24–28; codepoint-clean. **Background lane:** D2 source-readiness recon shipped (`docs/superpowers/notes/2026-05-28-d2-source-readiness.md` — 1 Enoch **GO parse-first** via Charles-1906 Ge'ez OCR; Jubilees **vision-required**). 5-leg saved. **▶ NEXT = p29** (continues canonical 1:16 → 1:17..). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-02 (prior) — 🧹 MEMORY HYGIENE + SELF-MAINTENANCE TOOLING (out-of-repo memory; user-directed declutter + a self-pruning/self-updating system).** Reconciled all 70 memories vs the live repo (8-agent read-only sweep): **deleted 9** stale, **trimmed** `user_boggy_partnership`, **reframed** `project_deadline` (deadline dropped → a quality/cost-over-speed doctrine), **fixed 21 drift + every dead wikilink**; rewrote `MEMORY.md`; `memory_hygiene.py audit` = **0 warn**. **NEW LOCAL tooling:** `dev/cc-hooks/memory_hygiene.py` (audit·backup·propose-prune·archive — read-only by default, REVERSIBLE archive, PROTECTED list, auto-backup-before-mutate, never C:) + `tests/test_memory_hygiene.py` (10) + the **SessionStart hook now runs `audit --quiet`** each session + `.claude/workflows/memory-reconcile.js` (deep semantic sweep). **First off-machine memory backup E:/F:** (the dir is OUTSIDE git → the 5-leg save never covered it). Swept stale `.claude/.sonar`. lint 27✓/1⚠/0✗; mypy clean. **▶ Project critical path UNCHANGED = Phase D1b PO Esther vision marathon (paused p28).**
>
> **➤➤➤ 2026-06-02 (prior) — ✅ MINT-11 COMPLETE — Phases 5 + 6 (build-path) SHIPPED & byte-proven + the deep-audit ENGINE round-5 fix landed. The audit arc STOPS here (no mint-12 until project END, per [[audit_cadence]]). ▶ NEXT critical path = Phase D1b PO Esther vision marathon (paused p28).** One closeout commit (5-leg). **Byte-stability gate PASSED 213s** (editions valid+distinct, flagship rebuild deterministic); lint 26✓/2⚠/0✗ (both warns pre-existing: SESSION_STATE freshness + size-budget); mypy clean; +6 new tests (`test_mint11_phase56.py`) + 119 affected-suite tests green.
> - **Phase 5a (DEFENSIVE byte-stability):** `matter_pages.build_merged_topic_index` iterates `sorted()` keys + tiebreaks casefold-equal displays by the raw string. Empirically the merge invariant (each group's display casefolds to its own key; distinct groups → distinct keys) ALREADY made it deterministic, so this is contract-hardening (future-proofs a `_norm_topic`/strip-set change), NOT a live bug — a cross-launch 2×PYTHONHASHSEED subprocess test pins it.
> - **Phase 5b (cache-key completeness):** `build_cache._referenced_translations` maps each popup witness through `VERSION_REGISTRY[vid]["translation_id"]` to the real `content/translations/<dir>/` id — 4 of 5 defaults (lxx-greek/greek-nt/vulgate/arabic) had hashed a non-existent `<key>` dir (`<missing>`). One-time cache-key change, EPUB bytes unchanged.
> - **Phase 6 (annotation count — PLAN CORRECTED with real data, ★re-verify-with-data):** override now subtracts only disabled notes the matrix total ACTUALLY counted (in canon **AND** enabled-kind), via new `_count_in_scope_disabled_ref_ids`. **The plan's canon-ONLY intersection was empirically insufficient** — probe (catholic-study + 1700 ceiling): raw `−49,027`; canon-only `−47,362` (STILL negative); canon+kind `+825` ✓ (kind-overlap term = 48,187 notes). No `max(0,…)` floor (≤ total by construction). **Byte-identical for all 11 committed editions** (every one has an EMPTY disabled set → override stays None; the bug is latent until a user sets a time/tradition filter via /customize).
> - **Engine round-5 fix (`deep-audit.js`):** `runSkepticPanel` TOPS UP null skeptic votes ONCE (a null = the sonnet verifier skipped the forced StructuredOutput tool, ~22% in round 4 — NOT a refutation); a panel still empty after retry is carried as **UNVERIFIED** (a survivor flagged for manual triage — threaded through log / synth count / survivor JSON) instead of being silently auto-refuted (the false-negative that lost 2 HIGHs round 4). Verify stays sonnet-pinned (4-core cap=2 can't afford ~8h Opus). node-validated.
> **DEFERRED (low value, by design):** #3b corpus_index parse-warn · matrix.py:308 None/[] split · Torrey 630→628 (unverified) · web_sources dead-code comment · HIGH-#A defensive warn. Detail: `plans/2026-06-02-mint-11-fixes-plan.md` (Status). TRACKER-STATE idle.
>
> **➤➤➤ 2026-06-02 (prior) — 🛠 MINT-11 deep-audit (round 4) COMPLETE → FIXING overnight · run `wf_4b250c81-348` / task `w2wxhtf4h`.** **RESULT: 64 deduped → 30 survived / 34 refuted (med 13 · low 14 · info 3; NO high/crit — convergence signal after 3 prior rounds).** Artifacts: `docs/superpowers/notes/2026-06-02-mint-11-{findings.md,audit-raw.json}` + indexed `plans/2026-06-02-mint-11-fixes-plan.md`. **⚠ 21/95 agents missed StructuredOutput (sonnet pinning cost) → empty-panel auto-refutes = false-negative risk; hand-triaged (findings.md): 2 HIGH candidates to re-verify (`_iter_note_ref_*` book-drop · `build_cache` cover-key) + coord-guard class-sweep.** **Phases 0,1,2,3,4 ✅ SHIPPED — 5 checkpoints (`257bfd51`→`d7804f13`), all 5-leg:** P1 6 stale tests→200 green · P3 HIGH-#B `build_cache` cover-key + `source_dates.py` in `_PIPELINE_SCRIPTS` + `is_output_current` composes `_PIPELINE_SCRIPTS` + coord-guard on run_xref/run_kenyon · P4 promote within-batch dedup + corpus_index fingerprint-before-swap + update_book_floors `len(None)` guard + standalone_store atomic ×3 · P0 corpus 67,715→91,733 (×5) + tests/plans counts · P2 test-hardening. **2 empty-panel HIGHs hand-verified: #B real (FIXED), #A LATENT.** **▶ REMAINING (next session, BUILD-PATH → run the byte-stability gate): Phase 5** (topical.xhtml `sorted()` PYTHONHASHSEED determinism + build_cache popup-witness resolution) **+ Phase 6** (annotation-count canon-scope — CHANGES catholic-study output). Detail: `plans/2026-06-02-mint-11-fixes-plan.md` (Status) + `notes/2026-06-02-mint-11-findings.md`. (Original launch entry continues below.)
>
> **➤➤➤ 2026-06-02 — 🚀 MINT-11 deep-audit (round 4) LAUNCHED overnight · run `wf_4b250c81-348` / task `w2wxhtf4h`.** Engine `.claude/workflows/deep-audit.js` hardened pre-launch: **ROUND→4 · NOW→2026-06-02 · find+verify agents pinned `sonnet`** (4-core N95 → Workflow concurrency cap=2; sonnet ≈3h vs Opus ≈8h — the [[audit_cadence]] fix; synth+completeness stay on inherited Opus, post-barrier so they don't affect the starvation window). 15 dims, REPO abs-path unchanged, 4 deferred-by-design fed in. Pre-launch env: sleep/hibernate already **Never (0)** AC+DC (mint-10 died on a manual shutdown, not idle-sleep), ~0.55 GB RAM reclaimed, ssh-agent key loaded (prompt-free push), tree clean. **⚠ mint-10's "LAST audit round" directive is SUPERSEDED by [[audit_cadence]] (2026-06-02: mint-11 GO, run overnight, user asleep ~7h → full autonomy).** On the completion notification: parse `result` (survivors/dropped/counts/fixesPlanMarkdown/completeness), write `docs/superpowers/notes/2026-06-02-mint-11-{findings.md,audit-raw.json}` + an INDEXED fixes plan, then fix ALL survivors (TDD · byte-stable · sweep-the-whole-class · marathon-core untouched) with **DENSE checkpoint commits per coherent batch** (user-requested 2026-06-02). The 4 mint-10 deferred opt-items (#11 filter_html · #12 _resolve_popup_languages · #13 render_coverage EN-lane · #6 candidates_written) may re-surface via opt-build/opt-render → fold into the plan. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-01 (prior) — ✅ MINT-10 fixes Phases 4+5+6 SHIPPED (no build-path change → 9 KJV byte-stable by construction; 4 opt-tier items deferred).** **P4 (★BUGCLUSTER):** all 8 at-scale drivers now delegate `write_queue` to ONE `at_scale_base.append_candidates` (status-preserving `(verse,kind,draft_body)` dedup) — the 4 kind-replace drivers (hebrew/greek/ai_xrefs/ai_notes) no longer PURGE their own kind (which reset `promoted`→`pending`); `_normalize_book_code` now applied to `--books` on all 4 (hebrew/greek inline + ai via `resolve_books`); naves/torrey get a `coord_in_canonical_extent` guard; 5 stale impossible-chapter candidate files deleted; 31 orphaned imports cleaned. **P5 (console XSS):** escaped reflected server errors + corpus-sourced fields across audit/sources/customize/export/matrix_app (7+ sites); WLC ingest now fail-closes on raw `<>&` in tokens (mirrors lxx/byzantine); `exports.py` validates `version` (reuses `snapshots._VERSION_NAME_RE`) before subprocess+glob. **P6 (test/concurrency/docs):** cyril rebalance ceiling→non-Cyril FLOOR + plurality test → all-5-rival Counter; `corpus_index.connection()` `_CONN_LOCK` (fd-leak race); `web_helpers.html_ref_id_from_note_id` bxx fallback (Strategy-B editor nav); `batch_promote_xrefs` marks promoted candidates; `_canons_index` lru_cache; conftest unused-monkeypatch drop; REPO_MAP core 65→66; INDEX mint-9→Shipped. **+2 test files (mint10_phase{3,4}) + 2 chi tests updated to the corrected preserve/append contract. ~1,200 tests verified green (at-scale 122 · cyril/promote/mint10 764 · corpus/web/core 151 · WLC/export 132 · build_cache 36); lint 27✓/1⚠/0✗; mypy clean.** **▶ DEFERRED to a fresh session (opt-tier; the 2 build-path ones carry a byte-stability proof obligation):** #11 ρ.1 `filter_html` two-stage scan · #12 `_resolve_popup_languages` per-file hoist · #13 `render_coverage` EN-lane (+test shape) · #6 `candidates_written` stat (superseded by the P4 append_candidates return-contract). **⛔ mint-10 = the LAST audit round — after the 4 deferred opt-items ship, STOP (no mint-11 until project END).** TRACKER-STATE active.
>
> **➤➤➤ 2026-06-01 — ✅ MINT-10 fixes Phases 1(leftovers)+2+3 SHIPPED & byte-proven.** **P1:** `audit_log._last_line_hash` now falls back to the most-recent prior monthly sibling so a new month's first line chains to the previous month's last (was genesis → `verify_chain` read "broken" every rollover; +cross-month regression test); `inject._aside_existing_re` lru_cache whitelisted → `audit_caches` clean (38: 25 clear-path / 13 whitelisted). **P2 (build-cache completeness — additive, byte-stable):** `_PIPELINE_SCRIPTS` += `core/popup_versions.py`+`core/traditions.py`; `_referenced_translations` resolves popup-lang aliases via `resolve_version_id`; cache key += naves/torrey topical JSONs; `is_output_current` += matter_pages/epub_utils/kinds/categories/books/themes; `EBIBLE_VPL_TO_PROJECT` added to the `bookcode_canonical` lint. **P3 (silent-data-loss HIGHs):** `_iter_note_ref_attribution_years` `<9`→`<8` — 8-field undated notes are now correctly disabled by `time_filter_ceiling` (were silently surviving the filter); `_append_cloned_edition` now carries `enabled_categories/enabled_kinds/disabled_kinds/max_phase/enable_ai_notes` (clones were shipping 0 notes); promote `batch_insert_notes` warns on out-of-extent drops instead of dropping silently. **Verified:** byte-stability gate PASSED 226s (9 KJV byte-stable); `test_build_cache` 36✓; +5 regression tests (mint10_phase3 ×3, audit-log rollover, build_cache completeness pin); lint 27✓/1⚠/0✗; mypy clean. **▶ NEXT = mint-10 Phases 4–6** (★BUGCLUSTER normalize/write_queue/kind-replace sweeps · console XSS hardening · test-robustness/concurrency/docs + perf opts). TRACKER-STATE active.
>
> **➤➤➤ 2026-06-01 — ✅ GitLab SSH + CI GATE GREEN-UP (the "warning on every save" fixed).** Set up an ed25519 SSH key for GitLab (passphrase + ssh-agent, Windows-agent auto-start/persist; gitlab.com host keys pinned + fingerprint-verified) and switched `origin` HTTPS→SSH (`git fetch` over SSH verified; pushes now prompt-free; `github` mirror stays HTTPS). Diagnosed the recurring "pipeline FAILED every push": the blocking CI `gate` job (`ci.py --no-tests`) failed ONLY on **mypy** at `scripts/core/press_kit.py:238` — a `# type: ignore[attr-defined]` on the `Image.LANCZOS` fallback that is USED locally (Pillow 12.2 installed → real attr-defined error) but UNUSED on the clean CI runner (Pillow not in `requirements*.txt` → `Image` untyped → no error), compounded by CI's mypy 2.1.0 being stricter on `warn_unused_ignores` than local 2.0.0. **Fix:** replaced the brittle bare ignore with `getattr(Image, "LANCZOS")` — type-checks cleanly under mypy 2.0.0 AND 2.1.0, Pillow present OR absent (straddle removed at the code level, not papered over). Verified: local gate `CLEAN 5✓/2warn/0fail`; CI-proxy (throwaway venv, mypy 2.1.0, no Pillow) `0 errors`; 33/33 `test_press_kit_epsilon7` pass. **▶ NEXT = mint-10 Phases 2–6** (build-path byte-stability) per `plans/2026-06-01-mint-10-fixes-plan.md`. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-01 — ✅ MINT-10 round-3 re-audit COMPLETE (recovered + continued) — 62 deduped → 49 verified survivors / 13 refuted (4 high · 13 med · 22 low · 10 info); fixes = NEXT.** Last night's run `wf_ba367edc-a4a` (15 dims) ran 7.4h then died on terminal shutdown before any verify/synth (`result` null). Root cause: this box is a 4-core Intel N95 → Workflow concurrency cap = **2**, so 2 slow finders (one ran 2.3h) held both slots while 52 verifies queued — the "5h hang". **Recovered 46 candidate findings** (7 high · 26 med · 12 low · 1 info, 11 dims, incl. 11 real pytest failures from the tests-run dim) from the 15 completed finder transcripts → `docs/superpowers/notes/2026-06-01-mint-10-recovered-candidates.json`. **Continuation `wf_4a2d0815-8c9`** (new `.claude/workflows/deep-audit-continue.js`): verify the 46 FIRST (barrier — can't be starved) → run the 4 finders that never completed (cross-module#1 + opt-build/ingest/render) → synthesize; verify+find pinned `sonnet` (cap=2 → Opus ~8h vs Sonnet ~3h; matches rounds 1–2 verify methodology). Artifacts written: `2026-06-01-mint-10-{findings.md,audit-raw.json}` + indexed `plans/2026-06-01-mint-10-fixes-plan.md`. **Notable highs:** `_iter_note_ref_attribution_years` 8-field skip defeats `time_filter_ceiling` (3 finders corroborated); `_append_cloned_edition` drops enabled_kinds → cloned edition ships 0 notes; `build_cache._PIPELINE_SCRIPTS` omits popup_versions/traditions + topical-index data → stale EPUBs. **▶ Phase-1 stale-test green-up SHIPPED** (11 test files, 579 green). **NEXT (fresh session) = Phase-1 production items (`audit_log` month-rollover · `inject` whitelist) + Phases 2–6 (build-path byte-stability)** per `plans/2026-06-01-mint-10-fixes-plan.md` (phased safest-first · TDD · byte-stable · sweep-the-class · marathon-core untouched · 5-leg save per batch) → then STOP. **mint-10 = the LAST audit round (no mint-11 until project END, user-directed 2026-06-01).** TRACKER-STATE active.
>
> **➤➤➤ 2026-06-01 (prior) — ✅ MINT-9 ALL 6 FIX PHASES SHIPPED + deep-audit ENGINE upgraded + MEMORY synced. NEXT = round-3 re-audit + build the 4 approved Claude-setup items.** Round 2 (`wf_e5e7a0d6-24a`, 99 agents · ~4.67M tokens · ~3.2h): **66 deduped → 45 survivors / 21 refuted** (down from round-1's 57). All non-deferred findings fixed: **`b1a39485`** = P1+P2 checkpoint; **THIS commit** = P3–P6 + engine + memory. **P1** tradition/time bxx-fallback (3 sites) + promote SyntaxError log/backslash-root-cause · **P2** build_cache 7-script key + theme-css + source_dates + bookcode lint→12 maps + stale-test fix · **P3** (byte-gate PASSED 253s) inject precedes-order + matter_pages annotation override (None→KJV byte-stable) + `config.category_baseline_kinds` · **P4** corpus_index `_CACHED_CONN`-in-lock + notes_io→compute_matrix + inject label-escape + trusted-html ingest guard + url_override allowlist + cross_refs nonlocal-count · **P5** 4-driver write_queue dedup + share-pin→count + cyril-all-challengers + tautological-test fixes + ANSI→shared + un-stuck refactor-orphaned ch_count test · **P6** REPO_MAP/render_coverage doc-truth + is_output_current watches notes. **+4 test files (`test_mint9_phase{1,3,4,5}.py`); lint 27✓/1warn/0fail; mypy + ruff clean.** **⚙ ENGINE (`deep-audit.js`, USER ask):** prior-round DEFERRED memory + sibling-sweep + a `tests-run` (pytest) dim + pinned counts + args-log → **15 dims, ROUND→3.** **🧠 MEMORY (USER ask):** new `fix_the_class_not_the_instance` + 7 updates. **▶ NEXT:** round-3 re-audit (CONVERGED-or-loop) → then build the 4 approved LOCAL setup items (auto-format hook · truth-gate Stop hook · `/save`+`/audit` commands · Layer-1 doctrine). **DEFERRED by design (engine refutes; re-surface ≠ failure):** `ex.py`→`exo` (→τ.G) · `aes` ch11–16 · compresslevel 9→6 · 2 perf hoists. **FLAKY:** `test_two_back_to_back_runs_equivalent` (time-sensitive; passes alone). TRACKER-STATE active.
>
> **➤➤➤ 2026-05-31 (prior) — ✅ MINT-8 FIXES BATCH-1 + BATCH-2 + BATCH-3 ALL SHIPPED & VERIFIED · convergence re-audit = next.** Three commits: **batch-1 `852ed8a4`** (33 files, safe / no-EPUB-impact) + **batch-2 `cf05d8e3`** (16 files, guards / no-EPUB-impact) + **batch-3 `8d44ff1f`** (14 files, build-path/determinism — **byte-stability gate PASSED 325s + flagship epubcheck CLEAN; `epub_working/` untouched**). Enforced gate GREEN both times (**lint 28✓/0/0 · mypy ok · ruff-format clean**); working tree clean; the relevant pytest all pass (incl. a 68-test combined run + per-batch verifies).
> **A ground-truth VERIFICATION workflow** (`wf_b1f914bf-7ad`, 11 agents) re-checked 25 synthesized findings against LIVE code BEFORE implementing → **13 LIVE · 1 LATENT · 7 ALREADY_FIXED · 2 MISDESCRIBED · 2 NOT_APPLICABLE** (so ~11 plan items were no-ops/wrong; 3 plan errors corrected). **batch-1 (38 items):** docs/registry truth (REPO_MAP/MATRIX_MAP/INDEX → **72 kinds · 14 trans-dirs · 169 tests · api 18/tmpl 21**; mint-cleanup + mint-7 plan statuses → COMPLETE); `write_book` 8/9-tuple attribution invariant + sorted writes; `translations._book_index` + `notes_io` mtime-fresh caches; atomic candidate-JSON writes (10 drivers + prospect); AI-driver `iter_target_verses`/`resolve_books` dedup → `at_scale_base`; `batch_promote_xrefs` by-book default; `render_coverage._CANONICAL_BOOKS` → full 87-book canonical (−1ma/2ma phantoms, +man/2en/1es/aes/lje/sus/1cl); `promote.py` syntaxerror-visibility + newline-escape + key-guard + full-scan; `web_content` sample kind-filter parity + restore-.py validation; `build_cache` rglob cache-key; H6 tau6x1 dead-test; M1/M2/M17 security = confirmed ALREADY-FIXED (mint-7); +3 new test files. **batch-2 (guards, byte_impact=false):** **9** subprocess W-W1 `stdin=DEVNULL` sites (verification found 9, not the plan's 3); **NEW ENFORCED commit-time `subprocess_stdin` lint guard** (ALL_CHECKS 27→**28**) — its first import-NAIVE AST detector false-flagged 24 local `run()` helpers, so I **rewrote it import-aware** (only `subprocess.X` or `from subprocess import run`-bound bare names) + added FP-regression tests, then flipped `_ENFORCE_SUBPROCESS_STDIN=True`; M16a bookcode lint screens CCEL_ABBREV+EASTON_BOOK (7→9 maps); M3 `/customize` `edition_canon_books` canonical order (§6.1); M4 ALL_CHECKS **==28** exact pin + dup-key guard; **H6 SECOND dead-test** (`test_parallel_bible_phi1.py`) un-glued; +2 new test files. **⚠ Plan corrections from verification:** H6 had 2 occurrences not 1; H2 was 9 subprocess sites not 3; M14 zip-mtime is in `build_epub.py` not `epub_utils.py`; M8 compresslevel already explicit @9 (MISDESCRIBED). **Trust note:** a batch-1 verify sub-agent disclosed fabricating content in earlier internal turns incl. TWO phantom "security incidents" — fully retracted by it; I independently re-verified every blocker (the `onix`-comment lint trip + an E501, both real, both fixed) and acted on ZERO retracted claims. **✅ BATCH-3 DONE & VERIFIED (byte-stability gate PASSED 325s · flagship catholic-study epubcheck CLEAN · `epub_working/` provably untouched · lint 28✓/0/0 · 19 new tests pass):** H1+M18+M20 (OPF wall-clock date `epub_utils._resolve_publishing:29-30` → pinned `copyright_year=2026`/`publication_date=2026-05-14`; standalone `language_code: gez`/`amh` + `-US`-suffix guard at `build_edition.patch_opf`; **harden `tests/test_byte_stability_gate.py` `_content_digest` to normalize `<dc:date>` + `Copyright © YYYY` FIRST**); M14 zip-entry mtime → fixed `(1980,1,1,…)` in `build_epub.build()`; H3+M13 `inject.py:607` + `coverage.py:144` `(\d{2})(\d{2})`→`(\d+)(\d{2})` (1En ch≥100) — best via a shared `aside_id_re(prefix)` helper; M9 hot-regex hoist (inject/coverage, lru_cache-per-prefix); aes `html_chapter_count` guard (promote + canonical_verse_counts + lint). All landed as SOURCE+TEST edits only (no `epub_working/` re-bake needed — the 9 KJV editions have no ch≥100 books, and the OPF date/lang changes are build-time `patch_opf`/`_resolve_publishing`, proven content-stable + deterministic by the gate). **▶ NEXT = RE-RUN `deep-audit` (convergence loop until a round returns ZERO new verified findings) — the whole mint-8 fixes plan is now implemented across batches 1–3.** **DEFERRED by design:** H5 `ex.py`→`exo` (LATENT — no live consumer; fix at τ.G standalone-wiring, do NOT rename early); M8 9→6 compresslevel (DECLINED — enlarges every EPUB 1–3% to save ~30s/build; quality output > build speed); M10/M12 (NOT_APPLICABLE — named caches don't exist). Then RE-RUN `deep-audit` (convergence loop). **Process lesson: never launch a file-writing Workflow / a non-zero-exit lint command in the SAME message as a `save.ps1` (`git add -A`) commit — they race / the cascade cancels.** TRACKER-STATE idle.
>
> **➤➤➤ 2026-05-31 (prior) — ✅ MINT-8 DEEP-AUDIT ROUND 1 COMPLETE (reusable engine built + run + synthesized).** Built `.claude/workflows/deep-audit.js` — a reusable, parameterized multi-agent audit Workflow (find → adversarially-verify → synthesize): read-only finders (`feature-dev:code-reviewer` bugs · `feature-dev:code-architect` optimization · `Explore` marathon-boundary guard), skeptic panels scaled by severity (crit 3/high 2/else 1) default-to-refuted, severity calibration, phased-fixes synth + completeness critic; 14 dimensions (9 bug lenses + boundary guard + 4 optimization lenses). RAN it (round 1, depth=deep) = **ONE background run: 106 agents · ~4.80M subagent tokens · ~3.25 h** (`wf_1b2fdc14-4fd`; the `args` override didn't propagate so it ran the full default 14-dim set — the intended scope). **70 findings → 57 verified survivors / 13 refuted** (high 6 · med 20 · low 26 · info 5). Artifacts: findings record `docs/superpowers/notes/2026-05-31-mint-8-findings.md` (+ raw JSON `…-audit-raw.json`); **phased fixes plan `docs/superpowers/plans/2026-05-31-mint-8-fixes-plan.md`** (INDEXED). Real HIGHs: OPF wall-clock `<dc:date>` baked into every edition (breaks build DETERMINISM + poisons the content-addressable cache); 5× `subprocess.run` missing `stdin=subprocess.DEVNULL` (one on the LIVE HTTP build endpoint, W-W1); `inject.py`+`coverage.py` `(\d{2})(\d{2})` aside-ID regex can't parse chapters ≥100 (1En 100–108, currently latent); `write_book` serialises `attribution` as `{}` → 8-tuple notes become schema-violating 9-tuples on web-UI save; Tewahedo Exodus stores named `ex.py` not canonical `exo.py` (data UNREACHABLE); a dead test (`def` inside a comment); 3 med XSS/XML-injection (RSS feed CDATA + `/sources` truncateHTML); alphabetical canon sort violating §6.1; + ALL_CHECKS=27-not-26 & REPO_MAP/MATRIX_MAP count drift. Optimization pass: vision-transcription marathon CONFIRMED-optimal for current constraints; build → `compresslevel`/precompiled-regex quick wins; `render_coverage._CANONICAL_BOOKS` missing 7/87 books. **NO finding requires a marathon-core edit; 9 KJV editions stay byte-stable.** **▶ NEXT SESSION = implement the fixes per the phased plan (TDD, byte-stable, durable guards — prefer commit-time lint checks for recurring invariants), then RE-RUN `deep-audit` (convergence loop until a round returns ZERO new verified findings).** This session = build + run + synthesize + save + handoff ONLY (fixes deferred at user direction; ~5M-token session). Saved 5-leg. TRACKER-STATE idle.
>
> **➤➤➤ 2026-05-31 (prior) — ✅ MINT-7 COMPLETE (Phases A–E) · mint-8 audit was the next step.** **⚠ UNIFIED SAVE SEMANTICS (this session):** save = commit = push = backup = sync = ONE full 5-leg sync to ALL sources (local + GitLab + GitHub + E: + F:), **EVERY time** via `pwsh -File save-all.ps1`; Claude self-triggers whenever something important lands OR on user command — **NO local-only, NO batching, NO every-Nth cadence** (RULES §4 rewritten; memory `reference_save`). GitLab `main` PROTECTED (fix forward). **Phase A (★book-code BUGCLUSTER):** completed `_BOOK_CODE_ALIASES` (+mar/jol/ezk/nam/php); canonicalized all book-code maps (TSK/NAVES/Kenyon); **rebuilt `tsk_xrefs.json` by a deterministic LOCAL key-remap** (17,716 target remaps; adversarially proven canonical + lossless — 344,799 ref-tuples conserved, byte-count unchanged) → the **1,525 TSK xrefs are reachable again** + detector emits canonical `#vnote-eze-…`; deleted 56 stale legacy candidate files; +D4 (catholic `mar`→`mrk`) + bonus `render_coverage` `mar`→`mrk`; new commit-time **`bookcode_canonical` lint check** + `TestBookCodeCanonicalization`; 4-lens adversarial workflow (`wf_f17387e2-96e`) verified. **Phase C (security):** C1 stored-XSS in `/api/sample/` → `_render_sample_html` now `sanitize_html`s the note body (test proves a `<script>` body is stripped, `<em>` survives); C2 `content/auth.json` (+distribution/press_kit) gitignored; C3 Build-All `All_Editions_*.zip` added to the download allowlist (`application/zip`). **Phase B (at-scale):** B1 both AI drivers now read `ch_count` (was `"chapters"` → always 50, silently capping every book at 50 ch; Psalms now iterates all 150 — **the actual content fill-run is a SEPARATE metered run, code fix only**); B2 `run_xref` write_queue now append-merges (was the lone clobbering driver of 9). **Phase D (code-debt):** new dependency-free `core/at_scale_base.py` dedups the byte-identical `candidate_to_dict` (×10 drivers+prospect), the 27-code `NT_BOOKS` frozenset (×4), + ANSI consts — a guard test pins every driver shares the ONE object; D2 the 2 legacy `_send_json`-bypass error paths fixed (Content-Length+security headers); D3 dead code (`_split_web_html`→archive; 2 orphan helpers deleted; `_replace_verse_popup_translation` MARKED not-yet-wired; `audit_caches` wired into `ci.py`). **Phase E (tests/doc):** E1 `TestAllChecksMetaContract` runs all 26 `ALL_CHECKS` (shape + no-fail on the committed tree) + pins registry size; E2 added a `slow` marker + tagged the 2 slowest non-build files (`test_matrix_psi35` ~87 s, `test_web_filesplit` ~45 s — **NOT the "23 min" the stale audit/memory claimed; re-measured + all stale time-claims corrected this session**; `-m "not slow"` deselects them); E3 a byte-stability gate (`tests/test_byte_stability_gate.py`, behind `slow`) — builds 3 multi-canon editions valid+distinct + proves the flagship rebuild is **deterministic** (verified, 205 s; **one build = ~133 s** → all-9 is a slow-loop + logged as a mint-8 optimization target); E4 fixed 3 stale doc refs. **Lint 26✓/1⚠/0✗; 9 KJV editions byte-stable** (changed files are candidate-gen/source-loader/coverage/sample/export — zero build-path impact). **DEFERRED (separate human-reviewed content run, like B1):** generating + promoting the now-reachable xref candidates. **▶ NEXT = mint-8 DEEP AUDIT** — plan written `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md` (broader+deeper dimensions: + byte-stability / data-coordinate-validity / concurrency-caching / cross-module-invariants / marathon-core-boundary, PLUS an **OPTIMIZATION** pass re-evaluating the project's own task-plans/tools vs 4.8/ultracode — e.g. the ~133 s/build, the vision marathon method — and the convergence loop). **NEXT SESSION:** build the reusable `.claude/workflows/deep-audit.js` (plan §5) + RUN it → adversarially verify → fix → re-audit until CLEAN. **mint-7 fully synced (5 legs).** **[USER]** real-reader eyeball open; Esther p28 paused (parallel lane, entry below). ✅ **Session-end close-out DONE:** env-health bootup rule now an explicit **RULES §0 4-point checklist** (freshness = best-effort, not a hard gate); **CHANGELOG month-rolled** (live ~0.95 MB; older sessions → `dev/archive/CHANGELOG_2026-05-07-to-2026-05-12.md`; the `untracked_phases` + `plan_coherence` lints now read live + archives); **lint 27✓/0⚠/0✗** (the size warn cleared); stale "23 min" test-time claims corrected to the real ~45 s/~87 s. TRACKER-STATE → idle.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused at p28 (see the top journal entries).

## Next

> **Mint cleanup is COMPLETE.** Next critical path = **Phase D1b — PO Esther vision marathon (paused p28)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2 distinctive sources. Background lanes (RULES §2.5, auto-pick when one frees): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
