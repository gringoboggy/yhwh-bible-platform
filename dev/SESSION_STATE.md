# Session state — current snapshot

> **➤➤➤ 2026-05-29 (LATEST) — ▶ MINT-CLEANUP — PHASE 0 COMPLETE (11/11 anti-bloat guards) + PHASE 1 (slim bootstrap) UNDERWAY; Phase-0-tail committed THIS turn; session-start RAM freed 0.64 GB.** From the 8-agent audit's 7-phase plan (`docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`; KEEP PYTHON; marathon core untouched). This turn added the Phase-0 tail to `scripts/lint_rules.py`: 4 new checks — `commercial_terms` (147 refs), `retired_terms` (deadline/sonar), `triad_plan_consistency` (caught PLAYBOOK→archived `PLAN_2026-05-21.md`), `stray_artifacts` (FAIL-tier, tree clean) — + UPGRADED `check_doc_cross_references` (archive-aware + dangling-link; caught a dangling SESSION_STATE→`PLAN_2026-05-24-epub-presentation-polish.md`) & `check_repo_map_complete` (reverse-path) + a `save.ps1` >200-file `--yes` gate. Guards ship WARN-when-breached (a pre-commit `fail` bricks its own commit) with `_ENFORCE_*` flags the cleanup phases flip to FAIL. Term-guards scan CURATED process docs only. `test_lint_guardrails` 16→35; `test_lint_rules`+`test_ci` 57 green; lint **16 pass / 9 warn / 0 fail**; ruff-clean; NO production code path touched. **▶ NEXT: Phase 1** — write `scripts/rotate_truth_records.py` (FIXER) → rotate SESSION_STATE/IN_FLIGHT to ≤2 entries + fix the dangling pointer → slim `CLAUDE_PROJECT_RULES.md` to ~25-30k tok (finished-arc narrative → `dev/archive/RULES_HISTORY.md`; resolve 6 contradictions; present-tense §1/§10) → `truth_record_budget`/`frozen_stats`/`retired_terms` GREEN. Esther vision marathon paused at p28 (resume post-cleanup). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-29 (prior) — ▶ PHASE D1b Task 4 — PO Esther p27 (canonical 1:4-tail completes p26 → 1:5–1:8; 1:8 cut after `ፈቃደ` → p28) RECOVERED from TWO consecutive OOM crashes + finished by a CONTROLLER verification pass; accumulator page `27` written (codepoint-clean); committed THIS turn + E:/F: bundle (user remounted both drives); tracker ACTIVE (p28 next).** This is NOT the documented p26 crash — the p26-recovery had committed cleanly (HEAD `5a7556f5`, tree clean). Since then TWO undocumented crashes hit, both mid-p27: session `2a3624ca` ran 2 blind transcribers + an adjudicator then crashed (#1); session `2e57d4aa` re-rendered the crops + launched a finishing-adjudicator convergence **workflow** (`wf_fe98cd7b`: 2 recovered blind drafts vs the image + Pereira's French) which **COMPLETED its converged StructuredOutput**, then crashed before the Verify phase ran (#2). Session-start RAM freed **593 MB** (10.1→10.7 GB; 17 background procs; no leaked python/java). **Recovery (NO heavy vision re-run — heavy fan-out is what OOM'd both prior sessions):** recovered the workflow adjudicator's completed reading from its agent transcript (`…/2e57d4aa/subagents/workflows/wf_fe98cd7b-df1/agent-a1dc716d…jsonl`), cross-confirmed by the first adjudicator attempt's independent text, then a CONTROLLER re-verification of the two load-bearing cruxes against the staged crops + French: **v7 numeral = printed `፫ ፸`** (own high-zoom confirmed a double-pillar tens form, NOT `፻`; Pereira's French "trente mille"/30,000 divergence FLAGGED, not harmonized — read-the-print) + **v7/v8 boundary = the abundant-wine sentence opens v8** (left-margin "8" beside `ዘሥናይ`; ✻ precedes `ወበዓሉስ`; French v8 "Et le vin était abondant"). Other resolutions: v5 feast word `በዓለ` (ayn-4th; both blind drafts misread `በኆለ`/`በኅለ`); v6 (long, hardest) converged per-glyph + French parallels (parenso=`ዘጸርንስ`, émeraude=`ዘሕብረ መረግድ`, fleurs de rose=`ጽጌ ረዳ`). **1:4 assembled across the p26→p27 break** (`ዕለ`+`ት`=`ዕለት` "days"; day-count `በ፻ወ፹`=180). 3 `uncertain` flags (numeral/French divergence; 1:5 `ሕዝብ`/`ሐዝብ` low-contrast ḥ; 1:6 `ጸፊዮ` middle glyph). Accumulator = **pages 24+25+26+27**. **NO store/base/build/test change** (`est_patrologia.py` untouched; 9 KJV editions byte-stable; `git status` = accumulator JSON + truth-records only). Self-upgrading-matrix lessons **(n)** recover-a-crashed-RECOVERY (from a workflow's completed StructuredOutput) + **(o)** controller-verify-recovered-cruxes + **(p)** print-vs-French numeral divergence, added to `_vision_notes`. **▶ NEXT: p28** (continues 1:8 `…ፈቃደ` tail → 1:9..) — same convergence+calibration method (per (h)–(p)), MAX 1 heavy agent, per-unit commits, controller renders / subagents Read. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `_vision_notes.md`. **[USER]** real-reader presentation eyeball still open.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther in progress (see the top entry).
- 2026-05-29 mint-cleanup — anti-bloat lint guards (Phase 0) + bootstrap slim (Phase 1); `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.

## Next

> Current next is in the top entry's "▶ NEXT" + the mint plan. Sequence: mint Phase 2 (roadmap refresh) → 3 archive sweep → 4 decommercialize → 5 gates → 6 polish; then resume the paused Phase-D1b Esther vision marathon (p28). Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`.

## Conventions (unchanged)

Local commit only via `save.cmd`/`save.ps1` (PowerShell; no remote — deleted 2026-05-12). Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `EXEC_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
