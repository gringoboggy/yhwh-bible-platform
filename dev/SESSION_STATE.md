# Session state — current snapshot

> **➤➤➤ FRESH SESSION START HERE.** `git pull` → read `AGENTS.md` → the triad → this block → `dev/IN_FLIGHT.md`.
>
> **▶▶ 2026-06-29 (LATEST) — ACTIVE ARC = v1.0.0 DEVICE-QA REMEDIATION (from the user's 4-reader QA round). Tracker `dev/audit/device-qa-v1.0.0-2026-06-28.md`. Tag NOT cut.** WIN landed **all pre-rebuild code** + handed Mac its file-disjoint lane:
> - **WIN done (committed):** `9345c842` removed **1,364 machine-authored "User" notes** (no PD source; corpus 91,712→90,348) · `3c46a46d` **Apple LTR** spine `page-progression-direction="ltr"` + single merged badge off-Kobo + clean note separators · `b3588727` badge-spacing nowrap + **Edition-ID → copyright page** · `e4210aff` **Kindle teleport fix** (M4b relocates BOTH study `vnotes-*` + translation `vnote-*` popups to reachable backmatter endnotes; matrix routes through `make_kindle_m4b`; m4b default-on; 17/17 green) · batch-4 **Kindle body `text-align:justify`**. The 2 CRITICALs (Apple "reads backwards", Kindle "notes teleport") are fixed in source.
> - **✅ WIN DONE (2026-06-29):** golden re-baselined (`651f8630`, Mac-cross-OS-verified 9/9) · **exact new count = 90,181** (was 91,555; `resolved_note_counts`) + in-repo headlines swept · **4 device artifacts rebuilt** (ethiopian-tewahedo Play/tablet/kindle-m4b/Kobo-kepub; all epubcheck 0/0/0/0; Kindle teleport fix proven at scale 30,005 study + 36,329 translation → endnotes) + **re-staged** (U1 Kobo loaded to `G:`; U2/U3/U4 in `OneDrive\Desktop\QA`) · **unit-test re-baseline DONE + green** (separators / eink-split / edition-ID→colophon / version-param).
> - **✅ MAC DONE (all 3 lanes):** #1 v1.0.0 `.dmg` notarized+stapled (SHA `6db97025…`) · #2 **cross-OS golden 9/9 PASS** · #3 public count cascade `91,555→90,181` + Pages deploy (www.yhwhyaway.com live) + repo descriptions. v0.1.0 release body left at 91,555 by design (those downloads ship 91,555 — WIN-confirmed); v1.0.0 body gets 90,181 at tag.
> - **▶ REMAINING (human-gated):** the **user's device re-QA** (U1 Kobo · U2 Apple · U3 Kindle · U4 Play — artifacts staged + ready) → report verdicts → flip `HUMAN_DECISIONS`/`EREADERS` → **then the `v1.0.0` tag (user-only)**. Post-rebuild device items (WS1 residual line-breaks, Kobo first-cat return-link) diagnose on the rebuilt artifacts after the verdicts. **Do NOT cut the tag.** _Prior history below._
>
> **▶▶ 2026-06-28 — v1.0.0 PRE-TAG WINDOWS LANE ✅ EXECUTED (user "plan + execute the full pre-tag lane"). Plan `~/.claude/plans/bootstrap-and-then-i-magical-origami.md` (three-way ME/Mac/USER split).** WIN did W0→W3, STOPPED at a clean device-QA handoff (tag NOT cut):
> - **W0 — `VERSION` 0.1.0→1.0.0** (`c2b71e97`, local; pre-commit green). Shared P0: Mac pulls before its `.dmg`.
> - **W1 — Windows desktop binary PROVEN at v1.0.0:** PyInstaller freeze → `dist\YHWH.exe` (677 MB) → **Azure Trusted Signing Valid**; Inno Setup 6.7.3 re-installed (per-user, `%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`) → `dev\build_msi.cmd` → **`dist\YHWH-1.0.0-windows-x64.exe` (541 MB) Azure-signed, signature Valid**. Whole freeze→installer→sign chain works end-to-end (cached Az ctx `gringo.boggy@hotmail.com`, no device login). Binaries are gitignored; attach at tag.
> - **W2 — M5 Play column BUILT:** `build_format_matrix --phase M5 ethiopian-tewahedo v1.0.0` → 5 `YHWH-ethiopian-tewahedo-v1.0.0-play-{colour}.epub` (epubcheck 0/0/0/0, K-R2 green, 66,687 noterefs all-resolve).
> - **W3 — all 4 device-QA artifacts STAGED** at `C:\Users\bogda\YHWH-device-staging\` (+ `v1.0.0-device-QA-README.md` with per-reader tap lists): **Kobo** `YHWH-koboQA.kepub.epub` (FRESH v1.0.0 eink w/ Phase-H fixes; eink epubcheck 0/0/0/0, kepub K-R2 green) · **Apple** `…-apple-tablet.epub` (epubcheck 0/0/0/0) · **Kindle** `…-kindle.epub` (epubcheck 0/0/0/0, **verified kindle-safe** 0 display:none) · **Play** `…-play-navy.epub` (epubcheck 0/0/0/0).
> **▶ NEXT (USER, parallel — `dev/HUMAN_DECISIONS.md` rows OPEN):** U1 Kobo taps · U2 Apple re-QA · U3 STK read · U4 Play phone QA — all 4 artifacts staged + README ready. **▶ NEXT (MAC, autonomous):** pull `VERSION=1.0.0` → `.dmg` build+notarize · cross-OS 9-KJV verify · (post-tag) website deploy + AppImage CI. **▶ NEXT (WIN, autonomous, at v1.0.0 prep):** `gen_release_catalog --tag v1.0.0` + reconcile asset count + finalize `RELEASE_NOTES_v1.0.0.md` + final `ci.py`/`trace_matrix`/`ebible verify`. **Do NOT cut the tag.**
>
> **▶▶ 2026-06-28 — NEXT WORK = v1.0.0 RELEASE GATE (round-16 ✅ DONE; user "prep for a fresh session to work on release gate").** Authoritative plan `docs/superpowers/plans/2026-06-14-v1.0.0-release-plan.md` (**§1 state + §8 Definition-of-Done REFRESHED to 2026-06-28 reality this session**); human-gated queue `dev/HUMAN_DECISIONS.md`; reader status `dev/EREADERS.md`. **The gate is ~95% automated-done; the rest splits three ways:**
> - **✅ DONE:** the whole audit arc (rounds 8→16 + remediation Phases A–I, byte-proven); WS1 158-verse empty-anchor re-split APPLIED + byte-proven (`6b690361`); all gates green (`ci.py` · `trace_matrix` 0 · `ebible verify` 0 · 9-KJV golden 9/9 · G1–G5); **M1 everywhere + M4 Kindle catalog columns LIVE** on v0.1.0; **M3 Kobo kepubs BUILT**.
> - **🔧 AUTONOMOUS-NEXT (no user needed):** (a) **build + sign v1.0.0 desktop binaries** — Win `.exe` (`dev/build_desktop.cmd` + `dev/sign_windows.ps1`, Azure Trusted Signing WORKING) [WIN] · macOS `.dmg` (`dev/build_dmg.sh`, notarized) [MAC] · Linux `.AppImage` (GitHub Actions CI, on tag); round-16 Phase I already pinned these scripts. (b) **build + STAGE the M5 Play-Books EPUB** (the ONLY column not yet built) + confirm the M2/M3/M4 device-QA artifacts are staged, so the user can run all 4 device rounds in parallel. (c) **`gen_release_catalog --tag v1.0.0` + website rebuild/deploy** (live publish is Mac's — no WIN Pages clone) + **SHA256SUMS** merge. (d) **finalize `dev/RELEASE_NOTES_v1.0.0.md`** vs the live catalog + **reconcile the asset count** (the 187/188-vs-87 reconcile-pending item). (e) Optional: a **dishonest-code / stub audit** sweep (§8 box).
> - **🚫 HUMAN-GATED (`dev/HUMAN_DECISIONS.md` — cannot be agent-done):** M2 Apple device re-QA (rebuilt tablet) · M3 Kobo tap round · M4 Send-to-Kindle device read · M5 Play phone QA · the † badge wrong-locus + Hebrew/Greek font A/B + mid-verse eyeball · **the `v1.0.0` tag command** (the user cuts it when §8 is green).
> **▶ Suggested first autonomous action:** dry-run the v1.0.0 Windows `.exe` build + Azure sign (prove the pipeline at the v1.0.0 version-string BEFORE tag time) **and** build+stage the M5 Play EPUB (unblocks the user's M5 phone QA), then STOP at a clean device-QA handoff. **Do NOT cut the tag.** Marathon / Esther / CAM overflow stays HOLD until the gate closes.
>
> **★ 2026-06-28 — ROUND-16 REMEDIATION ✅ COMPLETE (all phases A–I). PHASE H DONE this session — the 10 byte-sensitive build-path items, TDD red→green, fully byte-proven.** Phase-H commit `bd6b5c91`: **H1** kindle `build_one` make_kindle_safe+verify (P1 E999 — the /customize path; the catalog M4 kindle asset is UNCHANGED, matrix uses everywhere-base+kindle_post) · **H2** eink return-link per-chapter v-anchor gate (P2) · **H3** eink numbers-mode note-sym repair · **H4** eink glossary split-group `vn-part` indicator · **H5** verse-p-flush opener · **H6** S2 body-boiler re-gated on `s2_group` · **H7** certifiedBy→"YHWH Ya' Way" + OPF hygiene scanner (★the ONE deliberate 9-KJV golden re-stamp) · **H9 (F1)** computer→everywhere alias · **H8 (F2)** verse_marker_glyph RETIRED · **H10** dot badge eink guard. **Byte-proof:** baseline control = HEAD 9/9 == old golden (deterministic); round-B = 6 non-kindle cells BODY-IDENTICAL (items 2–10 byte-neutral, H7 isolated to the `certifiedBy` line) + 3 kindle `verify_kindle_safe==[]` (H1 deliverable); golden re-stamped from round-B + **independently re-verified 9/9**; `make_kindle_safe` re-zip reproducible+cross-OS (`_ocf_rezip`) ⇒ matches the ubuntu CI gate. F1/F2 build-free gates flipped clean; 18 `test_round16_phase_h.py` unit pins; F2 API smoke; lint 36/0; mypy clean. Boggy decisions resolved: F1→alias, F2→retire. **2 items stay verifier-DEFERRED by design** (C3 at-scale-stat, B4 sanitizer-balancing — recipes in the plan). Prior phases A–G+I were committed pre-Phase-H (then rebased onto Mac's `c5216c54`); see `git log` + `dev/CHANGELOG.md`. **→ milestone 5-leg `save-all.ps1` (E:/F: deferred — drives Mac-side).**
> **★ 2026-06-28 (history) — ROUND-16 ✅ FULLY COMPLETE (both lanes + the WIN RAM-seam finish). FINDINGS-ONLY — the unified plan was produced + then USER-APPROVED for remediation (see the block above).** WIN fresh session ran the flagship **ethiopian-tewahedo eink** + the 2 standalones through the kepub-aware build-inspect harness (CommitFree 43.4 GB pre-flight): **all 22 catalog assets built + gate-scanned CLEAN** — flagship eink **exit 0, no OOM** (~26 min; epubcheck 0/0/0/0 · G3 idmap 865 pieces/88,541 xrefs/0 dead · G4 badge 40,578 anchors/0 orphan · D8 order 1692 ch/86 bk · **G5 glossary PASS at scale** max_inner_cp 399,171<400,000 · verify_kr2 GREEN; 22 orphan-aside WARN corroborates mac#17) · standalones epubcheck 0/0/0/0 · kepubs verify_kr2 GREEN. **Flagship-eink OOM PROVEN permanently resolved** (round-15 D5 streamer). No product defect from the harness; every finding is source-level. **★ ONE unified severity-ranked remediation plan = `dev/audit/round16-unified-remediation-plan.md`** (≈38 findings: **5 medium · ~33 low · 0 high · 0 optimization**; codebase health good). The 5 mediums: P1 kindle build_one E999 · P2 eink return-link teleport · P3 config.py cp1252 · P4 glossary 3×/fallback-OOM · P5 family-C dead gate. Two Boggy decisions flagged (F1 `computer` alias-vs-row; F2 `verse_marker_glyph` wire-vs-retire). **▶ NEXT = user approves the plan → remediate safest-first (Phase A→I), TDD, byte-proof; OR another work-ladder item.**
> **★ 2026-06-27 — ROUND-16 BOTH LANES COMPLETE + cross-lane consolidated (FINDINGS-ONLY, NO fixes). ✅ MAC LANE FULLY DONE — cross-OS 3a/3b/3c ALL PASS on macOS (incl. the `audit_output_hygiene` hygiene-gate run + the `test_round16_source_gates` 5/5 selftests; round16-remediation "pending Mac" boxes flipped to DONE). ✅ FRESH SESSION (WIN, RAM seam) DONE — see the 2026-06-28 block above.**
> **Mac lane IN (pulled `5c2a5b0`):** engine-mac 32→**20** survivors (2 medium, both WIN-owned: kindle DIRECT-build E999 [refines my seed#2 — matrix path OK, `build_one` path ships the failing variant] + eink return-link teleport) + **cross-OS verify ✅** (G1 9/9 byte-identical Win↔Mac + G3/G4/G5/G6 PASS on macOS catholic-study eink). Mac's own phased plan + 8 seeds in `dev/audit/round16-engine-mac.md`. **Cross-lane consolidation + dedup map** (F2=mac#18, AppImage=mac#20, etc.) in `dev/audit/round16-remediation.md` "CROSS-LANE CONSOLIDATION". **5 mediums total**; combined next-round seeds = WIN 7 + Mac 8.
> WIN ran the engine `Workflow` `wf_571060b9-289` (LANE=win, ROUND=16, 11 dims, 62 agents, 5.2M tok): **36→26 survivors (3 medium, 23 low), 10 refuted.** Authored + committed the round-16 gates (`audit_output_hygiene` [headline html-integrity/code-leak], `audit_cross_product`, `audit_customize_completeness`, the `round16_build_inspect` harness, `test_round16_source_gates`). Build-free gates surfaced **F1 `computer` orphan target** + **F2 `verse_marker_glyph` orphan field** — both **independently corroborated by the engine** (#12/16/20, #17/21). The WIN build-inspect harness scanned **18 artifacts: all everywhere/tablet/kindle/eink-epub CLEAN**; the 3 kepub gate-FAILs are **harness-applicability, not product defects** (idmap/glossary are plain-eink-epub gates; epubcheck timed out under engine load; `verify_kr2_build` GREEN) → harness made kepub-aware. Engine artifacts + merged findings + phased plan: `dev/audit/round16-remediation.md` + `round16-engine-win-{survivors.json,plan.md,completeness.md}`. **Engine medium findings:** config.py cp1252-crash (no `encoding=`), glossary ~3×-copy/fallback-OOM, + my own family-C gate is a dead check. **Completeness-critic = 7 next-round seeds** (kepub colour fan-out, no build-time enum validation, the website release-catalog as a 3rd offering surface, eink cross-OS determinism, override→marker chain). **▶ FRESH SESSION (RAM seam, user-directed):** re-flip local `LANE='win'`, run `py -3 dev/round16_build_inspect.py --only ethiopian-tewahedo:eink` (flagship, RAM ceiling) + the 2 standalones, fold the engine-mac survivors + Mac cross-OS deltas, finalize the merge + completeness matrix, present the remediation phase. Steps in `dev/IN_FLIGHT.md`. **NO fixes applied — remediation is a separate user-approved phase.**
>
> **★ 2026-06-27 — ROUND-16 DEEP-AUDIT READY TO RUN (PREPARED — now RUN this session, see above).**
> Build-Program Bulletproofing — a two-lane **FINDINGS-ONLY** audit of the EPUB-builder across the full output
> cross-product (edition × reader-version × OS) + options-completeness + display-redundancy + HTML-integrity +
> per-reader marker-logic + builder-robustness lenses. The engine (`.claude/workflows/deep-audit.js`) is configured +
> committed: `ROUND=16`, 11 `ROUND16_DIMS`, 8 new dim objects, the `ROUND===16` selector branch, round-15 D1–D9 +
> round-14 build-source dims folded into `DEFERRED_BY_DESIGN`; `LANE='all'` committed (each box flips its own local
> `LANE`). **▶ NEXT WIN SESSION:** bootstrap → `git pull --rebase` → flip local `const LANE='win'` →
> `Workflow({scriptPath:".../.claude/workflows/deep-audit.js"})` (all 11 dims) CONCURRENTLY with the full-catalog
> build-inspect harness (12 base builds + 4 kindle-posts + 2 standalones → colour fan-out → scan each;
> flagship-eink LAST+SOLO; never pytest beside a build). **MAC** has its standing autonomous block in
> `dev/LANE_HANDOFF.md` (both lanes run ALL 11 dims; only the side-work differs — WIN harness / Mac cross-OS verify).
> Program `dev/audit/round-16-build-program-bulletproofing-2026-06-27.md`; tracker `dev/audit/round16-remediation.md`;
> full WIN run steps in `dev/IN_FLIGHT.md`. **The audit is NOT started — each lane runs to completion in a fresh
> session, then STOPS at a complete verified findings set + a phased fixes plan for the user to approve (NO fixes this round).**
>
> **★ 2026-06-27 — ROUND-15 ✅ FULLY CLOSED + NO REMAINING FOLLOW-UPS (all 9 dims D1–D9 + cross-OS verified, both
> lanes). Mac cross-OS-verified D3/D5/D8/D9 (`4f69aa05`) AND D2 (`405eda85`). **D1 LIVE RE-CUT DONE (WIN, user "do it"):**
> removed all 100 retired-SKU EPUBs + their SHA256SUMS lines from the live GitHub v0.1.0 release → D1 gate PASS (87 assets,
> 0 retired, bijection clean); no website breakage (website references 0 retired assets). The v1.0.0 release gate / next
> audit round are SEPARATE future work (need a user trigger). Both remotes synced, tree clean.** _(Cross-OS observation, low priority: catholic-study eink G3 xref count
> differs Win 55,774 vs Mac 45,057 — catholic-study eink is NOT in the 9-cell byte-stable golden set, so cross-OS
> byte-identity was never proven for it; the `--min-xrefs 10000` floor is robust to both. Worth a future cross-OS
> determinism check, but out of round-15 scope.)_
> **Done:** D1 release-asset gate (Mac) · **D2 (MED) COMPLETE** (WIN) — check_xrefs regex + G3 **xref-class breakout**
> (`v-`/`ch-` scripture cross-refs counted/bucketed separately + `--min-xrefs 10000` floor wired into the per-build gate;
> ethiopian 88,541 / catholic-study 55,774 xrefs, 0 dead) ·
> **D3 (HIGH) COMPLETE** (Mac store fix + the ★WIN discovery that vulgate popups are BAKED so the store fix didn't reach the
> product → surgical `epub_working/` re-bake + golden re-stamp; dropped Ps 2:13/4:10 now SHIP) · D4 (HIGH) `_cascade`
> source-provenance (WIN) · **D5 (HIGH) COMPLETE** (WIN, 2026-06-27) · D6 canon book-count gate (Mac) · D7 migration torn-safe
> + idempotence (Mac) · D8 `audit_canonical_order` (WIN) · D9 `audit_kepub_revid_family` — guard #19 = by-design (WIN).
> **D5:** the >64 MB flagship-glossary byte-streamer (`_stream_glossary_pieces_from_bytes`) PROVEN — CHECK A real **255 MB**
> monolith str==from-file **byte-identical (690/690 pieces)**; CHECK B **G5 PASS** on the fresh flagship eink build (399171 <
> 400000 cap); permanent real-threshold regression (no monkeypatch); **+ found+fixed a G5 `--reference-split` false-FAIL on real
> post-`rewrite_links` builds** (detect+skip+WARN). Report `dev/audit/round15-d5-glossary-byteproof.md`. **▶ Mac:** cross-OS
> verify the D5 build (rebuild flagship eink on macOS → G5 PASS + the new slow str==from-file test) and the D3 golden re-stamp;
> the `generate_verse_popups.py` base↔bake separator-drift follow-up. See `dev/IN_FLIGHT.md` + `dev/audit/round15-remediation.md`.
> **Round-14 DONE — all 8 deep-audit survivors GREEN + the WIN-surfaced G5 over-cap fixed + all 5 gates built, 9-KJV byte-stable both OSes.**
> Survivors: WIN **#4** S1-attribution (cascade-gate `s2_group or eink_backmatter`) · **#5** G1 · **#6 (HIGH)** est 10:2 scripture corruption
> (`_mv_displacement_would_corrupt` WEB-base discriminator); Mac **#1** eink_glyphs-cache · **#2** prospect None · **#3** canonical-extent.
> **G5 over-cap** finding (27 eink glossary pieces) root-caused (post-split `rewrite_links` bare-href inflation, NOT the splitter) → per-atom
> `_atom_rewrite_headroom` reservation → 0 over-cap. **A1 cross-OS LF chokepoint** (`zip_repro.ocf_member_bytes` at `build_epub:161` +
> `kindle_post:121`) → **Mac cross-OS VERIFIED 9/9 KJV cells byte-identical Win↔Mac**; **A4** ubuntu CI. **5 gates (committed+pushed):**
> G1 KJV golden-hash (`tests/golden/kjv_golden_hashes.json`, 9 cells from POST) · A4 CI · G3 idmap (`dev/audit_idmap_frags.py`) · G4 badge
> (`dev/audit_badge_conservation.py` + byte-neutral sidecar `badge_verses_skipped`) · G5 glossary (`dev/audit_glossary_contract.py`);
> **G3/G4/G5 wired** into a slow per-build gate (`tests/test_round14_build_gates.py`). 9-KJV byte-stability PROVEN post-fix (G1 golden re-run =
> "9 cells match"); all build-path fixes eink-gated/dormant. Program: `dev/audit/build-pipeline-deep-audit-program-2026-06-25.md`. HEAD `48807147`.
> **★ ROUND-15 (8/9 DONE — current status in the header above; this block = program + dims reference) — program
> `dev/audit/round-15-completeness-audit-program-2026-06-26.md` + tracker `dev/audit/round15-remediation.md` (READ FIRST).**
> The **9 completeness-critic gaps**: D1 release pipeline · D2 xref subsystem · D3 `versification.py` fold-tables · D4 /customize flag
> cross-product · D5 glossary-streaming FLAGSHIP · D6 `corpus_index`↔matrix book-count · D7 migration re-run safety · D8 nav/opf order ·
> D9 kepub `-sN` rev-id. Two-lane file-disjoint (truth_owner=windows): **WIN** = `build_edition.py` + D2/D5/D8/D9 + build-needing gates +
> `deep-audit.js` config; **MAC** = `versification.py` + D3/D1/D6/D7 + build-free gates + cross-OS verify. Round-14 SETTLED =
> DEFERRED_BY_DESIGN. **✅ WIN D4 FIXED** (`_cascade = s2_group`; dict source provenance 0→545 in the `{S1-on,S2-off,eink}` combo;
> RED→GREEN regression + 78 tests + **G1 golden 9/9 byte-stable PASS**) · **✅ D2 partial** (`check_xrefs.py:52` regex guard). **▶ MAC**
> handed the confirmed-LIVE **D3** (Douay/Vulgate Ps 2:13 + 4:10 DROPPED → `_VULGATE_PSALM_FIXES` `(2,13)→(2,12)`+`(4,10)→(4,8)`) +
> D1/D6/D7 + cross-OS verify (LANE_HANDOFF WIN→Mac at TOP). Mac monitor armed.
>
> **★ 2026-06-21 (history) — Grok-revert cleanup DONE + pushed** (2026-06-21; memory `project_grok_cleanup`). **Mac's Opt# byte-verify DONE** — Opt#3 (`33b79387`) changed build output → **REVERTED** (it dropped tablet badges); Opt#2/#4/#5 byte-neutral → kept. **★ Rules+accuracy consolidation EXECUTED (2026-06-21, Phases A–E,G,H — 9 commits; lint 35/0, all gates green).** The radar/contradiction fixes, count+redundancy+bloat consolidations, hook lane-v2 parity, **RULES §2.6 work-phase loop + `dev/HUMAN_DECISIONS.md`**, and 8 website accuracy fixes all landed (folding the 47-confirmed rules audit + 9-confirmed website audit). **Phase F DONE + LIVE-VERIFIED** (Mac, 2026-06-22): www.yhwhyaway.com live at **91,555** + re-rendered social card; og cache-bust `?v=20260622`; GitHub v0.1.0 release body corrected; `yhwh-website` `3ab8f70..fb4cfcc`. **Remaining = the v1.0.0 device-QA gate** (queued in `dev/HUMAN_DECISIONS.md`). See `dev/IN_FLIGHT.md`.
>
> **★ 2026-06-25 — Kobo deep-audit: WS1+WS2+WS3 all DONE + Mac-verified; OOM tier-1 + #1 done; 1 flagship-OOM site + ¶
> re-split remain.** Program tracker `dev/audit/kobo-deep-audit-program-2026-06-24.md`; live tasks in `dev/IN_FLIGHT.md`.
> **WS1 mid-verse ✅ · WS2 cascade de-dup ✅ · WS3 Kobo run-on popups ✅** (eink-gated visible `·` (U+00B7) +
> `<br class="kobo-vn-br">` separators replacing the Nickel-dropped hidden U+2028; threaded kw-only `eink=False` through
> the cascade/badge/chunk/budget-pack/backmatter chains; 9 TDD + 224 tests; catholic-study eink epubcheck 0/0/0/0;
> non-eink 0 leakage; **Mac cross-OS PASS** 22897/0). Discrete Prayer-of-Azariah ToC ✅. **Flagship-eink build OOM
> (task #8):** Mac's P1 tracemalloc profile (peak 2937 MB, glossary ~480 MB) → I landed **tier-1 frees** (`del
> pre_badge_texts`/`repair_texts` + `pop _study_backmatter_entries`, ~636 MB; Mac-verified 1182 MB) + **#1 glossary-split
> streaming** (`split_study_glossary_document` → `_iter_study_glossary_pieces` generator; `apply_file_split` keeps
> `plan_names`, writes pieces to disk, scans ids/nav from disk; + 2 glossary-read skips in `_merge_scripture_base_files`
> remap & `retarget_demoted_toc_anchors` toc-book scan). **Byte-identical (catholic-study eink 453/453, `test_file_split`
> 54/54); flagship-eink monitored peak 2937 → ~885 MB.** ⚠ **Flagship FULL build STILL OOMs at 1 MORE site**
> (post-`retarget_demoted_toc_anchors`, ~1.4 GB, undiagnosed `✗`/MemoryError) → **WS1+WS2+WS3 flagship re-stage BLOCKED,
> the next-session pickup** (one more traceback driver run / Mac's `eink_oom_profile.py`). **¶/"weird symbol" = 162 EMPTY
> verse anchors** (RE-SPLIT not rewrite); ratification package airtight (Mac P2: 158/158 safe, 0 unclassified) → **awaits
> USER RATIFICATION** (`dev/HUMAN_DECISIONS.md`). **Mac guard-#7 sibling: 119k U+2028 in `vnote` translation-popup asides**
> (same dropped-separator pattern as the WS3 cascade fix) = QUEUED. **1En 90:13–18 column-zipper defect** (Mac P3) =
> queued content fix. HEAD `d6c3d270`; both remotes synced; quiescent.
> **★ 2026-06-22 session — drive cleanup + Kobo QA + Opus-audit prep:** drives cleaned (~395 GB; D/E/F mirrored canonical `YHWH-v2.4-backups\` tree) + the GAPS/_acquire **junctions** (→ D: live storage) were deleted then FULLY recovered (`D:\YHWH-v2.4-GAPS`=892 via Mac's GAPS-FULL; memory `backup-drives`+`gaps-folder` — NEVER delete the D: junction targets). Kobo QA → 3 fixes in flight to batch-verify: (a) endnote per-kind redundancy **FIXED** via the 3 note-rehaul flags on catholic-study/evangelical-reformed/eastern-orthodox (`note_group_by_category`=one heading/category) — **pending rebuild-verify**; (b) Gen 10:6→7 Kobo page break (kepub-build artifact, not yet pinned); (c) translation-popup formatting (TBD). New program: **EPUB structural+content audit** (final-product testing per platform; spec `specs/2026-06-22-epub-structural-content-audit.md` + `dev/audit_book_structure.py` TBD). **★ Deep-audit round 10 + round-11 gap-sweep RAN, REMEDIATION WELL UNDERWAY (2026-06-22→23).** Both audit lanes merged + the Mac's round-11 sweep enumerated the 8 completeness classes into **69 sites**. Master tracker (live per-class status) = **`dev/audit/round10-remediation.md`** (read FIRST). **✅ ALL WIN round-10/11 remediation COMPLETE (2026-06-23):** all 8 round-11 gap classes (gap-7 migration-runner tri-state/argv/ledger/atomicity · gap-8 standalone de-hardcode + str/int xref) + byte-stability leftovers (W3a stray-theme removal · W3b default-theme cache-key hash · W6 · W7 · theme_id) + the **frozen-app `content_root()` HIGH** (`sys.frozen` guard + ~37 read/write sites routed through `paths.*` across 20 files) + the **round-12 2-HIGH zip-reproducibility** (shared `scripts/core/zip_repro.py` → press_kit + exports). **char-vs-byte DEFERRED → grand audit** (real data: catholic-study 20.7M non-ASCII bytes → byte-measure would re-cut every edition + break the 9-KJV-byte-stable invariant; W7 byte-WARN mitigates the symptom). All TDD-verified (**test_core 46 + test_scripts 994** + new frozen-sim/zip-repro/migrate/build suites green) + pushed (5 legs each). Structural auditor RAN (Mac: 293/294 books green; 1 `1en` misordering to confirm). **★ NEXT = the FINAL joint grand audit** (user 2026-06-23: full auditor top-to-bottom, down to verse + word, no time limit) once Mac's batch wraps + cross-OS-verifies WIN's pushes — agenda (char-vs-byte all-edition re-cut · 1en misordering · `sources_base` lazy-PATH tail) in `dev/LANE_HANDOFF.md` + `dev/IN_FLIGHT.md`. WIN owns the #3 tablet/Apple clamp (Meqabyan bp-26/27/28) + the **K-R4-2 floor** (the user's Kobo vnote bug — NOT closed). **Phase F website + Phase-1 doc-accuracy = ✅ DONE + LIVE (Mac).**
>
> **★ Round-13 WIN OPEN items #5/#6/#9 FIXED + pushed (2026-06-23, this session):** inject `escape_attr` (4 title-attr sites; escapes `& < > "`, preserves the valid `'`) · 3 dead module-level `REPO` removed (re-verify caught 2 spec false-positives — `api/exports`/`api/preflight` `REPO` are LIVE) · `ZipInfo.create_system=0` in all EPUB writers (catholic-study built → 383 entries `create_system=0` · epubcheck 0/0/0/0); `sources_base`=conservative defer (read-only published data). Remaining round-13 = the joint merge w/ Mac's half (char-vs-byte #2 · #7 `audit_popup_formula` · device-QA). See `dev/audit/round13-remediation.md`.
>
> **★ Kobo device-QA workstream OPENED (2026-06-23):** user QA'd the `ethiopian-tewahedo` eink kepub on his color Kobo → 4 defect clusters (badges invisible under Cardo, `◇`→`◊` · redundant note-body boilerplate · cramped translation popups · mid-chapter page-breaks). Font-pack thread CLOSED (3 fonts/5 files Cardo+Geʽez+Arabic; website copy fixed+pushed). Fixes need REAL-path tracing — a trace-agent pass had WRONG file:line on all 3 paths; inert edits reverted (tree clean). **Worklist = `dev/audit/kobo-device-qa-2026-06-23.md`; ⚠ verify every fix against the BUILT epub before reloading the Kobo.**
>
> **Samuel+Kings:** Mac 6/6 @ full `GAPS/`; **WIN GAPS recovered to 892 files (full) 2026-06-22** via Mac's GAPS-FULL zip (was 697 after the junction-delete incident).
>
> **Catalog truth (current):** **4** canon-shaped study editions × **5** cover colours = **20** M3/M4 assets; website catalog ~**187** assets on v0.1.0 (187-vs-188 to reconcile post-cleanup). Retired built-in SKUs must not return — `lint_rules.check_retired_edition_skus`.
>
> **Corpus truth (current — the canonical live figures; RULES §1 · MATRIX_MAP · SESSION_PLAYBOOK · REPO_MAP all point HERE rather than hard-code):** **90,348** source notes in `content/notes/` · **72** kinds · **15** categories · **87** books · **6** editions (registry). The shipped superset is **90,181** note-refs (2026-06-29 device-QA rebuild, after the 1,364 User-note removal — ethiopian 90,181 · catholic-study 40,585 · evangelical-reformed 37,377 · eastern-orthodox 40,036; via `edition_stats.resolved_note_counts`); the in-repo README/COPYRIGHT are swept to it, and the website + brand surfaces (index/roadmap/BIOS/social-card) are being reconciled by the Mac lane (#3).

## Release status (v0.1.0 live · v1.0.0 NOT tagged)

**Shipped (v0.1.0 track):** M1 everywhere · M2 Apple · M4 Kindle catalog columns live · the full Ethiopian Bible EPUB + Kobo kepub. ci pytest triage green · orphan inline-marker strip · 4-edition test pins.

**Blocks the v1.0.0 tag (current 2026-06-28 — full detail in the release plan §8 + `dev/HUMAN_DECISIONS.md`):**
- **🚫 human-gated:** M2 Apple device re-QA (rebuilt tablet artifact) · M3 Kobo user tap round · M4 Send-to-Kindle device read · M5 Play phone QA · the user's `v1.0.0` **tag command** (cut when §8 DoD is green).
- **🔧 autonomous, NOT yet done:** v1.0.0 **desktop binaries** built + signed (Win `.exe` Azure-signed · Mac `.dmg` notarized · Linux `.AppImage` on CI) · **M5 Play EPUB** built + staged (the only column not built) · `gen_release_catalog --tag v1.0.0` + website deploy (Mac) + **SHA256SUMS** merge · **`RELEASE_NOTES_v1.0.0.md`** finalized vs the live catalog · asset-count reconcile (187/188-vs-87).
- **✅ DONE:** the audit arc (rounds 8→16 + remediation Phases A–I, byte-proven) · WS1 158-verse re-split (`6b690361`) · all gates green (`ci.py`/`trace_matrix` 0/`ebible verify` 0/9-KJV golden 9/9/G1–G5) · v0.1.0 retired-SKU cleanup (D1, 87 assets) · **M1 + M4 catalog columns LIVE** · **M3 kepubs built** · Phase F website + rules consolidation + MacClaude Opt# byte-verify.

## Next — v1.0.0 release gate (round-16 DONE; sequenced per the release plan §3–§4)

> **Rule:** the release gate is the active arc. Marathon / Esther / CAM / extra website passes = **HOLD** until the tag is cut. **Do NOT cut the `v1.0.0` tag** — that is the user's call (§8 DoD must be all-green first).

| # | Autonomous (a fresh session does this) | Then human-gated (`dev/HUMAN_DECISIONS.md`) |
|---|---------------|----------------------|
| 1 | **Desktop binaries** — Win `.exe` `dev/build_desktop.cmd` + sign `dev/sign_windows.ps1` (Azure) [WIN] · Mac `.dmg` `dev/build_dmg.sh` notarized [MAC] · Linux `.AppImage` GitHub-Actions CI | — (build-only; binaries attach at tag) |
| 2 | **Stage device-QA artifacts** — build the **M5 Play EPUB** (only column not built) + confirm M2 tablet / M3 kepub / M4 STK deliverables are staged | M2 Apple re-QA · M3 Kobo taps · M4 STK read · M5 Play phone QA |
| 3 | **Catalog + site** — `gen_release_catalog --tag v1.0.0` · website rebuild/deploy [Mac] · SHA256SUMS merge · finalize `RELEASE_NOTES_v1.0.0.md` · reconcile asset count | — |
| 4 | **Final gate** — re-run `ci.py` + `trace_matrix` + `ebible verify`; optional dishonest-code/stub sweep (§8 box) | the user runs the `v1.0.0` **tag command** when §8 is all green |

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; catalog study editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit during work; milestone = `bash dev/save_mac.sh -m "…"` (Mac) or `save-all.ps1` (Win). **Remote:** `origin` = GitLab + `github` = GitHub mirror (both private). Tests: `.venv/bin/python -m pytest` + `export PYTHONUTF8=1`. "continue" ≠ "save".

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).