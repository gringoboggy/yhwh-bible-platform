# Session state — current snapshot

> **➤➤➤ 2026-05-25 (LATEST) — read FIRST. EPUB Wave 3 PREREQS 1–2 DONE + COMMITTED; resume at prereq #3, then the features, after this /clear.** Wave 3's re-bake-dependent features are gated behind 3 infrastructure prereqs. **DONE this checkpoint:** **(1)** `dev/MATRIX_MAP.md` brought current — the Wave 2–3 presentation enum settings added to the Variable-trace table + a new "Presentation / reader-styling pipeline" section (the CSS-append vs base-re-bake distinction). **(2) Split `build_edition.py` 4085→3123 lines** → NEW `scripts/matter_pages.py` (the 18 front/back-matter `render_*`/`inject_*` functions) + NEW `scripts/epub_utils.py` (3 shared helpers `_xml_escape_text`/`_resolve_publishing`/`load_canons`); one-directional imports `build_edition`→`matter_pages`→`epub_utils` (no cycle); ALL moved names re-exported from `build_edition` (`# noqa: F401`) so callers are unchanged; `pyproject.toml` `E501`/`C901` ignore extended to `matter_pages.py`. VERIFIED byte-identical: `lint_rules` **16/0/0** · ruff clean · `test_presentation_polish.py` **35/35** (== pre-split baseline) · catholic-study rebuilt → **epubcheck 0/0/0/0** · smoke-import (no circular import; re-exports are the same objects). **▶ NEXT — prereq #3, then features #4–#7 (the session task list held all 7):** **#3** build `scripts/resync_marker_glyphs.py` (a surgical in-place marker re-bake transformer) + a **categorize-diff verifier** (categorizes baked markers/asides and proves a re-bake changed ONLY intended markers, byte-multiset conserved elsewhere — `categorize-diff` is a documented TECHNIQUE, no existing script; build fresh, TDD). **#4** `marker_style=numbers`: a base-wide change to `inject.build_marker` (`inject.py:150`) → superscript footnote numbers + a numbering pass with an explicit reset boundary + drop the inline category glyph; and FIX the stray `‖` — it is the `xref` category glyph reused as the `.note-back` back-link char in `build_aside` (`inject.py:170`), so give the back-link a fixed `↩` and render the category symbol as a deliberate in-note element (spec §12.4). **#5** symbols-into-notes + a clickable in-note symbol → `id="legend-<cat>"` glossary link. **#6** widen / drop-KJV verse popups — edit each edition's `popup_languages_default` + the unset-default fallback (~`build_edition.py:749`); NO base re-bake (spec §12.3). **#7** Nave's topical-index back-matter page (from the 26,335 `topic-nave` notes). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3; design spec `docs/superpowers/specs/2026-05-24-epub-presentation-polish-design.md` §4/§12. **Also this session (non-Wave-3):** disabled the `semgrep` Claude plugin (`~/.claude/settings.json` `enabledPlugins` → `false`) — its hooks shell out to a `semgrep` CLI not installed on this Windows box, so every prompt printed `command not found`; takes effect on a Claude Code restart / `/reload-plugins`. The 4 regenerated long-title covers (ethiopian/catholic/anglican/lutheran) were visually verified — the `_fit_title_font` fix holds and the titles clear the borders (closes the prior "USER eyeball the 4 covers" item).
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 STARTED — the two CSS-only popup-layout settings SHIPPED + verified.** Re-bake-FREE slice of Wave 3 (Presentation Phase 2). Both §7-wired like `title_page_style` and applied in `build_one` by APPENDING a variant CSS block to the edition stylesheet (same mechanism as the theme override) — popup/note HTML unchanged, **NO base re-bake**. **§4.2 `verse_popup_style` (`cards` default / `stack`):** `cards` → `_VERSE_POPUP_CARDS_CSS` (tinted witness cards, gold/purple spines on `.vnote-hebrew`/`.vnote-greek`); `stack` → flat base (no append). **§4.4 `note_popup_style` (`chip` default / `pills`):** `chip` → tinted rounded label-chip on `.note .note-label`; `pills` → bordered tappable pills on `.note a:not(.note-back)` (the negative-space selector isolating in-note cross-refs from the back-link glyph — baked xrefs carry no class of their own). NEW in `build_edition.py`: `VERSE_POPUP_STYLES`/`NOTE_POPUP_STYLES` consts + `_*_CSS` blocks + `apply_verse_popup_style`/`apply_note_popup_style` (called back-to-back in `build_one` right after the theme append). Wiring: `scripts/api/editions.py` (both allowed-field lists + 2 enum validators), `web.py api_customize_data` (defaults cards/chip), `scripts/templates/customize.py` (2 `<select>`s). **VERIFIED:** `tests/test_popup_styles.py` **16** (verse 8 + note 8) · **86** build-level regression (title_page/cover_templates/presentation_polish/popup_styles) green · `lint_rules` **16/0/0** · ruff clean · catholic-study rebuilt → **epubcheck 0/0/0/0** · shipped-stylesheet content-verified (both blocks present, appended last; cascade-correct — `.note .note-label` 0,2,0 wins over base `.note-label` 0,1,0, while the `note-comm-` hide rule 0,2,1 still wins so intentionally-hidden labels stay hidden). **▶ NEXT — the re-bake-DEPENDENT remainder of Wave 3** (gated behind the tools/matrix prereqs): `marker_style`→numbers · symbols-INTO-notes (+ symbol→glossary link) + drop the stray `‖` · widened/drop-KJV popups · Nave's topical back-matter page. Prereqs first: update `dev/MATRIX_MAP.md` (the 2 popup-style data-flows + the remaining enum settings) · build `scripts/resync_marker_glyphs.py` + a categorize-diff verifier · split `build_edition.py` (~3,860 lines). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 2 COMPLETE: §4.6 cover picker SHIPPED + a user-reported title-overflow fix + 3 pre-existing bugs fixed during browser QA. Committed this session.** **§4.6 (25-design cover picker + uploads, task #13):** NEW `scripts/core/covers.py` `COVER_TEMPLATES` (25 stems = 5 families {`01_ornate_leafy`·`02_classical_corner`·`03_beadline`·`04_minimal_lines`·`05_missal_central`} × 5 colors {`black`·`brown`·`forest`·`navy`·`red`}) + `cover_template_catalog()` (PIL-free, so the enum validator imports it without Pillow). `cover_template` field §7-wired EXACTLY like `title_page_style`: `api/editions.py` allowed-field lists + enum-validation block; `web.py api_customize_data` per-edition default `""` + a shared top-level `cover_templates` catalog. NEW `scripts/api/covers.py::api_apply_cover_template(edition_id, stem)` (audit-logged) — validate stem∈COVER_TEMPLATES + edition → `generate_edition_covers._compose_cover(stem, title_for_edition(id))` → `ensure_backup` + `atomic_write_bytes` to `covers/<id>.jpg` → `api_save_edition_meta({cover_image, cover_template})`; routed `POST /api/covers/<ed>/template` in `_POST_ROUTES` (distinct suffix from the `/main` multipart + DELETE routes). `/customize` "Cover" panel (static markup in `renderEditions`, behaviour in NEW `wireCoversSection` — no innerHTML reassignment, security-hook-clean): clickable 25-thumbnail picker (thumbs = sandbox-served `/content/covers/templates/<stem>.png`; pick → immediate server recompose → in-place preview refresh) + main-cover (`/main`) & per-book-art (`/book/<code>`) upload affordances. **23 tests** (`tests/test_cover_templates.py`). **Verified LIVE in-browser** (Playwright): 11 cover sections × 25 thumbs render, real click → recompose → "Cover updated." + preview refresh; all editions interactive (see fix #1). **TITLE-OVERFLOW FIX (user-reported):** `_compose_cover` drew at a fixed 72pt with no width handling → long titles ran into the border art. NEW `_fit_title_font` auto-shrinks 72→28pt (4pt steps) until the widest line fits a **150px side safe-zone**; the 4 long-title covers (ethiopian/catholic/anglican/lutheran) regenerated at 64pt with ≥162px side clearance, the other 5 unchanged at 72pt. **3 PRE-EXISTING BUGS FIXED (surfaced by live browser QA):** (1) **`/customize` wired ONLY the first edition** — the rebind loop's `wrap.querySelectorAll('[data-edition]')` ALSO matches each edition's psi11 Preview button; on the first such box `btn` is null and the unguarded `btn.addEventListener` threw (swallowed by a try/catch) → the `forEach` aborted → editions 2-11 had NO handlers at all (save · popup-langs · traditions · reading-plans · covers). Fix: `if (!btn) return;` guard + regression test `test_edition_rebind_loop_skips_non_card_boxes`. **Had been broken for 10/11 editions; source-string tests couldn't catch it.** (2) **SQLite cross-thread 500** — `/customize` + `/matrix` 500'd in the RUNNING app: `corpus_index.connection()` cached a conn opened in the `_warm_corpus_index` (main) thread, reused on `ThreadingHTTPServer` request threads. Fix: `sqlite3.connect(..., check_same_thread=False)` (index is read-only-by-convention; writes use a separate conn in `rebuild()`) + cross-thread regression test. (3) **5 cover-template PNGs were actually JPEGs misnamed `.png`** (the `*_red` variants) → `_send_file`'s SEC-001 magic-byte guard 415'd them (broken picker thumbnails). Re-encoded to real PNG (lossless) + guard test `test_every_template_file_is_really_a_png`. Also repaired a stale pre-existing test (`querySelectorAll('input, select')` → `…, textarea')`, stale since Phase 1 added the description/dedication textareas). **GATES:** 221 tests green · `lint_rules` 16/0/0 · ruff clean · editions.yaml unchanged. **⚠ Could NOT capture a confirming screenshot — the Playwright session repeatedly closed mid-run; title-fit is proven by test + measurement (all 9 titles ≤724px after fit) — USER to eyeball the 4 regenerated covers + flag any that still crowd.** **▶ NEXT — Wave 3 (EPUB Presentation Phase 2; the BASE RE-BAKE wave, riskiest + most Claude-dependent):** the remaining 3 enum settings (`marker_style`→numbers · `verse_popup_style` · `note_popup_style`) · symbols-into-notes (+ symbol→glossary link) · widened/drop-KJV popups · Nave's topical back-matter page. Prereqs (tools/matrix review #12): update `dev/MATRIX_MAP.md`, build the `resync_marker_glyphs.py` companion + categorize-diff verifier, split `build_edition.py` (~3,800 lines). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3.
>
> **➤➤➤ 2026-05-25 (prior — §4.6 now SHIPPED, see top). EPUB Wave 2: §4.5 per-book title-page art DONE + committed; §4.6 cover picker was NEXT.** Session commits: nested-`<a>` 746→0 + a surfaced RSC-001 → epubcheck 0/0/0/0 (`c83c743`) · title-only edition covers + universal PIL-anchor placement (`6e58570`) · **§4.5 `title_page_style` (full-bleed default / framed) + per-book title-page art** (this commit). **§4.5 mechanics:** NEW `build_edition.apply_title_pages(tmp, edition, canon_books)` — called in `build_one` AFTER the canon filter — injects each KEPT book's art (resolution: `book_covers` override → `content/covers/_book_defaults/<code>.jpg` → text-only fallback) into its `book-title-frame`; **full-bleed** = art fills the page + title overlaid on a dark scrim panel (`.book-title-page.style-full-bleed` in `epub_working/stylesheet.css`), **framed** = a contained art plate; `patch_opf_book_images` registers them in the OPF (chain, right after `patch_opf_fonts`); the `data-book-idx`↔`config.load_books()` mapping keys book→art. Setting §7-wired: `TITLE_PAGE_STYLES` const · `api_save_edition_meta` enum block · `api_customize_data` (default `full-bleed`) · `/customize` `<select>`. **VERIFIED:** `tests/test_title_page_style.py` 12 + 170 customize/editions regression green; built catholic-study → **epubcheck 0/0/0/0**, 66 art images packaged + OPF-registered, deutero books text-only; Genesis full-bleed title page **visually confirmed** (Playwright — art fills page, title legible on the scrim); ruff clean. **▶ NEXT — §4.6 (25-design cover picker + uploads), task #13:** add a `cover_template` field (25 stems = {`01_ornate_leafy`·`02_classical_corner`·`03_beadline`·`04_minimal_lines`·`05_missal_central`} × {`black`·`brown`·`forest`·`navy`·`red`}; §7-wire exactly like `title_page_style`) → a clickable 25-thumbnail picker in `/customize` (thumbs = the sandbox-served `/content/covers/templates/<stem>.png`) → on pick, recompose the edition cover via the EXISTING `generate_edition_covers._compose_cover(stem, edition_title)` (already refactored this session) + update `cover_image`; PLUS surface the EXISTING upload endpoints `/api/covers/<ed>/main` + `/api/covers/<ed>/book/<code>` (`scripts/api/covers.py` → `scripts/web.py::_save_cover_bytes` → `covers.validate_upload_image`, 2:3 ±20%; the per-book book art IS 2:3 so the validator is fine) as UI affordances. Reuse map captured this session; plan = `dev/PLAN_2026-05-24-epub-presentation-polish.md` §2E. Then the rest of Wave 2/3 per that plan.
>
> **➤➤➤ 2026-05-25 (prior — COMMITTED `c83c743`) — EPUBCHECK NOW CLEAN — nested-`<a>` (746 RSC-005) FIXED + a surfaced RSC-001 FIXED; catholic-study epubcheck 0/0/0/0.** **(1) Nested-`<a>` — the doc'd root cause was WRONG** (re-verified with real data, [[reverify-conservative-nogo]]): NOT a `generate_verse_popups.py` `vchunk` over-scope (that fn only ever wraps the bare `<span class="vn">` needle — byte-identical at the bake commit `b0bf913`; `inject.find_verse_region` matches only the clean span-only wrapper; the offending word-study `note-ref`s aren't even in `content/notes/`). ACTUAL cause = a **static artifact in the recovered base** (v28a-50 snapshot, defunct tooling): **746 `note-ref`-nested-INSIDE-`vn-link`** across **8 files** (psa `033/034/035` · sir `038` · isa `040` · jer `042` · 2co+phi `058` · 1ti+jam `059`) — not the 2 the prior banner named. FIX = NEW `scripts/check_nested_anchors.py` (stdlib-parser detector + byte-exact regex repair relocating each note-ref to a sibling *before* the vn-link, glyph order preserved); `--fix` → **746→0** (byte-multiset conserved per file; only those 8 files changed) + **JVM-free `nested <a>` gate wired into `scripts/ship-check.py`** + `tests/test_nested_anchors.py` (10). **(2)** Clearing those **unmasked an RSC-001** (Phase-1 regression): `_drop_placeholder_introduction` removed `introduction.xhtml` + its spine itemref but its **manifest regex used `[^/]*`**, which can't cross the `application/xhtml+xml` slash → dangling manifest item → "file could not be found". FIX `[^/]*`→`[^>]*` + a fast unit test + **strengthened** the integration test (it only checked spine+zip, MISSING the manifest — the false-confidence gap that let this ship). **VERIFIED:** rebuilt catholic-study → **epubcheck 0 fatals / 0 errors / 0 warnings / 0 infos**; 63 popup/presentation tests green; `ruff format --check .` clean. **COMMITTED `c83c743`.** Touched: `scripts/check_nested_anchors.py` (NEW) · `scripts/ship-check.py` · `scripts/build_edition.py` · `tests/test_nested_anchors.py` (NEW) · `tests/test_presentation_polish.py` · 8× `epub_working/index_split_*.html`.
>
> **➤➤➤ 2026-05-24 (PHASE 1 SHIPPED, prior).** (Supersedes the design-phase banner below — Phase 1 is DONE + COMMITTED, not "implement next.") **EPUB PRESENTATION OVERHAUL — PHASE 1 COMPLETE.** Shipped + verified: **left-align body text · cover fits frame (`object-fit:contain`) · colophon rewritten (real per-edition counts via `matrix`, no `TODO_`, no stale "1,371"; identity = YHWH Ya' Way Editions / © Bogdan Zorlescu) · "A Guide to the Notes" edition-aware symbol glossary (clickable `id="legend-<cat>"` anchors ready for the Phase-2 symbol→glossary link) · About-this-Edition (auto-generated spec from resolved choices: canon/book-count · selected popup-witness languages · per-category note counts · theme; + editable free-text `description`) · optional Dedication page · Back matter (Sources & Acknowledgments → Reference Tables → Closing Colophon) · `description`+`dedication` editable in `/customize` · dropped the placeholder `introduction.xhtml` · `h2` page-break-avoid CSS · inject OPF-anchor guards.** Front/back-matter order: **titlepage → dedication(opt) → colophon → Guide → About → [scripture] → Sources → ref-tables → closing-colophon.** Code: `scripts/build_edition.py` (render_*_page + inject_*_page family + `_drop_placeholder_introduction`), `web.py` (`api_customize_data`), `api/editions.py` (EDITABLE_TEXT +description/dedication), `templates/customize.py` (textareas + `input,select,textarea`), `epub_working/stylesheet.css`, `tests/test_presentation_polish.py`. **GATES GREEN:** lint 16/0/0 · `test_presentation_polish.py` 34 passed · `ebible verify` errors=0/24,015 paired · ruff clean · **NO `content/`/corpus/base-HTML change**. Reviewed by 3 fresh-eyes passes (code + verification + tools/matrix) pre-commit.
>
> **▶ #1 ITEM (CRITICAL — demo-affecting) — ✅ RESOLVED 2026-05-25 (see the LATEST banner above for the full account).** The 746 RSC-005 were **not** a `generate_verse_popups` `vchunk` bug — that inherited diagnosis was wrong (re-verified). They were a static recovered-base artifact (`note-ref` nested in `vn-link`) across 8 files, fixed by NEW `scripts/check_nested_anchors.py` + a JVM-free `ship-check.py` gate + `tests/test_nested_anchors.py`. A surfaced RSC-001 (dangling `introduction.xhtml` manifest item — `_drop_placeholder_introduction` `[^/]*`→`[^>]*`) was also fixed. **catholic-study epubcheck now 0/0/0/0.**
>
> **▶ FORWARD ORDER:** `dev/PLAN_2026-05-24-end-scope.md` (the single active plan; PLAN_2026-05-21 → `dev/archive/`). Waves: **0** foundation (README · asset-licensing docs for the Midjourney covers · Git-LFS note) · **1** = THIS (done) · **2** covers (title-only + universal placement + 25-design clickable picker + per-book art upload) · **3** Phase-2 settings (4 enum fields + markers→numbers + symbols-into-notes + widened popups = BASE RE-BAKE; ~~the nested-`<a>` fix lands here~~ nested-`<a>` already FIXED 2026-05-25, standalone) · **4** downloadable-desktop-app packaging.
>
> **▶ TOOLS/MATRIX/ARCH (review #12) — before Wave 3:** update `dev/MATRIX_MAP.md` (add matter-page data-flows + the 4 Phase-2 enum settings; self-upgrading-matrix rule) · **build `scripts/resync_marker_glyphs.py` (does NOT exist) + a categorize-diff verifier** (Wave-3 re-bake PREREQS) · **split `build_edition.py` (~3,800 lines) → `scripts/matter_pages.py`**. Any-time/cheap: a one-command **`make phase-gate`** (the §5 gate is doc-only; ran ~16 min ad-hoc). Wave 2: cover **preview endpoint** + per-template title-safe-zones. Wave 4: PyInstaller spec (+ hidden-imports for `scripts/templates/*`) + launcher + launch+build smoke.
>
> **Backups → D: + external E:/F:, NOT C: (low on space).** `GAPS` moved to `D:\YHWH-v2.4-GAPS` (junction at `GAPS/`; code reads it transparently); `exports/` cleared (regenerable); C: 16 GB free.

> **➤➤➤ 2026-05-24 (LATEST) — read FIRST. EPUB PRESENTATION OVERHAUL — DESIGN APPROVED + SPEC WRITTEN; implement next (after a /clear).** Driven by a real Apple Books review of the flagship (`apple_books_screenshots/`). Per RULES §2, the presentation choices become **configurable per-edition builder settings with defaults** (editions.yaml → /customize → build pipeline). **SPEC = `docs/superpowers/specs/2026-05-24-epub-presentation-polish-design.md` (read it first).** **Decisions:** `marker_style` = **numbers (DEFAULT — inline footnote numbers, "safe always")**; `badge` (verse-end count) is OFFERED but **DEFERRED** (needs a per-verse note container whose base-HTML injection point is TBD — user also unsure). `verse_popup_style` = **cards** (default) / stack. **Popup witnesses default = Hebrew + Greek (LXX/NT) + Latin + Arabic** — the English `kjv` is DROPPED from popups (it duplicated/mismatched the WEB reading text); JPS/Douay/Brenton stay selectable per edition/book (registry = 9 in `popup_versions.py`). `note_popup_style` = **chip** (default) / pills. `title_page_style` = **full-bleed** (default) / framed, wiring the per-book art in `content/covers/_book_defaults/<book>.jpg`. **Category symbols move INTO the notes** (no inline symbols). **Front matter → 2 pages** (colophon: "YHWH Ya' Way Editions, 2026, © Bogdan Zorlescu", REAL computed counts, zero `TODO_`, kill the stale "1,371/14"). Tech fixes everywhere: left-align (kills stretched justification), cover `object-fit: contain`, real counts. **Scope = all 11 editions.** **3-phase plan:** Ph1 quick wins (left-align · cover fit · front-matter consolidation) · Ph2 the CSS/template-variant settings + `numbers` marker + widened popups + book-art wiring · Ph3 DEFERRED `badge` mode. **▶ NEXT (post-/clear): invoke `writing-plans` on the spec, then implement Ph1–2** (TDD; output INTENTIONALLY changes vs current — pin the NEW expected output, don't assume byte-identity). Reference mockups: `EPUB-design-options.html` (outer folder, standalone, opens in any browser) + `.superpowers/brainstorm/`. A built check EPUB sits in `_epub_check/` (outer folder). **ALSO committed this session:** Track C corpus-expansion **SCOPE-CAPPED** to the five named reference works (Matthew Henry / JFB / Barnes / Torrey's New Topical Textbook / Vincent's Word Studies — all IN scope: Henry seeded χ.2, Torrey sourced on disk, the rest PD/CCEL via the χ-cluster pipeline, NOT blocked on the user); Voyage AI + any work beyond those five DE-SCOPED (PLAN §3 Track C). Phase E (Latin appendix, `man/1es/2es`) PAUSED behind this overhaul.
>
> **➤➤➤ 2026-05-24 (prior) — DEFERRED BACKLOG CLEARED + COMMITTED (the 5 items the prior session stopped on for context budget); gates all green.** **(1) C1.run §7.1:** `content/notes/__init__.py::load_notes` now parses via `ast.literal_eval` (`notes_io.load_notes`) instead of `exec_module` — data files are NOT executed (RULES §7.1). Byte-equivalence PROVEN pre-swap (exec vs ast over all 87 books = **69,969 notes / 0 mismatch**); FileNotFoundError contract kept; RED canary `tests/test_core.py::TestContentNotesLoader` proves the body no longer runs. **(2) Invalid-escape cleanup:** the two backslash-space OCR SyntaxWarnings (luk.py:8924 + mat.py:22432, Kenyon witness notes) removed at SOURCE AND in baked `epub_working` (surgical aside-text replace in index_split_053/052 — NOT a lossy regen; both files re-parse well-formed); 0 residual; corpus-wide guard `test_notes_corpus_has_no_invalid_escape_sequences`. **(3) `ebible audit` NEW** subcommand + `make audit`: aggregates the off-dashboard audits (caches/vulture/mypy/pip-audit) into one CI gate — pure `run_audit_suite` + thin `cmd_audit`, missing-tool skips (exit 0), `--require-tools` strict; 5 unit + e2e (pip-audit not installed here, skipped). **(4) P3/P4 tail:** preview.py `_NOTE_TITLE`/`_NOTE_LABEL` indices+comment fixed to canonical (title=5/label=6 per inject.py:657); `VALID_PHASES` single-sourced from `config._PHASE_ORDER` (validate_taxonomy + validate_schemas both reference it; phase4 dropped to match runtime, no content uses it); 3 brittle `len(editions)==11` to `>=11`; `ebible print` passthrough removed (print_cover pivot-dead). **DEFERRED (reasoned):** release.py 3×subprocess-DEVNULL (orphaned pre-pivot — archival per §7.4 is the call) · C3b.promotediv (guarded stub = forward δ.1.x.A feature). **Gates:** lint_rules 16/0/0 · `verify` errors=0 / 24,015 paired · ruff-format clean · git blast-radius 13 files intended-only (luk/mat ±1 char); corpus unchanged 69,969 (item 2 text-only). Tests: test_core 43 · 4 dedicated files 252 · targeted test_scripts subset 382 · TestContentNotesLoader 4 · TestEbibleAudit 5. **▶ NEXT — `dev/PLAN_2026-05-21.md` §4.1 + roadmap:** Track B manuscript marathon (CRITICAL PATH; resume 1Ki5 R2) · Phase-E Latin / vision-OCR / Phase 3-4 builder tail · web.py A5/A10/A14/A11 + remaining P3/P4 (C2.addnote shape · C1.tmp · C3a.release archival) · user-only: [USER] e-reader device check.
>
> **➤➤➤ 2026-05-24 (prior) — `test_scripts.py` RUNNABLE AGAIN: 976 tests, ~2.9 min, GREEN (D.hang + D.slow both fixed).** **D.hang** (deadlock): the 9 live-socket smoke tests used single-thread `HTTPServer`+`serve_forever`+`shutdown()` (no timeout) → HTTP/1.1 keep-alive blocked request 2 → `shutdown()` deadlocked forever; fixed by `ThreadingHTTPServer` ×9 + `test_ops` mocking `api_preflight`. **D.slow** (the ~1 hr a full run then took): a session-autouse conftest fixture `_stub_exports_epubcheck` stubs the real epubcheck/Java run over the 12 `exports/` EPUBs (minutes/call, hit by ~10 preflight-calling tests) — only for the real `exports/` dir, so `TestEpubcheckWrapper`'s tmp tests stay real. Also fixed **2 stale pins** the now-runnable suite exposed (both broke pre-session): `test_default_theme_is_classic`→`test_every_edition_has_a_registered_theme` (themes assigned 2026-05-22), and the popup-registry test (`douay`/`vulgate` baked at `42a59e0` → `has_data` True). **The "never run full test_scripts.py" rule is genuinely RETIRED** (SESSION_PLAYBOOK §4) — a full run is ~3 min now. **ALSO DONE this session:** web.py cleanup A1/A6/A9/A12 (`fb50001` — dead `_send_dict_result` removed, 2 stale docstrings fixed, 85 orphaned banners dropped); **`test_web_filesplit.py` (88 tests, ~11 s) + `test_matrix_psi35.py` (39, ~12 s) confirmed FAST** — the D.slow conftest stub covers them session-wide (no extra work needed). **▶ DEFERRED (owner said stop 2026-05-24 — context budget):** `C1.run` `exec_module` §7.1 smell (import WORKS; `content/notes/__init__.py::load_notes` should use the ast-based `notes_io.load_notes` instead of `exec_module` — small) · remaining `audit_*` (vulture/mypy/pip-audit) → a CI / `ebible audit` target (NOT the live dashboard) · P3/P4 tail (C2.phase4 PHASES const · C3a/b dead-code · D.pins `==`→`>=` · **preview.py `_NOTE_LABEL`/`_NOTE_TITLE` constants swapped vs canonical** — cosmetic). Full backlog detail: `dev/AUDIT_2026-05-23-DEEP.md` + `dev/PLAN_2026-05-21.md` §4.1. (Commits this session: `309c0f7` phi/jam · `0892270` G1/G2 sec · `1ae0d23` a11y→preflight · `507bf7a` hebrew backfill · `e090998` D.hang · `f7888ad` D.hang-correction · `001320f` D.slow · `fb50001` web.py cleanup · this doc-sync.)
>
> **➤➤➤ 2026-05-24 (prior) — C1.chap HEBREW BACKFILL SHIPPED (owner-approved) `507bf7a`.** The `ch_count`-default-50 bug had truncated the at-scale hebrew generator at chapter 50, so the four OT-KJV books with >50 chapters carried no auto `lang-hebrew` past ch 50. Re-ran at the original **`--min-confidence 0.65`** (the 0.7 default yields ZERO; at 0.65 ch1-50 candidates EXACTLY match existing notes → dedup-skip 1-50, add 51+ only). Added **+2,151 `lang-hebrew`**: psa 840→2502, isa 1031→1406, jer 1332→1422, sir 1134→1158. **Corpus 67,818 → 69,969.** Max hebrew chapter now = each book's `ch_count`. Isolation PROVEN content-level (only `lang-hebrew` rose per book; only those 4 books' 6 epub split files changed — additive `inject --book`, NOT a regen). Built + valid: flagship carries it (22,897 lang-hebrew asides == master), **epubcheck 0/0**; ebible verify errors=0/24,015; taxonomy 69,969/100%; lint 16/0/0. TDD guard `TestLongBookHebrewBackfill`. **▶ NEXT — OWNER-DECISION (`dev/PLAN_2026-05-21.md` §4.1):** remaining `audit_*` dead-checks → CI (not the dashboard) · P2 web.py god-module pass (A1/A6/A9/A12) · P3/P4 mechanical tail. (4 commits this session before this one: `77fb13c` rename · `309c0f7` phi/jam · `0892270` G1/G2 security · a11y+preflight `1ae0d23`.)
>
> **➤➤➤ 2026-05-24 (prior) — Autonomous backlog: G1/G2 security + a11y-into-preflight SHIPPED.** Three commits: phi/jam data-cleanup (`309c0f7`), G1/G2 security (`0892270`), a11y+preflight (`1ae0d23`). **G1** (`core/http.py`) — added a `{http,https}` scheme allowlist so `file://` can't bypass the SSRF guard (was an LFI). **G2** (`core/preview.py`) — the UNAUTH `GET /api/preview` now runs `sanitize_html` on note bodies (was an XSS). **★C2.deadchecks** — `check_a11y.run_all()` added + composed into `/preflight` (WCAG/EPUB-accessibility now surfaces on the readiness dashboard); the other 3 `audit_*` tools (vulture/mypy/pip-audit subprocess wrappers) were DELIBERATELY left off the live dashboard (manual/CI only — network/slow). TDD +11 (G1 5 · G2 2 · a11y 4). Gates green (lint 16/0/0 · ruff clean · taxonomy 67,818/100%). **▶ NEXT — OWNER-DECISION items (`dev/PLAN_2026-05-21.md` §4.1):** **C1.chap backfill** (Psalms 51-150 / Isaiah / Jeremiah hebrew — the `ch_count` code-fix is DONE; re-running the at-scale generators would add THOUSANDS of reviewer-draft notes = a corpus-scale **content decision** for you, so it was NOT done unsupervised) · the remaining `audit_*` dead-checks → CI (not the dashboard) · P2 web.py god-module pass (A1/A6/A9/A12) · P3/P4 mechanical tail.
>
> **➤➤➤ 2026-05-24 (prior) — phi/jam DATA-cleanup SHIPPED + certified (the ★BUGCLUSTER follow-up is DONE).** The 165 spurious `lang-hebrew` notes are stripped from phi.py (76) + jam.py (89) via AST-span removal (1,815 pure deletions) and **270 `lang-greek`** notes generated + promoted (phi 156, jam 114; `run_greek_at_scale --min-confidence 0.65`, the original χ.1 threshold). **Corpus 67,713 → 67,818** (−165 +270 = +105). `epub_working` was updated by **SURGICAL removal** of the orphaned phi/jam hebrew markers+asides (`index_split_058`=phi/b70, `_059`=jam/b79) + targeted inject of the greek — **NOT** the playbook §7 bare-base regen, which is **LOSSY** (it drops `harvest_existing_langs`-preserved popups — proven: it wiped `vnote-paz-1-30`'s Douay+Vulgate — and re-qualifies ~88 unrelated xref hrefs; **playbook §7 needs this caveat**). Byte-compat PROVEN content-level (aside-by-id): ONLY phi/jam asides changed (165 hebrew gone, 270 greek in), only 2 files touched, paz + all xrefs byte-identical. TDD guard `tests/test_corpus_chi1.py::TestNTBookLanguageInvariant` (no NT book has `lang-hebrew`; phi/jam have `lang-greek`). Gates ALL green: lint 16/0/0 · taxonomy 67,818/100% · schemas 6/6 · trace_matrix 0 · trace_repo 0 · ebible verify errors=0/24,015 paired · test_corpus_chi1 23 · test_enabled_kinds_unified 12 · build catholic-study → epubcheck 0/0. Tests 7,064 → 7,066 (+2). **Committed this session.** **▶ NEXT (autonomous backlog, `dev/PLAN_2026-05-21.md` §4.1):** P0 **C1.chap >50-chapter backfill** (re-run hebrew/greek at-scale for Psalms 51-150 / Isaiah / Jeremiah — the runner-key-50-default bug, now code-fixed; same regen family — use the surgical method) · P1 **G1** `file://` SSRF + **G2** preview-XSS (security HIGH, open) · P2 wire the 4 dead audit-checks into preflight.
>
> **➤➤➤ 2026-05-24 (prior) — DEEP AUDIT COMPLETE + matrix MINT-cert.** The deferred every-file audit (`dev/AUDIT_2026-05-23-DEEP.md`) ran read-phase A–H + synthesis (100% tracked code/doc/config coverage) + **17 verified safe-fixes** (dead-code, latent bugs incl. the `ch_count` + book-code finds, Windows `DEVNULL` hygiene, docstrings, doc counts — **NO content/corpus/build change**) + **matrix MINT** (trace_matrix 0 · taxonomy 67,713/100% · ebible verify errors=0/24,015 paired · catholic-study build → epubcheck 0/0/0/0; lint_rules 16/0/0; 7,064 tests = baseline). **Uncommitted on disk = the audit docs + the 17 safe-fixes, awaiting "save".** **Headline QUEUED (user go/no-go) → `dev/PLAN_2026-05-21.md` §4.1:** P0 **★BUGCLUSTER** book-code drift (`php`/`jas`/… — confirmed **165 spurious `lang-hebrew`** on Philippians+James + 0 `lang-greek`) — **book-code CODE-FIX SHIPPED `c41e6d2`** (recurrence prevented); the phi/jam DATA-cleanup (strip 165 + generate Greek + regen epub_working) is an **atomic follow-up** — full recipe in `dev/SESSION_PLAYBOOK.md` §7 + the C1.chap >50-chapter backfill; P1 **G1** `file://` SSRF + **G2** preview XSS (security HIGH, open) + LICENSE/COPYRIGHT = **"Copyright (c) 2026 Bogdan Zorlescu. All rights reserved."** (CC0 was applied at `637e793` then REVERSED per user — they want the program protected from copying/theft; incorporated Bible sources stay PD); P2 wire the 4 dead audit-checks into preflight. **NEW: `dev/SESSION_PLAYBOOK.md`** = the session lifecycle + verification-gate checklist.
>
> **State correction (E.sessionstate):** any "UNCOMMITTED" label below on Douay/Vulgate/Arabic/JPS is STALE — those shipped at HEAD `42a59e0`. This 2026-05-24 line is the current state.
>
> **Program renamed → "YHWH Ya' Way"** (committed); `LICENSE` = all-rights-reserved (Bogdan Zorlescu). **▶ NEXT TASK (fresh session): the phi/jam corpus data-cleanup** — the work-order is the top banner of `dev/IN_FLIGHT.md`; full recipe in `dev/SESSION_PLAYBOOK.md` §7.
>
> **➤➤➤ 2026-05-21 (prior LATEST) — read FIRST.** Corpus **67,713 notes / 71 kinds / 11 editions / 87 books**; demo build LIVE — `inject` places **99.76%** (66,173 + 1,381 base-baked / 67,713), `ebible verify` **errors=0 / 24,015 paired**, valid EPUB — **ALL 11 editions epubcheck-clean (EPUB-3.3: 0 fatals/errors/warnings/infos — each of the 11 editions verified individually 2026-05-22, not just the 5 canon-shape reps)**; master HTML **0/61 malformed XML** (2026-05-22 inject-wellformedness + RSC-005 nested-`<p>` + RSC-012 xref-code fixes; `audit` **B8** gate added). Health: `trace_matrix` **0 unresolved refs**, `validate_taxonomy` 100% attributed, ruff-format + `lint_rules` clean. Shipped chronology + latest commits: `dev/CHANGELOG.md`.
>
> **The map layer (read for "where / how / what shipped"):** `dev/PLAN_2026-05-21.md` (forward roadmap; supersedes `PLAN_2026-05-09`) · `dev/IN_FLIGHT.md` (live task tracker) · `dev/MATRIX_MAP.md` (data-flow + base-HTML structure) · `dev/REPO_MAP.md` (file/folder index) · `dev/CHANGELOG.md` (shipped chronology) · **`dev/SESSION_PLAYBOOK.md`** (session lifecycle start→work→verify→finish-clean + the consolidated verification gates). (This file was trimmed from ~896 KB on 2026-05-21; history in `dev/SESSION_STATE_archive_2026-05-21.md`.)

## What shipped (2026-05-21 arcs)

- **DOUAY-RHEIMS + CLEMENTINE VULGATE SHIPPED — PHASE-2 TRANSLATION SPINE COMPLETE (2026-05-23, COMMITTED `42a59e0`).** TABLE-DRIVEN: `_VULGATE_SEGMENTS` generated from the Copenhagen `vul.json` (composed vul→org→KJV), then every book driven to **SHIFTS=0** against the real Douay via `_vg_verify.py` (the table proposes, the gate decides — the eBible Douay diverges from the idealized Vulgate, so the gate caught every composed error: gen49, exo-not-reorder, dan 3/4 boundary, lje, mat5 Beatitudes swap, lev15, 20 psalm fixes). Cross-book Daniel additions → `paz` (Theodotion Benedicite reorder) / `sus` / `bel` (the Astyages verse routed from the Susanna tail — caught by content spot-check, NOT the gate, since these books have no Douay *source* book). `wis` included; `tob/jdt/sir` omitted (recension — `vul.json` agrees); 14 Douay≠Vulgate chapters via shared concat-tail segs. Shared `versification.vulgate_to_kjv` (+ `_vulgate_cross`); thin `extract_{douay,vulgate}.py`. Stores **74 bk each / 33,343 + 33,344 v**, 0 oox; baked (`douay`+`vulgate` in `_BAKED_NOW`): **+31,867 `vnote-douay` + 31,866 `vnote-vulgate`, 0 other-content mismatches** across 33,768 asides. `ebible verify` errors=0/24,015 paired; **epubcheck catholic-study+anglican-bcp 0/0/0/0**; `lint_rules` 16/0/0; 153 vulgate + 16 popup + 333 LXX tests. 8 residual SHIFTS = proven Hebrew↔Latin transliteration/clause noise (not misplacements). License: Copenhagen facts re-expressed in-repo (CC0-clean; JSON not vendored). Follow-ups: Phase E appendix Latin (man/1es/2es, clean-digital-first) + shared vision-OCR engine + prior-translation re-verify. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **TRANSLATION SPINE: Arabic + JPS SHIPPED (Phase-2 finish, 2026-05-23, UNCOMMITTED).** NEW reusable `extract_translation.apply_remap` (+ `extract(remap=)`) — a `(code,ch,vs)->coord|None` callback that remaps to KJV + concatenates same-coord collisions in source order (mirrors `extract_lxx_swete.build_verses`); `remap=None` byte-identical to before (PROVEN: kjv re-extract = identical). **Arabic (Van Dyck)** = KJV versification across all 66 books/1189 ch except 2 content-aligned tail-splits (1Ti 6:21+22→6:21; 3Jn 1:14+15→1:14) → `versification.arabic_to_kjv` + `scripts/extract_arabic_vandyke.py`; 66 bk / **31,102 v**. **JPS (1917)** — runbook's "Masoretic, reuse wlc map" REFUTED: eBible `engjps` is already KJV-renumbered (0 divergent ch / 929; total 23,145 = KJV OT count) → PURE IDENTITY ingest, no remap. Baked (`arabic`+`jps` in `_BAKED_NOW`): categorize-diff +31,100 `vnote-arabic` + 23,145 `vnote-jps`, **0 other-content mismatches** across 33,704 asides; `ebible verify` errors=0/24,015 paired; **epubcheck 0/0/0/0** (coptic+flagship); `tests/test_arabic_vandyke_ingest.py` 20 + `tests/test_jps_ingest.py` 9 + popup-version pin. **arabic surfaces in coptic-orthodox; jps not yet surfaced (user wiring decision pending).** NEXT = Douay+Vulgate (large; scope in `dev/IN_FLIGHT.md`). Detail: `dev/CHANGELOG.md` 2026-05-23.
- **PER-EDITION COVER FIX SHIPPED (autonomous backlog §4.0 item 2; visual-QA finding b, 2026-05-23).** Built EPUBs now ship each edition's declared `cover_image` instead of the master cover. NEW `build_edition.apply_edition_cover(edition, build_dir)` (reuses sandboxed `press_kit.resolve_cover_path`) swaps `cover.jpeg` after the copytree, mirroring the theme-override step; OPF cover-image property untouched (only bytes change). **Back-compat:** the 2 standalone bibles (`cover_image: ""`) keep the master (byte-identical). Verified end-to-end: catholic-study EPUB cover == `content/covers/catholic-study.jpg`, standalone-geez == master; catholic-study **epubcheck 0/0/0/0**; `tests/test_covers.py` (4 unit + 1 build-level); `lint_rules` 16/0/0. **Both visual-QA per-edition-differentiation findings now closed** (theme fixed 2026-05-22, cover today). Detail: `dev/CHANGELOG.md` 2026-05-23.
- **DEUTEROCANON LXX REORDER PASS COMPLETE — Judith + 1 Esdras + Manasseh + Azariah SHIPPED (Phase 2 deutero pass, 2026-05-23).** Greek popups on the final four deferred deutero books — **+849 verses** (LXX-Swete now **50 books / 26,126 verses**). All content-aligned vs the real Swete↔KJV (never identity-mapped). **Judith** (`_JDT_SEGMENTS`): ch1-14 identity; G15:14→KJV16:1 song-intro pull; ch16 offset +1 then catch-up merge G16:7+G16:8=KJV16:8. **1 Esdras** (`_1ES_SEGMENTS`): ch4/7/9 identity; ch1/2/3/5/6/8 Greek-fewer (KJV combines — 18 KJV verses correctly get no Greek); ch8 = 5 merges + 1 split (G8:49+G8:50=KJV8:50). **Manasseh** (cross-book `_cross_book`): **Swete Ode 8** — NOT the documented Rahlfs Ode 12 (which is the Nunc Dimittis here; caught by re-verifying the real source) — clean 15-verse identity → man 1. **Azariah/Song-of-Three** (cross-book `_cross_book`+`_PAZ_FROM_DAT3`): Theodotion Dat 3:24-90 → paz 1; prayer offset −23; G3:52 combines KJV 1:29+1:30; Benedicite reorders (angels/heavens swap + cold/frost/lightning permutation); G3:67/68 source-empty leave KJV 1:46/1:49 without Greek. **Byte-compat proven** (46 existing books byte-identical post-ruff-format); popup content-compare confirms only the 4 books' asides changed (jdt 339 + 1es 430 + man 15 + paz 65 gained Greek, 0 added/removed, neighbors unchanged). `ebible verify` errors=0 / 24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; **333 LXX tests**. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **TOBIT LXX (Swete) SHIPPED (Phase 2 deutero pass, 2026-05-23).** Greek popups on all of Tobit (short recension `Tob`) — **+242 verses** (LXX-Swete now **46 books / 25,277 v**). ch1-5+8-14 identity; **ch6** offset −1 (Greek 6:1 "she ceased weeping" concatenated onto KJV 5:22, then 6:2-18→6:1-17); **ch7** multi-divergence content-verified (KJV 7:8 = G7:8+G7:9 concat; G7:10 = the Greek-merge of KJV 7:9+7:10 → 7:9; KJV 7:12 absent; G7:12-17→7:13-18) — KJV 7:10/7:12 get no Greek (correctness). Categorize-diff: tob pure-additions (index_split_026, 0 del). `ebible verify` errors=0/24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; 239 LXX tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **SIRACH LXX (Swete) SHIPPED — the full Greek transposition (Phase 2 deutero pass, 2026-05-23).** Greek popups on all of Sirach — **+1,388 verses** (LXX-Swete now **45 books / 25,035 verses**). The famous Greek **30:25–36:16a block transposition** + internal verse-merges, ALL derived by content-aligning the real Swete↔KJV text (9 anchors verified: grape-gatherer G30:25=KJV33:16, G31→34, G32→35, G33→36:1-11, G34→31, G35→32, G36:1-15→33:1-15, G36:17-31→36:12-26; conflated seam G36:16 omitted; ch20/23/41 identity — litany verified 1:1). **Completeness fix:** `build_verses` now **concatenates** LXX-split verse-halves (14 cases incl. Baruch 3:34) so popups show the WHOLE verse, not a truncated clause — this also retroactively improved committed Baruch 3:34. Categorize-diff: Sirach pure-additions (037/038, 0 del); bar 1-line concat. `ebible verify` errors=0/24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; 223 LXX tests. **Deferred (reorder tables pending, §4.0):** Judith, Tobit, 1 Esdras, Manasseh, Azariah, aes. Detail: `dev/CHANGELOG.md` 2026-05-23. **NOTE: re-touches committed bar.py + epub_working/044 (concatenation).**
- **Deuterocanon LXX (Swete) — 5 verification-correct books SHIPPED (Phase 2 spine, deutero pass, 2026-05-23).** Greek popups added to **Wisdom, Susanna (Theodotion Sut), Bel & the Dragon (Theodotion Bet), Baruch, Letter of Jeremiah** — `vnote-greek` **+754 verses** (LXX-Swete now **44 books / 23,647 verses**, was 39 / 22,893). Each mapping was INTERIOR-verified against the real Greek↔KJV text (not just endpoints): wis/sus/bel exact identity; **bar** one verified ch3 split (KJV 3:34 = Greek 3:34+3:35); **lje** one verified head split (KJV 1:1+1:2 = Greek G1, then +1). **Judith was DEFERRED on discovery** — ch16 matches 25=25 but hides a split+merge (KJV 16:1 = Greek 15:14) that identity would misplace; per-chapter count is necessary-not-sufficient. Categorize-diff: **+1508 insertions / 0 deletions**, confined to the 5 deutero base files; KJV/Hebrew/existing-Greek untouched; aside count unchanged (35,585). `ebible verify` errors=0 / 24,015 paired; flagship **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; `tests/test_lxx_swete_ingest.py` 178. **Deferred (need verified reorder tables — measured divergences in `dev/PLAN_2026-05-21.md` §4.0):** Judith, Tobit, 1 Esdras, Sirach, Prayer of Manasseh, Prayer of Azariah, aes, 1 Enoch. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **Greek NT (Byzantine Majority Text) FULL ingest SHIPPED (Phase 2 spine, sub-phase 3, 2026-05-23).** Greek popups now on **all 27 NT books** — `vnote-greek-nt` **0 → 7,951** (categorize-diff: KJV + Hebrew + LXX-Greek all UNCHANGED, only greek-nt added). Source switched from the spec-locked Scrivener TR (which was romanized/unaccented) to the **Robinson-Pierpont Byzantine Majority Text** (accented Unicode, byztxt, PD/Unlicense) so NT Greek matches the accented LXX. NEW `scripts/core/versification.byzantine_to_kjv` (25 tests): identity for all 27 books (Byzantine = KJV-standard versification; single-verse omissions Luke 17:36 / Acts 8:37·15:34·24:7 gap-preserved) + the one Romans-doxology reorder (RP 14:24-26 → 16:25-27). Driver `scripts/extract_byzantine_nt.py` → `content/translations/byzantine-greek/` (7,953 verses = NT 7,957 − 4 omissions; 0 collisions / 0 out-of-extent; ¶ stripped). Registry `greek-nt` relabelled "Byzantine Majority Text" + added to `_BAKED_NOW`. `ebible verify` errors=0 / 24,015 paired; **all 11 editions epubcheck 0 fatals/0 errors** (build gate exit 0); `lint_rules` 16/0/0; 27 tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **LXX-Greek (Swete) FULL ingest SHIPPED (Phase 2 spine, sub-phase 2, 2026-05-23).** Greek popups now on **all 39 OT books** — `vnote-greek` asides **8,601 → 22,812** (every diff categorized: KJV + Hebrew UNCHANGED, only Greek changed = Brenton/harvest replaced by Swete + added to ~28 books). NEW `scripts/core/versification.lxx_swete_to_kjv` (137 mapping tests) maps Swete→canonical KJV: identity for most books (+ guard), with the structurally-reordered books remapped from **content-aligned real data** (NOT memory) — Psalms full renumber (merges/splits + the pervasive superscription offset; emitted count exact 2461=2461), Jeremiah's full OAN reorder, Theodotion Daniel's Additions (3:24-90 omit, the +3 ch4 offset), the 1 Kings 20↔21 swap, Proverbs' 24/29 reorder, Esther's 6 Additions omitted; Exodus 36-39 (tabernacle) deferred (reordered+abbreviated, omitted not guessed). Driver `scripts/extract_lxx_swete.py` → `content/translations/lxx-swete-greek/` (22,893 verses, 0 collisions, 0 out-of-extent; sigla/`[n]` markers stripped). Registry repointed `lxx-greek`→`lxx-swete-greek` (Brenton seed dormant). `ebible verify` errors=0 / 24,015 paired; **all 11 editions epubcheck errors=0/warnings=0** (built with the Swete Greek); `lint_rules` 16/0/0; 140 tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **WLC Hebrew FULL ingest shipped (τ.5-A.x / Phase 2 spine, 2026-05-23, TDD).** Per the fully-customizable-builder roadmap. `scripts/extract_wlc_morphhb.py` + `scripts/core/versification.py` (NEW; 42 tests in `tests/test_wlc_ingest.py`): morphhb OSIS → `<em>`-per-word + KJV-versification remap via morphhb `VerseMap.xml`. `content/translations/wlc/` seed (3 verses) → **full 39 books / 23,142 verses** (canonical KJV coords). Verse-popup Hebrew **8,710 → 23,142**; every changed base verse categorized = **0 corruption** (14,432 added · 1,547 te'amim/normalize · 175 versification corrections · 29 marker drops · 4 special-letter fixes incl. the **Shema** bug found+fixed by characterization · 0 removed). `coord_in_canonical_extent` 0 out-of-extent; `ebible verify` errors=0 / 24,015 paired; flagship **epubcheck 0/0/0/0**; `lint_rules` 16/0/0. WLC source = OpenScriptures morphhb (gitignored `_acquire/morphhb/`). **NEXT = Phase 2 cont.:** LXX-Greek (Brenton) → Greek-NT, then Douay/JPS/Vulgate/Arabic. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **Popup multi-version MODEL refactor (B1 / Phase 1) shipped (2026-05-22, TDD; ZERO output change).** Per the fully-customizable-builder roadmap (`docs/superpowers/specs/2026-05-22-fully-customizable-builder-roadmap.md`). NEW `scripts/core/popup_versions.py` = single-source popup-version registry (kjv/wlc/lxx-greek bake now; greek-nt/brenton-en/douay/jps/vulgate/arabic await Phase-2 data via the `bakes_now` gate); legacy aliases (english→kjv/…), `is_trusted_html` (original-language raw passthrough), `normalize_coord` versification seam. `build_vnote_aside` list-based; `build_edition.POPUP_LANGUAGES` superset (legacy parallel-bible slots preserved) + content-class stripper. **Byte-identical:** full regen ALL books → 0 diff (35,585 asides); flagship epubcheck 0/0/0/0; 138 popup tests; build smoke 31; lint 16/0/0. **NEXT = Phase 2:** acquire+ingest full PD translations (WLC/LXX/Greek-NT spine first) per the roadmap's locked sources, flipping each version's `bakes_now` as data lands; then Phase 3 (per-book version-selection UI), Phase 4 (per-note curation + source review). CSS for new classes deferred to Phase 2.
- **Per-edition THEMES shipped (2026-05-22, TDD).** Assigned `theme:` to all 11 editions in `content/editions.yaml` (was: none set → all defaulted to the no-op `classic`, identical stylesheet everywhere). Builds now emit **4 distinct stylesheet groups** — `classic` (ethiopian/anglican/geez/amharic) · `scholarly` (scholarly-academic/jewish/lutheran) · `devotional` (catholic/eastern-orthodox/coptic) · `modern` (evangelical-reformed). `tests/test_themes.py` (+3); all 11 rebuild clean; **epubcheck 0/0/0/0** on the 4 reps; `validate_schemas` + `lint_rules` 16/0/0 clean. Fixes visual-QA finding (a). **This is Phase 0 of the themes+multi-translation-popups arc** — spec/plan in `docs/superpowers/`; next build = Plan B1 (popup multi-version model refactor). No master-HTML/corpus change.
- **All 11 editions epubcheck-verified + Track D ruff lint cleanup (2026-05-22).** Rebuilt all 11 editions from HEAD (`build_edition.py --all --force --no-parallel`) and ran epubcheck (Java 8 + PyPI-bundled jar) on each → **every one 0/0/0/0** (closes "5 canon shapes → 11 editions"; the 3 never-individually-checked — scholarly-academic / coptic-orthodox / lutheran-confessional — confirmed clean). Track D ruff debt **1071 → 646** in verified batches: 265 semantically-null autofixes (UP045/F541/…); reviewed F401 (the `web.py` + `templates/*` re-export regressions were caught by the full suite and repaired, then **marked clean — `__all__` on 17 templates + PEP-484 redundant-alias on web.py**); F821 → 0 incl. a **real `check_a11y.py` missing-`Counter` bug** fixed. Then **Tiers 1+2** cleared 135 more (mechanical UP035/SIM114/B011/B007/E731 + bug-hunt B023/B904/F841/B905 — **bug-hunt found NO real product bug, all benign**) → ruff **1071 → 511**. **Tier 4** (config) then per-file-ignored E501 in `scripts/templates/*` (inherent embedded-HTML strings) → ruff **511 → 243**; CONSERVATIVE scope (C901/SIM105/N8xx/19 non-template-E501 kept VISIBLE as honest backlog, not masked). **Session ruff total: 1071 → 243** across 4 commits; the 243 residual is genuine low-priority backlog. `ruff format` clean · `lint_rules` 16/0/0 · full `pytest tests/` **6462 passed / 0 failed**. No content/master-HTML change. See `dev/CHANGELOG.md` 2026-05-22.
- **OPF BISAC fix → ALL editions epubcheck-clean (2026-05-22).** After the footnote fix, catholic-study + anglican-bcp still had 2 RSC-005 OPF errors: `patch_opf` emitted a BISAC `dc:subject` with `property="authority"` but no paired `property="term"` (EPUB3 requires both). Fixed `patch_opf` to emit the `term` meta. catholic + anglican now epubcheck **0/0/0/0**; omega0 patch_opf 32 + build-smoke 31 green. TDD `tests/test_canon_splice.py::test_patch_opf_bisac_subject_pairs_authority_and_term`. **Every edition (all 5 canon shapes) is now epubcheck-clean.**
- **CANON-SPLICED EDITIONS: footnotes cleaned (2026-05-22).** Smaller-canon editions (Reformed/Catholic/Tanakh/Orthodox) had ~2,373 orphaned note-ref markers (RSC-012): `build_edition.filter_books_for_canon`'s `_BOOK_SEGMENT_RE` let a dropped LAST book's segment swallow the shared per-file notes-section (+ verse-refs-section), removing KEPT books' asides while their markers survived. Fixed: segments now stop at those shared containers (kept asides preserved) + new Pass 3 drops dropped-books' orphaned asides. Verified all 4 canon shapes 0 RSC-012: reformed **2,373→0** + epubcheck 0/0/0/0, jewish 0/0/0/0, orthodox 0/0/0/0, catholic 0 RSC-012. TDD `tests/test_canon_splice.py`; `TestCanonFilter` 12 + build-smoke 31 green. **FLAGGED:** catholic-study/anglican-bcp emit 2 separate RSC-005 OPF `dc:subject`/authority errors (BISAC metadata, unrelated to footnotes — addressed next).
- **FLAGSHIP EPUB epubcheck-CLEAN — 0/0/0/0 (2026-05-22).** Java installed → first real `epubcheck` run surfaced 50 pre-existing validity errors beyond well-formedness (25 RSC-005 illegal nested `<p>` + 25 RSC-012 broken xref links; non-visual, never caught before — no Java). Fixed both: `inject.build_aside` uses a `<div>` flow container when the body carries block content (inline-only bodies keep `<p>`, byte-identical; CSS mirrors `.note-comm > div > .note-label`); normalized **667** xref link targets across 48 note files (`php/jas/ezk/nam/jol` → canonical `phi/jam/eze/nah/joe`) + `fix_xref_targets --apply` (resolved=1245, **unresolved=0**; clears the ~190 absent-Ezekiel xref residual). Regenerated master HTML (0/61 malformed), rebuilt flagship → **`epubcheck` EPUB-3.3: 0 fatals / 0 errors / 0 warnings / 0 infos**. 9 inject-wellformed + 17 marker-glyph + 19 verse-popup + `lint_rules` 16/0/0 green. Detail: `dev/CHANGELOG.md` 2026-05-22.
- **MALFORMED-XHTML INJECT BUG FIXED + master HTML regenerated (2026-05-22).** A flagship-EPUB structural validation for the [USER] e-reader check found the master HTML was **58/61 files malformed XML** (1,322+ split `<aside>`/marker tags) — every built EPUB would fail epubcheck + strict EPUB3 readers. Hidden because `ebible verify`/`audit` B7 check marker↔aside **pairing**, not XML well-formedness. Git-bisected to the first inject (`7a1aecf`); base `5ee2ad1` was clean. Root cause: 3 unsafe insertion offsets in `scripts/inject.py` — (1) `find_aside_insertion_point` `existing_ch!=ch` landed at `m.end()` (mid opening-tag); (2) `find_chapter_region_b` ended at the next chapter's `id=` attribute (inside its `<a>`) + ran to EOF for the last chapter; (3) the verse-end fallback in `find_marker_insertion_point` used the raw region edge. All now land at safe tag/`</body>` boundaries. Regenerated `epub_working/` from clean base (inject[fixed]→`generate_verse_popups`→`resync_marker_glyphs`): **0/61 malformed**, **feature-identical** to `ffa20c7` (note markers 67,555 · vn-links 36,556 · vnote asides 36,535 · `<aside>` 104,151 — all delta 0); `audit` B7 **24,015/24,015 paired** + B8 **61 well-formed**. Rebuilt flagship EPUB validates PASS (0 orphan markers, was 512). Guards: `tests/test_inject_wellformed.py` (7) + new `audit.py` **B8** well-formed-XML check (so `ebible verify` catches the class). Detail: `dev/CHANGELOG.md` 2026-05-22.
- **End-of-session audit + flake fix (2026-05-22).** Swept clean (git clean; `ebible verify` errors=0; `trace_matrix` 0; `lint_rules` 16/0/0; build smoke 31; marker-glyph/verse-popup/validate-schemas all green). Made the long-documented `test_build_all_route_serves_json` flake deterministic (mock `api_export_build` through the `/api/build-all` route, no real socket build) → full `test_scripts.py` green. Repo ready for a fresh session; IN_FLIGHT idle.
- **Marker tooltip fix (2026-05-22).** `html_title_for` made data-driven (returns the kind's `title_attr` from kinds.yaml; was hardcoded → "Note" default for the 10 newer categories); `resync_titles` (in `scripts/resync_marker_glyphs.py`) rewrote **66,437** marker tooltips. 17 marker tests; `ebible verify` errors=0; build smoke green; idempotent.
- **Symbol-system fix (2026-05-22).** `inject.glyph_for` was a hardcoded 5-glyph prefix map ignoring the `symbol` field in `kinds.yaml`/`categories.yaml` → only 5 of 15 category symbols rendered (11 kinds collapsed to `◇`; notably 26,335 `topic-nave` as `◇` not `✦`, and `hist-*` as `◇` not `⌂`). Made `glyph_for` data-driven (reads the kind's symbol) + new re-runnable `scripts/resync_marker_glyphs.py` rewrote **54,005** in-place glyphs (inline markers + note-backs). **All 15 category symbols now render** (`✦` topic 26,335; `◇` comm corrected to 2,690; the 10 scaffold categories show their glyphs; `comm-ai` `Ⓐ` resolves). 11 tests; `ebible verify` errors=0; build smoke green; idempotent. Deferred: `html_title_for` tooltips (same hardcoded shape).
- **Verse-popup regeneration SHIPPED (2026-05-22).** Closed the demo-critical gap: `scripts/generate_verse_popups.py` (+ `tests/test_verse_popups.py`, 19 tests) regenerated verse popups across all KJV-covered books — coverage **24% → 90.5%** (9,689 → 36,556 verses; 77 books ~100%). KJV English floor + harvest-merge-preserved Hebrew/Greek (the 11 originally-wrapped books kept their he/gr); old Gen 1:1/1:2 offset fixed; idempotent; `ebible verify` errors=0; build smoke + 19 popup tests green. 9 TDD tasks committed through `70ac7d3`. Deferred: full he/gr/Greek-NT dataset acquisition + Ge'ez popups for the 7 no-KJV books (Meqabyan I-III, 2 Enoch, Jubilees, 4 Baruch, 1 Clement). Spec/plan in `docs/superpowers/`.
- **EPUB-QA followups (2026-05-22, committed).** `build_epub.py` drops `onix/` at the packaging chokepoint (`EXCLUDE_DIR_NAMES`) — the CC0 give-away carries no commercial ONIX (completes Ω.0 de-commercialization at the package layer). `build_edition._retitle_html_pages` rewrites calibre's `<title>Converted Ebook</title>` → edition title (XML-escaped, idempotent) in `build_one`. Marker-density QA on the maximized flagship: mean **1.018** markers/verse, 46% of verses bare, only 156 (0.39%) >6, max 19 (Gen 1:2); dict-easton ⌂ = 9.2% of markers → **no readability regression**. TDD `TestOnixExcludedFromPackage` + `TestPerFilePageTitle`; 31/31 build-smoke + ruff + `lint_rules` 16·0·0. **FLAGGED:** verse-popup wrapper covers only ~24% of verses (book-level — 2Sa yes / 1Ki no); confirm intended-vs-gap before the demo.
- **Matrix maximization (2026-05-22, committed `b905e22`).** `editions.yaml`: `scholarly-academic` += `topic` category (now reaches full corpus, enabled 41,374→67,709 — `topic-nave`'s 26,335 were orphaned in 0 editions); `ethiopian-tewahedo` `max_phase mvp→phase2` (ships its declared `dist-typological`/`dist-mariological` + `dict-easton`, 37,495→41,285; no topical flood — flagship lacks `topic`). TDD `tests/test_enabled_kinds_unified.py::TestMatrixMaximization`. Also IN_FLIGHT→idle, ν.8 logged, scope-addenda index. 762 edition/matrix tests pass · trace_matrix 0 · build smoke valid.
- **Test suite GREEN** — triaged + fixed all 92 post-rebuild failures. Most were phase pins reading the trimmed `SESSION_STATE.md`/`IN_FLIGHT.md` + the moved `PLAN_2026-05-09.md` (→ `dev/archive/`); fixed at root cause via the new `tests.fixtures.assert_phase_recorded` chokepoint (reads CHANGELOG) + `lint_rules.check_no_ephemeral_doc_pins` guard. Plus `dict-easton` count refresh + 13 pre-existing code fixes. **6404 passed / 0 logic failures** (1 pre-existing flaky HTTP-socket test, `test_build_all_route_serves_json`, passes in isolation).
- **Base-HTML recovered + committed** (`5ee2ad1`) — the lost WEB scripture HTML; `ebible build` makes valid EPUBs again.
- **Inject-tail spill resolver** (`d3acc4f`) — `inject.find_verse_region_b_spill` resolves Strategy-B chapters whose verses spill across a split-file boundary (jer/psa/isa/1ch). `scripts/audit_base_html.py` classifies regular vs irregular layout. 99.21% → 99.48%.
- **Reference-corpus rebuild + Easton's** (`38b80d3`): Nave's Topical rebuilt clean from a CCEL PDF (`extract_naves_ccel.py` → 26,335 `topic-nave`, was 15,258 OCR-noisy); Easton's Bible Dictionary ingested (`extract_eastons_ccel.py` → 3,779 `dict-easton`, new kind under `hist`). `promote.batch_insert_notes` (one write/book). Corpus 52,859 → 67,715.
- **Coord guard + dead-dispatch repoint** (`a935701`): `canonical_verse_counts.coord_in_canonical_extent` rejects out-of-extent coordinates at the promote boundary (0 invalid-coordinate notes corpus-wide); `run.py`/`add_note.py` injector dispatch repointed from the lost `source_archive/`+`kings_session/` scripts to `scripts/inject.py`.

## Next (per dev/PLAN_2026-05-24-end-scope.md, toward the 2026-06-07 deadline)

> **ACTIVE TRACK = fully-customizable-builder roadmap** (`docs/superpowers/specs/2026-05-22-fully-customizable-builder-roadmap.md`): Phase 0 themes ✓ · Phase 1 popup-model B1 ✓ · Phase 2 translation spine — **WLC ✓ · LXX-Greek Swete OT ✓ · Greek-NT Byzantine ✓ · deuterocanon LXX COMPLETE ✓ · Arabic (Van Dyck, 66 bk/31,102 v) ✓ · JPS (1917, 39 bk/23,145 v) ✓ (committed `f6d90c6`)** → **Douay + Vulgate IN PROGRESS** (`c1bce0c`; shared `vulgate_to_kjv` — NT + all prophets + Genesis segments verified SHIFTS=0, recension omits tob/jdt/sir; **dormant, NOT yet baked — bake is all-or-nothing**; ~50-80 alignments remain — **RESUME per the D/V grind map at the top of `dev/IN_FLIGHT.md`**) · then Phase 3 (per-book version-selection UI) + Phase 4 (per-note curation). **Availability: all 9 non-geez/amharic editions can now PICK any/all/none of the baked translations in `/customize` before building (fixed the stale `has_data` flag → data-driven via `bakes_now`); defaults left per-tradition (user "decide later").** The four PLAN_2026-05-21 tracks below remain valid but secondary.

> **Matrix audit 2026-05-22 — CLEAN.** `trace_matrix` 0 unresolved refs (all 11 editions resolve) · `validate_taxonomy` 100% attributed (67,713) · `ebible verify` errors=0 / 24,015 paired · `lint_rules` 16/0/0 · `ruff format` clean. No integrity fixes needed.

1. **DEMO (north star) — visual QA BROWSER-PASS DONE 2026-05-22; findings → `dev/VISUAL_QA_CHECKLIST.md` "Findings".** Rendered all 5 representative editions (unzip → localhost → Playwright): canon ×5 / canonical TOC order / verse popups (Gen 1:1 = KJV+Heb+Greek; John 3:16 + Psalm 1:1 = KJV) / note glyphs (scholarly shows ✦ topical, flagship correctly lacks it) / OPF `urn:` id (no ISBN) — all VERIFIED; sole console error = favicon 404. **2 TDD findings (configured-but-not-applied; neither breaks epubcheck):** (a) per-edition THEME — **FIXED 2026-05-22** (themes assigned to all 11 → 4 distinct stylesheet groups; epubcheck 0/0/0/0; `tests/test_themes.py`); (b) per-edition COVER — **FIXED 2026-05-23** (`build_edition.apply_edition_cover` swaps in each edition's `cover_image`; 9/11 editions now ship their curated cover, the 2 standalone bibles keep the master; `tests/test_covers.py`; epubcheck 0/0/0/0). Both per-edition-differentiation findings now closed. **Remaining demo item = the [USER] e-reader DEVICE check** (tap-popup overlay / page-turns / e-ink) — the only non-self-verifiable part. **Base-HTML coverage is COMPLETE** — all 87 books / 1,702 chapters present (`audit_base_html.py --coverage`; NO book truncated). The ~156-161-note inject residual is purely **verse-level versification** (aes/1en/mq/sir/jub) — NOT mechanically addable. Map: `dev/MATRIX_MAP.md` → "Base-HTML structure & coverage".
2. **MANUSCRIPT MARATHON — PAUSED** (the other major track): 1ki5 R2 spec-review held; then 1ki6 / 1sa2 (Kings/Samuel Geʽez dual-witness collation). HEAVY → script-based, single-chapter, OOM-aware (memory `feedback_concurrent_agent_cap`), check-in cadence (`feedback_marathon_pacing`).
3. **CORPUS EXPANSION — scope-capped 2026-05-24** (memory `corpus-reference-expansion`): bounded to FIVE named works still IN scope (Matthew Henry / JFB / Barnes / Torrey's New Topical Textbook / Vincent's Word Studies — Henry seeded χ.2, Torrey sourced on disk, the rest PD/CCEL via the χ-cluster pipeline; NOT blocked on the user). De-scoped: anything beyond those five + Voyage AI embeddings.
4. **CLEANUP / UPGRADES** — see PLAN_2026-05-24-end-scope.md (and dev/archive/PLAN_2026-05-21.md for Track B/C detail: editorial note-key review, matrix vestigial-layering refactor, etc.).

## Conventions (unchanged)

Local commit only via `save.cmd`/`save.ps1` (PowerShell; no remote — deleted 2026-05-12). Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `EXEC_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
