# Session state — current snapshot

> **➤➤➤ 2026-06-10 (LATEST, 🪟 Windows, turn 69 — ✅ ② mint 3.1/3.3 TRUTH-RECORD ROTATION shipped (`3fd07450`) + ★NEW USER DIRECTIVE: the WEBSITE FORMAT MATRIX — all 9 full canon editions × 5 formats (Apple Books · Kobo kepub · Kindle · Google Play Books · standard EPUB "computer & everywhere else") × the 25-template cover set (5 designs ↔ 5 formats, colour = downloader's choice), on the website Downloads page; spec = `docs/superpowers/specs/2026-06-10-website-format-matrix-design.md` (CI-built+uploaded artifacts [zero home bandwidth], one-resolver target_reader profiles, cover-swap colour variants, catalog UI + generated manifest, phases M1–M5: M1 everywhere+Apple ready NOW · M3 Kobo post-K-R4-2 · M4 Kindle = Mac kindle_safe · M5 Play = user phone-QA). ✅ ③ ROUND-5 COMPLETE — eth 26.30 MB + kepub 34.14 MB on the MERGED tree (Mac's kindle_safe push merged `1cf257b5`+`6f6dc704`), ALL GATES GREEN (verifier ×2: 393 pieces / 86 singletons / 66,498 noterefs all-resolve / 0 promoted-dup-spilled · epubcheck 0/0/0/0 · kepubify 1/0/0); **r5 kepub ON the Kobo** (G:\ swap done, v0.1.0 → E:\kobo-archive) + **K-R4-2 tap-list READY** (`notes/2026-06-10-kobo-round5-tap-list.md` — 7 taps, NEW `dev/kobo_tap_calibration.py`; 195/66,683 asides over the pop floor; EPUB==kepub stripped sizes by execution) + ★2nd USER DIRECTIVE: NEW **`dev/EREADERS.md`** e-reader compatibility tracker (always-there map, RULES §0; agrees w/ TARGET_CAPS + matrix spec §2). ✅ ④ v1.0.0 ASSESSMENT DECIDED: **v1.0.0 = matrix M1(+M2) + θ.4 + the two user-acceptance fixes** — (i) matrix **M1 ships WITH v1.0.0** (CI matrix workflow + Downloads catalog; everywhere+Apple ×9; M2 colour variants with it if smooth, else v1.0.x) and is START-NOW work (not gated on taps); (ii) **K-R4-2 fix lands post-taps** → round-6 rebuild → M3 Kobo column joins when green; (iii) **M4 Kindle column joins on the user's K-KIN-1..4 acceptance** (code DONE, epubcheck-proven); (iv) **θ.4 IS IN for v1.0.0** (the one chance to wire the update channel before the wide release; data plane exists, launcher prompt + appcast = small desktop-stage impl; NO telemetry per the 2026-06-08 decision); (v) **M5 Play Books = v1.1** (post user phone-QA; never blocks); (vi) ordering-flake pair = quality SHOULD (Mac hunting), not a release gate. Columns appear on the site only as they ship (never-over-claim). ▶ NEXT WIN SESSION = **M1 implementation** (CI matrix workflow + `gen_release_catalog.py` + catalog UI; Mac's optional spec review folds in if posted) ‖ K-R4-2 fix the moment the 7 taps land. AWAITING USER: 7 Kobo taps (`notes/2026-06-10-kobo-round5-tap-list.md`) + Send-to-Kindle K-KIN-1..4.** The rotation slice (TDD; 17 new pins; rotator+lint files 70/70 green; ruff/mypy clean): **(a)** `rotate_truth_records.py` rotates BOTH journal marker families now — SESSION_STATE's `> **➤➤➤` AND IN_FLIGHT's `> **▶` (continuation `> **WIN-LANE…` lines ride with their entry; date ranges from entry-start lines only, never prose); **(b)** NEW `plan_board_rotation` rotates the LANE_HANDOFF board (frontmatter + newest 2 turn sections + every heading carrying "do NOT rotate" stay live; older sections archive to the pre-existing `dev/archive/LANE_HANDOFF_LOG.md`, sentinel-migrated; the live board tail carries ONE pointer line); **(c)** NEW `count_entries(rel, text)` = the single what-is-an-entry resolver shared by the rotator AND lint (matrix==build doctrine); **(d)** lint: LANE_HANDOFF added to `_TRUTH_BUDGETS` (40k/120k/2) + **`_ENFORCE_HARD_TRUTH_BUDGET=True`** (a hard-size breach now FAILs the pre-commit gate); **(e)** `save-all.ps1` is the STANDING rotation actor — post-leg-1 it runs `--apply` and auto-commits the rotation diff before the push legs, so the budget can never re-breach across milestones. Live-tree rotation applied this turn: IN_FLIGHT ~150KB→lean (49→2 entries), LANE_HANDOFF 142KB→lean (turns 69+68+STANDING live), SESSION_STATE keeps 2. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>
> **➤➤➤ 2026-06-10 (🖥️ Mac, turn 69 — ✅ the kindle_safe VARIANT SHIPPED END-TO-END (board ① — the item-5 arc's Mac half CLOSED) + the convergence red wall CLEARED in passing. ▶ ACCEPTANCE = the USER's Send-to-Kindle re-verify on `~/Desktop/Ethiopian_Bible_catholic-study_kindle-safe_2026-06-10T224859Z.epub` (K-KIN-1..4 re-check); MAC NEXT = item-② ordering-writer hunt + arms.** The variant (plan `docs/superpowers/plans/2026-06-10-kindle-safe-variant.md`, TDD ×7 tasks, 8 commits): ① `TARGET_READERS` + `resolve_target_reader`/`is_kindle_target` ONE resolver (kindle = 5th target; api validator imports the constant, web_editions surface routed, wizard 5th card + TARGET_CAPS entry + customize option w/ Send-to-Kindle copy) ② `apply_kindle_safe_css` (visible endnotes per the CONFIRMED E3013 — artifact-measured hidden text **486,188 → 955 chars**; K-KIN-3 seam fixes: `.book-title-page` forced-break drop + `h1.bookpage-title` exemption + art re-cap; `.vn-sep` → visible endnote bullets) ③ `apply_kindle_toc_rows` (K-KIN-2 pills→plain inline rows; post-bilingual pre-split; in-zip: 75 rows / 0 pill lists) ④ `patch_opf` stamps `yhwh:target-reader` (additive only when set; ★post-wrap: user GO'd a Mac Java install — Temurin 21.0.11 JRE at `~/.local/opt`, sha-verified — and **epubcheck on the kindle artifact = 0/0/0/0 BY EXECUTION**, stamp proven, Mac now epubcheck-capable) ⑤ verifier **gate 5 `kindle_safe_checks`** (effective display:none ≤10K last-rule-wins-paired + single dc:language + CSS-marker fail-fast; fires-on-defect proven BOTH ways; non-kindle artifacts never judged) ⑥ **byte-proof: catholic-study before/after builds SHA-IDENTICAL** (`f3fbb0eb…`) with the field unset (RULES 7.2 held) ⑦ artifact gates ALL GREEN incl. gate 5 ACTIVE. New `tests/test_kindle_safe.py` (14) + `test_kindle_safe_gate.py` (5); 128-green neighborhood; lint 31✓/2 pre-existing warns. **★Convergence meter (board ③): run 27308009548 = 6F+1E (from 10F+1E) — WIN P3 cleared schemas/consoles/manuscript/bcp47, BUT 4 reds were P3's OWN fallout, all FIXED here in passing:** audit_caches ×3 (Phase C added `_web_anchors` AFTER Phase A's audit fix → whitelisted, committed-fixture singleton) · chi4 `mrk` (the test's legacy `{mar,joh}` Gospel set vs the 135-code store normalization — raw-JSON readers bypass the loader-normalize) · 2 stale `#vnote-` detector pins → `#v-` (the retarget's intended, gate-2b-consistent form). **Next push ⇒ expect green-or-ordering-pair-only** (work_cache teardown ERROR = the parked item-② diagnostic, fired again as predicted). **K-R4-2 prep (board ④) ANSWERED → the QA note:** `hist` ≈19K in BOTH worst verses and it is ONE dict-easton note each (Easton's DAVID/PAUL) ⇒ design (a)+(b) with (b) splitting WITHIN a single note body; taps still gate the fix. Baton **windows** (truth_owner); mode=parallel. TRACKER-STATE active.
>

## Recent ships (full chronology: `dev/CHANGELOG.md`; rotated entries: `dev/archive/SESSION_STATE_archive.md`)

- **Mint-cleanup arc COMPLETE (LANE 0, Phases 0–6)** — anti-bloat lint guards · bootstrap slim · roadmap refresh · archive sweep · decommercialize (~5,300 LOC) · enforce gates (mypy + remote/CI) · **polish (mint-6: `/distribution` console · superpowers INDEX+lint · `audit_caches`→preflight · sonar/`/exec` scrub)**. Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
- Standalone Ge'ez Bible — Phases A-C shipped (`scripts/build_standalone.py`; 4 books; epubcheck 0/0/0/0; the 9 KJV editions byte-stable).
- English back-translation of the Ge'ez — collated Kings/Samuel (324 v) + all 151 Psalms (`content/translations/geez-tewahedo-en/`).
- Phase D own-versification re-ingest — Patrologia vision-transcription lane; PO Esther paused ~p35 (see the top journal entries).

## Next

> **TWO live Windows lanes (Mac paused; see IN_FLIGHT top).** (1) **BUILD — σ "Edition Cover + Truthful Front Matter"** (spec+plan written; start σ.1 `resolved_note_counts`, the build-accurate counter; then HOLY-BIBLE cover · Your-Edition page · /customize identity · Ge'ez/Amharic covers · live-console reconcile). (2) **CONTENT — Phase D1b PO Esther vision marathon (paused ~p35)** → finish Esther → ingest `est_patrologia` → Ge'ez→KJV xref → add to standalone → epubcheck 0/0 + 9 editions byte-stable; then D1b-batch (1ch/2ch/ezr/neh/job) + D2. Also open: ρ.3 **C3 polish** (folded into σ.6). Background lanes (RULES §2.5): code-debt tail · doc-coherence currency · test-coverage growth · Phase-E Clementine (1es/2es). (The truth-record rotation gap CLOSED 2026-06-10 turn 69 — the rotator covers ➤➤➤/▶/the board, and `save-all.ps1` rotates on every full save.) Quality/completeness over speed; no time-gating.

## Inventory pointers

> `dev/MATRIX_MAP.md` = data-flow + base-HTML; `dev/REPO_MAP.md` = file/folder index; `dev/SESSION_PLAYBOOK.md` = lifecycle/gates; `dev/EREADERS.md` = the e-reader compatibility tracker (per-reader caps/quirks/QA — update every device round); `dev/TOOLCHAIN.md` = tool inventory. Popup versions: `scripts/core/popup_versions.py`. Standalone build: `scripts/build_standalone.py`. Manuscript engine: `scripts/core/manuscript_collation.py`. mint-6 surfaces: `scripts/api/distribution.py` + `scripts/templates/distribution.py` (`/distribution` console); `docs/superpowers/INDEX.md` (plans/specs index, guarded by `check_superpowers_coherence`).

## Conventions (unchanged)

Local commit via `save.cmd`/`save.ps1` (PowerShell). **Remote RESTORED 2026-05-30** — `origin` = GitLab + `github` = GitHub mirror (both `gringoboggy`, private); `git push` to both works. Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 21)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_MY_BIBLE_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `DISTRIBUTION_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `HOME_HTML` · `INDEX_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML`. Roles since the idiot-proof IA: `HOME_HTML` = the reader-friendly landing at `/` + `/home`; `INDEX_HTML` = the maintainer note editor at `/notes` (+ legacy `/index.html`). Each cross-links to the others (lint_rules check 6.2).
