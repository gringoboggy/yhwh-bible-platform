# Session state — current snapshot

> **➤➤➤ 2026-05-28 (LATEST) — ✅ PSALMS EN — BATCH 2 shipped: Ps 32-60 (27 chapters / 437 verses) — committed THIS turn (no backup due — commit #1 since the last bundle); tracker IDLE; ▶ next = Batch 3 (Ps 61-100).** Psalms EN now covers **66 of 151 chapters / 1,051 verses** (Ps 1-60 contiguous + 68/71/101/115/144/150). Same method: 4 translator + 4 INDEPENDENT reviewer subagents → caught + fixed **22 faithfulness drifts** (incl. removed fabricated "[my rock]" Ps 41:10 + "Uriah" Ps 50:2; Ps 54:12 "usury"→"deceit"; Ps 38:2 "ways"→"mouth"; Ps 59:10 priest/washpot crux surfaced). **Verify:** total standalone EN paras 938→**1,375** (324 Kings/Samuel UNCHANGED + 1,051 psa), **epubcheck 0/0/0/0**, `epub_working/` diff empty, tests 38 (generic invariant covers the new chapters). Remaining: ~85 Psalms. **▶ NEXT (user-chosen steady batches): Batch 3 = Ps 61-100** via the same method. Spec §10; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PSALMS EN — BATCH 1 shipped: Ps 2-31 (28 chapters / 393 verses) — committed (0bb5b164) + E:/F: backed up; tracker IDLE.** Psalms EN now covers **39 of 151 chapters / 614 verses** (`content/translations/geez-tewahedo-en/psa.py`). Same method: 4 translator subagents (Opus; faithful to the LXX Ge'ez, NOT KJV; "Yahweh"; [brackets]) + 4 INDEPENDENT reviewers → caught + fixed **20 faithfulness drifts** (e.g. Ps 16:13 "wicked"→"sword" per ኲናት; Ps 17:3 "horn of my life" not "salvation"; Ps 9:38 "your"→"his ear"; Ps 18:6 "champion"→"giant"). **Verify:** total standalone EN paras 545→**938** (324 Kings/Samuel UNCHANGED + 614 psa), **epubcheck 0/0/0/0**, `epub_working/` diff empty. Tests +1 (generic `test_all_psa_en_chapters_mirror_geez_source_order` → file 38). Remaining: ~112 Psalms. **▶ NEXT (user-chosen steady batches): Batch 2 = Ps 32-67** (~28 ch) via the same translator+reviewer method. Spec §10; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PSALMS EN PROOF BATCH shipped: 11 chapters / 221 verses — committed (7c97970d); tracker IDLE.** Validates the new occurrence-index keying on REAL data. `content/translations/geez-tewahedo-en/psa.py` = Ps 1, 21, 23, 36, 46, 68, 71, 101, 115, 144, 150 (the 8 dup-verse/irregular chapters + 3 normal; tier `ai-back-translation-reviewed-tier3`; VERSIFICATION=own; source order MIRRORS the Ge'ez store exactly incl. dups + gaps). The standalone EPUB renders DISTINCT English on each duplicate verse — proven: **Ps 36's two v24** ("those who bless him inherit the earth" / "even if he falls he is not cast down") + **two v25** ("Yahweh makes a man's steps firm" / "I have been young and grown old"). **Method:** 3 translator subagents (Opus; faithful to the LXX Ge'ez, NOT KJV; divine name "Yahweh"; [brackets] for genuine uncertainty) + 3 INDEPENDENT reviewer subagents → caught + fixed **13 real faithfulness drifts** (Ps 36:7 "way"→"life" per በሕይወቱ; Ps 68:19 "enemies"→"reproach"; Ps 23:7/9 "lift up"→"open" the gates; Ps 144:14 dropped an inserted "all"; …). **Verify:** total standalone EN paras 324→**545** (324 Kings/Samuel UNCHANGED + 221 psa), Ps 36 = 40 paras, **epubcheck 0/0/0/0**, `epub_working/` diff empty. Tests +4 (`TestEnglishPsalmsProofBatch` → file 37). **▶ NEXT (user check-in): the remaining ~140 Psalms** (≈2,300 verses) via the same translator+reviewer method — pace TBD. Spec §10; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PSALMS-READY: source-order occurrence-index EN keying shipped (spec §10.6) — committed (27d10ccb); tracker IDLE.** The dup-verse keying flaw is fixed: `build_standalone` now keys the back-translation by `(verse, occurrence)` in SOURCE ORDER (new `en_occurrence_map()`) and `render_chapter_body` looks up `(gv, seen[gv])`, so Ps 36-style chapters (24/25/24/25) attach each distinct English to its own verse. **Byte-identical for ALL shipped content** — the 1ki/1sa/2sa EN stores have 0 dup-occurrence keys → every verse keys at occurrence 1 → lookups unchanged: rebuild = **324 EN paras (unchanged)**, 1ki6=33, **epubcheck 0/0/0/0**, `epub_working/` diff empty. TDD RED→GREEN (3 tests caught the `(ch,v)` collision); `tests/test_build_standalone.py` 31→33 (+`test_dup_verse_numbers_get_distinct_english_by_occurrence`, +`test_en_occurrence_map_keys_dups_by_occurrence`). 2 files changed (`scripts/build_standalone.py`, `tests/test_build_standalone.py`); ruff-clean. **▶ NEXT (user-chosen): Psalms EN proof batch** — translator+reviewer subagents render the 8 irregular/dup-verse Psalms (21, 36, 46, 68, 71, 101, 115, 144) + a few normal → write `content/translations/geez-tewahedo-en/psa.py` in source order MIRRORING `geez-tewahedo/psa.py` exactly (incl. its irregular numbering — do NOT "fix" numbering, that's Phase D) → rebuild → confirm distinct EN on dup verses in a real EPUB → check in before the remaining ~140. Spec §10.6; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ EN LANE SCALED to ALL collated Kings/Samuel (324 verses) — committed (dbe13575) + E:/F: backed up THIS checkpoint; clean /clear boundary.** After the 1ki6 proof, the standalone Ge'ez Bible's English back-translation now covers all 10 collated own-vers chapters: 1ki 1-6 + 1sa 1/3/17 + 2sa 11. Per chapter: a translator subagent (Opus) → an INDEPENDENT reviewer subagent that fixed in-place; the review caught real **MT-drift** — KJV numerals NOT in the Ge'ez (1ki 5:15 `፸፻`=7,000 not 70,000; 5:11→2,000; removed fabricated 5:14/5:16 figures) + MT-smoothed corrupt names re-anchored to the Ge'ez (1sa 17:12, 2sa 11:20; kept the Ge'ez 2sa 11:3 "Eliab"). Stores `content/translations/geez-tewahedo-en/{1ki,1sa,2sa}.py`; rebuilt → EN in every collated chapter's popups (vnote-text == verse count), **epubcheck 0/0/0/0**, 9 editions byte-stable (`epub_working/` untouched). Tests → 31. **▶ NEXT (post-/clear, user-paced): Psalms EN** — first add source-order-position EN keying for dup-verse chapters (spec §10.6: the `(ch,v)` en_map collides on Ps 36-style dups; align to `chapter_verses_in_source_order`), then translate+review 151 Psalms (≈2522 verses — check in) · OR Phase D (own-vers re-ingest; no cheap wins). Spec §10; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ EN BACK-TRANSLATION LANE: 1 Kings 6 PROOF COMPLETE — the standalone Ge'ez Bible's 1ki6 popups now carry a faithful, adversarially-reviewed English back-translation of the actual Ge'ez wording — committed (E1-E4, 92f91a46→fe4be746 + truth-record) + E:/F: backed up THIS checkpoint.** The signature north-star feature (RULES §1), proven end-to-end. **E1** tier `ai-back-translation-reviewed-tier3`; **E2** `build_standalone` renders an OPTIONAL `<p class="vnote-text">` English para (graceful — EN-less popups render exactly as Phase C); **E3** a translator agent (Opus) + an INDEPENDENT reviewer agent (cross-checked the Swete LXX Greek + Douay/Vulgate, this Ge'ez text's ancestors) → `content/translations/geez-tewahedo-en/1ki.py` (ch6, 33 verses), faithful to the LXX/3-Kingdoms recension NOT the KJV (vindicated at vv1/19/20/33; "Yahweh" for እግዚእብሔር; `[brackets]` mark genuine textual uncertainty in the corrupt cherubim/door section); reviewer verdict "ship after 2 fixes" (v7 dropped tool, v21 over-supplied numeral) — applied to convergence; **E4** rebuilt → **33 `vnote-text` paras in 1ki6** (0 in EN-less chapters), the English ADDS to (doesn't replace) the KJV xref, **epubcheck 0/0/0/0**, `epub_working/` untouched (9 KJV editions byte-stable). Tests +7 (`tests/test_build_standalone.py` → 30); lint clean. **▶ NEXT (user-paced):** scale the EN lane — rest of Kings/Samuel (≈324 verses) then Psalms (≈2522; Psalms dup-verse chapters need source-order-position EN keying, spec §10.6) — OR Phase D (own-vers re-ingest; NO cheap flag-only wins — verified: the 6 patrologia books are KJV-renumbered, need real re-ingest). Spec §10; plan `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PHASE C COMPLETE: standalone Ge'ez Bible render path + first proof EPUB (epubcheck 0/0/0/0; 9 KJV editions byte-stable) — committed (10 commits, 5e8afdc8→89e3b59b) + ready for E:/F: backup THIS checkpoint.** Subagent-driven TDD (fresh implementer per task + controller verification; an SDD final code-quality review + a Phase-C-recon + Phase-D-readiness scan ran as background lanes). **Shipped:** **C1** per-book `VERSIFICATION` attr (`own`/`canonical`, default canonical) in `scripts/core/translations.py` (psa formalized `own`); **C2** `scripts/core/standalone_store.py` generates the own-vers Kings/Samuel store (`content/translations/geez-tewahedo/{1ki,1sa,2sa}.py`, 324 verses, tier `manuscript-collation-tier2`) + `*_apparatus.json` sidecars from the 10 base-structured v2 collations; **C3a-c** `scripts/build_standalone.py` — a GREENFIELD Ge'ez body-XHTML generator + XHTML doc wrapper + OPF manifest/spine swap + nav.xhtml & toc.ncx rewrite + `build_standalone()` orchestrator (copies the `epub_working` skeleton with build_one's ignore, swaps the body, reuses `build_epub.build`/`patch_opf`/`apply_edition_cover`); **C3d** a `build_one` standalone dispatch guard that fires ONLY for `edition.get("standalone")` (no-op for the 9 KJV editions); **C4** Psalms LXX→KJV chapter-correspondence xref (`lxx_psalms_to_kjv`, confidence `interpolated`) + the proof EPUB. **PROOF EPUB:** standalone-geez = 4 books / **161 chapters** (1ki1-6 + 1sa1/3/17 + 2sa11 + Psalms 1-151); popups = KJV xref + manuscript apparatus, **NO English** (the next lane — never faked from KJV); **epubcheck 0/0/0/0**. **BYTE-STABLE PROVEN:** `epub_working/` git-diff EMPTY since pre-Phase-C; catholic-study (KJV) builds + epubcheck 0/0/0/0; `lint_rules` 15✓/1⚠/0✗ (the ⚠ = active-marker, now idle). Tests +23 (`tests/test_build_standalone.py`). **The SDD final review caught a faithfulness bug** (Ps 36's non-adjacent dup verses 24/25/24/25 were being sorted→scrambled by `translations.get_chapter`) → fixed to render in SOURCE order via `chapter_verses_in_source_order` (`89e3b59b`; pin `TestSourceOrderPreserved`). **⚠ psa-data flag → Phase D:** the HaCohen Ge'ez Psalter store has irregular numbering in 8 chapters (Ps 36 missing v21/v23 + non-adjacent dup 24/25; Ps 21/46/68/71/101/115/144 adjacent dup verse #s) — pre-existing ingest artifacts, now rendered faithfully (source order + unique anchor ids); correcting the DATA is a Phase-D / psa-ingest concern. **▶ NEXT (two open lanes, user-paced):** (1) **EN back-translation** — faithful English of the actual Ge'ez wording (agent path + adversarial review, generalizing Track F) for the own-vers content → fold into the standalone popups; (2) **Phase D** own-vers re-ingest of the ~36 KJV-renumbered geez books — the ~30 ocr-tier3 PLUS the 6 patrologia-tier1 (1ch/2ch/ezr/neh/job/est_patrologia). **Correction (verified 2026-05-28):** the 6 patrologia books are stored KJV-renumbered (COLOMETRIC-MERGE skeleton-fit), so they ALSO need a genuine re-ingest via the `source_authoritative` bypass (preserving the patrologia's own numbering) — NOT just a `VERSIFICATION="own"` flag (the earlier readiness scan's "cheap flag-only wins" claim is REFUTED). There are NO cheap Phase-D wins; every book needs real re-ingest. Spec §9 + plan `docs/superpowers/plans/2026-05-28-geez-standalone-render-plan.md`. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PHASE B COMPLETE: Ge'ez→KJV partial-anchoring cross-ref tool shipped + folded into all 10 v2 collations — committed (B1–B5) + E:/F: backed up + RAM cleared THIS checkpoint.** Subagent-driven TDD (one implementer + controller diff-verification per task), single-lane. **B1** Ge'ez numeral parser (`፬ ፻ ፹`→480, `፷`→60). **B2** KJV English number-word parser ("four hundred and eightieth"→480, "threescore"→60). **B3** curated Ge'ez↔KJV proper-noun map + matcher (ሰሎሞን→Solomon, ግብጽ→Egypt…; word-start match so ኪሩብ→"cherubims"). **B4** `build_kjv_xref` — score each base verse vs each KJV verse (numeral ∩ + proper-noun hits), **max-total-score monotonic anchor** + order-preserving interpolation, confidence `anchored`/`interpolated`. (A length-maximizing filter was caught displacing the strong anchors with spurious weak lone-name hits at 1ki6 → fixed to score-weighted; plan B4 spec corrected at the same commit.) **B5** `scripts/apply_kjv_xref.py` folded `kjv_xref` + `metrics.kjv_coverage` into all 10 v2 (1ki6 v1→KJV 6:1 anchored, v2→6:2). NEW pure `scripts/core/geez_kjv_xref.py` + the driver. Tests +19 (`test_geez_kjv_xref.py` 8 + base-structured B5 11 → file now 29). **Engine UNCHANGED (test_manuscript_collation.py 45/45); witnesses + Samuel goldens byte-untouched (0 calibration paths in ANY B commit).** Coverage honest = 14–61% anchored per chapter (numeral/name-dense chapters anchor best — 2sa11 62%, 1sa17 47%; narrative chapters lower; the rest interpolated + monotonic). Commits: B1 `7d8de28d` · B2 `434812ee` · B3 `f769e4b8` · B4 `cea51187` · B5 `4200979a`. **▶ NEXT: Phase C** — standalone render path: add a `versification` attribute to `translations.py` → generate an own-versification Ge'ez store from the v2 collations (each base verse → `(ch, geez_v, geez_text)` + EN back-translation + `kjv_xref`) → a `standalone: true` build branch → first standalone Ge'ez Bible EPUB (epubcheck 0/0; the 9 KJV editions byte-stable). Phase C gets its own detailed plan at phase start. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ 1ki2 BASE DECISION RESOLVED → CAM (A5 `351a7df8`); PHASE A now FULLY complete on a consistent CAM base — committed + E:/F: backed up THIS checkpoint.** User chose CAM (consistent base witness, truest to the own-versification north star). A5 changed `_pick_base` to measure material extent by **TOKEN (word) count, not verse-unit count** — verse-unit count conflated segmentation granularity with content extent (1ki2: GG 46 short units vs CAM 27 long units, but ≈equal tokens 777/803). Verified against the diff: ONLY 1ki2 restructured (base GG→CAM, 46→27 verses); the 9 other chapters + all 4 Samuel goldens keep base=CAM (R5 green / `TestCalibrationInvariants` 45/45 / `manuscript_qa.py` exit 0 — only their stored `base_rationale` wording shifted units→tokens). +2 tests (token-extent unit pin + 1ki2=CAM pin; base-structured file now 18). Blast radius was fully mapped first (only 2 `_pick_base` callers; no test pins 1ki2's base or loads the legacy kings collations). **▶ NEXT: Phase B** — geez→kjv partial-anchoring cross-ref tool (own detailed plan at phase start). Marathon witnesses immutable.
>
> **➤➤➤ 2026-05-28 (prior) — ✅ PHASE A COMPLETE: Ge'ez own-versification engine re-architected + the 10 done chapters re-collated (base-structured v2) — committed (A1–A4) + E:/F: backed up + RAM cleared THIS checkpoint. ⚠ ONE open decision (1ki2 base).** Subagent-driven TDD: one implementer + controller diff-verification per task; a background CAM hi-res pre-pull lane ran throughout (never-single-thread; buffer now f134v / 1ki12). **A1** `collate_base_structured()` = base witness's own sense-units as the primary verses (no KJV spine, no `_map_objects_to_spine`). **A2** per-verse `apparatus` via ONE whole-chapter Ge'ez `align_verse(other,base)` + cursor attribution + module-level `tokens_conserved()` (every GG+CAM token incl. ⟦illegible⟧ conserved exactly; classes {agree,disagree,lacuna,insertion}). **A3** Ge'ez-internal `metrics` (`witness_agreement_pct` / `lacuna_counts` / `kjv_coverage:None` informative) + `base_structured_ok()` honesty gate (fails ONLY on broken conservation OR a blank base verse — never KJV coverage) + `manuscript_qa._SEMANTIC_PASS_FLOOR` DEMOTED fail→informative-warn (rev.6; KJV is a secondary cross-ref, not a gate). Samuel goldens flow through the UNCHANGED `collate()` → TestCalibrationInvariants + TestQAMetaTool green (45/45), `manuscript_qa.py` exit 0. **A4** driver `scripts/recollate_base_structured.py` → 10 `content/manuscript/{kings,samuel}/collation/<ref>_collation_v2.json`; the 4 Samuel goldens byte-stable (git-diff-proven, 0 calibration paths in the commit); A4 also fixed a latent base=CAM assumption in `tokens_conserved` (surfaced by 1ki2). **Author email** → `gringo.boggy@icloud.com` (global git config + A1 commit amended; older history retains the old email — full rewrite offered, declined-by-default). **⚠ OPEN — 1ki2 base=GG:** GG 46 units vs CAM 27, but ≈equal content (GG 777 ≈ CAM 803 tokens) = a verse-SEGMENTATION difference, NOT content extent; `_pick_base`'s material-extent clause keys on unit-count. The OLD KJV-spine 1ki2 collation ALSO used base=GG (pre-existing, not a regression). Decision pending: keep base=GG, or refine `_pick_base` to token-extent (→ base=CAM for all Kings+Samuel, aligning the decision-of-record; gated — re-verify R5 base=CAM for the Samuel goldens, then re-run the A4 driver). **▶ NEXT: resolve 1ki2 → Phase B** (geez→kjv partial-anchoring cross-ref tool; expand to its own detailed plan at phase start). Tests +16 (`tests/test_manuscript_collation_basestructured.py`). Engine: `scripts/core/manuscript_collation.py`. Marathon witnesses immutable. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-27 (prior) — ✅ MAJOR REDESIGN APPROVED + SPEC/PLAN/RULES SHIPPED: the Ge'ez manuscript collation pivots from KJV-spine to Ge'ez-OWN-versification (base-witness-primary + KJV as secondary cross-ref) — committing + E:/F: backing up THIS turn; READY for /clear → fresh session executes Phase A. Marathon witnesses UNCHANGED + immutable.** User caught the foundational flaw: KJV (66-book Masoretic) can't be the *structure* of an Ethiopian Bible — it lacks the Ethiopian-only books + imposes a foreign versification on the Ge'ez/LXX recension. Root cause: `manuscript_collation._map_objects_to_spine` proportionally binned witness sense-units onto the KJV spine + `manuscript_qa._SEMANTIC_PASS_FLOOR=95` penalized recension-shorter chapters (1ki6: CAM 33 / GG 18 / KJV 38 → honest 86.84% "fail" despite 0 fabrication, witnesses adversarially clean). Brainstormed (superpowers) → user chose **own-versification primary + maintained KJV cross-ref** (option 2). **5-layer design** (`docs/superpowers/specs/2026-05-27-geez-own-versification-design.md`): immutable witnesses → base-structured collation (base witness's sense-units = the verses; other witness = apparatus via Ge'ez↔Ge'ez `align_verse`) → geez→kjv partial-anchoring cross-ref (numerals + proper-nouns + notes + interpolation, confidence-tagged) → own-versification store (generalize the Ge'ez-Psalms `source_authoritative` precedent) → standalone render path (`standalone:true`; the 9 KJV editions stay byte-stable). **Phased plan** (`docs/superpowers/plans/2026-05-27-geez-own-versification-plan.md`): A=engine re-arch + re-collate the 10 done chapters (1ki1-6 + 4 Samuel goldens kept byte-stable; NO data risk; parallel) · B=cross-ref tool · C=render + first EPUB · D=re-ingest the 15 store books (data-gated, last). **Re-collation is MECHANICAL from existing witnesses — NO re-transcription** (witnesses store own sense-unit numbering; the KJV projection was downstream). **RULES §2 +2 principles:** 4 (no shortcuts — completeness over speed; pause/redesign for a better-more-complete approach) · 5 (never single-thread — ≥2 lanes + auto-pick-next + side-task backlog). Also this session (committed `90cb102`): the aggressive RAM-clear bootstrap codification. Stale C-7 `1ki6_collation.json` (old KJV-spined) dropped. Env: Claude Code 2.1.153 (latest); MCP chrome-devtools/playwright/twilio connected. CAM pre-pull buffer staged through f130v (into 1ki8). **▶ NEXT (post-/clear): execute Phase A** via subagent-driven-development per the plan. Manifest: 1ki:1-6 witnesses done (re-collate base-structured in A); 1ki:7-22 + 2ki:1-25 + Samuel 55 pending. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-27 (prior) — ✅ AGGRESSIVE SESSION-START RAM-CLEAR codified into the BOOTSTRAP PROTOCOL (process/rule/hook + memory; NO code/corpus/base/build/test change) — committed + E:/F: backed up THIS turn; ▶ then CONTINUING into 1ki6 C-7 (user: "just continue after save/backup"). Marathon UNCHANGED.** User-directed "make this part of bootstrap protocol." Now a standing session-start step (after the triad + env-health check), NOT gated on RAM pressure (the old "only if <3 GB" rule retired): enumerate (incl. a `$PID` parent-walk to map + spare the session's OWN tree) → PROTECT-list = the hard safety boundary (Windows core · `claude` · `node`/MCP · the `pwsh`+`powershell`+`WindowsTerminal`+`explorer` tree · `MsMpEng`/AV stays ON · `svchost` network) → KILL recoverable bloat (0-window browsers + `msedgewebview2` · iCloud/OneDrive sync · vendor updaters · `M365Copilot`/`Widgets`/`AppActions`/Cross-Device · `SystemSettings` · the respawning shell hosts) → report freed RAM. **First run freed ~1.2 GB net (8.24→9.47 GB free; 21 process groups killed; Edge had 0 windows).** 4 layers: **RULES §0** (new "Session-start RAM clear" subsection) · **PLAYBOOK §1.4** (rewritten) · **`bootstrap-triad.ps1`** (tracked `dev/cc-hooks/` source + runtime `.claude/hooks/` copy — identical; hook verified runs clean) · memory `session-hygiene` (old "<3 GB / rarely worth it" stance replaced). Editing the runtime hook trips the auto self-modification classifier → user toggles auto OFF for that one edit. **THIS COMMIT = 3 tracked files** (RULES + PLAYBOOK + `dev/cc-hooks/bootstrap-triad.ps1`) + SESSION_STATE/CHANGELOG/IN_FLIGHT; the runtime hook + memory live outside the repo/bundle (persist across /clear on their own). **▶ NEXT: 1ki6 C-7** = collate GG (18 v) × CAM (33 v) × KJV skeleton via the Phase-2 tool → C-8 honesty → C-9 flip `1ki:6` calibrated. Exact C-7 command + AMBIGUOUS carry-forwards (v1 480-numeral, v12 verb, v17 `ክ/ኩ`) in `dev/IN_FLIGHT.md`. Manifest: 1ki:1–5 calibrated; 1ki:6 in-progress; 1ki:7–22 + 2ki:1–25 + Samuel 55 pending. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-27 (prior) — ✅ 1ki6 C-6 CAM reviewed-clean (R1+R2+R3 adversarial, APPROVE CLEAN, 33 v) + ✅ Samuel scope-ahead dossier shipped (background) — committed (2 commits) + E:/F: backed up; READY FOR /clear → resume 1ki6 C-7.** **C-6:** R1 caught + restored a CRITICAL dropped-scripture omission (the divine promise canonical vv11-12, skipped by the blind pass at the L-column foot after the `ክፍል ፴ወ፭` rubric); R2 caught 2 more v10 defects (the `ወእስር`/`በማእስር` feet-crop misread + the dropped cedar-fastening clause 6:10b) + certified the rest clean; R3 APPROVE CLEAN (`validate_witness` OK 33 v, divine name 1st-order `እግዚእብሔር`, boundaries clean). CAM 1ki6 = 33 v (printed-closer, year-first 480 dating) vs GG's 18 (LXX, house-first) — the year-dating PLACEMENT differs (CAM head / GG end) = a C-7 collation point. `CAM_topology` appended. **Samuel scope:** `dev/MARATHON_LOOKAHEAD.md` += 1sa 1-31 + 2sa 1-24 (55 rows) + TOP RISKS (headline: 1sa17-18 LXX short text); **Kings + Samuel both fully scoped-ahead.** ▶ **RESUME (post-/clear): 1ki6 C-7** = collate GG (18 v) × CAM (33 v) × KJV skeleton via the shipped Phase-2 tool (exact command + expectations + the AMBIGUOUS carry-forwards [v1 480-numeral, v12 verb, v17 `ክ/ኩ`] are in `dev/IN_FLIGHT.md`) → C-8 honesty → C-9 flip `1ki:6` calibrated. ▶ **THEN 2-LANE ACCELERATED ENGINE:** foreground chapter C-1…C-9 (MAX 1 heavy vision agent) + BACKGROUND CAM hi-res pre-pull of the next chapter (`scripts/acquire_cudl_master.py`, disk-only, RAM-gated); both Kings+Samuel scope-ahead dossiers feed C-1; NARRATIVE continuous / LIST-REGNAL check-in; no corner-cutting (deadline +1 mo). Manifest: 1ki:1-5 calibrated, 1ki:6 in-progress, 1ki:7-22 + 2ki:1-25 + Samuel 55 pending. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-27 (prior) — ▶ 1ki6 marathon ADVANCING autonomously (user away ~2h; commit-each-step + periodic E:/F: backup; RAM-guarded, cannot self-`/clear` so hi-res stays in isolated subagents, MAX 1 heavy agent). C-4 + C-5 DONE + committed.** **C-4 vision-locate** CORRECTED the stale pre-pull label: **CAM 1ki6 is entirely on f128r** (f128v = 1ki7, kept as pre-pull); no new CUDL pull; stray `.sonar` removed. **C-5 blind CAM transcribe = 31 verses** (`1ki6_witnessCAM_hires.json`; `validate_witness` OK, screen CLEAN, 0 illegible); CAM opens with the **480-year dating** (printed-closer, year-first) vs GG's LXX house-first incipit; divine name 1st-order `እግዚእብሔር`. **▶ NEXT: C-6** adversarial CAM review — **MAKE-OR-BREAK: prove the CAM v10→canonical-v13 jump (divine-promise vv11-12 absent) is a genuine recension minus vs a homoeoteleuton skip** (orphaned `ከ` at M-top = skip red-flag; GG HAS the passage at its v6). Then C-7 collate → C-8 → C-9 flip `1ki:6` calibrated. Full granular state + the C-6 instructions: `dev/IN_FLIGHT.md`. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — ✅ 1ki6 C-3 (GG adversarial review) DONE — witness reviewed-clean (R1+R2); + the rolling scope-ahead method WIRED into the marathon; committing + E:/F: backing up THIS turn → READY for /clear → resume 1ki6 C-4 (CAM hi-res pull).** **C-3:** R1 found 2 CRITICAL homoeoteleuton omissions (v5; the v15→v16 nave-doors across the f031r-R→f031v-L folio turn) + 5 major glyph/harmonization fixes; a fix-agent restored both omissions (re-confirmed on parchment) → **18 v**; R2 upheld all 6 fixes — 0 substantive defects, 0 fabrication, v6 illegible genuine, the 1ki6:1 LXX house-first incipit confirmed a genuine recension feature; STOP boundary confirmed (no bleed into 1Ki7). `validate_witness` OK · screen CLEAN. AMBIGUOUS (→C-7 CAM): v17 `ክ/ኩ` + minor v16. `GG_topology` appended. Manifest **1ki:6 in-progress** (close = C-9). **NEW WORKFLOW (validated + wired):** rolling **text-only** scope-ahead agents (concurrent-safe beside 1 heavy vision agent — 1 heavy + ≤2 light = ~8 GB free, flat) built look-ahead dossiers for ALL remaining KINGS (1ki7–22 + 2ki1–25) → `dev/MARATHON_LOOKAHEAD.md`, surfacing the big LXX divergences (1ki12 Miscellanies · 1ki14 minus · 1ki20↔21 swap · 2ki14–17 REGNAL · 2ki17 expansion · 2ki18–20 Isaiah∥); **Samuel still PENDING.** Wired into the collation plan (C-1 consults it; **METHOD NOTE 6 = the BLINDNESS RULE** keeps recension-content away from the blind C-2/C-5 transcribers) + a bounded-concurrent low-buffer **CAM pre-pull** design (RAM-gated; first live test next session). Memory `feedback-concurrent-agent-cap` updated. Env: user dedicates machine RAM to sessions; **AV/security stays ON** (my advice — no benefit to disabling, real project-risk). `lint_rules` 15✓/1⚠(benign active-marker)/0✗ · `ruff format` 1037 clean. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-27 (prior) — ✅ CLAUDE CODE ENV CLEANUP + a §0 session-start health-check rule (harness / user-config + 1 in-repo doc rule; NO code / corpus / base / build / test change) — committing + E:/F: backing up THIS turn; marathon UNCHANGED, resume 1ki6 C-3.** User-directed environment hardening (new-account session). **OUTSIDE the repo (user/parent config — NOT in this commit, recorded for context):** global `~/.claude/settings.json` plugins trimmed **~100→62** — kept LOGIN-FREE local only (superpowers, remember, 12 LSPs, 4 local MCP tools [playwright/chrome-devtools/serena/context7], Claude-native dev skills + service *knowledge* packs); REMOVED every login/token/remote-MCP plugin (~30: notion/linear/sentry/atlassian/vercel/cloudflare/supabase/rootly/postman/sourcegraph/pagerduty/…), the noisy SessionStart/per-tool-hook ones (outputai's 16 KB dump, dash0, agentforce, hookify, security-guidance, semgrep, data-engineering, hyperframes), and the learning+explanatory output styles (user: "no learning mode"). Editing global settings.json is classifier-blocked under auto (self-modification) → applied with the user toggling auto OFF. **Sonar fully eradicated** (user: "i don't want sonar anymore"): the 115 MB CLI (`AppData/Local/sonarqube-cli`), its User-PATH entry, the `.claude/hooks/sonar-secrets/` scripts, and `.sonar`/`.scannerwork` artifacts — all gone (supersedes the prior entry's "files remain installed"). Repo-parent `.claude/settings.json` 3 SaaS plugins (pagerduty/mongodb/posthog) removed; bootstrap-triad SessionStart hook KEPT (local, no network). **Auth: subscription/OAuth** (`~/.claude/.credentials.json` present) — no `ANTHROPIC_API_KEY` set or needed; the prior "org cancelled subscription" wall was pre-login (corrects the prior entry's API-key-required note). **IN-REPO (this commit):** `dev/CLAUDE_PROJECT_RULES.md` §0 — NEW "session-start environment-health check (after the triad)" rule: surface available Claude Code + plugin updates (apply only on user OK; NO forced mid-session auto-update), verify enabled MCP servers connected (`/mcp`), tokenless-by-design. Cross-session memory `feedback_no_external_hooks.md` (+ MEMORY.md index) records the standing preference. **⚠ env changes take effect NEXT launch — start Claude in the repo-PARENT `…\YHWH-v2.4-full\` (where the bootstrap-triad hook + project memories live), not the `YHWH v2.4\` subdir.** **▶ NEXT (post-/clear): 1ki6 C-3** = adversarial GG review — marathon state UNCHANGED; the C-2 entry below is the still-current marathon truth. Manifest: 1ki:1–5 calibrated; 1ki:6 in-progress (C-1+C-2 done); 1ki:7–22 + 2ki:1–25 pending. **[USER]** real-reader presentation eyeball still open.

> **➤➤➤ 2026-05-27 (prior) — ✅ ACCOUNT-SWITCH + PORTABILITY HARDEN (process / harness / docs only; no code / corpus / base / build / test change) — committed this turn + E:/F: backed up; READY for /clear → resume at 1ki6 C-3 next session (marathon UNCHANGED).** New Anthropic account on the same Windows user (`bogda`); old account locked out. The new org has DISABLED Claude Code subscription auth → API-key path required (`setx ANTHROPIC_API_KEY "..."` once, then reopen the terminal — fresh PowerShell/cmd will hit the org-block until this is done; the live Claude Code session inherits the key via parent process). Cleanup at `~/.claude/settings.json` (user-level, OUTSIDE the repo): removed entire `hooks` block (sonar PreToolUse `pretool-secrets.ps1` + UserPromptSubmit `prompt-secrets.ps1`, both 404'd internally); set `sonarqube@` + `cockroachdb@` plugins to `false` (cockroachdb's PostToolUse `check-sql-files.py` fired a non-fatal error on every Write). `.env` Voyage key scrubbed (`VOYAGE_API_KEY=` empty; `.env` gitignored anyway). NEW tracked `dev/cc-hooks/` directory (`bootstrap-triad.ps1` + `install_cc_hooks.ps1` + `README.md`) closes the gap in the prior 0f9748e5 commit — the runtime SessionStart hook lives in gitignored `.claude/`, so this tracked source + idempotent installer is what survives a fresh clone / new machine / new account. One command (`pwsh dev/cc-hooks/install_cc_hooks.ps1` from the cwd-parent) rebuilds the entire Claude Code surface. Cross-session memory note added at `~/.claude/projects/.../memory/reference_account_switch.md` (+ indexed in `MEMORY.md`) so a future Claude isn't confused. Key audit clean: regex scan of `sk-ant-` / `ghp_` / `AKIA` / `AIza` / `xox*` / `*_API_KEY=…long` across `scripts/`, `dev/`, root configs → no findings; git config has no embedded URL creds; no remote. **▶ NEXT (post-/clear, user-paced): 1ki6 C-3** = adversarial GG review — marathon state UNCHANGED; the C-2 entry below is the still-current marathon truth. Manifest: 1ki:1–5 calibrated; 1ki:6 in-progress (C-1+C-2 done); 1ki:7–22 + 2ki:1–25 pending. **[USER]** real-reader presentation eyeball still open.

> **➤➤➤ 2026-05-27 (prior) — ▶ TIER-3 MARATHON RESUMED — 1 Kings 6 C-1 + C-2 DONE + committed + E:/F: backed up; ▶ resume at C-3 (GG review) after /clear.** Fresh session resumed the Kings Geʽez dual-witness manuscript marathon on the ratified AGENT-path method (tight ≤1568px crops, MAX 1 heavy agent, auto-commit per step, /clear between heavy steps). **C-1 (locate+classify):** 1ki6 = NARRATIVE (2–4 rounds, continuous cadence); located by vision to **f031r-L(L27)→f031r-M→f031r-R→f031v-L(~L13)** ≈ 3.25 cols. A sonnet locator first overshot the end to f033v (~3× the density 1ki5 set — 13 v in ~1.65 cols); an opus boundary-finder REFUTED + corrected it, fixing the end at the temple-completion "በ፯ ፡ ዓመት" (7 years) immediately before the red ክፍል ፴ rubric + "ቤዶ ሰሎምን በ፲ወ፫ ዓመት" (Solomon's own house, 13 yr = 1ki7:1). **C-2 (blind GG transcribe, 1 heavy opus agent):** `content/manuscript/kings/calibration/1ki6_witnessGG.json` = **17 v** (LXX/3-Kingdoms compressed recension), `validate_witness` **OK**, non-Ethiopic screen **CLEAN**, NARRATIVE class self-check **0 flags**; 44 honest `uncertain` + 1 `⟦illegible⟧` (v6), concentrated on resolution-limited temple dimensions (cubit numerals) + Greek-derived architectural/loan terms; ✣ body-cross NOT harmonized to እ. **⚠ Open for C-3:** the 1ki6:1 incipit reads "ወውእቱ ፡ ቤት ፡ ዘሐነፀ ፡ ሰሎምን…" (LXX house-first; year-dating at chapter END), differing from the 1ki5 boundary-note's MT expectation "ወእምዝ አመ ኮነ ፬…" — C-3 adjudicates on the parchment. Committed as the C-2 step (witness + IN_FLIGHT + SESSION_STATE); E:/F: bundle at this /clear checkpoint. **▶ NEXT (post-/clear): 1ki6 C-3** = adversarial GG review (read `GG_topology.md` in full; hunt harmonization + boundary-omission; fix-loop to convergence via `escalate_if_unbounded`; non-Ethiopic screen) → C-4 CAM hi-res pull (f128v pre-pulled = 1ki6/7) → C-5/C-6 CAM → C-7 collate → C-8 → C-9 flip 1ki:6 calibrated. Manifest: 1ki:1–5 calibrated; 1ki:6 in-progress; 1ki:7–22 + 2ki:1–25 pending. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`. **[USER]** real-reader presentation eyeball still open.

> **➤➤➤ 2026-05-27 (prior) — ✅ PROCESS FIX (no code/corpus/base/build/test change): the triad-read recurring-miss fixed DURABLY + a full RULES coherence pass — being committed + E:/F: backed up THIS turn; READY for /clear → resume 1 Kings 6 next session.** The recurring failure: on a fresh-session "continue" I jumped to a `git log` + a SESSION_STATE-only peek and SKIPPED the triad (RULES + PLAN). **Root cause:** the `feedback-terse-bias-to-action` cross-session memory said "on continue, read SESSION_STATE top + IN_FLIGHT + git, then act" — actively overriding the §0 triad-read. **Fixed in 3 layers:** (1) **forcing function** — a project **SessionStart hook** `.claude/hooks/bootstrap-triad.ps1` (wired in `.claude/settings.json` at the repo-parent cwd `C:\Users\bogda\Documents\YHWH-v2.4-full`) injects the triad-read reminder at every session start (memory can't enforce a per-session automated behavior; only a hook can); (2) **memory reconciliation** — rewrote `feedback-terse-bias-to-action` so terseness governs what comes AFTER the triad (don't then over-read the tree/plans/runner-code or over-deliberate), NOT the triad itself (the ~700-900-line minimum) + strengthened `reference-bootstrap`; (3) **RULES §0** now states "continue/push/go-ahead at a fresh session = read the triad FIRST, then resume," with the hook noted. **RULES coherence pass (user-requested — read the whole 2161-line doc):** fixed 4 internal staleness/contradictions — §15 "4-point"→"5-point" pre-summary audit + added the `claimed-saved-but-uncommitted` drift class (missing since the §12 git-truth gate landed); §11/§12 dormant-"zip" save language → "commit" (matches §4 save=local-commit); §12 trigger-4 "in-memory rules 1–6 … 7th rule" → the cross-session memory store (matches the dual-store "Learning capture" model below it); §10's pre-pivot "digital retail / POD / buyer" bullet reconciled to the 2026-05-14 free-public pivot (§1). The hook + the 2 memories live under `~/.claude` (OUTSIDE the repo — NOT in this commit or the git bundle; they persist across /clear). **▶ NEXT (post-/clear, user-paced): 1 Kings 6** — Track-B Geʽez manuscript marathon, resume at C-1 (GG transcribe 1ki6); CAM f128v already pre-pulled (it is 1ki6/7). Manifest: 1ki:1–5 calibrated; 1ki:6–22 + 2ki:1–25 pending. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`. **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior) — ✅ TIER-3 MARATHON: 1 Kings 5 CALIBRATED (full C-1…C-9, both witnesses reviewed-clean) — chapter-close committed + E:/F: backed up; ▶ next = 1 Kings 6.** Resumed at 1ki5 GG R2 and ran to chapter close on the ratified AGENT-path method (tight ≤1568 px crops, MAX 1 heavy agent, auto-commit per step, controller frees system RAM per step; the USER runs `/clear` at their discretion — Claude does not self-invoke it). **Shipped:** GG C-3 R2 reverted 3 of the 4 AMBIGUOUS-PARCHMENT MAJORs that R1 had harmonized toward the printed Bible (v1 ×2, v6; v8 confirmed) → `67b1330`; CAM C-4 hi-res pull then C-5 blind transcribe (18 v) → `abdf503`; CAM C-6 R1+R2 applied 5 fixes (2 critical incl. a dropped word + a de-harmonization, 3 major — **3 had hidden inside `uncertain` flags drifting to the printed form**) → `5cdbc30`; C-7 collate = **semantic 100 %, base=CAM, ww-agreement 12.6 % (distinct recension), 0 fabrication, lacuna 0**; C-8 independent honesty check PASS; C-9 manifest flip + ledger row. **KEY FINDING:** CAM 1ki5 physically lives on **f127v-R + f128r-L**, NOT the folio-arithmetic guess (f128r+f128v) — the f128v I pulled is actually 1ki6/7 (kept as a pre-pull for the next chapter); CAM chapters span columns, so locate by VISION, not arithmetic. **System upgraded (self-upgrading-matrix):** `CAM_topology.md` + `GG_topology.md` appended with the 1ki5 scribal-hand facts + the meta-rule *re-adjudicate every `uncertain`-flagged token (harmonization hides inside the flags at column-edges)*; memories added (CAM folio-location; Glob/Grep skip gitignored → reach GAPS images by absolute path). **Manifest: 1ki:1–5 calibrated; 1ki:6–22 + 2ki:1–25 pending. Scope: Kings 47 + Samuel 55 = 102 ch; 9 done, 93 left (42 Kings + 51 Samuel; Samuel deferred, Kings active).** Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`. **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior) — ▶ TIER-3 MARATHON RESUMED (Kings/Samuel Geʽez) — execution method RATIFIED + paid-API DE-SCOPED; clean resume point set for the post-`/clear` restart. Doc-only commit (no code/corpus/base/build change); this session = audit + planning only (NO vision run, NO API spend).** **DECISIONS (user-directed 2026-05-26):** (1) the **paid script-path** (`scripts/run_manuscript_{transcribe,review,collation}_at_scale.py` + `scripts/core/manuscript_vision.py` → optional `anthropic` SDK + funded `ANTHROPIC_API_KEY`; `requirements.txt` gates it as "the paid corpus-augmentation pass") is **OUT OF SCOPE** — no API budget. Verified on disk: `anthropic` NOT installed, `ANTHROPIC_API_KEY` NOT set. Those scripts stay as inert/dormant committed infra (NOT deleted) in case a key is ever funded. (2) The marathon runs on the **AGENT path** (Claude Max, no per-call $) — the method that produced clean 1ki1–4. (3) **Crash root-caused this session from file sizes:** the 4 OOM crashes were NOT the source files (GG folios **0.7 MB**, CAM hi-res **~10 MB**) — they were agents **LANCZOS-upscaling WHOLE folios 4–8×** (→30–60 MB buffered/agent ~20 min) × **3 stacked** ≈ 400k tokens → allocator panic. The vision pipeline downsamples >1568 px anyway, so whole-page upscale was doubly wasteful (huge bytes + tiny fidel). **RATIFIED METHOD:** zoom by **tight region crop** (one column/verse-band, ≤1568 px → fidel fills the budget = sharper AND ~1–3 MB), **MAX 1 heavy agent**, **auto-commit each step**, **`/clear` between heavy steps** (releases the parent buffer). **Unit = ONE agent-step, NOT a chapter** (a crash loses ≤1 step; resume from witness/manifest on disk). **▶ RESUME POINT (post-`/clear`): 1ki5 GG R2** — adversarial review of `content/manuscript/kings/calibration/1ki5_witnessGG.json` (R1 fixes applied; re-scrutinize the 4 AMBIGUOUS-PARCHMENT loci M-1…M-4 — orthographic/sub-glyph, may carry to C-7 CAM cross-ref per METHOD NOTE 4). Then C-4 CUDL f128r pull → C-5 CAM blind transcribe → C-6 CAM review → C-7 collate → C-8 → C-9 flip `1ki:5→calibrated`. Manifest: **1ki:1–4 calibrated, 1ki:5–22 + 2ki:1–25 pending.** Full method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md` (the EXECUTION METHOD note). **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior) — ✅ IN-DEPTH AUDIT (cycle 2) COMPLETE — full-suite sweep caught + FIXED 10 stale pins; ALL gates green; healthy, no CRITICAL/HIGH open. COMMITTED `b0508789` + E:/F: backed up (commit/backup #2). Full-suite re-run **OBSERVED 7,368 / 0** this turn; this truth-record update committed on top. ▶ READY FOR THE PRE-TIER-3 /clear.** The first full-suite run since 2026-05-25 = **10 failed / 7,358 passed**; ALL 10 are **stale test-pins / uncatalogued data** from the 3 intervening ships (Torrey, the `web.py` split, Phase E) gated on targeted-not-whole-suite tests — **zero logic regressions, zero from the de-dup.** FIXED all 10: `test_matrix_psi35` B2/B3/B4 → `web_matrix.py` (markers moved with the matrix fns in `bae92e4`); `test_web_filesplit` api_covers → assert `scripts.web_covers` (relocated in `bae92e4`, still re-exported); `test_vulgate_douay_ingest` → per-store `EXPECTED_BOOK_COUNTS` (vulgate 77 / douay 74 after Phase E); `test_audit_caches` ×3 → whitelisted `torrey_topical` in `.cache_audit_whitelist.py`; `test_time_travel_psi37` ×2 → catalogued Torrey (`prefix: "Torrey's New Topical Textbook"` year 1897) in `content/source_dates.yaml` (coverage 73.9%→97.6%; ceiling drop-count back in range). **VERIFIED:** 10 node-ids + the 5 touched files in full = **331 green** (the 7,358 unaffected — only test/data/config changed, no product code); then a full clean serial sweep **OBSERVED 7,368 passed / 0 failed (27:37, 2026-05-26)** — the unaffected deduction is now confirmed by observation, not inference. **FRESH GATES from scratch:** `ci.py --no-tests` CLEAN (ruff format ✓ · `lint_rules` 16/0/0 · mypy ✓ · vulture ✓ · pip-audit 0 CVEs) · `validate_taxonomy` **91,733/91,733 (100.0%)** · `ebible verify` **errors=0 / 32,264 paired** (88 warn/615 info baseline) · MATRIX_MAP/REPO_MAP prose current. Reconciled the 2026-05-23 deep-audit ledger (~95% resolved; header added). Findings: `dev/AUDIT_2026-05-26-FINDINGS.md`. **Commit/backup #2 = `b0508789`** (4 test files + `content/source_dates.yaml` + `scripts/.cache_audit_whitelist.py` + `dev/AUDIT_2026-05-23-DEEP.md` reconciliation header + NEW `dev/AUDIT_2026-05-26-FINDINGS.md` + CHANGELOG/SESSION_STATE/IN_FLIGHT). **THIS commit (#3) = doc-only** — records the observed 7,368/0 full-suite pass (SESSION_STATE/CHANGELOG/IN_FLIGHT). **▶ AFTER /clear — TIER-3** (Geʽez/Amharic parallel-Bibles, user-paced, the calendar-critical north-star) is the next arc; also still-open = the **fabrication-deferred Phase-E chapters** (1es 5/8, 2es 14) + a low-severity audit tail (docstrings/counts · 3 big refactors · quarantined cleanup · latent-SEC · `add_kind` no-category bug · rotate the Voyage key — all in the findings doc; none blocking). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `c45ca38` + E:/F: backed up) — ✅ POST-DE-DUP CLEANUP (B1.6 note · Phase-E re-verify · F1 junk).** Small follow-on to the B1.8/B1.9 de-dup (which is COMMITTED `6e5adad`, see prior banner). **(1) Phase-E deferred chapters RE-VERIFIED** against the real Latin↔KJV (re-verify-NO-GO rule) — **deferral CONFIRMED correct, no fabrication:** 2es 14 has SUB-VERSE splits (Latin v2 spans KJV 1-3; v42|43 mid-verse) and 1es 5/8 are NAME-LIST chapters whose transliterated family-groups can't be split-mapped without guessing; recorded the per-chapter analysis in `extract_vulgate_appendix.py` `_DEFER_CHAPTERS` (dict value unchanged → extraction byte-identical). **(2) B1.6** — documented that versification's psalm-fix `or` is correct (values typed `tuple[int,int]`, never None/omit); no behavior change. **(3) F1** — untracked + gitignored `content/.refactor_log.yaml` (write-only `refactor.py` append-log; 5,171 lines of 2026-05-10 test pollution; `git rm --cached` + `.gitignore` + local `entries: []` stub). **GATES:** `test_refactor` 29 · `ci.py --no-tests` CLEAN (ruff format ✓ · `lint_rules` 16/0/0 — removed log not in REPO_MAP · mypy ✓ · vulture ✓ · pip-audit ✓). NO corpus/base-HTML/build change. **▶ THIS SAVE commits + backs up** (`save.ps1` then `git bundle --all` to E:/F:; files: `scripts/extract_vulgate_appendix.py` · `scripts/core/versification.py` · `.gitignore` · deleted `content/.refactor_log.yaml` · CHANGELOG/SESSION_STATE/IN_FLIGHT). **▶ NEXT (user-directed): in-depth audit of the whole project / matrix / map / everything → fix findings → commit + backup #2 → pre-TIER-3 /clear.** After the audit, the ONLY remaining work should be **TIER-3** (Geʽez/Amharic parallel-Bibles, user-paced) + the **fabrication-deferred Phase-E chapters** (1es 5/8, 2es 14). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `6e5adad`) — ✅ DE-DUP B1.8 + B1.9 COMPLETE — the two `sources_*` near-clones the god-module splits deferred are now folded into shared bases. (Committed; was UNCOMMITTED awaiting save when written.)** Both behavior-PRESERVING refactors via the superpowers TDD chain (characterization test pinning current behavior FIRST → consolidate → prove still-green). Every public name / signature / dataclass field / error message / default is byte-identical; the `sources` hub + all callers are untouched (re-export smoke = 24 names OK). **B1.9 (`scripts/core/sources_commentary.py`, audit's "biggest de-dup win"):** the 6 commentary loaders (patristic/ethiopian/protestant/catholic/reformation/rabbinic) — byte-identical `__init__`/`for_verse`/`__len__` differing only in JSON path, person field (`father`/`commentator`), SourceMissingError text, and `by_father`/`by_commentator` accessor name — now share a private `_CommentaryCorpus` base (class attrs `ENTRY`/`PATH`/`PERSON_FIELD`/`_MISSING`); the **6 frozen dataclasses are KEPT DISTINCT** (field name + provenance docstring + `isinstance` identity preserved — tests construct + `isinstance`-check them); each loader is now a ~12-line subclass with a typed `by_father`/`by_commentator` delegating to `by_person`; the internal-only `_by_father`/`_by_commentator` index → `_by_person`. **B1.8 (`scripts/core/sources_ai_clients.py`):** `AnthropicXrefClient` + `AnthropicNoteClient` (explicit "siblings") now share a private `_AnthropicClient` base (`__init__`/`_valid_codes`/`_default_completion_fn` + cache wiring; class attrs `DEFAULT_MODEL`/`_CACHE_TTL`/`_OUTPUT_SCHEMA`); distinct `attribution`/`propose_xrefs`/`draft_note` stay verbatim. `manuscript_vision.py`'s 3rd sibling = OUT OF SCOPE (separate module, untouched). **NEW characterization tests (safety net, green BEFORE refactor):** `tests/test_commentary_loaders.py` (**18** = 6×3 uniform contract, public-surface-only so it holds pre+post) + **2** SDK-wiring pins (`test_default_completion_fn_wires_{xref,note}_ttl_and_schema` — faked `_anthropic_client`, assert each client wires its OWN TTL+schema). **GATES:** new 18+2 + 6 tradition suites + their detector tests + `test_corpus_chi_ai_xrefs` + `TestAINoteDetector` + `test_at_scale_wiring` + `test_manuscript_vision` + `test_core` ALL GREEN (908+67+10+44); `ci.py --no-tests` CLEAN (ruff format ✓ · `lint_rules` 16/0/0 · **mypy ✓ no type errors** · vulture ✓ · pip-audit ✓). **Lint net-neutral PROVEN** (committed `f0401c3` `sources_ai_clients.py` already had the same 2 C901 + 2 E501 in the verbatim-copied unique methods; new commentary module + test = 0-error). **NO corpus/base-HTML/build change** (pure code refactor — no promote/bake; §9 bake gate N/A). **▶ THIS SAVE commits** (`save.ps1` PowerShell-only; files: `scripts/core/sources_commentary.py` · `scripts/core/sources_ai_clients.py` · NEW `tests/test_commentary_loaders.py` · `tests/test_corpus_chi_ai_xrefs.py` · `tests/test_scripts.py` · AUDIT/CHANGELOG/SESSION_STATE/IN_FLIGHT; verify `git status` shows only intended). **▶ NEXT (all user-direction):** the deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess) · TIER-3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior) — ✅ SESSION WRAP — rules-hygiene meta-rule added (§12 "mistakes & near-misses → codify a preventive guard, always, same commit"); being committed + E:/F: backed up THIS turn. Ready for /clear after.** User-requested: verified the project's learn-from-mistakes machinery is explicit. It existed but skewed technical (§1 self-upgrading + §12 retrospective triggers + the dual-store learning-capture + many "codified after the Nth instance" rules); added the missing prominent rule covering the behavioral / near-miss / trust case (mandates a preventive guard at the SAME commit for any preventable failure). Doc-only. **THIS SESSION shipped + committed + E:/F: bundle-verified (3 prior units + this rule):** (1) `inject.py` Strategy-B nested-anchor root-fix `8bbb516`; (2) god-module split 1/2 `sources.py` → 5 modules + hub `774246f`; (3) god-module split 2/2 `web.py` → 8 modules + hub `bae92e4`; (4) this rules-hygiene commit. Every save was truth-gate verified (git log + clean tree + `git bundle verify` on E: AND F:). Both god-module splits proven faithful by AST symbol-equality (57==57 sources / 104==104 web, 0 logic mismatch). **▶ NEXT (all user-direction — clean resume point):** B1.8/B1.9 de-dup (now localized in the new `sources_ai_clients`/`sources_commentary` + `web_*` modules; behavior-changing) · the deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess) · TIER-3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `bae92e4` + E:/F: backed up) — ✅ GOD-MODULE SPLITS COMPLETE — (2/2) `scripts/web.py` (5,214 LOC) → 8 domain modules + a 2,028-line server-core/re-export hub. UNCOMMITTED, awaiting "save".** PURE RELOCATION of the module-level `api_*`/helper functions; the `Handler` class + route tables + dispatch + `main` STAY in `web.py` (not split). Re-export-shim pattern (same as split 1/2). **New (`scripts/`):** `web_helpers` (307) · `web_notes` (226) · `web_matrix` (394) · `web_editions` (457) · `web_covers` (167) · `web_sources` (341) · `web_content` (754) · `web_misc` (109). Acyclic (`web_helpers` ← domains ← hub); the hub's `# noqa: F401` re-exports keep the route table + every external `web.api_X` importer unchanged. **VERIFIED faithful by AST symbol-equality** (orig `774246f` vs new union): **104==104 symbols · 0 missing/extra/dup · 0 logic mismatch**. Gates: import smoke · **`test_web_routetable` 89** · 59 web-route + 293 `test_scripts` api-slice tests green · `lint_rules` 16/0/0 · `ruff format --check` clean. **Honest caveat:** ~87 STANDALONE comment lines dropped in the hub-slim (25 section dividers + historical "moved to `scripts/api/…`" notes + a few caching-rationale lines that sat between symbols; ALL inline comments + docstrings preserved) — judged acceptable (module boundaries supersede the dividers). **Files (uncommitted):** `scripts/web.py` + 8 new `web_*.py` + CHANGELOG/SESSION_STATE/IN_FLIGHT. **▶ NEXT (backlog — all user-direction):** the B1.8/B1.9 de-dup (deferred) · the deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess) · TIER-3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `774246f` + E:/F: backed up) — ✅ GOD-MODULE SPLIT (1/2): `scripts/core/sources.py` (3,031 LOC) → 5 focused modules + an 89-line re-export hub. UNCOMMITTED on disk, awaiting "save".** TIER-2 #7 (user-unblocked). PURE RELOCATION (no logic change / de-dup / rename) via the `build_edition`→`matter_pages` re-export-shim pattern; every caller unchanged. **New (`scripts/core/`):** `sources_base` (56) · `sources_lexicon` (552: Strong's/Tsk/Nave/Torrey/Kenyon) · `sources_commentary` (507: 6 commentary pairs) · `sources_ai_prompts` (920: model/TTL/system-prompt/schema consts) · `sources_ai_clients` (550: Anthropic xref/note clients + `_with_cache`). One-directional imports (base ← lexicon/commentary/ai_prompts ← ai_clients ← hub); no cycles. Hub re-exports all public names + the externally-used underscore names (`SourceMissingError`/`_normalize_book_code`/`_BOOK_CODE_ALIASES`/`_anthropic_client`/`_sources_dir`). **B1.8/B1.9 de-dup DEFERRED** (separate behavior-changing refactor). **VERIFIED faithful by AST symbol-equality** (a script compared `ast.unparse` of every top-level symbol, original `8bbb516` vs the 5 modules): **57==57 symbols · 0 missing/extra/dup · 0 logic mismatch · comments preserved (264→271)**. Runtime: import smoke (36 names) · 157 per-source/downstream/AI tests green (executing subagent's wider sweep = 1165) · `lint_rules` 16/0/0 · `ruff format --check` clean. **Files (uncommitted):** `scripts/core/sources.py` + 5 new `sources_*.py` + CHANGELOG/SESSION_STATE/IN_FLIGHT. **▶ NEXT:** god-module split (2/2) = `web.py` (5,214 LOC). **[USER]** real-reader presentation eyeball still open.**
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `8bbb516` + E:/F: backed up) — ✅ ROOT-FIXED `inject.py` so Strategy-B verse-level injects can't nest `note-ref` inside the next verse's `vn-link` — closes the nested-`<a>` recurrence. UNCOMMITTED on disk, awaiting "save".** Done via systematic-debugging → TDD (superpowers). **Root cause:** `find_verse_region_b` ended verse N's region at the next verse's `<span class="vn">`, but that span sits INSIDE its `<a class="vn-link">` open tag — so the region carried the next anchor's open tag at its tail, and a verse-end (empty-anchor) marker (topical/dict notes) landed between that `<a>` and its `<span>` = `<a>` in `<a>` (the asymmetry with Strategy A, which ends at the next vn-link's `<`). Confirmed by a 2-verse repro reproducing the exact `_BROKEN_RE` shape before fixing. **Fix:** back the region end up past an immediately-preceding `<a class="vn-link" …>` open tag (mirrors Strategy A); bare-span fallback unchanged; the spill resolver inherits it. **TDD** (`tests/test_inject_wellformed.py` +2): a region-boundary test + an end-to-end "verse-level inject → 0 nested `<a>`" test, both RED→GREEN. **NO re-bake** (committed base already `--fix`'d to 0; inject `already_in` = no-op; bare-base regen is lossy) — the fix is preventive, so `check_nested_anchors --fix` is now a safety net, not a per-ingest necessity. **GATES:** 2 new + `test_inject_wellformed` 21 + `test_nested_anchors` (base-zero gate) + spill/irregular 6 + marker/resync 42 green · `check_nested_anchors` (no `--fix`) **0 across 61 files** (base untouched) · `lint_rules` 16/0/0 · `ruff format --check` clean. Closes the "Follow-up (deferred)" from the nested-`<a>` ship. **Files (uncommitted):** `scripts/inject.py` · `tests/test_inject_wellformed.py` · CHANGELOG/SESSION_STATE/IN_FLIGHT. **▶ NEXT (all need user direction — natural checkpoint):** the deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess, needs careful hand-alignment vs KJV) · the 2 god-module splits (`sources.py`/`web.py` — user-deferred) · TIER-3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open. (Phase E `cc6523fe` + E:/F: backup BOTH CONFIRMED this session.)**
>
> **➤➤➤ 2026-05-26 (prior — now COMMITTED `cc6523fe` + E:/F: backed up, both confirmed) — ✅ PHASE E SHIPPED + a base regression FIXED + a rules-hygiene pass — being COMMITTED + E:/F: backed up THIS session (user-authorized; §14 resume audit confirms via `git log`).** Three things in this save: **(1) Phase E** — Clementine LATIN appendix added to man/1es/2es, Vulgate popup spine 74→77. **(2) Nested-`<a>` base regression FIXED** — `test_nested_anchors` was RED on the committed base (14,568 nested `<a>` across 38 untouched files, pre-existing from the Torrey/Nave/Easton `epub_working` injections; epubcheck never caught it because the build converts `vn-link <a>`→`<span>`); `check_nested_anchors.py --fix` repaired all → 0; repaired base re-gated: `test_nested_anchors` 10 ✓ · `ebible verify` 0 err · flagship ethiopian-tewahedo rebuilt → epubcheck 0/0. **(3) Rules-hygiene** (user-requested) — added the top-level "sources are NOT missing" guard, fixed the §4 save=zip-vs-commit contradiction, added the §9 nested-anchor gate + a §9-step-1 cross-ref; full contradiction audit otherwise clean.** Executed the Phase E plan via TDD (superpowers chain: executing-plans + test-driven-development). Added the la.wikisource Clementine Vulgate witness to the 3 deuterocanonical appendix books (the eBible body package omits them). **CORE FINDING (verify-against-real-data):** the plan's "versification needs NO edit" premise was DISPROVEN by a verse-by-verse text check vs the canonical KJV — `vulgate_to_kjv`'s segment table is Douay-tuned and MISALIGNS la.wikisource's 1es/2es (proven 1es 1:13); their numbering is close-to-but-NOT canonical, with scattered INTERNAL split/merge points (count-match ≠ alignment — 2es ch16: 78=78 yet shifted). After 3 automated approaches (segment table · proper-noun anchoring · length-based Gale-Church DP) each FAILED to certify cross-language name-sparse alignment, switched to an EXPLICIT hand-verified per-chapter `_JOIN_PREV` correction table (each split read against the KJV text; mirrors `_VULGATE_SEGMENTS`). **User chose "align properly"** at 2 decision points (over defer / ship-with-caveats). **SHIPPED (1,117 Latin v, additive-PROVEN):** man 15/15 (clean identity, every verse verified) · 1es 279/448 (ch1,2,3,4,6,7,9; verified splits 2:31→30, 9:49→48) · 2es 823/874 (15/16 ch; verified splits 10:60→59, ch16 19→18 realigning the rest; natural trailing gaps 5:56/7:70/16:78). **DEFERRED (omitted+flagged, NO-GUESS):** 1es ch5/ch8 (census/genealogy name-lists) + 2es ch14 (burning-bush) — multi-shift through transliterated name-lists, can't certify verse-perfect without fabrication; flagged in `_meta.yaml appendix_source.deferred_chapters` + ATTRIBUTIONS for a dedicated future pass. **GATES:** a categorize-diff PROVED the bake added EXACTLY 1,117 `vnote-vulgate` segments + 0 other changes (every non-appendix book byte-identical) · `ebible verify` errors=0 (32,264/32,264 paired, unchanged) · `lint_rules` 16/0/0 · `ruff format --check` clean · 10 phase-e tests green · built catholic-study → **epubcheck errors=0/warnings=0**; 1es (279)+2es (823) Latin verified IN the built EPUB; man/1es/2es are canonical in ethiopian-tewahedo (flagship)+orthodox editions so all 3 render. `_meta.yaml` 74→77 books / +1,117 v (→34,460) / outside-KJV 8→11. **▶ THIS SAVE commits + backs up** (`save.ps1` PowerShell-only; NEW: `scripts/extract_vulgate_appendix.py` · `tests/test_phase_e_vulgate_appendix.py` · 3 `content/translations/sources/vulgate-appendix/*.wiki` · `content/translations/vulgate-clementine/{man,1es,2es}.py`; MODIFIED: `_meta.yaml` · `content/sources/ATTRIBUTIONS.md` · **`epub_working/index_split_*.html` = 41 files** (3 Phase-E bake + 39 nested-anchor repair, overlapping at 025) · `dev/CLAUDE_PROJECT_RULES.md` (rules-hygiene) · CHANGELOG/MATRIX_MAP/SESSION_STATE — `epub_working/.backups/` is gitignored, verify `git status` shows only intended; then E:/F: `git bundle --all`). Backlog after: **root-fix `inject.py`** (place `note-ref`s at the verse head so `check_nested_anchors --fix` isn't needed every ingest) · the **deferred Phase-E chapters** (1es ch5/ch8, 2es ch14 — dedicated alignment pass) · the 2 god-module splits · TIER-3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-26 (prior — ✅ Phase E now SHIPPED, see top) — the original plan resume-point, EXECUTED this session (kept for history):** `docs/superpowers/plans/2026-05-26-phase-e-clementine-latin.md` (TDD; brainstormed + spec'd + user-approved this session).** Phase E = add the Clementine **Latin** witness to the 3 deuterocanonical appendix books `man`/`1es`/`2es` (Vulgate popup spine **74→77**), purely **additive** (their existing KJV/Greek popups stay byte-identical). **GROUNDED + READY:** versification needs **NO edit** (verified — `man`/`2es` map identity, `1es` already in `_VULGATE_SEGMENTS`, all in `CANONICAL_BOOKS` so the extent guard passes); source confirmed = la.wikisource `Vulgata Clementina` raw wikitext — `Oratio_Manasse`→`man` · `Liber_Tertius_Esdrae`→`1es` · `Liber_Quartus_Esdrae`→`2es` (mind the Vulgate III/IV-Esdras offset). Plan: fetch wikitext → new `scripts/extract_vulgate_appendix.py` (parse `==Caput N==` / `<sup>N</sup>`, strip `{{templates}}`) → remap via existing `vulgate_to_kjv` → bake the 3 books' asides → categorize-diff additive-only → epubcheck catholic-study 0/0. **`2es` is the one risk:** verify its per-chapter counts vs the 16-ch/874-v skeleton; **DEFER `2es`** (ship `man`+`1es`) if it diverges (no fabrication). Spec: `docs/superpowers/specs/2026-05-26-phase-e-clementine-latin-design.md`. **THIS SAVE commits the Phase-E spec + plan + this banner** (the only repo changes since `4c3e7e2`). **▶ PROCESS LEARNINGS THIS SESSION (after the Torrey ship):** I wrongly concluded the corpus items were "blocked on missing sources" + asked the user to supply them — WRONG: **everything is on-disk or has a plan** (new memory `sources-already-in-place`: read the plans first — `dev/archive/PLAN_2026-05-21.md` §4.1 + `dev/AUDIT_2026-05-23-DEEP.md`; source PDFs have arbitrary names so don't grep by book; verify CURRENT status against CHANGELOG, not stale plan labels). **#5 (no-KJV-book popups) is NOT a standalone TIER-2 item** — the popup-regen design (§3, lines 42/133) assigns those books' popups to the **τ.G Geʽez parallel-Bible track = TIER-3** (user-paced); Geʽez/Amharic text feeds the two standalone Bibles, not the 9 standard editions (scope rule). **▶ AFTER PHASE E:** #7 god-module splits (`sources.py`/`web.py` — actionable-solo, byte-compat-proof, "fresh-session task"); TIER-3 parallel-Bibles (user-paced; includes #5). **[USER]** real-reader presentation eyeball still open (now incl. the merged Nave's+Torrey topical index).
>
> **➤➤➤ 2026-05-26 (prior) — ✅ TORREY SURFACED IN THE EPUB TOPICAL INDEX — Nave's + Torrey merged back-of-book index SHIPPED + COMMITTED (`4c3e7e2`) + E:/F: backed up.** Closed the standing OTHER-OPEN item from the Torrey ingest (21,762 `topic-torrey` notes were in the corpus but invisible to an EPUB reader — topical notes are filtered from inline per Wave 3, and the back-of-book index was Nave's-only). Built via the superpowers chain (brainstorm → spec → TDD plan → execute). **Grounded in a read-only overlap analysis FIRST:** Nave's = 4,604 ALL-CAPS proper-name-heavy topics, Torrey = 630 Title-Case doctrinal topics; exact-string overlap = **0** (caps), casefold = **166** themes (**162 co-cite a verse**) → **complementary coverage + a corroboration core**, not redundancy — this shaped the design. **Shipped (`scripts/matter_pages.py`):** a unified A–Z index merging both via `build_merged_topic_index` (**casefold**-union — NOT comma-base, preserves Torrey subtopics; `(N·T)`/`(N)`/`(T)` source tags; Title-Case display `AARON`→`Aaron`; honest coverage-first intro) + `render_merged_topical_index_page` + `_write_topical_page` (mode branch, graceful source-missing degrade); `render_topical_index_page` gained a defaulted `intro=` so the **`naves` path is byte-identical to the 61226c5 Nave's-only ship**; Torrey credited on the Sources page. **NEW builder field `topical_index_source` ∈ {both, naves, torrey}, default `both`** (user-approved 2026-05-26 — the intended output change that surfaces Torrey; deliberate exception to §6.5 opt-in) wired the standard enum way (`api/editions.py` lists+validator · `web.py api_customize_data` · `templates/customize.py` `<select>` · `build_edition` re-export) + `.topic-src` CSS. Torrey JSON already ships frozen (whole-`content/` bundle). **VERIFIED (TDD):** NEW `tests/test_topical_merge.py` **28** · `test_topical_index.py` **7** (byte-compat) · `test_presentation_polish.py` **35** (real builds + spine order) · `lint_rules` **16/0/0** · `ruff format --check` clean. **BAKE-AND-PROVE (RULES §9):** flagship ethiopian-tewahedo rebuilt → **epubcheck errors=0/warnings=0**; built `topical.xhtml` = **5,068 topic entries (4,604 Nave's + 464 Torrey-only) · 167 `(N·T)` tags · Torrey credited** (was 4,604 Nave's-only). NO corpus/base-HTML change. Spec `docs/superpowers/specs/2026-05-26-torrey-topical-index-merge-design.md`; plan `docs/superpowers/plans/2026-05-26-torrey-topical-index-merge.md`. **▶ NEXT — COMMIT ON USER `save`** (`save.ps1`; 12 files = 9 modified [6 code/CSS + CHANGELOG/MATRIX_MAP/SESSION_STATE] + 3 new [spec/plan/tests]; then E:/F: `git bundle --all` backup) → remaining backlog = TIER 2 #5 no-KJV popups · #6 Phase E Latin · the deferred 2 god-module splits; TIER 3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open (now incl. the merged topical index).
>
> **➤➤➤ 2026-05-26 (prior) — ✅ TIER 2 #7 code-debt: tooling + safe fixes + ruff backlog 118,725→268 + de-dup SHIPPED (4 commits, COMMITTED + E:/F: backed up); the two god-module splits DEFERRED (user's call).** Worked #7's safe + high-value slices. **(1 tooling `48b5bd8`)** `vulture`+`pip-audit` declared/installed/wired into `scripts/ci.py` (report-only, graceful-skip); shipped deps (`requirements.txt`) have **0 known CVEs** (the 10 found are dev-env tooling only); +2 CI tests. **(2 safe fixes `48b5bd8`)** F811 — `TestCustomize` defined twice in `test_scripts.py` (715+1569) silently shadowed **8 customize.py tests** (never collected) → renamed first to `TestCustomizeScript`, all 8 recovered + green; +E401; audit micro-items A3/B1.3/A7 already resolved. **(3 ruff backlog `1cc1c2d`)** excluded `content/translations/**/*.py` DATA from `ruff check` code-style rules (`pyproject.toml` per-file-ignore, mirrors `content/notes/*.py`) → backlog **118,725→268** (118,478 were E501 in verse-text data); +6 safe autofixes + F401 cleanup; remaining 268 = intentional honest-backlog (C901 66 inherent-complexity · naming 70 · scattered SIM105 49 mostly in tests), left documented. **(4 de-dup, this commit)** A8 — `_editions_index()` dup across 4 api modules now delegates to `config.editions_by_id()` (160 tests pass). **▶ DEFERRED = the 2 god-module splits:** `sources.py` (2,950 LOC → lexicon/commentary/ai_clients/ai_prompts; would absorb the B1.8 AI-client + B1.9 commentary-loader de-dup) + `web.py` (5,214 LOC → extract clusters) — riskiest #7 work (central widely-imported modules), need full-suite + epubcheck + byte-compat proof, non-functional → a focused fresh-session task (re-export-shim pattern = the proven `build_edition`→`matter_pages` split). **▶ OTHER OPEN:** surface Torrey in the EPUB topical-index (`scripts/matter_pages.py` merge `naves_topical`+`torrey_topical`); TIER 3 parallel-Bibles (user-paced). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-26 (prior) — ✅ Torrey ingest now ACTUALLY COMMITTED (`13462720`) + E:/F: bundle-backed-up; + a session-end COMMIT/BACKUP TRUTH GATE added to the ending rules after a near-miss.** This session resumed on "continue", and a resume-time `git status` caught the 2026-05-26 Torrey ingest (21,762 notes, all gates green) sitting **UNCOMMITTED on disk** — despite the prior session reporting it "committed + backed up / safe to /clear". **Nothing was lost** (`/clear` doesn't touch the working tree); leaving it uncommitted was rule-correct ("continue" ≠ "save") — the failure was the *false claim*. Fixed both: (1) committed Torrey (`13462720`, 1318 files; ruff + `lint_rules` 16/0/0 pre-verified) + `git bundle --all` to E:/F:; (2) **root-caused the rule gap** — the ending rules guarded against committing-WITHOUT-permission but never verified a "your work is saved" claim is TRUE (the §12/§13/§14 audits check work is *recorded*, not that a commit *happened*). Added a commit/backup truth gate in **4 places**: PLAYBOOK §0 (contract) + new **§6.7** session-end step + RULES **§12** (now a 5-point pre-summary audit) + **§14** (resume audit now runs `git log`/`git status` as the automated backstop). Forbade the "post-/clear: commit" deferral (next session resumes on "continue" → never fires). Full account: `dev/CHANGELOG.md` 2026-05-26 (cont.). **▶ NEXT (backlog unchanged):** TIER 2 #7 code-debt (vulture/pip-audit/ruff backlog — install-guard: DECLARE in `requirements-dev.txt` + ask to turn auto-mode OFF first) → then surface Torrey in the EPUB topical-index (merge `naves_topical` + `torrey_topical` in `scripts/matter_pages.py`). TIER 3 = parallel-Bibles, user-paced. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-26 (prior — now COMMITTED `13462720` + backed up) — TRACK C: Torrey's New Topical Textbook FULL ingest COMPLETE + VERIFIED — was READY TO COMMIT + BACKUP (post-/clear).** Corpus **69,969 → 91,733 notes (100% attributed)**: +21,762 `topic-torrey` (Torrey 1897 PD, FULL — overlap with Nave's KEPT as cross-confirming corroboration, user's call). NEW χ-cluster pipeline (all TDD, **20 tests** `tests/test_torrey_ingest.py`): `scripts/extract_torrey_ccel.py` (parser → `content/sources/torrey_topical.json`, **628 topics**; section-letter gate + frequency filter + forward/backward line-unwrap + Jdj→Judges + Contents-run header-collapse + **legacy→canonical book-code normalization** jol→joe etc.) · `TorreyTopical`/`torrey_topical()` (`scripts/core/sources.py`) · `TorreyTopicalDetector` (+ in `ALL_DETECTORS`; `scripts/core/detectors.py`) · `scripts/run_torrey_at_scale.py` (`--net-new`/`--overlap-only`). **kind `topic-torrey`** (category `topic`, ✦) in `content/kinds.yaml`. **PERF FIX:** `batch_promote_xrefs.py --by-book` (one read+write per book) promoted 21,764 in **44 s** vs the per-candidate path's ~46 min (which I killed mid-run — it rewrote each large book file once per candidate; the `--by-book` path also SURFACED + I fixed a silent book-code bug the slow path was dropping). Baked: `inject --all-books` → 21,762 into `epub_working/` (51 files); flagship rebuilt → **epubcheck 0/0/0/0**. **GATES:** `lint_rules` 16/0/0 · `validate_taxonomy` 91,733/100% · `ebible verify` errors=0 / 32,264 paired · `ruff format --check` clean · 20 torrey tests + `record_count` pin (71→**72**) green. **RULES folded in (user-requested):** RULES §9 bake-and-prove gate + "register a new kind" checklist; PLAYBOOK §1.4 session-start RAM-hygiene + §6.5 session-end junk-cleanup; `matrix.py` "72 kinds". **⚠ NOT YET SURFACED IN EPUB OUTPUT:** topical notes are filtered from inline (Wave 3) + the topical-index back-matter page is Nave's-only (`scripts/matter_pages.py` → `naves_topical()`), so Torrey enriches the CORPUS + APP but a flagship EPUB reader doesn't yet see Nave's-vs-Torrey corroboration. **▶ POST-/CLEAR ORDER:** **(1) COMMIT** all uncommitted — `git add -A` is correct (every change is intended Torrey work + the rule docs; verified clean: 0 `.bak`, 0 JVM logs, 24 untracked all legit) — then **bundle-backup `git bundle --all` to E:/F: (NEVER C:)**, no push (no remote). **(2) TIER 2 #7 code-debt** (vulture + pip-audit + ruff backlog; install-guard: DECLARE in `requirements-dev.txt` first). **(3) THEN surface Torrey** in the EPUB topical-index (extend `matter_pages` to merge `naves_topical` + `torrey_topical`). **COMMIT MSG:** `Track C: Torrey's New Topical Textbook full ingest (+21,762 topic-torrey; corpus 69,969→91,733) + batch_promote --by-book perf fix + legacy book-code normalization + RULES/PLAYBOOK rule additions`. **[USER]** desktop `.exe` icon assets already exist → wire into `dev/launcher.spec` when polishing the desktop app (don't recreate); real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-25 (prior) — ✅ POST-WAVE-4 TIER 1 COMPLETE — coverage floor ACTIVATED · smoke-cleanup · Git-LFS decided; 12 latent test failures FIXED. COMMITTED + E:/F: re-bundled.** Activating the W4.5 coverage floor (`pip install -r requirements-dev.txt` → coverage 7.14.0) ran the FIRST true whole-suite measurement (no file filter) = **62% scripts/ line coverage**; floor set to **60** (a ~2-pt ratchet) in `scripts/ci.py`; `.gitignore` += `.coverage*`. That run EXPOSED **12 latent failures** (4 files) that prior "full-suite green" sweeps had MASKED because they were curated SUBSETS excluding the translation tests. All 12 = stale state-pins that never followed their data forward: (a) full translation ingests (f6d90c6: JPS/WLC/Arabic/Vulgate/Douay seed→full; only WLC's pins had been updated) + (b) Wave 3 #6's witness-id `popup_languages_default`. Fixed against VERIFIED reality (real counts; actual text — Vulgate ae-ligature "caelum"/lowercase "spiritus Dei", JPS all-caps "IN THE beginning"; popup-coverage re-pointed to `popup_versions.resolve_version_id`) + the root data defect (JPS registry `source_date: None`→1917, so `_meta` no longer carried the fetch date). **#3:** smoke-desktop `_MEI*` self-cleanup (TDD; `dev/smoke_desktop.py` snapshots+prunes only the dir THIS run leaks, + `tests/test_smoke_desktop.py`). **#2:** Git-LFS DECIDED — leave as-is (no remote + `git bundle --all` captures only LFS pointers not media + files < 100 MB/file); recorded in PLAN + backup memory. **GATES:** whole serial suite **7278 passed / 0 failed** · `ci.py --no-tests` CLEAN (ruff format ✓ · lint_rules 16/0/0 · mypy ✓) · coverage gate PASS (62% ≥ 60). No content/corpus/base-HTML change. **▶ NEXT — TIER 2** (opportunistic, non-blocking): Track C corpus (5 capped works) · 7 no-KJV popups · Phase E Latin · code-debt (ruff backlog + vulture/pip-audit). Then TIER 3 (parallel-Bibles, user-paced). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-25 (prior) — 🎉 WAVE 4 COMPLETE — W4.5 (local CI gate + mypy clean + coverage floor) SHIPPED; W4.1–W4.5 all done. COMMITTED + E:/F: RE-BUNDLED.** **mypy now CLEAN on the typed surface** — the 13 errors (all `scripts/inject.py:688-807`: a `stats` dict of int counters + list accumulators that mypy widened to `dict[str, object]`) fixed with a `stats: dict[str, Any]` annotation (runtime-identical; inject tests 13/13). **NEW `scripts/ci.py`** — the local CI gate (§9 meta-tool, injectable runner) composing `ruff format --check` (block) · `ruff check` (report-only — visible lint backlog) · `lint_rules` (block) · mypy/`audit_types` (block) · `pytest` (block) · a coverage floor (block IFF `coverage` installed, else a skipped warn). `make ci` (full) + `make ci-fast` (`--no-tests`). **NEW `requirements-dev.txt`** declares pytest/ruff/mypy/coverage (§0 install-guard: now declared, not agent-chosen — `coverage` is the one not-yet-installed; the floor activates after `pip install -r requirements-dev.txt`). `tests/test_ci.py` 6. **VERIFIED:** `ci.py --no-tests` → CLEAN (format ✓ · lint warn/non-blocking · `lint_rules` 16/0/0 · mypy ✓ no type errors) · `test_ci.py` 6/6 · `inject` 13/13 · ruff clean on new files. **▶ WAVE 4 DONE** — W4.1 frozen-build · W4.2 build-cap · W4.3 welcome-flow · W4.4 README+release · W4.5 CI/mypy/coverage. The autonomous productionization stream (`dev/PLAN_2026-05-24-end-scope.md`) is COMPLETE. **▶ NEXT — the parallel tracks:** Track B (Kings/Samuel Geʽez manuscript marathon — calendar-bound critical path, user-paced check-ins) + Track C (corpus expansion, capped to the 5 named reference works); plus the **[USER]** real-reader presentation eyeball (footnote numbers · in-note symbols + legend · widened popups · topical index · the new first-run welcome flow) on Apple Books / e-ink.
>
> **➤➤➤ 2026-05-25 (prior) — ✅ WAVE 4 — W4.4 (distribution README + release artifact) DONE + W4.3 empty-state; ONLY W4.5 LEFT.** **W4.3 tail:** `/export` no-editions empty-state (DOM, no `innerHTML`) → points to the wizard; `tests/test_build_ui_guardrails.py` 5; the persistent "getting-started card" is treated as subsumed by the welcome overlay. **W4.4:** NEW root **`README.md`** (on-ramp: what it is · use/run · build-an-edition · `./ebible`/`make` · packaging · licensing); `content/sources/ATTRIBUTIONS.md` gained an "Images & fonts" cross-reference index (cover templates / book art / fonts were each already documented in their own dirs); `.gitignore` += `/build/` `/dist/`. **RELEASE ARTIFACT:** `pyinstaller dev/launcher.spec` → `dist/YHWH.exe` **393 MB** one-file (bundles this session's W4.2+W4.3 code); **frozen-validated headlessly** — `/api/onboarding` → `first_run:true`, `/` + `/export` → HTTP 200 (the new onboarding module + build-gate import + updated consoles all work IN THE BINARY); packaged to `E:\` + `F:\YHWH-v2.4-releases\YHWH-Yaway-2026-05-25-6d22477\` (YHWH.exe + README); C: build output cleaned (~19.8 GB free). **NOTE:** the E:/F: *git bundle* is still at `6d22477` (this commit is 1 ahead — re-bundle on the next "backup"). **GATES:** `lint_rules` 16/0/0 · `ruff format` clean · frozen launch-probe green. **▶ NEXT — W4.5 (LAST Wave-4 piece):** CI / mypy / coverage floor — a `make ci` gate composing lint_rules + ruff + tests + mypy + a coverage floor; runs locally via `make` until a git remote exists. **Wave 4: W4.1✅ W4.2✅ W4.3✅ W4.4✅ — W4.5 only.** Per `dev/PLAN_2026-05-24-end-scope.md` Wave 4.
>
> **➤➤➤ 2026-05-25 (prior) — ✅ WAVE 4 — W4.2 (concurrent-build cap) + W4.3 first-run welcome flow (CORE) SHIPPED + COMMITTED + E:/F: BUNDLE-BACKED-UP.** **W4.2:** NEW `scripts/core/build_gate.py` — a process-wide `BoundedSemaphore` admission gate (default 1, env `YHWH_MAX_CONCURRENT_BUILDS`; non-blocking `build_slot()` → `BuildBusyError`). Both build routes (`/api/export/build/<id>` + `/api/build-all`, which converge on the ONE `do_PUT` bespoke block via the `do_POST → do_PUT` fallthrough — verified) now return **HTTP 409 `build_in_progress`** when a build is running instead of starting a 2nd heavy in-process frozen build. 12 tests (`tests/test_build_gate.py`); SIM117 per-file-ignore in `pyproject.toml`; FIXED 2 pre-existing stale `_POST_ROUTES` count/membership tests (`tests/test_web_routetable.py`, stale since the Wave-2 cover-template route — recent sweeps hadn't run that file). **W4.3 core:** (1) server onboarding state — `scripts/core/onboarding.py` (idempotent, corrupt-marker-safe) + NEW frozen-aware `paths.state_dir()` (`YHWH_DATA_DIR`-honoring sibling of `exports_dir()`); `/state/` gitignored; (2) endpoints `GET /api/onboarding` + `POST /api/onboarding/complete` (`_POST_ROUTES` 13→14; `tests/test_onboarding.py` 15 incl. a live round-trip); (3) **2b** build-UI guardrails — both build buttons disable while either build runs + friendly-409 via NEW safe-DOM `setBuildStatus` (`textContent`, no `innerHTML`; `tests/test_build_ui_guardrails.py` 4; browser-verified 0 JS errors); (4) **2d** first-run **welcome OVERLAY** — NEW reusable `WELCOME_OVERLAY_JS` in `templates/_design.py` (DOM-only modal), injected into `INDEX_HTML` (the root the launcher opens; bypasses `apply_design_system` so substituted directly + also wired into `apply_design_system` for future opt-in). **Browser-verified END-TO-END:** fresh `YHWH_DATA_DIR` → overlay shows ("Welcome to YHWH Ya' Way" + Start→/wizard + "Explore on my own") → click → `POST /api/onboarding/complete` writes `state/onboarding.json`, `first_run`→false → reload stays clean; 0 JS errors. **GATES:** 31 new tests + 35 export + 89 route-table green · `lint_rules` 16/0/0 · `ruff format` clean · 2 live-browser passes clean · NO corpus/base-HTML change. **▶ NEXT — W4.3 remainder** (2c dismissible "Getting started → wizard" card + `/export` no-editions empty-state — MINOR; the overlay now covers primary first-run guidance) then **W4.4** (distribution README + release artifact) + **W4.5** (CI/mypy/coverage). Per `dev/PLAN_2026-05-24-end-scope.md` Wave 4.
>
> **➤➤➤ 2026-05-25 (prior) — ✅ WAVE 4 (W4.1) — the θ desktop binary now BUILDS (a working frozen builder, not just launch+serve).** The θ.1/θ.2 desktop app (launcher + PyWebView shell, built 2026-05-10) had NEVER been verified to BUILD an EPUB when frozen (its 125 unit tests mock all I/O; the 2026-05-24 PLAN's "Wave 4" didn't know the θ work already existed). A real `pyinstaller dev/launcher.spec` build + a NEW end-to-end smoke (`dev/smoke_desktop.py`) exposed FOUR frozen-only failures, ALL FIXED: **(1) two subprocess re-invocations** — `api_export_build` (api/exports.py) shelled `[sys.executable, build_edition.py]` + `build_one` (build_edition.py) shelled `[sys.executable, build_epub.py]`; frozen, `sys.executable`=YHWH.exe (the launcher) → argparse rejects the script → now run IN-PROCESS when `sys.frozen` (dev keeps the subprocess for `--all` parallelism); **(2) `epub_working/` base HTML not bundled** (WinError 3) → bundled in the spec, with its **2.6 GB `.backups/`** subtree filtered out (real base ~122 MB); **(3) cp1252 charmap errors** — the corpus is read via `read_text()`/`open()` without `encoding=`, relying on the dev `PYTHONUTF8=1`; a frozen PyInstaller interpreter IGNORES that env var → fixed globally by **`-X utf8`** in the spec's run-time options (no code sweep); **(4) ephemeral output** — EPUBs landed in the read-only/ephemeral `_MEIPASS`; `paths._build_output_root()` is now frozen-aware with a **`YHWH_DATA_DIR`** override (else `user_data_root()` when frozen, else repo in dev), `api/exports.EXPORTS_DIR` uses the resolver → builds PERSIST. Also: NEW `dev/requirements-desktop.txt` (pinned pyinstaller==6.20.0 / pywebview==6.2.1); spec trimmed (`candidates/`+`translations/sources/` excluded; binary **579→397 MB**); 2 launcher migration tests pinned `--shell browser` (regressed once pywebview was installed — frozen+auto picks native; RULES §8); NEW `TestBuildOutputRootResolution` (4 tests). **SMOKE PASSED:** frozen `YHWH.exe` (396.8 MB) launches + serves + builds `jewish-study` → a **16.36 MB EPUB persisted to `D:\yhwh-smoke-data\exports`** (via YHWH_DATA_DIR), confirmed not in `_MEIPASS`. Verified: `ruff format` clean · **full dev re-sweep 1252 passed / 0 failed** (test_scripts + test_core + Wave-3 files + 125 test_desktop_theta incl. the 4 new path tests). **This + the audit work are being COMMITTED + bundle-backed-up to E:/F: this session (user-directed wrap-up; weekly Claude bandwidth resets 10pm).** **C: was at 6 GB — freed ~10 GB by removing 5 orphaned PyInstaller `_MEI*` extraction dirs (the smoke's `taskkill /F` prevents PyInstaller's auto-cleanup; `smoke_desktop.py` self-cleanup is a minor follow-up) + build temp → C: ~17 GB free.** **▶ NEXT — W4.2–W4.5** (concurrent-build cap · onboarding/empty-state UX · distribution README + release artifact · CI/mypy/coverage), per `dev/PLAN_2026-05-24-end-scope.md` Wave 4. The desktop build now works end-to-end — the remaining waves are productionization polish, not core function.
>
> **➤➤➤ 2026-05-25 (prior) — ✅ LIGHT POST-WAVE-3 AUDIT COMPLETE — verdict PASS.** Ran the light solo-Claude audit (`dev/AUDIT_2026-05-25-wave3-scope.md`) from scratch at HEAD `8a48ed3`; full report `dev/AUDIT_2026-05-25-wave3-FINDINGS.md`. **All gates green from scratch:** `lint_rules` 16/0/0 · `ruff --check` clean · `verify` errors=0 (24,015/24,015 paired) · `validate_taxonomy` 69,969/69,969 (100%) · `trace_matrix` 0 · `trace_repo` 0 · epubcheck **0/0/0/0** on ethiopian-tewahedo (flagship, 23.89 MB) + catholic-study (23.28 MB) + jewish-study (tanakh small-canon, 16.36 MB) · **full test sweep 1123 passed / 0 failed**. **FIXED a pre-existing full-sweep-only test-isolation bug:** `test_save_category_round_trip`/`test_save_kind_round_trip` (`tests/test_scripts.py`) leaked the `load_categories`/`load_kinds` `@lru_cache` singletons (stale `lang→✎`) into `test_marker_glyphs::test_all_15_category_symbols_reachable` (which then saw no kind producing `✎`); both round-trip tests now `cache_clear()` the loaders + `compute_matrix` on teardown (RULES §7.1), and the glyph test clears them at its start (state-independent, RULES §8). Was 1-failed/1122 → now **1123 passed**; files were never corrupted on disk (cache-only; `git status` clean of content). **Doc currency (self-upgrading-matrix):** `MATRIX_MAP` 3 stale "PLANNED Wave 3" labels → shipped + the #6 drop-kjv-default/KJV-floor and #7 topical-index/per-edition-`renumber_markers` data-flows added; design spec §4.3 now notes the KJV-floor deviation; `PLAN_2026-05-24-end-scope.md` marks Wave 3 ✅ DONE / Wave 4 ◀ NEXT. **Recorded (not fixed in-session):** epubcheck must be run with `--jar <…\site-packages\epubcheck\epubcheck.jar>` — auto-discovery resolves an unparseable `epubcheck.exe` PATH wrapper (the jar validates clean; Java 8 is on PATH now); `ALL_POPUP_LANGUAGES` confirmed live (not dead code). **Uncommitted on disk = this audit's edits** (MATRIX_MAP · PLAN · design spec · 2 test files · NEW FINDINGS doc · CHANGELOG · this SESSION_STATE) **awaiting "save".** **▶ NEXT — WAVE 4: productionization → downloadable desktop app** (PyInstaller or native-window wrapper around the local `http.server` + launcher + a packaged-build smoke test; Windows first), per `dev/PLAN_2026-05-24-end-scope.md`. Security surface is LIGHT (single-user local app). **[USER]** still open: eyeball the Wave-3 presentation changes (footnote numbers · in-note symbols + legend tap-through · widened popups · topical index) on a real reader (Apple Books / e-ink).
>
> **➤➤➤ 2026-05-25 (prior) — 🎉 EPUB Wave 3 COMPLETE — all 3 prereqs + 7 features shipped + COMMITTED.** #7 (LAST): **Nave's topical-index back-matter page** — a 4,604-topic concordance from `content/sources/naves_topical.json` (`sources.naves_topical()`), canon-filtered + deduped + canonically ordered, rendered alphabetically after the Reference Tables (spine `backsources→backreftables→backtopical→backcolophon`, colophon last). NEW `build_topic_index` + `render_topical_index_page` in `matter_pages.py`; `inject_back_matter` gained `canon_books` + writes `topical.xhtml` (OPF manifest+spine+nav); `NavesTopical.topics()` in `sources.py`; `.topic-entry`/`.topic-name` CSS. VERIFIED: 7 topical-index tests + `test_presentation_polish` (extended back-matter spine assertion) green · lint 16/0/0 · ruff clean · ethiopian-tewahedo + catholic-study rebuilt → **epubcheck errors=0/warnings=0** · flagship topical = 4,604 topics.
>
> **WAVE 3 SUMMARY (all shipped 2026-05-25):** **prereqs** — #1 `MATRIX_MAP` refresh · #2 `build_edition`→`matter_pages`+`epub_utils` split · #3 `resync_marker_glyphs`(re-bake transformer)+`categorize_diff`(verifier). **features** — #4 `marker_style=numbers` (base re-bake: 69,811 markers→sequential per-chapter numbers, gapless in every edition via a build-time renumber post-pass) · #5 `‖`→fixed `↩` back-link + category symbols moved INTO notes as `legend.xhtml#legend-{cat}` links · #6 drop-KJV/widen verse popups (5-witness default wlc/lxx-greek/greek-nt/vulgate/arabic + KJV-floor fallback) · #7 Nave's topical index. **Commits:** `cb9eac9` (prereq #3) · `dfbff8a` (#4+#5) · `1dbac70` (#6) · `61226c5` (#7). **BACKUP:** E:/F: `…\YHWH-v2.4-backups\YHWH-v2.4-repo-2026-05-25-wave3-61226c5.bundle` (390 MB, `git bundle --all`, both verified) = Wave 3 complete at `61226c5`; the audit-prep commit AFTER it is this-machine-only (NOT re-bundled — user-confirmed commit≠backup). **▶ NEXT (the /clear point — user-directed 2026-05-25): run the LIGHT post-Wave-3 audit per `dev/AUDIT_2026-05-25-wave3-scope.md`** (clean-state + gates-from-scratch + Wave-3 currency: MATRIX_MAP entries for #6/#7 · marker_style declarative · kjv-floor fallback · dead-code/ALL_POPUP_LANGUAGES · REPO_MAP). Fix safe issues in-session, write `AUDIT_2026-05-25-wave3-FINDINGS.md`, then proceed to **Wave 4** (productionization → downloadable desktop app) per `dev/PLAN_2026-05-24-end-scope.md`. **[USER]** eyeball the presentation changes (footnote numbers · in-note symbols + legend tap-through · widened popups · topical index) on a real reader (Apple Books / e-ink) when convenient.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 #6 SHIPPED + COMMITTED — verse popups drop the English KJV + widen to Hebrew/Greek(LXX+NT)/Latin/Arabic.** Default witness set = wlc/lxx-greek/greek-nt/vulgate/arabic (`DEFAULT_POPUP_WITNESSES` in `popup_versions.py`; the unset fallback in `_resolve_popup_languages` returns it). All 9 standard editions' `popup_languages_default` set to the 5 (legacy `latin`→registry `vulgate`); the 2 standalone bibles keep `[]`; catholic-study's obsolete per-book overrides cleared. NOT a base re-bake (per-edition prune). **KJV-floor fallback (subtlety):** the popups hit 90.5% via a KJV floor, so ~6% of verses carry ONLY English — dropping kjv there would empty the popup + break note cross-refs to that vnote (RSC-012; a first "remove empty popup" attempt threw 23 dangling-fragment errors), so the build KEEPS English as a last resort where no original-language witness exists (2,232 flagship / 1,279 catholic); redundant English dropped for the other ~94%. **VERIFIED:** 9 popup-witness tests + 1057 (test_scripts+core) green · lint 16/0/0 · ruff clean · ethiopian-tewahedo + catholic-study rebuilt → epubcheck errors=0/warnings=0 · built-popup audit: **0 empty, 0 dangling vn-link hrefs**, 5 witnesses present. **[USER]** widened popups render best on a real reader (Apple Books/e-ink) — eyeball when convenient. **▶ NEXT — #7 (LAST Wave-3 item):** Nave's topical-index back-matter page from the 26,335 `topic-nave` notes — `render_*`/`inject_*` in `scripts/matter_pages.py`, a `nav.xhtml` TOC entry, OPF manifest+spine append after the last bodymatter; back-matter order Sources → Reference tables → Topical index → Closing colophon.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 features #4+#5 SHIPPED + COMMITTED — marker_style=numbers + ‖ fix + symbols-into-notes (the base re-bake).** The shared base's **69,811 markers** now render as sequential per-chapter footnote NUMBERS (`<sup class="marker-num">`, no inline glyph → no tofu); the **69,811 asides** carry a fixed `↩` back-link + the category symbol moved INTO the note as a clickable `legend.xhtml#legend-{cat}` link. **Gapless in EVERY edition:** even the flagship filters ~26,428 markers (21 kinds disabled, incl. the 26,335 `topic-nave` → the topical INDEX, NOT inline markers), so `build_one` renumbers survivors per chapter right after `filter_html` (idempotent on the unfiltered superset). `inject.build_aside` emits the new format at the SOURCE — byte-identical to `resync_marker_glyphs.rewrite_asides` (consistency-pinned) — so future injects never re-introduce the `‖`. `marker_style` field §9-wired (default `numbers`; `badge` DEFERRED). `resync_glyphs`/`resync_titles` guarded against `marker-num`/`↩`. CSS `.marker-num` + `.note-sym` added; `pills` selector → `:not(.note-sym)`. **VERIFIED:** byte-pins (build_aside/renumber/rewrite) · coexistence guards · **1057** (test_scripts+core+build_smoke) + **108** (nested-anchor real-base + presentation-polish + marker/popup) green · lint **16/0/0** · ruff clean · ethiopian-tewahedo + catholic-study rebuilt → **epubcheck errors=0/warnings=0** · **gapless: 0 gaps across 2,845 chapters / 85,095 numbered markers**. **[USER]** the in-note symbol + legend tap-through is a reader-only popup behavior — eyeball on an Apple Books / e-ink device. **▶ NEXT — #6** widen/drop-KJV verse popups (edit each edition's `popup_languages_default` + the ~`build_edition.py:749` unset-default fallback so `kjv` is dropped + the default witness set widens to wlc/lxx-greek/greek-nt/vulgate/arabic; NO base re-bake, spec §12.3) · **#7** Nave's topical-index back-matter page (from the 26,335 `topic-nave` notes — `render_*`/`inject_*` in `scripts/matter_pages.py`).
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 PREREQ #3 DONE + COMMITTED; all 3 infra prereqs complete — resume at features #4–#7.** The marker re-bake TOOLING is built + proven; NO base re-bake yet (that lands with features #4+#5). **Correction:** `scripts/resync_marker_glyphs.py` was NOT non-existent (the prior banner's "no existing script" was STALE) — it already held the data-driven glyph/tooltip resync (`resync_glyphs`/`resync_titles`, 17 tests), so prereq #3 EXTENDED it: **`renumber_markers`** (per-chapter sequential footnote numbers; counter resets at each `id="ch-bNN-cN"` heading — cross-ref `…#ch-…` hrefs do NOT reset, lacking `id="`; drops the inline category glyph → no tofu), **`rewrite_asides`** (back-link `‖→↩` + the category symbol moves INTO the note as a clickable `legend.xhtml#legend-{cat}` link), **`resync_html`**, a **`--rebake-numbers [--write]`** CLI mode, + `default_kind_to_cat`/`default_cat_label`. NEW **`scripts/categorize_diff.py`** — the categorize-diff verifier (`categorize` + `prove_marker_rebake`): proves a re-bake changed ONLY the intended `<sup>`/back-link regions (id-set + id→kind inventory unchanged; everything else byte-identical). **VERIFIED:** `tests/test_resync_markers.py` **20** + existing `test_marker_glyphs.py` **17** green · `lint_rules` **16/0/0** · ruff clean · **dry-run `--rebake-numbers` over the real base = 69,811 markers + 69,811 asides across 60 files; verifier ACCEPTED every file; ZERO writes**. **▶ NEXT — features #4+#5 in ONE re-bake:** change `inject.build_marker` (`inject.py:150`) + `build_aside` (`:190`) to EMIT the new format (TDD byte-pins, matching the transformer) · §7-wire `editions.yaml marker_style` (default `numbers`) · guard `resync_glyphs`/`resync_titles` against `marker-num`+`↩` so the two tools coexist · run `--rebake-numbers --write` over the base · `prove_marker_rebake` + categorize-diff prove · add `.marker-num`/`.note-sym` CSS · build flagship → epubcheck 0/0/0/0. Then **#6** widen/drop-KJV popups (editions.yaml `popup_languages_default` + the ~`build_edition.py:749` fallback; NO re-bake) · **#7** Nave's topical-index back-matter page.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 PREREQS 1–2 DONE + COMMITTED.** Wave 3's re-bake-dependent features are gated behind 3 infrastructure prereqs. **DONE this checkpoint:** **(1)** `dev/MATRIX_MAP.md` brought current — the Wave 2–3 presentation enum settings added to the Variable-trace table + a new "Presentation / reader-styling pipeline" section (the CSS-append vs base-re-bake distinction). **(2) Split `build_edition.py` 4085→3123 lines** → NEW `scripts/matter_pages.py` (the 18 front/back-matter `render_*`/`inject_*` functions) + NEW `scripts/epub_utils.py` (3 shared helpers `_xml_escape_text`/`_resolve_publishing`/`load_canons`); one-directional imports `build_edition`→`matter_pages`→`epub_utils` (no cycle); ALL moved names re-exported from `build_edition` (`# noqa: F401`) so callers are unchanged; `pyproject.toml` `E501`/`C901` ignore extended to `matter_pages.py`. VERIFIED byte-identical: `lint_rules` **16/0/0** · ruff clean · `test_presentation_polish.py` **35/35** (== pre-split baseline) · catholic-study rebuilt → **epubcheck 0/0/0/0** · smoke-import (no circular import; re-exports are the same objects). **▶ NEXT — prereq #3, then features #4–#7 (the session task list held all 7):** **#3** build `scripts/resync_marker_glyphs.py` (a surgical in-place marker re-bake transformer) + a **categorize-diff verifier** (categorizes baked markers/asides and proves a re-bake changed ONLY intended markers, byte-multiset conserved elsewhere — `categorize-diff` is a documented TECHNIQUE, no existing script; build fresh, TDD). **#4** `marker_style=numbers`: a base-wide change to `inject.build_marker` (`inject.py:150`) → superscript footnote numbers + a numbering pass with an explicit reset boundary + drop the inline category glyph; and FIX the stray `‖` — it is the `xref` category glyph reused as the `.note-back` back-link char in `build_aside` (`inject.py:170`), so give the back-link a fixed `↩` and render the category symbol as a deliberate in-note element (spec §12.4). **#5** symbols-into-notes + a clickable in-note symbol → `id="legend-<cat>"` glossary link. **#6** widen / drop-KJV verse popups — edit each edition's `popup_languages_default` + the unset-default fallback (~`build_edition.py:749`); NO base re-bake (spec §12.3). **#7** Nave's topical-index back-matter page (from the 26,335 `topic-nave` notes). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3; design spec `docs/superpowers/specs/2026-05-24-epub-presentation-polish-design.md` §4/§12. **Also this session (non-Wave-3):** disabled the `semgrep` Claude plugin (`~/.claude/settings.json` `enabledPlugins` → `false`) — its hooks shell out to a `semgrep` CLI not installed on this Windows box, so every prompt printed `command not found`; takes effect on a Claude Code restart / `/reload-plugins`. The 4 regenerated long-title covers (ethiopian/catholic/anglican/lutheran) were visually verified — the `_fit_title_font` fix holds and the titles clear the borders (closes the prior "USER eyeball the 4 covers" item).
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 STARTED — the two CSS-only popup-layout settings SHIPPED + verified.** Re-bake-FREE slice of Wave 3 (Presentation Phase 2). Both §7-wired like `title_page_style` and applied in `build_one` by APPENDING a variant CSS block to the edition stylesheet (same mechanism as the theme override) — popup/note HTML unchanged, **NO base re-bake**. **§4.2 `verse_popup_style` (`cards` default / `stack`):** `cards` → `_VERSE_POPUP_CARDS_CSS` (tinted witness cards, gold/purple spines on `.vnote-hebrew`/`.vnote-greek`); `stack` → flat base (no append). **§4.4 `note_popup_style` (`chip` default / `pills`):** `chip` → tinted rounded label-chip on `.note .note-label`; `pills` → bordered tappable pills on `.note a:not(.note-back)` (the negative-space selector isolating in-note cross-refs from the back-link glyph — baked xrefs carry no class of their own). NEW in `build_edition.py`: `VERSE_POPUP_STYLES`/`NOTE_POPUP_STYLES` consts + `_*_CSS` blocks + `apply_verse_popup_style`/`apply_note_popup_style` (called back-to-back in `build_one` right after the theme append). Wiring: `scripts/api/editions.py` (both allowed-field lists + 2 enum validators), `web.py api_customize_data` (defaults cards/chip), `scripts/templates/customize.py` (2 `<select>`s). **VERIFIED:** `tests/test_popup_styles.py` **16** (verse 8 + note 8) · **86** build-level regression (title_page/cover_templates/presentation_polish/popup_styles) green · `lint_rules` **16/0/0** · ruff clean · catholic-study rebuilt → **epubcheck 0/0/0/0** · shipped-stylesheet content-verified (both blocks present, appended last; cascade-correct — `.note .note-label` 0,2,0 wins over base `.note-label` 0,1,0, while the `note-comm-` hide rule 0,2,1 still wins so intentionally-hidden labels stay hidden). **▶ NEXT — the re-bake-DEPENDENT remainder of Wave 3** (gated behind the tools/matrix prereqs): `marker_style`→numbers · symbols-INTO-notes (+ symbol→glossary link) + drop the stray `‖` · widened/drop-KJV popups · Nave's topical back-matter page. Prereqs first: update `dev/MATRIX_MAP.md` (the 2 popup-style data-flows + the remaining enum settings) · build `scripts/resync_marker_glyphs.py` + a categorize-diff verifier · split `build_edition.py` (~3,860 lines). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 2 COMPLETE: §4.6 cover picker SHIPPED + a user-reported title-overflow fix + 3 pre-existing bugs fixed during browser QA. Committed this session.** **§4.6 (25-design cover picker + uploads, task #13):** NEW `scripts/core/covers.py` `COVER_TEMPLATES` (25 stems = 5 families {`01_ornate_leafy`·`02_classical_corner`·`03_beadline`·`04_minimal_lines`·`05_missal_central`} × 5 colors {`black`·`brown`·`forest`·`navy`·`red`}) + `cover_template_catalog()` (PIL-free, so the enum validator imports it without Pillow). `cover_template` field §7-wired EXACTLY like `title_page_style`: `api/editions.py` allowed-field lists + enum-validation block; `web.py api_customize_data` per-edition default `""` + a shared top-level `cover_templates` catalog. NEW `scripts/api/covers.py::api_apply_cover_template(edition_id, stem)` (audit-logged) — validate stem∈COVER_TEMPLATES + edition → `generate_edition_covers._compose_cover(stem, title_for_edition(id))` → `ensure_backup` + `atomic_write_bytes` to `covers/<id>.jpg` → `api_save_edition_meta({cover_image, cover_template})`; routed `POST /api/covers/<ed>/template` in `_POST_ROUTES` (distinct suffix from the `/main` multipart + DELETE routes). `/customize` "Cover" panel (static markup in `renderEditions`, behaviour in NEW `wireCoversSection` — no innerHTML reassignment, security-hook-clean): clickable 25-thumbnail picker (thumbs = sandbox-served `/content/covers/templates/<stem>.png`; pick → immediate server recompose → in-place preview refresh) + main-cover (`/main`) & per-book-art (`/book/<code>`) upload affordances. **23 tests** (`tests/test_cover_templates.py`). **Verified LIVE in-browser** (Playwright): 11 cover sections × 25 thumbs render, real click → recompose → "Cover updated." + preview refresh; all editions interactive (see fix #1). **TITLE-OVERFLOW FIX (user-reported):** `_compose_cover` drew at a fixed 72pt with no width handling → long titles ran into the border art. NEW `_fit_title_font` auto-shrinks 72→28pt (4pt steps) until the widest line fits a **150px side safe-zone**; the 4 long-title covers (ethiopian/catholic/anglican/lutheran) regenerated at 64pt with ≥162px side clearance, the other 5 unchanged at 72pt. **3 PRE-EXISTING BUGS FIXED (surfaced by live browser QA):** (1) **`/customize` wired ONLY the first edition** — the rebind loop's `wrap.querySelectorAll('[data-edition]')` ALSO matches each edition's psi11 Preview button; on the first such box `btn` is null and the unguarded `btn.addEventListener` threw (swallowed by a try/catch) → the `forEach` aborted → editions 2-11 had NO handlers at all (save · popup-langs · traditions · reading-plans · covers). Fix: `if (!btn) return;` guard + regression test `test_edition_rebind_loop_skips_non_card_boxes`. **Had been broken for 10/11 editions; source-string tests couldn't catch it.** (2) **SQLite cross-thread 500** — `/customize` + `/matrix` 500'd in the RUNNING app: `corpus_index.connection()` cached a conn opened in the `_warm_corpus_index` (main) thread, reused on `ThreadingHTTPServer` request threads. Fix: `sqlite3.connect(..., check_same_thread=False)` (index is read-only-by-convention; writes use a separate conn in `rebuild()`) + cross-thread regression test. (3) **5 cover-template PNGs were actually JPEGs misnamed `.png`** (the `*_red` variants) → `_send_file`'s SEC-001 magic-byte guard 415'd them (broken picker thumbnails). Re-encoded to real PNG (lossless) + guard test `test_every_template_file_is_really_a_png`. Also repaired a stale pre-existing test (`querySelectorAll('input, select')` → `…, textarea')`, stale since Phase 1 added the description/dedication textareas). **GATES:** 221 tests green · `lint_rules` 16/0/0 · ruff clean · editions.yaml unchanged. **⚠ Could NOT capture a confirming screenshot — the Playwright session repeatedly closed mid-run; title-fit is proven by test + measurement (all 9 titles ≤724px after fit) — USER to eyeball the 4 regenerated covers + flag any that still crowd.** **▶ NEXT — Wave 3 (EPUB Presentation Phase 2; the BASE RE-BAKE wave, riskiest + most Claude-dependent):** the remaining 3 enum settings (`marker_style`→numbers · `verse_popup_style` · `note_popup_style`) · symbols-into-notes (+ symbol→glossary link) · widened/drop-KJV popups · Nave's topical back-matter page. Prereqs (tools/matrix review #12): update `dev/MATRIX_MAP.md`, build the `resync_marker_glyphs.py` companion + categorize-diff verifier, split `build_edition.py` (~3,800 lines). Plan: `dev/PLAN_2026-05-24-end-scope.md` Wave 3.
>
> **➤➤➤ 2026-05-25 (prior — §4.6 now SHIPPED, see top). EPUB Wave 2: §4.5 per-book title-page art DONE + committed; §4.6 cover picker was NEXT.** Session commits: nested-`<a>` 746→0 + a surfaced RSC-001 → epubcheck 0/0/0/0 (`c83c743`) · title-only edition covers + universal PIL-anchor placement (`6e58570`) · **§4.5 `title_page_style` (full-bleed default / framed) + per-book title-page art** (this commit). **§4.5 mechanics:** NEW `build_edition.apply_title_pages(tmp, edition, canon_books)` — called in `build_one` AFTER the canon filter — injects each KEPT book's art (resolution: `book_covers` override → `content/covers/_book_defaults/<code>.jpg` → text-only fallback) into its `book-title-frame`; **full-bleed** = art fills the page + title overlaid on a dark scrim panel (`.book-title-page.style-full-bleed` in `epub_working/stylesheet.css`), **framed** = a contained art plate; `patch_opf_book_images` registers them in the OPF (chain, right after `patch_opf_fonts`); the `data-book-idx`↔`config.load_books()` mapping keys book→art. Setting §7-wired: `TITLE_PAGE_STYLES` const · `api_save_edition_meta` enum block · `api_customize_data` (default `full-bleed`) · `/customize` `<select>`. **VERIFIED:** `tests/test_title_page_style.py` 12 + 170 customize/editions regression green; built catholic-study → **epubcheck 0/0/0/0**, 66 art images packaged + OPF-registered, deutero books text-only; Genesis full-bleed title page **visually confirmed** (Playwright — art fills page, title legible on the scrim); ruff clean. **▶ NEXT — §4.6 (25-design cover picker + uploads), task #13:** add a `cover_template` field (25 stems = {`01_ornate_leafy`·`02_classical_corner`·`03_beadline`·`04_minimal_lines`·`05_missal_central`} × {`black`·`brown`·`forest`·`navy`·`red`}; §7-wire exactly like `title_page_style`) → a clickable 25-thumbnail picker in `/customize` (thumbs = the sandbox-served `/content/covers/templates/<stem>.png`) → on pick, recompose the edition cover via the EXISTING `generate_edition_covers._compose_cover(stem, edition_title)` (already refactored this session) + update `cover_image`; PLUS surface the EXISTING upload endpoints `/api/covers/<ed>/main` + `/api/covers/<ed>/book/<code>` (`scripts/api/covers.py` → `scripts/web.py::_save_cover_bytes` → `covers.validate_upload_image`, 2:3 ±20%; the per-book book art IS 2:3 so the validator is fine) as UI affordances. Reuse map captured this session; plan = `dev/PLAN_2026-05-24-epub-presentation-polish.md` §2E. Then the rest of Wave 2/3 per that plan.
>
> **➤➤➤ 2026-05-25 (prior — COMMITTED `c83c743`) — EPUBCHECK NOW CLEAN — nested-`<a>` (746 RSC-005) FIXED + a surfaced RSC-001 FIXED; catholic-study epubcheck 0/0/0/0.** **(1) Nested-`<a>` — the doc'd root cause was WRONG** (re-verified with real data, [[reverify-conservative-nogo]]): NOT a `generate_verse_popups.py` `vchunk` over-scope (that fn only ever wraps the bare `<span class="vn">` needle — byte-identical at the bake commit `b0bf913`; `inject.find_verse_region` matches only the clean span-only wrapper; the offending word-study `note-ref`s aren't even in `content/notes/`). ACTUAL cause = a **static artifact in the recovered base** (v28a-50 snapshot, defunct tooling): **746 `note-ref`-nested-INSIDE-`vn-link`** across **8 files** (psa `033/034/035` · sir `038` · isa `040` · jer `042` · 2co+phi `058` · 1ti+jam `059`) — not the 2 the prior banner named. FIX = NEW `scripts/check_nested_anchors.py` (stdlib-parser detector + byte-exact regex repair relocating each note-ref to a sibling *before* the vn-link, glyph order preserved); `--fix` → **746→0** (byte-multiset conserved per file; only those 8 files changed) + **JVM-free `nested <a>` gate wired into `scripts/ship-check.py`** + `tests/test_nested_anchors.py` (10). **(2)** Clearing those **unmasked an RSC-001** (Phase-1 regression): `_drop_placeholder_introduction` removed `introduction.xhtml` + its spine itemref but its **manifest regex used `[^/]*`**, which can't cross the `application/xhtml+xml` slash → dangling manifest item → "file could not be found". FIX `[^/]*`→`[^>]*` + a fast unit test + **strengthened** the integration test (it only checked spine+zip, MISSING the manifest — the false-confidence gap that let this ship). **VERIFIED:** rebuilt catholic-study → **epubcheck 0 fatals / 0 errors / 0 warnings / 0 infos**; 63 popup/presentation tests green; `ruff format --check .` clean. **COMMITTED `c83c743`.** Touched: `scripts/check_nested_anchors.py` (NEW) · `scripts/ship-check.py` · `scripts/build_edition.py` · `tests/test_nested_anchors.py` (NEW) · `tests/test_presentation_polish.py` · 8× `epub_working/index_split_*.html`.
>
> **➤➤➤ 2026-05-24 (PHASE 1 SHIPPED, prior).** (Supersedes the design-phase banner below — Phase 1 is DONE + COMMITTED, not "implement next.") **EPUB PRESENTATION OVERHAUL — PHASE 1 COMPLETE.** Shipped + verified: **left-align body text · cover fits frame (`object-fit:contain`) · colophon rewritten (real per-edition counts via `matrix`, no `TODO_`, no stale "1,371"; identity = YHWH Ya' Way Editions / © Bogdan Zorlescu) · "A Guide to the Notes" edition-aware symbol glossary (clickable `id="legend-<cat>"` anchors ready for the Phase-2 symbol→glossary link) · About-this-Edition (auto-generated spec from resolved choices: canon/book-count · selected popup-witness languages · per-category note counts · theme; + editable free-text `description`) · optional Dedication page · Back matter (Sources & Acknowledgments → Reference Tables → Closing Colophon) · `description`+`dedication` editable in `/customize` · dropped the placeholder `introduction.xhtml` · `h2` page-break-avoid CSS · inject OPF-anchor guards.** Front/back-matter order: **titlepage → dedication(opt) → colophon → Guide → About → [scripture] → Sources → ref-tables → closing-colophon.** Code: `scripts/build_edition.py` (render_*_page + inject_*_page family + `_drop_placeholder_introduction`), `web.py` (`api_customize_data`), `api/editions.py` (EDITABLE_TEXT +description/dedication), `templates/customize.py` (textareas + `input,select,textarea`), `epub_working/stylesheet.css`, `tests/test_presentation_polish.py`. **GATES GREEN:** lint 16/0/0 · `test_presentation_polish.py` 34 passed · `ebible verify` errors=0/24,015 paired · ruff clean · **NO `content/`/corpus/base-HTML change**. Reviewed by 3 fresh-eyes passes (code + verification + tools/matrix) pre-commit.
>
> **▶ #1 ITEM (CRITICAL — demo-affecting) — ✅ RESOLVED 2026-05-25 (see the LATEST banner above for the full account).** The 746 RSC-005 were **not** a `generate_verse_popups` `vchunk` bug — that inherited diagnosis was wrong (re-verified). They were a static recovered-base artifact (`note-ref` nested in `vn-link`) across 8 files, fixed by NEW `scripts/check_nested_anchors.py` + a JVM-free `ship-check.py` gate + `tests/test_nested_anchors.py`. A surfaced RSC-001 (dangling `introduction.xhtml` manifest item — `_drop_placeholder_introduction` `[^/]*`→`[^>]*`) was also fixed. **catholic-study epubcheck now 0/0/0/0.**
>
> **▶ FORWARD ORDER:** `dev/PLAN_2026-05-24-end-scope.md` (the single active plan; PLAN_2026-05-21 → `dev/archive/`). Waves: **0** foundation (README · asset-licensing docs for the Midjourney covers · Git-LFS note) · **1** = THIS (done) · **2** covers (title-only + universal placement + 25-design clickable picker + per-book art upload) · **3** Phase-2 settings (4 enum fields + markers→numbers + symbols-into-notes + widened popups = BASE RE-BAKE; ~~the nested-`<a>` fix lands here~~ nested-`<a>` already FIXED 2026-05-25, standalone) · **4** downloadable-desktop-app packaging.
>
> **▶ TOOLS/MATRIX/ARCH (review #12) — before Wave 3:** update `dev/MATRIX_MAP.md` (add matter-page data-flows + the 4 Phase-2 enum settings; self-upgrading-matrix rule) · **build `scripts/resync_marker_glyphs.py` (does NOT exist) + a categorize-diff verifier** (Wave-3 re-bake PREREQS) · **split `build_edition.py` (~3,800 lines) → `scripts/matter_pages.py`**. Any-time/cheap: a one-command **`make phase-gate`** (the §5 gate is doc-only; ran ~16 min ad-hoc). Wave 2: cover **preview endpoint** + per-template title-safe-zones. Wave 4: PyInstaller spec (+ hidden-imports for `scripts/templates/*`) + launcher + launch+build smoke.
>
> **Backups → D: + external E:/F:, NOT C: (low on space).** `GAPS` moved to `D:\YHWH-v2.4-GAPS` (junction at `GAPS/`; code reads it transparently); `exports/` cleared (regenerable); C: 16 GB free.

> **➤➤➤ 2026-05-24 (LATEST) — read FIRST. EPUB PRESENTATION OVERHAUL — DESIGN APPROVED + SPEC WRITTEN; implement next (after a /clear).** Driven by a real Apple Books review of the flagship (`apple_books_screenshots/`). Per RULES §2, the presentation choices become **configurable per-edition builder settings with defaults** (editions.yaml → /customize → build pipeline). **SPEC = `docs/superpowers/specs/2026-05-24-epub-presentation-polish-design.md` (read it first).** **Decisions:** `marker_style` = **numbers (DEFAULT — inline footnote numbers, "safe always")**; `badge` (verse-end count) is OFFERED but **DEFERRED** (needs a per-verse note container whose base-HTML injection point is TBD — user also unsure). `verse_popup_style` = **cards** (default) / stack. **Popup witnesses default = Hebrew + Greek (LXX/NT) + Latin + Arabic** — the English `kjv` is DROPPED from popups (it duplicated/mismatched the WEB reading text); JPS/Douay/Brenton stay selectable per edition/book (registry = 9 in `popup_versions.py`). `note_popup_style` = **chip** (default) / pills. `title_page_style` = **full-bleed** (default) / framed, wiring the per-book art in `content/covers/_book_defaults/<book>.jpg`. **Category symbols move INTO the notes** (no inline symbols). **Front matter → 2 pages** (colophon: "YHWH Ya' Way Editions, 2026, © Bogdan Zorlescu", REAL computed counts, zero `TODO_`, kill the stale "1,371/14"). Tech fixes everywhere: left-align (kills stretched justification), cover `object-fit: contain`, real counts. **Scope = all 11 editions.** **3-phase plan:** Ph1 quick wins (left-align · cover fit · front-matter consolidation) · Ph2 the CSS/template-variant settings + `numbers` marker + widened popups + book-art wiring · Ph3 DEFERRED `badge` mode. **▶ NEXT (post-/clear): invoke `writing-plans` on the spec, then implement Ph1–2** (TDD; output INTENTIONALLY changes vs current — pin the NEW expected output, don't assume byte-identity). Reference mockups: `EPUB-design-options.html` (outer folder, standalone, opens in any browser) + `.superpowers/brainstorm/`. A built check EPUB sits in `_epub_check/` (outer folder). **ALSO committed this session:** Track C corpus-expansion **SCOPE-CAPPED** to the five named reference works (Matthew Henry / JFB / Barnes / Torrey's New Topical Textbook / Vincent's Word Studies — all IN scope: Henry seeded χ.2, Torrey sourced on disk, the rest PD/CCEL via the χ-cluster pipeline, NOT blocked on the user); Voyage AI + any work beyond those five DE-SCOPED (PLAN §3 Track C). Phase E (Latin appendix, `man/1es/2es`) PAUSED behind this overhaul.
>
> **➤➤➤ 2026-05-24 (prior) — DEFERRED BACKLOG CLEARED + COMMITTED (the 5 items the prior session stopped on for context budget); gates all green.** **(1) C1.run §7.1:** `content/notes/__init__.py::load_notes` now parses via `ast.literal_eval` (`notes_io.load_notes`) instead of `exec_module` — data files are NOT executed (RULES §7.1). Byte-equivalence PROVEN pre-swap (exec vs ast over all 87 books = **69,969 notes / 0 mismatch**); FileNotFoundError contract kept; RED canary `tests/test_core.py::TestContentNotesLoader` proves the body no longer runs. **(2) Invalid-escape cleanup:** the two backslash-space OCR SyntaxWarnings (luk.py:8924 + mat.py:22432, Kenyon witness notes) removed at SOURCE AND in baked `epub_working` (surgical aside-text replace in index_split_053/052 — NOT a lossy regen; both files re-parse well-formed); 0 residual; corpus-wide guard `test_notes_corpus_has_no_invalid_escape_sequences`. **(3) `ebible audit` NEW** subcommand + `make audit`: aggregates the off-dashboard audits (caches/vulture/mypy/pip-audit) into one CI gate — pure `run_audit_suite` + thin `cmd_audit`, missing-tool skips (exit 0), `--require-tools` strict; 5 unit + e2e (pip-audit not installed here, skipped). **(4) P3/P4 tail:** preview.py `_NOTE_TITLE`/`_NOTE_LABEL` indices+comment fixed to canonical (title=5/label=6 per inject.py:657); `VALID_PHASES` single-sourced from `config._PHASE_ORDER` (validate_taxonomy + validate_schemas both reference it; phase4 dropped to match runtime, no content uses it); 3 brittle `len(editions)==11` to `>=11`; `ebible print` passthrough removed (print_cover pivot-dead). **DEFERRED (reasoned):** release.py 3×subprocess-DEVNULL (orphaned pre-pivot — archival per §7.4 is the call) · C3b.promotediv (guarded stub = forward δ.1.x.A feature). **Gates:** lint_rules 16/0/0 · `verify` errors=0 / 24,015 paired · ruff-format clean · git blast-radius 13 files intended-only (luk/mat ±1 char); corpus unchanged 69,969 (item 2 text-only). Tests: test_core 43 · 4 dedicated files 252 · targeted test_scripts subset 382 · TestContentNotesLoader 4 · TestEbibleAudit 5. **▶ NEXT — `dev/PLAN_2026-05-21.md` §4.1 + roadmap:** Track B manuscript marathon (CRITICAL PATH; resume 1Ki5 R2) · Phase-E Latin / vision-OCR / Phase 3-4 builder tail · web.py A5/A10/A14/A11 + remaining P3/P4 (C2.addnote shape · C1.tmp · C3a.release archival) · user-only: [USER] e-reader device check.
>
> **➤➤➤ 2026-05-24 (prior) — `test_scripts.py` RUNNABLE AGAIN: 976 tests, ~2.9 min, GREEN (D.hang + D.slow both fixed).** **D.hang** (deadlock): the 9 live-socket smoke tests used single-thread `HTTPServer`+`serve_forever`+`shutdown()` (no timeout) → HTTP/1.1 keep-alive blocked request 2 → `shutdown()` deadlocked forever; fixed by `ThreadingHTTPServer` ×9 + `test_ops` mocking `api_preflight`. **D.slow** (the ~1 hr a full run then took): a session-autouse conftest fixture `_stub_exports_epubcheck` stubs the real epubcheck/Java run over the 12 `exports/` EPUBs (minutes/call, hit by ~10 preflight-calling tests) — only for the real `exports/` dir, so `TestEpubcheckWrapper`'s tmp tests stay real. Also fixed **2 stale pins** the now-runnable suite exposed (both broke pre-session): `test_default_theme_is_classic`→`test_every_edition_has_a_registered_theme` (themes assigned 2026-05-22), and the popup-registry test (`douay`/`vulgate` baked at `42a59e0` → `has_data` True). **The "never run full test_scripts.py" rule is genuinely RETIRED** (SESSION_PLAYBOOK §4) — a full run is ~3 min now. **ALSO DONE this session:** web.py cleanup A1/A6/A9/A12 (`fb50001` — dead `_send_dict_result` removed, 2 stale docstrings fixed, 85 orphaned banners dropped); **`test_web_filesplit.py` (88 tests, ~11 s) + `test_matrix_psi35.py` (39, ~12 s) confirmed FAST** — the D.slow conftest stub covers them session-wide (no extra work needed). **▶ DEFERRED (owner said stop 2026-05-24 — context budget):** `C1.run` `exec_module` §7.1 smell (import WORKS; `content/notes/__init__.py::load_notes` should use the ast-based `notes_io.load_notes` instead of `exec_module` — small) · remaining `audit_*` (vulture/mypy/pip-audit) → a CI / `ebible audit` target (NOT the live dashboard) · P3/P4 tail (C2.phase4 PHASES const · C3a/b dead-code · D.pins `==`→`>=` · **preview.py `_NOTE_LABEL`/`_NOTE_TITLE` constants swapped vs canonical** — cosmetic). Full backlog detail: `dev/AUDIT_2026-05-23-DEEP.md` + `dev/PLAN_2026-05-21.md` §4.1. (Commits this session: `309c0f7` phi/jam · `0892270` G1/G2 sec · `1ae0d23` a11y→preflight · `507bf7a` hebrew backfill · `e090998` D.hang · `f7888ad` D.hang-correction · `001320f` D.slow · `fb50001` web.py cleanup · this doc-sync.)
>
> **➤➤➤ 2026-05-24 (prior) — C1.chap HEBREW BACKFILL SHIPPED (owner-approved) `507bf7a`.** The `ch_count`-default-50 bug had truncated the at-scale hebrew generator at chapter 50, so the four OT-KJV books with >50 chapters carried no auto `lang-hebrew` past ch 50. Re-ran at the original **`--min-confidence 0.65`** (the 0.7 default yields ZERO; at 0.65 ch1-50 candidates EXACTLY match existing notes → dedup-skip 1-50, add 51+ only). Added **+2,151 `lang-hebrew`**: psa 840→2502, isa 1031→1406, jer 1332→1422, sir 1134→1158. **Corpus 67,818 → 69,969.** Max hebrew chapter now = each book's `ch_count`. Isolation PROVEN content-level (only `lang-hebrew` rose per book; only those 4 books' 6 epub split files changed — additive `inject --book`, NOT a regen). Built + valid: flagship carries it (22,897 lang-hebrew asides == master), **epubcheck 0/0**; ebible verify errors=0/24,015; taxonomy 69,969/100%; lint 16/0/0. TDD guard `TestLongBookHebrewBackfill`. **▶ NEXT — OWNER-DECISION (`dev/PLAN_2026-05-21.md` §4.1):** remaining `audit_*` dead-checks → CI (not the dashboard) · P2 web.py god-module pass (A1/A6/A9/A12) · P3/P4 mechanical tail. (4 commits this session before this one: `77fb13c` rename · `309c0f7` phi/jam · `0892270` G1/G2 security · a11y+preflight `1ae0d23`.)
>
> **➤➤➤ 2026-05-24 (prior) — Autonomous backlog: G1/G2 security + a11y-into-preflight SHIPPED.** Three commits: phi/jam data-cleanup (`309c0f7`), G1/G2 security (`0892270`), a11y+preflight (`1ae0d23`). **G1** (`core/http.py`) — added a `{http,https}` scheme allowlist so `file://` can't bypass the SSRF guard (was an LFI). **G2** (`core/preview.py`) — the UNAUTH `GET /api/preview` now runs `sanitize_html` on note bodies (was an XSS). **★C2.deadchecks** — `check_a11y.run_all()` added + composed into `/preflight` (WCAG/EPUB-accessibility now surfaces on the readiness dashboard); the other 3 `audit_*` tools (vulture/mypy/pip-audit subprocess wrappers) were DELIBERATELY left off the live dashboard (manual/CI only — network/slow). TDD +11 (G1 5 · G2 2 · a11y 4). Gates green (lint 16/0/0 · ruff clean · taxonomy 67,818/100%). **▶ NEXT — OWNER-DECISION items (`dev/PLAN_2026-05-21.md` §4.1):** **C1.chap backfill** (Psalms 51-150 / Isaiah / Jeremiah hebrew — the `ch_count` code-fix is DONE; re-running the at-scale generators would add THOUSANDS of reviewer-draft notes = a corpus-scale **content decision** for you, so it was NOT done unsupervised) · the remaining `audit_*` dead-checks → CI (not the dashboard) · P2 web.py god-module pass (A1/A6/A9/A12) · P3/P4 mechanical tail.
>
> **➤➤➤ 2026-05-24 (prior) — phi/jam DATA-cleanup SHIPPED + certified (the ★BUGCLUSTER follow-up is DONE).** The 165 spurious `lang-hebrew` notes are stripped from phi.py (76) + jam.py (89) via AST-span removal (1,815 pure deletions) and **270 `lang-greek`** notes generated + promoted (phi 156, jam 114; `run_greek_at_scale --min-confidence 0.65`, the original χ.1 threshold). **Corpus 67,713 → 67,818** (−165 +270 = +105). `epub_working` was updated by **SURGICAL removal** of the orphaned phi/jam hebrew markers+asides (`index_split_058`=phi/b70, `_059`=jam/b79) + targeted inject of the greek — **NOT** the playbook §7 bare-base regen, which is **LOSSY** (it drops `harvest_existing_langs`-preserved popups — proven: it wiped `vnote-paz-1-30`'s Douay+Vulgate — and re-qualifies ~88 unrelated xref hrefs; **playbook §7 needs this caveat**). Byte-compat PROVEN content-level (aside-by-id): ONLY phi/jam asides changed (165 hebrew gone, 270 greek in), only 2 files touched, paz + all xrefs byte-identical. TDD guard `tests/test_corpus_chi1.py::TestNTBookLanguageInvariant` (no NT book has `lang-hebrew`; phi/jam have `lang-greek`). Gates ALL green: lint 16/0/0 · taxonomy 67,818/100% · schemas 6/6 · trace_matrix 0 · trace_repo 0 · ebible verify errors=0/24,015 paired · test_corpus_chi1 23 · test_enabled_kinds_unified 12 · build catholic-study → epubcheck 0/0. Tests 7,064 → 7,066 (+2). **Committed this session.** **▶ NEXT (autonomous backlog, `dev/PLAN_2026-05-21.md` §4.1):** P0 **C1.chap >50-chapter backfill** (re-run hebrew/greek at-scale for Psalms 51-150 / Isaiah / Jeremiah — the runner-key-50-default bug, now code-fixed; same regen family — use the surgical method) · P1 **G1** `file://` SSRF + **G2** preview-XSS (security HIGH, open) · P2 wire the 4 dead audit-checks into preflight.
>
> **➤➤➤ 2026-05-24 (prior) — DEEP AUDIT COMPLETE + matrix MINT-cert.** The deferred every-file audit (`dev/AUDIT_2026-05-23-DEEP.md`) ran read-phase A–H + synthesis (100% tracked code/doc/config coverage) + **17 verified safe-fixes** (dead-code, latent bugs incl. the `ch_count` + book-code finds, Windows `DEVNULL` hygiene, docstrings, doc counts — **NO content/corpus/build change**) + **matrix MINT** (trace_matrix 0 · taxonomy 67,713/100% · ebible verify errors=0/24,015 paired · catholic-study build → epubcheck 0/0/0/0; lint_rules 16/0/0; 7,064 tests = baseline). **Uncommitted on disk = the audit docs + the 17 safe-fixes, awaiting "save".** **Headline QUEUED (user go/no-go) → `dev/PLAN_2026-05-21.md` §4.1:** P0 **★BUGCLUSTER** book-code drift (`php`/`jas`/… — confirmed **165 spurious `lang-hebrew`** on Philippians+James + 0 `lang-greek`) — **book-code CODE-FIX SHIPPED `c41e6d2`** (recurrence prevented); the phi/jam DATA-cleanup (strip 165 + generate Greek + regen epub_working) is an **atomic follow-up** — full recipe in `dev/SESSION_PLAYBOOK.md` §7 + the C1.chap >50-chapter backfill; P1 **G1** `file://` SSRF + **G2** preview XSS (security HIGH, open) + LICENSE/COPYRIGHT = **"Copyright (c) 2026 Bogdan Zorlescu. All rights reserved."** (CC0 was applied at `637e793` then REVERSED per user — they want the program protected from copying/theft; incorporated Bible sources stay PD); P2 wire the 4 dead audit-checks into preflight. **NEW: `dev/SESSION_PLAYBOOK.md`** = the session lifecycle + verification-gate checklist.
>
> **State correction (E.sessionstate):** any "UNCOMMITTED" label below on Douay/Vulgate/Arabic/JPS is STALE — those shipped at HEAD `42a59e0`. This 2026-05-24 line is the current state.
>
> **Program renamed → "YHWH Ya' Way"** (committed); `LICENSE` = all-rights-reserved (Bogdan Zorlescu). **▶ NEXT TASK (fresh session): the phi/jam corpus data-cleanup** — the work-order is the top banner of `dev/IN_FLIGHT.md`; full recipe in `dev/SESSION_PLAYBOOK.md` §7.
>
> **➤➤➤ 2026-05-21 (prior LATEST) — read FIRST.** Corpus **67,713 notes / 71 kinds / 11 editions / 87 books**; demo build LIVE — `inject` places **99.76%** (66,173 + 1,381 base-baked / 67,713), `ebible verify` **errors=0 / 24,015 paired**, valid EPUB — **ALL 11 editions epubcheck-clean (EPUB-3.3: 0 fatals/errors/warnings/infos — each of the 11 editions verified individually 2026-05-22, not just the 5 canon-shape reps)**; master HTML **0/61 malformed XML** (2026-05-22 inject-wellformedness + RSC-005 nested-`<p>` + RSC-012 xref-code fixes; `audit` **B8** gate added). Health: `trace_matrix` **0 unresolved refs**, `validate_taxonomy` 100% attributed, ruff-format + `lint_rules` clean. Shipped chronology + latest commits: `dev/CHANGELOG.md`.
>
> **The map layer (read for "where / how / what shipped"):** `dev/PLAN_2026-05-21.md` (forward roadmap; supersedes `PLAN_2026-05-09`) · `dev/IN_FLIGHT.md` (live task tracker) · `dev/MATRIX_MAP.md` (data-flow + base-HTML structure) · `dev/REPO_MAP.md` (file/folder index) · `dev/CHANGELOG.md` (shipped chronology) · **`dev/SESSION_PLAYBOOK.md`** (session lifecycle start→work→verify→finish-clean + the consolidated verification gates). (This file was trimmed from ~896 KB on 2026-05-21; history in `dev/SESSION_STATE_archive_2026-05-21.md`.)

## What shipped (2026-05-21 arcs)

- **DOUAY-RHEIMS + CLEMENTINE VULGATE SHIPPED — PHASE-2 TRANSLATION SPINE COMPLETE (2026-05-23, COMMITTED `42a59e0`).** TABLE-DRIVEN: `_VULGATE_SEGMENTS` generated from the Copenhagen `vul.json` (composed vul→org→KJV), then every book driven to **SHIFTS=0** against the real Douay via `_vg_verify.py` (the table proposes, the gate decides — the eBible Douay diverges from the idealized Vulgate, so the gate caught every composed error: gen49, exo-not-reorder, dan 3/4 boundary, lje, mat5 Beatitudes swap, lev15, 20 psalm fixes). Cross-book Daniel additions → `paz` (Theodotion Benedicite reorder) / `sus` / `bel` (the Astyages verse routed from the Susanna tail — caught by content spot-check, NOT the gate, since these books have no Douay *source* book). `wis` included; `tob/jdt/sir` omitted (recension — `vul.json` agrees); 14 Douay≠Vulgate chapters via shared concat-tail segs. Shared `versification.vulgate_to_kjv` (+ `_vulgate_cross`); thin `extract_{douay,vulgate}.py`. Stores **74 bk each / 33,343 + 33,344 v**, 0 oox; baked (`douay`+`vulgate` in `_BAKED_NOW`): **+31,867 `vnote-douay` + 31,866 `vnote-vulgate`, 0 other-content mismatches** across 33,768 asides. `ebible verify` errors=0/24,015 paired; **epubcheck catholic-study+anglican-bcp 0/0/0/0**; `lint_rules` 16/0/0; 153 vulgate + 16 popup + 333 LXX tests. 8 residual SHIFTS = proven Hebrew↔Latin transliteration/clause noise (not misplacements). License: Copenhagen facts re-expressed in-repo (CC0-clean; JSON not vendored). Follow-ups: Phase E appendix Latin (man/1es/2es, clean-digital-first) + shared vision-OCR engine + prior-translation re-verify. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **TRANSLATION SPINE: Arabic + JPS SHIPPED (Phase-2 finish, 2026-05-23, UNCOMMITTED).** NEW reusable `extract_translation.apply_remap` (+ `extract(remap=)`) — a `(code,ch,vs)->coord|None` callback that remaps to KJV + concatenates same-coord collisions in source order (mirrors `extract_lxx_swete.build_verses`); `remap=None` byte-identical to before (PROVEN: kjv re-extract = identical). **Arabic (Van Dyck)** = KJV versification across all 66 books/1189 ch except 2 content-aligned tail-splits (1Ti 6:21+22→6:21; 3Jn 1:14+15→1:14) → `versification.arabic_to_kjv` + `scripts/extract_arabic_vandyke.py`; 66 bk / **31,102 v**. **JPS (1917)** — runbook's "Masoretic, reuse wlc map" REFUTED: eBible `engjps` is already KJV-renumbered (0 divergent ch / 929; total 23,145 = KJV OT count) → PURE IDENTITY ingest, no remap. Baked (`arabic`+`jps` in `_BAKED_NOW`): categorize-diff +31,100 `vnote-arabic` + 23,145 `vnote-jps`, **0 other-content mismatches** across 33,704 asides; `ebible verify` errors=0/24,015 paired; **epubcheck 0/0/0/0** (coptic+flagship); `tests/test_arabic_vandyke_ingest.py` 20 + `tests/test_jps_ingest.py` 9 + popup-version pin. **arabic surfaces in coptic-orthodox; jps not yet surfaced (user wiring decision pending).** NEXT = Douay+Vulgate (large; scope in `dev/IN_FLIGHT.md`). Detail: `dev/CHANGELOG.md` 2026-05-23.
- **PER-EDITION COVER FIX SHIPPED (autonomous backlog §4.0 item 2; visual-QA finding b, 2026-05-23).** Built EPUBs now ship each edition's declared `cover_image` instead of the master cover. NEW `build_edition.apply_edition_cover(edition, build_dir)` (reuses sandboxed `press_kit.resolve_cover_path`) swaps `cover.jpeg` after the copytree, mirroring the theme-override step; OPF cover-image property untouched (only bytes change). **Back-compat:** the 2 standalone bibles (`cover_image: ""`) keep the master (byte-identical). Verified end-to-end: catholic-study EPUB cover == `content/covers/catholic-study.jpg`, standalone-geez == master; catholic-study **epubcheck 0/0/0/0**; `tests/test_covers.py` (4 unit + 1 build-level); `lint_rules` 16/0/0. **Both visual-QA per-edition-differentiation findings now closed** (theme fixed 2026-05-22, cover today). Detail: `dev/CHANGELOG.md` 2026-05-23.
- **DEUTEROCANON LXX REORDER PASS COMPLETE — Judith + 1 Esdras + Manasseh + Azariah SHIPPED (Phase 2 deutero pass, 2026-05-23).** Greek popups on the final four deferred deutero books — **+849 verses** (LXX-Swete now **50 books / 26,126 verses**). All content-aligned vs the real Swete↔KJV (never identity-mapped). **Judith** (`_JDT_SEGMENTS`): ch1-14 identity; G15:14→KJV16:1 song-intro pull; ch16 offset +1 then catch-up merge G16:7+G16:8=KJV16:8. **1 Esdras** (`_1ES_SEGMENTS`): ch4/7/9 identity; ch1/2/3/5/6/8 Greek-fewer (KJV combines — 18 KJV verses correctly get no Greek); ch8 = 5 merges + 1 split (G8:49+G8:50=KJV8:50). **Manasseh** (cross-book `_cross_book`): **Swete Ode 8** — NOT the documented Rahlfs Ode 12 (which is the Nunc Dimittis here; caught by re-verifying the real source) — clean 15-verse identity → man 1. **Azariah/Song-of-Three** (cross-book `_cross_book`+`_PAZ_FROM_DAT3`): Theodotion Dat 3:24-90 → paz 1; prayer offset −23; G3:52 combines KJV 1:29+1:30; Benedicite reorders (angels/heavens swap + cold/frost/lightning permutation); G3:67/68 source-empty leave KJV 1:46/1:49 without Greek. **Byte-compat proven** (46 existing books byte-identical post-ruff-format); popup content-compare confirms only the 4 books' asides changed (jdt 339 + 1es 430 + man 15 + paz 65 gained Greek, 0 added/removed, neighbors unchanged). `ebible verify` errors=0 / 24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; **333 LXX tests**. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **TOBIT LXX (Swete) SHIPPED (Phase 2 deutero pass, 2026-05-23).** Greek popups on all of Tobit (short recension `Tob`) — **+242 verses** (LXX-Swete now **46 books / 25,277 v**). ch1-5+8-14 identity; **ch6** offset −1 (Greek 6:1 "she ceased weeping" concatenated onto KJV 5:22, then 6:2-18→6:1-17); **ch7** multi-divergence content-verified (KJV 7:8 = G7:8+G7:9 concat; G7:10 = the Greek-merge of KJV 7:9+7:10 → 7:9; KJV 7:12 absent; G7:12-17→7:13-18) — KJV 7:10/7:12 get no Greek (correctness). Categorize-diff: tob pure-additions (index_split_026, 0 del). `ebible verify` errors=0/24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; 239 LXX tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **SIRACH LXX (Swete) SHIPPED — the full Greek transposition (Phase 2 deutero pass, 2026-05-23).** Greek popups on all of Sirach — **+1,388 verses** (LXX-Swete now **45 books / 25,035 verses**). The famous Greek **30:25–36:16a block transposition** + internal verse-merges, ALL derived by content-aligning the real Swete↔KJV text (9 anchors verified: grape-gatherer G30:25=KJV33:16, G31→34, G32→35, G33→36:1-11, G34→31, G35→32, G36:1-15→33:1-15, G36:17-31→36:12-26; conflated seam G36:16 omitted; ch20/23/41 identity — litany verified 1:1). **Completeness fix:** `build_verses` now **concatenates** LXX-split verse-halves (14 cases incl. Baruch 3:34) so popups show the WHOLE verse, not a truncated clause — this also retroactively improved committed Baruch 3:34. Categorize-diff: Sirach pure-additions (037/038, 0 del); bar 1-line concat. `ebible verify` errors=0/24,015 paired; flagship + catholic-study **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; 223 LXX tests. **Deferred (reorder tables pending, §4.0):** Judith, Tobit, 1 Esdras, Manasseh, Azariah, aes. Detail: `dev/CHANGELOG.md` 2026-05-23. **NOTE: re-touches committed bar.py + epub_working/044 (concatenation).**
- **Deuterocanon LXX (Swete) — 5 verification-correct books SHIPPED (Phase 2 spine, deutero pass, 2026-05-23).** Greek popups added to **Wisdom, Susanna (Theodotion Sut), Bel & the Dragon (Theodotion Bet), Baruch, Letter of Jeremiah** — `vnote-greek` **+754 verses** (LXX-Swete now **44 books / 23,647 verses**, was 39 / 22,893). Each mapping was INTERIOR-verified against the real Greek↔KJV text (not just endpoints): wis/sus/bel exact identity; **bar** one verified ch3 split (KJV 3:34 = Greek 3:34+3:35); **lje** one verified head split (KJV 1:1+1:2 = Greek G1, then +1). **Judith was DEFERRED on discovery** — ch16 matches 25=25 but hides a split+merge (KJV 16:1 = Greek 15:14) that identity would misplace; per-chapter count is necessary-not-sufficient. Categorize-diff: **+1508 insertions / 0 deletions**, confined to the 5 deutero base files; KJV/Hebrew/existing-Greek untouched; aside count unchanged (35,585). `ebible verify` errors=0 / 24,015 paired; flagship **epubcheck 0/0/0/0**; `lint_rules` 16/0/0; `tests/test_lxx_swete_ingest.py` 178. **Deferred (need verified reorder tables — measured divergences in `dev/PLAN_2026-05-21.md` §4.0):** Judith, Tobit, 1 Esdras, Sirach, Prayer of Manasseh, Prayer of Azariah, aes, 1 Enoch. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **Greek NT (Byzantine Majority Text) FULL ingest SHIPPED (Phase 2 spine, sub-phase 3, 2026-05-23).** Greek popups now on **all 27 NT books** — `vnote-greek-nt` **0 → 7,951** (categorize-diff: KJV + Hebrew + LXX-Greek all UNCHANGED, only greek-nt added). Source switched from the spec-locked Scrivener TR (which was romanized/unaccented) to the **Robinson-Pierpont Byzantine Majority Text** (accented Unicode, byztxt, PD/Unlicense) so NT Greek matches the accented LXX. NEW `scripts/core/versification.byzantine_to_kjv` (25 tests): identity for all 27 books (Byzantine = KJV-standard versification; single-verse omissions Luke 17:36 / Acts 8:37·15:34·24:7 gap-preserved) + the one Romans-doxology reorder (RP 14:24-26 → 16:25-27). Driver `scripts/extract_byzantine_nt.py` → `content/translations/byzantine-greek/` (7,953 verses = NT 7,957 − 4 omissions; 0 collisions / 0 out-of-extent; ¶ stripped). Registry `greek-nt` relabelled "Byzantine Majority Text" + added to `_BAKED_NOW`. `ebible verify` errors=0 / 24,015 paired; **all 11 editions epubcheck 0 fatals/0 errors** (build gate exit 0); `lint_rules` 16/0/0; 27 tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **LXX-Greek (Swete) FULL ingest SHIPPED (Phase 2 spine, sub-phase 2, 2026-05-23).** Greek popups now on **all 39 OT books** — `vnote-greek` asides **8,601 → 22,812** (every diff categorized: KJV + Hebrew UNCHANGED, only Greek changed = Brenton/harvest replaced by Swete + added to ~28 books). NEW `scripts/core/versification.lxx_swete_to_kjv` (137 mapping tests) maps Swete→canonical KJV: identity for most books (+ guard), with the structurally-reordered books remapped from **content-aligned real data** (NOT memory) — Psalms full renumber (merges/splits + the pervasive superscription offset; emitted count exact 2461=2461), Jeremiah's full OAN reorder, Theodotion Daniel's Additions (3:24-90 omit, the +3 ch4 offset), the 1 Kings 20↔21 swap, Proverbs' 24/29 reorder, Esther's 6 Additions omitted; Exodus 36-39 (tabernacle) deferred (reordered+abbreviated, omitted not guessed). Driver `scripts/extract_lxx_swete.py` → `content/translations/lxx-swete-greek/` (22,893 verses, 0 collisions, 0 out-of-extent; sigla/`[n]` markers stripped). Registry repointed `lxx-greek`→`lxx-swete-greek` (Brenton seed dormant). `ebible verify` errors=0 / 24,015 paired; **all 11 editions epubcheck errors=0/warnings=0** (built with the Swete Greek); `lint_rules` 16/0/0; 140 tests. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **WLC Hebrew FULL ingest shipped (τ.5-A.x / Phase 2 spine, 2026-05-23, TDD).** Per the fully-customizable-builder roadmap. `scripts/extract_wlc_morphhb.py` + `scripts/core/versification.py` (NEW; 42 tests in `tests/test_wlc_ingest.py`): morphhb OSIS → `<em>`-per-word + KJV-versification remap via morphhb `VerseMap.xml`. `content/translations/wlc/` seed (3 verses) → **full 39 books / 23,142 verses** (canonical KJV coords). Verse-popup Hebrew **8,710 → 23,142**; every changed base verse categorized = **0 corruption** (14,432 added · 1,547 te'amim/normalize · 175 versification corrections · 29 marker drops · 4 special-letter fixes incl. the **Shema** bug found+fixed by characterization · 0 removed). `coord_in_canonical_extent` 0 out-of-extent; `ebible verify` errors=0 / 24,015 paired; flagship **epubcheck 0/0/0/0**; `lint_rules` 16/0/0. WLC source = OpenScriptures morphhb (gitignored `_acquire/morphhb/`). **NEXT = Phase 2 cont.:** LXX-Greek (Brenton) → Greek-NT, then Douay/JPS/Vulgate/Arabic. Detail: `dev/CHANGELOG.md` 2026-05-23.
- **Popup multi-version MODEL refactor (B1 / Phase 1) shipped (2026-05-22, TDD; ZERO output change).** Per the fully-customizable-builder roadmap (`docs/superpowers/specs/2026-05-22-fully-customizable-builder-roadmap.md`). NEW `scripts/core/popup_versions.py` = single-source popup-version registry (kjv/wlc/lxx-greek bake now; greek-nt/brenton-en/douay/jps/vulgate/arabic await Phase-2 data via the `bakes_now` gate); legacy aliases (english→kjv/…), `is_trusted_html` (original-language raw passthrough), `normalize_coord` versification seam. `build_vnote_aside` list-based; `build_edition.POPUP_LANGUAGES` superset (legacy parallel-bible slots preserved) + content-class stripper. **Byte-identical:** full regen ALL books → 0 diff (35,585 asides); flagship epubcheck 0/0/0/0; 138 popup tests; build smoke 31; lint 16/0/0. **NEXT = Phase 2:** acquire+ingest full PD translations (WLC/LXX/Greek-NT spine first) per the roadmap's locked sources, flipping each version's `bakes_now` as data lands; then Phase 3 (per-book version-selection UI), Phase 4 (per-note curation + source review). CSS for new classes deferred to Phase 2.
- **Per-edition THEMES shipped (2026-05-22, TDD).** Assigned `theme:` to all 11 editions in `content/editions.yaml` (was: none set → all defaulted to the no-op `classic`, identical stylesheet everywhere). Builds now emit **4 distinct stylesheet groups** — `classic` (ethiopian/anglican/geez/amharic) · `scholarly` (scholarly-academic/jewish/lutheran) · `devotional` (catholic/eastern-orthodox/coptic) · `modern` (evangelical-reformed). `tests/test_themes.py` (+3); all 11 rebuild clean; **epubcheck 0/0/0/0** on the 4 reps; `validate_schemas` + `lint_rules` 16/0/0 clean. Fixes visual-QA finding (a). **This is Phase 0 of the themes+multi-translation-popups arc** — spec/plan in `docs/superpowers/`; next build = Plan B1 (popup multi-version model refactor). No master-HTML/corpus change.
- **All 11 editions epubcheck-verified + Track D ruff lint cleanup (2026-05-22).** Rebuilt all 11 editions from HEAD (`build_edition.py --all --force --no-parallel`) and ran epubcheck (Java 8 + PyPI-bundled jar) on each → **every one 0/0/0/0** (closes "5 canon shapes → 11 editions"; the 3 never-individually-checked — scholarly-academic / coptic-orthodox / lutheran-confessional — confirmed clean). Track D ruff debt **1071 → 646** in verified batches: 265 semantically-null autofixes (UP045/F541/…); reviewed F401 (the `web.py` + `templates/*` re-export regressions were caught by the full suite and repaired, then **marked clean — `__all__` on 17 templates + PEP-484 redundant-alias on web.py**); F821 → 0 incl. a **real `check_a11y.py` missing-`Counter` bug** fixed. Then **Tiers 1+2** cleared 135 more (mechanical UP035/SIM114/B011/B007/E731 + bug-hunt B023/B904/F841/B905 — **bug-hunt found NO real product bug, all benign**) → ruff **1071 → 511**. **Tier 4** (config) then per-file-ignored E501 in `scripts/templates/*` (inherent embedded-HTML strings) → ruff **511 → 243**; CONSERVATIVE scope (C901/SIM105/N8xx/19 non-template-E501 kept VISIBLE as honest backlog, not masked). **Session ruff total: 1071 → 243** across 4 commits; the 243 residual is genuine low-priority backlog. `ruff format` clean · `lint_rules` 16/0/0 · full `pytest tests/` **6462 passed / 0 failed**. No content/master-HTML change. See `dev/CHANGELOG.md` 2026-05-22.
- **OPF BISAC fix → ALL editions epubcheck-clean (2026-05-22).** After the footnote fix, catholic-study + anglican-bcp still had 2 RSC-005 OPF errors: `patch_opf` emitted a BISAC `dc:subject` with `property="authority"` but no paired `property="term"` (EPUB3 requires both). Fixed `patch_opf` to emit the `term` meta. catholic + anglican now epubcheck **0/0/0/0**; omega0 patch_opf 32 + build-smoke 31 green. TDD `tests/test_canon_splice.py::test_patch_opf_bisac_subject_pairs_authority_and_term`. **Every edition (all 5 canon shapes) is now epubcheck-clean.**
- **CANON-SPLICED EDITIONS: footnotes cleaned (2026-05-22).** Smaller-canon editions (Reformed/Catholic/Tanakh/Orthodox) had ~2,373 orphaned note-ref markers (RSC-012): `build_edition.filter_books_for_canon`'s `_BOOK_SEGMENT_RE` let a dropped LAST book's segment swallow the shared per-file notes-section (+ verse-refs-section), removing KEPT books' asides while their markers survived. Fixed: segments now stop at those shared containers (kept asides preserved) + new Pass 3 drops dropped-books' orphaned asides. Verified all 4 canon shapes 0 RSC-012: reformed **2,373→0** + epubcheck 0/0/0/0, jewish 0/0/0/0, orthodox 0/0/0/0, catholic 0 RSC-012. TDD `tests/test_canon_splice.py`; `TestCanonFilter` 12 + build-smoke 31 green. **FLAGGED:** catholic-study/anglican-bcp emit 2 separate RSC-005 OPF `dc:subject`/authority errors (BISAC metadata, unrelated to footnotes — addressed next).
- **FLAGSHIP EPUB epubcheck-CLEAN — 0/0/0/0 (2026-05-22).** Java installed → first real `epubcheck` run surfaced 50 pre-existing validity errors beyond well-formedness (25 RSC-005 illegal nested `<p>` + 25 RSC-012 broken xref links; non-visual, never caught before — no Java). Fixed both: `inject.build_aside` uses a `<div>` flow container when the body carries block content (inline-only bodies keep `<p>`, byte-identical; CSS mirrors `.note-comm > div > .note-label`); normalized **667** xref link targets across 48 note files (`php/jas/ezk/nam/jol` → canonical `phi/jam/eze/nah/joe`) + `fix_xref_targets --apply` (resolved=1245, **unresolved=0**; clears the ~190 absent-Ezekiel xref residual). Regenerated master HTML (0/61 malformed), rebuilt flagship → **`epubcheck` EPUB-3.3: 0 fatals / 0 errors / 0 warnings / 0 infos**. 9 inject-wellformed + 17 marker-glyph + 19 verse-popup + `lint_rules` 16/0/0 green. Detail: `dev/CHANGELOG.md` 2026-05-22.
- **MALFORMED-XHTML INJECT BUG FIXED + master HTML regenerated (2026-05-22).** A flagship-EPUB structural validation for the [USER] e-reader check found the master HTML was **58/61 files malformed XML** (1,322+ split `<aside>`/marker tags) — every built EPUB would fail epubcheck + strict EPUB3 readers. Hidden because `ebible verify`/`audit` B7 check marker↔aside **pairing**, not XML well-formedness. Git-bisected to the first inject (`7a1aecf`); base `5ee2ad1` was clean. Root cause: 3 unsafe insertion offsets in `scripts/inject.py` — (1) `find_aside_insertion_point` `existing_ch!=ch` landed at `m.end()` (mid opening-tag); (2) `find_chapter_region_b` ended at the next chapter's `id=` attribute (inside its `<a>`) + ran to EOF for the last chapter; (3) the verse-end fallback in `find_marker_insertion_point` used the raw region edge. All now land at safe tag/`</body>` boundaries. Regenerated `epub_working/` from clean base (inject[fixed]→`generate_verse_popups`→`resync_marker_glyphs`): **0/61 malformed**, **feature-identical** to `ffa20c7` (note markers 67,555 · vn-links 36,556 · vnote asides 36,535 · `<aside>` 104,151 — all delta 0); `audit` B7 **24,015/24,015 paired** + B8 **61 well-formed**. Rebuilt flagship EPUB validates PASS (0 orphan markers, was 512). Guards: `tests/test_inject_wellformed.py` (7) + new `audit.py` **B8** well-formed-XML check (so `ebible verify` catches the class). Detail: `dev/CHANGELOG.md` 2026-05-22.
- **End-of-session audit + flake fix (2026-05-22).** Swept clean (git clean; `ebible verify` errors=0; `trace_matrix` 0; `lint_rules` 16/0/0; build smoke 31; marker-glyph/verse-popup/validate-schemas all green). Made the long-documented `test_build_all_route_serves_json` flake deterministic (mock `api_export_build` through the `/api/build-all` route, no real socket build) → full `test_scripts.py` green. Repo ready for a fresh session; IN_FLIGHT idle.
- **Marker tooltip fix (2026-05-22).** `html_title_for` made data-driven (returns the kind's `title_attr` from kinds.yaml; was hardcoded → "Note" default for the 10 newer categories); `resync_titles` (in `scripts/resync_marker_glyphs.py`) rewrote **66,437** marker tooltips. 17 marker tests; `ebible verify` errors=0; build smoke green; idempotent.
- **Symbol-system fix (2026-05-22).** `inject.glyph_for` was a hardcoded 5-glyph prefix map ignoring the `symbol` field in `kinds.yaml`/`categories.yaml` → only 5 of 15 category symbols rendered (11 kinds collapsed to `◇`; notably 26,335 `topic-nave` as `◇` not `✦`, and `hist-*` as `◇` not `⌂`). Made `glyph_for` data-driven (reads the kind's symbol) + new re-runnable `scripts/resync_marker_glyphs.py` rewrote **54,005** in-place glyphs (inline markers + note-backs). **All 15 category symbols now render** (`✦` topic 26,335; `◇` comm corrected to 2,690; the 10 scaffold categories show their glyphs; `comm-ai` `Ⓐ` resolves). 11 tests; `ebible verify` errors=0; build smoke green; idempotent. Deferred: `html_title_for` tooltips (same hardcoded shape).
- **Verse-popup regeneration SHIPPED (2026-05-22).** Closed the demo-critical gap: `scripts/generate_verse_popups.py` (+ `tests/test_verse_popups.py`, 19 tests) regenerated verse popups across all KJV-covered books — coverage **24% → 90.5%** (9,689 → 36,556 verses; 77 books ~100%). KJV English floor + harvest-merge-preserved Hebrew/Greek (the 11 originally-wrapped books kept their he/gr); old Gen 1:1/1:2 offset fixed; idempotent; `ebible verify` errors=0; build smoke + 19 popup tests green. 9 TDD tasks committed through `70ac7d3`. Deferred: full he/gr/Greek-NT dataset acquisition + Ge'ez popups for the 7 no-KJV books (Meqabyan I-III, 2 Enoch, Jubilees, 4 Baruch, 1 Clement). Spec/plan in `docs/superpowers/`.
- **EPUB-QA followups (2026-05-22, committed).** `build_epub.py` drops `onix/` at the packaging chokepoint (`EXCLUDE_DIR_NAMES`) — the CC0 give-away carries no commercial ONIX (completes Ω.0 de-commercialization at the package layer). `build_edition._retitle_html_pages` rewrites calibre's `<title>Converted Ebook</title>` → edition title (XML-escaped, idempotent) in `build_one`. Marker-density QA on the maximized flagship: mean **1.018** markers/verse, 46% of verses bare, only 156 (0.39%) >6, max 19 (Gen 1:2); dict-easton ⌂ = 9.2% of markers → **no readability regression**. TDD `TestOnixExcludedFromPackage` + `TestPerFilePageTitle`; 31/31 build-smoke + ruff + `lint_rules` 16·0·0. **FLAGGED:** verse-popup wrapper covers only ~24% of verses (book-level — 2Sa yes / 1Ki no); confirm intended-vs-gap before the demo.
- **Matrix maximization (2026-05-22, committed `b905e22`).** `editions.yaml`: `scholarly-academic` += `topic` category (now reaches full corpus, enabled 41,374→67,709 — `topic-nave`'s 26,335 were orphaned in 0 editions); `ethiopian-tewahedo` `max_phase mvp→phase2` (ships its declared `dist-typological`/`dist-mariological` + `dict-easton`, 37,495→41,285; no topical flood — flagship lacks `topic`). TDD `tests/test_enabled_kinds_unified.py::TestMatrixMaximization`. Also IN_FLIGHT→idle, ν.8 logged, scope-addenda index. 762 edition/matrix tests pass · trace_matrix 0 · build smoke valid.
- **Test suite GREEN** — triaged + fixed all 92 post-rebuild failures. Most were phase pins reading the trimmed `SESSION_STATE.md`/`IN_FLIGHT.md` + the moved `PLAN_2026-05-09.md` (→ `dev/archive/`); fixed at root cause via the new `tests.fixtures.assert_phase_recorded` chokepoint (reads CHANGELOG) + `lint_rules.check_no_ephemeral_doc_pins` guard. Plus `dict-easton` count refresh + 13 pre-existing code fixes. **6404 passed / 0 logic failures** (1 pre-existing flaky HTTP-socket test, `test_build_all_route_serves_json`, passes in isolation).
- **Base-HTML recovered + committed** (`5ee2ad1`) — the lost WEB scripture HTML; `ebible build` makes valid EPUBs again.
- **Inject-tail spill resolver** (`d3acc4f`) — `inject.find_verse_region_b_spill` resolves Strategy-B chapters whose verses spill across a split-file boundary (jer/psa/isa/1ch). `scripts/audit_base_html.py` classifies regular vs irregular layout. 99.21% → 99.48%.
- **Reference-corpus rebuild + Easton's** (`38b80d3`): Nave's Topical rebuilt clean from a CCEL PDF (`extract_naves_ccel.py` → 26,335 `topic-nave`, was 15,258 OCR-noisy); Easton's Bible Dictionary ingested (`extract_eastons_ccel.py` → 3,779 `dict-easton`, new kind under `hist`). `promote.batch_insert_notes` (one write/book). Corpus 52,859 → 67,715.
- **Coord guard + dead-dispatch repoint** (`a935701`): `canonical_verse_counts.coord_in_canonical_extent` rejects out-of-extent coordinates at the promote boundary (0 invalid-coordinate notes corpus-wide); `run.py`/`add_note.py` injector dispatch repointed from the lost `source_archive/`+`kings_session/` scripts to `scripts/inject.py`.

## Next (per dev/PLAN_2026-05-24-end-scope.md, toward the 2026-06-07 deadline)

> **ACTIVE TRACK = fully-customizable-builder roadmap** (`docs/superpowers/specs/2026-05-22-fully-customizable-builder-roadmap.md`): Phase 0 themes ✓ · Phase 1 popup-model B1 ✓ · Phase 2 translation spine — **WLC ✓ · LXX-Greek Swete OT ✓ · Greek-NT Byzantine ✓ · deuterocanon LXX COMPLETE ✓ · Arabic (Van Dyck, 66 bk/31,102 v) ✓ · JPS (1917, 39 bk/23,145 v) ✓ (committed `f6d90c6`)** → **Douay + Vulgate IN PROGRESS** (`c1bce0c`; shared `vulgate_to_kjv` — NT + all prophets + Genesis segments verified SHIFTS=0, recension omits tob/jdt/sir; **dormant, NOT yet baked — bake is all-or-nothing**; ~50-80 alignments remain — **RESUME per the D/V grind map at the top of `dev/IN_FLIGHT.md`**) · then Phase 3 (per-book version-selection UI) + Phase 4 (per-note curation). **Availability: all 9 non-geez/amharic editions can now PICK any/all/none of the baked translations in `/customize` before building (fixed the stale `has_data` flag → data-driven via `bakes_now`); defaults left per-tradition (user "decide later").** The four PLAN_2026-05-21 tracks below remain valid but secondary.

> **Matrix audit 2026-05-22 — CLEAN.** `trace_matrix` 0 unresolved refs (all 11 editions resolve) · `validate_taxonomy` 100% attributed (67,713) · `ebible verify` errors=0 / 24,015 paired · `lint_rules` 16/0/0 · `ruff format` clean. No integrity fixes needed.

1. **DEMO (north star) — visual QA BROWSER-PASS DONE 2026-05-22; findings → `dev/VISUAL_QA_CHECKLIST.md` "Findings".** Rendered all 5 representative editions (unzip → localhost → Playwright): canon ×5 / canonical TOC order / verse popups (Gen 1:1 = KJV+Heb+Greek; John 3:16 + Psalm 1:1 = KJV) / note glyphs (scholarly shows ✦ topical, flagship correctly lacks it) / OPF `urn:` id (no ISBN) — all VERIFIED; sole console error = favicon 404. **2 TDD findings (configured-but-not-applied; neither breaks epubcheck):** (a) per-edition THEME — **FIXED 2026-05-22** (themes assigned to all 11 → 4 distinct stylesheet groups; epubcheck 0/0/0/0; `tests/test_themes.py`); (b) per-edition COVER — **FIXED 2026-05-23** (`build_edition.apply_edition_cover` swaps in each edition's `cover_image`; 9/11 editions now ship their curated cover, the 2 standalone bibles keep the master; `tests/test_covers.py`; epubcheck 0/0/0/0). Both per-edition-differentiation findings now closed. **Remaining demo item = the [USER] e-reader DEVICE check** (tap-popup overlay / page-turns / e-ink) — the only non-self-verifiable part. **Base-HTML coverage is COMPLETE** — all 87 books / 1,702 chapters present (`audit_base_html.py --coverage`; NO book truncated). The ~156-161-note inject residual is purely **verse-level versification** (aes/1en/mq/sir/jub) — NOT mechanically addable. Map: `dev/MATRIX_MAP.md` → "Base-HTML structure & coverage".
2. **MANUSCRIPT MARATHON — PAUSED** (the other major track): 1ki5 R2 spec-review held; then 1ki6 / 1sa2 (Kings/Samuel Geʽez dual-witness collation). HEAVY → script-based, single-chapter, OOM-aware (memory `feedback_concurrent_agent_cap`), check-in cadence (`feedback_marathon_pacing`).
3. **CORPUS EXPANSION — scope-capped 2026-05-24** (memory `corpus-reference-expansion`): bounded to FIVE named works still IN scope (Matthew Henry / JFB / Barnes / Torrey's New Topical Textbook / Vincent's Word Studies — Henry seeded χ.2, Torrey sourced on disk, the rest PD/CCEL via the χ-cluster pipeline; NOT blocked on the user). De-scoped: anything beyond those five + Voyage AI embeddings.
4. **CLEANUP / UPGRADES** — see PLAN_2026-05-24-end-scope.md (and dev/archive/PLAN_2026-05-21.md for Track B/C detail: editorial note-key review, matrix vestigial-layering refactor, etc.).

## Conventions (unchanged)

Local commit only via `save.cmd`/`save.ps1` (PowerShell; no remote — deleted 2026-05-12). Tests: full interpreter path + `$env:PYTHONUTF8="1"`, one file at a time. "continue" ≠ "save". Scope frozen 2026-05-20 (consolidation phase).

## Console inventory (web.py UI surface, 18)

`APIHELP_HTML` · `AUDIT_HTML` · `AUDIT_LOG_HTML` · `BUILD_TRACKER_HTML` · `COMPARE_HTML` · `COVERS_HTML` · `CUSTOMIZE_HTML` · `DIFF_HTML` · `EXEC_HTML` · `EXPORT_HTML` · `GREEK_HTML` · `HEBREW_HTML` · `MATRIX_HTML` · `OPS_HTML` · `PREFLIGHT_HTML` · `PUBLISHER_HTML` · `SOURCES_HTML` · `WIZARD_HTML` (note editor served at `/` is `INDEX_HTML`). Each cross-links to the others (lint_rules check 6.2).
