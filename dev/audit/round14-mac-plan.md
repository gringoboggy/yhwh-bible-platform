# Round-14 deep-audit — Mac survivors + fixes plan

Engine `deep-audit.js LANE=mac ROUND=14` (`wf_61e196d1-2f2`, 38 agents, opus finders + adversarial skeptic panels). **13 deduped findings -> 8 verified survivors / 5 refuted.** By severity: {'high': 2, 'medium': 2, 'low': 4}. Full survivor records (evidence + verifier-corrected fixes) in `round14-mac-survivors.json`. ALL 8 survivors touch `scripts/**` / `tests/**` => **WIN-lane fixes** (Mac is findings-only this round; writes confined to `dev/audit/`). The 5 refuted are below the plan.

> ★ The 2 HIGHs are propagation-lens catches (program logic fault -> silent epub defect):
> (1) `_merge_mid_verse_breaks` displacement-blindness silently relocates **est 10:2's first word ('Aren't') into est 10:1** on the shipped eink flagship — real scripture corruption, no gate catches it (G1 is KJV-only; `audit_verse_formatting.py` endorses the bad merge via the same blind detector).
> (2) the only real-build byte-stability gate never builds the **tablet/kindle** byte-stable cells -> directly motivates the round's headline **G1 KJV golden-hash gate** (must cover all 9 cells incl. tablet/kindle, baselined from POST-re-split per the WS1 byte-proof).

---

## Executive summary

Round 14 surfaced **13 deduped findings → 8 verified survivors / 5 refuted** (0 survivors are UNVERIFIED; every survivor cleared its skeptic panel). By severity the survivors are **{high: 2, medium: 2, low: 4}** — all 8 are bug/correctness/byte-stability/data-validity/debt; **0 optimization** recommendations survived. The most serious is the eink mid-verse merge corrupting **est 10:2** ("Aren't" relocated into est 10:1) on the *shipped* Ethiopian eink flagship, with no gate to catch it — the round's other high finding (the missing G1 KJV golden-hash gate) is the closing infrastructure that would catch the whole class of eink/tablet/kindle gating leaks and uniform re-baselines. Overall codebase health is strong: the two mediums are the *same* one-line cache-coverage miss (already RED in an existing AST guard), and all four lows are localized (a None-deref crash in a non-build CLI, a latent defense-in-depth hole that is currently clean on disk, and a dormant /customize-only attribution strip). Note: the 8 survivors include two duplicate pairs (eink_glyphs cache × 2, prospect None-deref × 2) folded below into single actionable items.

## Phased fixes

Ordered safest/most-foundational first: additive + guard-adding land before behavior-changing; the HIGH silent-data-corruption fix is sequenced last because it is behavior-changing and needs a regression pin, but it is the highest execution priority.

### Phase 1 — Foundational byte-stability guards (additive; zero EPUB output-byte change)

- [ ] **[MEDIUM] eink_glyphs.py omitted from `_PIPELINE_SCRIPTS` → stale cached eink/kepub EPUB** — `scripts/core/build_cache.py:62-142` (folds findings #3 + #4, same defect).
  - **Fix (corrected):** one line only — add `"core/eink_glyphs.py",` to `_PIPELINE_SCRIPTS`, placed with the K-R13b-era `core/` modules (next to `"core/vnote_separators.py"` ~line 141) with a one-line comment noting `EINK_CATEGORY_BADGE_GLYPHS` is baked into the eink legend/chips/glossary/note-sym and no data hash covers it. This auto-covers both the content key (`compute_cache_key` part 9, build_cache.py:405-411 hashes `scripts/<name>`) and `is_output_current`'s mtime list (build_edition.py:6915). Do **not** add it to `TestCacheCoverageGuard.WAIVED` (the glyph table genuinely shapes eink output).
  - **Test/guard:** none to write — `tests/test_build_cache.py::TestCacheCoverageGuard.test_every_build_path_core_module_is_covered_or_waived` (AST transitive-closure scan, ~lines 1018-1102) is **already RED on exactly this module**; the one line turns it green. Do NOT re-build the finder's proposed dynamic-discovery guard — it already exists. Confirm with `.venv/bin/python -m pytest tests/test_build_cache.py::TestCacheCoverageGuard -q`. Optionally add `"core/eink_glyphs.py"` to the positive-pin `required` tuple for sibling symmetry.
  - **Build path:** YES — changes the cache key once (one conservative rebuild per edition) but **no output bytes change**; 9 KJV editions stay byte-stable. Byte-stability proof: regen + `git diff` shows no EPUB delta.

- [ ] **[HIGH] No real-build golden gate — G1 must build the 3×3 KJV target variants with stored hashes** — `tests/test_byte_stability_gate.py:16-18, 33, 46, 73-100`.
  - **Fix (corrected):** Add `tests/test_kjv_golden_hash_gate.py` + `tests/golden/kjv_golden_hashes.json`. Build the byte-stable KJV set across its documented target variants via `apply_target_override`/`build_one`'s `target_reader`, normalize **only** the volatile OPF fields (URN, `dcterms:modified`, `dc:date`, rights-year — reuse `_content_digest`'s regexes), and assert each variant against a STORED hash — this is the only thing that catches a uniform re-baseline AND an eink/tablet/kindle gating leak (the existing determinism check is blind to both). Two corrections to the finder: (1) do **not** delete `test_byte_stability_gate.py` — keep its flagship `ethiopian-tewahedo` determinism + canon-distinctness assertions and let the golden gate coexist; (2) capture initial goldens on the canonical build lane and cross-lane verify once before committing (the prior-round `ZipInfo.create_system=0` + fixed `dc:date` make Mac↔Win byte-identity plausible, but confirm reproducible to avoid a benign-platform red). Keep the matrix to documented byte-stable targets (`everywhere` + non-eink variants); do **not** golden the eink target against `everywhere` (eink intentionally mutates bytes). Mark the module `slow`.
  - **Test/guard:** the new gate IS the guard (round headline deliverable). Additive only; no engine or marathon-core change.
  - **Build path:** test-only, but exercises real builds — the goldens must be lane-verified before commit.

### Phase 2 — Additive crash & data-integrity guards (non-build-path or byte-identical output)

- [ ] **[LOW] `prospect.py main()` dereferences a None `out_path` → AttributeError aborts `--all-chapters`** — `scripts/prospect.py:335-344` (folds findings #5 + #6, same defect).
  - **Fix:** add a None guard before the `out_path.relative_to(REPO_ROOT)` at line 338, mirroring the six at-scale sibling drivers' `if out:` pattern. After the `verses==0` check insert `if stats["out_path"] is None:` → print a "nothing new / queue already complete" line (include `verses`/`deduped` counts) and `continue`; keep the `rel = ...relative_to(...)` only on the write path. Restores the documented exit-0 "queue already complete" behavior (fires on every re-run of an already-prospected chapter and on any all-deduped chapter, common in the mature corpus).
  - **Test/guard:** regression test re-running prospect on an already-queued chapter (verses>0, nothing new) asserting exit 0, no crash.
  - **Build path:** NO — `prospect.py` is an ingest CLI, not in the EPUB pipeline; no byte-stability impact.

- [ ] **[LOW] Canonical-extent guard is a silent no-op for all 8 Tewahedo-distinctive books** — `scripts/core/canonical_verse_counts.py:191-192, 244`.
  - **Fix (corrected):** keep the finder's intent (consult existing verse tables + `books.yaml` `ch_count` instead of the keep-all `return True`/`0`), but do **not** import the 3,700-line `scripts/extract_parallel_pdf.py` OCR CLI into the core leaf. Instead: (1) relocate the six `MQ1/MQ2/MQ3/FOUR_BARUCH/JUBILEES/ONE_ENOCH _VERSE_COUNTS` dicts into a new dependency-free leaf `scripts/core/distinctive_verse_counts.py` and re-import them back into `extract_parallel_pdf.py` (byte-identical there). (2) In `coord_in_canonical_extent`, before the `if not sets: return True`, look up a `{book:{chapter:max_verse}}` extent map and validate as a per-chapter **ceiling** + chapter membership (`chapter in map and 1 <= verse <= map[chapter]`) — a ceiling, not set-membership (the dicts are `{chapter:count}`). (3) For `1cl`/`2en` (no verse dicts) enforce only the `books.yaml` `ch_count` chapter ceiling (lazy-load `books.yaml` inside the fn). (4) `html_chapter_count`: return `max(chapter keys)` for the six, `books.yaml` `ch_count` for `1cl`/`2en`, else fall through to 0. (5) Reserve `return True`/`0` for genuinely unknown/test codes only.
  - **Test/guard:** regression test asserting impossible coords now return False (`1en 999:99`, `jub 500:1`, `mq1 200:1`) AND every existing on-disk distinctive note coord still validates True (proves byte-identical output). Because this is an **ingest-recurring invariant**, also wire a commit-time `lint_rules` check (over a pytest-only guard) that no on-disk distinctive coord exceeds its table/`ch_count` ceiling.
  - **Build path:** promote/emit guard, but current on-disk data is clean (0 over-extent coords verified) → Ethiopian build is byte-identical; 9 KJV editions unaffected. Byte-stability proof: regen + `git diff`.

### Phase 3 — Behavior-changing fixes (require regression pins + byte-stability proof)

- [ ] **[LOW] S1 note-dedup silently drops dict source attribution (and category restatements) when S2 group-by-category is off** — `scripts/build_edition.py:4213-4219` (helpers 2974, 2997-3012).
  - **Fix:** gate the source/category-restating strips on `s2_group`. Pass `s2_group` into `_strip_redundant_note_label`/`_strip_redundant_body_boilerplate` (or guard the 4213-4219 call site) so the dict-* leaf-label strip (2974) and **all four** `_strip_redundant_body_boilerplate` cases (dict/topic/xref/text-witness at 3000/3003/3006/3009) run only when the cascade byline/head will actually re-surface the removed text. Leave the kind-default-label strip (trigger a) and the comm-ethiopian self-attribution strip (trigger b) unconditional — they stay safe under S1-alone. `note_attribution_dedup`(S1) and `note_group_by_category`(S2) are independent /customize checkboxes with no coupling validation, so S1-on/S2-off is reachable.
  - **Test/guard:** regression test building an edition with `note_attribution_dedup=true` and `note_group_by_category=false`, asserting a dict-* note still renders its PD source string.
  - **Build path:** YES, but dormant in the 4 shipped study editions (all set S1+S2+S3a together) → byte-identical for shipped editions. Prove via regen + `git diff` on `catholic-study`.

- [ ] **[HIGH] Eink mid-verse merge corrupts est 10:2 — verse's first word "Aren't" relocated into est 10:1 on the shipped flagship** — `scripts/build_edition.py:5645-5659` (heuristic 5650; corrupting move 5653-5658). **Highest execution priority.**
  - **Fix (corrected):** make `_merge_mid_verse_breaks` displacement-aware using the **canonical WEB base text** (`content/translations/sources/web/eng-web_vpl.txt`, book-code-mapped `est`→`EST`) — NOT the popup aside (a different/KJV-style translation, proven by `vnote-est-10-2 = "And all the acts..."` ≠ `"Aren't all the acts..."`). Before the relocation at 5654: derive the alphabetic word-sequence of `lead` and **SKIP** the merge (leave the benign un-merged break) when that sequence is a PREFIX of the current verse's WEB text AND NOT a SUFFIX of the prior verse's WEB text. Current/prior verse ids are already in hand (`mk`'s anchor `v-est-10-2` + the prior block's last v-anchor). This discriminator separates all 26 sites: est 10:2 → skip (fix); `1co 9:2` and `gen 48:2` → still merge. The "prior verse ends in terminal punctuation" and "lead is short / starts with Aren't" shortcuts both FAIL (gen 48:2, 1co 9:2 disprove them) — the WEB-source comparison is the only reliable discriminator. Keep the eink gating (call site 5700) so the 9 KJV editions stay byte-identical (additive, eink-only). Mirror the same guard in `dev/audit_verse_formatting.py` so it stops reporting est 10:2 as a `mid_verse_break` ERROR.
  - **Test/guard:** regression test pinning **est 10:2 must NOT merge** and **1co 9:2 must STILL merge** (counter-example). Extending a text-integrity golden gate to the eink flagship is desirable but secondary (eink full build is currently OOM-blocked) — the displacement guard + the est/1co pins are the load-bearing fix.
  - **Build path:** YES, eink-only. Byte-stability proof: regen the 9 KJV editions → byte-identical (eink gating untouched); regen the Ethiopian eink flagship → est 10:2 reads "Aren't all the acts...", est 10:1 ends "...islands of the sea."

## Optimization decisions

No optimization recommendations survived this round (0/0). The settled/de-scoped decisions are reaffirmed as confirmed-optimal:

| Area | Verdict | Recommendation |
|---|---|---|
| zip `compresslevel` 9→6 | confirmed-optimal | KEEP 9 — enlarges every EPUB 1-3% to save ~30s/build; quality output > build speed (declined on merits). |
| Split `scripts/web.py` / `scripts/build_edition.py` for size | confirmed-optimal | KEEP — large files of small cohesive functions; not a defect. |
| Data model (Python tuples, no DB) | confirmed-optimal | KEEP — data-as-Python-tuples is deliberate; no database. |
| Public-server hardening (CSRF, rate-limit, multi-tenant auth) | confirmed-optimal (out of scope) | NONE — single-user LOCAL desktop app. |
| Voyage embeddings integration | confirmed-optimal (dropped) | NONE — only key-rotation security survives; **no paid-API** dependency reintroduced. |
| Marathon core (`build_standalone.py`, `core/manuscript_*`, `core/po_vision_store.py`, `content/manuscript/**`, patrologia, `GAPS/`) | off-limits | NO edits — read-only; flagging is itself out of scope absent an outright crash. |
| eink full-build OOM | WIN's active fix | This audit verifies build feasibility + frees only; do NOT re-diagnose the OOM site. |

## Constraints carried

- **Never touch the marathon core** — `scripts/build_standalone.py`, `scripts/core/manuscript_*.py`, `scripts/core/po_vision_store.py`, `content/manuscript/**`, `content/translations/sources/patrologia/**`, `GAPS/` are read-only; none of the 8 fixes touch them (the canonical-extent fix relocates the six distinctive-count dicts out of `extract_parallel_pdf.py`, which is an OCR CLI, not marathon core).
- **9 KJV editions must stay byte-stable** — every build-path fix (Phase 1 cache key, Phase 3 S1 strip, Phase 3 est 10:2 merge) is additive/eink-or-customize-gated and carries a `regen + git diff` byte-identity proof obligation for the KJV set before commit.
- **Additive schema only** — byte-identical when unset; writes go through `notes_io.atomic_write` / `ensure_backup`.
- **No paid API** — Voyage integration stays dropped; no new external dependency.
- **Prefer commit-time `lint_rules` over pytest-only guards for ingest-recurring invariants** — applied to the canonical-extent ceiling check (Phase 2); the eink_glyphs coverage guard already exists as an AST closure test (no new guard needed).
- **Bandwidth-first save cadence (RULES §4)** — each fix lands as a LOCAL COMMIT; the full 5-leg push happens only at a milestone.
- **FINDINGS-ONLY** — this is the plan; **STOP before applying any fix** (user marching order 2026-06-08). Do not edit, do not build, do not push.

---

## Refuted (adversarially killed — do NOT re-open without NEW evidence)

- **(file-split)** rewrite_links idmap-miss fallback is silent and only NOTEREF cross-file links are gated — a dangling non-noteref cross-file link would ship undetected (e-reader-only) — refuted 1/1. Read scripts/build_edition.py apply_file_split (5677-5879), rewrite_links (5809-5833), _merge_scripture_base_files (5462-5563), and dev/verify_kr2_build.py gate-2 (540-619).

(a) The code partly does what's claimed: rewrite_links._full does `idmap.get(frag, filemap.get(orig, orig))` (5815) and _bare
- **(eink-gating-leak)** resolve_note_popup_split_byte_cap does not self-gate to eink (unlike its sibling eink resolvers) — byte-cap split isolation rests on one call-site gate — refuted 1/1. I read the resolver (scripts/build_edition.py:3421-3436), grepped every use of split_byte_cap/resolve_note_popup_split_byte_cap, read the sole call site (4060), the consumer (4354), two cited siblings (2287, 2308), the char-cap resolver (3337-3355), and the API validator (scripts/api/editions.py:966
- **(eink-gating-leak)** Eink popup-separator/glyph isolation in the badge-cascade chain is purely thread-dependent and the everywhere+kindle KJV variants run the same split/cascade path — one dropped eink= kwarg silently re-baselines KJV bytes with no gate — refuted 2/2. I read all cited regions in scripts/build_edition.py. The code facts are accurate: line 4056 split_cap is NOT eink-gated; 4060 split_byte_cap IS; line 3348 returns DEFAULT_NOTE_POPUP_SPLIT_CAP (4400) for non-tablet; all 4 call sites (4190/4281/4313/4354) thread eink= and every emitter defaults eink=
- **(glossary-streaming)** Glossary byte-stream splitter diverges from the str splitter in the single-piece case (byte-length short-circuit + whitespace-intro drop); contradicts its byte-identity contract — refuted 1/1. I read scripts/build_edition.py:5060-5360 (the helpers, both splitters, the dispatcher) and tests/test_file_split.py:1030-1119, plus grepped every caller and the production constants.

(a) The code does what the evidence claims. Byte path short-circuits on BYTE length (`if len(raw) <= target` at 525
- **(cascade-dedup)** badges_skipped (the P2 'must be 0' orphan invariant) is recorded but never enforced — a marker/aside desync silently ships raw inline note-ref markers — refuted 1/1. I read the cited regions and the surrounding machinery. The literal behavior is accurate: build_edition.py:4184-4188 bails (ok=False; break) when the aside regex misses, and 4223-4230 does stats["badges_skipped"] += 1; continue with no edit (markers left un-collapsed), recorded only at 8304 — no ass


## Completeness-critic gaps (seeds for the next round)

- **Distribution / release / packaging pipeline (release.py, gen_checksums.py, merge_release_checksums.py, gen_release_catalog.py, core/distribution.py, core/press_kit.py, swap_epub_cover.py, build_kindle.py, core/kindle_post.py — ~2,504 LOC) was not a dimension at all, despite the prompt naming a 'dist-packaging dim' and 'stale-artifact ship is a real defect'.** — None of the 12 covered dimensions touched the packaging surface. SESSION_STATE itself records two live defects this round would catch: the v0.1.0 release still attaches EPUBs for two RETIRED notes-onl _Lens:_ Drive gen_release_catalog + gen_checksums + merge_release_checksums for a tag and assert: (a) every attached release asset has exactly one SHA256SUMS line and there are no orphan lines; (b) the catalo
- **Cross-reference (xref) subsystem: apply_kjv_xref.py, link_xrefs.py (426), fix_xref_targets.py (364), check_xrefs.py (309), core/geez_kjv_xref.py (429) — writes xref links into the canonical notes and renders them as in-book links, but is in NO covered dimension and wired into NO gate.** — grep of scripts/ci.py, .github/, .gitlab-ci.yml, Makefile for check_xrefs/link_xrefs/fix_xref_targets returns empty — dangling xref targets ship unguarded as dead links. After canon-splice + file-spli _Lens:_ Run check_xrefs against each BUILT edition and assert every rendered '#vnote-'/xref target resolves to an emitted anchor in the SAME spine piece (cross-file misses are the danger post-file-split). Sep
- **versification.py (1,492 LOC) cross-versification fold/merge segment tables — per-chapter Douay/Vulgate↔KJV verse re-slotting tuples (psa/isa/sir/dan/etc.). The data-validity dimension surfaced only the canonical_verse_counts Tewahedo no-op and never validated these tuples against real verse data.** — A single wrong segment tuple silently re-slots or DROPS a verse's content into a neighbor — the identical corruption class the correctness dim found at est 10:2 ('Aren't' merged into the prior verse), _Lens:_ For each fold/merge segment, cross-check against the verses actually present in content/translations/<douay|vulgate>/<book>.py: assert the segment ranges tile [1, last_verse] with no gap/overlap, ever
- **Combinatorial /customize edition-flag interaction space. The surviving S1 finding (note dedup drops dict attribution when note_group_by_category is OFF) is one bad pairwise combo; the flag cross-product was not swept.** — web_editions.py surfaces note_group_by_category × reader_eink_verse_lines × reader_eink_study_inline × reader_eink_study_layout(backmatter|inline|popup) × popup_translation × time_filter as user-reach _Lens:_ Enumerate the cross-product of /customize-reachable boolean+enum flags from web_editions.py:417-479, build a minimal fixture edition per combo, build it, and assert no note attribution/category/popup-
- **glossary-streaming was verified byte-identical only on catholic-study (453/453), NOT on the ethiopian flagship — the one edition whose ~480MB glossary actually motivated the streaming refactor and whose full eink build still OOMs at a post-glossary site.** — _iter_study_glossary_pieces_from_file (build_edition.py:5333), _patch_study_glossary_nav (:5392) and the boundary-piece merge / cross-piece id stitching (:5710-5759, last_name = plan_names[a][-1]) rea _Lens:_ Feed the FLAGSHIP glossary document specifically into both split_study_glossary_document (in-memory list form) and the disk-streamed _iter_study_glossary_pieces_from_file path and assert piece names, 
- **cross-module dimension returned 0/0 — the corpus_index ⇄ data-tuple ⇄ matrix book-count bake chain was not exercised. matrix.py has TWO edition_canon_books implementations (_compute_matrix_via_file_walk and corpus_index.compute_matrix_indexed, matrix.py:101-102) that must agree, and one is baked into the matter pages.** — corpus_index.py (1,557 LOC) builds a derived SQLite index from the Python-tuple source-of-truth; compute_matrix delegates the per-edition book count to it, and that count is printed into copyright/you _Lens:_ Assert _compute_matrix_via_file_walk().edition_canon_books == corpus_index.compute_matrix_indexed() for every edition canon (the two must be identical post-fold); recount book/note counts straight fro
- **Migration DEFINITIONS (scripts/migrations/0001_migrate_to_user_data.py, 0002_backfill_traditions.py) were not audited — round-11 fixed only the migration RUNNER (tri-state/argv/ledger/atomicity).** — A non-idempotent migration corrupts user data on re-run. Both 'migrate' and 'migrations' are WAIVED in the build-cache coverage guard with the reason 'derived index … never baked' (test_build_cache.py _Lens:_ Read each scripts/migrations/000*.py for re-run safety (guards already-applied state, writes through notes_io.atomic_write, idempotent) and assert none mutate content/notes/** or content/translations/
- **Canonical book/chapter ORDER in the emitted EPUB nav.xhtml + content.opf spine (RULES S6), including the known-unconfirmed 1en misordering — only the WEB UI order is tested.** — test_web_editions_canonical_order asserts the books.yaml payload the UI reads (test docstring: 'single source of truth the UI reads'), NOT the artifact. SESSION_STATE records '1 1en misordering to con _Lens:_ Parse the built nav.xhtml and content.opf spine for each edition and assert book+chapter order equals config canonical order with folds applied; specifically confirm 1en's chapter sequence end-to-end 
- **Kepub-specific artifact integrity beyond the kobtoSpan dup-id waiver — the K-R6-2 bare -sN rev-id family (guard #19) is an OPEN real defect class, and the eink-gating-leak dimension (1/3) only looked at CSS/markup leaking the other direction.** — Guard #19 explicitly says 792 bare rev ids + 36k+ prefixes were observed on the v0.1.0 .kepub and 'triage artifact-age vs gate vs real regression before re-flagging' — that triage was NOT done this ro _Lens:_ Build a fresh .kepub for ethiopian-tewahedo and re-count bare -sN rev ids vs proper -sN-family ids on the freshly-built artifact (not the aged v0.1.0 asset) to settle guard #19 as artifact-age vs live
