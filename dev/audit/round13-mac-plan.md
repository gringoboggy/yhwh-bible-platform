# Round-13 GRAND AUDIT — Mac half (deep-audit `LANE=mac`, 18 model-bound dims)

`Workflow wf_d8b42907-01b` · 33 survived / 12 refuted (of 45 deduped) · bySeverity {"medium": 5, "low": 17, "info": 11} · Opus-forced · adversarially verified · scope=product · round=13 · 2026-06-23.
One verify stage (`verify:opt-vision`) hit a transient API drop — its finding(s) carried per the empty-panel-is-not-a-refutation rule (check `unverified` flags).

Companion data: `round13-mac-survivors.json` (full findings + verifier panels + dropped + completeness gaps). Merge target: `round13-remediation.md` (WIN-owned). WIN owns scripts/+tests/ fixes; Mac owns dev/ + verifies.

---

## Executive summary

This round-13 deep audit produced **45 deduped findings → 33 verified survivors / 12 refuted** (0 of the survivors are UNVERIFIED — their skeptic panels were empty even after retry, so they need manual triage rather than auto-confirmation). By severity the survivors break down as **5 medium · 17 low · 11 info**; by class, **23 are bug/correctness/security/debt/test/doc** and **10 are optimization** confirmations. The most serious cluster is the **Play/non-kindle reader-sim gate gap** (4 mediums): a kindle-collapsed `dc:language` artifact was staged and labelled `everywhere`, and every Play "PASS" was run against the wrong EPUB because no gate asserts the multi-language fingerprint — a verification-integrity hole, not a shipped-output break. The other two mediums are a real concurrency race in `corpus_index.dashboard_stats` (use-after-close on the shared SQLite handle) and the M4b study-glossary stranding 371 Jubilees/Meqabyan reader return-links. Overall codebase health is **strong**: zero high/critical, no defect touches the 9 byte-stable KJV editions, and every fix is additive, gate-hardening, or dev-prose — consistent with a project at a shipped v0.1.0 milestone.

## Phased fixes

### Phase 0 — Zero-risk hygiene (dev-prose + dead-code + tracked-stray; no engine, no test)
These cannot affect any build output; land first to clear noise.

- [ ] **[info] Delete orphaned shim `resolve_reader_eink_study_inline()`** — `scripts/build_edition.py:2317-2319`. Remove the 3-line dead shim (zero callers in scripts/tests/dev); keep the live `resolve_reader_eink_study_layout` and the back-compat bool field it reads at 2312. Leave exactly one blank line before `_CASCADE_CATEGORY_HUES` so `ruff format` passes. **Build path:** definition-only deletion, no caller → byte-stable by construction (rebuild flagship digest to confirm).
- [ ] **[low] Remove tracked stray `content/notes/gen.py.pre-amplify`** — `content/notes/gen.py.pre-amplify`. `git rm` the 297 KB pre-amplify backup; runtime-inert (all loaders `glob("*.py")`), superseded by the live `gen.py`, real backups live in gitignored `content/notes/.backups/`. **Guard:** add the optional fail-soft `lint_rules.py` notes-dir check (fail if `content/notes/` holds a file that is neither `*.py` nor `.backups/`) so future strays are caught at commit time. **Build path:** none (glob excludes it) → byte-stable.
- [ ] **[low] Fix REPO_MAP.md stale scripts/ counts** — `dev/REPO_MAP.md:40,45-48`. Recount: `scripts/*.py` 136→142, `scripts/core/` 68→71, `scripts/templates/` 22→21 consoles, `_book_defaults/` 66→87; leave api count (18). Prefer `~N` literals (mirror MATRIX_MAP deferring totals to SESSION_STATE) since neither `trace_repo.py` nor `repo_map_complete` validates per-dir counts. **Build path:** none.
- [ ] **[low] Fix RULES §7 dead pointer** — `dev/CLAUDE_PROJECT_RULES.md:715-718`. Change `scripts/_dedup_ethiopian_notes.py` → `dev/archive/_dedup_ethiopian_notes.py` (moved in `b01a9c19`) and delete the now-false clause ", tracked in the SESSION_STATE inventory". **Build path:** none.
- [ ] **[low] Fix docs/superpowers/INDEX.md header count** — `docs/superpowers/INDEX.md:7`. `89 documents — 59 plans` → `90 documents — 60 plans · 30 specs` (whatever live `ls` gives at fix time). **Guard:** optionally extend `check_superpowers_coherence` to assert header count == `len(files)`. **Build path:** none.
- [ ] **[info] Fix MATRIX_MAP finding #1 stale docstring claim** — `dev/MATRIX_MAP.md:135-136`. Replace "68 kinds / 91,597 notes / 6 editions" with the live docstring state: 72 kinds / 15 categories / 6 editions, no pinned note total (defers to `trace_matrix.py`). **Build path:** none.
- [ ] **[low] Fix MAC build-doc cd-path** — `dev/MAC_DESKTOP_BUILD_AND_NOTARIZE.txt:5-7`. Change `cd .../YHWH\ v2.4` → repo root `/Volumes/MacHD2/yhwh-bible-platform` (no such subdir; build self-resolves so operator-misdirection only). Folds into the IN_FLIGHT task-2 path/count reconciliation. **Build path:** none.

### Phase 1 — Additive guards + tests (pin existing-correct behavior before any behavior change)
These add coverage/lints with no production-code change.

- [ ] **[low] Pin byte-stability determinism on the per-push gate** — `tests/test_byte_stability_gate.py:30,73-100`. The only real-edition determinism gate is `slow`-tagged and runs only in the schedule/manual GitLab `tests-full` job (`allow_failure: true`) → never blocks a push. Add ONE per-push, non-slow check in `tests/test_build_smoke.py`: build the cheapest canon-filtered edition twice (cache monkeypatched off) and assert matching `_content_digest`. Lift `_content_digest` into `tests/conftest.py`/`tests/_epub_digest.py` so the slow gate and the fast check share it (no production change); keep the multi-edition + flagship sweep slow-tagged. Dropping GitLab `allow_failure` is a weaker fallback (per-push GitLab is disabled by `workflow:rules`) — prefer the GitHub variant. **Build path:** reads but never mutates output → byte-stable.
- [ ] **[low] Cover the 2 untested SyntaxError loader branches** — `scripts/core/translations.py:84-96,104-117`. Three byte-identical `except SyntaxError: return None` loaders exist; only `notes_io.load_notes_from_text` is behaviorally pinned. Add to `tests/test_build_standalone.py::TestVersificationAttr`: `assert tx.load_book_verses_from_text('VERSES = [(  # broken') is None` and `assert tx._load_book_attr_from_text('VERSIFICATION = (  # broken', 'VERSIFICATION') is None`. Test-only. **Build path:** none.
- [ ] **[medium→guard] Add the non-kindle multi-language gate assertion (closes the whole class)** — `scripts/reader_sim.py:159-165`, `dev/verify_kr2_build.py:132`. Root cause of all 4 Play mediums: the kindle direction is gated (`opf.count("<dc:language>") != 1`) but the inverse (non-kindle ⇒ ≥2) exists nowhere, so a collapsed `everywhere`/apple/kobo artifact passes green. Add ONE shared helper `dc_language_checks(opf)` in `dev/verify_kr2_build.py` that holds BOTH directions so they cannot drift: kindle-stamped ⇒ exactly 1; not-kindle ⇒ ≥2 (block-present, **not** `==6`, so a future per-edition/standalone-geez language config never false-fails). Wire it into `verify_kr2_build.main()`; `reader_sim.gate_reader` for `apple/kobo/play` inherits it via `_verify_kr2` (no reader_sim change). Add a unit test mirroring `test_opf_clean.py`: collapsed-to-1 non-kindle fails, 6-value non-kindle passes, 1-value kindle-stamped passes. **Build path:** verifier-only → byte-stable. *(This is the single code fix that resolves finding-cluster 4/19/20/22; the staging re-stage below is the operational complement.)*
- [ ] **[medium] Re-stage the mislabeled Play QA artifact + void prior PASSes** — `build/reader-sim/play/YHWH-ethiopian-tewahedo-v0.1.0-everywhere-navy.epub`, `dev/reader_sim/STAGING_MANIFEST.md:9`, `dev/reader_sim/play/qa-checklist.md`. The staged file is a kindle-collapsed strip (1 `dc:language`, 0 hides) masquerading as `everywhere`; the verified GitHub v0.1.0 release asset of the same name (27,527,967 B) correctly has 6 langs + 7 hides. Delete the stale file; re-stage from the release asset OR `python3 scripts/reader_sim.py --build play` (→ `--target-reader everywhere`, no kindle strip). Gate before any sim: `unzip -p <epub> content.opf | grep -c '<dc:language>'` == 6. Treat the turn-127 Thorium PASS and the `LANE_HANDOFF_LOG` PASSes (1568/1694) as void; re-run the sim. **Build path:** `build/` is gitignored, operational only → 9 KJV editions byte-stable. *(Deduped with finding 19 — same artifact, one action.)*

### Phase 2 — Security / silent-data-loss (containment + faithful round-trip; high priority, narrow blast radius)

- [ ] **[low/security] Cover-image path-traversal — close the Windows drive-letter escape (whole class, 4 sites)** — `scripts/web_covers.py` `_validate_cover_path`; resolvers at `scripts/core/covers.py:62-71,329-354,510-533`; build sinks `build_edition.py:7071,6964`. **Minimal required:** in `_validate_cover_path`, after the `norm.startswith('/')` reject, add the drive-letter reject mirroring `safe_path._check_string_safety:78` — `if len(norm) >= 2 and norm[1] == ':': return "cover path must be relative to content/, not absolute"`. Both `cover_image` and each `book_covers` entry already flow through this validator. **Optional defense-in-depth** (any-entry hardening regardless of how editions.yaml was edited; relative shipped paths unaffected → byte-stable): wrap `covers.resolve_cover_path`, `covers.read_image_meta`, and `build_edition._resolve_book_art` in `safe_path.resolve_under(content_root, ...)` under `except SafePathError → return None` (treat absolute input as rejection). **Do NOT** add a per-`book_covers` validate step (`editions.py:1225-1249` already does it). **Build path:** relative paths unchanged → rebuild flagship to confirm 9 KJV byte-identical.
- [ ] **[low/correctness] web editor `write_book` silently drops the documented `NOTES_<CODE>` alias from 80/83 notes files** — `scripts/web_helpers.py:156-198`. The full re-serialization writer emits only docstring + `NOTES=[...]`, stripping the `NOTES_1CH = NOTES  # backward-compat alias` member that `content/notes/__init__.py`'s contract documents — first note-editor save of any of those 80 books drops it (the other two writers, `promote.*` and `attribute.py`, preserve it). **Fix:** DETECT-AND-PRESERVE — while parsing the tree for the docstring, scan `tree.body` for an `ast.Assign` whose single target is an `ast.Name` starting `"NOTES_"` (≠ `NOTES_DIR`) with value `ast.Name id=="NOTES"`; capture `alias_name`; after the closing `]` append `f"\n{alias_name} = NOTES  # backward-compat alias\n"` **only when present**. Do NOT synthesize `f"NOTES_{book_code.upper()}"` unconditionally — the 7 alias-less files (1ki/1sa/2ki/2sa/ezr/gen/rut) must not gain one. **Test:** round-trip `psa.py` → asserts `NOTES_PSA = NOTES` survives; round-trip `gen.py` → asserts no alias introduced. **Build path:** `write_book` is web-editor-only, never in the KJV EPUB build → byte-stable.

### Phase 3 — Concurrency + latent correctness (behavior-changing; one-line, scoped, byte-stable)

- [ ] **[medium/concurrency] `corpus_index.dashboard_stats` second query escapes the `_read_cursor()` lock — gap-4 use-after-close race** — `scripts/core/corpus_index.py:1487-1514`. The second `conn.execute(...)` (line 1509) runs AFTER the `with _read_cursor() as conn:` block exits, so `_CONN_LOCK` is no longer held; a concurrent `invalidate()`/`rebuild()` closing `_CACHED_CONN` (triggered by a notes-file save on another worker thread under `ThreadingHTTPServer`) between the line-1494 fetch and the line-1509 execute → `sqlite3.ProgrammingError: Cannot operate on a closed database`, 500-ing `/dashboard`. Verified the ONLY escaping site of 11 `_read_cursor`/query sites. **Fix:** move the `chap_rows` query inside the existing `with` block (both `placeholders` and `book_codes` are in scope); leave the two processing loops (1496-1505, 1516-1522) outside — they operate on materialized `.fetchall()` lists and don't touch `conn`. Matches every other helper in the module. **Guard:** extend the `tests/test_corpus_index_delta.py` dashboard_stats equivalence pin to assert both result sets complete. **Build path:** none (web console path) → byte-stable.
- [ ] **[low/correctness] `translations.get_chapter()` TypeError on mixed int/str verse keys — incomplete round-11 gap-2 fix** — `scripts/core/translations.py:233-242`. `out.sort(key=lambda p: p[0])` raises `TypeError: '<' not supported between str and int` on `VERSIFICATION=="own"` stores with lettered verses ("2a","B1"); `get_chapter` crashes BEFORE `api_compare`'s safe `verse_sort_key` alignment runs → generic 500 on `/api/compare`. **Fix:** replace line 241 with `out.sort(key=lambda p: verse_sort_key(p[0]))` (already in-module; byte-identical for pure-int chapters). One-liner covers ALL THREE callers: `web_content.py:115` (api_compare), `web_content.py:279` (api_sample_html), `core/preview.py:362` (preview). **De-mask the test:** add a `tests/test_compare_own_versification.py` case calling the REAL `get_chapter` (drop the `monkeypatch.setattr(tx,"get_chapter",...)`) against a chapter mixing an int and "2a"/"B1", asserting `(1,2,"2a",3)` order, not a raise. **Priority low:** scanned all 8 own-vers stores (34,346 verses) — zero string verses today; land before the τ.G standalone ingest introduces them. **Build path:** byte-identical for canonical int chapters → 9 KJV byte-stable.

### Phase 4 — Platform / build-path behavior changes (gated, additive, byte-stability proof obligation)
Each touches the build pipeline → MUST rebuild + diff to prove the 9 KJV editions byte-identical (changes are reader-target-gated, so KJV/`everywhere` paths should be untouched).

- [ ] **[medium/kindle] M4b study-glossary strands 371 Jubilees/Meqabyan return-links** — `scripts/core/kindle_post.py:402-409,420`. `_glossary_back_link` builds the return only from `v-{book}-{ch}-{verse}`; the 4 Ge'ez-only canon books (jub 192, mq1 84, mq2 50, mq3 45 = 371) have no scripture verse anchors → anchor-less `<strong>`-only fallback, and `_prepare_glossary_aside` first strips the aside's ORIGINAL valid `#vbadge-{book}-{ch}-{verse}-{seg}` back-href. **Fix:** in `_apply_kindle_m4b_members`, during the same scripture scan that builds `v_anchor_files` (L546-558), also collect `vbadge_files: dict[id→spine]` by scanning `id="vbadge-..."` (survives clean, e.g. `vbadge-jub-24-3-s1`). Pass into `_glossary_back_link`; when the `v-` anchor is missing, fall back to `{vbadge_spine}#vbadge-...`, deriving the vbadge id from the `aid` string directly (swap `vnotes-`→`vbadge-`; do NOT change `_VNOTES_COORD_RE`). **Gate:** in `verify_kindle_m4b_glossary_html`, after the fix assert EVERY `<div class="study-glossary-entry">` carries a `vn-back` `<a>` (fail on `<strong>`-only); fail loudly if neither a `v-` nor a `vbadge` target is found. m4b-only confinement. **Build path:** kindle-m4b path only, no KJV/tablet path → byte-stable (rebuild ethiopian m4b + run gate).
- [ ] **[low/kindle] M4b glossary hardcodes zip-root content layout; test masks it with substring-only asserts** — `scripts/core/kindle_post.py:580-616,502-507,402-409`. `_apply_kindle_m4b_members` writes bare member names + bare hrefs; under an `OEBPS/` layout the glossary lands at zip-root while hrefs resolve to `OEBPS/...` → RSC-001 / dead badge links. Latent (shipped editions use flat-root). **GATE FIX (do this):** in `verify_kindle_m4b` (719-742), resolve each badge href + glossary manifest href against the OPF dir (`posixpath.normpath(join(opf_dir, h.split('#')[0]))`) and `m4b-7`-fail if the member isn't in `names`; mirror in `tests/test_kindle_m4b.py` (replace substring asserts with resolve-and-exists). **CODE-PATH FIX (optional, only if a non-root layout ever ships):** make members location-aware — hoist OPF discovery ABOVE the piece-writing loop, use `prefix = posixpath.dirname(opf_name or '')`, store `f'{prefix}/{piece_name}'` only when prefix truthy and `!= '.'`, keep all href VALUES bare. Gated as a strict no-op for flat-root → byte-stable. **Build path:** kindle-m4b only.
- [ ] **[low/apple] Tablet/Apple OPF omits `ibooks:specified-fonts=true` → Apple Books may ignore embedded Cardo / Noto Serif Ethiopic** — `scripts/build_edition.py:1634-1857 (patch_opf)`. Per Apple Books Asset Guide, embedded fonts in a flowing book require `specified-fonts=true`; fonts ship in the manifest but the flag is absent (`grep "specified-fonts|ibooks:"` → 0). **Fix (both edits together, gated to `resolve_target_reader(edition)=="tablet"`):** (1) add the registered ibooks vocab to `<package>` via regex appending `prefix="ibooks: http://vocabulary.itunes.apple.com/rdf/ibooks/vocabulary-extensions-1.0/"`; (2) inject `<meta property="ibooks:specified-fonts">true</meta>` into `<metadata>`. The meta WITHOUT the registered prefix trips epubcheck OPF-066/067 — must ship paired. Do not emit on non-tablet; do not add `com.apple.ibooks.display-options.xml`. **Device-proof:** rebuild tablet ethiopian superset, confirm in Apple Books on M2 that a Ge'ez popup + body render embedded, not fallback serif. **Build path:** tablet-scoped → the 9 KJV `everywhere` editions byte-identical (rebuild + diff to confirm).

### Phase 5 — Distribution / website (CI + sitemap; metadata-only, no build output)

- [ ] **[low/opt — recurring-ingest invariant] Attribute-escaping sweep: 3 sibling `title=`/aria emitters still use `html.escape(quote=False)`** — `scripts/generate_verse_popups.py:105`, `scripts/inject.py:242` (+ mirror `scripts/resync_marker_glyphs.py:183`). round13-#9 hardened only `inject.build_marker`; the same artifact now ships THREE escaping disciplines. quote=False does NOT escape `"`, so a future book title / category label containing a literal double-quote breaks out of `title="..."` → epubcheck RSC-005 (the exact failure round13-#9 prevented). **Fix:** apply `escape_attr()` (inject.py:169, preserves apostrophe → byte-stable on today's data) at all three attribute-title sites; sites (b)/(c) MUST change together (inject source + re-bake mirror) to stay byte-identical. Leave the element-content uses (`inject.py:241` safe_label, the `<strong>` body) on quote=False — those are correct. **Guard — prefer a commit-time `lint_rules.py` check over pytest** (this class recurs every ingest/voice-broadening wave): add a lint that flags any `title="..."`/`aria-*` interpolation built from `html.escape(..., quote=False)` in the emitters, so a new attribute emitter can't silently regress. Also extend `tests/test_resync_markers.py::TestTitleEscaping` with a verse-popup noteref title + a category label containing a literal `"`. **Build path:** byte-identical no-op on today's data (verified 0 titles/labels contain `"`) → rebuild + diff to confirm.
- [ ] **[low/dist] SHA256SUMS merge uses filename as a BRE in `grep -v` (latent false-delete; 2 sites)** — `.github/workflows/format-matrix.yml:148`, `.github/workflows/build-linux.yml:87`. Raw asset names interpolated into a Basic-Regex → each `.` matches any char; a future dot-aligned sibling (`...-v0X1X0.epub`) silently drops a checksum line. (`scripts/merge_release_checksums.py` is already safe — dict-keyed by exact name.) **Fix:** replace both `grep -v` with an exact-field `awk` literal compare: `awk -v n="$name" '{ i=index($0,"  "); if (substr($0,i+2) != n) print }'`. Do NOT use `grep -vF` (substring-prone). **Build path:** release-asset list, not build output → byte-stable.
- [ ] **[low/dist] `notary_autofinish.sh` regenerates `dist/SHA256SUMS.txt` from the Mac's dmg-only `dist/`** — `dev/notary_autofinish.sh:72`. Produces a deceptively-partial single-line sums file an operator could mistake for the release file and upload (wiping 5 families' checksums). **Fix:** write to `dist/SHA256SUMS.dmg-only.txt` (add `--ext .dmg`) and update the status line (≈L82) to point at `scripts/merge_release_checksums.py --tag <tag> --upload` and warn "do NOT upload this file directly". Mac-only. **Build path:** none.
- [ ] **[low/website] `how-to-use.html` (Guide nav page) missing from sitemap.xml — hand-kept PAGES list drifted** — `website/build.mjs:161-163`. The page is built, self-canonical, in global nav, inbound-linked from all 10 top-level pages, but `grep -c how-to-use sitemap.xml` == 0; the literal list will keep dropping future pages. **Fix:** derive the sitemap from `builtPages` — `builtPages.filter(p => !p.startsWith('read/') && p !== '404.html')`, map `/index.html`→`/`, drop pages whose front-matter `canonical` differs from their own URL (keeps beta.html/404.html out), then `.concat(readerUrls.sort())`. Minimal stopgap: add `'/how-to-use.html'` to the array. **Build path:** website only, not EPUB build → KJV byte-stable.

## Optimization decisions

| Area | Verdict | Recommendation |
|---|---|---|
| Attribute-escaping sweep (3 sibling `title=` emitters) | **change** | Promoted to Phase 5 fix — apply `escape_attr()` at all 3 sites + a commit-time lint (recurring-ingest invariant). |
| Tewahedo reverification / triaged-reingest spec | **confirmed-optimal** | Architecture still right under Opus 4.8 / 1M / Workflow. Keep the shipped binary `sequential` flag over building the §4.1(c) `vision_concurrency` profiles table; add the knob only if true N>1 is later measured-and-wanted. No-paid-API stands. |
| Native-TOC nested-navPoint evaluation | **confirmed-optimal** | Keep `reader_native_toc_chapters` default OFF (device-render fact, not a model-capability question). Only open action: fold the 30-s Kobo native-ToC datum into the existing HUMAN_DECISIONS color-Kobo row. Dev-doc nit: `build_edition.py:3732`→`5913` citation is stale. |
| Paid script-path vs agent-path vision transcription | **confirmed-optimal** | Agent path is the only no-cost route; HARD CONSTRAINT = no paid script-path API. Script-path stays committed-but-inert; do not install `anthropic` or spend a key. |
| MAX-1-heavy-agent local cap | **confirmed-optimal** | OOM driver is **physical RAM** (8 GB Mac / 16 GB N95), orthogonal to 1M context. Keep `sequential:true` for all local runs; RAM-safe parallel-blind-passes remain pod-only and the pod is user-DROPPED. |
| ≤1568px region crops (never whole-folio upscale) | **confirmed-optimal** | Hard property of the vision encoder, not a tunable; bigger crops only re-introduce OOM. No change. |
| Controller-renders / subagents-Read (image bytes out of controller) | **confirmed-optimal** | Structural OOM defense; **more** important under 1M context (resist the temptation to Read tiles into the controller). No change. |
| Ingest orchestration (detector→candidate→promote) | **confirmed-optimal** | Single unified driver is NOT a safe win; per-book parallelism is blocked by the shared per-chapter `append_candidates` read-modify-write (last-writer-wins). Only optional ergonomic win: a thin `run_ingest --then-promote` wrapper chaining existing `main()` + `promote_by_book()` (compose, don't recompute). No correctness/perf change required. |
| Render-coverage + standalone-build + EN back-translation lanes | **confirmed-optimal** | Verified against live data (canonical books byte-exact, calibration counts correct, alias maps additive). Optional polish only: dead `--json` flag, hoist `import yaml` out of a loop, collapse 3 alias maps — none change bytes. |
| `build_standalone` EN occurrence-map alignment (**off-limits core**) | **confirmed-optimal** | Provably correct for ALL current standalone data (gseq==eseq incl. the Ps 36 dup quartet). Do NOT edit the off-limits core. For the future τ.G wiring lane only: add an additive **external** preflight parity check (new guard/lint module) asserting base vs `-en` stores share identical source-order coord sequences, so a future asymmetric re-ingest fails loudly. |

## Constraints carried

- **Marathon core is OFF-LIMITS** — never edit `scripts/build_standalone.py`, `scripts/core/manuscript_*.py`, `scripts/core/po_vision_store.py`, `content/manuscript/**`, `content/translations/sources/patrologia/**`, `GAPS/`. The standalone parity check (opt table) is an **external** new module, never a core edit.
- **9 KJV editions MUST build byte-stable** — every Phase-4/5 fix is reader-target-gated (`tablet`/`kindle`-only) or metadata/dev-only; each build-path fix (cover containment, ibooks meta, m4b glossary, attribute-escaping) carries a **byte-stability proof obligation**: rebuild the flagship (and a canon-filtered edition) and `git diff` the normalized content digest to prove zero KJV output change before considering it done.
- **Additive schema only** — byte-identical when unset; all preserve-or-detect fixes (alias preservation, gate assertions) add nothing to defaults.
- **Atomic writes** — all notes-file writes continue through `notes_io.atomic_write` / `ensure_backup`.
- **No paid API / no database / keep Python** — vision stays on the no-cost agent path; data-as-Python-tuples retained; do not split `web.py`/`build_edition.py` for size.
- **Save cadence (RULES §4):** bandwidth-first — each fix lands as a **LOCAL COMMIT**; the full 5-leg sync happens **only at a milestone**, not per fix.
- **THIS AUDIT IS FINDINGS-ONLY.** Produce the plan, then **STOP** — do NOT apply any fix (user marching order, 2026-06-08). The phases above are the execution order for a *later* fix session, not actions to take now.
