# Round-16 — Mac-lane engine findings (engine-mac)

> **FINDINGS-ONLY round.** `truth_owner = windows` — WIN folds these into `dev/audit/round16-remediation.md` (dedup by `keyOf` = file::title). This file is the Mac lane's file-disjoint deliverable: the `engine-mac` survivors (adversarially verified by the engine's default-refuted panels) + the Mac cross-OS verify results. Do NOT apply fixes.

- Engine: `deep-audit.js` LANE='mac', ROUND=16, NOW=2026-06-27, depth=deep (agents=55, subagent_tokens=5178390, tool_calls=1324).
- Dimensions (11): correctness(2), security(2), cross-module(2), builder-robustness(2), cross-product(2), marker-logic-xreader(2), html-integrity(2), whitespace-pagebreak(1), display-redundancy(1), options-completeness(2), os-binary-parity(1).
- Counts: deduped=32 → **survived=20** (refuted=12); bySeverity={"medium": 2, "low": 18}.
- Ownership: **9 survivors touch `build_edition.py`/`epub_working/` → WIN-owned fix surface**; the rest are dev/core/templates.

## Mac cross-OS verify (side-work — program done-definition item 3)

- **G1 — 9-KJV golden byte-identity: ✅ 9/9** (`test_kjv_golden_hashes_match` PASSED on macOS, 2697s; the 9 cells {catholic-study, evangelical-reformed, eastern-orthodox} × {everywhere, tablet, kindle} byte-identical Win↔Mac).
- **G3 / G4 / G5 — ✅ PASS** on a fresh macOS catholic-study eink build (`test_round14_build_gates`, build OK 6:47; idmap / glossary / badge gates green; G3 floors `--min-links`/`--min-xrefs 10000` met).
- **G6 (`audit_canonical_order`) — ✅ PASS** (369 spine pieces / 1299 ch / 75 books / 3 appendix; order-clean).
- **`audit_output_hygiene` (3b) — ✅ PASS** on the macOS catholic-study eink (members=370; leak_hits=0, nested_a=0, empty_verse_anchors=0, empty_asides=0, midchapter_breaks=0; orphan_asides=22 = WARN, the known hidden-aes residual → non-fail). `--selftest` PASS (9 dirty / 0 clean).
- **Build-free source gates (3c) — ✅ PASS** on macOS: `tests/test_round16_source_gates.py` **5/5** (`audit_cross_product` + `audit_customize_completeness` + `audit_output_hygiene` selftests non-tautological; the `computer`-orphan + `verse_marker_glyph`-orphan detections fire).
- **⇒ MAC CROSS-OS VERIFY COMPLETE (program done-def #3):** 3a G1 9/9 ✓ · 3b G3/G4/G5/G6 + `audit_output_hygiene` ✓ · 3c build-free `_selftest` ✓. **Mac lane fully run.**

### Standalone build-inspect (Mac — round-16 catalog-coverage completion, 2026-06-27)

WIN's full-catalog sweep was killed mid-`standalone-geez` (remediation tracker §"Final WIN sweep state"), so the two standalones were the only un-inspected non-flagship catalog assets. Built + scanned both on macOS via the round-16 harness (`dev/round16_build_inspect.py --only standalone-{geez,amharic}:standalone`); the round-16 gate suite ran per asset (epubcheck + `audit_idmap_frags` G3 + `audit_badge_conservation` G4 + `audit_canonical_order` G6 + `audit_output_hygiene`). **Both assets CLEAN — `gate_fails=[]`, harness exit 0.**

| asset | size | epubcheck | G3 idmap | G4 badge | G6 canon-order | R16 hygiene |
|---|---|---|---|---|---|---|
| `standalone-geez` | 1.72 MB | 0/0/0/0 | PASS — 6 pcs, frag=165 (xref=165), noteref=2972, ncx=165, uids=6109, **0** dup/fail | clean (`is_badge_edition=0`) | clean (spine=6) | PASS — members=7, leak/nested/empty/midbreak **all 0**, book_breaks=3 |
| `standalone-amharic` | 1.55 MB | 0/0/0/0 | PASS — 3 pcs, frag=126 (xref=126), noteref=2243, ncx=126, uids=4612, **0** dup/fail | clean (`is_badge_edition=0`) | clean (spine=3) | PASS — members=4, leak/nested/empty/midbreak **all 0** |

**The 2 WARNs per asset are expected non-applicability (NOT findings)** — the same harness-applicability class WIN flagged for the kepubs:
- **badge:** standalones are not badge/study editions (`is_badge_edition=0`) → the `…stats.json` sidecar is absent and badge-conservation is vacuously clean. Correct by design.
- **canonical-order:** the standalone Ge'ez/Amharic Bibles do not carry the study `ch-bNN-cMM` chapter anchors the gate keys on → it reports "order not assertable" and passes (`chapters=0/books=0`). The standalone uses its own per-book chapter scheme; G6 is a study-edition gate. Correct by design — and corroborates the engine's "seed #3 standalone target degeneracy = not a defect" (remediation §"refuted seeds").

**Scope note (honest framing):** there is no Windows-side standalone result to byte-compare against (WIN's sweep died mid-geez), so this is round-16 **catalog-coverage completion**, not a Win↔Mac byte-identity proof. Standalone EPUBs also carry build-time OPF timestamps (`dc:date`/`dcterms:modified`) and are not in the 9-KJV byte-stable golden set, so byte-identity is neither expected nor required for them. Results JSON: `build/r16/standalone-{geez,amharic}-mac.json` (gitignored). ⇒ With this, **every round-16 non-flagship catalog asset is now build-inspected clean** (WIN's 18 + Mac's 2 standalones = 20); the only remaining inspect item is the flagship `ethiopian-tewahedo` eink (RAM ceiling → WIN-only by design).

### Cross-OS observations (LOW — for WIN)
- **(harness) `tests/test_round14_build_gates.py:58`** runs each gate via `subprocess.run([sys.executable, dev/<gate>.py, epub])` WITHOUT `env={**os.environ,"PYTHONPATH":REPO}` / `cwd=REPO`. On a clean macOS `python -m pytest` (no ambient PYTHONPATH) the `audit_canonical_order.py` subprocess's `from scripts.core import config` → `ModuleNotFoundError('scripts')`. WIN passes only via a globally-exported PYTHONPATH. Fix: pass PYTHONPATH/cwd so the gate is invocation-independent (the coming `test_round16_build_gates.py` should adopt the same). G3/G4/G5 don't import `scripts` at main scope → unaffected. _(This sibling also relates to survivors [9]–[12] / dim builder-robustness subprocess hygiene.)_
- **(build) catholic-study eink `spine_pieces=369`** on this round-16 build vs **307** on the round-15 board; chapters/books/appendix identical (1299/75/3), order-clean. Piece-count drift — sanity-check whether a file-split cap changed (no `build_edition.py` change expected this round).

### Dedup note
- Survivors **[7] and [8] are the same defect** (cross-module `bookcode_canonical` lint omits `patristic_commentaries.json`, `lint_rules.py:2276-2282`) surfaced by two finders with slightly different titles → escaped the engine `keyOf` dedup. Merge to one on WIN's reconcile. The completeness-critic additionally flags the WHOLE 6-store enumeration class (see below).

## Survivors — summary

| # | dim | sev | owner | file:line | title |
|---|-----|-----|-------|-----------|-------|
| 1 | cross-product | medium (was high) | WIN | `scripts/build_edition.py:2015-2017, 1021, 1364, 1736, 4673; cf. scripts/build_format_matrix.py:205-220` | Kindle target: desktop /customize build path produces the retired E999-failing variant (make_kindle_safe never applied) — declared 'Send-to-Kindle' != built |
| 2 | marker-logic-xreader | medium | WIN | `scripts/build_edition.py:3749-3761 (decision at 3757)` | eink study-glossary return link teleports to chapter start (not the verse) for the 13 Strategy-B books that DO carry v-anchors (psa/job/kings/chronicles/...) - over-broad strategy keying, a turn-135 regression |
| 3 | correctness | low (was medium) | — | `scripts/core/edition_stats.py:50-79` | edition_stats cache key (_edition_signature) omits the 3 popup-language-cap fields -> stale popup-language set baked into the EPUB About page and shown in the editor after a runtime target_reader / max_popup_languages / popup_languages_capped edit |
| 4 | correctness | low | — | `scripts/core/at_scale_base.py:307-311, 452-458` | At-scale driver progress stat over-counts written candidates (ignores append_candidates' dedup) — both shared sites |
| 5 | security | low | — | `scripts/templates/index.py:315 (also 218); server read path scripts/web_notes.py:83` | Note-editor render boundary loads stored note bodies via innerHTML with no sanitization — only the SAVE path is gated, the READ/render path is not (boundary-doctrine gap) |
| 6 | security | low (was medium) | — | `scripts/core/http.py:167-172 (get); 232-240 (put)` | SSRF allowlist in http.py is bypassed by HTTP redirects (initial URL checked, redirect target not re-validated) |
| 7 | cross-module | low | — | `scripts/lint_rules.py:2276-2282` | bookcode_canonical lint omits patristic_commentaries.json — 5 of 6 identical commentary stores screened |
| 8 | cross-module | low | — | `scripts/lint_rules.py:2276-2282` | bookcode_canonical lint omits the 6th commentary store (patristic_commentaries.json) — guard now incomplete for a detector-feeding source |
| 9 | builder-robustness | low (was medium) | WIN | `scripts/build_edition.py:4783-4793, 5092-5116, 5191-5236, 5271-5278, 5386-5393` | Residual ~3x-copy RAM peak in the study-glossary slice splitter + a 2x transient in _decode_nl (offset-based slice fix recommended, not done) |
| 10 | builder-robustness | low (was medium) | WIN | `scripts/build_edition.py:6646-6647` | enrich_nav_chapters reads the ~480MB study-glossary monolith whole (no EINK_STUDY_BACKMATTER_STEM skip guard) — OOM when reader_native_toc_chapters is enabled on the eink flagship |
| 11 | builder-robustness | low (was medium) | WIN | `scripts/build_edition.py:8716` | --all ThreadPool worker count is target-blind (fixed at 5) with no MemoryError backoff — concurrent in-process eink builds co-reside the glossary monolith and can OOM |
| 12 | builder-robustness | low (was medium) | WIN | `scripts/build_edition.py:5191-5236 (helpers 4783-4793, 5092-5116); reached via the fallback at 5393` | Study-glossary str-path splitter holds ~3 full-size copies of the decoded body — the byte-streamer's whole-document fallback re-opens the flagship-eink OOM it was built to close |
| 13 | marker-logic-xreader | low | WIN | `scripts/build_edition.py:4204-4205 (gated by marker_style at 8429)` | numbers + eink: Cardo-unsafe note-sym category glyphs ship as tofu boxes because the eink glyph substitution runs only in the badge path |
| 14 | html-integrity | low (was high) | — | `scripts/core/html_sanitize.py:350-351, 376-413 (and html_sandbox.py 155-183)` | sanitize_html / sandbox_ai_html do not balance output — emit unclosed/stray/nested-<a> tags and silently drop note-body text (non-well-formed XHTML or content loss in shipped EPUB) |
| 15 | html-integrity | low | — | `scripts/lint_rules.py:2586` | no_reviewer_scaffolding guard inspects only the body field (index 7); the label and attribution fields also render into shipped HTML and are unscreened |
| 16 | html-integrity | low (was medium) | WIN | `scripts/build_edition.py:1833` | Literal placeholder token TODO_CERTIFIER_NAME shipped in every edition's OPF (a11y:certifiedBy) |
| 17 | display-redundancy | low | WIN | `scripts/build_edition.py:3857-3877` | eink backmatter glossary repeats the category header + source byline + verse-return link across split-group footnotes of one verse (no part indicator) |
| 18 | options-completeness | low (was medium) | — | `scripts/web_editions.py:419 (loader); customize.py:350 (control); api/editions.py:68,1256 (validator); editions.yaml:197 (shipped value)` | verse_marker_glyph is a fully-wired option with NO build read — a silent no-op (catholic-study ships ¶ with zero effect) |
| 19 | options-completeness | low (was medium) | — | `scripts/web_editions.py:393-499 (api_customize_data return; the 4 keys absent)` | Four presentation fields omitted from api_customize_data loader — /customize can't display or round-trip persisted values (can't reset a non-default to default) |
| 20 | os-binary-parity | low | — | `dev/build_appimage.sh:78-97` | Linux AppImage ships an unbranded teal placeholder icon while Win/Mac bundle the branded icon |

## Survivors — detail

### [1] cross-product — medium (recalibrated from high) · **WIN-owned (build_edition.py/epub_working)**
**Kindle target: desktop /customize build path produces the retired E999-failing variant (make_kindle_safe never applied) — declared 'Send-to-Kindle' != built**

- **Where:** `scripts/build_edition.py:2015-2017, 1021, 1364, 1736, 4673; cf. scripts/build_format_matrix.py:205-220`
- **Panel:** kept 2/2 (confidence: high,high)
- **Evidence:** is_kindle_target() still drives 4 build-time tunings inside build_one (1021 popup-lang cap=2, 1364/1429 compact_for_kindle, 1736 single dc:language, 4673 FILE_SPLIT_TARGET_KINDLE), but build_edition.py NEVER imports or calls scripts.core.kindle_post.make_kindle_safe (grep across the file: no kindle_post import; build_one tail 8060-8120 has no kindle post-pass). The proven Send-to-Kindle recipe (everywhere base + make_kindle_safe strip+OCF re-zip) exists ONLY in build_format_matrix._apply_kindle_post (lines 205-220). The unhide pass was retired: build_edition.py:6732 'The apply_kindle_unhide pass was part of the retired --target-reader kindle variant; the production path now uses kindle_post for hidden stripping.' So a desktop user who selects '📬 Kindle (Send to Kindle — visible endnotes)' (customize.py:535; wizard.py:170-172 and wizard.py:579 'the metadata passes Amazon’s delivery checks') and rebuilds via build_one persists target_reader=kindle and gets an artifact that is NEITHER unhidden NOR stripped — it still carries display:none note containers >10K chars and trips Amazon E999/E3013, FAILING the exact channel the option promises. The retirement was only half-done: the matrix base build was redirected to 'everywhere' (base_build_target), but the user-reachable direct path was left building the failing variant.
- **Proposed fix:** Make the two paths build the identical proven artifact. In build_one, when is_kindle_target(edition) apply scripts.core.kindle_post.make_kindle_safe + verify_kindle_safe as the final post-pass over the produced EPUB (mirror build_format_matrix._apply_kindle_post); and since the proven recipe IS the everywhere base, retire the divergent is_kindle_target content tunings (compact_for_kindle:1364, 2-lang cap:1021, FILE_SPLIT_TARGET_KINDLE:4673, single dc:language:1736) so a direct kindle build == everywhere + make_kindle_safe byte-for-byte. Alternatively, if kindle is to remain matrix-only, remove the kindle option from customize.py:535 + wizard.py:170 and have build_one reject target_reader=kindle rather than emit a known-failing artifact.
- **Verifier-refined fix:** Fix at the single chokepoint both failing paths share -- build_one's tail in scripts/build_edition.py, gated on is_kindle_target(edition). After build_epub writes output_path (in BOTH the frozen in-process branch near 8562 and the dev subprocess branch), add: `if is_kindle_target(edition): from scripts.core.kindle_post import make_kindle_safe, verify_kindle_safe; tmp = output_path.with_suffix('.epub.kindlepost'); make_kindle_safe(output_path, tmp); tmp.replace(output_path); fails = verify_kindle_safe(output_path); if fails: raise RuntimeError(f'kindle-safe recipe not satisfied: {fails}')`. This guarantees every kindle artifact build_one emits is physically stripped of display:none/visibility:hidden, collapsed to one dc:language, and OCF re-zipped (verify_kindle_safe asserts conformance), so the desktop export and the CLI both produce a Send-to-Kindle-deliverable file. SAFETY: gated on is_kindle_target, so it is byte-identical for every non-kindle edition -> the 9 KJV editions and the shipped everywhere Ethiopian Bible stay byte-stable. No double-processing: the matrix (everywhere base -> _apply_kindle_post) and build_kindle.py (everywhere base -> make_kindle_safe) never reach is_kindle_target. Touches no marathon-core file; schema-additive (no new fields). The finder's option to ALSO retire the in-pipeline content tunings (compact_for_kindle:1364, 2-lang cap:1021, FILE_SPLIT_TARGET_KINDLE:4673, single dc:language:1736) is OPTIONAL/cosmetic -- it would make a direct kindle build byte-match the device-proven matrix asset, but those tunings only REMOVE content (cannot add an E999 failure mode beyond what make_kindle_safe already clears) and the desktop export is a local build, not a release asset, so byte-identity with the matrix is not required for correctness. Do NOT take the finder's alternative of deleting the UI option / rejecting target_reader=kindle in build_one -- that removes a promised feature when a correct build is cheap. Add a test that builds a tiny edition with target_reader=kindle through build_one and asserts verify_kindle_safe(artifact) == [].

### [2] marker-logic-xreader — medium · **WIN-owned (build_edition.py/epub_working)**
**eink study-glossary return link teleports to chapter start (not the verse) for the 13 Strategy-B books that DO carry v-anchors (psa/job/kings/chronicles/...) - over-broad strategy keying, a turn-135 regression**

- **Where:** `scripts/build_edition.py:3749-3761 (decision at 3757)`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** _study_verse_return_link routes EVERY Strategy-B book's backmatter return link to the chapter opener: `if book and book.get("strategy") == "B" and book.get("bxx"): target_id = f"ch-{book['bxx']}-c{ch}"` else `v-{code}-{ch}-{v}`. The docstring justifies this for ANCHOR-LESS Strategy-B books (Jubilees/2 Enoch). But 13 Strategy-B books carry real v-anchors in epub_working/ (counted: psa 2468, job 1070, 1ch 942, 2ch 822, 1ki 816, 2ki 719, 1es 448, neh 406, jdt 339, ezr 280, tob 244, est 168, man 18), all in the shipped Ethiopian eink backmatter canon (reader_eink_study_layout: backmatter). A pre-turn-135 eink build returns to the verse (`href="#v-1ki-1-1"`, confirmed in exports/.cache eink artifacts); the current code emits `href="#ch-b10-c1"` for ANY 1ki verse, so returning from a note on e.g. Ps 119:160 lands at Ps 119:1. Not a dead link (ch-anchor exists; empirical orphan scan = 0) but a precision/teleport regression. Commit 5a8c1bcc (turn 135) introduced it; its test (test_marker_badge_style.py::test_study_return_strategy_b_uses_chapter_anchor) only pins the correct anchor-less 'jub' case, so no test catches the v-anchored books.
- **Proposed fix:** Gate on actual v-anchor presence, not on strategy=='B'. apply_badge_markers already detects presence per (code,ch) at line 4153 (`if f'id="v-{code}-{ch}-' in text`); thread a verse_anchored bool from apply_badge_markers -> _emit_backmatter_glossary_inner -> _study_verse_return_link (or add an additive verse_anchored field to books.yaml). Use v-{code}-{ch}-{v} when anchors exist; keep ch-{bxx}-c{ch} ONLY for genuinely anchor-less books (jub/2en/mq1-3/aes). This mirrors the kindle path which is already presence-based (kindle_post.py:404-411 _glossary_back_link uses v_anchor_files.get(v_id) and falls back only when absent). Keep the jub test; add a 1ki/psa case.
- **Verifier-refined fix:** Gate the return-link target on ACTUAL per-chapter v-anchor presence, not on strategy=='B'. Make the change additive and default-preserving so the existing jub test and all non-eink/KJV outputs stay byte-identical; only the ethiopian-tewahedo eink build changes.

1) _study_verse_return_link (build_edition.py:3749) — add an optional kwarg; keep legacy strategy inference as the default so the direct-call test is unchanged:

    def _study_verse_return_link(code: str, ch: int, v: int, *, verse_anchored: bool | None = None) -> str:
        label = f"{ch}:{v}"
        book = config.get_book(code)
        if verse_anchored is None:
            # legacy fallback (direct callers/tests): infer from strategy
            verse_anchored = not (book and book.get("strategy") == "B" and book.get("bxx"))
        if verse_anchored:
            target_id = f"v-{code}-{ch}-{v}"
        elif book and book.get("bxx"):
            target_id = f"ch-{book['bxx']}-c{ch}"   # genuinely anchor-less B book → chapter opener
        else:
            target_id = f"v-{code}-{ch}-{v}"
        return f'<a href="#{target_id}" class="note-back study-return">{html.escape(label, quote=False)}</a>'

2) _emit_backmatter_glossary_inner (3799) — thread the flag through:
   add `verse_anchored: bool = True` to its signature and change line 3808 to
   `... {_study_verse_return_link(code, ch, v, verse_anchored=verse_anchored)} ...`.

3) Call site (4336) — pass the boolean already computed by the 4153 branch. The condition at 4153 (`f'id=\"v-{code}-{ch}-\" in text`) is exactly the per-chapter anchor-presence signal: capture it (e.g. `chapter_has_v_anchors = f'id=\"v-{code}-{ch}-' in text` set where the if/elif is decided) and pass `verse_anchored=chapter_has_v_anchors` into _emit_backmatter_glossary_inner.

Do NOT hardcode an anchor-less book list (the finder's 'jub/2en/mq1-3/aes' is inaccurate — aes/2es carry anchors); rely purely on presence so it self-corrects and matches the kindle path. Known acceptable edge: a v-anchored chapter whose anchors spilled to the previous split file (e.g. 1ch 3) yields verse_anchored=False for that chapter and degrades to the chapter opener — still a valid link, same as today, never RSC-012.

Tests (tests/test_marker_badge_style.py): KEEP test_study_return_strategy_b_uses_chapter_anchor (jub, no kwarg → ch-b15-c5) and test_study_return_strategy_a_uses_verse_anchor (gen → v-gen-1-1) — both pass unchanged with the default. ADD a case proving a v-anchored Strategy-B book returns to the verse: `assert 'href=\"#v-psa-119-160\"' in _study_verse_return_link('psa',119,160, verse_anchored=True)` and one with verse_anchored=False asserting the ch-anchor. Confirm a fresh ethiopian eink build still passes the RSC-012/orphan gate and the 9 KJV editions remain byte-identical.

### [3] correctness — low (recalibrated from medium)
**edition_stats cache key (_edition_signature) omits the 3 popup-language-cap fields -> stale popup-language set baked into the EPUB About page and shown in the editor after a runtime target_reader / max_popup_languages / popup_languages_capped edit**

- **Where:** `scripts/core/edition_stats.py:50-79`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** `_resolved_note_counts_cached` is an lru_cache keyed on `_edition_signature(edition_id)`, whose key tuple (lines 56-78) lists the four popup tier fields (`popup_languages_default/per_book/per_chapter/per_verse`) but NOT `target_reader`, `max_popup_languages`, or `popup_languages_capped`. Yet `resolved_note_counts(...)['popup_languages']` is computed via `_resolve_popup_languages` -> `resolve_popup_language_pick` -> `resolve_popup_language_cap`, which read exactly those omitted fields: `edition.get('max_popup_languages')` (build_edition.py:1019), `edition.get('popup_languages_capped')` (build_edition.py:1041), and `target_reader` via `is_kindle_target`/`resolve_target_reader` (build_edition.py:1982). The only caller that clears this cache is notes_io._invalidate_corpus_index_if_notes_file (fires on content/notes/*.py writes only); the edition-record edit paths (scripts/api/editions.py:389/591/1296/1387, scripts/api/customize.py:80/123) clear `config.load_editions`+`matrix.compute_matrix` but NOT `edition_stats`. So in the long-lived desktop web process, editing ONLY a cap field (e.g. flipping an edition to target_reader=kindle, which caps popups to Hebrew+Greek) does not change the signature and does not clear the cache: `resolved_note_counts` returns the pre-edit (uncapped) popup_languages. That field is baked into the EPUB 'What's Inside' sentence (matter_pages.py:386-394 `_popup_language_labels` -> 409 `_whats_inside_sentence`) and surfaced in the customize UI (web_editions.py:173 `popup_langs = list(rc['popup_languages'])`). Because build_cache.compute_cache_key DOES hash target_reader, a target_reader edit forces a real rebuild during which matter_pages re-reads the still-stale edition_stats lru_cache, so the wrong language list ('...Latin, and Arabic' instead of capped 'Hebrew and Greek') is written into the rebuilt kindle EPUB. Note counts (total/per_book/per_kind) are unaffected; only popup_languages is wrong.
- **Proposed fix:** Add the three cap-affecting fields to the `_edition_signature` key tuple: `"target_reader"`, `"max_popup_languages"`, `"popup_languages_capped"`. This is the minimal correct fix (the signature is the documented invalidation mechanism for runtime edition edits). As defense-in-depth for the whole class, also call `edition_stats.cache_clear()` in the edition-record edit paths (api/editions.py + api/customize.py) beside the existing `matrix_mod.compute_matrix.cache_clear()` calls, mirroring what notes_io already does for note-file writes, so any future output-affecting field forgotten in the signature still invalidates on edit.

### [4] correctness — low
**At-scale driver progress stat over-counts written candidates (ignores append_candidates' dedup) — both shared sites**

- **Where:** `scripts/core/at_scale_base.py:307-311, 452-458`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** In run_word_detector_for_book: `out = append_candidates(...)` then `if out: files_written += 1; candidates_written += len(chapter_candidates)`. The same shape is in run_ai_detector: `out = append_candidates(...)` then `if out: files_written += 1; candidates_written += len(cands)`. But append_candidates only writes the NEW entries after deduping `(verse, kind, draft_body)` against both the existing on-disk queue AND within the run (`if key in seen: continue`), and it returns a truthy Path whenever *any* new entry was added — not the count. So `len(chapter_candidates)`/`len(cands)` is the PRE-dedup total. On any re-run over an already-populated candidates dir (the common idempotent case), the printed TOTAL `candidates` and the returned `candidates_written` over-report — they count candidates that were silently skipped as duplicates and never written. Verified both sites; the other accumulator drivers (xref/naves/torrey/ethiopian) report via append_candidates' own `n_candidates` payload and are unaffected; run_kenyon uses its own full-reindex write.
- **Proposed fix:** Make append_candidates return the number of entries actually written (e.g. `return len(new_dicts)` when it writes, `0` otherwise — keeping a falsy-on-nothing-new contract), then change both call sites to `n = append_candidates(...)` / `if n: files_written += 1; candidates_written += n`. This keeps the existing `if out:` truthiness behaviour (0 is falsy) while making the reported count reflect what was actually persisted.
- **Verifier-refined fix:** Fix the WHOLE class (7 sites) and preserve the Path|None contract that prospect + the 2 contract tests depend on. Make append_candidates return the count of entries actually written (return len(new_dicts) on a write; return 0 for the empty-input and nothing-new branches, replacing both `return None` and the final `return out_path`); update its docstring. Update the count-consuming WRITE sites to use the returned int: at_scale_base.py:309-311 and 456-458 -> `n = append_candidates(...); if n: files_written += 1; candidates_written += n`; same shape at run_xref_at_scale.py:87-90, run_naves_at_scale.py:80-83, run_torrey_at_scale.py:113-115 (write branch only — KEEP the dry_run branch counting len(chapter_candidates)), run_ethiopian_at_scale.py:101-104 (write branch only — KEEP the dry_run projection at 97-99). For kenyon, change its own write_queue (run_kenyon_at_scale.py:82-end) to return len(new_dicts)/0 and use it at lines 147-148 (`if n: total_files += 1; total_candidates += n; by_book[book] += n`). Update the now-int wrappers' annotations (`-> Path | None` -> `-> int`) on run_xref/naves/torrey/ethiopian/ai_notes/ai_xrefs write_queue. CRITICAL: prospect.py needs the Path (prospect.py:349 .relative_to), so special-case ITS wrapper (prospect.py:161-163) to reconstruct the path locally and keep Path|None: `path = CANDIDATES_DIR / f"{book}_ch_{chapter:03d}.json"; return path if append_candidates(path, book, chapter, candidates) else None`. Finally update the 2 contract tests for the new int return: tests/test_mint10_phase4.py:96 -> `assert ret == 1`; :111 -> `assert append_candidates(...) == 0`. This keeps every `if out:` truthiness intact, keeps prospect's Path semantics, leaves dry-run projections untouched, touches no marathon-core file, and does not affect the build pipeline (KJV editions remain byte-stable). Given low materiality, an acceptable lighter alternative is to leave it as-is (a documented cosmetic stat over-count) — but if fixed, it MUST be fixed across all 7 sites, not just the 2 cited.

### [5] security — low
**Note-editor render boundary loads stored note bodies via innerHTML with no sanitization — only the SAVE path is gated, the READ/render path is not (boundary-doctrine gap)**

- **Where:** `scripts/templates/index.py:315 (also 218); server read path scripts/web_notes.py:83`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** index.py:315 `$('#f-body').innerHTML = d.body || '';` assigns the stored note body straight into a contenteditable via innerHTML. The body comes from `api_notes` (web_notes.py:69-87), which returns `tuple_to_dict(tup)` (web_helpers.py:124 -> `"body": body or ""`) — the RAW stored body with NO sanitize. The save path sanitizes (web_notes.py:162 `payload["body"] = sanitize_html(...)`) and every other render boundary sanitizes on output (preview.py:137, web_content.py:394 sample, verse_of_day.py:356 RSS, inject.py:233 build) — but the editor render-on-LOAD does not, breaking the documented 'sanitize at every render boundary' doctrine. innerHTML does NOT run `<script>` but DOES fire `<img src=x onerror=...>` / `<svg onload=...>`. Bodies that entered the store via bulk corpus ingest (extract_*/Nave's/Easton's/Torrey/commentary — none of those scripts call sanitize_html/sandbox_ai_html; only the AI promote.py/batch_promote_xrefs.py paths do) bypass the save-path gate entirely, so opening such a note would execute the payload in the localhost origin. Secondary site, same class: index.py:218 builds the list excerpt with `(n.body||'').replace(/<[^>]+>/g,'')` then injects it via innerHTML at 211 — a regex tag-strip is not an HTML-safe escape. Mitigation today: I scanned all 88 content/notes/*.py and found 0 occurrences of <img/<svg/<script/<iframe/onerror=/onload=/onclick= — corpus is currently clean, so this is latent, not an active exploit; single-user local app limits blast radius to the operator's own browser.
- **Proposed fix:** Close the read boundary so the editor never trusts the store: either (a) sanitize on the API read path — run `sanitize_html` over the body in `api_notes`/`tuple_to_dict` before returning it, OR (b) on the client, run the existing allowlist serializer `normalizeBody(d.body)` (index.py:348) BEFORE assigning it at line 315 instead of assigning the raw body. Also replace the regex tag-strip excerpt (index.py:210/218) with `textContent` assignment or a real escape. This makes the editor consistent with the other render boundaries and removes the dependence on the corpus happening to stay clean.

### [6] security — low (recalibrated from medium)
**SSRF allowlist in http.py is bypassed by HTTP redirects (initial URL checked, redirect target not re-validated)**

- **Where:** `scripts/core/http.py:167-172 (get); 232-240 (put)`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** `get()` validates only the initial URL — `_check_allowlist(url, allowlist)` (line 167) — then calls `with urlopen(url, timeout=timeout) as r:` (line 172) using the DEFAULT `urllib.request.urlopen`. The module has zero redirect handling (grep for `redirect|build_opener|HTTPRedirectHandler` returns nothing). Python's default opener installs `HTTPRedirectHandler`, which transparently follows 3xx responses; the redirect `Location` is NOT re-run through `_check_allowlist`. So an allowlisted host (e.g. archive.org / ebible.org / openbible.info, or a host pasted via the user-reachable `url_override` in api_sources_cache_fetch which is validated against DEFAULT_PD_SOURCES_ALLOWLIST) can return `302 Location: http://127.0.0.1:.../` or `http://169.254.169.254/...` and the fetch follows it, defeating the module's documented purpose ("the single place to make outbound HTTP requests" with egress pinning). `put()` has the identical pattern (check at 232, unguarded `urlopen(req, ...)` at 240). Caveat: urllib's redirect handler restricts redirect schemes to http/https/ftp, so `file://` LFI via redirect is already blocked — the exposure is internal-http(s) SSRF only. Sweep: both egress functions in the file (get + put) share the default opener; these are the only 2 `urlopen(` sites — a single shared fix covers both. No test exercises redirect-bypass.
- **Proposed fix:** Stop using the default redirect-following opener. Build a dedicated opener with a redirect handler that re-validates each hop: subclass `urllib.request.HTTPRedirectHandler` and override `redirect_request` to call `_check_allowlist(newurl, allowlist)` (raising SSRFBlockedError on a non-allowlisted hop) and to cap the redirect count; pass that opener (via `build_opener`) as the default `urlopen` in both `get()` and `put()`. Simpler/stricter alternative acceptable for this PD-only fetch surface: install a no-follow handler that raises HttpError on any 3xx so redirects must be resolved explicitly by the caller. Add a regression test that stubs a 302 from an allowlisted host to a disallowed host and asserts SSRFBlockedError.
- **Verifier-refined fix:** Re-validate every redirect hop with a custom opener built PER CALL (the allowlist is a per-call argument, so it cannot be a static module-level default urlopen). In scripts/core/http.py add, after _check_allowlist:

class _AllowlistRedirectHandler(urllib.request.HTTPRedirectHandler):
    def __init__(self, allowlist):
        self._allowlist = allowlist
    def redirect_request(self, req, fp, code, msg, headers, newurl):
        # re-run the same egress pin on the redirect target; raises SSRFBlockedError on a non-allowlisted hop
        _check_allowlist(newurl, self._allowlist)
        return super().redirect_request(req, fp, code, msg, headers, newurl)

def _validating_opener(allowlist):
    return urllib.request.build_opener(_AllowlistRedirectHandler(allowlist)).open

Change the get()/put() signatures from `urlopen=urllib.request.urlopen` to `urlopen=None`, and immediately after the existing `_check_allowlist(url, allowlist)` line (167 / 232) add:
    if urlopen is None:
        urlopen = _validating_opener(allowlist)

Notes that make this safe: by the time the opener is built, the top-of-function _check_allowlist has already guaranteed allowlist is non-None and the initial host is valid. build_opener with an instance of an HTTPRedirectHandler SUBCLASS REPLACES the default redirect handler (so all redirects route through the validating one). opener.open(url|req, timeout=timeout) is signature-compatible with the current urlopen(...) calls, so lines 172/240 are unchanged. SSRFBlockedError raised inside redirect_request is neither HTTPError nor URLError/TimeoutError/OSError, so it propagates out of the retry loop uncaught — giving the desired immediate, no-retry block exactly like the initial check. The default HTTPRedirectHandler already caps total redirects (max_redirections), so an explicit counter is optional. The test seam is preserved: callers/tests that pass their own urlopen= still bypass this (they are stubbing the network). Do NOT adopt the finder's no-follow alternative as the default: archive.org/ebible.org legitimately 301/302-redirect (mostly to *.archive.org, accepted by the subdomain matcher) and raising on any 3xx would break real PD-source fetches. Add a regression test that injects a stub raising a 302 from an allowlisted host to a disallowed host through the real validating opener path and asserts SSRFBlockedError. This touches only the fetch/egress surface — no marathon-core files, no build path — so the 9 KJV editions remain byte-stable and the change is additive.

### [7] cross-module — low
**bookcode_canonical lint omits patristic_commentaries.json — 5 of 6 identical commentary stores screened**

- **Where:** `scripts/lint_rules.py:2276-2282`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** The ★BUGCLUSTER guard's json tier screens exactly five commentary stores:

    json_specs = [
        "content/sources/ethiopian_commentaries.json",
        "content/sources/protestant_commentaries.json",
        "content/sources/catholic_commentaries.json",
        "content/sources/rabbinic_commentaries.json",
        "content/sources/reformation_commentaries.json",
    ]

But there are SIX _CommentaryCorpus subclasses, each backed by a *_commentaries.json store and ALL loaded by the same base class (scripts/core/sources_commentary.py): Patristic (PATH = _SOURCES / "patristic_commentaries.json", line 345), Ethiopian (362), Protestant (381), Catholic (397), Reformation (414), Rabbinic (430). patristic_commentaries.json is the only one not in json_specs. The lint's own docstring says it screens "the 5 *_commentaries.json stores" and its pass message reports "21 book-code maps/lists emit only canonical codes" — giving false confidence that EVERY such store is screened. The store is live: PatristicCommentaries is consumed by a detector (detectors.py:965). I checked the whole class: of the 6 commentary stores 5 are screened and 1 is not; the patristic store today has only 8 entries, all canonical (no legacy codes), and a repo-wide scan for legacy-alias values returned zero hits, so there is no live corruption — runtime is also mitigated because the shared loader normalizes the .book field at construction (sources_commentary.py:308). The gap is purely the commit-time defense-in-depth tier being incomplete for one of six structurally identical stores, so a future patristic ingest carrying a legacy code (joh/ps/mar/php/...) would NOT be flagged by the guard that exists precisely to catch it.
- **Proposed fix:** Add "content/sources/patristic_commentaries.json" to the json_specs list at scripts/lint_rules.py:2276-2282 (and update the line-2269 docstring comment from "5 *_commentaries.json stores" to "6"). One-line, byte-safe, no engine change; the live store is already canonical so the lint stays green. This completes the guard's stated contract that every book-code store in the repo emits only canonical codes.
- **Verifier-refined fix:** Add `"content/sources/patristic_commentaries.json"` to the `json_specs` list at scripts/lint_rules.py:2276-2282 (one line, additive, byte-safe, no engine change; the live store is canonical so the lint stays green). Do NOT blindly flip "5" -> "6" in the line-2269 comment as the finder suggested: that comment is a historical round-7 account ("The 5 *_commentaries.json stores carried 135 legacy 'book' values") and the patristic store ships clean with 0 legacy values, so attributing the 135-value cleanup to 6 stores would be inaccurate. Instead, append a short clause clarifying the present count, e.g. change the comment to note "round-7 normalized 5 stores; the patristic store (γ.3) is screened here too for completeness — 6 stores total now covered." The dynamic pass message uses f"{screened} ..." so no hardcoded number needs updating there.

### [8] cross-module — low
**bookcode_canonical lint omits the 6th commentary store (patristic_commentaries.json) — guard now incomplete for a detector-feeding source**

- **Where:** `scripts/lint_rules.py:2276-2282`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** The ★BUGCLUSTER guard's json tier screens exactly five stores: `json_specs = ["content/sources/ethiopian_commentaries.json", "content/sources/protestant_commentaries.json", "content/sources/catholic_commentaries.json", "content/sources/rabbinic_commentaries.json", "content/sources/reformation_commentaries.json"]`. But six book-keyed commentary corpora are loaded identically by `_CommentaryCorpus._load`: `PatristicCommentaries` (sources_commentary.py:341-345, `PATH = _SOURCES / "patristic_commentaries.json"`) is missing. That store feeds the notes pipeline — detectors.py:965: `self.corpus = sources.patristic_commentaries()`. Both the lint comment (lint_rules.py:2269 "The 5 *_commentaries.json stores") and the loader comment (sources_commentary.py:305 "The 5 JSON stores are normalized + lint-screened") assert 5 are screened, but patristic rides unscreened, so the next patristic ingest could land a legacy code (joh/ps/...) in the source JSON with no commit-time catch (per memory: ★BUGCLUSTER, recurs every ingest). Live note-routing is backstopped by `book=_normalize_book_code(...)` at sources_commentary.py:308; the gap is the source-of-truth JSON itself. Checked all 6 corpus classes and all 13 content/sources/*.json — only patristic is an unscreened commentary store; none currently carries legacy values.
- **Proposed fix:** Add "content/sources/patristic_commentaries.json" to `json_specs`. Root-cause fix: derive `json_specs` from the corpus classes' `PATH` attrs (or glob('content/sources/*_commentaries.json')) so future stores auto-screen, and correct the stale "5 stores" wording at lint_rules.py:2269 and sources_commentary.py:305 to 6. Optionally add a test asserting every *_commentaries.json appears in json_specs.
- **Verifier-refined fix:** Add the missing store to json_specs in scripts/lint_rules.py:2276-2282: insert "content/sources/patristic_commentaries.json". Root-cause (preferred, prevents recurrence): replace the hardcoded list with a glob so future stores auto-screen, e.g. `json_specs = sorted(str(p.relative_to(REPO)) for p in (REPO / "content/sources").glob("*_commentaries.json"))`. A glob is safer than importing the corpus classes to read their PATH attrs because it keeps the lint import-light (no need to load sources_commentary at commit time) and cannot miss a new store. Update the stale wording: lint_rules.py:2269 "The 5 *_commentaries.json stores" -> "6", and sources_commentary.py:305 "The 5 JSON stores are normalized + lint-screened" -> "6". Optionally add a regression test asserting every content/sources/*_commentaries.json appears in json_specs (only needed if the hardcoded-list form is kept; the glob form makes it self-maintaining). All changes are lint/comment-only: no build-output change, the 9 KJV editions stay byte-stable, schema is untouched, and nothing in the off-limits marathon core is touched. patristic_commentaries.json currently carries only canonical 'gen' codes, so the lint will PASS immediately after the addition.

### [9] builder-robustness — low (recalibrated from medium) · **WIN-owned (build_edition.py/epub_working)**
**Residual ~3x-copy RAM peak in the study-glossary slice splitter + a 2x transient in _decode_nl (offset-based slice fix recommended, not done)**

- **Where:** `scripts/build_edition.py:4783-4793, 5092-5116, 5191-5236, 5271-5278, 5386-5393`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** _split_head_body_tail returns full-size slice copies: `return text[:body_start], text[body_start:body_close], text[body_close:]` (4793), and _study_index_section_parts likewise returns `body[...]` slices (5116). In _iter_study_glossary_pieces these stack before the del-chain frees them: after `head, body, tail = parts` (5213) body~=text (2x), then `_study_index_section_parts(body)` materializes inner~=body (3x transient peak) before `del body` (5219); then `_study_glossary_chunk_atoms(inner)` (5221) materializes atoms~=inner (3x again) before `del inner` (5225). The flagship's primary path is the proven byte-streamer (>64MB), but the structural-surprise fallback `_iter_study_glossary_pieces(_decode_nl(raw), ...)` (5393) feeds the FULL decoded monolith through this 3x slicer while `raw` (~480MB bytes) is still referenced. _decode_nl itself amplifies: `b.decode(...).replace("\r\n","\n").replace("\r","\n")` (5278) chains two .replace calls, each allocating a fresh full-size copy (~2x transient of an ~880MB UCS-2 str) before the splitter even runs — together ~raw + several x the str on the 16GB box.
- **Proposed fix:** Convert _split_head_body_tail / _study_index_section_parts to return OFFSETS into the parent string and have _iter_study_glossary_pieces build each piece by slicing the parent only at yield time (peak ~1x instead of ~3x), preserving byte-identity. For the fallback, replace the chained `.replace().replace()` in _decode_nl with a single-pass newline normalization (e.g. splitlines/join or one re.sub) to kill the 2x decode transient, or route the structural-surprise fallback through the per-book byte path instead of decoding the whole monolith.
- **Verifier-refined fix:** Isolate the fix to the glossary str path; do not touch the shared helper signatures. In _iter_study_glossary_pieces add `del parts` immediately after `head, body, tail = parts` (line 5213) and `del sec_parts` immediately after `body_prefix, sec_open, inner, body_suffix = sec_parts` (line 5218). This makes the existing `del body` (5219) and `del inner` (5225) actually drop the only remaining references, restoring the docstring's intended peak (cuts ~4x->~3x transient) — byte-identical output, no impact on the byte streamer or the 9 KJV editions. Leave _split_head_body_tail / _study_index_section_parts as-is (returning slices) since they have byte-stable callers (split_html_document at 4913; merge at 5527/5533). _decode_nl optimization is optional and marginal (the only large-input decode is the D5-dead fallback); if done, use `re.sub(r"\r\n?", "\n", b.decode("utf-8"))` (exactly equivalent to the chained replaces) — do NOT use str.splitlines()/join, which would split on \v\f\x1c-\x1e\x85   and corrupt the U+2028 vnote separators / break byte-identity.

### [10] builder-robustness — low (recalibrated from medium) · **WIN-owned (build_edition.py/epub_working)**
**enrich_nav_chapters reads the ~480MB study-glossary monolith whole (no EINK_STUDY_BACKMATTER_STEM skip guard) — OOM when reader_native_toc_chapters is enabled on the eink flagship**

- **Where:** `scripts/build_edition.py:6646-6647`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** `for p in list_split_html_files(tmp):` then `for m in re.finditer(r'\bid="ch-b(\d+)-c(\d+)"', p.read_text(encoding="utf-8")):` — no skip for the study-glossary stem. This pass runs at build_one:8519 (after inject_eink_study_backmatter:8487 creates index_split_900.html and before apply_file_split:8540 splits it), so on an eink+backmatter edition it read_text()s the ~480MB monolith whole = the ~880MB UCS-2 decode blow-up the byte-streamer was built to avoid. The sibling retarget_demoted_toc_anchors explicitly guards exactly this: `if p.stem != EINK_STUDY_BACKMATTER_STEM and 'class="toc-book"' in p.read_text(...)` with the comment "Skip the eink study glossary (~480 MB ...): loading its ~480 MB (the flagship-eink OOM site, pre-split) is pure waste." Currently latent (reader_native_toc_chapters is unset in every editions.yaml edition; default off), but it is a builder option a user can enable on ethiopian-tewahedo (eink + reader_eink_study_layout: backmatter). Of the 3 file-tree passes in the post-glossary window, the two siblings (retarget_demoted_toc_anchors, _merge_mid_verse_breaks:5743) guard the stem; enrich_nav_chapters is the only one that does not.
- **Proposed fix:** Add the one-line guard mirroring the sibling: `if p.stem == EINK_STUDY_BACKMATTER_STEM: continue` at the top of the loop body (6647). The glossary carries only study-book-head ids, never ch-bNN-cMM, so skipping it is a guaranteed byte-identical no-op that removes the OOM read.
- **Verifier-refined fix:** Add the guard mirroring the two siblings as the first statement of the loop body at scripts/build_edition.py:6647, inside `for p in list_split_html_files(tmp):` — `if p.stem == EINK_STUDY_BACKMATTER_STEM: continue`. Guaranteed byte-identical (the glossary holds no ch-bNN-cMM ids and sorts last after the `seen`-deduped scripture splits) and removes the ~480MB whole-file read = the eink-flagship OOM when reader_native_toc_chapters is enabled.

### [11] builder-robustness — low (recalibrated from medium) · **WIN-owned (build_edition.py/epub_working)**
**--all ThreadPool worker count is target-blind (fixed at 5) with no MemoryError backoff — concurrent in-process eink builds co-reside the glossary monolith and can OOM**

- **Where:** `scripts/build_edition.py:8716`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** `with ThreadPoolExecutor(max_workers=min(len(targets), 5)) as pool:` — the worker count is fixed regardless of --target-reader. The in-file note pins it as round-7-audited "CONFIRMED-OPTIMAL" (2026-06-10), which predates the eink glossary monolith path (round 14-15). The heavy glossary work (apply_file_split -> _iter_study_glossary_pieces_from_file holding the full ~255-480MB raw bytes + per-book decode) runs IN the build_one thread in the PARENT process — only build_epub (the zip) is forked as a subprocess (8558-8580). So `build_edition.py --all --target-reader eink` (a supported combo; main() explicitly handles args.all + args.target_reader at 8690-8697) runs up to 5 build_one threads concurrently, each co-residing its glossary monolith in one process — multiples of the per-build peak the byte-streamer was tuned for as a SINGLE build. The as_completed loop catches MemoryError via `except Exception` (8734) so it degrades to a failed edition, but there is no proactive memory-headroom check or sequential-retry backoff, so one OOM can cascade across the concurrent heavy builds and silently drop artifacts ("the builder must not crash").
- **Proposed fix:** Make the worker count target-aware rather than re-deriving a new universal number: e.g. `workers = 1 if args.target_reader == 'eink' else min(len(targets), 5)` (or gate on psutil.virtual_memory().available headroom per heavy build). Optionally retry an edition that raised MemoryError once, sequentially, after the pool drains, so an --all eink run still produces every artifact.
- **Verifier-refined fix:** Adopt the finder's PRIMARY target-aware worker count (drop the psutil alternative — psutil is not a declared dependency; grep of requirements*.txt/pyproject.toml is empty). At build_edition.py:8716 compute the pool size before the executor: `workers = 1 if args.target_reader == "eink" else min(len(targets), 5)` then `with ThreadPoolExecutor(max_workers=workers) as pool:`. This is byte-safe (worker count is scheduling only — output bytes are deterministic per edition, so the 9 KJV editions stay byte-identical), additive, and touches no marathon core. The condition is complete because the ~480MB glossary monolith is created ONLY under the eink target and no edition bakes target_reader:eink into editions.yaml. Update the stale "CONFIRMED-OPTIMAL (round-7)" comment (8708-8710) to note the eink carve-out so it is not re-asserted. The finder's optional sequential-MemoryError-retry is a reasonable nice-to-have but secondary — note that an OS OOM-kill cannot be caught in Python anyway, so capping concurrency (not retry) is the load-bearing fix.

### [12] builder-robustness — low (recalibrated from medium) · **WIN-owned (build_edition.py/epub_working)**
**Study-glossary str-path splitter holds ~3 full-size copies of the decoded body — the byte-streamer's whole-document fallback re-opens the flagship-eink OOM it was built to close**

- **Where:** `scripts/build_edition.py:5191-5236 (helpers 4783-4793, 5092-5116); reached via the fallback at 5393`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** The OOM-safe path is the byte streamer: `_iter_study_glossary_pieces_from_file` (5386) reads the ~480 MB flagship monolith as bytes and, for files over `_GLOSSARY_STREAM_BYTE_THRESHOLD` (64 MB, 5254), calls `_stream_glossary_pieces_from_bytes` (5391, the proven D5 fix). But its fallback decodes the WHOLE file and hands it to the str splitter: `except _GlossaryStructureError: yield from _iter_study_glossary_pieces(_decode_nl(raw), stem, target)` (5393). Inside `_iter_study_glossary_pieces`, `_split_head_body_tail` returns `text[:body_start], text[body_start:body_close], text[body_close:]` (4793) — `body` is a ~full-size copy created while `text` is still referenced; then `_study_index_section_parts(body)` returns `body[open_m.end():close_start]` (5116) — `inner` is another ~full-size copy while BOTH `text` and `body` are alive. At line 5218 `text` + `body` + `inner` co-reside (~3x the ~880 MB decoded str ≈ 2.6 GB) BEFORE `del body` (5219); `del text` only runs at 5231 after `atoms` (≈ inner size) is built (5221), so a second ~2.6 GB peak occurs at 5221. Add the ~480 MB `raw` still held by the caller frame during the fallback ≈ ~3.1 GB — a MemoryError on the 16 GB box, and (since the frozen desktop app builds IN-PROCESS, build_edition.py:8558 / api/exports.py:200) it crashes the server process, not an isolable subprocess. The trigger is concrete and a regression trap: because the byte patterns (5259-5262) are exact ASCII twins of the str patterns, the only way `_stream_glossary_pieces_from_bytes` raises `_GlossaryStructureError` on a real ~480 MB monolith is a future glossary-markup change (e.g. renamed `study-notes-index`/`study-book-head` class) — exactly the large input the streamer exists to handle is the one routed into the un-bounded fallback. CLASS SWEEP: the slice-while-parent-referenced helpers `_split_head_body_tail`/`_study_index_section_parts` are also consumed by `split_html_document` (4913) and `_merge_scripture_base_files` (5527/5533), but those inputs are scripture base files (~2-100 MB) so the 3x copy is bounded/harmless; the glossary path (5209/5214) is the ONLY OOM-scale consumer.
- **Proposed fix:** Make the fallback memory-bounded so it cannot re-introduce the ceiling the byte path removed. Preferred (the recommended offset-based slice): never materialize the intermediate `body` and `inner` strings — compute body_start/body_close and section open/close as offsets into the single decoded `text` (or directly into `raw`) and build each emitted piece by slicing `text` once at piece boundaries, dropping `_split_head_body_tail`/`_study_index_section_parts`'s full-size return slices for this path. Simpler alternative: in `_iter_study_glossary_pieces_from_file`, do NOT fall back to the whole-string splitter for inputs above `_GLOSSARY_STREAM_BYTE_THRESHOLD`; on `_GlossaryStructureError` for such a file, raise a clear build error (the structure genuinely changed and the byte patterns at 5259-5262 must be updated) rather than silently decoding ~480 MB + 3x slicing into an OOM. Either keeps str==from-file byte-identity for all in-spec inputs while removing the residual RAM ceiling.
- **Verifier-refined fix:** Adopt the finder's "simpler alternative" (it is the safe one; do NOT do the offset-rewrite as the default — see caveat). In _iter_study_glossary_pieces_from_file, after a successful read but before the try/except, capture that the input is over threshold, and on _GlossaryStructureError for a >_GLOSSARY_STREAM_BYTE_THRESHOLD file, DO NOT decode-and-str-split. Instead raise a clear, actionable build error, e.g.:

    raw = path.read_bytes()
    if len(raw) <= _GLOSSARY_STREAM_BYTE_THRESHOLD:
        yield from _iter_study_glossary_pieces(_decode_nl(raw), stem, target)
        return
    try:
        yield from _stream_glossary_pieces_from_bytes(raw, stem, target)
    except _GlossaryStructureError as e:
        raise RuntimeError(
            f"study-glossary monolith {path.name} ({len(raw)} bytes) exceeds the "
            f"{_GLOSSARY_STREAM_BYTE_THRESHOLD}-byte stream threshold but the byte "
            f"streamer could not parse its structure (glossary markup likely changed) — "
            f"update the byte-twin patterns at build_edition.py:5259-5262; refusing the "
            f"whole-document str fallback (it decodes ~{len(raw)//1_000_000} MB + holds "
            f"~3 full-size copies → OOM)."
        ) from e

Why this and not the offset-rewrite: (a) it is purely additive on a path that is dead in production today, so it cannot change byte output for ANY in-spec input — the ≤64MB str path and the >64MB byte-streamer path are both untouched, the 9 KJV editions and the shipped flagship stay byte-identical; (b) it converts a confusing OOM (which, frozen, crashes the in-process server) into a deterministic message that points the maintainer at the exact patterns to fix; (c) the OOM-only scenario it replaces produces no usable build anyway, so no correct behavior is lost.

CAVEAT on the finder's "preferred" offset-based rewrite: that path is riskier and should NOT be taken lightly. _iter_study_glossary_pieces is the REFERENCE implementation that _stream_glossary_pieces_from_bytes is proven byte-identical against (D5: 690/690 on the 255MB monolith). Rewriting it to offset-slicing changes that reference and would require re-proving the str==from-file byte-identity test green AND re-proving the ≤64MB catholic-study eink build byte-stable; only do it if both are re-verified. The error-fail fix above achieves the same robustness goal with none of that exposure.

Optional belt-and-suspenders (independent of the above, harmless): in _iter_study_glossary_pieces, rebind `parts = None` after line 5213 and `sec_parts = None` after line 5218 so the existing `del body`/`del inner` actually free those copies for the in-spec (≤64MB / fallback) path — the current dels are no-ops because the tuples retain the references. This lowers the catholic-study peak too and is byte-output-neutral.

### [13] marker-logic-xreader — low · **WIN-owned (build_edition.py/epub_working)**
**numbers + eink: Cardo-unsafe note-sym category glyphs ship as tofu boxes because the eink glyph substitution runs only in the badge path**

- **Where:** `scripts/build_edition.py:4204-4205 (gated by marker_style at 8429)`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** _eink_safe_note_sym (the Cardo-safe substitution the module documents at line 2850 as fixing glyphs 'empirically absent from Cardo -> a blank box') is invoked ONLY inside apply_badge_markers: `if eink_target: row = _eink_safe_note_sym(row)` (4205), and apply_badge_markers runs only `if marker_style == "badge"` (8429). A marker_style=numbers edition (a /customize-selectable option, web_editions.py:453) built for the eink target therefore never substitutes the per-note `<a class="note-sym">` glyphs. Those glyphs are present unmodified in the base (epub_working/index_split_005.html note-sym values: diamond/house/command/sparkle/star/double-bar = exactly the set _eink_safe_note_sym maps), so on Kobo's Cardo reading font they render as blank boxes in every note aside. The inline marker is renumbered to marker-num so only the aside glyph is affected. G4 explicitly skips numbers editions, so nothing flags it.
- **Proposed fix:** Make the eink glyph substitution marker_style-agnostic: run a small eink-gated post-pass over surviving `<a class="note-sym">...</a>` spans (apply _eink_safe_note_sym / _eink_category_badge_glyph to the glyph text) for eink targets regardless of badge vs numbers - e.g. fold it into one of the existing eink repair passes (the repair_texts block ~8445-8463) so numbers+eink gets the same Cardo-safe glyphs the badge+eink path already gets. Non-default opt-in combo; cosmetic only, no navigation impact.
- **Verifier-refined fix:** Add an eink-gated repair pass that substitutes surviving aside note-sym glyphs, but GATE IT TO non-badge editions only (the badge path already handles note-sym at 4205 + the WS2 cascade drop at 3937, and every shipped edition is badge — so non-badge gating guarantees the 9 KJV editions and the flagship eink superset stay byte-identical / no golden-hash regression).

In the repair_texts block (~8445-8463), after the existing passes, add:

    marker_style already computed at 8427; eink already resolvable via resolve_target_reader(edition) == "eink".
    if resolve_target_reader(edition) == "eink" and marker_style != "badge":
        n = 0
        for name, txt in repair_texts.items():
            if 'class="note-sym"' in txt:
                new = _eink_safe_note_sym(txt)
                if new != txt:
                    repair_texts[name] = new
                    n += 1
        stats["eink_note_sym_substituted_files"] = n

This reuses the existing module-level _NOTE_SYM_GLYPH_RE/_eink_safe_note_sym (idempotent for mapped categories), runs on the already-loaded in-memory dict (no extra glob/read), is a strict no-op for every badge edition (skipped) and every non-eink edition (skipped), and touches the marathon core not at all. Severity low; cosmetic, no navigation impact.

### [14] html-integrity — low (recalibrated from high)
**sanitize_html / sandbox_ai_html do not balance output — emit unclosed/stray/nested-<a> tags and silently drop note-body text (non-well-formed XHTML or content loss in shipped EPUB)**

- **Where:** `scripts/core/html_sanitize.py:350-351, 376-413 (and html_sandbox.py 155-183)`
- **Panel:** kept 1/2 (confidence: high,high)
- **Evidence:** Empirically reproduced against the live functions: sanitize_html('compare a<b>c') -> 'compare a<b>c' which xml.dom.minidom rejects ('mismatched tag'); '<p>x<p>y' -> '<p>x<p>y' (unclosed <p>); 'text <em>bold' -> 'text <em>bold' (unclosed <em>); 'hi </b> there' -> 'hi </b> there' (stray close, no open). Silent content LOSS (valid XHTML, so epubcheck cannot catch it): '<<double>>' -> '&lt;&gt;' (word 'double' deleted), 'foo <bar baz qux' -> 'foo ' ('baz qux' deleted), 'He said <so> loudly' -> 'He said  loudly'. Nested anchors PASS THROUGH: '<a href="#x">foo <a href="#y">bar</a></a>' is returned verbatim — the exact RSC-005 nested-<a> class the project fights, and sandbox_ai_html inherits it (sb('compare a<b>c here') is identically malformed). Root cause: _Sanitizer.handle_endtag (line 402) emits '</tag>' for any ALLOWED tag with no check that it was ever opened; handle_starttag (line 355) opens tags (incl. a-in-a) with no nesting stack; get_output (line 350) never closes tags left open at EOF. _AISandbox in html_sandbox.py repeats the same stackless pattern (handle_starttag:158, handle_endtag:177, get_output:155). This is the SOLE well-formedness boundary for note bodies: live consumers are inject.build_aside (scripts/inject.py:233 -> ships into epub_working/ + the EPUB aside), detectors.py:884-885 + promote.py:505 + batch_promote_xrefs.py:45 (sandbox_ai_html on UNPREDICTABLE LLM output — labels AND bodies), web_notes.py:162, web_content.py:394, core/preview.py:137, verse_of_day.py:356. No lint validates that a note body is well-formed HTML / rejects a raw '<', and (per this round's scope) per-edition builds never re-verify well-formedness after injection — so a single raw '<' or '>' in a note body (comparison operator, or the angle-bracket editorial notation common in textual-critical apparatus) or one stray LLM tag ships as malformed XHTML (epubcheck RSC-005/016 build break) OR silently deletes reader-facing text.
- **Proposed fix:** Make the sanitizer guarantee balanced, well-formed, nested-<a>-free output instead of streaming tags as parsed. In _Sanitizer (and _AISandbox): keep a stack of currently-open non-void emitted tags; in handle_starttag, refuse to OPEN an <a> while another <a> is already open (drop the inner <a> open/close tags, keep its inner text — matches the transparent-tag policy); push every other emitted open tag. In handle_endtag, only emit '</tag>' if it matches an open tag on the stack (pop to it, auto-closing any intervening open tags), and DROP a close tag that has no matching open. In get_output/close(), emit closers for every tag still on the stack. Add an adversarial well-formedness test asserting xml.dom.minidom.parseString(f'<r>{sanitize_html(x)}</r>') succeeds and that sandbox_ai_html(x) is a subset, for inputs: 'a<b>c', '<p>x<p>y', 'text <em>bold', 'hi </b> there', nested-<a>, '<<double>>', 'foo <bar baz qux'. (Note the content-loss cases cannot be fully recovered once '<' is consumed as a tag-start; the balancing fix at minimum stops the malformed-XHTML ship and the unbalanced-tag corruption.)
- **Verifier-refined fix:** OPTIONAL defense-in-depth hardening only (not a fix for live corruption; byte-identical on the current corpus since 0/91,712 bodies change). If pursued, give _Sanitizer and _AISandbox an open-tag stack: in handle_starttag refuse to open an <a> while another <a> is already open (drop inner <a> open/close, keep inner text); push every other emitted non-void open tag. In handle_endtag emit '</tag>' only if it matches an open tag on the stack (pop to it, auto-closing intervening tags) and drop unmatched close tags. In get_output/close emit closers for tags still on the stack. Add an adversarial well-formedness regression (xml.dom.minidom.parseString(f'<r>{sanitize_html(x)}</r>') succeeds; sandbox tag-set is a subset) for the listed inputs. Do NOT touch the marathon core; keep additive. Note this is a hardening of the runtime sanitizer; the existing base gates (test_all_master_html_is_wellformed_xml, check_nested_anchors, epubcheck) already prevent any malformed/nested-a note body from shipping.

### [15] html-integrity — low
**no_reviewer_scaffolding guard inspects only the body field (index 7); the label and attribution fields also render into shipped HTML and are unscreened**

- **Where:** `scripts/lint_rules.py:2586`
- **Panel:** kept 1/1 (confidence: medium)
- **Evidence:** _count_reviewer_scaffold_bodies checks only `body_node = elt.elts[7]` for '[reviewer:'. But a note tuple is (chapter, verse, suffix, anchor, kind, title, label, body[, attribution]); the LABEL (index 6) is emitted into shipped HTML by inject.build_aside as `<span class="note-label">{safe_label}</span>` (scripts/inject.py:241,254) and the ATTRIBUTION (index 8) becomes the cascade source byline (`html.escape(display)` at scripts/build_edition.py:3932). A '[Reviewer:' token that leaked into label or attribution would ship to the reader and the commit-time guard would not flag it — the guard does not cover the whole class of reader-facing text fields.
- **Proposed fix:** In _count_reviewer_scaffold_bodies, also test elt.elts[6] (label) and, when present, elt.elts[8] (attribution) for the case-insensitive '[reviewer:' token, so the guard screens every note field that can reach shipped HTML, not just the body.
- **Verifier-refined fix:** In scripts/lint_rules.py _count_reviewer_scaffold_bodies, extend the body-only check to every reader-facing free-text field — label (index 6, always present given the len>=8 guard) and attribution (index 8, present only when len(elt.elts)>8) — mirroring the existing isinstance(ast.Constant, str) test. E.g. inside the loop, after the existing len<8 skip:

    def _has_reviewer(node):
        return (
            isinstance(node, ast.Constant)
            and isinstance(node.value, str)
            and "[reviewer:" in node.value.lower()
        )
    fields = [elt.elts[6], elt.elts[7]]            # label, body
    if len(elt.elts) > 8:
        fields.append(elt.elts[8])                  # attribution (optional)
    if any(_has_reviewer(n) for n in fields):
        hits += 1

Do NOT add index 5 (title) — it is unpacked as `_title` in inject.py:765 and never rendered (the aside title comes from html_title_for(kind)). Also broaden the check's name/message/docstring (check_no_reviewer_scaffolding_in_bodies) to say it now screens all reader-facing note fields (label/body/attribution), not just the body, so the report points to the right field. This is a lint-only change: it cannot touch the marathon core, cannot alter build output, and keeps the 9 KJV editions byte-stable (the corpus currently has 0 '[reviewer:' tokens, so the guard still passes).

### [16] html-integrity — low (recalibrated from medium) · **WIN-owned (build_edition.py/epub_working)**
**Literal placeholder token TODO_CERTIFIER_NAME shipped in every edition's OPF (a11y:certifiedBy)**

- **Where:** `scripts/build_edition.py:1833`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** In patch_opf()'s edition_meta block: `'    <meta property="a11y:certifiedBy">TODO_CERTIFIER_NAME</meta>\n'` — interpolated unconditionally into content.opf for every build (patch_opf runs every edition). Confirmed shipping: `unzip -p exports/Ethiopian_Bible_ethiopian-tewahedo_v28a_*.epub content.opf | grep TODO_CERTIFIER_NAME` matches. This is the headline '(a) unrendered template/placeholder token' leak class — a literal TODO_ scaffold string in shipped accessibility metadata, making the WCAG-2.1-AA self-certification meta meaningless/invalid. It is the ONLY TODO_ token in the whole emission surface (swept build_edition.py + matter_pages.py + scripts/templates/*). It shipped undetected because NO gate covers emitted OPF metadata: check_nested_anchors/G1 run on the BASE only, and the no_reviewer_scaffolding lint (lint_rules.py:2596) is commit-time AND note-body-only. The base epub_working/content.opf has no such token — it is injected purely at build time.
- **Proposed fix:** Replace the placeholder with the already-resolved publisher/project name (pub = _resolve_publishing(edition) is in scope; pub['publisher_name'] is used escaped at line 1658): `f'    <meta property="a11y:certifiedBy">{_xml_escape(pub["publisher_name"])}</meta>\n'` (falls back to 'YHWH Ya'' Way Editions'/'Independent'). If self-certifying with no named third party, set it to the project name or drop the certifiedBy meta entirely (dcterms:conformsTo without a11y:certifiedBy is valid). Separately, extend the WIN dev/audit_output_hygiene.py artifact scanner to scan content.opf / *.opf metadata (not just HTML bodies) for TODO_/PLACEHOLDER/FIXME/[Reviewer:/{{...}} tokens, since no existing gate re-verifies emitted OPF.
- **Verifier-refined fix:** In scripts/build_edition.py:1833 either (a) drop the optional certifiedBy meta entirely (dcterms:conformsTo alone is valid per EPUB Accessibility), or (b) replace the literal with a real self-certification name. If keeping it, prefer a fixed project name over pub['publisher_name'] (which can fall back to 'Independent'): e.g. `'    <meta property="a11y:certifiedBy">YHWH Ya’ Way</meta>\n'`, or convert the line to an f-string `f'    <meta property="a11y:certifiedBy">{_xml_escape(pub["publisher_name"])}</meta>\n'` accepting the 'Independent' fallback. CRITICAL ADDITIONAL STEP the finder omitted: this changes the OPF bytes for all 9 KJV byte-stable cells and certifiedBy is not normalized out of _content_digest, so the change FAILS G1 unless you re-baseline the golden under review: `PYTHONUTF8=1 PYTHONPATH=. py -3 tests/test_kjv_golden_hash_gate.py --regen` (and re-run the byte-stability gate) — same re-bake+re-stamp pattern as the D3 Vulgate fix. Secondary (optional, WIN-side): extend the emitted-output hygiene scanner to scan *.opf metadata for TODO_/PLACEHOLDER/FIXME/{{...}} tokens, since no current gate re-verifies emitted OPF. Does not touch the marathon core; schema change is a value substitution, not a structural addition.

### [17] display-redundancy — low · **WIN-owned (build_edition.py/epub_working)**
**eink backmatter glossary repeats the category header + source byline + verse-return link across split-group footnotes of one verse (no part indicator)**

- **Where:** `scripts/build_edition.py:3857-3877`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** In _emit_backmatter_glossary_inner, when a category exceeds the Kobo ceiling it is chunked and packed into multiple groups, and each group is emitted as its own footnote via the loop: `for c_idx, group in enumerate(groups, start=1): ... parts.append(_study_glossary_footnote(group, cat=cat, ..., verse_back=verse_back, s2_group=s2_group, eink=eink))`. Every _study_glossary_footnote re-runs _emit_cascade_sections, which unconditionally re-emits the same `<p class="vn-cat-head">…{label}</p>` (3920-3923) and the same `<p class="vn-source-byline">…</p>` (3931-3932), and _study_glossary_footnote re-appends the same `verse_back` study-return link (3793) — with NO `(N/M)` part indicator (contrast the popup-unit path which adds `part_span` at 4394). I confirmed 16 single baked dict-easton asides exceed the 7,748-stripped-char split threshold in the shipped base (max 19,065), so for those verses the flagship ethiopian-tewahedo eink build prints e.g. "Historical / Cultural" + "Easton's Illustrated Bible Dictionary…" + "14:5" repeated verbatim across consecutive backmatter footnotes of the SAME category+verse. This is exactly the S2-cascade header/byline repetition the dimension flags as ungated, and only the first group (-c1) is the badge target so the repeats also read as unexplained duplicates.
- **Proposed fix:** In _emit_backmatter_glossary_inner, when `len(groups) > 1`, treat the continuation groups (c2+) as continuations: suppress the repeated `vn-source-byline` and the `study-verse-back` link on groups after the first (emit them once on c1), and add a `(c_idx/len(groups))` part span to the cat-head (mirroring the popup-unit `part_span` at 4394) so the repeat is labelled rather than redundant. Plumb a `part=(c_idx, n_groups)` kwarg through _study_glossary_footnote/_study_glossary_category_body/_emit_cascade_sections; keep single-group output byte-identical (part=None).
- **Verifier-refined fix:** Implement ONLY the part-indicator labeling (the safe, sufficient fix), and do NOT blanket-suppress the byline or the verse-back link. Concretely: in _emit_backmatter_glossary_inner, when `len(groups) > 1`, plumb a `part=(c_idx, n_groups)` kwarg through _study_glossary_footnote → _study_glossary_category_body → _emit_cascade_sections, and in _emit_cascade_sections append a part span to the cat-head only when part is not None and n_groups>1, mirroring the popup-unit form at 4394, e.g. `f' <span class=\"vn-part\">({c_idx}/{n_groups})</span>'`. Keep single-group output byte-identical (part defaults to None → no span, so all current single-group asides and every non-backmatter path are unchanged). Leave `vn-source-byline` emission exactly as-is — _emit_cascade_sections already prints the byline per actual source within each group, which is correct (group c2 may lead with a different `hist` source than c1, e.g. dict-easton vs topic-torrey; suppressing it would mis-attribute the continuation). Leave the per-aside `verse_back` study-return link on every group — each `<aside epub:type=\"footnote\">` is an independent navigate target and needs its own back-link. (If same-source byline de-dup is still wanted later, it must be source-boundary-aware: suppress group cN's leading byline only when its first source_key equals group c(N-1)'s last source_key — never a blanket suppress; but the part span alone resolves the 'unexplained duplicate' complaint for this low-severity item.) Scope is flagship ethiopian eink only; the 9 KJV goldens are unaffected.

### [18] options-completeness — low (recalibrated from medium)
**verse_marker_glyph is a fully-wired option with NO build read — a silent no-op (catholic-study ships ¶ with zero effect)**

- **Where:** `scripts/web_editions.py:419 (loader); customize.py:350 (control); api/editions.py:68,1256 (validator); editions.yaml:197 (shipped value)`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** The field has every wiring leg EXCEPT the build read: a /customize control (customize.py:350 `data-field="verse_marker_glyph" ... placeholder="·" title="leave empty for default verse number"`), a validator (api/editions.py:1256 `if field == "verse_marker_glyph" and len(val) > 4`), a loader (web_editions.py:419 `"verse_marker_glyph": e.get("verse_marker_glyph", "")`), and a save round-trip test (test_scripts.py:1633). But a repo-wide grep for `verse_marker_glyph`/`marker_glyph` consumption found NO read in build_edition.py, inject.py, generate_verse_popups.py, scripts/core/*.py, OR the preview renderer scripts/core/preview.py (which resolves its inline glyph per-kind from kinds.yaml at preview.py:108, not from this field). catholic-study sets `verse_marker_glyph: ¶` (editions.yaml:197) — a real shipped edition whose marker choice never reaches the EPUB. Checked all 4 plausible consumer files + a full --include=*.py grep; zero consumers.
- **Proposed fix:** Either (a) wire the build read so the chosen glyph actually replaces the verse-number marker in build_edition (the original intent), or (b) if marker_style/marker_badge_style superseded it, remove the control (customize.py:350) and the field from EDITABLE_TEXT_FIELDS so builders are not offered a setting that does nothing, and drop the dead `verse_marker_glyph: ¶` from catholic-study. Add a test asserting the chosen path (rendered-or-removed) so it cannot silently regress.
- **Verifier-refined fix:** Pick one of two paths; both are additive and keep the 9 KJV editions byte-identical because they leave the field at default "". (a) WIRE IT (matches original nu.2 intent): in build_edition.py read glyph = (edition.get("verse_marker_glyph") or "").strip() and, only when non-empty, substitute it for the verse-number marker at the verse-render site; the empty default preserves byte-identical output for every edition that does not set it (all 9 KJV). Add a test that a non-empty glyph appears in the rendered chapter and that an empty/unset glyph yields byte-identical output. (b) REMOVE THE DEAD OPTION: drop the /customize control (customize.py:350), remove verse_marker_glyph from EDITABLE_TEXT_FIELDS (api/editions.py:68), the EDITABLE set (api/editions.py:700), the validator branch (api/editions.py:1256-1257), the FieldSpec (validate_schemas.py:229) and the loader key (web_editions.py:419), delete verse_marker_glyph: ¶ from editions.yaml:197, and update test_scripts.py. Either way add a regression test asserting the chosen state (rendered or removed) so it cannot silently re-orphan. Do not touch the marathon core; no build/inject change is required for path (b).

### [19] options-completeness — low (recalibrated from medium)
**Four presentation fields omitted from api_customize_data loader — /customize can't display or round-trip persisted values (can't reset a non-default to default)**

- **Where:** `scripts/web_editions.py:393-499 (api_customize_data return; the 4 keys absent)`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** The per-edition dict returned by api_customize_data() surfaces title_page_style, marker_style, book_toc_ornament, topical_index_source, etc., but a diff of every customize.py `data-field` control against the loader keys shows FOUR controls have no loader entry: `chapter_number_format` (customize.py:417), `chapter_number_decoration` (customize.py:500), `note_popup_split_cap` (customize.py:480), `note_popup_split_byte_cap` (customize.py:486). All four have the OTHER legs: validator (api/editions.py:809/831/949/966), EDITABLE_TEXT_FIELDS membership, and an active build read with real effect (build_edition.py:6144 `(edition.get("chapter_number_format") or "digit")`, :6145 decoration, resolve_note_popup_split_cap:3357, resolve_note_popup_split_byte_cap). Because the loader omits them, each control renders its hard-coded fallback (`e.chapter_number_format||'digit'`, `||'plain'`, `?? ''`) regardless of the stored value. buildCustomizePayload (customize.py:1639) only sends fields where `curStr !== orig`, so a builder who set chapter_number_format=word in YAML sees 'digit', and CANNOT reset it to digit via the UI (the no-op is suppressed) — the on-disk non-default is stuck and invisible. Confirmed the 4 names appear nowhere in web_editions.py and no test asserts their presence in the payload.
- **Proposed fix:** Add the four keys to the api_customize_data per-edition dict mirroring the build resolvers: `"chapter_number_format": e.get("chapter_number_format", "digit")`, `"chapter_number_decoration": e.get("chapter_number_decoration", "plain")`, `"note_popup_split_cap": resolve_note_popup_split_cap(e)` and `"note_popup_split_byte_cap": resolve_note_popup_split_byte_cap(e)` (surface them RESOLVED so the tablet→0 split-cap default shows correctly, matching how marker_badge_style/target_reader are already surfaced). Add a round-trip test asserting the payload reflects a saved non-default for each.
- **Verifier-refined fix:** Add four keys to the api_customize_data per-edition dict (scripts/web_editions.py, inside the 393-499 block): "chapter_number_format": e.get("chapter_number_format", "digit"), "chapter_number_decoration": e.get("chapter_number_decoration", "plain"), "note_popup_split_cap": e.get("note_popup_split_cap"), "note_popup_split_byte_cap": e.get("note_popup_split_byte_cap"). Surface the two split caps RAW (NOT resolved): the YAML loader already yields None for an unset/cleared cap (stored as bare `null`) and an int when pinned, so the control's `?? ''` renders blank for 'use the calibrated default' and the stored integer when pinned — a true round-trip that preserves the unset-vs-pinned distinction and keeps the placeholder ('4400 / 8858 device-calibrated default') meaningful. (The finder's resolved variant collapses blank into the explicit default, killing that distinction and the placeholder, and needs cross-module imports.) The two selects use e.get with literal defaults exactly like the adjacent title_page_style/book_toc_ornament/topical_index_source rows. Add a loader round-trip test asserting the /customize payload reflects a saved non-default for each of the 4 fields. Change is additive and API-only — 9 KJV editions remain byte-stable.

### [20] os-binary-parity — low
**Linux AppImage ships an unbranded teal placeholder icon while Win/Mac bundle the branded icon**

- **Where:** `dev/build_appimage.sh:78-97`
- **Panel:** kept 1/1 (confidence: high)
- **Evidence:** `if [[ -f "content/covers/icon.png" ]]; then cp "content/covers/icon.png" "$APPDIR/YHWH.png"` else it generates a solid-color placeholder PNG (`raw = b''.join(b'\x00' + b'\x2c\x4a\x6e' * w ...)` = RGB 44,74,110). content/covers/icon.png does NOT exist in the repo (confirmed: content/covers/ has only the per-edition .jpg covers), so the placeholder branch runs — the v0.1.0 AppImage shows a teal square in file managers/launchers, whereas Windows bundles assets/icons/program_icon.ico (launcher.spec:216) and macOS bundles assets/icons/YHWH.icns (launcher.spec:194). Branded PNGs already exist: assets/icons/icon_256.png, icon_512.png, icon_1024.png.
- **Proposed fix:** Point the icon source at an existing branded PNG, e.g. `if [[ -f assets/icons/icon_256.png ]]; then cp assets/icons/icon_256.png "$APPDIR/YHWH.png"` (fall back to the generated placeholder only if absent), giving the Linux AppImage the same brand identity as the Win/Mac binaries.

## Refuted / dropped (do NOT re-litigate)

- (cross-module) §6.1 canonical-order encoder lint omits encode_per_chapter_tokens, a live editions.yaml writer
- (cross-product) `computer` is an orphan target_reader: a user-selectable reader option with no FORMAT_MATRIX cell, no build behaviour, no catalog asset, no test
- (cross-product) `computer` is an orphan reader target: UI-pickable in two surfaces but no FORMAT_MATRIX cell, no catalog asset, and zero build behavior (silent everywhere-alias)
- (cross-product) `distinct_targets` helper would re-introduce the retired `--target-reader kindle` variant as a base build; its docstring contradicts `base_build_target`
- (marker-logic-xreader) numbers marker_style study-note markers (note-ref → #note-X) have no orphan/target-resolution gate
- (whitespace-pagebreak) Empty-verse repair silently drops un-fillable verses with no count/diagnostic; a real-text verse emptied by a future filter/merge would ship indistinguishable from an intended placeholder
- (whitespace-pagebreak) _merge_scripture_base_files aborts to a 400KB-split no-op on malformed input without any warning or stat, silently reintroducing per-book mid-book spine seams
- (display-redundancy) Same source byline printed twice in one popup when an attribution is shared across two categories
- (options-completeness) `computer` target_reader is offered in both the wizard and /customize but has no build branch — it silently produces a reader-identical build to `everywhere` (dead/meaningless choice)
- (options-completeness) target_reader=computer is offered + validated but has no build branch — byte-identical to 'everywhere' (dead/meaningless enum value)
- (os-binary-parity) Native-shell deps (pywebview, and macOS pyobjc bridges) are declared nowhere — a clean rebuild silently ships browser-mode-only Win/Mac binaries
- (os-binary-parity) PyInstaller version pinned exactly in Linux CI but a loose range for Win/Mac — the 3 OS binaries can be frozen with different bootloaders

## Completeness-critic — whole-class sweep seeds (next-round)

### Commentary-store enumeration parity — the WHOLE 6-store class, not just the bookcode_canonical lint the round found
- **Why:** The round surfaced only ONE omission (bookcode_canonical lint at scripts/lint_rules.py:2277-2281 lists 5 of 6 *_commentaries stores, missing patristic). But the same omits-one-store defect exists in a DIFFERENT consumer: scripts/core/detectors.py ALL_DETECTORS (line 1415) registers PatristicCommentaryDetector/Ethiopian/Protestant/Catholic/Reformation = 5 detectors, with NO RabbinicCommentaryDetector — yet content/sources/rabbinic_commentaries.json is a live store (has _meta + entries) and scripts/core/sources_commentary.py exposes all 6 facades. So the rabbinic corpus is never auto-detected/prospected. The cross-module dim (2/3) stopped at the lint and did not sweep the rest of the consumer matrix. This is the explicit 'sweep the whole class, not one site' hazard the prompt warned about.
- **Lens:** Enumerate the canonical 6 *_commentaries.json stores, then for EVERY consumer that should handle all of them — detectors.ALL_DETECTORS (scripts/core/detectors.py), the bookcode_canonical store list (lint_rules.py:2277), prospect.py auto-discovery, scripts/core/sources.py facade, citation_index/bibliography/provenance_tiers, validate_schemas — diff the handled set against {patristic,ethiopian,protestant,catholic,reformation,rabbinic}. File one finding per consumer that drops a store (silent corpus exclusion).

### Client-side innerHTML render-boundary class across the 27 desktop UI templates (security found only ONE)
- **Why:** The security dim's surviving finding flagged the note-editor READ/render path loading stored note bodies via innerHTML with no sanitization (boundary-doctrine gap), but it found that single site. There is a broad class of `el.innerHTML = DATA.map(...)` in scripts/templates/*.py (audit.py:173/193/228, audit_log.py:174, compare.py:124/167, build_my_bible.py:541/584/638, etc.) that interpolate stored note/translation/book text. Spot-check shows it is MIXED: compare.py uses escHtml() throughout (safe), while the note-editor path does not — so a single-pass finder that found one cannot conclude the rest. No systematic classification of escaped-vs-raw was done.
- **Lens:** grep -n 'innerHTML' scripts/templates/*.py and for each site trace whether the interpolated value is stored user/note/source content and whether it passes through esc()/escHtml(). Report every site that renders stored content raw (same gap as the note-editor read path); state the count checked and which are safe.

### matter_pages.py render_* front-matter generators — per-edition metadata interpolation + cross-edition topic-ref validity
- **Why:** html-integrity (3/3) named 'the matter_pages render_* family' but every survivor landed in html_sanitize/html_sandbox/no_reviewer_scaffolding/OPF — none in matter_pages itself. matter_pages.py has a rich generator family (render_copyright_page:23, render_dedication_page:177, render_your_edition_page:431, render_closing_colophon_page:807, render_reference_tables_page:685, render_topical_index_page:953, render_merged_topical_index_page:982) that interpolate edition title/byline/copyright/license/stats and note-derived topics directly into shipped HTML. Two untested risks: (a) unescaped f-string/.format interpolation of edition- or source-supplied text; (b) build_merged_topic_index (883) emitting topic entries whose verse refs point at books spliced OUT of the current edition's canon (the canon-filter splice edge case guard #5 warns about).
- **Lens:** Read each render_*/build_*_topic_index in matter_pages.py; flag any interpolation of edition/source/note text not run through html.escape, and any topical-index ref that is not filtered against the edition's canon_books before rendering (compare to reading_plans.validate_plan_refs which DOES filter). Verify on a canon-filtered edition (catholic-study).

### Cover-generation cross-OS font determinism + Ethiopic fallback
- **Why:** os-binary-parity returned only 1/3 (the AppImage placeholder icon) and explicitly scoped to launcher.spec/paths.py/build scripts — it never touched the GENERATED cover assets. scripts/generate_edition_covers.py resolves title fonts via hardcoded Windows absolute paths (e.g. C:\Windows\Fonts\ebrima.ttf at line 162, nyala) for the Ethiopic title face, with _font_path_ethiopic()/_load_font() silently falling back to an in-repo NotoSerifEthiopic or PIL's pathless default when those paths are absent. On Mac/Linux those C:\ paths never exist → covers are rendered with a DIFFERENT font than Windows (non-deterministic across OS, or tofu if the in-repo Ethiopic face is not reached). The same OS-path dependence likely affects generate_book_title_covers.py.
- **Lens:** Trace _font_path_ethiopic/_font_path_for_text/_load_font fallback chains in generate_edition_covers.py and generate_book_title_covers.py; assert the bundled content/assets/fonts/*.ttf is ALWAYS the resolved face (never a C:\ absolute that no-ops off-Windows), and that a cover built on Mac/Linux is byte-identical to the Windows render. Pairs with the os-binary parity gate.

### eink glyph-substitution surface coverage — the whole glyph-emit class
- **Why:** A surviving finding showed the numbers marker_style ships Cardo-unsafe category glyphs as tofu because eink substitution runs only in the badge path. The substitution helpers are scripts/core/eink_glyphs.eink_category_badge_glyph and build_edition.py:_eink_safe_note_sym (2857, applied at 4205); matter_pages symbol_legend (278) and the badge path are covered. But the round fixed one path, not the class. Other glyph-emit surfaces were not enumerated against the 2 helpers: verse_marker_glyph (the ¶ option), chapter_number_format glyphs, render_reference_tables_page (matter_pages:685), per-book/per-chapter symbol overrides, the kindle endnote markers, and cover glyphs.
- **Lens:** grep every site that emits a categories.yaml `symbol` or a note-sym into eink output (search for symbol/glyph emission in build_edition.py, inject.py, matter_pages.py, kindle_post.py) and cross-check each against eink_category_badge_glyph/_eink_safe_note_sym coverage; report each glyph surface that ships an unsubstituted (tofu-risk) glyph on the eink target.

### editions API validator leg (api_save_edition_meta) enum completeness — complement of the loader gap the round found
- **Why:** options-completeness (2/4) found the LOADER leg broken (api_customize_data omits 4 presentation fields) and a build-read no-op (verse_marker_glyph). The complementary leg — the SAVE/VALIDATOR in scripts/api/editions.py:795 api_save_edition_meta (a 65KB file with scattered enum checks at 1278/1403/1466/1490) — was not enumerated field-by-field. A field accepted with no enum validation persists an invalid value that the build then mis-handles or silently drops; an enum validator whose allowed-set has drifted from build_edition's handled set rejects a legal choice.
- **Lens:** List every editions.yaml presentation field, then for each locate its branch in api_save_edition_meta and compare the accepted enum set against the values build_edition actually handles (FORMAT_MATRIX/TARGET_READERS/marker styles/note_popup_style/etc.). Flag fields with no validator and validators whose enum diverges from the build's handled set.

### whitespace-pagebreak returned 0/2 — wrong scope; re-lens onto matter_pages page emission (empty/blank pages)
- **Why:** This dim surfaced zero survivors, which is suspicious given the dim was scoped strictly to build_edition.py spine/merge functions. It never looked at the front/back-matter page emitters in matter_pages.py, where the empty-input path is the real risk: inject_reading_plans_page (1326) when all refs are canon-dropped, render_topical_index_page when topical_index_source yields no matching topics for the edition, render_symbol_legend_page / render_dedication_page when their inputs are empty — each can ship an empty/near-empty page shell or a stray page-break seam between matter and content.
- **Lens:** For each inject_*_page / render_*_page in matter_pages.py, trace the empty-input branch: does it SKIP emitting the page, or emit an empty shell into the spine (a blank page + a dead ToC entry)? Verify on a canon-filtered edition where reading-plan/topical refs get dropped. Reuse audit_spine_breaks but extend it to matter-page files, not just scripture spine pieces.

### Per-edition verse-COUNT / anchor-existence invariant (distinct from D8 reading-flow ORDER) for reading-plan and topical refs
- **Why:** D8 validated reading-flow ORDER (1299 ch / 75 books) but no dim checks that each edition renders the EXPECTED verse COUNT per chapter, nor that reading-plan/topical refs resolve to anchors that actually EXIST in the built spine. reading_plans._ref_status (scripts/core/reading_plans.py:216) filters only at BOOK level (book not in canon_books -> drop) — it does NOT verify the chapter/verse anchor exists, so a plan day like 'rev 23:1' (Revelation has 22 ch) or a chapter beyond an edition's range passes the book check and ships a dead nav target. Likewise a mid-verse merge / empty-verse repair / canon-splice could drop or duplicate a verse id without altering chapter order (so D8 would pass).
- **Lens:** Build a gate that (a) compares built-spine verse-id counts per (edition,book,chapter) against scripts/core/canonical_verse_counts.py, surfacing any nonzero delta; and (b) resolves every reading-plan and topical-index ref against the ACTUAL set of anchor ids in the built spine (not just book-in-canon), reporting refs that point past a chapter/verse the edition doesn't render.


## Engine phased fixes plan (raw — WIN reconciles into the authoritative plan)

## Executive summary

Round 16 produced 32 deduped findings; after adversarial refutation, 20 are verified survivors and 12 were refuted. Of the 20 survivors, 0 are UNVERIFIED (their skeptic panel was empty even after retry, so they were auto-confirmed here, not skipped). By severity the survivor set is {"medium": 2, "low": 18}; all 20 are bug/correctness/security/debt/test/doc findings and 0 are optimization recommendations. The codebase is healthy: no high/critical findings, the two mediums are both half-completed retirements that leave a user-reachable build path producing a non-conforming artifact (Kindle export missing `make_kindle_safe`; eink study-return teleporting to chapter start for 13 anchored books), and the remaining 18 are guard gaps, latent OOM/RAM hazards on the 16 GB box, dead/unwired options, and one shipped placeholder token. Two pairs are the same defect surfaced twice — #7≡#8 (patristic lint gap) and #9≈#12 (glossary str-path 3× RAM) — and are folded into single work items below without changing the survivor count.

## Phased fixes

Ordered safest/most-foundational first: commit-time guards, then security boundaries, then silent-data-loss/stat correctness, then byte-safe builder-robustness, then API/options, then behavior-changing build-path fixes (byte-stability obligations), then OS-binary parity.

### Phase 1 — Commit-time lint/guard completeness (additive, no build path, byte-stable)

- [ ] **low — bookcode_canonical lint omits the 6th commentary store (patristic_commentaries.json)** — `scripts/lint_rules.py:2276-2282` (folds findings #7 ≡ #8). Replace the hardcoded `json_specs` list with a glob so future stores auto-screen: `json_specs = sorted(str(p.relative_to(REPO)) for p in (REPO / "content/sources").glob("*_commentaries.json"))`; fix the stale "5 stores" wording at `lint_rules.py:2269` and `scripts/core/sources_commentary.py:305` → "6". **Guard:** add a pytest asserting every `content/sources/*_commentaries.json` appears in the screened set (only needed if the list form is kept; the glob is self-maintaining — prefer the glob since this is a ★BUGCLUSTER that recurs every ingest). Store is currently all-canonical so lint stays green. **No build path.**
- [ ] **low — no_reviewer_scaffolding guard inspects only body (index 7); label + attribution also ship to readers** — `scripts/lint_rules.py:2586`. Extend `_count_reviewer_scaffold_bodies` to also screen label (`elt.elts[6]`, always present given the len≥8 guard) and attribution (`elt.elts[8]`, only when `len(elt.elts)>8`), reusing the existing `isinstance(ast.Constant, str)` test; do NOT add index 5 (title — never rendered). Broaden the check name/message/docstring to "label/body/attribution". **Guard:** this IS the commit-time guard; corpus has 0 `[reviewer:` tokens so it passes immediately. **No build path.**

### Phase 2 — Security boundary hardening (additive, no build path, byte-stable)

- [ ] **low — note-editor render boundary loads stored bodies via innerHTML with no sanitization (only SAVE is gated)** — `scripts/templates/index.py:315` (+ list excerpt at `:210/:218`); server read at `scripts/web_notes.py:83`. Close the READ boundary: either sanitize on the API read path (`sanitize_html` over body in `api_notes`/`tuple_to_dict`) OR run the existing allowlist serializer `normalizeBody(d.body)` (index.py:348) before assigning at line 315. Replace the regex tag-strip excerpt with `textContent`/real escape. Bulk-ingest bodies (Nave's/Easton's/Torrey/commentary) bypass the save-path gate, so this removes the dependence on the corpus staying clean. **Guard:** add a test that a body containing `<img src=x onerror=…>` is neutralized on render. **No build path.**
- [ ] **low — SSRF allowlist in http.py bypassed by HTTP redirects (initial URL checked, redirect target not re-validated)** — `scripts/core/http.py:167-172` (get) and `:232-240` (put), both shared `urlopen` sites. Add a per-call `_AllowlistRedirectHandler(urllib.request.HTTPRedirectHandler)` whose `redirect_request` re-runs `_check_allowlist(newurl, allowlist)`, build a `_validating_opener(allowlist)`, and change both signatures from `urlopen=urllib.request.urlopen` to `urlopen=None` with `if urlopen is None: urlopen = _validating_opener(allowlist)` after the existing check. Do NOT default to no-follow (archive.org/ebible.org legitimately 30x). **Guard:** regression test injecting a stub 302 from an allowlisted to a disallowed host and asserting `SSRFBlockedError` propagates uncaught (no retry). **No build path.**
- [ ] **low (optional defense-in-depth) — sanitize_html / sandbox_ai_html emit unbalanced/nested-`<a>` output and silently drop note text** — `scripts/core/html_sanitize.py:350-351,376-413` and `scripts/core/html_sandbox.py:155-183`. Give `_Sanitizer`/`_AISandbox` an open-tag stack: refuse a second open `<a>`, emit a close only for a matching open tag, close still-open tags at EOF. Byte-identical on the current corpus (0/91,712 bodies change), and the base gates (`test_all_master_html_is_wellformed_xml`, `check_nested_anchors`, epubcheck) already block shipping — so this is hardening, not a live fix. **Guard:** adversarial well-formedness regression (`xml.dom.minidom.parseString(f'<r>{sanitize_html(x)}</r>')` for the listed malformed inputs). **No build path** (runtime sanitizer; corpus-clean ⇒ inject output unchanged).

### Phase 3 — Silent-data-loss / stat & cache correctness (additive, byte-stable)

- [ ] **low — at-scale driver progress stat over-counts written candidates (ignores append_candidates dedup) — whole class, 7 sites** — `scripts/core/at_scale_base.py:307-311, 452-458` + 5 sibling drivers. Make `append_candidates` return `len(new_dicts)` on write / `0` on the empty/nothing-new branches (replacing both `return None` and `return out_path`); update write-branch consumers to `n = append_candidates(...); if n: files_written += 1; candidates_written += n` at at_scale_base.py:309/456, `run_xref_at_scale.py:87-90`, `run_naves_at_scale.py:80-83`, `run_torrey_at_scale.py:113-115` (write branch only — KEEP dry_run `len(...)`), `run_ethiopian_at_scale.py:101-104` (write branch only), and `run_kenyon_at_scale.py` (its own `write_queue` → int, used at :147-148). CRITICAL: `prospect.py:161-163` needs the Path (prospect.py:349 `.relative_to`) — special-case its wrapper to reconstruct the path locally and keep `Path|None`. **Guard:** update the 2 contract tests (`tests/test_mint10_phase4.py:96` → `assert ret == 1`; `:111` → `== 0`). **No build path** (offline ingest tooling). Lighter acceptable alternative: leave as a documented cosmetic over-count — but if fixed, fix ALL 7 sites.
- [ ] **low — edition_stats cache key (_edition_signature) omits the 3 popup-cap fields → stale popup-language set baked into the EPUB About page after a runtime cap edit** — `scripts/core/edition_stats.py:50-79`. Add `"target_reader"`, `"max_popup_languages"`, `"popup_languages_capped"` to the `_edition_signature` key tuple (the documented invalidation mechanism). Defense-in-depth for the class: call `edition_stats.cache_clear()` beside the existing `matrix_mod.compute_matrix.cache_clear()` in the edition-record edit paths (`scripts/api/editions.py:389/591/1296/1387`, `scripts/api/customize.py:80/123`), mirroring notes_io. **Guard:** test that flipping an edition to `target_reader=kindle` changes the signature and yields the capped `popup_languages`. **Build path note:** affects the rebuilt-kindle About page only; for the 9 KJV (cap fields unset) a clean build is byte-identical — confirm KJV golden hashes unchanged.

### Phase 4 — Builder-robustness / OOM hardening (build path, byte-identical for in-spec inputs)

- [ ] **low — study-glossary str-path splitter holds ~3 full-size copies + the byte-streamer's whole-document fallback re-opens the flagship-eink OOM** — `scripts/build_edition.py:5191-5236` (helpers 4783-4793, 5092-5116), reached via fallback at `:5393` (folds #9 ≈ #12). Do NOT take the offset-rewrite (it changes the D5 reference impl proven byte-identical at 690/690 — would force re-proving str==from-file + catholic-study eink byte-stability). Instead, in `_iter_study_glossary_pieces_from_file`: route `len(raw) <= _GLOSSARY_STREAM_BYTE_THRESHOLD` to the str path, and on `_GlossaryStructureError` for a >threshold file RAISE a clear `RuntimeError` pointing at the byte-twin patterns (build_edition.py:5259-5262) instead of decoding the ~480 MB monolith. Belt-and-suspenders (byte-output-neutral): rebind `parts = None` after `:5213` and `sec_parts = None` after `:5218` so the existing `del body`/`del inner` actually free the copies on the in-spec path. **Guard:** test that a >threshold file with mutated glossary markup raises the actionable error (not OOM). **Build path:** additive on a production-dead fallback + no-op frees ⇒ 9 KJV + flagship byte-identical; confirm a fresh catholic-study eink build still passes.
- [ ] **low — enrich_nav_chapters reads the ~480 MB study-glossary monolith whole (no EINK_STUDY_BACKMATTER_STEM skip) → OOM when reader_native_toc_chapters is enabled on the eink flagship** — `scripts/build_edition.py:6646-6647`. Add `if p.stem == EINK_STUDY_BACKMATTER_STEM: continue` as the first statement inside the `for p in list_split_html_files(tmp):` loop, mirroring the two sibling passes (retarget_demoted_toc_anchors, `_merge_mid_verse_breaks`) that already guard the stem. **Guard:** none required (byte-identical — glossary holds no `ch-bNN-cMM` ids and sorts last). **Build path:** byte-identical; latent today (option default-off).
- [ ] **low — --all ThreadPool worker count is target-blind (fixed at 5) with no MemoryError backoff → concurrent in-process eink builds co-reside the glossary monolith and can OOM** — `scripts/build_edition.py:8716`. Compute `workers = 1 if args.target_reader == "eink" else min(len(targets), 5)` and pass to `ThreadPoolExecutor`. Drop the psutil alternative (not a declared dependency). Update the stale "CONFIRMED-OPTIMAL (round-7)" comment at `:8708-8710` to note the eink carve-out. **Guard:** worker-count assertion test for the eink vs non-eink branch. **Build path:** worker count is scheduling only ⇒ output bytes deterministic per edition; 9 KJV byte-identical.

### Phase 5 — Options completeness (API loader, additive, byte-stable)

- [ ] **low — four presentation fields omitted from api_customize_data loader → /customize can't display or reset persisted non-defaults** — `scripts/web_editions.py:393-499`. Add `chapter_number_format` (default `"digit"`), `chapter_number_decoration` (default `"plain"`), and the two caps `note_popup_split_cap`/`note_popup_split_byte_cap` surfaced RAW (`e.get(...)`, no resolve — preserves the None=unset vs int=pinned distinction the `?? ''` placeholder relies on). **Guard:** loader round-trip test asserting the payload reflects a saved non-default for each of the 4 fields. **No build path** (API-only). 
- [ ] **low — verse_marker_glyph is a fully-wired option with NO build read (silent no-op; catholic-study ships `¶` with zero effect)** — loader `scripts/web_editions.py:419`; control `customize.py:350`; validator `api/editions.py:1256`; value `editions.yaml:197`. Pick one: (a) WIRE IT — read `glyph = (edition.get("verse_marker_glyph") or "").strip()` in build_edition.py and substitute at the verse-render site only when non-empty (empty default ⇒ byte-identical for all 9 KJV); or (b) REMOVE THE DEAD OPTION across control/EDITABLE_TEXT_FIELDS/validator/FieldSpec/loader/editions.yaml/test. **Guard:** regression test asserting the chosen state (rendered vs removed) so it can't silently re-orphan. **Build path:** path (a) touches build but default-`""` keeps 9 KJV byte-identical (prove); path (b) is no build change.

### Phase 6 — Behavior-changing build-path fixes (byte-stability proof obligations)

- [ ] **medium — Kindle target: the desktop /customize build path produces the retired E999-failing variant (`make_kindle_safe` never applied)** — `scripts/build_edition.py` (is_kindle_target tunings at 1021/1364/1736/4673; build_one tail ~8558-8580; cf. `scripts/build_format_matrix.py:205-220`). Fix at the single chokepoint both failing paths share — build_one's tail, gated on `is_kindle_target(edition)`: after `build_epub` writes `output_path` (both the frozen in-process branch and the dev subprocess branch), run `from scripts.core.kindle_post import make_kindle_safe, verify_kindle_safe`, strip+OCF-re-zip into a temp, `replace` the output, and `raise RuntimeError` if `verify_kindle_safe(output_path)` returns failures. Do NOT delete the UI option. **Guard:** build a tiny edition with `target_reader=kindle` through build_one and assert `verify_kindle_safe(artifact) == []`. **Build path:** gated on is_kindle_target ⇒ byte-identical for the 9 KJV + everywhere Ethiopian (matrix and build_kindle.py never reach is_kindle_target) — prove KJV golden hashes unchanged.
- [ ] **medium — eink study-glossary return link teleports to chapter start (not the verse) for the 13 Strategy-B books that DO carry v-anchors (psa/job/kings/chronicles/…)** — `scripts/build_edition.py:3749-3761` (decision at 3757), threaded through `_emit_backmatter_glossary_inner:3799` (line 3808) and the call site at 4336. Gate the return target on ACTUAL per-chapter v-anchor presence, not `strategy=='B'`: add `verse_anchored: bool | None = None` to `_study_verse_return_link` (legacy strategy inference as the default so the direct-call test is unchanged), thread a `verse_anchored` flag from the per-chapter `f'id="v-{code}-{ch}-' in text` signal already computed near line 4153. Do NOT hardcode an anchor-less book list (aes/2es carry anchors). **Guard:** KEEP the jub/gen tests; ADD `_study_verse_return_link('psa',119,160, verse_anchored=True)` → `#v-psa-119-160` and a `verse_anchored=False` case → ch-anchor. **Build path:** eink-only; 9 KJV are non-eink ⇒ byte-identical (prove golden) + fresh ethiopian eink must pass the RSC-012/orphan gate.
- [ ] **low — numbers + eink: Cardo-unsafe note-sym category glyphs ship as tofu boxes (eink glyph substitution runs only in the badge path)** — `scripts/build_edition.py:4204-4205` (gated by marker_style at 8429). Add an eink-gated repair pass in the repair_texts block (~8445-8463) that runs `_eink_safe_note_sym` over `class="note-sym"` texts, GATED to `resolve_target_reader(edition) == "eink" and marker_style != "badge"` (every shipped edition is badge ⇒ no-op for them). Reuse the existing module-level regex/substitution (idempotent). **Guard:** test that a numbers+eink build substitutes the diamond/house/command/sparkle/star/double-bar glyphs. **Build path:** non-badge eink only ⇒ 9 KJV + flagship eink superset byte-identical / no golden regression (prove).
- [ ] **low — eink backmatter glossary repeats the category header + source byline + verse-return link across split-group footnotes of one verse (no part indicator)** — `scripts/build_edition.py:3857-3877`. Implement ONLY the part-indicator: when `len(groups) > 1`, plumb `part=(c_idx, n_groups)` through `_study_glossary_footnote → _study_glossary_category_body → _emit_cascade_sections` and append a `<span class="vn-part">({c_idx}/{n_groups})</span>` to the cat-head, mirroring the popup-unit form at 4394. Do NOT blanket-suppress the byline (group cN may lead a different source) or the per-aside verse-back link (each `<aside>` is its own navigate target). Single-group output stays byte-identical (`part` defaults to None). **Guard:** test that a >7,748-stripped-char dict-easton aside emits `(1/2)`/`(2/2)` and that single-group asides are unchanged. **Build path:** flagship ethiopian eink only ⇒ 9 KJV goldens unaffected (prove).
- [ ] **low — literal placeholder token `TODO_CERTIFIER_NAME` shipped in every edition's OPF (a11y:certifiedBy)** — `scripts/build_edition.py:1833`. Either drop the optional `certifiedBy` meta (dcterms:conformsTo alone is valid) or replace with a real name, e.g. `<meta property="a11y:certifiedBy">YHWH Ya' Way</meta>` (prefer a fixed project name over `pub['publisher_name']`'s "Independent" fallback). **Guard:** extend the emitted-output hygiene scanner to scan `*.opf` metadata for `TODO_`/`PLACEHOLDER`/`FIXME`/`{{…}}` tokens (no current gate re-verifies emitted OPF). **Build path — REAL byte obligation:** this changes OPF bytes for all 9 KJV byte-stable cells and certifiedBy is NOT normalized out of `_content_digest`, so it FAILS G1 — re-baseline the golden under review (`PYTHONUTF8=1 PYTHONPATH=. py -3 tests/test_kjv_golden_hash_gate.py --regen` + re-run the byte-stability gate), same re-bake+re-stamp pattern as the D3 Vulgate fix.

### Phase 7 — OS-binary parity (distribution, no byte-stability gate)

- [ ] **low — Linux AppImage ships an unbranded teal placeholder icon while Win/Mac bundle the branded icon** — `dev/build_appimage.sh:78-97`. `content/covers/icon.png` does not exist so the placeholder branch runs; point the icon source at an existing branded PNG: `if [[ -f assets/icons/icon_256.png ]]; then cp assets/icons/icon_256.png "$APPDIR/YHWH.png"` (fall back to the generated placeholder only if absent). **Guard:** none required; verify the built AppImage carries the branded `YHWH.png`. **No KJV byte path** (Linux binary); note "deploy" = REBUILD the AppImage from source then re-publish the asset + SHA256.

## Optimization decisions

Zero standalone optimization recommendations survived this round (the JSON optimization list is empty). The table below records the verdicts on the areas commonly re-raised, plus the resource items that are tracked as bug-class builder-robustness findings above rather than as separate optimizations.

| Area | Verdict | Recommendation |
|---|---|---|
| zip `compresslevel` 9→6 | confirmed-optimal | DECLINED on merits (enlarges every EPUB 1-3% to save ~30 s/build); quality output > build speed. Do not re-propose. |
| Split `web.py` / `build_edition.py` for size | confirmed-optimal | DECLINED — large files of small cohesive functions. |
| Glossary byte-streamer (D5) | confirmed-optimal | Proven 690/690 byte-identical @255 MB + G5 PASS. The residual ~3× str-path RAM + whole-document fallback OOM is the Phase-4 builder-robustness fix (#9≈#12), NOT a re-diagnosis of D5. |
| `--all` ThreadPool worker count | change | eink carve-out to 1 worker (Phase 4, #11) — the round-7 "CONFIRMED-OPTIMAL" pin predates the eink glossary monolith. |
| `_decode_nl` chained `.replace` | optional/marginal | Only the D5-dead fallback decodes large input; if touched use `re.sub(r"\r\n?", "\n", …)` (never `splitlines()/join` — corrupts U+2028 vnote separators). Low priority. |
| Data-as-Python-tuples / no database | confirmed-optimal | KEEP PYTHON; deliberate design — do not propose a DB. |
| Voyage embeddings integration | confirmed-dropped | Only key-rotation security survives; no paid-API path. NO paid-API constraint stands. |
| Marathon core (`build_standalone.py`, `core/manuscript_*`, `po_vision_store.py`, `content/manuscript/**`, patrologia, GAPS/) | OFF-LIMITS | No edits, no optimization, no flagging as defect unless an outright crash. |

## Constraints carried

- **Never touch the marathon core**: `scripts/build_standalone.py`, `scripts/core/manuscript_*.py`, `scripts/core/po_vision_store.py`, `content/manuscript/**`, `content/translations/sources/patrologia/**`, `GAPS/`. None of the fixes above touch these.
- **9 KJV editions must stay byte-stable**: every build-path fix is gated/additive so the 9 KJV (and the shipped everywhere Ethiopian superset) are byte-identical when the new behavior is unset — EXCEPT the `TODO_CERTIFIER_NAME` OPF change (Phase 6), which intentionally changes OPF bytes and therefore requires a golden re-baseline (`test_kjv_golden_hash_gate.py --regen` + byte-stability gate), the same re-bake+re-stamp pattern as the D3 Vulgate fix.
- **Additive schema only**: new edition fields/kwargs default to the pre-existing behavior; no field removals except the optional `verse_marker_glyph` removal path (which itself ships no build-output change).
- **Atomic writes**: any note/edition writes go through `notes_io.atomic_write` / `ensure_backup`.
- **No paid API**: drop the psutil and Voyage-embedding alternatives; lint stays import-light (glob, not corpus-class imports).
- **Prefer commit-time lint over pytest-only** for ingest-recurring invariants: the patristic lint gap (Phase 1) and the reviewer-scaffold field gap (Phase 1) are fixed in `lint_rules.py` so they fire at commit on every future ingest, not only under a test sweep.
- **Bandwidth-first save cadence (RULES §4)**: each fix lands as a LOCAL COMMIT; the full 5-leg push (commit + origin + github + E:/F: bundles) happens only at a milestone, not per fix.
- **FINDINGS-ONLY**: this is the plan. STOP here — do NOT apply any fix (user marching order 2026-06-08).
