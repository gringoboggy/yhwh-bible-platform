[
 {
  "area": "marker-logic-xreader (0/2) and html-integrity (1/4) ran as SOURCE-ONLY finders, but their core failure modes are only observable in a BUILT artifact — so the dims that most needed the WIN harness under-delivered.",
  "why": "Every surviving finding this round is source-derived. Yet the marker dim's headline asks (a badge/number/vn-link whose target id is ABSENT in the piece the reader actually lands in after apply_file_split; a dead badge after the kindle display:none strip; a note shipped with no reachable marker) and the html-integrity asks (a nested <a> REINTRODUCED by a per-edition post-pass after the base-only check_nested_anchors ran; an unrendered {{}}/{%%}/repr token reaching emitted HTML) cannot be confirmed by reading build_edition.py — they require parsing the assembled, split, link-rewritten EPUB/kepub/kindle output. The intended detector (dev/audit_output_hygiene.py + the extended G4) is the WIN build-inspect harness, whose results are not represented in this survivor set. 0/2 and 1/4 are the signature of an artifact dim that ran without its artifact.",
  "suggested_lens": "Build one ethiopian-eink .epub + its kepub, one catholic-study eink, and one kindle (everywhere base -> kindle_post.make_kindle_safe) artifact, then for each: (1) collect every href=\"#id\"/href=\"file#id\" from badges/vn-links/numbers markers and assert the target id exists in the SAME piece (eink/kindle) or the named cross-file piece (after apply_file_split rewrite); flag every orphan with (edition, format, reader, piece, id); (2) run scripts/check_nested_anchors on the per-edition BUILT pieces, not just epub_working/, to catch a post-pass-reintroduced nested <a>; (3) regex every emitted .xhtml for residual {{, {%, <class ', &#x27;, and raw stray < / > inside text nodes."
 },
 {
  "area": "scripts/core/kindle_post.py (753 lines) — the SHIPPED Kindle reader recipe — was under-read by the redundancy/whitespace/marker dims that concentrated on build_edition.py.",
  "why": "make_kindle_safe (the productized recipe per MATRIX_MAP: kindle = everywhere base + kindle_post) strips every display:none/visibility:hidden but LEAVES the popup asides and hidden=\"\" in place, so on Kindle every previously-hidden footnote aside renders as INLINE visible endnote content — a completely different rendered shape than the popup editions the display-redundancy/whitespace/marker finders inspected. Nobody checked the post-strip kindle artifact for empty/duplicate inline asides, repeated category bylines now made visible, or marker-without-target. Separately, _apply_kindle_m4b_members tracks badges_before and badges_retargeted but never asserts they are EQUAL (a vnotes aside that fails _VNOTES_ASIDE_RE leaves its badge pointing at a removed same-file id; only verify_kindle_m4b_scripture_html's m4b-1 catches it, and apply_kindle_m4b may not be on the shipped path at all — ship status unconfirmed).",
  "suggested_lens": "Run make_kindle_safe over a catholic-study everywhere build and scan the OUTPUT (not the input) for: empty <aside>/<section> shells, byte-identical adjacent inline asides (now visible), repeated 'Easton's Dictionary'/category headers exposed by the strip, and any verse-notes-badge/vn-link whose target is absent. Separately grep the build/release drivers for callers of apply_kindle_m4b / make_kindle_m4b to confirm whether the M4b glossary-relocate path ships or is dead; if it ships, add a badges_retargeted == badges_before assertion."
 },
 {
  "area": "The Kobo kepub COLOUR-VARIANT fan-out — swap_epub_cover.swap_cover re-zipping a kepubify output for every non-signature colour — is the multiplier behind most Kobo catalog assets, yet the harness spot-checks only ONE colour.",
  "why": "In build_format_matrix.build_edition_assets the kepubify branch produces a single kepub base, then for each variant colour calls swap_cover(kepub_base, composite, asset), which fully re-zips the package with Python's zipfile. kepubify (an external pinned tool) emits koboSpan-wrapped content and may name/relocate the cover entry differently than build_epub's cover.jpeg slot; swap_cover assumes DEFAULT_COVER_ENTRY='cover.jpeg' and raises only if it is absent. The verify_kr2_build gate runs per asset, but the cross-product dim explicitly relied on a single non-signature spot-check and the round produced zero findings here — so the kepub+swap interaction (cover-entry survival post-kepubify, koboSpan byte-preservation through the re-zip, determinism of every variant) is effectively unaudited across the ~80-102 colour assets.",
  "suggested_lens": "kepubify one eink base, then for each COVER_COLOURS variant run swap_cover and assert: (a) the kepub's cover entry name actually equals the swap --entry (else every variant silently keeps the signature cover or raises); (b) every non-cover member is byte-identical pre/post swap (koboSpan + nav + opf untouched); (c) the swapped kepub still passes dev/verify_kr2_build.py + epubcheck. Add a fan-out gate that scans ALL colour assets, not one."
 },
 {
  "area": "No build-time / validate_schemas-time enum-membership validation of editions.yaml presentation fields — the only enum enforcement lives in api_save_edition_meta (the /customize SAVE path), which the wizard's edition_templates/*.yaml and any hand-edit bypass.",
  "why": "EDITIONS_SPEC in scripts/validate_schemas.py validates target_reader (line 240), marker_badge_style (264), chapter_number_format (230), verse_marker_glyph (229), reader_eink_study_layout (262), cover_template (184) as type=str ONLY, with no constraint=lambda v: v in <ENUM> (contrast KINDS_SPEC.phase line 279 which DOES constrain membership). marker_style, title_page_style, verse_popup_style, note_popup_style, and topical_index_source have NO FieldSpec at all. The 4 content/edition_templates/*.yaml (school-friendly-nrsv, children, family-devotional, monastic-daily-office) are loaded by the wizard and a hand-edited editions.yaml feeds build_edition directly — both bypass api_save_edition_meta. A typo'd or now-orphan value (e.g. a stale target_reader: computer, or marker_style: badges) therefore reaches the build resolver, which either raises (crash) or silently falls through to a default (wrong shipped output) with no gate. options-completeness (7/7) audited the 5-leg WIRING per field, not whether invalid DATA VALUES are caught before the build consumes them.",
  "suggested_lens": "Add constraint=lambda v: v in <ENUM> to every editions presentation FieldSpec (and add the missing FieldSpecs for marker_style/title_page_style/verse_popup_style/note_popup_style/topical_index_source), mirroring KINDS_SPEC.phase; then sweep content/edition_templates/*.yaml (and the snapshot loader path) for any enum value not in its resolver's valid set, and add a load-time guard in config.load_editions so the build never silently defaults on a bad value."
 },
 {
  "area": "The website release-catalog layer (scripts/gen_release_catalog.py -> website catalog.json/catalog.html + releases.html) is a THIRD edition x format x colour OFFERING surface that the round-16 cross-product gate does not cover.",
  "why": "The user ask was 'every way we offer the same Bibles.' The cross-product survivors audit only the /customize reader dropdown and the wizard target cards; D1 settled the release-asset<->SHA256SUMS bijection. But gen_release_catalog computes which format COLUMNS go live (all() over each edition's signature-colour asset) and which M2 colour PICKERS render per cell, plus 'never-remove-live legacy offerings' (the legacy flagship epub+kepub kept live while their columns are dark). This catalog<->FORMAT_MATRIX<->present-assets three-way consistency is its own invariant: the page could advertise a column/colour whose asset was never built, omit a cell the matrix does build, or keep a legacy offering whose asset was retired (the D1 re-cut removed 100 retired SKUs) — none caught by the asset-bijection gate. website-deploy is excluded from product-scope rounds, so no dim looked here.",
  "suggested_lens": "Assert gen_release_catalog's live columns + colour pickers == exactly {FORMAT_MATRIX cells} x {COVER_COLOURS} that have a real release asset (reuse the D1 asset list); assert every 'never-remove-live legacy offering' still resolves to a published asset; assert no catalog download button points at a name absent from SHA256SUMS. Wire it as a build-free gate alongside audit_cross_product."
 },
 {
  "area": "Cross-OS build-output determinism is gated ONLY for the 9 KJV byte-stable cells; the eink, kepub, flagship-ethiopian, standalone, and all colour-variant outputs have NO cross-OS byte gate — and a Win-vs-Mac divergence is already on record.",
  "why": "os-binary-parity (4/4) was static spec/source analysis only (it explicitly did not build binaries). The 9-cell golden set is {catholic-study, evangelical-reformed, eastern-orthodox} x {everywhere, tablet, kindle}; Mac cross-OS-verifies only those 9 + ONE catholic-study eink (gates pass, NOT byte-identity). Round-15 already observed catholic-study eink G3 xref count differs Win 55,774 vs Mac 45,057 — direct evidence the eink page-break/glossary-streaming passes can emit OS-dependent output (likely dict/set/glob ordering). Because the release is built on whichever box, the eink/kepub/flagship/colour-variant artifacts a user downloads could differ byte-wise (and structurally) from the other lane's, with no automated detector.",
  "suggested_lens": "Rebuild at least one eink cell + its kepub on BOTH boxes and byte-diff (extend G1's golden approach to one eink anchor cell); root-cause the known catholic-study eink Win-vs-Mac G3 count divergence by auditing the eink-only passes (_merge_scripture_base_files, the glossary streamer, apply_file_split) for unsorted set/dict/os.scandir iteration; assert the flagship-ethiopian and standalone builds are deterministic across two runs on the same box first, then across boxes."
 },
 {
  "area": "Per-book / per-chapter tri-state SYMBOL-OVERRIDE resolution feeding the marker layer (note_families_on/off_per_book/_per_chapter, disabled_note_ids/enabled_note_ids) was not traced per-reader by the marker dim (0 survivors).",
  "why": "These override maps decide which notes survive the per-edition filter, which determines which markers/badges exist. The marker-logic-xreader dim traced apply_badge_markers and the numbers renumber pass but not the OVERRIDE -> filter -> marker chain: an override that drops a note but leaves its inline marker yields an orphan marker (dead tap), and conversely a note kept by an enabled_note_ids override whose marker was already dropped yields a note-without-marker. The badge collapse (one badge per verse) and the eink glossary-backmatter relocation make this worse — a per-chapter off-override that empties a verse's note set should remove the badge, but the badge shell may remain. With 0 survivors in this dim, the override interaction is the most likely un-walked path.",
  "suggested_lens": "Construct a test edition with a note_families_off_per_chapter (and a disabled_note_ids) that empties exactly one verse's notes, build it for badge AND numbers AND eink, and assert: no verse-notes-badge/marker remains for that verse, no empty <aside> badge target shell, and the renumber stays gapless; then grep build_edition for every site that emits a marker/badge to confirm each consults the SAME resolved survivor set the filter used."
 }
]