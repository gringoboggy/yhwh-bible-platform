# Round-12 Mac deep-audit — NEW dimensions (findings-only)

**Generated:** 2026-06-23 · Mac lane · WIN BIG-batch ② · workflow `w3fyie8yt` (8 agents).

**Scope:** the 4 dimensions round-10 left uncovered (its own completeness gaps). Each dim was audited then **independently verify+completeness-critic**'d (confirm + hunt missed). Round-11 gap-1..6 and the de-scoped vnote-translation arc were excluded as out-of-scope.

**Result:** **26 confirmed findings** (**2 high · 10 medium · 5 low · 9 info**), **0 false-positives**, 8 caught by the critic pass.

> **WIN:** these are NET-NEW vs round-11. Top priority = the **2 HIGH** non-reproducible zip writers (`press_kit.py`, `api/exports.py`) — they extend the W2/W5 byte-determinism class. Then the tablet `resolve_reader_toc_collapsible` override (matrix≠build). Mac is findings-only.

## Summary

| Dim | reported | final | fp | missed | top sev |
|---|---|---|---|---|---|
| platform-apple-play | 5 | 7 | 0 | 2 | medium |
| popup-new-emitter | 3 | 6 | 0 | 3 | medium |
| zipfile-byte-repro | 5 | 8 | 0 | 3 | high |
| audit-caches-cache-blindness | 5 | 5 | 0 | 0 | medium |

---

## platform-apple-play — Apple (tablet/eink) + Google Play Books build-profile correctness/coverage

**Findings:** 7 (reported 5, 0 FP, 2 critic-found).

**Class fix:** The Apple/tablet correctness class is rooted in one anti-pattern: target-specific build behavior that BYPASSES the edition's stored/user value instead of resolving through it. The canonical fix is to make resolve_reader_toc_collapsible() honor the strict opt-in for ALL targets (return `edition.get('reader_toc_collapsible') is True`), move the tablet 'collapsible by default' to a wizard/customize presentation DEFAULT (overridable), surface the RESOLVED value in api_customize_data + customize.py so matrix==build holds, and gate the customize checkbox by target so a collapsible ToC can never reach the everywhere/Play profile. Play needing no own profile is intentional and documented (spec §2 row 5, EREADERS open-question #4) — not a defect; only the everywhere-build collapsible-ToC leak needs a guard. The stale spec §4.3 de-dupe note is a doc-sync, not a code fix.

| sev | file:line | status | issue → fix |
|---|---|---|---|
| medium | `scripts/build_edition.py:4476-4485` | DETECTED | resolve_reader_toc_collapsible() returns True UNCONDITIONALLY for target_reader=='tablet' (Apple Books), ignoring the edition's saved reader_toc_collapsible value. This defeats the documented STRICT OPT-IN contract for exactly the one target where the wizard ENABLES the toggle (TARGET_CAPS.tablet.toc_expandable=true, box not disabled). A user who picks 'Phone / tablet' and then UNCHECKS 'Expandable Contents' in the wizard still saves reader_toc_collapsible:false (wizard.py:1218), but the tablet build re-forces collapsible <details> ToC anyway. The companion comment at build_edition.py:5535-... |
| medium | `scripts/web_editions.py:473` | DETECTED | api_customize_data surfaces the RAW stored reader_toc_collapsible (e.get('reader_toc_collapsible', False)) instead of the resolved value resolve_reader_toc_collapsible(e). Combined with the tablet hard-override in build_edition.py:4483, the /customize + matrix surfaces show 'off' for a tablet edition where the user disabled it, while the built EPUB ships it ON — a matrix==build / one-resolver drift (MATRIX_MAP finding #3 class). Note max_popup_languages/target_reader ARE surfaced through their resolvers on adjacent lines (462,476), so toc_collapsible is the odd one out. → **fix:** Either (a... |
| low | `scripts/templates/customize.py:557` | DETECTED | The /customize 'Expandable chapter lists' checkbox (data-field=reader_toc_collapsible) is freely editable on ANY target, with no target gating (unlike the wizard, which disables it for non-capable targets). A user can set reader_toc_collapsible:true on an everywhere-target edition; the Google Play Books catalog cell builds from the everywhere base (FORMAT_MATRIX play -> target_reader 'everywhere'), so that edition's Play asset would ship <details> collapsible ToC, which dev/EREADERS.md:186 documents as rendering 'CLOSED AND STUCK' on Play Books (the one reader that locks them). Does not aff... |
| low | `docs/superpowers/specs/2026-06-10-website-format-matrix-design.md:133-137` | DETECTED | Stale spec claim: '§4 De-dupe note ... until the everywhere/tablet/computer profiles diverge, 27 of the 45 base artifacts are byte-aliases — build ~18 real bases + alias the rest; the catalog's Apple cell aliases the everywhere artifact until tablet has a real profile delta.' The tablet profile NOW diverges from everywhere in at least 7 build branches (file-split OFF vs ON at build_edition.py:4471; toc_collapsible forced ON at 4483; note_popup_split_cap 0 at 3245; left-align notes CSS at 7464; note_popup_style category-color at 2238; strip vn-sep separators at 2708; marker badge). Verified:... |
| low | `scripts/templates/wizard.py:561-567 / 564` | MISSED | TARGET_CAPS.tablet declares max_popup_languages:null (uncapped) and the gate_reason is empty (toc_expandable enabled), which the code honors — that part is fine. The MISSED defect is the inverse asymmetry of confirmed Finding #1 at the wizard layer: applyTargetGating (wizard.py:600-602) FORCES STATE.toc_expandable=false whenever caps.toc_expandable is falsy, so for everywhere/eink/computer/kindle the wizard can ONLY ever save reader_toc_collapsible:false. That means the ONLY target whose stored value the build is allowed to read back (a non-tablet edition with the flag set true via /customi... |
| info | `scripts/build_edition.py:3320-3335` | MISSED | Latent footgun (NOT an active defect): resolve_note_popup_split_byte_cap() is the ONLY popup/reader resolver that is not internally target-gated — it returns DEFAULT_NOTE_POPUP_SPLIT_BYTE_CAP=8858 for every edition including tablet. The Apple M2 intent (build_edition.py:3239 'one merged badge per verse') is preserved ONLY because the single call site at line 3913 guards it with `if eink_target else 0`. If any future caller invokes the resolver without replicating that eink gate, tablet/Apple builds would silently split heavy-verse popups into (N/M) part-badges, contradicting the M2 layout d... |
| info | `dev/EREADERS.md:19` | MISSED (low-value / doc-precision) | The Apple Books row claims 'Page-break CSS ✅ honored' and 'Embedded fonts ✅ honored' as if tablet-specific, but I verified there are NO target-conditional page-break or @font-face/font-embed branches anywhere in build_edition.py (grep for page-break/page_break/break-before/@font-face/font_embed crossed with tablet/eink/kindle/target returned zero hits; the only page-break mention tied to a target is the Kindle SEAM CSS noted at 1973, and fonts at 6714 are OPF-registration for all editions). So those two Apple cells describe BASE/universal EPUB behavior, not an Apple-profile fix. Not a code ... |

**Completeness:** High confidence the Apple+Play dimension is fully covered. I independently confirmed all 5 auditor findings by reading each cited site plus its companions (wizard.py:594-602/1218, build_edition.py:5535-5543, web_editions.py:462/473/476, customize.py:557, api/editions.py:125/818-832, the FORMAT_MATRIX rows 2033-2085, the spec §4, build_format_matrix.py target_for_cell/distinct_base_targets, gen_release_catalog.py, and the single split-byte-cap call site at 3913). I enumerated ALL 18 target_reader/is_kindle_target branch sites in build_edition.py (grep-verified: 1735/1765/2238/2286/2307/2389/2404/2708/2739/3245/3894/4471/4483/4494/5155/7297/7464) and accounted for each — the tablet-specific...

---

## popup-new-emitter — NEW popup/aside/noteref/badge emitter or hidden-target integrity class

**Findings:** 6 (reported 3, 0 FP, 3 critic-found).

**Class fix:** Enforce a prefix-free id namespace at EVERY noteref/footnote emitter: no emitted target id may be a strict prefix of any sibling footnote id in the same artifact (the K-R6-2 Kobo forward-scan 'slice-swallow' rule already applied to the KJV popup-unit path via the `-sN` tail). Two emitters re-broke it by using variable-length human tokens as the terminal id segment: build_edition._emit_backmatter_glossary_inner (category id, where `lit`⊂`liturgy`) and build_standalone.render_chapter_body (bare first-occurrence vs `-N` duplicate-verse occurrences). Fix both by giving every id a fixed terminal disambiguator (always-present `-gN`/`-oN` index, never a bare or variable token), and add a single cross-emitter guard/test: collect all `epub:type=\"footnote\"` ids in a built artifact and assert none is a strict prefix of another.

| sev | file:line | status | issue → fix |
|---|---|---|---|
| medium | `scripts/build_edition.py:3737-3756, 4159-4189` | MISSED (new class, not the vnote-translation arc) | eink-backmatter study-glossary noteref namespace is NOT prefix-free across categories. Each category badge targets a padded-footnote aside id `vnotes-{code}-{ch}-{v}-{cat}` (line 3740 sid / 3737 badge_target; emitted as href at 4185 `href="#{target_id}"`, with the matching aside id at 3740). The category ids include both `lit` and `liturgy`, and `lit` is a STRICT PREFIX of `liturgy`. On any verse carrying both a `lit` note and a `liturgy` note (both are real, populated categories: 5 `lit` kinds + 3 `liturgy` kinds in content/kinds.yaml; co-occurrence is normal in Ethiopian Tewahedo study ed... |
| medium | `scripts/build_standalone.py:96-105` | MISSED (new class, not the vnote-translation arc) | Standalone Ge'ez noteref/footnote id namespace is NOT prefix-free for duplicate verse numbers within a chapter. render_chapter_body uses `suffix = "" if seen[gv] == 1 else f"-{seen[gv]}"` (line 96), so the FIRST occurrence of a repeated verse number gets a BARE id (`v-psa-36-24` / `vnote-psa-36-24`, lines 97-98) while later occurrences get `-2`,`-3` (`vnote-psa-36-24-2`). The bare first-occurrence id is a strict prefix of its later-occurrence siblings, and all sit as `epub:type="footnote"` asides in the same hidden verse-refs-section (lines 106-108). The first v24 noteref (`href="#vnote-psa... |
| medium | `scripts/build_standalone.py:96-98` | MISSED | The standalone prefix-collision is FAR broader than finding #2's dup-verse framing. Because single-occurrence verses get a BARE id (suffix=''), EVERY chapter with >=10 verses mints cross-verse digit-extension prefix pairs in the same hidden verse-refs-section: vnote-psa-36-1 < vnote-psa-36-10..19, vnote-psa-36-2 < vnote-psa-36-20..29, vnote-psa-36-3 < vnote-psa-36-30..39, etc. Ps36 alone yields 33 strict-prefix sibling footnote-id pairs (computed), of which only 2 are the dup-verse pairs the auditor flagged; the other 31 are plain consecutive verses. This is verbatim the failure build_editi... |
| low | `scripts/inject.py:227, 746` | MISSED | The MAIN inject popup path has the same same-verse bare-vs-suffix prefix collision the auditor only flagged for backmatter/standalone. full_id = f'{prefix}{ch:02d}{v:02d}{suffix}' and the store convention gives the FIRST note of a verse a bare suffix '' while later notes get 'a','b','c' (verified in content/notes/zec.py: first-20 suffix fields = ['','a','b','c','',...]). So note-zec0101 (bare) is a strict prefix of siblings note-zec0101a / note-zec0101b in the same chapter notes-section, and the noteref id ref-zec0101 likewise prefixes ref-zec0101a. The fixed-width {cc}{vv} padding DOES pre... |
| info | `scripts/core/kindle_post.py:277,282` | DETECTED (latent, not currently exercised) | Latent (info): the Kindle m4b coordinate parsers `_VNOTES_COORD_RE`/`_VNOTE_COORD_RE` (`^vnotes-([a-z0-9]+)-(\d+)-(\d+)`) and the collect regex `_VNOTES_ASIDE_RE` (matches any aside with class containing `verse-notes` + id `vnotes-`) would also match the eink-backmatter `study-glossary-cat verse-notes` footnote asides if a Kindle post-process ever ran over an eink-backmatter artifact: the category suffix `-lit`/`-liturgy` is silently dropped by the coord regex (anchored, non-greedy on the suffix), collapsing distinct category targets to one (code,ch,v) key in _vnotes_sort_key/back-link. Tod... |
| info | `scripts/build_standalone.py:89, 3636 (ch-anchor scheme)` |  | Standalone chapter-opener anchors ch-{book}-c{chapter} (line 89) and the build_edition fallback return-link ch-{bxx}-cN have cross-chapter prefix collisions (ch-psa-c1 ⊂ ch-psa-c10..c19; 11 pairs for a 20-chapter span). Lower risk than the footnote case because each chapter is its own XHTML file (build_manifest_and_spine) so these anchors are NOT siblings in a single hidden footnote section scanned by one popup target — but if any tool forward-scans by id-prefix across the spine (e.g. the kindle_post m4b glossary which concatenates), the bare c1 could shadow c10. → **fix:** Note for complet... |

**Completeness:** All three reported findings CONFIRMED genuine by reading source + the live store + the kinds registry (finding #1 medium, #2 medium, #3 info — severities accurate). #1 and #2 are genuinely NEW emitter prefix-collision classes, not the de-scoped vnote-translation decline arc. KEY GAP I FOUND: finding #2 is materially under-scoped — the standalone bare-first-occurrence id scheme produces 33 strict-prefix sibling footnote-id pairs in Ps36 alone (only 2 are dup-verse), and the IDENTICAL class lives in the MAIN inject.py popup path (note-/ref- ids, first-note bare suffix). Coverage confidence: HIGH for the build_edition backmatter-glossary + build_standalone + kindle_post emitters (read every ...

---

## zipfile-byte-repro — every zipfile.ZipFile(..,"w") writer pins reproducible metadata

**Findings:** 8 (reported 5, 0 FP, 3 critic-found).

**Class fix:** Add one shared reproducible-zip helper (e.g. in scripts/core/ or a small zip_repro module): _ZIP_EPOCH = (1980, 1, 1, 0, 0, 0) plus a factory like def _zip_member(name, *, stored=False) -> zipfile.ZipInfo that returns a ZipInfo with date_time=_ZIP_EPOCH, external_attr=0o644<<16, and compress_type set to ZIP_STORED (mimetype) or ZIP_DEFLATED. Route ALL five writers through it. This (a) fixes the two genuine UNPINNED shipping writers — press_kit.build_zip (4 writestr sites) and exports All-Editions bundle (the bare zf.write) — and (b) collapses the three duplicated _ZIP_EPOCH definitions (build_epub inline tuple x2, swap_epub_cover, kindle_post) into a single source of truth so a future writer can't silently regress. Add a byte-stability test: build each shipping zip twice over identical input and assert byte-identical output (the existing build_epub byte-stability gate pattern), covering press_kit and exports.

| sev | file:line | status | issue → fix |
|---|---|---|---|
| high | `scripts/core/press_kit.py:305-350` | UNPINNED | UNPINNED (SHIPS, high). build_zip() opens ZipFile(buf,'w',ZIP_DEFLATED) then writes every member with bare zf.writestr(name, body) — blurbs/sample at L315, cover PNGs at L331, manifest.json at L350. None pass a ZipInfo, so zipfile stamps the current build wall-clock time into every local-file-header + central-directory date_time. Two runs over identical edition+blurbs+cover produce byte-different zips. Compounded: the manifest body itself embeds generated_at = datetime.now(timezone.utc) (L298) when the now= param is not injected. This zip is a SHIPPING artifact: served for download via api/... |
| high | `scripts/api/exports.py:407-409` | UNPINNED | UNPINNED (SHIPS, high). The All-Editions bundle opens ZipFile(zip_path,'w',ZIP_DEFLATED) (L407) and adds each built EPUB with bare zf.write(fp, arcname=fp.name) (L409). zf.write() reads the file's on-disk st_mtime into the member date_time, so the bundle's central directory varies with filesystem mtimes even when the contained EPUB bytes are byte-identical reproducible builds. The bundle is a user-facing download (download_url = /api/export/download/{zip_name}, L413). Secondary non-determinism: the zip filename embeds a wall-clock timestamp ts = datetime.now(timezone.utc) (L404) — acceptabl... |
| medium | `scripts/api/exports.py:404 & scripts/core/press_kit.py:298` | MISSED-SECONDARY | Secondary non-determinism beyond the writer itself: both artifacts also embed a wall-clock value INSIDE the payload, not just the zip header. exports.py L404 ts=datetime.now() goes into the zip filename (acceptable for a unique download name, but the path is never stable), and press_kit.py L298 generated_at=datetime.now() is written into manifest.json BODY when now= is not injected — so even after pinning every ZipInfo date_time, the manifest member's bytes still churn run-to-run. The class fix for press_kit must also thread now= from the API caller (api/press_kit.build_press_kit_zip L123 c... |
| info | `scripts/build_epub.py:143-159` | PINNED | PINNED (reference). ZipFile(out_path,'w') with mimetype STORED-first; every member gets date_time=(1980,1,1,0,0,0) + external_attr=0o644<<16 (L148-159). Reproducible. Note: it inlines the (1980,1,1,0,0,0) tuple literal rather than referencing a shared _ZIP_EPOCH constant, so it does not participate in the class fix's single source of truth. → **fix:** No defect. When extracting the shared helper, replace the two inline (1980,1,1,0,0,0) literals (L149, L156) with the shared _ZIP_EPOCH / _zip_epoch_info() so all writers share one constant. |
| info | `scripts/core/kindle_post.py:98-119` | PINNED | PINNED (reference, W2/W5). _ocf_rezip() pins date_time=_ZIP_EPOCH + external_attr=0o644<<16 on mimetype (L107-111) and every member (L115-119); _ZIP_EPOCH defined L95. Reproducible. Output ships (Kindle-safe EPUB). → **fix:** No defect. This _ZIP_EPOCH constant (L95) is a candidate home / import target for the shared helper. |
| info | `scripts/swap_epub_cover.py:60-71` | PINNED | PINNED. ZipFile(out_path,'w') copies entry order verbatim; each member gets a fresh ZipInfo with date_time=_ZIP_EPOCH + external_attr=0o644<<16 (L63-71); mimetype STORED, rest DEFLATED compresslevel=9; _ZIP_EPOCH defined L33. Reproducible. Output ships (cover-swapped EPUB). → **fix:** No defect. Third independent copy of _ZIP_EPOCH (L33) — fold into the shared helper. |
| info | `dev/cc-hooks/memory_hygiene.py:277-280` | OUT-OF-SCOPE-DEV | Out-of-scripts/ write-mode zip writer the dimension's scripts/-only enumeration excludes by scope, but it IS unpinned: write_memory_zip opens ZipFile(dest,'w') then bare zf.write(f, ...) (L280) stamping wall-clock into each member. Backs up the Claude memory dir to external E:/F:. Not a shipping Bible artifact and the backup filename is already timestamped-by-name (stamp L289), so SHA256 churn is irrelevant here — flagged only for enumeration completeness. → **fix:** No action required (dev backup tool, out of dimension scope); pin only if memory backups ever need content-hash dedup. |
| info | `dev/kindle_bisect.py:106,231,366,413,462,532,601,750,819` | OUT-OF-SCOPE-DEV | Out-of-scripts/ scope, but nine write-mode ZipFile(...,'w') rung-builders that re-zip staged EPUB probe artifacts the USER uploads to Send-to-Kindle. Spot-checked: these are DIAGNOSTIC one-variable probes (intentionally dead popups) for STK-failure bisecting, not the shipped EPUB, and each rung is a throwaway forensic artifact. Unpinned, but byte-reproducibility is immaterial to a one-shot manual upload. Flagged only so the writer enumeration is exhaustive repo-wide. → **fix:** No action required (dev forensics tool, out of dimension scope and ephemeral by design). |

**Completeness:** HIGH confidence the dimension is fully covered for the stated scope (scripts/). I ran an exhaustive repo-wide grep for ZipFile(...,'w'/'a'/'x'), .write(), .writestr(), plus shutil.make_archive and kepubify paths. Within scripts/ there are EXACTLY 5 write-mode zip writers: build_epub.py:143 (PINNED), swap_epub_cover.py:60 (PINNED), kindle_post.py:106 (PINNED), press_kit.py:305 (UNPINNED), exports.py:407 (UNPINNED). The auditor caught both UNPINNED defects and all 3 PINNED references with accurate line cites — its scripts/ enumeration is complete, no scripts/ writer was missed. The remaining 6 ZipFile sites in scripts/ (fetch_sources x2, audit_epub_structure, note_diff, build_format_matrix,...

---

## audit-caches-cache-blindness — audit_caches.py blind to @functools.cache / @cache / @cached_property

**Findings:** 5 (reported 5, 0 FP, 0 critic-found).

**Class fix:** Rename/extend `_is_lru_cache_decorator` (scripts/audit_caches.py:46-59) into a general cache-decorator recognizer that returns a classification tag. Match four families: (1) ast.Name id in {"lru_cache","cache"}; (2) ast.Attribute with attr in {"lru_cache","cache"} whose value is Name "functools"; (3) cached_property in both Name and functools.Attribute forms. Have discover_lru_caches carry the tag through. Treat lru_cache/cache identically (both expose a module-level function.cache_clear() surface → existing clear-path + whitelist logic just works, and the two already-present whitelist entries at .cache_audit_whitelist.py:67,70 immediately re-arm, taking discovered caches 44→46 with both classified 'whitelisted'). Treat cached_property separately — it has no function.cache_clear() surface, so exclude it from find_clear_sites() and report it informationally rather than as no_clear_path. This closes the whole class: today's two real @functools.cache misses plus any future @cache/@cached_property additions.

| sev | file:line | status | issue → fix |
|---|---|---|---|
| medium | `scripts/audit_caches.py:46-59` | ROOT-CAUSE | _is_lru_cache_decorator() only recognizes @lru_cache and @functools.lru_cache. It returns False for @functools.cache, bare @cache, @cached_property, and @functools.cached_property. Therefore discover_lru_caches() never emits any function decorated with those memoizers, and audit() never classifies them (no clear-path check, no whitelist check, no no_clear_path failure). The cache-hygiene audit is structurally blind to the entire @cache / @cached_property family. Verified empirically: a live run of `python3 scripts/audit_caches.py --json` discovers 44 caches and neither @functools.cache site... |
| medium | `scripts/manuscript_qa.py:156` | MISSED / pure-value (whitelist-able; entry already present but currently dead) | @functools.cache on top-level _collate_case(ref, suf, ch, book) is a MISSED site — the audit never discovers it. It caches a (gg, cam, golden, collation) tuple loaded from the four immutable Samuel calibration JSONs (content/manuscript/samuel/calibration/*). Per its own docstring it is a §7.1 read-once project-internal-published-data singleton: pure-value / whitelist-able, NOT runtime-mutable (updates ship via git + process restart). No _collate_case.cache_clear() call exists anywhere in scripts/ or tests/. → **fix:** Once _is_lru_cache_decorator recognizes @functools.cache, this site is au... |
| medium | `scripts/core/manuscript_collation.py:180` | MISSED / pure-value (whitelist-able; entry already present but currently dead) | @functools.cache on top-level load_kjv_skeleton(book, chapter) is a MISSED site — the audit never discovers it. It caches KJV spine rows parsed via ast.literal_eval from content/translations/kjv/<book>.py. Per its docstring it is project-internal published data, immutable in-process: pure-value / whitelist-able, NOT runtime-mutable. No load_kjv_skeleton.cache_clear() call exists anywhere in scripts/ or tests/. → **fix:** Once @functools.cache is recognized, this site is auto-discovered and the EXISTING whitelist entry at scripts/.cache_audit_whitelist.py:67 (`_.load_kjv_skeleton`) takes eff... |
| medium | `scripts/.cache_audit_whitelist.py:67,70` | DEAD whitelist entries (re-armed by the class fix) | Two whitelist entries — `_.load_kjv_skeleton` (line 67) and `_._collate_case` (line 70) — are currently DEAD / ineffective. The whitelist authors explicitly intended both @functools.cache functions to be governed by the cache audit (each has a documented §7.1 rationale), but because _is_lru_cache_decorator never matches @functools.cache, discover_lru_caches never emits these functions, so audit() never reaches the whitelist lookup for them. The whitelist documents an intent the audit silently fails to enforce — a false sense of coverage. This proves the blindness is a real defect, not a the... |
| low | `scripts/audit_caches.py:102-116` | LATENT (no live site; matrix.py:108 is a comment, not a decorator) | Forward-looking gap (no current offending site): discover_lru_caches breaks on the FIRST matching decorator and records node.name as a top-level function name. @cached_property is fundamentally different — it is a per-instance descriptor with NO module-level <func>.cache_clear() surface (you clear it by `del instance.__dict__[name]`). If a future @cached_property is added under scripts/ and the recognizer naively lumps it with lru_cache, find_clear_sites('<prop>') would search for a non-existent <prop>.cache_clear() and wrongly report no_clear_path. Today scripts/ has ZERO real @cached_prop... |

**Completeness:** HIGH confidence the dimension is fully and exhaustively covered; the prior auditor missed nothing. I independently ran the live audit (44 caches discovered, all @lru_cache; neither @functools.cache site present) and ran five orthogonal greps over scripts/: (1) @functools.cache -> exactly 2 (manuscript_qa.py:156, manuscript_collation.py:180), both already reported; (2) bare @cache -> 0; (3) @cached_property/@functools.cached_property -> 0 real (the only matrix.py:108 hit is inside a docstring, already noted as latent); (4) 'from functools import' -> only lru_cache forms (no 'import cache'/'import cached_property' aliases) so the two missed sites are reachable only via the @functools.cache ...

---
