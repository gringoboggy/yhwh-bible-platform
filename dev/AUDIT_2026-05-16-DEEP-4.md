# Project audit — 2026-05-16 DEEP-4 (parallel-subagent sweep, post-ω.48 + PDF-recovery)

**Trigger:** user "one more major audit, fix if something pops up
and save just for my sanity" — a confidence sweep after the
external-tidy-up scare that deleted the source PDF + Phase-4
handoff package (since recovered into the repo) and the ω.48
hygiene bundle. (Audit triggered + run 2026-05-15; finalized
2026-05-16 across the date boundary.)

**Form:** DEEP-class — 3 parallel investigation subagents (ω.48 +
PDF-recovery integrity incl. an end-to-end reproducibility proof /
state-doc+memory+Ω.0 coherence / atomic_write-regression +
test-isolation re-scan + repo-integrity) + a solo tool battery +
full SERIAL pytest. Smaller delta than DEEP-3 (no new book ships
— ω.48 + the recovery only), so this is a targeted "nothing
broke, nothing lost" verification rather than a wide coherence
sweep.

---

## 0. TL;DR

**Project state is CLEAN + nothing was lost.** The ω.48
atomic_write change is regression-free; the recovered PDF is the
correct file and the pipeline reproduces shipped output
byte-identically; docs/memory/Ω.0 are coherent; the 193 MB PDF +
handoff package are git-ignored and untracked. **0 WARN findings.
1 cosmetic nit (F-DEEP4-1) — FIXED this pass.** Carry-forward
F-DEEP3-2 (partial-by-design) + F-DEEP2-3/4 (resolved at ω.48) +
the deferred editions_path= refactor remain as DEEP-3 left them.

### Foreground (every check passes)

- ✓ **End-to-end reproducibility proof (subagent A, the sanity
  headline):** re-extracted `wis` (Wisdom of Solomon, τ.7.x.r)
  from the recovered PDF → 254 verses; AST-parsed `VERSES`
  list **fully equal** to the committed copy; all module scalars
  identical incl. `EXTRACTION_DATE`. The only textual delta is
  post-ingest black-formatting style (double-quote + wrapped vs
  the tool's raw single-quote one-per-line) — **zero semantic
  difference**. The recovered PDF is the correct file, the
  post-ω.48 pipeline works, rendering is reproducible. Repo
  restored pristine via `git checkout` (verified clean after).
- ✓ **PDF + handoff recovery integrity:** PDF at resolution_paths
  #5 (`content/translations/sources/parallel-bible-eotc/
  Bible_Amharic_and_Geez.pdf`), 202 694 977 B (≈193 MB), the
  project resolver returns that in-repo path, pymupdf
  `page_count == 2539`. All 4 referenced handoff docs (01/02/03/05)
  present at `project_maccabees_expansion/` inside the project.
- ✓ **ω.48 correctness:** `notes_io.py::atomic_write` has
  `newline=""` (compiles; `atomic_write_bytes` unchanged);
  customization.yaml Ω.0 banner is comment-only (parses; all
  `enabled:false`/`isbn_barcode` flags inert/unchanged);
  _meta.yaml convention comment present (parses; stats still
  20/10757/10).
- ✓ **atomic_write regression risk (subagent C):** ~50 callers
  enumerated; **no at-risk byte-exact assertions**. Decisively:
  `tests/conftest.py` protected-paths snapshot guard already
  `replace(b"\r\n", b"\n")` before hashing — ω.48's on-disk LF
  makes it **strictly safer** (less normalization). `test_scripts
  .py:3137` raw-bytes editions hash is a read-only preview
  (no mutator) + editions.yaml is already pure LF.
- ✓ **State-doc + memory + Ω.0 coherence (subagent B):**
  SESSION_STATE/CHANGELOG/PLAN/IN_FLIGHT one consistent story
  (ω.48→DEEP-3→τ.7.x.q+r; 20/10757/10/next-τ.7.x.s everywhere);
  memory `feedback_editions_crlf_gitnoise` correctly carries the
  ω.48 partial/benign update + intact "read git diff" guidance;
  **no overclaim** — CHANGELOG + SESSION_STATE both record
  F-DEEP3-2 as "PARTIAL BY DESIGN", consistent with the DEEP-3
  ledger; no Ω.0 commercial regression (ω.48 customization.yaml
  edit is comment-only).
- ✓ DEEP-2/DEEP-3 isolation fixes still intact: test_refactor.py
  isolated `refactor_log_path=` (25 sites); test_scripts.py
  `matrix_mod.compute_matrix.cache_clear()` ×8 + the class-
  accurate DEEP-3 comment.
- ✓ Linters: lint_rules 11·0·0; lint_plan 4·0·0. Ruff 504 clean.
  Dead code clean (vulture ≥80). Types clean (mypy scripts/core/
  — re-confirms the ω.48 notes_io.py change is type-safe).
  Caches 32/32.
- ✓ Repo hygiene: `git check-ignore` exit 0 for the PDF AND
  `project_maccabees_expansion/`; `git ls-files` shows neither
  tracked; backups all 5 roots populated (150 / 2147 / 151 / 3 /
  50); closed-arc census 14 tau7x* / 151 classes + 6 tau6x* / 68.
- ✓ Full SERIAL pytest: **5659 passed / 1 skipped / 1 failed**
  (960 s). Identical pass count to the DEEP-3 baseline and the
  ω.48 `-n auto` run — **zero regressions from the ω.48 core-
  chokepoint atomic_write change**. The sole fail is
  `test_in_flight_idle_after_pilot` (IN_FLIGHT active for THIS
  audit → **5660 / 1 / 0** at audit close, the DEEP-2/DEEP-3
  mid-audit pattern).

(`scripts/audit_deps.py` still skipped — `pip-audit` not on PATH;
non-blocking per memory `feedback_license_flagging`.)

---

## 1. F-DEEP4-1 — IN_FLIGHT.md duplicate header lines (cosmetic, FIXED)

`dev/IN_FLIGHT.md` carried two byte-identical headline lines (with
a blank line between) at the top of the "### Earlier —
AUDIT_2026-05-15-DEEP-3" block and the "### Earlier — τ.7.x.q/r"
block — demotion-edit artifacts from the prior ships'
current→prior→earlier IN_FLIGHT restructurings. Zero functional
impact (IN_FLIGHT is a working scratch tracker; no
numeric/sequence contradiction; flagged by subagent B as
non-load-bearing). **FIXED this pass** (per the "fix if something
pops up" instruction + the DEEP-2/DEEP-3 precedent of actioning
clear cosmetic nits): both duplicate headline+blank pairs
collapsed to the single intended headline. No other file affected.

---

## 2. Carry-forward ledger

- **RESOLVED earlier (ω.48), re-verified holding this pass:**
  F-DEEP2-3 (customization.yaml Ω.0 banner — comment-only, flags
  inert), F-DEEP2-4 (_meta.yaml ingest_record convention
  documented — stats unchanged), F-DEEP3-1 (the editions.yaml
  content leak — content fix re-confirmed via the E2E proof:
  re-extracted output is byte-identical, no `book_toc_ornament`
  pollution).
- **F-DEEP3-2 — PARTIAL BY DESIGN, confirmed correct:** the ω.48
  `atomic_write` LF hardening fixed the primary write path; the
  residual editions.yaml `git status` flicker (test-only
  `snapshots._dump_edition_record` path) is intrinsic benign
  git-normalized noise that never reaches a commit. DEEP-4
  independently re-confirms this is the right call (subagent C:
  the snapshot guard already normalizes CRLF→LF, so the residual
  is provably inert). No further pursuit — symptom-whacking every
  Windows writer remains the wrong risk/reward.
- **DEFERRED (unchanged):** the `editions_path=` structural
  refactor (DEEP-3 higher-risk; not load-bearing — the actual
  data bug is fixed); an optional future uniform `ingest_record`
  rename; the τ.6.x.3 batched 87-book ocr-tier3 audit-handoff.
- **NEW deferred:** none.

## 3. Forward-staging

Nothing blocks τ.7.x.s. The source PDF is recovered, in-repo,
git-ignored, resolver-verified, and proven to reproduce shipped
output byte-for-byte — **rendering the remaining 67 Amharic /
79 Geʽez books can resume with zero re-discovery**. Next per
the most-logical path: τ.7.x.s = the Daniel-additions cluster
paz/sus/bel (`ተረፈ ዳንኤል` p1449-1453), then the Π.1-mapped
Jubilees [1454,1514] (τ.7.x.t) + 1 Enoch [1515,1566] (τ.7.x.u).
Operational reminder (memory `feedback_editions_crlf_gitnoise`):
read `git diff`, not `git status`, for editions.yaml on Windows.
