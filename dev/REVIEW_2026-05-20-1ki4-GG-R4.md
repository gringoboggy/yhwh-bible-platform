# C-3 Round 4 — adversarial GG review for 1 Kings 4

**Reviewer:** fresh isolated subagent, no prior context. No access to CAM/KJV/standard-Ethiopic/web.
**Target artifact:** `content/manuscript/kings/calibration/1ki4_witnessGG.json` (post fix-round-3).
**Parchment:** GG-00106 f030r-R + f030v-L,M.
**Method:** column-strip + row-strip + per-token crops at 6x-24x LANCZOS upscale with autocontrast cutoff=2, written to `%LOCALAPPDATA%/Temp/yhwh_1ki4_R4/`. Same-scribe glyph calibration used the following known-form references:
- **`ይ` known-ref:** v22 `ይሁዳ` line-1 first char (R2 and R3 both confirmed). Topology: small open-curl loop at top + thin descending foot at right base.
- **`ደ` known-ref:** v18 `እክከ ደወስ` second-word first char (R3 confirmed). Topology: heavier closed-loop top + substantial descender on right.
- **`ስ` known-ref:** v22 `ወእስ ባውዱ` 3rd char (R3 confirmed). Topology: TWO clear vertical strokes joined at top + foot at right base.
- **`ለ` known-ref:** v23 `እለ ይስሕቡ` 2nd char (R3 confirmed). Topology: single sweeping curve descending to a foot.

**Verdict (advance summary):** **NEEDS_FIX (round-4)** with a SMALL, FOCUSED list — exactly the canonical "1ki3-R4 pattern" of manufactured ping-pong. R4 confirms 8 of R3's 10 fixes outright on parchment, but finds **R3 over-corrected the `እለ`/`እስ` axis on 2 specific loci** (v25 and v28) — the parchment ACTUALLY inks `ስ` there, NOT `ለ` as R3 claimed. R3 was right that v23 inks `ለ`, but wrong on v25 and v28. So fix-round-4 should re-revert TWO loci (`እለ ዋዉደ` → `እስ ዋዉደ` v25; `እለ ሰምዑ` → `እስ ሰምዑ` v28) and keep everything else from fix-round-3. No new genuine defects found beyond this. Convergence holds.

The catastrophic worst-class failure (mid-word column-boundary token drop) remains NOT present. METHOD NOTE 2 boundary at `ወአል | ሕምት` remains intact.

---

## R3 fixes — independent verification (10 items)

| # | R3 fix (current JSON) | R4 verdict | Evidence |
|---|---|---|---|
| 1 | v14 `ይሁዳ` | **CONFIRMED** | `f030v_L_v14_yhuda_max_14x.png` at 14x shows the first char of the contested token MATCHES the v22 known-`ይ` reference (small loop + thin descender). NOT the v18 known-`ደ` (heavier loop + substantial descender). R3 right. |
| 2 | v15 `ወይሰርዑ` | **CONFIRMED** | `f030v_L_v15_weyiserU_14x.png` at 14x shows the contested 2nd char matches the `ይ` topology family — same form as v22 known-`ይ`. R3 right. |
| 3 | v17 `ይሁዳ` | **CONFIRMED** | `f030v_L_v17_yhuda_14x.png` and `f030v_L_v17_line30_10x.png` at 10-14x show the first char of `?ሁዳ` matches v22 known-`ይ`. R3 right. |
| 4 | v17 `ይበልዑ` | **CONFIRMED** | `f030v_L_v17_line32_yibelu_10x.png` at 10x shows the line ending `ብዝናሙ:ይበል` — the contested char is `ይ` (matching same-scribe ይ in `ወይስትዩ` two lines below which is unambiguous). R3 right. |
| 5 | v23 `እለ ይስሕቡ` | **CONFIRMED** | `f030v_M_v23_line13_14x.png` at 14x shows the conjunction 2nd char is a clear **single sweeping curve** descending to a foot — the `ለ` topology. R3 right. |
| 6 | **v25 `እለ ዋዉደ`** | **🔴 R3 WAS WRONG — manufactured ping-pong** | `f030v_M_v25_line_14x.png` at 14x clearly shows `ሕ:ስም:ውስተ:ኹሉ:አሕዛብ:`**እስ**`:ዋው...` The conjunction 2nd char has TWO clear vertical strokes joined at top + foot at right base — the **`ስ`** topology, matching the v22 known-`ስ` reference. NOT `ለ`. **R2's revert was right here; R3 over-corrected it back to `ለ`. Fix-round-4: revert AGAIN to `እስ ዋዉደ`.** |
| 7 | **v28 `እለ ሰምዑ`** | **🔴 R3 WAS WRONG — manufactured ping-pong** | `f030v_M_v28_es_14x.png` and `f030v_M_v28_search_8x.png` at 8-14x both clearly show `ነገሥት:ምድር:`**እስ**`:ሰምዑ:ጥበቢሁ✣` The conjunction 2nd char unambiguously has TWO vertical strokes + foot — `ስ` topology. **R2's revert was right; R3 over-corrected. Fix-round-4: revert AGAIN to `እስ ሰምዑ`.** |
| 8 | v7 `ይትመጻውሙ` (ጻ restored) | **CONFIRMED** | `f030r_R_v7_line2_14x.png` at 14x shows line-2-end clearly inks `እለ:ይትመጻው` with a clear `ጻ` 4th-order ts (curl + cross-stroke) between `ይትመ` and `ው`. The `ጻ` IS on the parchment; R2's drop was a fabrication-by-omission; R3's restoration is right. (Also confirmed: the conjunction in v7 is `እለ` with curve-stroke `ለ`, not `እስ`.) |
| 9 | v22 `ወደኖሙ` (one word) | **CONFIRMED** | `f030v_M_v22_line9_14x.png` at 14x unambiguously shows `አሚነዊ:ታሕተ:`**ወደኖሙ**`:ወበስ` as a single 4-character word with `ወ-ደ-ኖ-ሙ` topology — closed-loop `ደ`, labialised `ኖ`, `m+u` final. R3 right on segmentation AND character identity. |
| 10 | v18 `ሕደወቱ` (first-char `ሕ`) | **CONFIRMED** | `f030v_L_v18_above_line_14x.png` at 14x clearly shows the line ending `...ወተቀንደ:ሉቱ:በኩሉ:መዋዕለ:`**ሕ** with the final char unambiguously 6th-order `ሕ` (two-bar topology), and the next line starting `ደወቱ ✣ ❈`. Combined word is `ሕደወቱ`. R3 right. |

**Tally: 8 R3 fixes CONFIRMED, 2 R3 fixes WRONG (manufactured ping-pong) — exactly the 1ki3-R4 convergence pattern.**

---

## Kept items across rounds — sanity check

| # | Token | R4 verdict | Evidence |
|---|---|---|---|
| 1 | v1 `ወሰሎምን` (bare `ሰ`) | **CONFIRMED** | `f030r_R_rubric_v1_10x.png` at 10x shows the 2nd character has a single broad top-stroke with no foot — `ሰ` (bare 1st-order s) topology. NOT `ስ`. |
| 2 | v6 `ካፋን` | **CONFIRMED** | `f030r_R_v6_above_14x.png` at 14x clearly shows `ወልደ:`**ካፋን**`:ሳዕ` with `ካ` having the canonical 1st-order topology (vertical stem + right-turning horizontal top-stroke). NOT `ኒ`. |
| 3 | v17 `ሮዓ ባሕር` | **CONFIRMED** | `f030v_L_v17_roa_band_10x.png` at 10x clearly shows `ከመ:`**ሮዓ**`:ባሕር` with `ሮ` open-circle + `ዓ` 4th-order ʕa. NOT `ኆጻ`. |
| 4 | v17 `ብዝናሙ` | **CONFIRMED** | Same crop shows `ብዝናሙ` with clear `ና` (4th-order n with right-pointing horizontal). NOT `ኆ`. |
| 5 | v18 `ፈስግ` | **CONFIRMED** | `f030v_L_v18_fasag_14x.png` at 14x shows `ፈስግ` with `ስ` (two-vertical + foot). NOT `ለ`. |
| 6 | v21 `ፈስግ` ×2 | **CONFIRMED** | `f030v_M_top_5x.png` lines 4 + 6 show both `ፈስግ` instances with the same `ስ` topology as v18. NOT `ለ`. |
| 7 | v18 `ወተቀንደ` | **CONFIRMED** | `f030v_L_v18_above_line_14x.png` at 14x shows `ኃ:`**ወተቀንደ**`:ሉቱ:...` with clear 5-char word ending in `ደ` (closed-loop + foot). |
| 8 | v18 `እክከ ደወስ` restoration | **CONFIRMED** | `f030v_L_v18_eke_dewes_14x.png` at 14x clearly inks `እክከ:ደወስ:` as two tokens after `❈` before `ግብጽ`. |
| 9 | v22 `ወእስ` (kept distinct from over-corrected loci) | **CONFIRMED** | `f030v_M_v22_line7_wees_14x.png` at 14x shows `❈ወእስ:ባውዱ` with 3rd char two-vertical-stroke `ስ` topology. |
| 10 | v22 `ይሁዳ` (scribe-internal-inconsistency reference) | **CONFIRMED** | `f030v_M_v22_yhuda_tight_16x.png` at 16x and `f030v_M_v22_line_10x.png` at 10x both confirm v22 inks `ይ` (small open loop + thin descender). |
| 11 | v26 `አርዝ` | **CONFIRMED via inheritance** (not re-zoomed by R4 — accepted at the R3 verification level). |
| 12 | v28 `ወይነሥኤ` | **CONFIRMED via inheritance** (R3 confirmed; not re-zoomed by R4). |
| 13 | v28 `ይከምዑ` | **CONFIRMED via inheritance** (R3 ambiguous-accept; R4 saw `f030v_M_v28_search_8x.png` line 1 contains `እምኹሉ:አሕዛብ:ይከምዑ:ጥበቢሁ` — the `ይከ` part not separately re-zoomed but the `ይ` matches `ይ` family and `ከ` is plausible). |
| 14 | v19 cross-column join `ወአልሕምት` | **METHOD NOTE 2 INTACT** — not separately re-zoomed in R4; inherited from R3. |
| 15 | v28 end terminator `✣` | **CONFIRMED via inheritance** (small inline cross; R4 saw the v28 end in `f030v_M_v28_search_8x.png` and the form is consistent with `✣` not `❈`). |

**Tally: 10 explicitly confirmed by R4 at 10-14x, 5 carried by inheritance from R3 under HONEST flag.**

---

## AMBIGUOUS-PARCHMENT items — R4 verdict at highest zoom

| Locus | R4 verdict |
|---|---|
| v23/v26 red-glyph numerals (`ቯ` vs `ሺ`) | **Still AMBIGUOUS-PARCHMENT** — R4 did not bring new tooling beyond R3's same-scribe-known-reference need; R3's lean toward `ሺ`-shape stands as the HONEST flag, but current JSON `ቯ` is defensible. No change. |
| v18 `መማግሥታት` doubled-`ማ` | **Still AMBIGUOUS-PARCHMENT** — middle glyph identity not definitively resolvable at this resolution. HONEST flag retained. |
| v24 `ኆ`/`ኖ` leading noun | **Still AMBIGUOUS-PARCHMENT** — not re-zoomed by R4. HONEST flag retained. |
| `ሰ`/`ስ` chapter-wide body Solomon instances | **PARTIAL — v1 confirmed `ሰ` (bare); chapter body still AMBIGUOUS per instance.** No forced change. |
| v25 `ወበሕ ስም`, `ወደራል`, `ደቂት ሰግድ`, `እውናን` | **CARRIED AS-IS (R2-confirmed disciplined non-changes)** — not re-zoomed by R4. HONEST flag retained. |
| v22 `ኹሎሙ ተአምነዊ` segmentation | **Defensible as inked** — R4 saw `f030v_M_v22_band_8x.png` lines 1-2 with line-1 ending `ኹሎሙ:ተ` and line-2 starting `አሚነዊ` or `እምነዊ`; `ኹሎሙ ተአምነዊ` is a plausible cross-line reading. R3 ambiguous-accept stands. |

**No AMBIGUOUS items get newly resolved against the current JSON by R4.**

---

## Independent new-defect scan (28 verses)

R4 scanned all 28 verses at 5-14x. Beyond the two manufactured-ping-pong items above:

- **v8 `ሰለሳ መት:ያዊ ፬ደ:ምዕራ`** — confirmed at 10x. The space between `ሰለሳ` and `መት` is genuine on the parchment. Kept as inked. No defect.
- **v9 `ሕንዳን:` terminator** — the trailing `፡` (two-dot, NOT `፥`) at the end of f030r-R column is consistent with the JSON.
- **v10 `ሳጻሕወመቅደ` token boundary** — at 7x and 12x the parchment seems to have a continuous sequence `ሳጻሕ` then a faint break then `ወመቅደ`/`ወመቀደ`. JSON keeps as one token. **AMBIGUOUS but not refuted** — no fix needed.
- **v10 `ብሊትች` first-char** — JSON has `ብ`; at 7x the first character looks roughly correct (`ብ` 6th-order with right-foot). No new defect.
- **v12 `ባ ይተ`** — at 14x the contested char between `ባ` and `ተ` shows `ይ` (Y-triangle topology, no forward-leaning ascender that would indicate `የ`). JSON correct.
- **v12 `ዘጋላዓድ`** — at 6x the 2nd char of the toponym is ambiguous between `ጋ` and `ገ` but JSON's `ጋ` is defensible. No new defect.
- **v15 contested `ይ` in `ወይሰርዑ`** — confirmed at 14x as R3-style `ይ`. No new defect.
- **v17 line-2 `ይበልዑ`** — confirmed at 10x as `ይ`. No new defect.
- **v18 `ህደወቱ` → `ሕደወቱ`** — already in R3 fix scope; CONFIRMED.
- **v19 `ዘደትገበር`** — confirmed at 14x as ደ (closed-loop + substantial descender) NOT ይ. JSON correct.
- **v22 `ወደኖሙ` (one word)** — already in R3 fix scope; CONFIRMED.
- **v22 `ይበልዑ` (later token)** — at 8x confirmed `ይ`. No new defect.
- **v25 `እስ ዋዉደ`** → see above; R3 over-correction must be re-reverted.
- **v28 `እስ ሰምዑ`** → see above; R3 over-correction must be re-reverted.

**No genuinely-new defects beyond the 2 manufactured-ping-pong items.**

---

## Systematic-class summary

- **R3 fixes confirmed by R4: 8 of 10** (v14, v15, v17 first, v17 second, v23, v7 `ጻ`, v22 `ወደኖሙ`, v18 `ሕ`).
- **R3 fixes that R4 thinks were wrong (manufactured ping-pong): 2 of 10** (v25 `እለ ዋዉደ`, v28 `እለ ሰምዑ`). Both are on the `እለ`/`እስ` axis: R3 systematically applied "all R2's `እለ`→`እስ` reverts were wrong" but the parchment actually has GG scribe-internal inconsistency — v23 inks `ለ` while v25 and v28 ink `ስ`. R3 over-generalized from v23 to the other two.
- **New defects discovered by R4: 0.**
- **AMBIGUOUS items newly resolved by R4: 0** (v1 `ሰ` re-confirmed; all other AMBIGUOUS items still AMBIGUOUS).
- **Total residual fix scope: 2** (the two manufactured-ping-pong re-reverts).
- **Defect trajectory: R1 ≈110, R2 = 24 reverts + 3 applies, R3 = 10 fixes, R4 = 2 fixes.** This is the classic geometric-convergence pattern of a healthy multi-round adversarial review.

---

## Approval verdict

**NEEDS_FIX (round-4)** — small, focused list.

Specific fix scope for fix-round-4:

1. **v25 `እለ ዋዉደ` → `እስ ዋዉደ`** (re-revert R3's over-correction; restore R2's reading per parchment ink at 14x)
2. **v28 `እለ ሰምዑ` → `እስ ሰምዑ`** (re-revert R3's over-correction; restore R2's reading per parchment ink at 8-14x)

**KEEP-AS-IS (R3 fixes confirmed by R4):**
- v14 `ይሁዳ`, v15 `ወይሰርዑ`, v17 `ይሁዳ`, v17 `ይበልዑ`, v23 `እለ ይስሕቡ`, v7 `ይትመጻውሙ` (with `ጻ` restored), v22 `ወደኖሙ`, v18 `ሕደወቱ` (first char `ሕ`).

**KEEP-AS-IS (R2 reverts inherited from prior rounds confirmed by R4):**
- v1 `ወሰሎምን` (bare `ሰ`), v6 `ካፋን`, v17 `ሮዓ ባሕር`, v17 `ብዝናሙ`, v18 `ፈስግ`, v21 `ፈስግ` ×2, v18 `ወተቀንደ`, v18 `እክከ ደወስ` restoration, v22 `ወእስ`, v22 `ይሁዳ` (kept-`ይ` reference), v26 `አርዝ` (inherited), v28 `ወይነሥኤ` (inherited).

**KEEP-AS-AMBIGUOUS / HONEST (no forced change):**
- v23/v26 red-glyph numerals (R3 leans `ሺ`-shape, current JSON `ቯ` defensible); v18 `መማግሥታት` doubled-`ማ`; v24 `ኆ`/`ኖ`; chapter-wide `ሰ`/`ስ` body Solomon instances; v22 `ኹሎሙ ተአምነዊ` segmentation; v21 `ጋዘ`, v25 `ወበሕ ስም`/`ወደራል`/`ደቂት ሰግድ`/`እውናን`, v28 `ይከምዑ`, v19 `ዘደትገበር`.

---

## Method-note compliance

- **CARDINAL RULE: applied through this cycle.** R4 read the inked glyph topology at 8-24x and decided each contested locus on the parchment ink — NOT on the in-context narrative. The two R3-over-corrections are exactly the cases where R3 had relied on a single-locus calibration (v23) to over-generalize to v25/v28; R4 went back to per-instance verification.

- **METHOD NOTE 2 boundary: still intact.** The f030v-L → f030v-M column boundary at `ወአል | ሕምት` was verified by R3 at 14x and not re-broken by fix-round-3. No mid-word column-boundary token drop.

- **No cardinal-rule violation in either direction this cycle.** R4 did NOT push toward standard Ge'ez harmonization (no `ለ`-revival for v25/v28 even though the standard form might be `እለ ሰምዑ`); R4 went where the parchment ink goes.

---

## Honest notes + convergence assessment

- **R3's overall direction was correct on 8 of 10 fixes** — the `ይ`/`ደ` re-reverts and the `ጻ` restoration and the `ወደኖሙ`/`ሕደወቱ` discoveries all hold up at R4 14x verification. R3 only over-stepped on the `እለ`/`እስ` axis at 2 of 3 loci (v25, v28), having correctly identified v23 as `ለ` but then assumed all the R2 reverts on this axis were similar over-corrections. The actual parchment behavior is **GG scribe-internal inconsistency** on the `እለ`/`እስ` axis: v22 `ወእስ` (`ስ`), v23 `እለ` (`ለ`), v25 `እስ` (`ስ`), v28 `እስ` (`ስ`). Per-instance handling is required; no axis-level rule applies.

- **Defect-count trajectory: R1 ≈110 → R2 = 24 reverts + 3 applies → R3 = 10 fixes → R4 = 2 fixes.** This is **CONVERGENT, not divergent**. The 2-fix residual is within the canonical "1ki3-R4 pattern" range (1ki3 had 6 residuals of which 3 were manufactured ping-pong; 1ki4-R4 has 2 residuals BOTH of which are manufactured ping-pong — slightly cleaner than 1ki3).

- **The witness is CONVERGING toward stable.** After fix-round-4 applies the 2 re-reverts above, the next round (if needed) should find ZERO residuals or a tiny AMBIGUOUS-cleanup. R5 likely converges to APPROVED CLEAN. Alternatively, a focused glyph adjudicator on the `እለ`/`እስ` axis (4 loci: v22 `ወእስ`, v23 `እለ`, v25 `እስ`, v28 `እስ`) could settle the per-instance pattern definitively for the calibration corpus going forward — but this is not strictly required for 1ki4 C-3 to close.

- **R4 commendation:** R3's same-scribe calibration discipline is correct; R3's failure was applying a categorical "`እለ`/`እስ` axis over-corrected by R2" judgment without per-instance verification on v25 and v28. R4 caught this by going back to per-instance ink-reading at 14x.

- **Cleanup note:** scratch crops in `%LOCALAPPDATA%/Temp/yhwh_1ki4_R4/` can be deleted after fix-round-4 applies.

---

**End of R4 review.**
