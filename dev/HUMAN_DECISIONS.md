# Human decisions — what the project needs from the user

> **The answer to "what do you need from me?"** This is the durable, **append-only** queue of
> the only things that genuinely require Boggy (a real device he owns, an account/credential
> only he holds, a publish/tag he must trigger, or a judgment call the rules don't already
> settle). The autonomy doctrine (RULES §2.6 step 4, guard #5(a), memory
> `feedback_autonomous_work_ladder`) routes every human-gated need HERE instead of stopping the
> session — work continues on everything else.
>
> **Rules of this file:** append a row when a new human-need surfaces; never silently drop one.
> Mark a row **Resolved** (don't delete it) when the user acts, with the date. Anything NOT in
> this file is something Claude can and should do autonomously. This is NOT a rolling truth
> record — it does **not** rotate.

| Date raised | What's needed | Why it needs the user | What it unblocks | Raising lane | Status |
|---|---|---|---|---|---|
| 2026-06-21 | **Kobo tap round** on the COLOR Kobo (the flagship `ethiopian-tewahedo` `.kepub.epub`, then the rest of the M3 set) | Real on-device popup/tap behavior — only the physical color Kobo is the true oracle (memory `kobo_color_ereader_end_stage_qa`) | M3 Kobo catalog column goes live; v1.0.0 gate | windows | OPEN |
| 2026-06-21 | **Apple Books device re-QA** of the rebuilt tablet artifact (`ethiopian-tewahedo --target-reader tablet`, badges restored post-Opt#3 revert) | The user's prior device verdict was FAIL (§user-fail M2); only his Apple device confirms the fix | M2 Apple sign-off; v1.0.0 gate | windows | OPEN |
| 2026-06-21 | **Send-to-Kindle device check** — load the deliverable onto the user's own Kindle and confirm notes render as visible endnotes | STK upload + on-device read is the M4 acceptance oracle (KDP is only a test oracle, not the target — memory `project_kindle_sendtokindle_goal`) | M4 Kindle device sign-off; v1.0.0 gate | windows | OPEN |
| 2026-06-21 | **Google Play Books phone QA** of one edition | M5 needs a real phone upload + read; `live:false` until then | M5 Play column; v1.0.0 gate | windows | OPEN |
| 2026-06-21 | **Run the `v1.0.0` tag command** when the release plan's Definition of Done is green | Cutting the release tag is the user's call (the gate's final human step) | The v1.0.0 release | windows | OPEN |
| 2026-06-23 | **The verse/page where the `†` dagger badge jumped to the wrong "II" study note** (Kobo device-QA B-3) | Couldn't reproduce from the build — Gen 1:1 `†` resolves correctly; need the exact locus to trace the bad anchor/href | Closes Kobo device-QA B-3 (`dev/audit/kobo-device-qa-2026-06-23.md`) | windows | OPEN |
| 2026-06-24 | **Page-break on-device eyeball** — load the staged merged flagship kepub (`C:\Users\bogda\YHWH-device-staging\YHWH-koboQA.kepub.epub`) on the color Kobo and confirm Genesis→Revelation flows cleanly with breaks ONLY at book titles (no mid-chapter / gen 10:6→7 / gen 3→4 half-empty pages) | The page-break fix (Parts 1+2) is verified by `audit_spine_breaks.py` PASS + epubcheck 0/0/0/0, but only the physical Kobo confirms the on-device reading flow | Closes the page-break device-QA (A); confirms the v1.0.0 eink reading experience | windows | **Resolved 2026-06-24** — user device eyeball: NO page breaks Genesis→Revelation (program doc + IN_FLIGHT) |
| 2026-06-24 | **Poetry mid-verse "breaks" — fix or keep?** Of the 62 WS1 mid-verse breaks, **17 are in poetry** (psa 13 · sng 4): a single verse split across poetic lines (each line its own `<p>`). The other ~45 are narrative-prose breaks (gen/num/jdg/1ch/2ch/job/jer/…) — unambiguous defects I'm fixing. | Whether psalm/Song verse-per-line is a *defect* (merge like prose) or *intentional poetry typography* (keep) is a presentation call; your device complaint was narrative-only. **Default (proceeding): KEEP poetry line-structure, fix only the narrative breaks.** Object if you want poetry merged too. | Finalizes the WS1 fix scope | windows | **Resolved 2026-06-25** — user: "keep". Poetry line-structure preserved; WS1 fix targets the ~45 narrative breaks only. |
| 2026-06-24 | **Hebrew/Greek font A/B on the color Kobo** — on the staged kepub, open a Hebrew popup with the Aa reading font set to **"Cardo"** vs **"Publisher Default"**; report whether Hebrew/Greek render as real glyphs or tofu in each | Decides the one question sources can't (`dev/audit/kobo-font-override-research.md` Q1): does an author `!important` beat Kobo's firmware reading-font override, or is "Publisher Default" required? | Tells us whether the eink `!important` font fix is sufficient or whether the deferred "Publisher Default" front-matter page + Arabic embed are needed | windows | OPEN |
| 2026-06-25 | **Mid-verse line-break eyeball** — re-load the freshly staged `C:\Users\bogda\YHWH-device-staging\YHWH-koboQA.kepub.epub` on the color Kobo and confirm the random mid-verse line breaks are gone: spot-check **gen 17:23, 19:1, 30:1, 48:1** and read through **Genesis → Exodus** narrative — each verse should now flow as one block (no break mid-sentence) | The fix is verified by the auditor (62 → 0 narrative breaks on the built epub + kepub), but only the physical Kobo confirms the on-device reading flow; poetry (Psalms/Song) intentionally keeps its verse-per-line layout | Closes WS1 mid-verse-break device-QA; one step toward the program's final clean eyeball | windows | OPEN |
| 2026-06-25 | **Ratify the 162-verse WEB re-split (WS1 "¶" root cause).** Mac proved the "weird symbol"/¶ is NOT KJV-in-WEB contamination — it's **162 EMPTY verse anchors** across the canon: each verse's WEB text was merged into the *next* verse (off-by-one dropped boundary), so the empty verse's inlined KJV popup (¶ and all) shows on eink. Fix = re-split base verse N+1 at the WEB[N]/WEB[N+1] seam, moving WEB[N]'s clause back under the empty v-N anchor — **NO wording change** (the WEB text is already in the file; data in `dev/audit/ws1-empty-verse-resplit-data.json`). Plus 34 triage cases (legit WEB omissions like luk 17:36 / act 8:37,15:34,24:7 — leave empty; + Sirach/deuterocanon numbering offsets — per-verse). | It's a **162-verse, ALL-edition, versification-sensitive base change** that re-baselines even the 9-KJV byte-stable set, and touches scripture-verse boundaries — your faith-driven "no scripture guessing" scope means you should ratify the WEB re-split (and eyeball a sample: gen 8:15, mat 5:4, psa 10:12 before/after) before WIN re-baselines. | Unblocks the WS1 "¶"/dropped-boundary fix (the real root cause of finding #2) | windows | OPEN |
