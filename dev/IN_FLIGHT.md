# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ 🔄 2026-06-11 (🖥️ Mac, turn 74 — K-KIN: ★BLOCKER #1 CLOSED IN-BUILD (oracle-proven, zero TOC errors) · blocker #2 ISOLATED → halfspine bisect running).** **Blocker #1 (the demoted-frame TOC class) is FULLY FIXED in the build, two organs:** (a) the splitter force-isolates ONLY real `book-title-page` divs (`_BP_TITLEPAGE_RE`) — demoted `appendix-section` frames flow with parent-book content, husk pieces can't be minted; (b) **`retarget_demoted_toc_anchors`** — ship-fix rebuild #1 (160847Z) proved KFX refuses the frame TAG itself even merged mid-content (E24010 + W14001 ×3; the E24010 message named `display:none` targets), so nav.xhtml + toc.ncx + the in-book TOC now point at each demoted book's FIRST CONTENT anchor (`#ch-b45-c1` etc.; frame keeps its bp id). **Rebuild #2 (163704Z, via the new `--target-reader` path): ALL GATES GREEN + epubcheck 0/0/0/0 + Previewer shows ZERO TOC errors (E24010/E24001/W14001 all gone).** NEW gates, both fires-on-defect-proven on real artifacts: **4k** (no frame-only husk piece) + **4l** (no TOC ref targets a demoted frame); 4h + piece-geometry re-keyed to the `book-title-page` CLASS (bare-bp-id keying = the splitter's own defect class). **Blocker #2 ISOLATED: the generic no-E-code internal error persists on the fully-current 163704Z** (orphan-vnotes dropped, husks gone, TOC clean) — chained delink-on-tochusk FAILED identically ⇒ link graph exonerated. **NEXT: rung-3 HALFSPINE bisect ON 163704Z** (`dev/kindle_bisect.py --rung halfspine --keep first|second|lo:hi`, shipped + 8 tests; probes staged on Desktop): Previewer half-first → half-second; outcomes: F/P ⇒ bisect first · P/F ⇒ bisect second · P/P ⇒ AGGREGATE size/count cause · F/F ⇒ global/duplicated construct. Previewer cycle ≈ 25-40 min/run on this box (`-convert -locale en -output ~/kp3-runs/<dir>`). Also SHIPPED this turn: **K-R6-6 chip badges** (`marker_badge_style: chip|glyph+count`, eink→chip via ONE resolver — ◈ never renders on Kobo; emitter + CSS + API validator + /customize, 13 tests) · `dev/build_kindle_safe.py` → `build_one(target_reader="kindle")` (yaml flip retired; stamp verified in-zip) · rebased onto WIN M1 (6702af52), 111 cross-slice tests green. ④ STK upload: **HOLD until Previewer converts clean** (blocker #2 open). HOLD: K-R4-2 cap re-tune (user taps pending).
>
> **▶ 🔄 2026-06-11 (🪟 Windows, turn 75 — MATRIX M1 SHIPPED; CI dispatch + catalog go-live remaining).** All 5 M1 items DONE + e2e-proven (SESSION_STATE top has the full anatomy): FORMAT_MATRIX one-home (+COVER_COLOURS+catalog_asset_name) · 18 committed composites (CI never composites in-runner) · swap_epub_cover + build_format_matrix driver + format-matrix.yml (proof: catholic-study ×2 cells, epubcheck --strict 0/0/0/0 ×2, ALL K-R2 GATES GREEN ×2) · gen_release_catalog (full-count gating, paginated, legacy-Kobo cell; proven vs the live release) · releases.html catalog band (visual QA passed; dark state deploys quiet). **★M1 CLOSED — CATALOG LIVE (same turn):** ① 5-leg sync `matrix-m1-shipped` (6702af52) ✓ → ② CI run 27364486873 dispatched (user-authorized after an auto-mode soft-deny) → **completed/success — all 9 edition jobs + fan-in green; 18 assets + SHA256SUMS on v0.1.0 (25 assets total)** → ③ catalog regenerated (everywhere+apple live ×9, sha256s resolved) + site deployed (`yhwh-site-publish` 01049a8→9f2332a) + **LIVE-VERIFIED on www.yhwhyaway.com/releases.html** (both format panels, 18 links, Kobo legacy note). **Open:** ④ next lanes — M2 colour variants (the 225-composite commit-vs-pin decision) · M3 Kobo column (K-R4-2 cap, user taps pending) · M4 Kindle column (Mac kindle_safe) · M5 Play Books (user phone-QA) · Phase D1b PO Esther (paused ~p35).
>

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

## Background backlog (never single-thread — RULES §2.5)

> When a lane frees, auto-pick the next: the mint-7 plan phases · CAM hi-res pre-pull · base-structured re-collation · geez→kjv xref anchoring · Phase-E Clementine (1es/2es) · doc-coherence currency · test-coverage growth · Phase-D source acquisition.
