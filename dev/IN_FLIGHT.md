# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ ✅ MILESTONE 2026-06-10 (🪟 Windows, turn 61) — the K-R2 arc CLOSED on the artifact: round-3 kepub ON THE KOBO (`G:\YHWH-Ethiopian-Bible-koboQA-r3.kepub.epub`, 34.6 MB) + all 5 fonts on `G:\fonts` (Naskh added, user-directed). ▶ NEXT SESSION = the USER's round-3 eyeball, then θ.4 → STAGE D icons → STAGE F v0.1.0 cut.** Artifact gates ALL GREEN (epub `kr2c` + the kepub post-koboSpan, via the NEW `dev/verify_kr2_build.py`): 374 pieces · 86 title singletons · max 405 KB/mean 233 KB (was 881/666) · ToC standalone · NO stranded openers (piece + FILE seams) · 66,498 noterefs resolve · OPF 83 · ONE Colophon · epubcheck 0/0/0/0 · kepubify 1/0/0. The real-artifact verify CAUGHT a 6th class (5 pre-existing FILE-seam orphans: Gen 27/1Ch 3/Ps 73/Isa 33/Jer 25) → cross-FILE opener pop (`e7685d4b`; first regex draft lazy-span-defeated on real data — tempered-dot fix; the synthetic had masked it). Mac's AST guard caught the dead version param post-rebase (`16c976b0`). Font pack BUILT staged (`dist/yhwh-kobo-font-pack.zip`; upload gated on the user's font experiment). **USER round-3 checklist:** ① every book title on its own page? ② Gen 1:1 ◈15 pops (no ToC jump)? ③ numerals centered, no stranded "27"/"3"? ④ library card says 83? ⑤ ONE Colophon in the ToC? ⑥ set Cardo as reading font → does the Footnote-preview dialog render Hebrew/Greek (the font-pack ship gate)? **▶ MAC turn-61 board (6 items):** splitter adversarial review · canon-filtered catholic-study seam gate · kepub deep-verify · TARGET_CAPS capability research · STAGE-F copy re-true #2 · expandable-Contents device recipe. Baton **windows** (truth_owner); mode=parallel.
> **▶ ✅ DONE 2026-06-09 (🖥️ Mac, turn 60) — the turn-57/58 adversarial review RECOVERED (power-cut mid-wrap) + ALL findings fixed; K-R2 R1 research ANSWERED (vendor-confirmed: split at book boundaries); R2 font-pack prep landed. Mac backlog turn-59 = FULLY CLEAR.** The kids cut power right after the review workflow finished — the result survived in the journal (`wf_12a4c922-823`), recovered intact. **Review verdict (`notes/2026-06-09-turn57-58-win-review.md`):** both WIN commits FAITHFUL; 13 confirmed (3 med, rest low/info; dedup → 9 actionable + a verified-correct map), 2 refuted (the CSP fixed-window "survivor" is structurally sound; the artifact-gate provenance is proper lane truth-record). **All actionable fixed this commit:** ① Ethiopic **Extended-B** (U+1E7E0-1E7FF) appended at **7 sibling sites** (stylesheet.css + style_config + app skin `_design.py` + website style.css + dist + LICENSES + ATTRIBUTIONS; the K② recipe's 4-*ranges* were misread as 4-*blocks* — font cmap-verified glyph-backed everywhere incl. the website woff2, re-parsed this session) + `_ETHIOPIC_RANGE` pin re-trued; ② K① numbers-mode sibling: `.note-ref + .vn-link { margin-left: 0.4em }` + kepub `#book-inner` 0.7em twin (adjacency-only — a blanket right-margin would loosen mid-verse typography) + pin; ③ W4 **class** swept: dead `version` dropped from the 5 render_* siblings + the 3-function cascade it exposed (`inject_copyright/dedication/symbol_legend` + `_write_topical_page`; the inject family was never uniform) + an AST self-enforcing guard `TestNoDeadVersionParams`; ④ OPF pin asserts the Noto item's `media-type="font/ttf"` itself; ⑤ fonts README rewritten to shipped reality (was prescribing "acquire Noto SANS Ethiopic via apply_style"); ⑥ W3 emitter-side stays-dead twin; ⑦ spec §4.1 fifth-stack note + test-comment re-true; ⑧ count corrections: b96320a0's "9 stale reds" = **8** (the `_send_json` CSP pin was GREEN at the parent — proactive hardening, not a repair) and W4 swept **11** call sites, not 10. **Gates:** font/marker/topical/skin/phi1 batch **123 green incl. the e2e built-EPUB font gate** (new range + OPF item shape proven in-zip on a real eth build) · presentation-polish 41 · TestBuildEdition+ψ.19.1 46 · topical re-run 62/62 · mypy clean · ruff = baseline · lint 29✓/3 pre-existing warns (the 1 "fail" = this tracker's freshness check, cleared by this very entry). **▶ R1 ANSWERED (`notes/2026-06-09-kepub-pagebreak-research.md`, 4-angle sweep + adversarial re-fetch):** Kobo's OWN epub-spec lists **N for all 12 page-break properties on e-ink kepub** — a decade-persistent, Kobo-acknowledged limitation; **a new spine file is the ONLY guaranteed break** (explains breaks firing at `index_split` boundaries but never mid-file; nesting/`#book-inner` is NOT the variable — the failure is categorical; kepubify is break-neutral). **WIN prescription:** `apply_file_split` forces a piece boundary before every `<section class="book-title-page">` (also collaterally helps K-R2-2's "jump to ToC"), keep the CSS for Apple Books, verify kepubify's dummy-titlepage heuristic post-split, gate on canon-filtered catholic-study. **▶ R2 PREP DONE (`notes/2026-06-09-kobo-font-pack.md`):** `yhwh-kobo-font-pack.zip` shape (flat ttf ×5 + README.txt + LICENSE-OFL.txt — standalone font redistribution MUST carry the OFL text, unlike in-EPUB embeds), Noto Naskh Arabic registered in LICENSES/ATTRIBUTIONS (version/size placeholders pinned at WIN pack-build), Guide copy DRAFTED in-note but **held off the live site until the artifact exists** (never-over-claim). Ship gate = the user's staged-fonts eyeball (does the preview dialog follow the reading font?). **▶ NEXT (win, K-R2 sequence):** repro K-R2-2 → splitter-at-book-boundaries (R1 note = the recipe) → numerals → metadata sweep → rebuild+reload G:\. **▶ MAC:** W1 AB① contingency armed (fires on the user's Apple Books re-test) · M3 armed (dmg on the v0.1.0 cut). Junk swept: a stray empty `uv.lock` stub deleted.
>
> **▶ 🔄 2026-06-09 (🪟 Windows, turn 60) — the K-R2 Kobo round-2 FIX ARC: structural + targeting + sweep slices SHIPPED locally; Mac turn-60 PULLED + RECONCILED (its R1 research independently prescribes the splitter fix I shipped — vendor-confirmed; its font-pack note = the task-6 recipe); NEXT = rebuild eth + kepub → reload `G:\` → milestone push → user round 3.** Shipped (local commits): **K-R2-1/2/4 structural (`6cb498ee`)** — forced book-title singleton pieces in `split_html_document` (a fresh spine file = the only break Kobo kepub honors; also bounds the K-R2-2 navigate-fallback blast radius — the ToC now sits in its OWN piece), ch-heading-CLASS cut candidates (the real base's `page_N` ids gave NO hard candidates → 700-880 KB over-cap pieces; mean piece should now drop ≤ ~target), no-trailing-opener pop rule (kobo9/13's stranded "3" = the ch3 heading at piece 000_00's tail — exactly this seam). **K-R2 reader targeting (`1107ded1`, user-directed mid-session)** — wizard step-1 "Where will you read it?" (everywhere/eink/tablet/computer) + TARGET_CAPS gating (expandable Contents = tablet-only, grayed elsewhere with reasons); `target_reader` field through schema/API/customize; `reader_toc_collapsible` = STRICT opt-in (the books_only coupling + the never-surfaced api_customize_data fields had silently killed the /customize checkbox on every edition). **K-R2-4 (`5e91a742`)** — numerals center on the inner `.section-heading` block (Kobo justification stomps `<p>` text-align). **K-R2-5/6/7/8 sweep (this commit)** — 3,477 alt-name replacements (books.yaml/nav/ncx/h1s/title-attrs; nested-anchor green); OPF 88→83 + per-edition `dc:description` (patch_opf) + eth description; ONE Colophon (front = "Copyright"; closing drops Generated/URN, `closing_colophon` option); K-R2-5 audit = lang markup already correct, NO action. **K-R2-3:** font pack STAGED on `G:\fonts` (user-approved as a website/release artifact — task open: pack + LICENSES for Noto Naskh Arabic [Mac was prepping] + Guide step). **▶ REBUILD GATES (task 5):** eth `--force` → epubcheck 0/0/0/0 · nested-anchor 0 · noteref in-piece resolution · NEW piece-size distribution check (expect ToC its own piece + title singletons + mean ≤ ~530 KB kepub) · kepubify → swap `G:\` (Move-Item, never delete) → 5-leg milestone push. Mac `bbdd9b9` pulled: its R1 research note (`notes/2026-06-09-kepub-pagebreak-research.md`) CONFIRMS the shipped approach (Kobo e-ink kepub honors NO page-break CSS — a new spine file is the only break) and adds two post-split checks for the rebuild gates: kepubify's dummy-titlepage heuristic + a canon-filtered catholic-study gate; its font-pack note (`notes/2026-06-09-kobo-font-pack.md`) = the task-6 artifact recipe (flat ttf ×5 + README + OFL text; Naskh registered in LICENSES).
> **▶ ✅ DONE 2026-06-09 (🪟 Windows) — Mac's M2 finding RESOLVED: the 10 η.1 "sample" seed notes PURGED from Genesis (user GO'd "remove all 10"). The "nothing invented / named PD sources" promise holds again.** Mac's device-QA M2 PASSED (the cascade survives backgrounds-off) and flagged ~10 gen notes shipping with the placeholder attribution _"Reference sample note (η.1) — seeded so every symbol has a displayable example"_ — AI-seeded bodies (ANE context, Gen 1/Gen 2 harmonization, Isaac→Christ typology, the Babel chiasm, the 1 Enoch parallel, …) contradicting the project's core promise. **The user chose REMOVE.** ★ Discovered the seeds live in TWO places (base drift): the live store `content/notes/gen.py` (10 tuples removed; loads 4903→4893) AND the **BAKED base** `epub_working/index_split_000.html` + `_001.html` (10 marker+aside pairs surgically removed — NOT a re-bake, so no drift regression; backed-up + id-matched exactly 1:1). Removing only from gen.py first left the baked bodies as **unattributed orphans** (the live-attribution-lookup architecture) — caught it, then removed from the base too. **Verified:** base nested-anchor 0 + marker/aside balanced (000: 556→551, 001 balanced); eth rebuild → all 10 bodies + "Reference sample note" GONE (0), Gen 1:1 **◈18→◈15**, cascade intact (40,594→40,585 groups); **epubcheck 0/0/0/0**; **175 tests green** (note-rehaul + corpus-index + build-my-bible); integrity sweep = no other placeholder/seed/η.1 attribution in any note store or the base. Corpus 91,733→91,723 (count refs are snapshots, not pins). Shared-base change ⇒ affects every edition's Genesis (intended). **▶ NEXT (win):** CDN-free HOME + rich-text editor + Win `.exe` frozen-verify + θ.4 + D/F. **▶ MAC:** the device behaviours M2-1 (popup-fires) / M2-10 (pagination) ← user's Apple Books + Kobo.
>
> **▶ ✅ DONE 2026-06-09 (🪟 Windows) — eth NOTE-REHAUL RE-BASELINE COMPLETE + fully verified (post-reboot); STAGE-C EPUB staged for device-QA; Mac flipped to M2. PARALLEL (Mac back up); baton windows.** The reboot cleared the 54 GB `AppXSvc` leak (8.4 GB free). Flipped the 3 eth flags `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True in `content/editions.yaml` (schema CLEAN; `git diff` = eth block only). **All gates GREEN:** built `ethiopian-tewahedo --force` (25.85 MB; cascade emitted — 40,594 category groups, `vn-group`==`vn-cat-head`==`vn-cat-sym`) → **byte gate ✓** (catholic-study byte-IDENTICAL before/after the flag flip, SHA `8e0fe3b5…dcdfea7`; the 9-KJV invariant holds empirically + by construction) → **epubcheck 0/0/0/0** (EPUB 3.3, no errors/warnings) → **nested-anchor 0** (`<a>` balanced 190,248/190,248) → **render-verify ✓** per Mac's S2-review gate: (a) comm-ethiopian (jhn 1:1) — group `vn-source-byline` SUPPRESSED (0), the father byline (Cyril/Athanasius `<strong>…</strong> <em>…</em> <small>(date)</small>`) in the BODY only ⇒ **no double-attribution** (BYLINE-1 proven on shipped data); (b) Gen 1:1 = **◈18**, 8 category groups, full `vn-group→vn-cat-head(glyph+label)→vn-source(byline once)→vn-item` cascade with the survivable C1–C6 cues; (c) jhn Cyril byline grammatical. **Ragged-byline failure signatures all 0** (dangling `Bk` 0 / `NPNF Series N` 0 / any `NPNF` 0 / `vol.` 0 — SK-2/POLISH-1 confirmed). note-rehaul suite **49 green**; validate_schemas CLEAN. **STAGE-C eth EPUB staged → `E:\epub-stage-c-eth\` for device-QA** (Mac can also rebuild deterministically from the committed source). **▶ AA MED/LOW arc COMPLETE — the loved η.1 skin is now WCAG-AA + manuscript-cohesive across every console (release-gating for v0.1.0):** ✅ M6 (heat ramp) · M7 (wizard stepper) · M8 (matrix sticky) · M9 (toggle) [prior MED batch `7c673614`] · **M5/M16** (cards → defined gold-line perimeter + soft shadow; the blanket alert-red top-stripe is dropped, now opt-in `.ms-card-accent`) · **M12** (welcome overlay → manuscript: parchment card, ink/sepia serif, gold CTA) · **M13** (greek/hebrew "Look up" → gold primary, unified across console families) · **M14** (apihelp method badges → a coherent indigo/gold/red manuscript trio) · **L4** (header `bg-white/25` chip → gold tint) · **L5** (sources highlight warm) · **L6** (covers slot states warm + the pre-existing placeholder AA fix) · **L7** (publisher focus glow → indigo) · **L9** (EB Garamond self-hosted via a sandboxed `/fonts/` route + `@font-face` — serves 200 `font/woff2`, traversal-guarded). **26/26 `test_skin_aa.py` pins**; visually verified (wizard / build_tracker / apihelp / greek render warm + cohesive, 0 console errors). ACCEPTED as-is (all pass contrast): M10 (4 distinct status-card border/text colours; the indigo info-card is on-brand) · L1 (deliberate amber/emerald caution callouts) · L8 (note-editor status colours — deferred per the editor-separation north star) · L2 (refuted) · L3 (decorative). **▶ NEXT (win):** CDN-free HOME + rich-text editor (Mac's idiot-proof design) + Win `.exe` frozen-verify + θ.4 + D/F. **▶ MAC (turn 48):** device-QA M2 on the STAGE-C EPUB (backgrounds-off matrix) ‖ items 3 (STAGE-F copy) + 5 (dmg recipe). `LANE_HANDOFF` turn 48.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ S2-cascade 2 HIGH fixes (+coupled) DONE + verified; ⛔ eth re-baseline BLOCKED on a 54 GB `AppXSvc` commit-leak (reboot needed); I'm SOLO over BOTH lanes (user: "take over any mac work").** Applied Mac's S2-cascade-review fixes (`notes/2026-06-09-S2-cascade-review.md`), TDD, pinned against BAKED structure: **BYLINE-1** (HIGH) — `_is_self_attributing_comm_ethiopian` now detects the SURVIVING baked shape (the `note-comm-ethiopian` wrapper class + the `<strong>/<em>/<small>` father→work→(date) triad) instead of the stored `<aside>` prefix the sanitizer strips; fixed at BOTH dead sites (S1 `:1987`, S2 `:2488` — fix-the-class) · **SK-2** (HIGH) — `_SOURCE_LOCATOR_RE` absorbs a preceding `Bk`/`Book`/`Hom.`/`Homily` (no dangling `Bk`) · **POLISH-1** — series-strip loops to a fixpoint (no dangling `NPNF Series N`) · **BYLINE-4** — `any()`→`all()` (a self-attributing row can't hide a co-bucketed plain byline) · **S2-GUARD-3** — leaf count hardened to the `<div class="vn-item note-` wrapper via `_count_cascade_leaves` · **S2-GUARD-1** — documented the leaf-count downgrade (construction proof) · **SK-4** — spec count refreshed (22 over-collapse keys = 19 comm-ethiopian-suppressed + 3 rendering, all intended/lossless; verified vs corpus). 7 new/updated tests in `test_note_rehaul.py`; **full suite 49 green** + `TestNoteRehaulS1Helpers/S2SourceKey/S2Cascade` 24 green; ruff format+check clean. **NOT yet committed when the leak was found.** **⛔ BLOCKER: system commit at 99% (`AppXSvc` svchost PID leaking ~54 GB committed — Windows Store-deploy bug; non-elevated restart/taskkill = Access Denied).** This is what OOM-crashed the box earlier + MemoryError'd `test_marker_style`'s build at `build_edition.py:2647`. **A reboot (or elevated `net stop AppXSvc`) frees it; the eth re-baseline + any full build MUST wait for that.** **▶ POST-REBOOT (win, solo):** re-run the pre-commit gate → **eth re-baseline** (add `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` `true` to the ethiopian-tewahedo block in `content/editions.yaml` → build `ethiopian-tewahedo --force` → byte gate [9-KJV byte-identical; eth changes intended] → epubcheck 0/0/0/0 → nested-anchor → render-verify per the review gate: comm-ethiopian verse no double-attribution/no ragged `Bk`/`NPNF`, Gen 1:1 `◈16` cascade, jhn Cyril grammatical) → STAGE-C EPUB. Then AA MED/LOW + HOME + rich-text editor + Win `.exe` + θ.4 + D/F. Baton **windows (solo)**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ app-UX skin AA arc (HIGH set + invisible-data MEDIUMs) SHIPPED (`1ed22783`); pulled Mac turn-45; MAC OFFLINE for the night → I'M SOLO (no Mac planning until the user says so).** Crash-recovery: an OOM hit before push left tonight's work uncommitted but intact — verified (482 skin/console-presentation tests green; `lint_rules.py` 31/0; ruff format clean), rebased onto Mac's turn-45 (`00fc05b0`), committed. **AA arc** (per the user's keep-gold + lighter-hover `#C49A2E` + indigo accent decision, from `docs/superpowers/notes/2026-06-09-eta1-skin-adversarial-review.md`): closed **all ship-blocking HIGHs** — H1 gold-hover 3.46→`#C49A2E` (6.01:1) · H2 dark-input 1.08:1 → theme-aware `--ms-field-bg` · H3/M11 dark-theme-complete (`:root[data-theme=dark]` block + light grounds scoped behind `:root:not([data-theme=dark])`) · H4 hint-text slate-400 ×176 → `#6E5840` · H5/H6 emerald CTAs → gold primary — **plus the invisible-data MEDIUMs** M1 (book-rows) · M2 (matrix counts) · M3 (compare verse-num/missing) · M4 (input border solid `--ms-gold-line`) · M15 (header double-rule). New `tests/test_skin_aa.py` computes true WCAG luminance + pins every pair (14 green). **Pulled Mac turn-45 (`00fc05b0`):** S2-cascade adversarial review (**2 HIGH fix-before-bake** — `docs/superpowers/notes/2026-06-09-S2-cascade-review.md`), HOME-html AA colors (`…-home-html-aa-colors.md`), M2 backgrounds-off QA matrix. **★ The eth re-baseline gate (Mac's S2 review) is now SATISFIED** → actionable SOLO. **▶ NEXT (win, solo, in order):** (1) read the S2-cascade-review's 2 HIGH fixes → apply → THEN the **eth re-baseline** (flip eth `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True → build + byte gate + epubcheck + nested-anchor); (2) remaining AA **MEDIUM/LOW** (M5 card-stripe scope · M6 build_tracker heat ramp · M7 wizard stepper · M8 matrix sticky headers · M9/M10/M13/M14 status-color policy · M12 welcome overlay · M16 card perimeter; L-series incl. L9 EB-Garamond `/fonts/` serving) + HOME AA colors per Mac's note; (3) CDN-free HOME + rich-text note editor + Win `.exe` frozen-verify + θ.4 + D/F. Milestone push = 4 commits (lane_ping fix `f830827a` · S3a `19c7f6cb` · IN_FLIGHT `ab9f45d9` · AA arc `1ed22783`). Baton **windows (solo)**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ note-rehaul S2 (cascade) SHIPPED (`90ac7dc9`) + the S2 attribution-sourcing question RESOLVED (supersedes the "embed into `build_aside` + re-bake" guess a few blocks down). DECISION: a BUILD-TIME LIVE attribution lookup by note id, NOT a re-bake.** The 3-probe drift investigation (`wf_fac9b66a-9ac`) found drift is **kind=0 / ids 100% stable** (only 1,370 stale LABELS + 3 stale bodies — logged as a separate pre-existing finding, possibly the baked labels are RICHER than live's "Note" so re-deriving could regress) ⇒ a live attribution lookup is base-consistent; the re-bake path is HIGH-risk (no clean entrypoint; mutates the SHARED base ⇒ breaks 9-KJV byte-identity + the `build_aside↔rewrite_asides` parity pin + the `categorize_diff` verifier; forces a 10-edition re-release). Shipped: the cascade emission in `apply_badge_markers` + `_source_display`/`_source_key`/`_note_attribution_index`/`_emit_cascade_sections`/`apply_note_cascade_css` (15 group spines, reuse `stylesheet.css:751-791` hues + topic `#5A5F7E`) + the §4 leaf-conservation guard + comm-ethiopian byline suppression + `note_group_by_category`/`note_topic_dedup` wiring; **36 rehaul tests green**; flag absent on every edition ⇒ byte-identical (latent). Pushed as a milestone WITH a fat Mac turn-44 batch (adversarial-review S2 before the re-baseline + M2 backgrounds-off QA matrix + expand STAGE-F + finalize HOME-vs-AA + v0.1.0 dmg recipe). **✅ S3a DONE** (`a94748a8`) — vocab-aware topic union (longest-match against the 5,232-name Nave/Torrey vocab; NO comma-split; one merged Topics row, deduped, `·`-joined, sources cited in the byline); 8 InBuild green (S1 3 + S2 3 + S3a 2). Also `ef4269e0`: lane_ping `--before-push` false-BEHIND fixed (merge-base). **Local commits NOT yet pushed** (remotes at the turn-44 milestone `69a13674`; push at the re-baseline milestone). **▶ NEXT (win), in order:** (1) the **eth re-baseline** (flip eth `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True → build + byte gate + epubcheck + nested-anchor → STAGE-C EPUB to Mac M2) is **GATED on Mac's turn-44 adversarial review of the S2 cascade** (I asked Mac to review BEFORE I bake); (2) **the app-UX skin AA arc in parallel** (`docs/superpowers/notes/2026-06-09-eta1-skin-adversarial-review.md`: ship-blocking HIGHs — gold-hover 3.46→`#C49A2E` 6.01:1 at `_design.py:236-238`; dark-mode input 1.08:1 `:246`; a `:root[data-theme="dark"]` skin block; hint-text 2.58:1×176 `:196`/`:362`; emerald CTAs `wizard.py`; + M-series per-console hex in build_my_bible/sources/matrix/compare/build_tracker — user decision = KEEP gold buttons + `#C49A2E` hover + INDIGO `#243B6B` accents where gold fails; **must NOT break the loved skin** → careful, verify contrast). Then the CDN-free HOME + rich-text note editor (Mac's `2026-06-09-idiot-proof-app-design.md`) + Win `.exe` frozen-verify + θ.4 + D/F. Baton **windows**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ★ NEW APP-UX ARC: shipped the η.1 manuscript skin (whole-app, loved) + v0.1.0 RE-PLANNED around an "idiot-proof app" north star. ★ READ `docs/superpowers/notes/2026-06-09-v0.1.0-app-ux-replan.md` FIRST.** A live design session with the user produced: **η.1 manuscript skin** (`82144f6a`) — whole-app console skin matching www.yhwhyaway.com, injected at `web.py _send_html` (Tailwind palette remap + accent layer: beige body, dark-brown BANNER header + gold rule, GOLD primary buttons, gold-tint defined borders, serif body) + note-editor layout (aligned capitalized 3-col headers Books/Select-a-book/Editor, wrapping book titles, example placeholders) + a lint fix (`repo_map_complete` skips gitignored runtime dirs). User-validated live ("beautiful", "clean rectangles", "there ya go"). **★ NEW NORTH STAR (user, emphatic): the SHIPPED app must be IDIOT-PROOF for end-users** — readers/builders, NOT maintainers. The note-editor (raw HTML, JSON, kind budgets) is a MAINTAINER tool → must be separated from / not the default landing; replace raw-HTML note editing with a Bold/Italic rich-text toolbar; a friendly landing with the social-card BANNER as hero. **★ v0.1.0 RE-PLAN (user-requested "replan everything + fat Mac backlog"):** WIN = S2 cascade (resolve attribution-sourcing/base-rebake first) → S3a → app-UX idiot-proof impl (after Mac's design) → verify frozen-fix on Win `.exe` → θ.4 update feature → D/F. **MAC = a FAT parallel backlog** (LANE_HANDOFF turn 42): (1) DESIGN the idiot-proof app [unblocks WIN], (2) prep EB-Garamond self-hosting, (3) draft STAGE-F outward copy, (4) adversarial review of the η.1 skin across all consoles, (5) device-QA M2 ← STAGE-C EPUB. **Local commits NOT yet pushed before this milestone save.** Baton **windows**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — STAGE C: ✅ STAGE E landed (Mac M1 CLOSED) · note-rehaul S1 DONE locally (`f404686d`→rebased `f01f64d9`) · update-feature decided. INCOMING from Mac turn 41 (3 asks).** **STAGE E (`23e3c178`, pushed all 5 legs):** `launcher.spec` macOS pyobjc `hiddenimports` (webview.platforms.cocoa + objc/Foundation/AppKit/WebKit/Quartz/Security/CoreFoundation/UniformTypeIdentifiers — the real finding-7 fix) + `.icns` BUNDLE icon + `requirements-desktop` marker-gated pyobjc pins + explicit native→browser fallback in `launcher.py`; 129 desktop tests green. Lane board turn 39 hands Mac **M1** (native-window dmg, now actionable) + M0 fallback; ACKed Mac's 5 turn-38 follow-ups (object-fit, regression tests, docstrings, SESSION_STATE scope, Kobo) into STAGE C. **Note-rehaul S1 (`note_attribution_dedup`) DONE locally — TDD, base-consistent:** 6 pure helpers + the in-`apply_badge_markers` integration + wiring (web_editions / api·editions EDITABLE+EDITABLE_BOOL / customize checkbox); 14 S1 tests + the 6 badge-unit back-compat tests green; flag-off = byte-identical (the 26-pass eth integration + gen conservation prove it). **★Spec re-verified against the LIVE corpus (8-dim read-only workflow) — corrections found:** S3a "split topic terms on comma" is WRONG (250 topic names carry internal commas → 36.9% of topic notes would mis-parse → S3a MUST use the authoritative Nave/Torrey vocab, longest-match); the source-key PD-boilerplate strip doesn't actually clean TSK's tail; comm-ethiopian self-attribution = the unique `<aside class="note-comm-ethiopian">` structural marker (up to 4 notes/verse). **★★BASE-DRIFT FINDING:** `epub_working/` has drifted from the live notes (gen 1:2m = kind `word`/label "Note" in gen.py but renders lang-hebrew/"Hebrew." in the baked base) → S1 redesigned to operate on the RENDERED row + marker kind (NOT the live tuple) so it's correct regardless of drift. **S2 must source the attribution byline base-consistently** — likely embed attribution into `inject.build_aside` + re-bake (which ALSO fixes the drift); decided at S2. A wider `inject --all-books` + BAKE-AND-PROVE re-bake is a flagged precursor/parallel task. **Update feature (user-decided 2026-06-08):** θ.4 = wire the existing `core/updates.py` appcast data plane into the launcher (startup poll → gentle prompt + in-app release-notes); scope = adoption-signal + one-way notes, **NO telemetry** (roadmap LANE-T `update-feat`); build in the desktop stage. **▶ NEXT:** S2 (cascade emission + 15 per-category group-spine CSS [14 hues extracted; topic needs one, e.g. `#5A5F7E`] + comm-ethiopian byline suppression + the §4 `DISTINCT_OUT==DISTINCT_IN` guard) → flip eth flags True (single re-baseline) + full byte gate + Mac device-QA → S3a (vocab-aware topic union) → S3b later. **Local commits NOT yet pushed** (push at the cascade milestone). Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE C IN PROGRESS: 3 surgical fixes shipped (findings 3+2+1b); note-rehaul cascade is the next major arc.** Done + committed locally (suite green; push at the STAGE C milestone): **finding 3** title-page bleed (`d2970962` — `.bookpage-art` max-height:42vh + `.book-title-frame` display:block/break-inside:avoid; eth epubcheck 0/0/0/0 + in-EPUB content verified; on-device paginate-render = Mac M2) · **finding 2** Your-Edition table → float `.ye-row` block (`d2970962`) · **finding 1b** justify-scoping (`1ae3a2cc` — rewrote the stale 2026-05-24 `TestLeftAlign` "no-justify" contract to the real one [prose justified by default + a build GUARD that no ToC/title/heading/pill selector resolves to justify]; resolved 2 PRE-EXISTING reds; defensive ToC explicit-left CSS deferred pending Mac M2). **★Lesson:** on the cap=2 box NEVER run pytest concurrently with a build — overlapping runs starved each other to 35–48 min each (clean solo = fast). **▶ NEXT MAJOR ARC = the note-rehaul (S1→S2→S3a) — spec READ + internalized:** `docs/superpowers/specs/2026-06-08-note-presentation-rehaul-design.md` (build-time, lossless, option-gated; cascade verse→category→source→note inside `apply_badge_markers` `build_edition.py:1856-2074`; the NON-NEGOTIABLE completeness guard `DISTINCT_OUT==DISTINCT_IN`; 15 explicit per-category group-spine CSS; 4 bool fields wired across `web_editions`/`api/editions`[EDITABLE_BOOL+EDITABLE]/`templates/customize`/`build_edition`; Addendum A legend popup = a POST-`apply_file_split` per-piece pass; per-stage byte-stability proof). Sequence: S3a+S1 → S2 → re-baseline eth + Mac device-QA → S3b (default-OFF) later. Then ToC pill-toggle+incipit (Ph3 / finding 1·1c), finding 6 app top-nav (web-only, no EPUB), audit Phase 3 (splitter orphan Notes heading), Ph4/Ph5. Then D → E (unblocks Mac M1) → F (v0.1.0). **▶ Mac:** v0.1.0 plan handed (M1→M4); idle pending STAGE C EPUB / the E spec edit. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — ✅ STAGE B CLOSED + STAGE A COMPLETE; now on STAGE C.** STAGE B 3 real-build re-verifications ALL GREEN: byte-stability gate PASSED (deterministic; 9-KJV byte-identical by construction — build path + `epub_working` unchanged since baseline `b5ad8c98`, only 4 off-path `scripts/core` deltas) · epubcheck 0/0/0/0 on eth + catholic-study (no RSC-007/008; font chain reconciled — `apply_style` off-build-path, `@font-face` only in `epub_working/stylesheet.css`, `patch_opf_fonts` registers exactly `EMBED_FONT_PATHS`) · triple-seam on canon-filtered catholic-study CLEAN (audit 0 critical + scanner: spine gapless, `BOOK I..LXXII` gapless, cross-piece hrefs intact over 89,874 ids, 0 nested-`<a>`). No new defect. STAGE A clone-hoists shipped (`run_hebrew`/`run_greek` + `run_ai_notes`/`run_ai_xrefs` → `at_scale_base`; 8 tests re-homed; 85 at-scale + 6 bugcluster green; ruff/mypy/lint clean). **▶ NEXT = STAGE C** (presentation + note-rehaul: audit Phase 3 splitter-orphan · justify-default + guard · note-rehaul S1→S3a + Addendum A legend popup · ToC toggle + incipit · finding 3 title-page bleed · finding 2 Your-Edition table · finding 6 app top-nav) → D → E (unblocks Mac M1 native-window dmg) → F (v0.1.0). **▶ Mac:** v0.1.0 EXECUTION PLAN handed (`docs/superpowers/notes/2026-06-08-mac-lane-v0.1.0-execution-plan.md`, M1→M4 gated on WIN C/E/F); idle pending STAGE C/E. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE B data-validity cluster CLOSED; session-boundary milestone (user starts a NEW SESSION after this).** Candidate cleanup (114 out-of-extent removed — 39 files deleted + 44 rewritten, the audit's "111" undercount confirmed) + the `candidate_extent` lint (Phase 0 #2; ALL_CHECKS 32→33, +negative test, green) + **gap #4 answered:** promote validates at the boundary (`:368`/`:469`); the published-store real-loader sweep found only 3 out-of-extent notes (aes 10:11-13 lang-hebrew, NON-shipping parked residual) → **ZERO impossible coords ship**. All gates green. **▶ WIN NEXT SESSION (fresh start):** the 3 REAL-BUILD re-verifications (i font-embed byte/epubcheck · ii post-pass×canon-filter×file-split on built catholic-study · iii published-store DONE) → 2 STAGE-A clone-hoists → STAGE C (Mac diagnosis: title-page page-BLEED fix + stats-table fix + extra-popup Addendum A) → D (WIN `.ico`/Linux `.png`; Mac `.icns` done) → E (Mac PROVED native-window deps) → F (outward+release; absolute-sync). **▶ MAC:** idle pending WIN C/E. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE A done (−2 gate-nothing clone-hoists) + STAGE B keystone & build-free latent holes shipped; milestone pushed.** STAGE A Phase 5 finished (`a1552624`). **STAGE B Phase 2:** the `aes`/`_book_shape_cached` CLASS fix (`6045eb36` — scan-skip-empties + `html_chapter_count`→max(shape) + `TEWAHEDO`+=1cl/2en; byte-identical for the 80 contiguous books; promote guard now LIVE for aes — rejects ch17 / accepts ch11) + the build-free latent holes (`c78eb193` — edition_stats cache twin · prospect verse-gap skip · Naves/Torrey book-code normalize). All gates green (ruff/lint 30-0/mypy + targeted tests). **▶ WIN NEXT — STAGE B data-validity cluster + REAL BUILDS:** candidate-extent lint (Phase 0 #2) + 111-candidate cleanup (RECOUNT post-aes-fix — aes candidates now correctly flagged) · published-store out-of-extent sweep + promote-boundary trace (gap #4) · the 3 real-build re-verifications (font-embed byte/epubcheck · post-pass×canon-filter×file-split · published stores) → then the 2 STAGE-A clone-hoists → C/D/E/F. **▶ MAC (turn 34):** render-first diagnosis (title-page + stats-popup, for STAGE C) + macOS native-window finding-7 preflight (de-risk STAGE E); icns + Addendum A already landed. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — EXECUTING the v0.1.0 master plan (user ratified via "do"); STAGE A green slice committed + pushed; Mac handed 2 parallel tasks.** The FINDINGS-ONLY HOLD is LIFTED. **STAGE A shipped (test/doc/lint, 0 shipped-byte risk, all green):** audit **Phase 0** (extended `check_atomic_writes` to `content/notes` `.write_text`/`.write_bytes` + 3 atomic fixes incl. the audit-missed `fix_xref_targets.py:254`; PUT route coupled 10→11; +7 guard tests) + **Phase 1** (all 9 stale-test/doc realignments to v0.0.3 reality — canon 76/78/87→75/77/86, EMBED_FONT ×3, comm-reformation→jewish-study, retired-term, website 87→83, corpus-index EXACT via canon-restrict, 2es title, 4× REPO_MAP — numbers re-verified) + **Phase 5 partial** (render_coverage dup/mislabel +test; dead `book_native_names` helpers; `_append_cloned` numeric-0). Gate green: ruff + lint 30/0 + mypy + test files (incl. a 194-pass run). **▶ STAGE A Phase 5 now ALSO done (local `a1552624`):** bugcluster coord-guard sweep test exercises each driver's `_emit_extent_ok` · coverage shares `_compile_injected_id_re` + behavioral test · Greek/Easton/Torrey negative fires-on-defect lint tests. **Only the 2 at-scale clone-hoists remain in STAGE A** (`run_hebrew`/`run_greek` + `run_ai_notes`/`run_ai_xrefs` → `at_scale_base`) — they GATE NOTHING (offline tooling), so **▶ WIN NEXT = STAGE B first** (higher value: aes/`_book_shape_cached` CLASS fix · edition_stats stale-cache twin · candidate-extent lint [Phase 0 #2, moved here] · 111-candidate cleanup · 3 real-build re-verifications), then mop up the 2 clone-hoists, then C/D/E/F. **▶ MAC NOW (no WIN dep, pulled forward from release-time):** (1) `assets/icons/YHWH.icns` via `iconutil` (unblocks STAGE D); (2) design the user-sanctioned **extra note-helper / legend popup** as a note-rehaul spec addendum (native EPUB3 footnote, no-JS, reader-robust, per-edition builder option) for WIN to implement in STAGE C. Release-time Mac items unchanged (native-window dmg · v0.1.0 artifact · device-verify). **3 user directives recorded** (master plan + memory): outward-sync = ABSOLUTE everywhere incl. edit/delete-repost + DRAFTED X follow-ups; extra-popup latitude. `LANE_HANDOFF` turn 32. Baton **windows**.
>
> **▶ ✅ MILESTONE 2026-06-08 (🪟 Windows) — ROUND-6 AUDIT MERGED + v0.1.0 MASTER PLAN written; FINDINGS-ONLY HOLD (product fixes await the user ratifying the plan).** Win's 4 heavy dims (Opus) → 13 survivors; merged with Mac's 30 → **43 (0 crit/high; the 9-edition program is functionally MINT)** → `docs/superpowers/notes/2026-06-08-round6-split-audit-findings.md`. **★ Single source of truth for what's next = `docs/superpowers/plans/2026-06-08-v0.1.0-master-plan.md`** — sequences audit + device-QA 1–7 + note-rehaul S1–S4 + app icons + outward surfaces → **v0.1.0 (still beta)**, safest-first (A green/honest → B latent holes → C presentation → D icons → E mac native-window → F outward+release); lane division inside. Committed `deep-audit.js` default flipped to **Opus**. **▶ ON BOOT (either lane):** read this board → the master plan → the findings note + the device-QA note. **Win** owns shared-code impl (Stages A–F); **Mac** = the turn-30 laundry list (START the note-rehaul design spec now; release-time `.icns` + native-window dmg + v0.1.0 artifact + device-verify). **DO NOT apply product fixes until the user ratifies the master plan.** `LANE_HANDOFF` turn 30. (The Mac DONE/HOLDING entry + the stale WIN round-6 on-boot runbook below are SUPERSEDED by this.)
>
> **▶ ✅ DONE / HOLDING 2026-06-08 (🖥️ Mac) — round-6 MAC audit COMPLETE + device-QA captured + v0.1.0 target set; AWAITING winclaude's merge + v0.1.0 fix instructions. DO NOT re-run the audit.** Mac ran `LANE='mac'` (14 dims) on **Opus** → **30 survivors / 0 unverified** → `_audit-split/findings-mac.json` @ `0e1e122c` on `lane-transfer/audit` (both remotes); **win merges** (truth_owner). Then captured the user's real-device QA (8 screenshots) → `docs/superpowers/notes/2026-06-08-device-qa-and-note-presentation-rehaul.md` (7 findings, reconciled onto the existing `2026-06-06` device-QA plan/spec; note-presentation north star = pretty on ANY reader / reader-robust structure / cascade hierarchy; **release target = v0.1.0, STILL A BETA**, v1.0.0 deferred further out). Shipped the `memory_hygiene` Mac-parity fix (`cc5b4907`) + codified RULES **guard #6** (cross-lane problem hand-off). **Mac lane is IDLE (findings-only hold) — next instructions come FROM winclaude** (its 4 heavy dims → the merge → the v0.1.0 fix sequence); **finding 7 (macOS native-window / browser-fallback) is a MAC-lane build task** when that starts. `LANE_HANDOFF` turn 29. (The 🪟 round-6 boot order below is the WIN lane's — still finishing its heavy dims; not a Mac action.)
>
> **▶ ON BOOT 2026-06-08 (🪟 Windows) — RUN THE ROUND-6 SPLIT DEEP-AUDIT (FINDINGS-ONLY, BEFORE FIXES). This is the marching order; everything is wired + pushed (`c5c1ba2a`).** New standing doctrine is LIVE (RULES Guard #5 + §4: never stop to ask the user · full standing authority · bandwidth-first / bare-minimum context · **LOCAL-COMMIT during work, full 5-leg push only at a milestone**). The lane sync **ping** is wired (`scripts/lane_ping.py` → the SessionStart radar + `save-all.ps1 --before-push` auto-rebase). The auditor is **refreshed to round 6 + READY**. **Plan + Mac meantime-backlog: `docs/superpowers/plans/2026-06-08-round6-split-audit-plan.md`.**
> **WIN-LANE steps:** (1) `git pull --rebase origin main` (the boot radar flags it if Mac pushed). (2) Edit `.claude/workflows/deep-audit.js` LOCALLY, do NOT commit: `const LANE = 'win'` (that one line now also fixes REPO + agent types — parity is baked). (3) `Workflow({scriptPath:"<repo>/.claude/workflows/deep-audit.js"})`; **CONFIRM the startup log = 4 dimensions** (tests-run · opt-build · byte-stability · rx-surfaces). (4) Finders/verifiers are sonnet-pinned, cap=2 — do NOT bump to Opus or add finders (cost: ~$80/1h lesson). Sleep/hibernate OFF, terminal open. (5) Parse `JSON.parse(<result>).result.survivors`; keep locally. (6) When Mac's `findings-mac.json` lands (branch `lane-transfer/audit`), run `deep-audit-merge.js` on the N95 → write `docs/superpowers/notes/2026-06-08-round6-split-audit-findings.md`. **STOP — present findings, do NOT apply fixes.** (7) Milestone 5-leg save of the findings doc.
> **MAC LANE** runs `LANE='mac'` (14 read-only dims) per the plan, mirrors its out-of-repo halves (doctrine + ping memories · its SessionStart-hook ping block · Mac save-path `--before-push` wiring) + ACKs turn 23, and works the **meantime-backlog** if it finishes before the win heavy lane. Baton **windows**. `LANE_HANDOFF` turn 23.
>
> **▶ 🔄 2026-06-07 (🪟 Windows) — v0.0.3: full-notes Ethiopian Bible + device-QA fixes; committing + pushing + deploying.** Autonomous, user-directed (low bandwidth → local commits, push/deploy at the end). **Versioning reset (user): v0.0.1/v0.0.2 = former beta.1/beta.2 → this = v0.0.3; first full = v1.0.0.** Device-QA fixes a–i: full apparatus (91,563 notes, all popups) + cross-verse dedup + justified notes (h); Psalm superscriptions wrapped (g); empty Additions-to-Esther dropped + Daniel additions demoted to appendices + per-edition BOOK-eyebrow renumber → **83 books** (f); cleaned ", or …" names (e); in-content ToC book+chapter pills (b); front-matter blank-page/colophon fit (d); Your-Edition table fixed-layout (a); website count 87→83 + v0.0.3 + de-dup release card (i). Built + **epubcheck 0/0/0/0**; kepub regenerated. **▶ NEXT: push both remotes · `gh` release v0.0.3 (EPUB+kepub+SHA256SUMS) · build+deploy website · new X post · then the website "how to use" step-by-step page · (desktop-app rebuild to v0.0.x is a follow-up — Mac-gated).** Baton **windows**.
>
> **▶ ✅ DONE 2026-06-06 (🪟 Windows) — v1.0.0-beta.2 RELEASED + website DEPLOYED LIVE; chapter-start verse-boundary bug FIXED.** Autonomous beta-2 device-QA session. **Shipped + committed + pushed (both remotes) + bundled E:/F::** **P1–P4** (`8fb606aa`): ◈ note badges, popup dedup (audit 1043→0) + category-grouped tinted cards, justify + 1.05em + reader-native serif, centred book title pages, clickable in-content ToC, book-level native ToC, structural-audit tool (`scripts/audit_epub_structure.py`). **★Verse-boundary fix (`809f711f`):** 161 chapters re-split (empty-v1 + WEB v1&2 merged-under-v2 → correct v1/v2), KJV/JPS-anchored + confidence-gated (`scripts/_fix_chapter_verse_boundaries.py` — `--dry-run` lists the **116 FLAGGED** archaic/genealogy/apocrypha chapters left untouched). **De-hyphenation (`b0e85fbd`):** 64 OCR "word- word" breaks (`scripts/_dehyphenate_notes.py`). **Release (`2998cddf`):** `gh` v1.0.0-beta.2 prerelease = the full **ethiopian-tewahedo** EPUB (`--force`, NOT cached — verified Exo 2:1 holds its text + `◈3`) + a Kobo **`.kepub.epub`** (kepubify v4.0.4 in gitignored `.tools/`) + SHA256SUMS, clean filenames; audit 0-crit + epubcheck 0/0/0/0. **Website (`0db4a3eb`/`71fbfce` monorepo + `gringoboggy/yhwh-website` Pages):** releases.html offers the one full Ethiopian EPUB + the kepub, a capped-3 "What's changed" log, live-beta copy; **DEPLOYED LIVE at www.yhwhyaway.com (HTTP 200 verified).** **▶ MONDAY = first FULL v1.0 (NO MORE BETAS; user plans bandwidth reset → 1-2 heavy audits → fix what surfaces):** the 116 flagged verse chapters · kepub per-device font (Apple-larger plain / Kobo-smaller .kepub) · empty/blank-page (⑫) · front-matter "Edition⟷ID" stretch (⑬) · "two popups, same translations" dedup · website marker/symbol guide + single-download UX + step-by-step "how to use" page (do WITH the user's screenshots) · rebuild + re-sign desktop apps (Win Azure + **macOS notarize on the Mac** + Linux CI) · full-markers stress copy. Refs: `notes/2026-06-06-chapter-start-verse-boundary-bug.md`, `specs/2026-06-06-beta-device-qa-presentation-design.md` + plan. Baton **windows**.
>
> **▶ (superseded) 2026-06-06 (🪟 Windows) — BETA-2 POLISH P1–P4 SHIPPED + VERIFIED (`8fb606aa`); ★verse-boundary bug FIXED 161 ch (`809f711f`) + OCR de-hyphenation done (`b0e85fbd`).** **DONE + verified on a clean catholic-study rebuild (cache cleared) + live Playwright render:** ① **◈ note badges** (no more bare-number clutter), ② **dedup** (audit `scripts/audit_epub_structure.py`: 1043 dup note-rows → **0**), ③ **category-grouped tinted note cards** (topical last; soft per-category fills + accent border — confirmed `note-comm` = soft blue), ⑤ **justify** body + **1.05em** Apple-tuned size + **reader-native serif** (dropped embedded Cardo from body, kept on Heb/Grk popups), ⑨ **clickable in-content ToC** label, ⑩ **centred book title page** (root cause: `body p{text-align:left}` was directly left-aligning the `<p>` eyebrow/subtitle while the `<h1>` title inherited center → explicit `.bookpage-* {text-align:center}` fixes all 87), ⑧ **book-level native ToC** default (new `reader_native_toc_chapters` opt-in), ④ **structural-audit tool** (DUP_NOTE_ROWS/DUP_IDS/BROKEN_NOTEREF/UNBALANCED_TAGS/NEAR_EMPTY). epubcheck **0/0/0/0**, badge unit tests pass, ruff/lint/mypy clean, pre-commit green. **★BLOCKER for "in business" (user) — CHAPTER-START VERSE-BOUNDARY BUG:** at most chapter starts the base has an **empty verse-1** + WEB verse-1&2 **merged under number 2** (e.g. Exo 2: "1 ◈2 2 A man of the house of Levi…" — "A man…" is WEB 2:1 but sits under "2"). Pre-existing in the recovered base (NOT beta-2 work); ~301 chapters (over-count, needs precise scan). **Root-caused + full fix plan in `notes/2026-06-06-chapter-start-verse-boundary-bug.md`** — needs a careful, confidence-gated re-split aligning the merged WEB text against the on-disk reference translations (KJV/JPS/Douay/WLC/LXX vpl — user: "we should have every source to check"); **FIXED 2026-06-06 (`809f711f`):** repaired **161 chapters** via `scripts/_fix_chapter_verse_boundaries.py` — a content-preserving reorder + one KJV/JPS-anchored, confidence-gated split; verified exo2/gen1/exo13/gen25/lev4 correct, audit 0-crit, epubcheck 0/0/0/0, idempotent, note-count 48190 unchanged; **116 archaic/genealogy/apocrypha chapters FLAGGED** (left untouched — `--dry-run` lists them) for a follow-up. **OCR de-hyphenation DONE (`b0e85fbd`):** 64 "word- word" breaks repaired (curated, `scripts/_dehyphenate_notes.py`). All fixes bundled to **E:/F:**; **building the real `ethiopian-tewahedo` beta2 EPUB → `E:\` now** (badge mode, all fixes, for device testing). **▶ RELEASING v1.0.0-beta.2 TODAY** (the fixed Ethiopian Bible EPUB is the core; Win Azure-sign + Linux CI; **macOS dmg follows when the Mac is up** — clobber onto the tag per the beta.1 pattern). The first FULL **stable** release is ~Monday (bandwidth reset + 1-2 heavy audits → fix what surfaces). Beta2-today todo: bump VERSION → beta.2, cut the GitHub release + checksums, website update-log + releases auto-surface. Deferred to the Monday pass: website marker/symbol guide + single-download/update-log + step-by-step usage page (do WITH the user's screenshots), kepub Kobo target + per-device font (⑦), empty/blank-page (⑫), front-matter label-stretch (⑬), the full-markers stress copy. ⚠ **remotes behind** — pushed through `0c0c08dd`; verse-fix + de-hyph are local + E:/F: only (push at full-done, per user "commit to E/F to save time"). Baton **windows**.
>
> **▶ (superseded) 2026-06-06 (🪟 Windows) — BETA DEVICE-QA POLISH → v1.0.0-beta.2: spec + 8-phase plan committed; executing autonomously (user GO: "bang it all out… don't ask questions").** User device-QA (Apple Books iOS + colour Kobo) surfaced **13 polish items**; full design `specs/2026-06-06-beta-device-qa-presentation-design.md` + plan `plans/2026-06-06-beta-device-qa-presentation-plan.md`. Scope: distinct note(**◈**)/translation markers · colour-coded **tinted de-duped note cards** (grouped, topical last) · **justify** + per-device font (Apple larger plain EPUB / Kobo smaller **.kepub**) · **book-level native ToC** + kepub popups · clickable in-content ToC · **centred book title page** (eyebrow/subtitle were left, title centred) · empty/blank-page fix (splitter + title-frame) · **OCR de-hyphenation** + structural audit + lint guard · front-matter label-stretch · website **single-download + capped(3) update log** → cut **v1.0.0-beta.2** (Win Azure-sign + Linux CI + EPUB now; **macOS notarize Mac-gated** → follows). Executing 8 phases, commit per phase, final 5-leg push. Baton **windows**.
>
> **▶ ⏸ 2026-06-06 (🪟 Windows) — DEVICE-QA round-2 logged; beta LIVE + stable on all 3 platforms; ALL LANES ON A BREAK (user-directed: "we've all done enough today").** The user device-tested the live beta. ✅ **Kobo: the 2 big blockers are fixed** (no more crash / font legible). ✅ **Apple Books: "way better, very good beta."** Open polish (full detail in memory `kobo-color-ereader-end-stage-qa`, **none blocking**): **★recurring per-book title-page misalignment** ("number left, title center" — the CSS is ALREADY centered, so **render-to-pin the off element BEFORE coding**; don't blind-tweak CSS again ← the priority polish) · Kobo native-ToC too verbose (dial RX P4a-2 to book-level) · Kobo popups don't fire (EPUB3 footnote → Kobo fallback) · note-numbering vague (needs an example) · a numbers-mode (~91,733-marker) stress build (the user did this himself). **Pulled Mac's wrap** (`79634446`: socials LIVE, download buttons wired, outreach pack → `notes/2026-06-06-outreach-pack.md`). **▶ NEXT (whenever the user re-engages):** (1) title-page render-then-fix (priority); (2) Mac's 2 prepared web-submissions (faith.tools + christian_foss) with corrected all-platforms copy; (3) the user's first personal outreach send (Beta maṣāḥǝft). Baton **windows**.
>
> **▶ ✅ DONE 2026-06-06 (🪟 Windows) — `v1.0.0-beta.1` LAUNCH COMPLETE: Windows signed + in the release, site warning removed + live, Linux AppImage BUILT on CI — all 3 platforms + the Bible shipped.** Windows `YHWH.exe` SIGNED via **Azure Trusted Signing** (Valid · `CN=Bogdan Zorlescu` · timestamped — full recipe in memory `reference_windows_signing_azure` + reusable `dev/sign_windows.ps1`; ★role = **`Artifact Signing Certificate Profile Signer`** assigned **by ObjectId**, Owner ≠ signer). Uploaded the signed exe + the **Ethiopian Bible** EPUB + a merged `SHA256SUMS.txt` to the existing `v1.0.0-beta.1` tag (`--clobber`; **ONE Bible per the user**, not 2). Removed the unsigned/SmartScreen warning copy from `releases.html`/`index.html` + **deployed LIVE** (`yhwh-website` `4743f71`, www.yhwhyaway.com). **Linux AppImage BUILT on CI** — `.github/workflows/build-linux.yml` (GitHub Actions Ubuntu → PyInstaller → AppImage) ran green; hardened a 2nd pass (+ Pillow for cover rendering, pinned PyInstaller 6.20.0, self-merging checksums). `YHWH-1.0.0-beta.1-x86_64.AppImage` (301 MB) **LIVE in the release** + merged into `SHA256SUMS.txt` (all 4 assets checksummed). Socials → Mac. Repo PUBLIC; the release ships **all 3 platforms — Windows (signed) / Linux / macOS (notarized) — + the Ethiopian Bible**. **▶ OPEN TAIL (optional, not blocking):** add the catholic-study EPUB to the release if the user wants a 2nd Bible; the Linux build is web-mode (pywebview/GTK skipped) → a native-shell Linux build is a future nicety. Baton **windows**.
>
> **▶ ✅ DONE 2026-06-05 (🪟 Windows) — WIN AUDIT FIX-SLICE shipped → round-5 release gate CLEAR (both lanes done).** Mac `a49f1be7`+`30debc58`; Win this commit. Build-path slice: **Phase 1** = the HIGH `edition_stats`→`_PIPELINE_SCRIPTS` stale-EPUB cache-miss, expanded *fix-the-class* to the full build-path-core **transitive closure (25 modules → +10 covered** incl. `config`/`translations` live-bake + `corpus_index`/`sources_lexicon` behind the `matrix`/`sources` shims, **12 waived)** + a transitive-closure self-enforcing guard (Windows-owned `test_build_cache.py`, off Mac's `lint_rules.py`); **Phase 4** = badge orphan-skip stat (additive) + `filter_html` per-kind→single-alternation consolidation. **Proven byte-IDENTICAL to HEAD** (before/after full-build digests: catholic-study/jewish-study/ethiopian-tewahedo all unchanged) + determinism gate + 43+4+14 tests + ruff/lint_rules/mypy clean + a **7-agent adversarial review** that approved filter_html/badge AND caught the 2 mis-waivers (`config`/`translations`) + the transitive guard blind spot → all fixed pre-save. **▶ The release `v1.0.0-beta.1` gate is now CLEAR — public flip + release await the user's GO.** Findings doc `notes/2026-06-05-round5-split-audit-findings.md` Win boxes ticked; Mac's deferred `check_atomic_writes` lint extension is unblocked. Baton **windows**.
>
> **▶ READY 2026-06-05 (🖥️ Mac) — POST-BETA GROWTH PLAN written (PLAN-only): "eyes on the site & program."** `plans/2026-06-05-post-beta-growth-plan.md` (INDEX 68→69; workflow `wf_16ef99cc-4bb`, 8 agents). Faith-grassroots spine + SEO graft + ONE deferred Show HN; two-funnel insight (faithful/heritage on Telegram/FB/YouTube/mailing-lists vs OSS/Bible-tech on GitHub/HN/search); hero = the rare manuscript reader. Phases 0→4 + milestone-only steady-state; inviolable guardrails (never mention donations in outreach · gift-not-pitch · doctrinal humility · ~1–2 hr/wk). Claude drafts; Playwright drives posting (no API keys); the human relationships are the user's. **▶ Phase 0 content SHIPPED + DEPLOYED LIVE (user GO'd):** 3 evergreen pages live at www.yhwhyaway.com (`yhwh-website` `8190057`; monorepo `86441d89`) — `about-the-geez-bible.html` (sources/method/free-forever + correction mailto) · `ethiopian-bible-canon.html` (the 81-book canon — Enoch/Jubilees/Meqabyan) · `copy-the-bible-text.html` (the copy-paste differentiator); plain/reverent, **no donation CTAs**, linked from `geez.html`, in the sitemap. **★Also LIVE this turn — EN-indicator fix (`278c1c90`):** moved the English badge OFF the book pills (the `_en_books` ≥50-row threshold only Psalms cleared → 1ki/1sa/2sa looked English-less though every chapter has it) ONTO the per-chapter cells in the book index — honest per chapter, future-proof for Geʽez-only chapters; progress pills now show stage only. Verified live (pills 0 `pb-en`; psa index 152 `rdx-en`; 1ki 7). **✅ AI-method disclosure RESOLVED (user-directed "state our exact method") + LIVE** (`yhwh-website` `9b79846`; monorepo `ab9fba6b`): the About page now states the full method plainly — read off the images with AI vision-language models, re-read & compared, collated against a 2nd witness where one exists, **calibrated-not-harmonized** (differences flagged, never quietly corrected), reviewed + reader-corrected; the English a separately-checked literal back-translation. Vendor now NAMED — "**Claude** (Anthropic's AI)" (user already discloses Claude); LIVE (`yhwh-website` `57c0e62`, monorepo `a284804f`). **★Separately — macOS NOTARIZATION DONE this session:** the auto-finisher accepted submission `782d48b8`, stapled `YHWH-1.0.0-beta.1.dmg` (`stapler validate` ok) + regen `dist/SHA256SUMS.txt`; `dev/NOTARIZATION_STATUS.md`=DONE — the long-stuck notary launch-prep item is CLOSED. Remaining Phase 0 = must-fix QA (`releases.js` API fix, HTTPS, Giscus, email-forward). Outreach phases 1–4 run AFTER `v1.0.0-beta.1` ships. Baton **mac**.
>
> **▶ ✅ DONE 2026-06-05 (🖥️ Mac) — WEBSITE CHAPTER-READER: planned (spec+plan) → BUILT + pushed → DEPLOYED LIVE (user pivoted PLAN→BUILD this turn).** Built the Geʽez/Amharic Bibles + their literal-English ON the website — **only pills with actual transcribed work are clickable now.** Gate = the progress map's own-versified signal (◑/●), never OCR ◐; per chapter parallel (Geʽez|English) where EN exists, single-column Geʽez/Amharic otherwise (English after full release). **Today = 161 parallel chapters** (psa/1ki/1sa/2sa; Amharic = ∅), auto-grows. Free copy-paste (selectable Unicode, gutter `user-select:none`, Copy-chapter button) + Ethiopic-numeral/rubricated manuscript styling. Spec `specs/2026-06-05-website-chapter-reader-design.md`, plan `plans/2026-06-05-website-chapter-reader-plan.md` (INDEX updated). Implementing in `scripts/gen_website_progress.py` + `website/{build.mjs,style.css,partials,src/read/**}`; tests `tests/test_website_progress.py` (7, +fixed the stale `none`-stage regression). **✅ DONE this turn: 165 reader pages generated + the static pipeline wired + pushed to the monorepo; verified (ruff/mypy/lint, 0 console errors, Noto Ethiopic loaded, 0 dead links, top-level byte-stable).** **✅ DEPLOYED LIVE at www.yhwhyaway.com** (user GO'd): `yhwh-website` `1308c95`, 169 files; verified HTTP 200 + Geʽez column + clickable pill live. **The on-site Geʽez Bible (Psalms-led) is LIVE; the whole arc — plan → build → push → deploy — is DONE.** Baton **mac** (website-reader lane). ⚠ The win-lane handoff (next) marks a release-gating audit fix-slice as Mac's priority, but the USER pulled the chapter-reader forward this session — building + pushing it per direct instruction; the audit fix-slice stays queued (file-disjoint: `website/**`+`gen_website_progress.py` vs `lint_rules`/tests/migration scripts).
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — ★ROUND-5 SPLIT AUDIT COMPLETE (both lanes) → release-gating fix-session SET UP + SPLIT.** Win lane done (15 survivors: 1 HIGH · 9 MED · 5 LOW; 2 refuted); Mac's 33 already FIXED. **Merged plan + phased fix queue: `docs/superpowers/notes/2026-06-05-round5-split-audit-findings.md`** (raw `_audit-split/findings-win.json`). **▶ Fresh session(s) execute the fix-session, SPLIT file-disjoint:** 🪟 **Windows** = Phase 1 HIGH (`build_cache.py` — `edition_stats.py` cache-miss → stale EPUB; add to `_PIPELINE_SCRIPTS` + evict + coverage lint) + Phase 4 build-path (`build_edition.py` badge stat + `filter_html` opt) → **byte-stability proof per change** (regen 9 KJV + `git diff` + `test_byte_stability_gate.py`). 🖥️ **Mac** = Phase 2 atomic-write (`_reingest_eastons`/`_strip_reviewer_scaffold`/`lint_rules` blind-spot — surgical, the #18 it skipped) + Phase 3 test fixes (`validate_schemas`/`test_lint_rules`×2/`test_batch_promote_xrefs`) + `corpus_index` doc + the `needs_vnote_pass` regression test. **Disjoint** (Win=`build_cache`+`build_edition`; Mac=`lint_rules`+tests+migration scripts) → both run+push in parallel, pull before push, tick the doc checkboxes. **Phase 0** (`needs_vnote_pass`) ALREADY fixed. **✅ Mac slice DONE 2026-06-06** (`a49f1be7` + `30debc58`): atomic-write migrations (`_reingest_eastons`×2 / `_strip_reviewer_scaffold`) · `validate_schemas` `reader_toc_books_only` FieldSpec (test_validate_schemas 49✓) + cleared its 2 invisible mypy `type`-shadow errors + **added it to the mypy gate** (`[tool.mypy] files`, expand-outward) · `test_lint_rules` freshness/flaky (12✓) · `corpus_index` os.replace comment · `needs_vnote_pass` regression test (slow-tagged, verify on SSD); lint/ruff/mypy clean. **⏸ DEFERRED — the `check_atomic_writes` lint EXTENSION** (~80-site `.write_text`/`.write_bytes` sweep overlaps `build_edition.py`/marathon-core → do AFTER the win build-path lands). **▶ Only the WIN slice remains** (Phase 1 `build_cache` HIGH + Phase 4 `build_edition` badge/`filter_html`, byte-stability-proofed) before the release gate clears. **Release `v1.0.0-beta.1` GATED on the win slice** → then public flip (user GO) + release together. Baton **windows**. `LANE_HANDOFF` turn 22.
>
> **▶ NEXT SESSION (🖥️ Mac) — PLAN the website chapter-reader (turn the progress pills into an on-site Geʽez+English Bible).** User-directed 2026-06-05: a fresh session's job = **PLAN, not build** — clickable pills/chapters → on-site Geʽez + literal-English chapter reader, chapter-by-chapter progress; *"building the Bible on the website."* **▶ Full bootable brief: `docs/superpowers/notes/2026-06-05-website-chapter-reader-NEXT-SESSION-brief.md`** (vision · hard constraints incl. the never-over-claim clickable-only-where-real rule · recon list [coverage inventory FIRST — Psalms is the only ~complete one] · open questions→decisions · deliverable = a spec + phased plan, Psalms-first). Runs while the win lane finishes its audit. Don't build, don't touch the EPUB pipeline, don't re-decide versioning (app-semver vs content-milestones already settled). **Content cleanup outcome:** #13 jud/phm OCR-colophon spill removed (`93003e05`); **★#15 KEPT — the 31 `1ma`/`2ma` candidate files are real Maccabees-expansion drafts (21 substantive candidates each), NOT cruft** → register the Maccabees canon later, do NOT delete; #12 (111 impossible-coord candidates) deferred. Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — MAC-LANE ROUND-5 AUDIT FINDINGS FIXED (all fixable); a HEAD-START on the merge fix-queue.** While the win-lane audit (`wf_eeaa8368-6da`) runs on the N95, Mac fixed its lane's survivors. **`444fe8cc`** = 2 HIGH (`batch_promote_xrefs`: missing-book `1ma`/`2ma` candidates marked "promoted" w/o insert → data loss; reviewer-scaffold not stripped in the fast path) + 3 MED (`needs_vnote_pass` missing per-chapter/verse tiers; the RED `ALL_CHECKS==28` pin → 32; `corpus_index` 3 unguarded `_CACHED_CONN` closes → `_CONN_LOCK`, deadlock-checked). **`9e81ee9e`** = ~15 LOW/INFO via parallel re-verify→controller-apply (`wf_f61b5d15-d5d`): inject Psalm-119 aside ordering, RSS http(s) scheme allowlist, 7 `matrix_app` XSS escapes, lint canonical-order/bookcode/render-coverage gaps, EN-dir tracking, 4 byte-identical store escape-sequence fixes (Py3.16-safe), `sources_lexicon` topic normalization, stale REPO_MAP/SESSION_PLAYBOOK. **SKIPPED 8 (reported in-commit):** content-store deletions (#12/#13/#15 — build-harmless, defer to a user-aware cleanup), note-editor innerHTML (#2), kenyon re-index by-design (#5), atomic_write lint (#18 — 100+ violations), tau6x2 share-pins (#7), ingest confirm-optimal (#27). **Gates green:** 59+6+40+4 tests, live lint-guardrails, ruff, `ALL_CHECKS`=32, escape data byte-identical. **▶ When the win lane finishes + `deep-audit-merge.js` runs, these Mac findings are already resolved.** Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — BETA-LAUNCH PREP shipped; release GATED on the full audit; public flip pending user GO.** Mac (baton) ran the pre-public safety audit (`wf_8ba4c1c2-bc3`, 8 agents) → **GO**: code theft-protected (LICENSE airtight; 9 bad-faith reuse theories refuted) + **git history clean across 726 commits → NO secret purge needed** (⚠ the "purge a committed Voyage key + rotate" instruction is a FALSE premise — `.env` was never tracked; do NOT history-rewrite). **Shipped:** `ab5384b1` (16 CC0/commercial self-license → all-rights-reserved), `af6177f3` (LANE_HANDOFF Spaceship key-ID/secret-name scrub), **site EN-fix LIVE** (`yhwh-website` `9b236a3` — EN on Psalms only, verified). gh=`gringoboggy`; LICENSE email `yhwhyaway.com` works (kept). **▶ REMAINING:** (a) **public flip** of the monorepo (PRIVATE) — awaits explicit user GO (irreversible); (b) **release `v1.0.0-beta.1` — GATED on the full split-audit + fixes** (win lane `wf_eeaa8368-6da` still rolling on the N95 → merge via `deep-audit-merge.js` → fix → then flip + release together). LICENSE/COPYRIGHT correct (all-rights-reserved). Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows → Mac handoff) — progress-page fixes saved; beta launch handed to Mac; AUDIT ROLLING on Windows.** Windows shipped the progress-page **"not started" removal** (LIVE) + the **EN-flag fix** (saved, needs redeploy) and prepared the full beta-launch handoff (`docs/superpowers/notes/2026-06-05-beta-launch-and-en-fix-handoff.md`). **Mac finishes:** EN site deploy · beta `v1.0.0-beta.1` publish (Windows `dist/YHWH.exe` MS-signed + ready; ⚠ **gh auth per-machine — Mac `gh auth login` as gringoboggy**) · source-public AFTER a Voyage-key history purge (`54ac7493`) + rotate · CC0/commercial doc sweep. LICENSE/COPYRIGHT correct (all-rights-reserved). **▶ Audit `wf_eeaa8368-6da` (win lane) ROLLING on Windows — do NOT stop** (TaskStop orphans pytest — [[audit-orphan-processes]]); when done, win survivors merge w/ Mac's `findings-mac.json` via `deep-audit-merge.js`. Baton → **mac**. `LANE_HANDOFF` turn 20.
>
> **▶ ON BOOT — fresh 🖥️ Mac session: you HOLD THE BATON (mac). The split deep-audit MAC LANE is DONE; your job is the BETA-LAUNCH queue** (full detail `docs/superpowers/notes/2026-06-05-beta-launch-and-en-fix-handoff.md`; summary in `LANE_HANDOFF` turn 20). On boot: `git fetch origin && git pull --ff-only origin main`; the notary SessionStart backstop auto-fires; `/resume`. **The audit is DONE — do NOT re-run it.** Launch steps, IN ORDER (user-gated where noted):
> 1. **Deploy the website EN fix → live.** Source committed (`555d548e`/`178ded0e`) but not yet on the live Pages repo. Per `website/README.md`: `node website/build.mjs` → mirror `website/dist/` into `/Volumes/MacHD2/yhwh-site-publish` → commit + push. Verify `geez.html` legend = `◐ source in hand · ◑ transcribed · ● Bible-ready` and **EN shows on Psalms ONLY**.
> 2. **⚠ `gh auth login` as `gringoboggy` FIRST** — gh auth is PER-MACHINE; the Mac is NOT yet authed (the wrong `bridge4kaladin-collab` account must stay logged out). Needed to create repos / publish the release.
> 3. **★BEFORE making the monorepo public — REQUIRED secret-history purge:** commit `54ac7493` scrubbed a **Voyage API key from `.env`**; a live key may sit in git HISTORY (exposed the instant the repo goes public). Inspect `54ac7493` + surrounding history; if a live key is there, **purge with git filter-repo/BFG + ROTATE the Voyage key**, THEN flip public. (HEAD confirmed clean; `content/auth.json` never committed.) Do NOT go public until clean.
> 4. **Publish beta `v1.0.0-beta.1`:** Windows `dist/YHWH.exe` (474.9 MB, MS-signed, no SmartScreen wall) + 2 EPUBs are READY; the macOS `.dmg` AUTO-joins when Apple notarization clears (NOT a gate). `releases.js` REPO = `gringoboggy/yhwh-bible-platform` → either make the monorepo public (after step 3) + publish the release there, or use a separate public releases repo + repoint `releases.js`; then repoint the "Code" nav link + redeploy. **License = ALL RIGHTS RESERVED, source-available** (`LICENSE`/`COPYRIGHT.md` are CORRECT).
> 5. **License doc-wording sweep (cosmetic):** stale "CC0"/"commercial" wording lingers in `VERSION` ("free-public CC0 phase", "first commercial release") + a couple docs → sweep the user-facing ones to all-rights-reserved.
>
> **▶ AUDIT STATE — MAC LANE COMPLETE (2026-06-05, round 5, 12 dims):** 33 survivors / 7 refuted of 40 (**2 high · 3 med · 24 low · 4 info**) pushed to branch **`lane-transfer/audit`** → `_audit-split/findings-mac.json` (190 KB, full result). The **WIN LANE is still ROLLING on Windows** (`wf_eeaa8368-6da`) — **do NOT touch it (it runs on the N95); stopping it orphans pytest** ([[audit-orphan-processes]]). When it finishes, **WINDOWS merges** win+mac via `deep-audit-merge.js` on the N95 → `docs/superpowers/notes/2026-06-05-round5-split-audit-findings.md`; findings → the post-beta fix queue. **★2 HIGH** (both `scripts/batch_promote_xrefs.py` fast-path: marks 577 missing-book `1ma`/`2ma` candidates 'promoted' without inserting; skips the reviewer-scaffold strip). **★3 MED** incl. `tests/test_lint_rules.py::test_registry_not_silently_shrunk` pins 28 vs `ALL_CHECKS`=32 → CURRENTLY FAILING.
> **▶ NOTARY:** Apple still PENDING (3 submissions In Progress 24h+); the launchd auto-finisher (now auto-discovers via `notarytool history`) + the Mac SessionStart backstop self-staple on Accept — NO action. **Env:** `.venv/bin/python` (NOT `python3` = sys 3.9); `export TMPDIR=/Volumes/MacHD2/<dir>` for any tmp-using run; epubcheck jar at `.venv/lib/python3.14/site-packages/epubcheck/epubcheck.jar`. **[USER] minor:** `rev 1:8` "A Alpha" dup dict-easton note (de-dup or leave).
>
> **▶ ✅ DONE 2026-06-05 (🪟 Windows) — Website factual-copy pass shipped (`a1e94035`, disjoint: `website/**` + `README.md` only); `main` `4e8cf37c→a1e94035`.** A plain "story behind it" credits section (build timeline + real usage numbers + team photo, hoodie logo blurred) + GitHub Sponsors activated + the overselling/favor-framing register stripped site-wide ([[feedback_plain_professional_copy]]). **The split deep-audit is READY on the new HEAD — protocol unchanged from `LANE_HANDOFF` turn 18/19:** both lanes pull → set `LANE` locally (Mac `'mac'`=12 dims / N95 `'win'`=4 dims, confirm the startup count) → Mac pushes `findings-mac.json` to `lane-transfer/audit`; N95 merges via `deep-audit-merge.js` → `notes/2026-06-05-round5-split-audit-findings.md`. Audit = read-only (no baton contention); baton `windows` (owns the merge-commit). TRACKER-STATE idle.
>
> **▶ ✅ DONE 2026-06-05 (🖥️ Mac) — AUTO-NOTE RE-INGEST #2–5 COMPLETE; the track is finished. Mac is READY for the split deep-audit.** Both remaining commits shipped + pushed (origin + github): **#2 θεός (1,196) + #4 φῶς (76) [`cf5eefa0`]** via a curated `_GREEK_DEF_OVERRIDES` in `sources_lexicon.StrongsGreek.get()` (two malformed openscriptures Strong's entries — θεός's primary "supreme Divinity" sense lived in `derivation`; φῶς front-loaded a leaked etymology fragment; the class is exactly 2 over all 7,669 lang-greek notes) + frozen one-shot `_reingest_greek_glosses.py` lockstep; and **#3 + #5 topic-torrey (676 union, 483 distinct) [`4e8cf37c`]** — ★**#5 was a topic-nave MISATTRIBUTION: it is entirely topic-TORREY** (nave is clean). Root cause: `extract_torrey_ccel.py::parse_text` admitted 2 junk "topics" (a Tyre-block description ending in `.` + a wrapped Zechariah citation dump) that STOLE their real topic's ref block; a discriminator rejects `.`/`N:N` headings while keeping `current` so the refs flow back (n_refs preserved 55,566; 630→628 topics); one-shot `_reingest_torrey_topics.py` regenerates the index + recomputes 676 bodies via `TorreyTopicalDetector` (reproduces all 21,764 current bodies exactly) + lockstep; 0 notes dropped, 0 residual junk. **Gates green on both:** `check_nested_anchors` 0, categorize id+kind invariant (91,572/91,572), `ebible verify` errors=0 (32,263/32,263), **eth + catholic-study epubcheck 0/0/0/0**, 2 new lint guards (`greek_gloss_quality`, `no_torrey_topic_leak`) + 14 tests, ruff/format/mypy/lint clean (30 pass / 0 fail). **▶ NEXT = the user-coordinated SPLIT DEEP-AUDIT** — Mac sets `LANE='mac'` + runs the 12 read-only code-review dims of `.claude/workflows/deep-audit.js` (confirm dim count = 12) → push `findings-mac.json` to branch `lane-transfer/audit`; N95 runs the 4 build/test dims (`LANE='win'`) + merges via `deep-audit-merge.js`. On the same `main` `4e8cf37c`. Baton `mac`.
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — SPLIT END-OF-PROJECT DEEP-AUDIT prepped + ready; awaiting Mac's re-ingest #2–5 (user: "#2–5 first").** Auditor `.claude/workflows/deep-audit.js` made round-5 + current (new `rx-surfaces` dim) + split-ready (`LANE` win/mac). Plan `docs/superpowers/plans/2026-06-05-split-audit-plan.md`. Beta artifacts built + verified (2 EPUBs 0-broken-links + `dist/YHWH.exe` smoke-passed). **▶ Windows next (fresh session, after Mac signals #2–5 done):** set `LANE='win'` → run the 4 build/test dims → merge Mac's findings (branch `lane-transfer/audit`) → ship `v1.0.0-beta.1`. Meanwhile (now): pre-build the merge workflow + apply the merged memory set (`lane-transfer/rules`). Baton `mac` (#2–5).
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — AUTO-NOTE RE-INGEST track (Mac baton): defect #1/5 SHIPPED; #2–5 pending.** Plan: `docs/superpowers/notes/2026-06-06-auto-note-reingest-plan.md` (the authoritative spec — read §0 ship-bar + §1 before #2). The 5 defects are the two content audits' findings (`notes/2026-06-06-auto-note-quality-audit.md`).
> - **✅ #1 dict-easton un-cap (FULL articles) + `_HEAD` glue + XHTML-escape — DONE, all gates green, committed.** 1,650 store notes → full Easton article (was MAX_BODY=480 truncated). Method = a **frozen one-shot** `scripts/_reingest_eastons.py` (models `_strip_reviewer_scaffold.py`): regenerate each source entry old-style (cap+greedy `_HEAD`) AND new-style (full + `_HEAD_NEW` `(?![a-z])` + XHTML-escaped), pair by **EXACT old-body equality** (heuristic-free → 640 coord collisions auto-resolve), lockstep exact-string-replace old→new in source + base. Extractor `extract_eastons_ccel.py` fixed permanently (same 3 fixes). Verified: byte-exact reconstruction (`apply_map(HEAD)==working`, 108 files) + categorize-diff (id+kind unchanged) + check_nested_anchors 0 + **eth+cath epubcheck 0/0/0/0** + ebible verify errors=0 (32263/32263) + new `check_no_truncated_easton` guard + `tests/test_easton_reingest.py` (7) + ruff/format/mypy/lint clean. **★Lessons:** re-verify the plan's own numbers (the "2,223" was a scratch overcount; real 1,650); the epubcheck gate caught literal `<`/`>` in 2 entries (1 Tim 3:16 betacode + a `<> <>` separator) that truncation had hidden → escape the prose. Edge cases logged-not-guessed: `rev 1:8` f/g ("A Alpha" pre-existing dup — **[USER] review**, left untouched, not truncated), DALMANUTHA (dup → replace-all), ZUZIMS (source-only, never baked).
> - **▶ NEXT = defect #2 lang-greek Theós head-drop (1,196 = 100% of θεός)** — every θεός gloss dropped the primary "God" sense (greppable `θεός</em>).</strong> figuratively, a magistrate`). Find the Greek/Strong's extractor (`grep -rl 'lang-greek\|Strong' scripts/`), correct G2316, lockstep-replace the 1,196 (likely one old-body→one new-body ×1,196). Then #3 topic-torrey ref-dump (596) · #4 lang-greek Phōs paren-imbalance (76) · #5 topic-nave description-as-heading (87). One defect per commit; same §0 ship bar (incl. **build BOTH eth + catholic-study + epubcheck 0/0/0/0** — see [[feedback_gate_canon_filtered_editions]]; and **XHTML-escape any new body prose** — the #1 lesson). Mac venv now has epubcheck (`EPUBCHECK_JAR=.venv/lib/python3.14/site-packages/epubcheck/epubcheck.jar`).
> - **⚠ Coordination:** baton `mac`; **winclaude gets NEW instructions next boot** (user-directed); the Mac is being moved/unplugged. Transfer branch `lane-transfer/rules` holds the merged out-of-repo memory set for Windows to apply (then delete the branch). A fresh session: `/resume`, then continue at #2.
>
> **▶ ✅ DONE 2026-06-05 (overnight 🪟 Windows) — RX PHASE 4 COMPLETE + FULLY VERIFIED + SAVED (the LAST RX phase before the device test): build-time file-splitter (4b) + Kobo-safe TOC (4a). The RX arc's Windows work is DONE; only the [USER] device test (user-side) remains.** `apply_file_split(tmp, edition)` in `scripts/build_edition.py` (`~2241`, hooked in `build_one` just before zip; mirrors `apply_badge_markers`; base `epub_working/` NEVER touched). Splits the 2–5 MB `index_split_*.html` into ~0.4 MB pieces for colour-e-ink Kobo. **ethiopian-tewahedo: 53 files → 227 pieces, largest 472 KB (from 5 MB), 0 pieces >600 KB, epubcheck 0/0/0/0, 0 broken links.** Boundaries = `id="bp-NN"`/`id="ch-bNN-cMM"` (matches BOTH `<a class="ch-anchor">` + `<p class="ch-heading">` — matching only ch-anchor silently merged ~5 chapters/file into 2 MB units, a bug found+fixed); a giant prose chapter (one `<p class="verse-p">`) sub-splits at inline `<a class="vn-link">` verse anchors with paragraph close/reopen (`_subsplit_chapter`), skipping `<div>` poetry chapters. **★Key insight:** the base MIXES aside placement — many asides are INLINE after the verse, not in `<aside class="notes-section">` — so `_strip_notes_sections` now harvests+strips EVERY leaf note aside (`_CHILD_ASIDE_RE`) to pure prose (3181→979 KB content for Numbers), then per-piece notes-sections are rebuilt BY REFERENCE (bare `#id` footnote/popup stays same-file = native popups). Global id→piece map rewrites ~39.5 K cross-file hrefs + promotes cross-piece bare links; OPF manifest+spine + nav.xhtml + toc.ncx regenerated (preserve matter/font/css entries). Deterministic (byte gate green); cache auto-rekeys; **default ON** (`DEFAULT_READER_FILE_SPLIT=True`; opt-out `reader_file_split:false`; `reader_file_split_target` tunes). +`tests/test_file_split.py` (15: boundaries/well-formedness/notes-distribution/determinism/subsplit/OPF-nav-ncx-regen); badge tests opt out of split (fast); test_marker_style + test_nested_anchors green; ruff/mypy/lint 27✓/2⚠/0✗. Plan `plans/2026-06-05-rx-phase4-toc-and-filesplit-plan.md` (+INDEX). **✅ Phase 4a DONE (Kobo-safe TOC):** all 11 editions pinned `reader_toc_collapsible:false` in `editions.yaml` (per-edition → §6.5 no-op guard + reader-toc tests intact; ornament feature [lives in `<summary>`] undisturbed — a global default-flip would've broken `test_default_is_no_op` + `test_inserts_ornament`) so `apply_reader_toc_transforms` unwraps the `<details>` accordion → flat `<p class="toc-book-label">` + chapter `<ol>` (chapters KEPT); `epub_working/stylesheet.css` `.toc-wrap ol.toc-chapters` drops `display:flex` (inline-block pills wrap + per-pill margin). **Verified (ethiopian-tewahedo build): 87/87 books unwrapped, 0 flex, epubcheck 0/0/0/0**, composes w/ splitter; +`test_shipping_editions_pin_kobo_safe_flat_toc`. Book-list compaction = [USER]-decision (loses 1-tap chapter nav). **★GATE CAUGHT A CANON-FILTER BUG → FIXED (unified stack-aware splitter):** catholic-study (canon-filtered) failed epubcheck — 2 FATAL RSC-016 (unclosed `<p>`/`<body>`) + RSC-005 + 29 cascade RSC-012. A chapter anchor can nest INSIDE the previous chapter's trailing `<p class="verse-p">` (Acts 21→22); the old coarse-unit + `_subsplit_chapter` net-`<p>`-count splitter cut there and split the `<p>` (the superset packed those chapters together so it passed — packing-luck). **Rewrote the splitter unified + stack-aware:** cut candidates = book/chapter starts + EVERY verse anchor; at each piece boundary `_stack_at_positions` (one tag scan) gives the open-element stack → the piece reopens what it starts inside + closes what's still open at its end, so ANY cut (inside `<p>`/`<div>`) is well-formed; this subsumes verse sub-splitting (`_subsplit_chapter` removed). Verified: base-wide sweep **338 pieces / max 445 KB / 0 unbalanced**; 15 unit tests (+`TestStackAwareSplit` Acts-shaped fixture); ruff/check/mypy clean. **✅ ALL GATES PASSED + SAVED:** re-gate **catholic-study + ethiopian-tewahedo epubcheck 0/0/0/0** (211/216 pieces, max ~475 KB); determinism/regression suite **35 green** (byte-stability gate + popups-build + smoke + zip — confirms full-build determinism + that split-default-ON + the popup contract survive); base sweep 338/0-unbalanced. **Saved 5-leg `6c98f76f`** (P4a + the splitter fix) atop `37a10673` (P4b base) + `f5ecd48b` (MATRIX_MAP RX-currency). **Mac signalled build-ready** (`LANE_HANDOFF.md` turn 13). Lesson → memory `feedback_gate_canon_filtered_editions`. **▶ The ONLY remainder = the [USER] device test** (sideload on the COLOR Kobo + Apple Books; user-side, batched at the very end). Standalones don't use `index_split_*` (1.76 MB EPUB; not a Kobo problem) — no splitter needed there. Post-RX backlog (USER-gated re-ingest · LANE T code-debt/doc-coherence/test-coverage) when the user directs it. Baton: `windows`. **TRACKER-STATE idle — RX arc COMPLETE; between tasks.**
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — EPUB READING-EXPERIENCE OVERHAUL (Kobo device-QA arc) — Layer A DONE · Phases 1–3 + 5 SHIPPED · only P4 (Kobo TOC + file-splitter) remains.** User loaded the v27 EPUB on the COLOR Kobo + shared Apple Books crops (`E:\epub-kobo-crops` → staged `C:\Users\bogda\AppData\Local\Temp\kobo-crops`; the v27 EPUB extracted at `…\Temp\kobo-epub-x`). **All root-caused:** (1) **`[Reviewer:…]` scaffold in 88,773/91,733 notes (96.8%)** — `scripts/core/detectors.py` embeds `<em>[Reviewer:…]</em>` in `body` at 8 sites (clean pattern = its own `reviewer_notes=` field, already used at 178/346); `promote.py` writes it verbatim; baked in source+base; (2) **'too cluttered' = the deferred `badge` mode** (`specs/2026-05-24-epub-presentation-polish-design.md` §4.1; user CONFIRMED = one count-badge per verse → tap→note-list); (3) Kobo perf (99 MB uncompressed / 2–3.4 MB per `index_split`); (4) Kobo no popups; (5) Kobo no title-art (full-bleed `position:absolute`); (6) Apple title-art misaligned; (7) tiny Heb/Grk (no embedded font); (8) Apple note-box width overflow (no `overflow-wrap` on `.note`/`.vnote`); (9) Apple blue-"│" artifact (`.vn` still `vertical-align:super;line-height:0`); (10) Apple TOC expand→page-break; (11) Kobo TOC broken (`<details>`+flex). **★Constraint (user): ALL notes + features ON** — declutter via display, not deletion. **Decisions:** badge=vision; strip professionally; all-on. **Master plan** `plans/2026-06-05-epub-reading-experience-overhaul.md` — Layer A (D1 badge-injection · D2 cross-reader popups · D3 Kobo TOC/title · D4 file-size · D5 font-embed · D6 leak-surface) → Layer B (P1 strip+guard · P2 CSS · P3 fonts · P4 Kobo structural · P5 badge) → Layer C Mac compat-research. **✅ Phase 1 SHIPPED** (`a6e989a7`, rebased onto Mac `979bde50`): 88,773 `<em>[Reviewer:…]</em>` spans stripped source (79 files) + base (55 files); 8 `detectors.py` body-embeds→`reviewer_notes=` + `extract_eastons_ccel.py` + `promote.py` defensive strip + new `lint_rules.check_no_reviewer_scaffolding_in_bodies` guard (88,773→0); one-shot `scripts/_strip_reviewer_scaffold.py`. Gates: nested 0/61, ebible 32263/32263, epubcheck 0/0/0 ×2, categorize-diff = only scaffold changed, lint 27/2/0, mypy clean, 164 touched-test asserts green. **✅ Layer A discovery** (`wf_95b6739a-c67`, 6 investigators) → `notes/2026-06-05-rx-discovery-findings.md`: D1 badge = per-verse native popup-footnote (NO JS); D2 popup contract already correct (Kobo fail = size/hidden-nesting); D3 Kobo TOC = `reader_toc_collapsible:false` flip + framed title + `.toc-wrap details{page-break-inside:avoid}`; **D4 NO splitter exists → build ~300 LOC semantic splitter (61@2–3.4MB → ~350@0.4MB)**; D5 embed **Cardo** (OFL) via existing `patch_opf_fonts`/`EMBED_FONT_PATHS` + Abyssinica SIL (standalones); D6 leak surface (5 patterns/8 sites). Mac handed the disjoint compat-research (`LANE_HANDOFF.md` turn 9; baton stays windows). **✅ Phase 2 SHIPPED:** `.note`/`.vnote` `overflow-wrap:break-word` (#8) + `.vn` baseline-shift `vertical-align:0.35em` replacing `super;line-height:0` (#9 blue-"│") + `.toc-wrap details{page-break-inside:avoid}` (#10) + **`title_page_style` default full-bleed→framed** (`build_edition.py:2891` + `web_editions.py` + `customize.py` select; #5/#6 — v27 used 66 full-bleed `position:absolute` overlays = the Kobo no-art ROOT cause, now 87 framed). +`TestPhase2CrossReaderCSS` (4); gates test_presentation_polish 38✓ · test_title_page_style 12✓ · nested 10✓ · epubcheck 0/0/0/0 ×2 · lint 27/2/0 · mypy clean. **Cardo (Reg/Ital/Bold) + NotoSerifEthiopic staged** in `content/assets/fonts/` for P3 (full-font embed — `fonttools` NOT installed so NO subsetting; +~1.4 MB acceptable). **✅ Phase 3 SHIPPED (font embed):** `style_config.EMBED_FONT_PATHS` = 3 Cardo faces + Noto Serif Ethiopic (range-scoped U+1200-137F); fonts in `epub_working/fonts/` (bytes ship via `build_one` copytree; `patch_opf_fonts` only patches OPF — **added it to `build_standalone.py`**, was missing → would've been an epubcheck "undeclared resource"); 4 `@font-face` (outside managed region); `.vnote-hebrew`/`.vnote-greek` lead with Cardo; ATTRIBUTIONS+LICENSES OFL notices. +`tests/test_font_embed.py` (10✓ incl. e2e zip-has-bytes+OPF-item) · test_nested 10✓ · test_parallel_bible_phi1 34✓ · **epubcheck 0/0/0/0 on ethiopian-tewahedo + catholic-study + standalone-geez** · lint 27/2/0 · mypy clean. Fonts in all 3 zips (+~0.5 MB). Fixes #7 (device-confirm pending). **✅ Phase 5 SHIPPED (badge DEFAULT):** `apply_badge_markers(tmp, edition)` build-time per-edition post-pass in `build_one` (after `apply_bilingual_toc`, after kind/canon/tradition filters + `renumber_markers`; gated, `DEFAULT_MARKER_STYLE=badge`) — verse-grouped BY POSITION (`v-{code}-{ch}-{v}` anchor-first + `<span class=vn>` fallback), collapses each verse's `note-ref` markers to ONE `verse-notes-badge` (`vbadge-{code}-{ch}-{v}`, noteref) + MERGES that verse's asides into ONE `<aside class="verse-notes" id="vnotes-{…}" epub:type="footnote">` of `.vn-item` rows (native popup, NO JS). **Gen 1: 225 markers → 31 badges (conserved); ethiopian-tewahedo Gen 1:1 = 11.** `build_edition:1707/1712/1797/3776`; /customize select badge-default. **★base `epub_working` UNTOUCHED** (0 badge markers; only stylesheet.css gained rules). Gates: test_marker_style 13✓ · nested 10✓ · **check_nested_anchors on the BUILT badge EPUB 0/61** · **epubcheck 0/0/0/0 badge+numbers** · ebible 32263/32263 · **determinism/byte-stability gate PASSED** · lint/ruff/mypy clean. Edge-fixes: anchor-first grouping for spill (1ch3); last-verse extends past `</p>` to chapter-end (1ki7:51); tradition rows wrap balanced inner (RSC-016). **▶ NEXT (fresh session) = Phase 4 (Kobo structural — the LAST RX phase):** (a) **Kobo-safe TOC restructure** — drop `<details>`+flexbox (Kobo-broken #11); pick a COMPACT cross-reader form (NOT a giant always-expanded pill list — `reader_toc_collapsible:false` at build_edition:1955 yields a huge flat TOC; consider book-list-only / per-book linking); (b) **build-time file-splitter** (`apply_file_split` post-pass ~300 LOC) — split the 2–3 MB `index_split_*` into ~0.4 MB pieces for Kobo speed (#3); ★hard part = rewrite EVERY cross-file `index_split_NNN.html#id` href (TOC page alone has 1,864; thousands total) via an id→new-file map + regen OPF manifest/spine + nav.xhtml; epubcheck must resolve every link; do it build-time (base canonical) like badge. Then the **[USER] DEVICE TEST** (sideload on the COLOR Kobo + Apple Books). **Full handoff: `.remember/remember.md`.** Also open (post-RX, user-greenlight): the auto-note INGEST-defect backlog from Mac's audit (`notes/2026-06-06-auto-note-quality-audit.md`: dict-easton 37.9% truncated · lang-greek Theós 1196 · topic-torrey 596 · etc.). Master plan `plans/2026-06-05-epub-reading-experience-overhaul.md`. Baton: `windows`. TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (🪟 Windows) — σ ARC PLANNED: "Edition Cover + Truthful Front Matter" spec + impl plan written; Mac lane PAUSED → Windows runs both streams.** Brainstormed w/ the user (visual companion): the cover becomes **"HOLY BIBLE" + a builder-chosen subtitle** (overflow-proof fitter — fixes the title-over-border bug); a new **"Your Edition" first page** (name + the builder's notes + an auto, **always-truthful** "what's inside" + total + per-book counts + popup languages); and — the crux the user flagged — the counts + the symbol **glossary must honor the new ρ.3 per-book/chapter/note choices** (today they use the edition-wide matrix and would lie once a builder overrides), via a single build-accurate `resolved_note_counts` reused from the build's own filter sets. Ge'ez/Amharic standalone Bibles get default covers (`05_missal_central_red` / `01_ornate_leafy_brown`, Ethiopic titles), builder-overridable. Spec `specs/2026-06-04-edition-cover-and-truthful-front-matter-design.md` (APPROVED, `3c0988bc`); plan `plans/2026-06-04-edition-cover-and-truthful-front-matter-plan.md` (σ.1 counter → σ.2 cover → σ.3 front matter → σ.4 /customize identity → σ.5 Ge'ez/Amharic covers → σ.6 live-console reconcile). Per-chapter counts stay live in the console (not printed) — user-approved.
> - **★LANE PLAN (user-directed; Mac paused).** The user asked for a concerted Windows+Mac plan but **macclaude is paused + finished its side (website + Apple notarization); nothing new from it.** So **Windows owns BOTH lanes**, baton `windows`. Ready-to-activate split if the Mac resumes (file-disjoint): **CONTENT lane** = the Ge'ez transcription marathon + P0 (`content/manuscript/**`, `content/translations/sources/patrologia/**`, the own-vers store, manifests) — Windows-only (needs the local GAPS/CUDL assets + the strong machine; the cloud/pod was dropped); **BUILD lane** = the σ cover/front-matter plan (`generate_edition_covers.py`, `matter_pages.py`, `edition_stats.py`, `build_edition.py`, `build_standalone.py`, `editions.yaml`, `customize.py`, `api/*`, `matrix.py`) — portable code, the Mac's natural pickup. The only overlap (σ.5 touches `build_standalone.py`+`editions.yaml`; transcription touches them only at a book-completion) is baton-sequenced. Existing content plans: `plans/2026-05-28-geez-patrologia-vision-plan.md` (Esther p35) + `plans/2026-05-17-kings-manuscript-collation.md`.
> - **✅ σ.1 COMPLETE (build-accurate counter; foundation):** σ.1.1 extracted `build_edition.compute_edition_filter_sets` from `build_one` (pure refactor, determinism-gate-proven, reviewed) · σ.1.2 `scripts/core/edition_stats.py::resolved_note_counts` (total/per-book/per-category/per-kind/popup-languages, honoring the ρ.3 hierarchy by reusing those filter sets + a base-HTML coverage gate). **Its cross-check pin caught + fixed 2 real pre-existing bugs:** (a) a `filter_books_for_canon` **leading-spillover leak** — 2 Esdras' continuation shipped ~339 orphan note markers (epubcheck RSC-012 risk) into canons that drop it; new leading-spillover guard strips it only when ALL the leading region's books are dropped (conservative; +2 fast guard tests; ethiopian-tewahedo keeps 2es untouched), (b) a stray duplicate base marker `ref-1ch0103` removed from `epub_working/index_split_015.html`. **★Intentional output change** for editions that drop 2es (catholic-study etc.) — they stop shipping invalid orphans; NOT a regression (determinism gate PASSED 185s; nested-anchors 0/61). **Cross-check pin: resolved == built EPUB exactly** (catholic-study 41,792; ethiopian-tewahedo 43,382). Adversarial build-path review: APPROVE. Commits `a8ae68dc`·`be9b60e1`·`54ecd7d9`.
> - **✅ σ.2 COMPLETE (HOLY-BIBLE cover; the user's overflow bug fixed):** `cover_main_title`(default "HOLY BIBLE")+`display_name` fields; `cover_text_for_edition`→(main,subtitle); **overflow-proof `fit_text_block`** (wrap-then-shrink, hard-break floor — guarantees every line ≤ width); `_compose_cover` draws main+rule+subtitle in the safe band; 9 covers regenerated (byte-deterministic; byte-gate green); clean `display_name`s set. **★Visually confirmed** (catholic-study + ethiopian-tewahedo: "HOLY BIBLE"+subtitle cleanly inside the gold border). Review APPROVE; the vertical-overflow edge (pathological custom input) scheduled σ.4.4 (where the /customize input lands). Commits `1b2eb667`·`5467c4e4`.
> - **✅ σ.3 COMPLETE (the "Your Edition" first page + truthful glossary):** glossary (`_legend_categories_for_edition`) repointed matrix→`resolved_note_counts` (honors the ρ.3 hierarchy); new `render_your_edition_page`/`inject_your_edition_page` (name + builder notes + auto truthful "what's inside" + total + per-book table, all from the counter), placed FIRST after the cover; old About retired; front matter reordered `titlepage→your-edition→dedication→copyright→legend→scripture`. **epubcheck 0/0/0/0** (catholic-study), determinism gate PASS, nested-anchors 0/61; Your-Edition total == counter == 41,792. Review APPROVE; one count-consistency item (the copyright page still counts via the matrix) scheduled σ.6.2 (the count-reconcile phase). Commits `87293637`·`1b86871f`·`a6415eaa`.
> - **✅ σ.4 COMPLETE (`/customize` edition-identity control):** a "Your edition's name & notes" card — a name `<select>` of **smart suggestions derived from the build** (canon + `enabled_categories`; "Study Bible" offered ONLY when study families are on — never spurious) + "✏ Custom…" (capped 48) + a notes textarea (`description`); single hidden `display_name` field (no dup payload); recompose-on-save (empty-stem→factory template); `display_name`/`cover_main_title` added to `EDITABLE_TEXT` + `api_customize_data` (+`enabled_categories`) + clone-carry + the preview EDITABLE set (review fix) + an 80-char server cap. **σ.4.4 vertical-overflow guard** (`_compose_cover_layout`: clamp top_y + re-fit subtitle/main to the safe band; pixel-bounds test) — **byte-neutral for the 9 real covers** (verified 0 hash mismatches). **★Visual QA (Playwright, 0 console errors):** the card renders per-edition with correct suggestions (catholic→"Catholic Study Bible…", jewish→"Jewish Study Tanakh…"); Custom hidden until picked; notes box present. Review APPROVE after the preview-EDITABLE fix. Commits `819923ff`·`822c0b92`·`6408a5dc`·`44f3fb70` + review fix.
> - **✅ σ.5 COMPLETE (Ge'ez/Amharic default covers):** Ethiopic-capable cover font (`_font_for_text`: Nyala→Ebrima→PIL-default when text has U+1200–U+137F; threaded through `fit_text_block`+`_compose_cover_layout` so measure==draw — overflow guarantee holds; **Latin path byte-neutral → 9 standard covers untouched**). `standalone-geez` = `05_missal_central_red` / `መጽሐፍ ፡ ቅዱስ` / "Ge'ez Tewahedo Bible"; `standalone-amharic` = `01_ornate_leafy_brown` / `መጽሐፍ ቅዱስ` / "Amharic Tewahedo Bible"; both covers composed + committed; `build_standalone` ships them (`apply_edition_cover`, was a no-op on empty cover_image). **epubcheck 0/0/0/0** on a built standalone-geez; tofu empirically disproven (Times = identical .notdef boxes; Ebrima = distinct glyphs). **★Visually confirmed by the controller** — both covers show real, well-formed Ge'ez/Amharic script (gold-on-red / gold-on-brown), builder-overridable defaults. Review APPROVE (+stale-comment fixes). Commits `4bb03ba6`·`1c5ecd8a`·`911974df` + review fix `77757702`.
> - **✅ σ.6 COMPLETE → σ ARC COMPLETE (all 6 phases shipped).** Rerouted the last count surfaces through the build-accurate counter: `resolved_note_counts` extended with `per_book_chapter`; `/build-tracker` (total/per-book/heat-grid/category/kind/popups) + the **copyright page** (annotation total + category count) now read it; **removed the redundant mint-9 #8 `_annot_override` block + `_count_in_scope_disabled_ref_ids`** (resolved_note_counts subsumes it); `api_export_preview` `notes_shipping`/`category_breakdown` repointed; fixed the `matrix.py` "actually-shipping"→"potential" docstrings. **All 5 "what you built" surfaces now agree + equal the real EPUB.** epubcheck 0/0/0/0 (catholic-study), determinism gate + the σ.1 cross-check pass, nested-anchors 0/61, lint 26✓/2⚠/0✗. Review APPROVE (no issues ≥80-conf). Commits `92a52d6e`·`7c78e6ee`·`663987af`.
> - **σ ARC SUMMARY (spec+plan SHIPPED):** σ.1 build-accurate counter (+caught/fixed the 2es orphan-spillover + a stray base marker) · σ.2 HOLY-BIBLE cover + overflow-proof fitter (the user's bug, fixed) · σ.3 "Your Edition" first page + glossary→counter · σ.4 /customize name+notes control + vertical guard · σ.5 Ge'ez/Amharic Ethiopic-script default covers · σ.6 count reconcile. Subagent-driven, per-task spec+code review + visual QA throughout. Plan `plans/2026-06-04-edition-cover-and-truthful-front-matter-plan.md` (SHIPPED). The Mac lane shipped a website cover-system showcase in parallel (file-disjoint, rebased in).
> - **▶ NEXT SESSION OPENS WITH: the Kobo device-QA issues.** The user loaded a build on the COLOR Kobo (2026-06-04) → several issues (fewer on Apple Books); crops at **`E:\epub-kobo-crops`** (nested `epub-kobo-crops\` subfolder; 30 `epub-NN.jpg` reader screenshots [mostly clean macOS/Apple Books] + `linked-photo.jpg` = a Kobo device photo). Read those FIRST next session (copy to a temp dir — Read can't reach `E:\`). Also seen in passing: a `[Reviewer: …before promoting]` editorial placeholder leaked into a shipped Greek "Pistis" note popup → content-QA sweep. Memory [[kobo-color-ereader-end-stage-qa]] updated. Other open lanes: ρ.3 C3 polish residue · Phase D1b Esther (paused p35) · the reading-plans dirty-check backlog item (below). Baton: `windows`. TRACKER-STATE active.
> - **↻ Backlog (pre-existing, found in the σ.4 review — NOT σ-introduced):** `/customize` reading-plans dirty-check never re-baselines `readingPlansState.original` after a successful save → the Save button stays "dirty" post-save for reading-plan changes (ψ.19). Small fix; out of σ scope.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE C2 COMPLETE: the `/build-my-bible` navigator FRONTEND, byte-neutral, shipped.** The 4-level drill-down console (Bible→book→chapter→verse) toggling note-symbol families/kinds + translation-popup languages at every level. Subagent-driven (implementer + spec + code-quality review per task; all approved after fixes), **8 commits `ce591d93`→`48d0f21f`** (rebased onto Mac's `2064b86d`), plan `plans/2026-06-04-hierarchical-customize-phaseC2-navigator.md` (C2-2…C2-6 all done). **C2-2** shell + route + §6.2 cross-link (console 18→19). **C2-3** lazy drill-down JS (clickable breadcrumb, BOOK/CHAPTER caches + stale-guards, spinner, XSS-safe). **C2-4** interactive symbol tri-state (book+chapter, category→kind) + popup checklists (book/chapter/verse) → `PUT /api/edition-meta/<id>` (absolute-replace from JS `WORK`; bulk-clears-finer §3.2; debounced+serialized autosave; +`popup_languages_per_book` overrides fix). **C2-5** verse note "ships?" checkbox + atomic `PUT /api/edition/<id>/note-override` (`default|on|off`; `enabled_note_ids` force-on / `disabled_note_ids` force-off, mutually exclusive, force-on wins) + `ships`/`kind_enabled` read flags. **Bible level read-only** w/ /matrix + /customize cross-links. **★BYTE-NEUTRAL** (no build_one/resolver/config/`epub_working`; byte-stability gate PASSED; `git diff epub_working` empty; 9 KJV byte-identical). **★VISUAL QA live (Playwright, 0 console errors):** 4 levels render+navigate; book-xref-OFF→`note_families_off_per_book.gen=xref` + verse note unchecked; verse Hebrew popup→`popup_languages_per_verse[gen:1:1]`; editions.yaml restored byte-identical post-QA. **▶ NEXT = Phase C3 polish** (/build-tracker resolved-state annotation · website "how you make it yours" copy · reconcile /customize+/sources to surface the new chapter/verse levels). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🖥️ Mac lane) — WEBSITE LAUNCH-SWAPS #3/#4/#6 SHIPPED LIVE; #5 owed to user.** User-directed (website items 3–6 + the nav fix), file-disjoint from Windows' ρ.3 Phase C. **Deployed to GitHub Pages** (`yhwh-website` `6ca90da`→`3d03836`, live at www.yhwhyaway.com): **#3 Giscus comments LIVE** — enabled Discussions on `yhwh-website`, installed the **giscus GitHub App** (user did the passkey sudo), pre-created the mapped **"Website feedback"** discussion (#1, Announcements cat `DIC_kwDOSwgvcM4C-gWD`), wired `data-repo`/`data-repo-id` (`R_kgDOSwgvcA`)/category in `feedback.html`; **#4 donations LIVE** — Ko-fi (`ko-fi.com/gringoboggy`, browser-verified) + PayPal (`paypal.me/gringoboggy`, HTTP 200) → real `<a>` buttons; **GitHub Sponsors kept PENDING** (`gh: user.hasSponsorsListing == false` — not yet approved; placeholder span retained, swap when it goes public); **nav** `Support`→**`Give`** (read as tech-support); **#6 copy** — feedback page flipped future→present (comments live), customization "coming soon" line aligned to "being built now". **▶ STILL OWED: #5 email forwarding** for `gringo.boggy@yhwhyaway.com` — passkey-gated Spaceship/Spacemail, **user is setting it up** (mailto links work meanwhile). Monorepo source committed + pushed (both remotes). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE B COMPLETE (ρ.3 popup engine), byte-stable, shipped.** Per-coordinate translation-popup-language resolution across Bible→book→chapter→verse. Subagent-driven TDD, 6 commits (`3969e1a6`→`45091d77`); plan `plans/2026-06-04-hierarchical-customize-phaseB-popup-engine.md`. **New fields** `popup_languages_per_chapter`/`_per_verse`; **engine** `_resolve_popup_languages(edition, book, chapter, verse)` 5-tier most-specific-wins (is-None cascade for explicit-empty; backward-compat invariant — 2-arg + no-override-4-arg identical) + the single call site threads ch/vs; **stale-cache fix** in `build_cache._referenced_translations` (walks per-chapter/verse, +4 tests). **★Byte-stability PROVEN:** gate PASSED 187s · `epub_working/` untouched · nested-anchors 0/61 · epubcheck 0/0/0/0 · suites green (popups 12 · build_cache 40 · symbols 21); 9 KJV byte-identical. +24 tests (1 slow e2e: per-verse override strips Greek from gen 1:1, keeps it in 1:2). Folded in the 2 Phase-A review nits. **Headless** (UI/API = Phase C). **▶ Phase C1 (API write path) ✅ SHIPPED** (`68f1598b`: 6 new fields via a DRY `_validate_keyed_list_field` helper + `_KEYED_LIST_FIELDS` dispatch table in `api_save_edition_meta`; EDITABLE + clone-carry + `api_customize_data` decode; +56 tests; byte-neutral). **▶ Phase C2-1 (navigator read API) ✅ SHIPPED** (`eabd00bb`: `api_build_my_bible`, lazy 3-level drill-down feed; 27 tests). UX **locked = drill-down + breadcrumb** (user-chosen); C2 plan written (`plans/2026-06-04-hierarchical-customize-phaseC2-navigator.md`, 6 sub-tasks; C2-1 done). **▶ NEXT = C2-2…C2-6** = the navigator FRONTEND (console shell + §6.2 cross-link · drill-down JS · the symbol tri-state + popup-checklist toggle controls + save via `api_save_edition_meta` · the verse/note level + `enabled_note_ids` force-on write path · byte-backstop + visual QA) → C3 polish (cross-link · /build-tracker resolved-state · website "how you make it yours" copy). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE A COMPLETE (ρ.3 symbol engine), byte-stable, shipped.** Per-coordinate note-symbol resolution across all 4 levels (Bible→book→chapter→individual) + `enabled_note_ids` force-on. Subagent-driven TDD, 6 commits (`8a751bc5`→`ea1f824e`); plan `plans/2026-06-04-hierarchical-customize-phaseA-symbol-engine.md` (all 8 tasks done). **New fields** `note_families_{on,off}_per_{book,chapter}` + `enabled_note_ids`; **engine** `config.enabled_kind_codes_for` (most-specific-wins; phase/AI hard gates; no-override == `enabled_kind_codes` for every real edition) + `_iter_note_ref_symbols` (91,733 = exact corpus) + `compute_symbol_disabled_html_ref_ids` (mirrors tradition filter, short-circuits empty) wired into `build_one` (force-on subtracted LAST; `disabled_kinds_for_filter = disabled − overridden_kinds`; `filter_html` unmodified). **★Byte-stability PROVEN:** gate PASSED 187s · `epub_working/` untouched · nested-anchors 0/61 · flagship epubcheck 0/0/0/0 · affected suites green; 9 KJV byte-identical. +25 tests. **Headless** (UI/API = Phase C). **▶ NEXT = Phase B** (popup-language hierarchy: `_resolve_popup_languages(edition, book, chapter, verse)` + per-chapter/per-verse fields, spec §6) **→ Phase C** (`/build-my-bible` navigator console). The superseded line below (my prior "Phase A plan ready") is now realized. Baton: `windows`. TRACKER-STATE active.
>
> **▶ DONE 2026-06-04 (🖥️ Mac lane — Apple signing + website overhaul + Geʽez; user-authorized commit/push, file-disjoint from Windows) — ★FIRST NOTARIZED macOS BUILD in flight; website overhaul shipped LIVE.** **Apple Developer signing wired** (Mac's #1 task): `dev/launcher.spec` platform-branched to a **onedir** notarizable `.app` on macOS (onefile stays on Win/Linux — onefile-in-.app is deprecated, "clashes with macOS security"); `dev/YHWH.entitlements` (hardened-runtime for the frozen-Python app); `build_dmg.sh` passes `--entitlements`; `build_desktop.sh` uses the repo `.venv` python (not system 3.9) + no silent install; PyInstaller 6.20 declared in `requirements-dev.txt`; `VERSION`→`1.0.0-beta.1`. User created a Developer ID Application cert (`Bogdan Zorlescu (AAHZNDCGFW)`; I installed Apple's G2 intermediate to complete the chain) + a `yhwh-notary` keychain profile. **`dist/YHWH-1.0.0-beta.1.dmg` built + signed (`codesign --verify` OK) + submitted** (`0c0d10c1`) → **notarization STUCK ~8h = an APPLE NOTARY-SERVICE OUTAGE 2026-06-04** (submission `0c0d10c1` still "In Progress"; repeated request-timeouts on Apple's endpoint; our build is FINE — `codesign --verify` OK, Developer-ID + secure timestamp). The build_dmg `--wait` AND a 6×30m re-attach BOTH timed out — Apple never returned a verdict. Commit `4bf2ebf8` (pushed both remotes). **⚠ FINISH WHEN APPLE RECOVERS (manual; the signed dmg is on disk at `dist/`):** `xcrun notarytool wait 0c0d10c1-5e3b-4c6c-a418-368edae22eea --keychain-profile yhwh-notary && xcrun stapler staple dist/YHWH-1.0.0-beta.1.dmg && spctl -a -vv -t open --context context:primary-signature dist/YHWH-1.0.0-beta.1.dmg && .venv/bin/python scripts/gen_checksums.py dist --out dist/SHA256SUMS.txt` — don't clear `dist/` until stapled (else re-notarize); stapling changes the dmg so checksums regen AFTER. (Or re-run `dev/build_dmg.sh` with `CODESIGN_IDENTITY`+`NOTARIZE_KEYCHAIN_PROFILE` set.) **Website (all LIVE on `yhwh-website` + committed to monorepo):** first-person mission/About reword (his real "I just wanted one free English Ethiopian study Bible" origin) + reworked hero creed; whole-site **de-gaud** pass (5-agent critique workflow → hand-applied); the "fully customizable" **Bible-shaped cascade** copy (notes AND popups at whole→book→chapter→verse, framed "coming" — matches Windows's hierarchical-customize feature); **11→9 starting-editions honesty fix** (2 standalone not buildable yet); **Beta page collapsed into a Releases download hub** (`beta.html`→redirect, Beta dropped from nav); honest "almost here" beta state; wordmark `ኪዳን`(covenant)→`እግዚአብሔር`(God, matches brand); feedback page no longer promises a phantom comment box; footer socials show handles not platform names. Commits `846223e7`/`1a9dbd28`/`dedd36d6`; deploys `87b1bb4`→`6ca90da`. **Geʽez emphasis (accuracy):** README headline bullet for the Geʽez/Amharic manuscript Bibles + honest 9-editions + cross-platform quick-start; refreshed GitHub+GitLab+website repo descriptions (Geʽez-led); profile README elevated Geʽez + dropped stale "coming soon". Commit `5aac549e` + profile `2c76649`. **▶ NEXT: when Apple's notary service recovers → finish the notarization (re-attach + staple + verify + regen checksums — command above); then the public-launch swaps (public source repo + post the v1.0.0-beta.1 release + flip download links + Giscus go-live).**
> Baton: `windows` (Mac worked file-disjoint; user OK'd this commit/push). TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (Windows, evening) — ☠ VM/CLOUD APPROACH DROPPED (pod `tvdzqrdpyw95bf` TERMINATED + confirmed gone) · Sam/Kings → LOCAL agent-path marathon (LANE M) · ▶▶ FOREGROUND = HIERARCHICAL EDITION CUSTOMIZATION (Phase A plan ready).** User-directed: *"drop all the vm plans, we're doing it all the way we have been."* **Pod bulk FAILED** — `1sa2` ran 2h10m then CAM error (`rc=2`, no CAM); chapters 2–51 all failed instantly (`rc=1`, 2–5s each) → **0 usable witness JSONs** (only artifact = 1sa2 GG raw passes; not finalizable without CAM). Pod terminated via RunPod API (`podTerminate`, verified — pods list empty); ~$16 credit stays on the account for future. Sam/Kings continues on the **local marathon** (`plans/2026-05-17-kings-manuscript-collation.md`) — NOT the cloud. The 2 cloud docs marked DROPPED.
> - **★ FOREGROUND (the real user #1) — HIERARCHICAL EDITION CUSTOMIZATION:** navigate the edition *like a Bible* (Bible → book → chapter → individual), toggling BOTH **note symbols** (families + kinds) AND **translation popups** at every level; most-specific-wins; byte-identical when untouched. **Tier-1 (edition-wide, `/matrix`) and individual-note (`/sources`, Phase ρ.1/ρ.2) ALREADY SHIP** — the genuinely new work is the book+chapter family levels, per-chapter/verse popups, force-on, and a `/build-my-bible` navigator console. Spec **APPROVED**: `docs/superpowers/specs/2026-06-04-hierarchical-edition-customization-design.md`. **Phase A (symbol engine) plan WRITTEN**: `docs/superpowers/plans/2026-06-04-hierarchical-customize-phaseA-symbol-engine.md` — per-coordinate resolver `config.enabled_kind_codes_for(edition, all_kinds, book, chapter)` + `build_edition.compute_symbol_disabled_html_ref_ids` (mirrors the tradition filter; ref-ids already encode book+chapter+verse → no new strip plumbing) + `enabled_note_ids` force-on; 8 TDD tasks; byte-stability gate = ship bar. **▶ NEXT: execute Phase A** (subagent-driven), then Phase B (popups), Phase C (the navigator). Decisions locked: granularity = category+kind; precedence finest-wins (per-book ON overrides edition disable); force-on absolute; bulk-toggle clears finer in scope (UI).
> - **★KINGS CAM-FOLIATION BUG still stands** (data-integrity — independent of the VM drop): `D:\yhwh-folio-crops\1ki_b3\CAM\` f135–140 are physically 1Ki22→2Ki~7, NOT 1ki13–18; §17 CAM 1ki 13–18 + the prior CAM 1ki 7–12 fill are SUSPECT; GG 1ki 13–18 stands; re-map CAM 1ki by vision from f126=1ki1 when the local marathon resumes Kings.
> Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior — SUPERSEDED; the pod has since FAILED + been TERMINATED, VM approach dropped) 2026-06-04 (Windows) — ☁ POD SAMUEL TRANSCRIPTION BULK RUNNING (autonomous, resumable) · website reworked+deployed+HTTPS · ★KINGS CAM-foliation BUG found · ▶▶ NEXT SESSION #1 = BUILD PER-BOOK NOTE SELECTION (user-directed), before any manuscript.** Full resume how-to in `.remember/remember.md`.
> - **#1 NEXT (user-directed, BEFORE manuscript): per-book note-family selection.** Today note families are EDITION-WIDE only — `scripts/core/config.py:enabled_kind_codes(edition, all_kinds)` has NO per-book dimension; the build applies one enabled-kinds set to every book. User requires per-book AS AN OPTION (a family on in Genesis, off in Exodus). Clean additive build mirroring the existing per-book pattern (`traditions_per_book`/`popup_languages_per_book`/`book_covers`): add `enabled_kinds_per_book`/`disabled_kinds_per_book` (+categories) to `editions.yaml` (default empty = back-compat, byte-identical) → give `enabled_kind_codes` an optional `book=` arg layering the per-book override → resolve per-book in the inject/filter loop (BUILD-PATH → prove 9 KJV byte-stable) → extend the `/customize` per-book matrix → tests → THEN update the website "How you make it yours" copy to promise it. RULES §6.5 + §9.
> - **☁ POD Samuel dual-witness bulk — RUNNING autonomously** on RunPod `tvdzqrdpyw95bf` "yhwh-cloud" (`root@205.196.17.156 -p 11489` — EPHEMERAL, changes on restart). Agent path (Claude Code on the pod via Max sub, NOT paid API): pod driver runs 2 blind `claude -p` vision passes/witness reading **native-res COLUMN TILES** (`manuscript_folio_crop.py` 3×3) → `manuscript_finalize_chapter.py` (converge→assemble→collate). ★Whole-folio 1568px downsample = ILLEGIBLE (3–5px/char → model honestly returns `[illegible]`); **column tiles FIXED it** (GG faithful: Hannah's prayer 1sa2). ★Pod is RATE-LIMIT-bound not core-bound (shared Max-sub limit; concurrency-4 → garbage/throttle → run concurrency-1 sequential, slow ~30–50min/pass, CAM denser → 4800s timeout). Budget $16.2 ÷ $0.21/hr ≈ 77hr ≈ **~25–30 of 51 ch** (user: "run till money runs out"). **Resume:** ssh → `cat /root/bulk_samuel.log` (per-ch START/END/rc) + `ls /root/out/*_witnessCAM.json` (done); **VERIFY 1sa2 CAM quality** (the live CAM-tile check — if `[illegible]`/garbage, CAM tiles failed → stop+retune; if faithful, let it run); `scp root@…:/root/out/*.json` → `content/manuscript/samuel/calibration/` + commit from N95 (**pod NEVER pushes** — single-committer=N95); RESUMABLE (skips done). Stop/start via RunPod API (key `~/.runpod/config.toml`; podStop/podResume GraphQL). Pod tooling: `/root/yhwh/_pod_transcribe_driver.py` + `/root/_pod_bulk_samuel.sh` (copies in repo-parent `…/YHWH-v2.4-full/`); ⚠ formalize the driver into `scripts/` (+tests) next session.
> - **★KINGS CAM-FOLIATION BUG (data-integrity — do NOT trust Kings CAM until re-mapped):** a vision pass found the `D:\yhwh-folio-crops\1ki_b3\CAM\` crops (f135–140) are physically **1 Kings 22 → 2 Kings ~1–7**, NOT 1ki 13–18 (penned folios 135–140 ARE correct; the §17 chapter→folio ASSUMPTION was wrong; 1ki/2ki book-seam = f135v→f136r). So §17's CAM 1ki 13–18 acquisition is wrong AND the prior **CAM 1ki 7–12 manifest fill is SUSPECT** (likely shifted). GG 1ki 13–18 (§17 table) is independent + stands. **Re-map CAM 1ki by vision from the f126=1ki1 anchor forward; correct 7–12; fill 13–22.** The manifest was NOT written with the bad 13–18 data (no corruption — just don't trust §17's CAM column).
> - **Website (Mac lane; Windows stepped in, user-directed):** removed the per-book-limitation callout + tightened the hero creed in `website/src/index.html`; deployed from Windows to GitHub Pages (`yhwh-website` `54c3544`); **cert provisioned + Enforce HTTPS ENABLED**; verified live (`https://www.yhwhyaway.com`, note gone + new creed). Mac handoff: `dev/LANE_HANDOFF.md` (turn 7, ★pull publish copy before deploying) + `website/README.md`. More copy rewording owed. ⚠ **rotate the Claude Code OAuth token** (`/root/.claude_oauth_token`) — it was partially echoed into a transcript.
> Baton: `windows`. Pod API key + Claude OAuth token stored locally (gitignored). TRACKER-STATE active.
>
> **▶ (prior — superseded by the pod run above) 2026-06-03 (Windows lane) — P0 KINGS: 1ki 7–12 mapped (both witnesses); 1ki 13–18 GG done + CAM acquired/cropped, CAM pass PENDING; Samuel transcription wired. [PAUSED for a reboot.]** Samuel COMPLETE (prior). This session: mapped **1ki 7–12 both witnesses HIGH** (filled `kings/manifest.yaml`; kings pending 41→35), mapped **GG 1ki 13–18 HIGH**, IIIF-acquired + cropped **CAM f135–f141** — but the **CAM 1ki 13–18 vision pass was interrupted by the reboot.** **★Wired the P4 Samuel transcription** (paid-API `run_manuscript_transcribe_at_scale.py`; `anthropic` 0.105.2 installed, dry-run OK; needs `ANTHROPIC_API_KEY` → `~/.config/yhwh/anthropic.key`). **▶ RESUME (fresh session) — read `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` §17 FIRST** (it has the GG 13–18 table + the exact CAM-13–18 resume + transcription how-to): (1) run CAM 1ki 13–18 (crops at `D:\yhwh-folio-crops\1ki_b3\CAM\`) → fill 13–18 both witnesses → gate; continue 1ki 19–22 → 2ki. (2) Drop the API key in → kick off Samuel transcription in the background (parallels Kings mapping, no contention). Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior) 2026-06-03 (Windows lane) — P0 Sam/Kings: 1 SAMUEL COMPLETE (1–31, both witnesses); NEXT = 2 Samuel.** Added 1sa 18–31 for both witnesses (GG companion + CAM passes; **first CUDL-IIIF acquire** of CAM f114–f117 — needs `PYTHONPATH=<repo>`). Recension: GG omits 18:1–5; CAM ch18 = 18:1 covenant; 1 Samuel ends mid-folio (GG f017v / CAM f117r), 2 Samuel 1 right after. `samuel/manifest.yaml` 1sa 1–31 filled (boundary-generous, status `pending`); anchor index §14. Gate: image-existence GREEN; samuel now only 2sa pending (23). Committed `ab86dd87` (+ documents Mac's `brand/` in REPO_MAP). Pulled Mac turn-3 (brand kit + GitHub Sponsors tiers published). **Full 5-leg save (PUSHED) this session-end; baton → windows.** **▶ NEXT (fresh session): P0 2 Samuel 1–10, 12–24** — GG on disk (f017v–f028); **CAM-IIIF acquire** the 2sa folios (only f120 local; continue the view sequence past f117 — read penned numbers to verify); same method → then 1ki 7–22 → 2ki 1–25. READ `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` FIRST. Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior) 2026-06-03 (Windows lane) — P0 Sam/Kings folio-index: 1 SAMUEL 1–17 COMPLETE (both witnesses, vision-verified); dense-section wall RETIRED.** Unique-event anchoring + recension awareness (GG=LXX-compressed, CAM=MT-fuller) fixed the wall that gave the prior session geometrically-impossible 1sa 7–16 assignments. Three cross-checked HIGH-confidence vision passes (CAM keystone 7–17 · GG companion 2–17 · CAM 1–6 re-verify; penned folio numbers confirm the view↔folio arithmetic). **Corrected a real bug:** the prior 1sa 4–11 CAM map was shifted (on-disk filename labels ~+3 off) — re-mapped; filled 2,12–16. Rewrote `samuel/manifest.yaml` 1sa 1–17 (both witnesses, boundary-generous, status pending) via a deterministic generator; added reusable `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` (method + 4-book anchor tables + availability: **GG 100% on disk; CAM partial → CUDL-IIIF**). Gate: image-existence GREEN; has-folios 43→37 samuel. Commits `8b6cb947`+`683bf66e` (synced this session-end). **▶ NEXT (fresh session): P0 1sa 18–31** (GG on disk f011v+; **first CAM-IIIF acquire** via `scripts/acquire_cudl_master.py`, anchor f106r=view215, 2 views/leaf) **→ 2sa 1–24 → 1ki 7–22 → 2ki 1–25** — same unique-event method, MAX-1 heavy vision, sub-batch check-ins, run the manifest gate per batch. Then P1 pod run (or local-free grind) per the user's pricing-trigger. **Baton: held by `windows`.** TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (Mac website lane) — ★WEBSITE LIVE at www.yhwhyaway.com (GitHub Pages, NOT Spaceship).** All 5 pages serving (HTTP 200); HTTPS cert auto-provisioning → enable Enforce HTTPS next. Spaceship cPanel/FTP unreachable from the Mac (ports 2083/21 blocked, only 443) → pivoted: built `website/dist/` → NEW public repo **`github.com/gringoboggy/yhwh-website`** (clean, no monorepo history, GH no-reply email) → Pages enabled; the domain's product-locked apex `A`/`www CNAME` freed by **disconnecting the domain from Spaceship web hosting**; then Spaceship DNS API (443) set apex `A`→185.199.108–111.153 + `www CNAME`→`gringoboggy.github.io`. **Email (Spacemail MX/SPF/DKIM/SRV) preserved** (backup `/Volumes/MacHD2/yhwh-dns-backup-*.json`). DEPLOY = `node website/build.mjs` → push `dist/` to `gringoboggy/yhwh-website` (memory `reference_spaceship_hosting`/`project_website_launch` + `website/README.md` rewritten to this). Earlier this session: website v2 shell (`4494a129`) + `scripts/gen_checksums.py` (`b9b732bc`) + `runpodctl` on the Mac. **▶ NEXT (fresh session, USER-CHOSEN): set up Apple Developer signing → get a notarized macOS build (≥1 cert on there).** Remaining launch swaps: Enforce HTTPS · downloads-live + first beta build + `SHA256SUMS.txt` (tool ready) · Giscus go-live + pre-create thread · email-forwarding test · donation links. Spaceship web hosting now unused for the site (cancel before Jun 29 or keep; Spacemail kept for email). **Baton: `windows`** (P0 Kings — file-disjoint). TRACKER-STATE active.
>
> **✅ DONE 2026-06-03 — LANE-HANDOFF BATON SYSTEM SHIPPED (Windows↔Mac two-lane coordination).** Turn-based baton: the holder is the active worker + sole pusher + truth-record owner that turn. Built `scripts/lane_handoff.py` (pure stdlib; 8 tests; mypy+ruff clean) + `dev/LANE_HANDOFF.md` (the committed shared baton) + `.claude/commands/{handoff,resume,sync}.md` + SessionStart incoming-check on both OSes (`bootstrap-triad.ps1` + new `bootstrap-triad.sh`) + `.gitignore` carve-outs + RULES §4 bullet. Brainstorm→spec→plan→build this session; verified end-to-end (handoff→banner→mark-seen→silent). Spec/plan: `{specs,plans}/2026-06-03-lane-handoff-baton-system-*.md`. **Baton currently held by `windows`.** **▶ Mac ADOPTION pending** (Mac is on the website now): set `dev/.lane=mac`, wire the Mac local SessionStart to `bootstrap-triad.sh`, `chmod +x` it — then a `/handoff to-mac` is the system's first real use. TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-03 — P0 Sam/Kings FOLIO-INDEX on the N95 (free).** User chose N95-free P0 over the $0.26/hr RunPod RTX 4000 Ada — the pod can't transcribe pending chapters until folios are mapped (free N95 vision work); **pod stays OFF until bulk transcription P4**. Per `plans/2026-06-02-samkings-folio-index-p0-plan.md`. **Harvest (free, no vision):** backfilled 1ki6 (GG+CAM; was pending → kings 42→41) + 1sa2 GG from existing witness JSONs (`1535e74e`); harvest now exhausted. **1sa 4-6 pilot PROVEN end-to-end:** GG-walk (1 Opus vision agent, content-anchored vs KJV landmarks → 1sa4=[f004r,f004v], 5=[f004v,f005r], 6=[f005r,f005v]) + CAM IIIF acquire views 218/219/220=f107v/f108r/f108v (~40s/folio; **penned folio nums 107/108 VISION-CONFIRMED** → view→folio arithmetic holds) + manifest fill; gate image-existence GREEN, samuel both-folios 51→48. CUDL IIIF reachable from this box. ★Lesson: GG `ክፍል`/`ምዕራፍ` rubric numerals ≠ modern chapter numbers (content-anchor onsets). **▶ NEXT = user GO for the BULK** (89 ch remain: 48 sam + 41 kings, all genuinely un-transcribed → GG-walk + CAM-IIIF). Proposed bulk method: contiguous per-book runs — bulk-acquire a CAM view range + one GG-walk agent per ~6-8 folio run, ~5-ch sub-batches, gate+commit per batch, marathon-paced check-ins. ⚠ COMMIT COORDINATION: Mac lane commits/pushes FIRST; N95 local commits OK, push deferred. TRACKER-STATE active.
>
> **☁ ACTIVE 2026-06-02 — RUNPOD CLOUD LANE: PAUSED (pod STOPPED, $0.00/hr) awaiting good pricing + the user's "feed it" trigger; CORRECTED PLAN written; PHASE-0 gate started.** Deployed the A5000 → verified SSH/env (direct TCP/SCP works at `root@<IP> -p <PORT>`; 128-core host so no OOM; Python 3.12 + git present, node absent) → **STOPPED** ($0.00/hr displayed; ~$0.08 used; pod `v2ou3ui5h8iapf`, restart re-reads the connect string; Terminate to fully wipe). **★CORRECTION:** `run_manuscript_transcribe_at_scale.py` = paid-API path (`ANTHROPIC_API_KEY`, out of budget) → the cloud run must use the **AGENT path** (Claude Code on the pod via the Max subscription; vision sub-agents Read crops). Clean seam: pure `assemble_witness(model_out,…)` → a sub-agent returning `model_out` is a drop-in for the API call; downstream collate/apparatus/store unchanged. **PLAN** `plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md` (P1 agent workflow `samkings-dualwitness-batch.js` + converge gate + assemble shim, TDD on calibration · P2 lean-tarball bring-up [no GitHub cred] + setup-token + GAPS · P3 pilot+measure · P4 bounded run + budget guard; frugal lifecycle). **PHASE-0 STARTED:** `tests/test_samkings_manifest_complete.py` RED → **93 pending** (sam 51, kings 42); GG fully on-disk, CAM hires partial (CUDL-IIIF for Sam-pending + 1ki13–22 + all 2ki). Lean tarball built `D:\yhwh-pod-subset.tgz` (75 MB). Prior runbook open-items resolved: scp (not rsync/runpodctl); bundle/tarball (no PAT); direct SSH confirmed; first-connect verified. **✅ P1 COMPLETE + PROVEN end-to-end on 1ki1 (free, N95).** Built + tested (15 P1 tests, mypy clean): `manuscript_converge.py` (converge gate + `sanitize_model_out` [strip rubric ❈, clamp OOB uncertain idx, downgrade orphan illegible→damaged] + `renumber_verses` [witness contiguity]) · `manuscript_assemble_witness.py` · `manuscript_render_crops.py` · `manuscript_finalize_chapter.py` (converge→assemble→collate) · `.claude/workflows/samkings-dualwitness-batch.js` (reads run-config from a JSON file via a bootstrap agent — Workflow `args` DON'T propagate on this harness; `sequential` flag = MAX-1 heavy-vision). **1ki1 proof** (`wf_7d11849e-21a`; 4 MAX-1 blind passes; ~1.9h/603k tok): real faithful Ge'ez (✣/❈, glyph-order doubts, Adonijah `አደንያስ`/`አዶንያስ`, En-Rogel/Shunammite flagged faded); after the sanitize/renumber fixes BOTH witnesses validate + collation SUCCEEDS (base=CAM · 50 v · **witness agreement 56.97% = 417/732** · base_structured_ok=True). **★FINDING (data):** the pass-convergence gate is verse-binary → reads low (GG 11%/CAM 2%) because any 1-token diff flags the whole verse + the passes segmented 53 vs 50; the meaningful agreement is ~57% token-level → **refine the gate to token-level** (flag divergent tokens, not whole verses; propose to user — the conservative gate works, doesn't block). Also: added `HOW_I_WORK.md` (portable global rules) + the plan's foolproof **parallel-operation protocol** (single-committer=N95, pod never pushes; file-disjoint lanes; shared-quota-bounded). **▶ NEXT: USER authenticates (GitHub+GitLab) → pull repo to the Mac (2nd full lane; its Claude self-provisions) → token-level convergence refinement · P0 folio-mapping. Pod run still waits on the user's pricing-trigger.** TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-02 — PHASE D1b PO Esther vision marathon RESUMED · PAGES 28–34 DONE — ★CHAPTER 1 COMPLETE (vv1–22) + CHAPTER 2 COMPLETE (to 2:23) + CHAPTER 3 to 3:8 (3:8 cut → p35), 5-leg saved · NEXT = p35 (3:9–13 → Addition B begins).** Method this page: 2 blind Opus passes (Workflow `wf_d49c0422-6d2`, run SEQUENTIALLY = MAX-1-heavy) → A complete + French-consistent; B corroborated thru v13 then HONESTLY stopped at an image-budget limit → the faithful 2nd witness for v14–16 + all A-vs-B adjudication = the CONTROLLER reading the full strip + targeted high-zoom crux crops, cross-checked vs Pereira's French AND the printed apparatus (MNPQ). **★A 3rd fresh pass (B2) was REJECTED for HARMONIZING** to the standard LXX/Hebrew text (fabricated cut-off v16 + standard eunuch names contradicting the French) → logged as `_vision_notes (q)`; the apparatus-as-2nd-anchor lesson logged as `(r)`. Resolutions: v12/13 boundary (v13 starts `ወነገሮሙ`), 3 princes `አርቄስዮስ/ስርስቲዮስ/ማግሌ`, v10 prints 8 `ለ`-tokens vs 7 French/LXX names (8th `ወለዢ` flagged), name1 printed `ገማ` (read-the-print vs apparatus/French `ሐማ`). Accumulator pages 24–28; codepoint-clean. Background lane done: D2 source-readiness recon (1 Enoch GO parse-first / Jubilees vision-required). **p29 (1:16-tail + 1:17–22, ch1 close) was the HARDEST page** — both blind passes unreliable (pass B dittographed vv19–21) → controller multi-tile read + a dedicated ADJUDICATOR agent (rendered own crops; reconciled A/B + French + apparatus). Caught: queen `አስጢን` (both misread Esther), `መገብተ` governors, v17 onset `ወነገሮሙ` + a GENUINE doubled `አበየቶ ፡ ለንጉሥ` clause (apparatus-confirmed M/Q `o`), v19/v20 boundary fix, v22 `ይፈርህዎሙ`; new `_vision_notes` failure-classes (s) heavy-wrap margin-layout + (t) both-passes-unreliable→adjudicator + apparatus-arbitrates-doubling. Accumulator pages 24–29. **p30 (2:1-7-head, ch.2 begins) — both blind passes unreliable (A garbled/skipped v4; B wrong on interiors) → dedicated ADJUDICATOR convergence; queen `አስጢን` (vv1,4, both misread Esther) vs `አስቴር` (genuinely Esther, v7); demonstrative `ግዘንቱ` (k); Mordecai genealogy + `ናቡክድናጾር`. v7 continues → p31. Accumulator pages 24–30. **p31 (2:7-tail + 2:8-13) DONE — complete 2:7 assembled; Esther under the eunuch Gây/Hegai; ★v12 read-the-print SAVE (pass A harmonized 'myrrh' to the French; the Ge'ez says `ዕፍረት` scent — restored, extends (p)); v8 onset `ወሰበ ፡ አዘዘ`; Mordecai `መርዶኬዎስ`; Ge'ez numerals `፲ወ፪`/`፮`. v13 cut → p32. Accumulator pages 24–31. **p33 (2:19-tail + 2:20-23 + 3:1-2) DONE — ★CHAPTER 3 BEGINS; complete 2:19 assembled.** A CLEAN high-agreement page — both passes honored the (u) discipline (no MT eunuch-names; conspirators stay unnamed `፪ ኅጽዋን`) → NO adjudicator, controller-adjudicated via French + 2 crops + apparatus. ★DIVINE NAME `እግዚአብሔር` (2:20). ★Haman enters (`ሃማ` son of Hammedatha the Bougaean `ብግያዊ`, 3:1). Demonstrative mixed on one page: `ዝንቱ` (3:1) vs `ለግዝንቱ` (2:23) per (k). The (u) guard kept both passes faithful (positive counterpart to p32's catch). v3:2 cut at `ወመርዶኬ` → p34. Accumulator pages 24–33. **p34 (3:2-tail + 3:3–3:8) DONE — complete 3:2 assembled (`ወመርዶኬዎስሰ`, the contrastive enclitic -ሰ, cf. 2:20 `ወእስቴርሰ`); 3:8 cut at `…ዘኩሉ ፡ አሕዛብ` → p35.** The 2 blind passes (`wf_7c0886f0-9e5`) DIVERGED **and SHARED a misread** → CONTROLLER adjudication (crisp crux crops + French + apparatus) → **new failure-class (v): two INDEPENDENT passes can converge on the SAME wrong glyph (A=B agreement ≠ proof — verify load-bearing glyphs on a fresh crop).** ★v6 onset `ወፈቀደ` ('and he sought to destroy'; BOTH misread `ፈ`→`ረ`); ★v4 carries a genuine 2nd clause `መርዶኬዎስ ፡ አይሁዳዊ ፡ ውእቱ` ('Mordecai is a Jew') that pass B UNDER-read; ★v7 (faintest) year `በ፲ወ፪` + lot `ዕፁ` cast for the **14th of Adar** (`ዓሡሩ ፡ ወረቡዑ`; LXX, not the MT's 13th); ★v8 pass A read the full last lines (controller-confirmed — NOT an over-read, (q) holds). Mordecai refuses to bow → Haman rages → resolves to destroy all the Jews → casts the lot → opens his accusation. Accumulator pages 24–34. **▶ NEXT = render p35 (banner `III, 9 — B, 1`: completes 3:8 → 3:9–13 → Addition **B** begins, the first interleaved LXX Addition since A; same method — guard MT-plus per (u) AND verify load-bearing glyphs per the new (v) even on A=B agreement).** TRACKER-STATE active (user-paced marathon — check in per page).
>
> **✅ DONE 2026-06-02 — MINT-11 COMPLETE (the whole audit arc closes here; STOP, no mint-12 until project END per [[audit_cadence]]).** Phases 0–4 shipped earlier (5 checkpoints `257bfd51`→`d7804f13`); **Phases 5 + 6 + the deep-audit engine round-5 fix shipped this session in ONE closeout commit (5-leg), byte-stability gate PASSED 213s.** **P5a** topical `sorted()`+raw-tiebreak (DEFENSIVE — merge invariant already deterministic; cross-launch test added) · **P5b** `build_cache._referenced_translations`→`translation_id` real dirs (4/5 witnesses were `<missing>`) · **P6** annotation override scoped to canon+enabled-kind via new `_count_in_scope_disabled_ref_ids` — **the plan's canon-only fix was empirically insufficient (real-data probe: raw −49,027 → canon-only −47,362 STILL negative → canon+kind +825 ✓); byte-identical for all 11 committed editions (empty disabled sets)** · **engine** `deep-audit.js runSkepticPanel` retries null votes once then carries an empty panel as UNVERIFIED (not auto-refuted) — the round-4 false-negative fixed (node-validated). lint 26✓/2⚠/0✗; mypy clean; +6 new tests. **▶ NEXT critical path = Phase D1b PO Esther vision marathon (paused p28; parked lane below).**
>
> **▶ (prior) 2026-06-02 — MINT-11 deep-audit round 4 COMPLETE → FIXING overnight (run `wf_4b250c81-348` / task `w2wxhtf4h`).** Engine `.claude/workflows/deep-audit.js` hardened: **ROUND→4 · NOW→2026-06-02 · find+verify pinned `sonnet`** (cap=2 → ~3h not ~8h; [[audit_cadence]]); synth+completeness inherit Opus (post-barrier). 15 dims; 4 deferred-by-design fed in (ex.py→exo · aes 11–16 · compresslevel 9→6 · web.py-split). Pre-launch env: sleep/hibernate=Never(0) AC+DC, ~0.55 GB RAM reclaimed, ssh key loaded, tree clean.
> **▶ PROGRESS (2026-06-02 overnight) — Phases 0,1,2,3,4 SHIPPED (5 checkpoints `257bfd51`→`d7804f13`, all 5-leg pushed):** audit = 64 deduped → 30 survived/34 refuted (NO high/crit). **P1** 6 stale tests→200 green · **P3** HIGH-#B build_cache cover-key (`cover_image_per_book`→`book_covers`) + `core/source_dates.py` in `_PIPELINE_SCRIPTS` + `is_output_current` composes `_PIPELINE_SCRIPTS`+data + coord-guard run_xref/run_kenyon · **P4** promote within-batch dedup + corpus_index fingerprint-before-swap + update_book_floors `len(None)` guard + standalone_store atomic ×3 · **P0** corpus 67,715→91,733 ×5 + tests 186/plans 29 · **P2** skip→assert + inflight active-branch. **2 empty-panel HIGHs hand-verified: #B real (FIXED), #A LATENT** (all 87 books have id_prefix/bxx). **▶ REMAINING (next session, BUILD-PATH → run the byte-stability gate): Phase 5** (matter_pages.py:870/882 `sorted()` topical.xhtml PYTHONHASHSEED determinism + cross-launch test; build_cache popup-witness 4/5→`<missing>` resolution) **+ Phase 6** (annotation-count canon-scope @ build_edition.py:3050 — **CHANGES catholic-study output**). **DEFERRED low-value:** #3b corpus parse-warn · matrix.py:308 None/[] split · Torrey 630→628 · web_sources dead-code comment · HIGH-#A warn. **Engine round-5 lesson** (in deep-audit.js verifyDim comment + findings.md): 21/95 sonnet StructuredOutput misses auto-refuted real findings → re-run null panels / pin verify Opus. Detail: `plans/2026-06-02-mint-11-fixes-plan.md` (Status) + `notes/2026-06-02-mint-11-findings.md`.
>
> **▶ ACTIVE 2026-06-01 (cont.) — mint-10 fix Phases 1–6 SHIPPED across 3 commits; only 4 opt-tier items remain → then STOP.** SSH→GitLab + CI gate green (`9ab9989c`); Phases 1(leftovers)+2+3 (`bba99df4`, byte-gate PASSED 226s); **Phases 4+5+6 (THIS commit — NO build-path change → 9 KJV byte-stable by construction).** **P4:** 8 at-scale drivers → one `append_candidates` (status-preserving dedup; kind-replace purge removed) · `_normalize_book_code` on `--books` · naves/torrey coord guard · 5 stale candidate files deleted · 31 orphaned imports cleaned. **P5:** console XSS escaping (7+ sites) · WLC ingest fail-closed on raw `<>&` · exports version-validation. **P6:** cyril ceiling→floor + all-5-rival plurality · `corpus_index` `_CONN_LOCK` · web_helpers bxx · batch_promote mark-promoted · `_canons_index` lru_cache · conftest monkeypatch drop · REPO_MAP 66 · INDEX mint-9→Shipped. ~1,200 tests green; lint 27✓/1⚠/0✗; mypy clean.
> **▶ NEXT (fresh session) = the 4 DEFERRED opt-tier items** (per `plans/2026-06-01-mint-10-fixes-plan.md`): **#11** ρ.1 `filter_html` two-stage scan (BUILD-PATH → byte gate) · **#12** `_resolve_popup_languages` per-file hoist (BUILD-PATH → byte gate) · **#13** `render_coverage` EN-lane (+ `test_render_coverage` shape) · **#6** `candidates_written` pre-dedup stat (superseded by the P4 `append_candidates` return contract — re-confirm or drop). After they ship: **mint-10 is the LAST audit round — STOP, no mint-11 until project END** ([[audit_cadence]]). Marathon core untouched throughout.
> **▶ ALSO QUEUED (user-directed, after the mint-10 opt-items):** go through the GitHub + GitLab account/repo settings via Chrome (MCP) + add missing visible files (README/CHANGELOG/LICENSE) — see memory `project_github_gitlab_account_settings`.
>
> **▶ ACTIVE 2026-06-01 — MINT-10 round-3 re-audit COMPLETE (recovered + continued) — 62→49 survivors/13 refuted (4h·13m·22l·10i); FIXES now in progress.** Last night's run `wf_ba367edc-a4a` (15 dims, 7.4h) died on terminal shutdown before any verify/synth. Root cause: 4-core N95 → Workflow cap=2, so 2 slow finders froze everything (the "5h hang"). **Recovered 46 candidate findings** (7h·26m·12l·1i, 11 dims, incl. 11 pytest failures) from the 15 done finder transcripts → `docs/superpowers/notes/2026-06-01-mint-10-recovered-candidates.json`. **Continuation `wf_4a2d0815-8c9`** (new `.claude/workflows/deep-audit-continue.js`): verify-first (barrier) → 4 missing finders (cross-module#1 + opt-build/ingest/render) → synth; pinned sonnet (cap=2 → ~3h not ~8h). Artifacts written: findings.md + audit-raw.json + indexed `plans/2026-06-01-mint-10-fixes-plan.md`. **▶ Phase-1 stale-test green-up SHIPPED** (11 test files, 579 green). **NEXT (fresh session) = Phase-1 production (`audit_log` month-rollover · `inject` whitelist) + Phases 2–6 (build-path byte-stability)** per `plans/2026-06-01-mint-10-fixes-plan.md` (phased safest-first · TDD · byte-stable · sweep-the-class · marathon-core untouched · prefer commit-time lint guards · 5-leg save per batch) → then STOP. **mint-10 = LAST audit round (no mint-11 until project END).**
>
> **▶ (prior) 2026-06-01 — MINT-9 fix batch ALL 6 PHASES SHIPPED + engine upgraded + memory synced; round-3 re-audit + setup-build are NEXT.** Round 2 (`wf_e5e7a0d6-24a`, 99 agents · ~4.67M tokens · ~3.2h): **66 deduped → 45 survivors / 21 refuted**. All non-deferred findings fixed across 6 phases; **commit `b1a39485` = P1+P2 checkpoint; THIS commit = P3–P6 + engine + memory sync.** Artifacts: `docs/superpowers/notes/2026-05-31-mint-9-{findings.md,audit-raw.json}` + INDEXED plan.
> **✅ P1 (HIGH):** #2 tradition/time filters bxx-fallback for Strategy-B books (3 sites); #3 promote.py post-splice SyntaxError LOGS + ROOT CAUSE (`py_str` escapes backslash FIRST). **✅ P2 (stale-cache/guards):** #1 build_cache hashes all 7 pipeline scripts; #7 theme css; #17 source_dates.yaml; #23/#24 bookcode lint +3 maps (→12); fixed stale `test_changelog_documents_omega20b`. **✅ P3 (build-path, byte-gate PASSED 253s):** #11 inject precedes-order (Strategy-B); #8 matter_pages `annotation_count_override` (None→KJV byte-stable); #12 `config.category_baseline_kinds` phase/AI-gated baseline. **✅ P4 (concurrency/low-sev):** #20 corpus_index `_CACHED_CONN` reset moved INSIDE rebuild lock; #10 notes_io clears compute_matrix; #37 inject label/cat-title html.escape (byte-id on clean corpus); #36 LXX+Byzantine trusted-html ingest guard; #35 sources url_override allowlist; #21/#22 cross_refs_stripped nonlocal-count. **✅ P5 (at-scale/test-debt):** #9 naves/torrey/xref/ethiopian write_queue same-run dedup (mirror kenyon); #6 share-pin→count milestone; #25 cyril pin guards ALL challengers; #38/#39 tautological tests; #18/#19 ANSI consts→core.ui/at_scale_base; + un-stuck a stale `ch_count` test (refactor-orphaned). **✅ P6 (docs/opt):** REPO_MAP counts (182 tests/27 plans/notes dir/SESSION_PLAYBOOK); render_coverage Patrologia=rendered; is_output_current watches notes/*.py. **+4 mint-9 test files (test_mint9_phase{1,3,4,5}.py); lint 27✓/1warn/0fail; mypy clean; ruff clean.**
> **⚙ ENGINE UPGRADED (`deep-audit.js`, USER ask):** 5 meta-fixes from how rounds 1-2 missed their own findings — prior-round DEFERRED_BY_DESIGN memory (refutes re-litigation), sibling-site sweep, a `tests-run` dim that runs pytest, pinned authoritative counts, args-visibility log → now **15 dims, node-validated, ROUND default→3**. **🧠 MEMORY SYNCED (USER ask):** new `feedback_fix_the_class_not_the_instance`; updated deep-audit-tool/bookcode/W-W1/bootstrap×4-plan-name/project_overview/slow-test(+flaky-lint-test note).
> **▶ RUNNING NOW (2026-06-01) — mint-10 = round-3 re-audit LAUNCHED in background:** `Workflow` run `wf_ba367edc-a4a` / task `wdhpdk0y1` (engine `.claude/workflows/deep-audit.js`, 15 dims, ROUND=3, NOW=2026-06-01, 4 deferred-by-design fed in). **Engine hardened pre-launch:** `REPO` default changed from the relative `'YHWH v2.4'` to the ABSOLUTE `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4` to remove a cwd-ambiguity risk (this session's tools resolved relative paths against the subdir, which would have made every finder read a non-existent `YHWH v2.4/YHWH v2.4/…`). On the completion notification: parse `result` (survivors/dropped/counts/fixesPlanMarkdown/completeness), write `docs/superpowers/notes/2026-06-01-mint-10-{findings.md,audit-raw.json}` + (if survivors) an INDEXED fixes plan, then fix ALL survivors (TDD, byte-stable, sweep the whole class not one site, marathon core untouched), update the systems the round teaches (engine `deep-audit.js` / RULES / memory / truth-record), and SAVE (5-leg) at each coherent batch. **⛔ USER DIRECTIVE 2026-06-01: mint-10 is the LAST audit round — do NOT re-run the audit or the convergence loop afterward. Once findings are fixed + systems updated + everything saved, STOP. No round-4, no re-audit-to-confirm.**
> **▶ NEXT (after the audit):** (1) **round-3 re-audit** with the upgraded engine → CONVERGED (zero new verified) or another fix pass; (2) **build the 4 USER-approved setup items** (auto-format PostToolUse hook · truth-gate Stop hook · `/save` + `/audit` slash commands · Layer-1 doctrine doc) — all LOCAL/login-free, honoring [[no-external-hooks]]. **DEFERRED-by-design (engine now refutes; do NOT fix early):** `ex.py`→`exo` (→τ.G; additive `_book_path` alias is the only safe early action) · `aes` ch11–16 residual · compresslevel 9→6 · the 2 perf hoists (filter_html / _resolve_popup_languages — punted as build-path-risk-vs-low-value). **Known FLAKY:** `test_lint_rules…test_two_back_to_back_runs_equivalent` (time-sensitive freshness msg; passes in isolation — see [[slow-test-files]]).
>
> **✅ RESOLVED 2026-05-31 — bootup/env-health rule made EXPLICIT (was the user-flagged session-end TODO).** RULES §0's post-triad step is now an explicit **4-point checklist** (RAM-clear · git/remote state · plugin/MCP baseline incl. the `gitkraken-hooks` WANTED exception · version-freshness as best-effort-NOT-a-hard-gate since "latest" isn't offline-verifiable). Decision was **explicit + realistically-scoped** (not a hard gate — freshness can't be gated offline). Side-find during the review: the "23 min" slow-test claim (audit + `feedback_slow_test_files` memory) was **STALE** — re-measured `test_matrix_psi35` ~87 s / `test_web_filesplit` ~45 s; corrected the comments, pyproject marker desc, memory, PLAYBOOK §4, and SESSION_STATE.
>
> **✅ MINT-8 ROUND 1 COMPLETE 2026-05-31 — the three TODO blocks below are now DONE.** Plan authored + reusable `.claude/workflows/deep-audit.js` built + RUN (106 agents · ~4.80M tokens · ~3.25 h · `wf_1b2fdc14-4fd`) + synthesized. **70 findings → 57 verified / 13 refuted** (high 6·med 20·low 26·info 5). Artifacts: `docs/superpowers/notes/2026-05-31-mint-8-findings.md` (+ raw JSON) + phased plan `docs/superpowers/plans/2026-05-31-mint-8-fixes-plan.md` (indexed). **▶ NEXT SESSION = implement the phased fixes (TDD, byte-stable, durable guards) → RE-RUN `deep-audit` until a round is CLEAN (convergence).** Original TODO detail retained below for provenance.
> **⚠ (DONE) MINT-8 AUDIT — author the PLAN before the final mint-7 close-out (user-directed 2026-05-31).** When mint-7 D + E are done, write `docs/superpowers/plans/<date>-mint-8-audit-plan.md` — **broader + DEEPER** than mint-7's 6-dimension/15-agent sweep — for NEXT session to **create + run** (not run this session). **Standing convergence process:** audit → implement fixes → **re-audit** → REPEAT until an audit returns CLEAN (nothing found after that round's fixes). mint-8 is hopefully the last for a while, but the loop runs to convergence. The plan should spec: the audit dimensions (deeper than correctness/security/code-debt/tests/docs — e.g. byte-stability, build/EPUB integrity, data-coordinate validity, concurrency/caching contracts, cross-module invariants, the marathon-core off-limits boundary), the find→adversarially-verify methodology, severity calibration, and the convergence/exit criterion.
> **→ TWO artifacts (user idea 2026-05-31): make the deep audit a REUSABLE TOOL, not hand-rolled each time.** (1) the audit PLAN (above); (2) a **reusable named Workflow** `.claude/workflows/deep-audit.js` (parameterized over scope/dimensions/depth — find→adversarially-verify, severity-calibrate, synthesize) invocable on demand via `Workflow({name:"deep-audit"})`, optionally wrapped in a slash command. NOTE what already exists (don't rebuild): STATIC audit is reusable today — `scripts/ci.py`, `ebible audit` (caches/dead-code/types/deps), `lint_rules.py` (26 checks), `/preflight`. The GAP is only the DEEP multi-agent audit (mint-N style), which `deep-audit.js` fills. Next session: author `deep-audit.js` + the mint-8 plan, run it, then the convergence loop reuses the tool each round.
> **→ OPTIMIZATION DIMENSION (user-directed 2026-05-31) — a REQUIRED audit dimension, not just bug-finding.** The audit must also re-evaluate whether the project's **task-execution plans + tools are still the most logical / optimized approach given TODAY's capabilities** (Opus 4.8 1M-context, ultracode, Workflow orchestration, parallel agents) — because several plans were designed BEFORE 4.8/ultracode (e.g. the Patrologia + Kings/Samuel **vision-transcription marathon** method with its OOM-era MAX-1-heavy-agent / tight-crop / per-step caps; the ingest → detector → candidate → promote pipelines; the build/inject + render-coverage pipelines). For EACH remaining real project task: is the current method still optimal, or is a workflow-orchestrated / 1M-context / parallel-agent approach now strictly better (faster / cheaper / higher-fidelity)? Output = confirm-it's-optimal OR a concrete better plan. **Boundary:** this targets the PROJECT'S WORK (transcription, ingest, build, etc.), NOT the meta-tooling / env / plugin setup. Fold this into the deep-audit dimensions + the mint-8 plan.
>
> **➤➤➤ 2026-05-31 (prior) — ✅ MINT-7 ARC COMPLETE (A–E) · mint-8 audit (above) was the next step, now RUN.** Mint-cleanup arc (0–6) + the mint-7 audit were done. **Phase A (★book-code BUGCLUSTER):** completed `_BOOK_CODE_ALIASES`; canonicalized TSK/NAVES/Kenyon maps; rebuilt `tsk_xrefs.json` by a deterministic LOCAL key-remap (17,716 remaps; proven canonical + lossless) → 1,525 TSK xrefs reachable + detector emits canonical `#vnote-eze-…`; 56 stale legacy candidate files deleted; +D4 (catholic `mar`→`mrk`) + bonus `render_coverage` `mar`→`mrk`; new commit-time **`bookcode_canonical` lint check** + `TestBookCodeCanonicalization`; 4-lens workflow (`wf_f17387e2-96e`) verified. **Phase C (security):** C1 `_render_sample_html` now `sanitize_html`s the note body (stored-XSS); C2 `content/auth.json` (+distribution/press_kit) gitignored; C3 Build-All `All_Editions_*.zip` added to the download allowlist (`application/zip`). Lint **26✓/1⚠/0✗**; 9 KJV byte-stable. **DEFERRED (separate human-reviewed content run):** generating + promoting the now-reachable xref candidates. **Phase B (at-scale):** B1 both AI drivers now read `ch_count` (was `"chapters"` → always 50; Psalms now iterates all 150 — the content fill-run is a SEPARATE metered run, code fix only); B2 `run_xref` write_queue now append-merges. **Phase D (code-debt):** D1 new `core/at_scale_base.py` dedups `candidate_to_dict`(×10)/`NT_BOOKS`(×4)/ANSI across drivers+prospect+detectors (guard test pins single-source `is`-identity; write_queue stays per-driver); D2 the 2 legacy `_send_json`-bypass error paths fixed; D3 `_split_web_html`→archive, 2 orphan helpers deleted, `_replace_verse_popup_translation` marked not-yet-wired, `audit_caches`→`ci.py`. **Phase E (tests/doc) DONE:** E1 `ALL_CHECKS` meta-test (`TestAllChecksMetaContract` — all 26 checks run + shape + no-fail + registry-size pin); E2 `slow` marker + tagged the 2 ~23-min files (`-m "not slow"` deselects); E3 byte-stability **determinism** gate (`tests/test_byte_stability_gate.py`, behind `slow`; verified 205 s — builds 3 multi-canon editions valid+distinct + flagship rebuild byte-deterministic; **one build = ~133 s** → all-9 + the slow-build logged for mint-8 optimization); E4 3 doc-ref fixes. **▶ mint-7 ARC COMPLETE & SYNCED (A·C·B·D·E).** **▶ NEXT (next session) = mint-8 DEEP AUDIT** — plan `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md`; BUILD + RUN the reusable `.claude/workflows/deep-audit.js` (plan §5; the TODO block above has the full dimensions + the OPTIMIZATION spec) → adversarially verify → fix → RE-AUDIT until CLEAN. mint-7 plan boxes all checked. **⚠ Save = full 5-leg to ALL sources EVERY time (`save-all.ps1`); Claude self-triggers — RULES §4 unified 2026-05-31.**
>
> **➤➤➤ PARALLEL LANE (paused) — Phase D1b: PO Esther vision marathon, PAUSED at p28** (continues canonical 1:8 `…ፈቃደ` tail → 1:9..). Method: controller renders `render_body_for_vision(doc[28], …, dpi=230, geez_top_fraction≈0.45 — re-check per page)` to OS temp → 2 blind Opus transcribers + adjudicator + calibration (per `_vision_notes` (h)–(p)); assemble 1:8 + 1:9..; Ethiopic-only codepoint-validate; write accumulator page `'28'`; commit. MAX 1 heavy vision agent; per-unit commits. Accumulator = pages 24+25+26+27. After all PO Esther units: Task-4 `est_patrologia.py` via `po_vision_store.write_po_vision_module(...)` → xref sidecar → standalone wiring (5 books, epubcheck 0/0, 9 editions byte-stable) → EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `content/translations/sources/patrologia/_vision_notes.md`. (mint-7 and this Esther lane are independent — RULES §2.5 never-single-thread; pick either to advance.)
>

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

## Background backlog (never single-thread — RULES §2.5)

> When a lane frees, auto-pick the next: the mint-7 plan phases · CAM hi-res pre-pull · base-structured re-collation · geez→kjv xref anchoring · Phase-E Clementine (1es/2es) · doc-coherence currency · test-coverage growth · Phase-D source acquisition.
