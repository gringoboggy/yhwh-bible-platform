# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **➤➤➤ 2026-05-30 (LATEST) — Tracker ACTIVE — MINT-CLEANUP PHASE 2 COMPLETE (roadmap refresh); committed + E:/F: backed up this turn. NEXT = Phase 3 (archive sweep).** Plan `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md` (0 ✓ → 1 ✓ → 2 ✓ → 3 archive sweep → 4 decommercialize → 5 gates → 6 polish; KEEP PYTHON; marathon core untouched). **Phase 2 shipped:** new `dev/PLAN_2026-05-29-roadmap.md` (deadline-free; reality-accurate; lane-structured with a Depends table column; own-vers §4 parallel-data-lane model) REPLACES the archived `PLAN_2026-05-24-end-scope.md` (`git mv`'d with `SCOPE_2026-05-14-parallel-bible.md` → `dev/archive/`; PLAN_SINGULAR). Triad plan-name synced across RULES §0 (×2) + PLAYBOOK §1/§7 + REPO_MAP + both `bootstrap-triad.ps1` (runtime + tracked) + cc-hooks README (×2); flipped `_ENFORCE_TRIAD_PLAN` + `_ENFORCE_RETIRED_TERMS` (PLAYBOOK "hard deadline"/"2026-06-07" ×2 + REPO_MAP `sonar` row cleared; REPO_MAP text-fix pulled forward from Phase 6). PLAYBOOK §7 de-staled (deadline + wrong "script-based not agents" note dropped). **Lint 20 pass / 5 warn / 0 fail (EXIT 0)**; `test_lint_guardrails` 36 green (3 WARN-asserts → state-aware per §8, `test_warns_*`→`test_flags_*`); ruff-clean; NO production code path touched. Phases 0+1 = HEAD `83862111`; the Phase-2 work is committed + E:/F: backed up this turn. **▶ RESUME (Phase 3):** `git mv` ~75 dated finished docs (AUDIT_*/CALIBRATION_*/PILOT_*/PROPOSAL_*/SESSION_END_*/SCOPE_* addenda/HANDOFF_README_v7) → `dev/archive/` + add a `dev/archive/README.md` index → re-lint (`dev_doc_sprawl` GREEN; the 20 doc-xref warns clear as the archive-aware check resolves them). Then 4 decommercialize (CHECKPOINT first) → 5 gates (remote + CI) → 6 polish. **[USER]** Kobo color e-reader = end-stage real-reader QA; Windows code-signing (SSL.com OV + eSigner) = a Phase-5/ship-time buy; Esther vision marathon paused at p28.
>
> **➤➤➤ 2026-05-29 (prior) — Tracker ACTIVE — Phase D1b Task 4: PO Esther p27 DONE (canonical 1:4-tail → 1:5–1:8; 1:8 cut after `ፈቃደ` → p28) — RECOVERED from TWO consecutive OOM crashes + a controller verification pass; accumulator page `27` written (codepoint-clean); committed + E:/F: bundle THIS turn (user remounted both drives).** Two undocumented crashes since the p26 commit `5a7556f5`, both mid-p27: `2a3624ca` (2 blind + adjudicator) then `2e57d4aa` (re-rendered crops + a finishing-adjudicator convergence workflow `wf_fe98cd7b` that COMPLETED its StructuredOutput before crashing; Verify phase never ran). Recovered the completed adjudication from the workflow agent transcript (cross-confirmed by the first attempt) + a CONTROLLER re-verification of the 2 load-bearing cruxes vs the staged crops + French — **numeral `፫ ፸`** printed (French "trente mille" divergence flagged), **v7/v8 boundary = wine opens v8**. v5 feast `በዓለ`; v6 converged per-glyph. NO heavy vision re-run (that OOM'd both prior sessions). 1:4 joined `ዕለ`+`ት`=`ዕለት` (180 days). 3 `uncertain` flags. Accumulator = pages 24+25+26+27. **▶ RESUME: p28** — render `render_body_for_vision(doc[28], out, dpi=230, geez_top_fraction≈0.45 — re-check per page)` to OS temp (controller renders; subagents Read the PNG) → 2 blind Opus transcribers + adjudicator + a calibration check (per (h)–(p)) → assemble 1:8 (concatenate its p28 head onto the p27 `…ፈቃደ` tail) + 1:9.. → Ethiopic-only codepoint-validate → write accumulator page `'28'` → commit. MAX 1 heavy agent; per-unit commits. **After all PO Esther units:** Task-4 final write `est_patrologia.py` via `po_vision_store.write_po_vision_module(slot='est_patrologia', book='est', verses=<assembled; DROP the p24/p25 A5 + p25 A13 + p26 1:4 partials, use the complete verses in their completing pages>, po_book='est', page_range=(24,65))` → `ruff format` it → Task 5 xref sidecar → Task 6 standalone wiring (build 5 books, epubcheck 0/0, 9 editions byte-stable) → Task 7 EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `_vision_notes.md`.
>

## ➤➤➤ ACTIVE — Mint-cleanup arc (2026-05-30)

> **Current task:** Phase 0 ✓ + Phase 1 ✓ + Phase 2 ✓ (roadmap refresh) DONE — **committed + E:/F: backed up this turn.** **Next = Phase 3 (archive sweep):** `git mv` ~75 dated finished docs → `dev/archive/` + an index README → `dev_doc_sprawl` GREEN (also clears the 20 doc-xref warns unmasked by archiving the old plan). Plan: `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md`.
>
> **Paused (resume after the cleanup):** the Phase-D1b Patrologia vision marathon — PO Esther at **p28** (continues canonical 1:8 tail → 1:9..). State/method in the top journal entry + `content/translations/sources/patrologia/_vision_notes.md`; plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`. MAX 1 heavy vision agent; per-unit commits; controller renders / subagents Read.
>
> **Standalone status:** Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.
