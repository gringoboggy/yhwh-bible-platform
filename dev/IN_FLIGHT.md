# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ 🔄 2026-06-11 (🖥️ Mac, turn 73 — K-KIN ROOT-CAUSE ARC: husk fix → rebuild → ONE STK confirmation).** Root cause NAMED by the local Previewer oracle (E24010 ×3 → E24001: the 3 demoted Daniel-addition title husks as TOC targets; SESSION_STATE top has the full anatomy). **TOCHUSK VERDICT IN (×2 runs): E24010/E24001 ELIMINATED — husk = blocker #1 CONFIRMED — but a GENERIC internal error remains (no E-code; layered failure, TOC masked it).** **Open, in order:** ① CHAINED PROBE — delink on top of tochusk: `.venv/bin/python dev/kindle_bisect.py --rung delink --src ~/Desktop/Ethiopian_Bible_catholic-study_kindle-safe_2026-06-11T024442Z_rung-tochusk.epub` → epubcheck → Previewer (`-convert -locale en -output ~/kp3-runs/<dir>`; ~13 min; logs `Logs/*_log.csv`): PASS ⇒ link graph = the shadowed blocker #2 (sub-bisect noterefs/asides/id-count); FAIL ⇒ rung-3 HALF-SPINE on the tochusk base → ② SHIP FIX in build_edition.py (design direction: retarget demoted-appendix TOC/nav/ncx/in-book entries to the book's first CONTENT anchor + drop/merge the husk piece — splice-residue organ #2; gate 4k) → ③ kindle-safe rebuild + all gates (4j proves orphan 1,598→0 too) + epubcheck → ④ ONE STK confirmation upload (user) → ⑤ K-R6-6 chip badges (`marker_badge_style: chip|glyph+count`, eink default via `resolve_target_reader`; emitter ~3104) → ⑥ re-run the 2 marker_style integration builds post-merge → ⑦ WIN-handed lint findings (B023 ×5 late-binding closures build_edition.py:3094/:3667–3668 — confirm + bind-via-default-arg or waive; F841 :2659 unused `total`). HOLD: K-R4-2 cap re-tune (user taps pending). NOTE: kindle builds now take `--target-reader kindle` (WIN M1 blocker #1) — no more editions.yaml flip in dev/build_kindle_safe.py paths.
>
> **▶ 🔄 2026-06-11 (🪟 Windows, turn 75 — MATRIX M1 SHIPPED; CI dispatch + catalog go-live remaining).** All 5 M1 items DONE + e2e-proven (SESSION_STATE top has the full anatomy): FORMAT_MATRIX one-home (+COVER_COLOURS+catalog_asset_name) · 18 committed composites (CI never composites in-runner) · swap_epub_cover + build_format_matrix driver + format-matrix.yml (proof: catholic-study ×2 cells, epubcheck --strict 0/0/0/0 ×2, ALL K-R2 GATES GREEN ×2) · gen_release_catalog (full-count gating, paginated, legacy-Kobo cell; proven vs the live release) · releases.html catalog band (visual QA passed; dark state deploys quiet). **★M1 CLOSED — CATALOG LIVE (same turn):** ① 5-leg sync `matrix-m1-shipped` (6702af52) ✓ → ② CI run 27364486873 dispatched (user-authorized after an auto-mode soft-deny) → **completed/success — all 9 edition jobs + fan-in green; 18 assets + SHA256SUMS on v0.1.0 (25 assets total)** → ③ catalog regenerated (everywhere+apple live ×9, sha256s resolved) + site deployed (`yhwh-site-publish` 01049a8→9f2332a) + **LIVE-VERIFIED on www.yhwhyaway.com/releases.html** (both format panels, 18 links, Kobo legacy note). **Open:** ④ next lanes — M2 colour variants (the 225-composite commit-vs-pin decision) · M3 Kobo column (K-R4-2 cap, user taps pending) · M4 Kindle column (Mac kindle_safe) · M5 Play Books (user phone-QA) · Phase D1b PO Esther (paused ~p35).
>

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

## Background backlog (never single-thread — RULES §2.5)

> When a lane frees, auto-pick the next: the mint-7 plan phases · CAM hi-res pre-pull · base-structured re-collation · geez→kjv xref anchoring · Phase-E Clementine (1es/2es) · doc-coherence currency · test-coverage growth · Phase-D source acquisition.
