# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ 🔄 2026-06-14 (🖥️ Mac, turn 82 — ★K-KIN Send-to-Kindle verification OPEN; the "RESOLVED" claim was CORRECTED to honest status).** Full from-the-beginning Kindle reconstruction done (workflow `wf_f714f284-c10`). The real goal channel is **Send-to-Kindle** (NOT KDP/Previewer — both falsified/wrong). The turn-81 "E999 RESOLVED" confirmed only KDP + the Previewer; **Send-to-Kindle delivery of the shipped `FIXED.epub` is UNCONFIRMED** (it was never STK-tested; the only STK success ever, test-2, is unrecoverable). The `apply_kindle_strip_hidden` fix is real + likely-necessary but not proven sufficient on STK. **PENDING USER (candidate #1):** upload `~/Desktop/Ethiopian Bible - Catholic Study (Kindle) FIXED.epub` via the Send-to-Kindle **WEB UPLOADER** (amazon.ca/sendtokindle). ON VERDICT — **update this record**: ✅ PASS → K-KIN truly resolved + WIN may light FORMAT_MATRIX M4; ❌ FAIL → retry on a confirmed-up STK day, then reproduce test-2's recipe from source. (The KDP "converting" run is a validity check only, not the goal verdict.) Detail: `docs/superpowers/notes/2026-06-10-kindle-e999-investigation.md` CORRECTION section + SESSION_STATE turn-82. Baton **windows** (truth_owner); mode=parallel.
>
> **▶ 🔄 2026-06-13 (🪟 Windows, turn 80 — ★ROUND-7 K-R6-2 BYTE-CAP POPUP-SPLIT SLICE SHIPPED + GROK/KILO SECOND-AGENT FULLY WIRED.** ① **Grok Build drove the byte-cap fix HEADLESS** (`grok --prompt-file … --model grok-build --effort max --check --permission-mode bypassPermissions`): `DEFAULT_NOTE_POPUP_SPLIT_BYTE_CAP` 8,000→**8,858** (proven-open floor; estimator dominance = the margin) · `_POPUP_UNIT_SHELL_BYTES` 512→**600** (full emitted `<aside>` est ≤ cap, so gate 4n never trips on a splitter-passed unit) · single-unit control verse gen-1-3→**gen-1-8** (gen 1:3 is markup-dense → legitimately splits) · test_popup_split assertions/docstrings updated. Its `--check` pass self-flagged 2 residuals (an E501 it fixed; the stale `/customize` "8000"). I reviewed the diff + **`tests/test_popup_split.py` 52/52 GREEN** (no regressions) + finished the stale-8,000→8,858 CLASS (customize UI · api/editions.py ×2 · verify_kr2_build.py comment); ruff format clean. Grok stopped after it looped on `run_terminal_command` tool-output errors post-fix (work done+recorded; child tree killed, 0 orphans). **✅ SHIPPED (this milestone):** `-s1` pin sweeps in `test_marker_style.py` + `test_note_rehaul.py` (ruff clean) → round-7 eth + catholic-study builds (r7-pin-sweep artifacts in build/round7/) + kepubify → `dev/verify_kr2_build.py` **ALL K-R2 GATES GREEN** (4m/4n) on eth .epub/.kepub + catholic r7(s) → epubcheck 0/0/0/0 on the artifacts. Remaining user action: NEW-filename device load + leg-4 retaps on the fresh r7 builds.** ② **SECOND-AGENT SETUP** (full recipe = `dev/TOOLCHAIN.md §Grok`): Grok Build CLI (`~/.grok/bin`, OAuth sub) + Kilo Code (xAI connected via bundled `kilo auth login --provider xai`; model `kilo/x-ai/grok-build-0.1`; MCP=chrome-devtools, broken git MCP dropped) + Grok Build sidebar; removed broken `erikkralj.vscode-grok`; `AGENTS.md` + `.vscode/extensions.json` tracked; SSH push verified (GitLab+GitHub). Baton **windows** (truth_owner); mode=parallel.**
>
> **▶ 🔄 2026-06-12 (🖥️ Mac, turn 78 FINAL — ★BOTH LANGCAP ORACLES FAIL ⇒ THE BYTES-ONLY MODEL IS DEAD: full-graph threshold ∈ (28.6, 40.5)MB — bytes × intact-popup-graph INTERACT · (B)+(C) in-build SHIPPED + byte-identity PASS · milestone push on user command ("bandwidth almost up").** ★VERDICTS: cap=2 Heb+Grk @ 48.7MB = FAIL (generic internal error) AND cap=1 Hebrew-only @ 40.5MB raw (MEASURED, epubcheck 0/0/0/0, full 33,969-popup graph intact) = FAIL — while the 0:39 DOC-SUBSET probe @ 45.3MB PASSED and vnotegut (full graph, 28.6MB) PASSED. ⇒ Raw bytes alone do NOT drive the KFX error: with the FULL popup/link graph the ceiling sits somewhere in (28.6, 40.5)MB; with a ~40-doc subset 45.3 clears. Single-volume kindle via language-capping alone is NOT provably shippable (zero-loss compaction tops ~47-49; cap=1 lands 40.5 = still over the full-graph ceiling). **TWO-VOLUME = the only PROVEN option** (halves P/P, full apparatus, live links). ★β RAN SAME SESSION (user GO): composed probe easton-trim (KINDTRIM rung, 3,773 items, −3.7MB) + cap-1 hebrew + compaction = 36.8MB raw, full 33,969-popup graph, epubcheck 0/0/0/0 → **FAIL (same generic error)** ⇒ FULL-GRAPH CEILING ∈ (28.6, 36.8)MB. Single-volume is now effectively CLOSED: getting under ~33MB means cutting Hebrew popups or most remaining notes — below the product's apparatus floor. **RECOMMENDATION → (α) TWO-VOLUME KINDLE** (proven P/P, 100% apparatus, live links; build leg = spine split at the proven seam + per-volume TOC/OPF/cover; the (B)+(C) cap machinery stays as a builder option, not the kindle ship lever). ★PINPOINT LADDER RUNNING (user-directed 'pinpoint as close as we can', push per verdict): rung BYTEDIAL (TDD 61/61) — per-aside child-boundary byte dial, global remainder carry, full graph constant (f=0 = the vnotegut shape; text-only dial was floored by 39.9MB markup mass). Run 1 @ 32.70MB (24.0% tail, 174,259 children dropped, epubcheck 0/0/0/0) = **PASS (Success, ET Supported)** — the first full-graph pass above vnotegut ⇒ ceiling ∈ (32.7, 36.8). Run 2 @ 34.75MB (27.7%) = **PASS** ⇒ ceiling ∈ (34.75, 36.8). Run 3 @ 35.80MB (29.6%) = **PASS** ⇒ ceiling ∈ (35.8, 36.8). Run 4 @ 36.30MB (30.5%) = **PASS** ⇒ vs the foreign anchor the window is (36.3, 36.8). Run 5 (THE FAMILY TEST, byte-matched to the easton+cap1 FAIL within 58 B) = **PASS** ⇒ ★THE CEILING IS NOT BYTES — IT IS (with high consistency) TOTAL ELEMENT COUNT. Census across every verdict artifact (`<[a-zA-Z]` opens, regenerated on disk): vnotegut 363,105 P · dial32.7 501,920 P · dial36.82 645,786 P · easton+cap1 712,219 F · cap1 742,463 F · full 1,055,054 F — ONE threshold separates ALL verdicts, bytes separate NONE (36.8MB sits on both sides); historical fits: halves ≈527k P/P, 0:39 ≈640k P, 0:49 ≈780k F. ⇒ ELEMENT CEILING ∈ (645,786, 712,219). Run 6 @ 679,614 elements (37.69MB) = **PASS** · Run 7 @ 693,700 (38.27MB) = **PASS** ⇒ element ceiling ∈ (693,700, 712,219) — 18.5k window. Run 8 @ 716,025 elements (39.51MB, ABOVE the foreign 712,219 FAIL anchor) = **PASS** ⇒ ★TOTAL-ELEMENT MODEL ALSO FALSIFIED — the boundary is MULTI-DIMENSIONAL (trim family keeps whole multi-KB Hebrew paras per popup and fails ≥712k; the within-popup-thinning dial family passes ≥716k; subsets with FULL popups pass ≤~640k and fail ≥~780k). ⚠ UNPROVEN QUADRANT: no probe has EVER passed >39.5MB — the full-apparatus ship shape is ~636k elements but ~70MB ⇒ its verdict is NOT implied. ★★RUN 9 = THE ANSWER: rung SHIPSHAPE (TDD 67/67; un-em popups + bidi-isolated FSI/PDI text-prefix labels + classed bold headers; the vn-sep-span label form caught fires-on-defect — first build left 756k elements, fixed merge lands 516,921 @ 64.7MB, epubcheck 0/0/0/0) = **PASS (Success, ET Supported, 0 errors)** ⇒ ★SINGLE-VOLUME FULL-APPARATUS KINDLE IS PROVEN at the artifact level — 100% content, zero loss, full 33,969-popup live graph, at 64.7MB (the high-byte/low-element quadrant ALSO exonerated: bytes never decide — passes span 28.6→64.7MB, fails span 36.8→74.7MB; the true driver = popup MARKUP DENSITY: word-<em>s 264,914 + label paras+spans ~239k + header strongs 33,969). FINAL LADDER (12 runs this session): dial passes 363k/501k/645k/679k/693k/716k elements · trim fails 712k/742k · shipshape PASS 516,921@64.7MB. ▶ SHIP LEG (next session, Mac): implement the 3 levers kindle-gated at the step-3 emitter (b9ae8bcc site; TDD + byte-identity off-kindle) — NOTE: levers make the kindle=2 cap default UNNECESSARY for the blocker (cap stays builder option; the ratified kindle=2 default REOPENS — user re-pick) → rebuild build_one(target_reader=kindle) → gates+epubcheck → Previewer clean → ONE STK (user). Probe artifacts: shipshape-full staged on Desktop; all 16 verdicts in ~/kp3-runs/. ★SHIP MATH (zero content loss, kindle-gated markup substitutions at the step-3 emitter shipped today): popup word-<em>s → plain text (−264,914) · label paras → text-prefix/CSS (−119,625) · header <strong> → CSS bold (−33,969) ⇒ **FULL APPARATUS = 636,546 elements** (57k under the proven floor; cap-2 shape = 453,955). ⇒ CANDIDATE ENDGAME: single-volume kindle with 100% apparatus — no cap default needed, no two-volume; cap stays a builder option (re-opens the ratified kindle=2 default — user re-pick at the seam). NEXT CODE LEG: extend the kindle emitter with the 3 levers (TDD) → rebuild → oracle → ONE STK (user). Verdicts in ~/kp3-runs/bytedial-*+elemdial-*/. Probe artifacts: rung-langcap-hebrew (40.5MB) staged on Desktop; verdicts in ~/kp3-runs/nosplit-langcap{,-hebrew}/. kindle_bisect: langcap `keep_groups` parameterized + `--cap-keep` (TDD 5 new, 51/51). ② Oracle #1 ran as the session's first action per the turn-77 plan; FAIL path executed (cap-1 measured + oracled in-session). ② The plan-② implementation went IN-BUILD while the oracle ran, all TDD: `resolve_popup_language_cap`/`resolve_popup_language_pick` (explicit `max_popup_languages` 1..4 > kindle-default 2 > uncapped; groups hebrew=wlc · greek=lxx-greek+greek-nt · latin=vulgate · arabic=arabic; pick key `popup_languages_capped`, default Heb+Grk, dedupe+raise-on-junk/over-cap), BIBLE-WIDE early-return in `_resolve_popup_languages` (tiers 1-3 bypassed under cap; **kjv rides along** when the bible-wide baseline carries English — capped catholic-study resolves exactly {wlc, lxx-greek, greek-nt} = the staged candidate), `_vnote_pass_needed` extracted + cap-aware (bare kindle edition can't skip the pass), kindle-gated compaction step 3 in `_apply_popup_languages_and_translation` (labels → `Heb`/`Grc`/`Lat`/`Ara`/`Eng` via COMPACT_SOURCE_LABELS keyed on content class — unknown classes keep full labels; header → `<Book> N:M` from book_native_names english — UPGRADES the rung's comma surgery that left 'or Ezra Sutu'el 1:1'/'translated from the Greek Septuagint 1:1' on 1,149 popups; unknown book code keeps header + headers_kept stat), stats labels_compacted/headers_compacted wired into build stats. ③ API: validator runs the build's OWN resolvers on the MERGED record (cross-field: a target flip that makes a stored pick over-cap REFUSES atomically — pinned by the test whose RED run actually leaked `target_reader: kindle` into editions.yaml, caught+reverted); ★in-passing DEFECT CLASS fixed: clone whitelist was hand-typed and dropped 19 newer fields (target_reader, marker_badge_style, …) → `EDITABLE_TEXT_FIELDS`/`EDITABLE_BOOL_FIELDS` module registry now feeds BOTH api_save and `_append_cloned_edition` (+ `popup_languages_capped` in clone list fields; CLONE_SCALAR_SPECIAL={title,cover_image}). ④ UI: /customize cap select + 4-group checkbox pick (hidden-input sync, standard dirty path) + amber BYPASS note on the per-book navigator when a cap applies; wizard TARGET_CAPS gains `max_popup_languages` (kindle 2 / others null) + honest kindle note (Heb+Grk default). Forensics note RESOLUTION updated. ⑤ ★BYTE-IDENTITY PROOF = PASS: catholic-study (everywhere) rebuilt at HEAD vs pre-change worktree (4291bfe7) — identical normalized sha256 `fd1ce649…` (URN/modified/date/rights-year scrubbed, the byte-stability-gate normalization; cache key includes build_edition.py source so both builds were REAL); worktree + /tmp proof dirs removed. Post-rebase onto WIN round-7 checkpoint (6126035b): 2 conflicts union-merged (api/editions.py — their `note_popup_split_byte_cap` joined the NEW module-level EDITABLE registry + both validator blocks kept; IN_FLIGHT both turn-78 entries kept) — **160 cross-slice tests green post-rebase** (their 3 recorded-open calibration failures untouched, WIN-owned). Desktop staging now: nosplit source + rung-langcap + rung-langcap-hebrew (8 spent probes deleted on user command; all verdicts preserved in ~/kp3-runs/*/Summary_Log.csv — imgstrip verdict was ✗ Error/ET-Not-Supported, recorded there). Board: pulled 2f552b89+4291bfe7+fb98e2e8 (WIN turn-76 wrap: Kobo popup root cause = Nickel cap, round-7 = WIN); pull rules mirrored to Mac memory (auto-pull-on-BEHIND + 'pull' command word + stash→pull→pop). STK upload: HOLD until the in-build rebuild converts clean.**
>

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

## Background backlog (never single-thread — RULES §2.5)

> When a lane frees, auto-pick the next: the mint-7 plan phases · CAM hi-res pre-pull · base-structured re-collation · geez→kjv xref anchoring · Phase-E Clementine (1es/2es) · doc-coherence currency · test-coverage growth · Phase-D source acquisition.
