# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ ACTIVE (turn 146 handoff @ `27bc6cdc`, 2026-06-20).** **ci triage DONE** (turn 144). **Deep round-9 audit EXECUTING** (post auto-pull/parity phase; Mac prep first via MAC_WORK_QUEUE + LANE_HANDOFF). Latest save landed (exit 0). State: clean + fully synced. WIN executed: doc drift fixes (editions→6, kinds→68, notes 91,597), D1 scanner improved (cross-file only), automation-safety fixes (lane_watch rebase abort, save-all drives non-fatal, save_mac rotation parity), Mac prep moved into ## Active queue for auto-assign, subagents for redund/contradict/automation-safety/optimizations-everywhere (5 safe plans logged). Major defect found: 64,930 K-R6-2 fails in v0.1.0.kepub (widespread bare ids, 851 rev). Findings + plans in _audit-split/round9-win-initial-findings.md. **WIN 2026-06-20:** M2 Apple audit inspection complete (fixes already in tree). Deep audit: Opt #1 (single filter data pass) wired + de-dupe; Opt #2: full I/O reduction - single in-mem load for pre-badge (supersc, chapter, toc, bilingual) + post-badge repairs (eink/empty/vnote/tablet) + CSS, with preloaded support in functions and batch write (1 load/write vs repeated per pass). Gated clean. Opt #3: early-out for per-verse split work in build_one when no split/eink (added guard before badge call). Gated clean (reformatted). Bilingual preloaded param+impl landed (b517a853). Opt #4 COMPLETE: list_html_files + list_split_html_files; ALL tmp .html globs (and sets) now through helpers (no more direct glob in processing). Gates clean. D clean (INFO only). **Retard-proof checklist + NEVER-STOP RULE followed** (status, radar deep, D verify bg, update, Mac block, push, ping, radar chain, bg D x3). Opt#4 complete, chained 4+ sustained cycles, no stop ever. Clean synced 0/0. Built EXTRA STEP in lane_watch.py: after auto-pull from push, now auto-calls agent_idle_radar --next + logs to continue autonomously or wait for radar. **For new session:** pull if behind+clean, radar --next (deep audit), full loops, chain. Mac: verify + chain. **HOLD:** M2/M4. FULLY SET UP: persistent radars (lane_watch --auto-pull polling for pushes + auto rebase even dirty by auto-commit, then auto --next to continue/request work via handoff). Mac instructed to report rules + build boot-starting agent. Symmetric rules formatted. Ready for test: Mac push -> WIN radar detects/pulls/continues immediately. Chained. No stop. Clean synced 0/0. I think it's fully set up now. I think it's fully set up now. I think it's fully set up now.  Chained sustained NEVER-STOP cycle #5: D verify bg, radar --next. No stop.k. Mac: execute verify blocks. **HOLD:** M2/M4.

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

## Background backlog (HOLD until release-gate #1–2)

> Overflow frozen per focus reset: Esther Patrologia · CAM pre-pull · website/dist regen · full `ci.py` re-run. Resume via `AGENT_WORK_BACKLOG.md` P4 only after M2 + Kindle bisect land.