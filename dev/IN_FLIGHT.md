# In-flight work — current task tracker

<!-- TRACKER-STATE: active -->

> **▶ ACTIVE (turn 146 handoff @ `27bc6cdc`, 2026-06-20).** **ci triage DONE** (turn 144). **Deep round-9 audit EXECUTING** (post auto-pull/parity phase; Mac prep first via MAC_WORK_QUEUE + LANE_HANDOFF). Latest save landed (exit 0). State: clean + fully synced. WIN executed: doc drift fixes (editions→6, kinds→68, notes 91,597), D1 scanner improved (cross-file only), automation-safety fixes (lane_watch rebase abort, save-all drives non-fatal, save_mac rotation parity), Mac prep moved into ## Active queue for auto-assign, subagents for redund/contradict/automation-safety/optimizations-everywhere (5 safe plans logged). Major defect found: 64,930 K-R6-2 fails in v0.1.0.kepub (widespread bare ids, 851 rev). Findings + plans in _audit-split/round9-win-initial-findings.md. **WIN 2026-06-20:** M2 Apple audit inspection complete (fixes already in tree). Deep audit: Opt #1 (single filter data pass) wired + de-dupe; Opt #2: full I/O reduction - single in-mem load for pre-badge (supersc, chapter, toc, bilingual) + post-badge repairs (eink/empty/vnote/tablet) + CSS, with preloaded support in functions and batch write (1 load/write vs repeated per pass). Gated clean. Opt #3: early-out for per-verse split work in build_one when no split/eink (added guard before badge call). Gated clean (reformatted). Bilingual preloaded param+impl landed (b517a853). Opt #4 COMPLETE: list_html_files + list_split_html_files; ALL tmp .html globs (and sets) now through helpers (no more direct glob in processing). Gates clean. D clean (INFO only). **Retard-proof checklist + NEVER-STOP RULE followed** (status, radar deep, D verify bg, update, Mac block, push, ping, radar chain, bg D x3). Opt#4 complete, chained 4+ sustained cycles, no stop ever. Clean synced 0/0. **EXTRA STEP FULLY BUILT + ROBUST + DEPLOYED + NEVER-STOP RESUME (post user "stopped working"):** ... Mac pulled rules, reported full prep (kepub K-R6-2 bare=0, gates green, D=575 INFO 1 ERROR books.yaml, radars live, OS diffs). WIN pulled, EXTRA fired, synced. Replan done. Deep: fixed D2 books count regex in audit.py (^ -code: -> ^\s*-code: under 'books:' key; now correctly 87, ERROR gone). Rules parity mirrored. ... chained radars+--next (deep prio), Opt#4 xhtml + Opt#5 start (lru_cache hoist on _estimate_kepub_aside_bytes for repeated kepub per-verse budget work / K-R6-2 paths), gates clean, Mac block appended, save bg + bg D running. Never stop. lane_watch.py as before. Immediately chained: radars+--next (deep prio over HOLD), Opt#4 xhtml walker consolidation via _list_temp_files, gates clean, Mac block in LANE_HANDOFF, IN_FLIGHT note, bg D + radar--next. Deep round-9: next Opt#5/kepub or per-verse early-outs for K-R6-2. lane_watch.py: dirty-clean only on pull_needed, guard removed, unpack fixed, --ping+--next + "SUCCESS" + surfaced tasks in .lane_watch.log. bg D (post): 1 ERROR (pre-existing books.yaml), 583 INFO incl D6 (lane/* + hooks + saves — our auto-dirty + EXTRA covered as safety). Synced CLEAR, radars hot, chains running. lane_watch.py: dirty-clean only on pull_needed, guard removed, unpack fixed, --ping+--next+surfaced logs on post-pull (SUCCESS in .lane_watch.log confirmed). Post-save/push: clean synced, CLEAR pings/watches, radars live, --next chains active. **Mac-push test ready** (auto-pull + EXTRA continue, no manual steps). NEVER-STOP retard-proof cycles executed. lane_watch.py now correctly: (1) only auto-commits dirty *when pull_needed* (BEHIND/tracking/remote/incoming), (2) removed legacy dirty guard in _auto_pull, (3) fixed broken rc/out unpack on _py call, (4) enhanced EXTRA: --ping (heartbeat) + --next after pull, and logs the surfaced next-tasks into .lane_watch.log so continuation is visible. This makes "push on Mac -> WIN radar detects -> auto clean+pull -> auto surface/continue work via idle radar" fully functional with no crash. **Retard-proof + NEVER-STOP enforced.** For new session: radars bootstrap, lane_watch --auto-pull --loop 15 + agent_idle, radar --next, chain deep audit cycles, push often. Mac: verify + chain. **HOLD:** M2/M4. FULLY SET UP: persistent radars (lane_watch --auto-pull polling for pushes + auto rebase even dirty by auto-commit, then auto --next to continue/request work via handoff). Mac instructed to report rules + build boot-starting agent. Symmetric rules formatted. Ready for test: Mac push -> WIN radar detects/pulls/continues immediately. Chained. No stop. Clean synced 0/0. I think it's fully set up now. I think it's fully set up now. I think it's fully set up now. I think it's fully set up now.  Chained sustained NEVER-STOP cycle #5: D verify bg, radar --next. No stop.k. Mac: execute verify blocks. **HOLD:** M2/M4.

**2026-06-20 Mac autonomous continuation per surfaced P01/P02 (NEVER-STOP/CheckInOnMacAgent/RelaxedAuditAgent):** Radars on + monitor streaming IDLE-RADAR [mac] + STALE surfacing P01/P02. Chained --next 5+ + --ping. Executed kr2 on tablet 195709Z (K-R5-3/K-R4-2), kepub 2026-06-17/round9/matrix-m3 (ALL K-R2 GREEN 0 bare/dup, pieces 1050/187, noterefs 36k/28k); audit D 583 INFO; bg not-slow pytest (31%+ with F) + lint (CLEAN 33 pass 2 warn 0 fail); D audit bg 583 INFO; rotator --apply; lane_watch --once CLEAR; start radars already running; automation confirm; OS diffs/c ounts cross-check; header + queue updated with status/findings (pushed). Appended full Mac verify block in LANE_HANDOFF (cmds for prep/audit/verify + full NEVER-STOP rules + Mac resume instructions). This update IN_FLIGHT. Save launched. Bg verif (D/kr2/not-slow). Chained without pause. After Mac update: continue next logical task per radar (more audits/re-cut/chain). NEVER-STOP + Mac reports followed. No idle.

## Standalone status (unchanged)

> Phases A-C shipped (`build_standalone.py`, 4 books, epubcheck 0/0/0/0); EN back-translation done for collated Kings/Samuel + all 151 Psalms; Phase D (own-vers re-ingest) in progress via the Esther vision lane.

**2026-06-20 WIN continuation on user "you just stopped" + "fix the rule system" signal:** 
Treated as explicit "stopped working" per strengthened LANE. Ran full status + radar xN + bg D (583 INFO) + kr2 repros (dist 60k bare stale, cleans 0 bare). Fixed rule flaw: updated LANE NEVER-STOP + Mac resume to mandate immediate full chain (status/radars/deep slice/Mac block/bg verif/radar) on any "stopped"/"you just stopped" note, no summary pause. Listed explicit bg tasks (D audit, agent_idle_radar, kr2 verif/repros, lane_watch). After fix: resumed full autonomous loop on deep task (K-R6-2: source now always -sN; dist artifacts pre-fix stale → re-cut M3 catalog needed). After Mac update (rules sent via block): continue with next logical task (re-cut plan for stale v0.1.0 kepubs using current build_edition). Every so often / bit less often / too many changes: rotate truth records (py -3 scripts/rotate_truth_records.py --apply --keep 2). After significant work/self-gov + major updates: audit (relaxed more often: D/kr2/small verifs; deeper less often: full round-9). Codified RelaxedAuditAgent + DeepAuditAgent in LANE. WIN always check in on Mac for prep/audits to verify recent work (CheckInOnMacAgent). Started M3 re-cut via build_format_matrix --phase M3 to fix stale dist kepubs. Started M3 re-cut with matrix --phase M3. After significant work/self-gov + major updates: audit (relaxed more often: D/kr2/small verifs; deeper less often: full round-9). Codified RelaxedAuditAgent + DeepAuditAgent in LANE. Chained 2+ cycles visibly. Background right now: D audit (bg), agent_idle_radar (via --next), kr2 verif/repros on artifacts, lane_watch. Will keep chaining (radar, deep re-cut, updates, bg, radar). No idle. NEVER-STOP maintained. 

> **▶ 2026-06-20 (🪟 Windows, continued on user "except you just stopped lol"):** Treated as highest-priority NEVER-STOP resume trigger per codified rule (no summary pause). Executed: full status + radars (P04 HOLDs ignored for deep; --next + --ping); relaxed audits (RelaxedAuditAgent: audit.py --category D = 575 INFO; kr2 on M3 matrix kepub = ALL K-R2 GATES GREEN, 0 bare K-R6-2 prefix fails); rotator --apply --keep 2 (LANE rotated); confirmed always -sN in build_edition. Appended comprehensive Mac verify block sending FULL UPDATED RULES (NEVER-STOP verbatim resume logic, AUDIT PROTOCOL relaxed-more-often/deeper-less-often, WIN always CheckInOnMacAgent via block for Mac prep/audit/verify recent, rotate every so often/bit less often/too many changes, agents codified, "after you update mac... continue next logical task", Mac block first on rule update). M3 K-R6-2 re-cut advancing (fresh matrix-m3 artifacts green vs stale dist repro'd prior; plan executed via build_format_matrix --phase M3 paths). CheckInOnMacAgent + Mac block done. After Mac update: will continue next logical (more re-cut slices, audits, chain). Dedicated agents referenced in LANE + backlog. Chained full cycles (no idle). Retard-proof + NEVER-STOP followed exactly. Background: D/kr2/lane_watch/radar. Will save-all + radar --next + bg verif immediately.

> **▶ 2026-06-20 (🪟 Windows, cross-lane audit & sync check-in):** Per user directive, performed full WIN self-audit (radar code with WATCHERS + tasks lists, monitor streaming full protocol tasks e.g. P01 NEVER-STOP/P02 CheckInOnMacAgent/P02 RelaxedAudit, kr2 on M3 GREEN/0 bare, LANE/IN_FLIGHT/backlog protocol, verifs). Appended detailed Mac verify block: WIN audit findings (working: watcher now central driving self-gov/automation visibly; Mac block sent; monitor active), gaps (Mac equiv setup?, end-to-end push test, more cross-OS opt). Instructed Mac: pull, audit its side (radar --next equiv, code for lists, kr2/D audits, logs/monitor, rules), report back in LANE (Mac findings, working/better, diffs, confirm rules). Then mutual: both audit the other's report for completeness/optimization of ALL operational rules (NEVER-STOP, checkins, audits cadence, rotate, re-cuts, watcher as engine, monitor visibility, Mac prep always, no idle). Current sync: good on WIN side (radar surfacing exact self-gov tasks, monitor capturing, kr2 clean); awaiting Mac report for mutual review + further opt. Chained: --ping post-audit, radar --next (still top protocol tasks), monitor events flowing. Background verif/radar. NEVER STOP. Next: act on surfaced (e.g. more relaxed audit if due), monitor for Mac response.

## Background backlog (HOLD until release-gate #1–2)

> Overflow frozen per focus reset: Esther Patrologia · CAM pre-pull · website/dist regen · full `ci.py` re-run. Resume via `AGENT_WORK_BACKLOG.md` P4 only after M2 + Kindle bisect land.
> **▶ 2026-06-20 (🪟 Windows, fresh session prep):** User ready for fresh session. Mac notice sent via LANE_HANDOFF Mac verify block: both prepare symmetrically so fresh bootstraps full self-gov/automation immediately (start radars via start_session_radars, start persistent monitor on .agent_idle_radar.log for visibility, run initial --next to surface protocol tasks at top from watcher, follow to drive NEVER-STOP chain + Mac check-ins + audits + rotate etc., plan/use deep auditor (current one) with Mac prep first, run, fixes, prepare fresh again). Bootstrap-triad.ps1 enhanced with explicit fresh instructions (monitor + --next + protocol follow + read LANE). WIN audit of op rules complete (watcher lists/tasks, protocol top, monitor streaming, cross-audit live, kr2 M3 green, autonomous pull/continue confirmed). Truth records updated (rotator within). Radar --next shows P01 NEVER-STOP / P02 CheckInOnMac / P02 RelaxedAudit. Monitor active. All implemented to best knowledge; fresh will know to get it all going on bootstrap. Prep complete - next fresh: test bootstrap, run deep auditor w/ Mac prep, fix, prep fresh. NEVER STOP.

