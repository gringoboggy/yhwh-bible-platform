# C-3 Round 3 — adversarial GG review for 1 Kings 4

**Reviewer:** fresh isolated subagent, no prior context. No access to CAM/KJV/standard-Ethiopic/web.
**Target artifact:** `content/manuscript/kings/calibration/1ki4_witnessGG.json` (post fix-round-2).
**Parchment:** GG-00106 f030r-R + f030v-L,M.
**Method:** column-strip + token-strip crops at 6x-18x LANCZOS upscale with autocontrast cutoff=2, written to a tmp scratch directory under `%LOCALAPPDATA%\Temp\yhwh_1ki4_R3\`. Same-scribe glyph calibration used a known-`ስ` reference pair (v23 `ስስሎምን` doubled `ስ`-foot pattern) and a known-`ሰ` reference (v23 `ሰረገስተ` opening `ሰ` with bare right vertical) at the same row of the manuscript. Same-scribe calibration of `ይ` was anchored on v22 `ይሁዳ` (R2-confirmed `ይ`-form, Y-triangle topology with foot at right base) versus the asymmetric-D-with-foot `ደ` shape (v18 `እክከ ደወስ` second word, where `ደ` is unambiguous).
**Verdict (advance summary):** **NEEDS_FIX (round-3)**. Round-3 finds R2 OVER-CORRECTED the `ይ`/`ደ` axis (R2 reverted ≥4 `ይ`→`ደ` instances that the parchment ACTUALLY inks with `ይ`) and OVER-CORRECTED the `እለ`/`እስ` axis (R2 reverted ≥3 `እለ`→`እስ` instances that the parchment actually inks with `እለ`). R2 also incorrectly DROPPED the `ጻ` in v7 `ይትመጻውሙ` that fix-round-1 had kept (the `ጻ` IS on the parchment, in the line-2-of-v7 token before the cross-line break). The v22 `ወዶዕ ሙ` reading is also wrong: parchment inks `ወደኖሙ` (one word, `ደ-ኖ`). On the positive side, R2's reverts of v17 `ሮዓ ባሕር`/`ብዝናሙ` and v18 `ፈስግ`/`ወተቀንደ` and v6 `ካፋን` and v26 `አርዝ` ARE confirmed by R3 at high zoom — R2's verdicts on those loci hold. So we are in the canonical Round-3 over-correction pattern: R2 swung the pendulum too far from R1's harmonizations, and now an opposite-direction-correction list is needed for fix-round-3.

The catastrophic worst-class failure (mid-word column-boundary token drop) remains NOT present. METHOD NOTE 2 boundary at `ወአል | ሕምት` confirmed intact. So this is not BLOCKED.

---

## Round-2 reverts — independent verification

For each of the 24 reverts the fix-round-2 applied per R2's spec, R3's verdict at 8-18x zoom:

### `ለ`/`ስ` family reverts (7)

| Token | Current JSON | R3 verdict | Evidence |
|---|---|---|---|
| v18 `እምፈስግ` | `ፈስግ` (R2 revert) | **CONFIRMED-INK** | f030v_L_v18_fasag.png at 14x — 2nd char shows two-vertical `ስ`-pair with foot at right-bottom, matches the v23 `ስስሎምን` reference pattern. R2 right. |
| v21 `ፈስግ` (first) | `ፈስግ` (R2 revert) | **CONFIRMED-INK** (R2 right by inference from v18 same-scribe consistency, this token not separately re-zoomed at 16x+). |
| v21 `ፈስግ` (second) | `ፈስግ` (R2 revert) | **CONFIRMED-INK** (same as above). |
| v21 `እስ ይጻወልዎ` | `እስ` (R2 revert) | **AMBIGUOUS-PARCHMENT** — the f030v_M_v22_wees.png crop at 14x (which also covers end of v21 across line break) shows the contested char with two-vertical topology + foot, consistent with `ስ`. R2 plausibly right. |
| v22 `ወእስ` | `ወእስ` (R2 revert) | **CONFIRMED-INK** — f030v_M_v22_wees.png at 14x, 3rd char of `ወእስ` has clear two-vertical `ስ`-pair with foot (matches v23 `ስስሎምን` reference). R2 right. |
| v23 `እስ ይስሕቡ` | `እስ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_M_v23_es.png at 18x clearly shows the contested 2nd character as a curve-top descending to single stroke (`ለ` topology), NOT a two-vertical `ስ`-pair. **R2 was WRONG. Revert to `እለ ይስሕቡ`.** |
| v25 `እስ ዋዉደ` | `እስ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_M_v25_es_max.png at 18x clearly shows the contested 2nd character as a curve-stroke `ለ` topology. **R2 was WRONG. Revert to `እለ ዋዉደ`.** |
| v28 `እስ ሰምዑ` | `እስ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_M_v28.png at 8x line 2 clearly shows `እለ` (curve-stroke topology) before `ሰምዑ`. **R2 was WRONG. Revert to `እለ ሰምዑ`.** |

**`ለ`/`ስ` reverts: 4 CONFIRMED, 3 R1-WAS-RIGHT, 1 AMBIGUOUS.**

### `ይ`/`ደ` family reverts (7)

| Token | Current JSON | R3 verdict | Evidence |
|---|---|---|---|
| v14 `ደሁዳ` | `ደሁዳ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_L_v14_yhuda_max.png at 16x clearly shows first char of `?ሁዳ` as Y-triangle topology with stem descending to a foot at right base — that is the canonical `ይ` shape, MATCHING the v22 `ይሁዳ` reference. NOT the asymmetric-D `ደ`. **R2 was WRONG. Revert to `ይሁዳ`.** |
| v15 `ወደሰርዑ` | `ወደሰርዑ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_L_v15.png at 6x line 3 shows the contested char as the SAME Y-triangle `ይ` topology as v14, v22. **R2 was WRONG. Revert to `ወይሰርዑ`.** |
| v17 `ደሁዳ` (first) | `ደሁዳ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — f030v_L_v16_v17.png at 6x line 3 shows `ይሁዳ` with the Y-triangle `ይ`. **R2 was WRONG. Revert to `ይሁዳ`.** |
| v17 `ደበልዑ` | `ደበልዑ` (R2 revert) | **R1-WAS-ACTUALLY-RIGHT** — same crop, line 5 shows `ይበል` starting with the Y-triangle `ይ` (continuing to next line as `...ዑ`). **R2 was WRONG. Revert to `ይበልዑ`.** |
| v18 `ወተቀንደ` | `ወተቀንደ` (R2 revert) | **CONFIRMED-INK** — f030v_L_v18_qende.png at 12x line 4 shows the inked verb has 5 fidels `ወ-ተ-ቀ-ን-ደ` ending in unmistakable `ደ` (asymmetric D with foot), NOT the 7-fidel R1 form `ወይትቀንዩ`. R2 right. |
| v18 `ህደወቱ` | `ህደወቱ` (R2 revert) | **PARTIALLY-CONFIRMED-INK / MINOR-FIRST-CHAR** — the word spans the line break: f030v_L line 4 ends `ሕ`, line 5 begins `ደወቱ`. Combined parchment reading is `ሕደወቱ` (with 6th-order `ሕ` having two-bar topology, not 1st-order `ህ`). Second char IS `ደ` (R2's main claim verified). MINOR: JSON has `ህደወቱ` but parchment is `ሕደወቱ` — first-char vowel-mark mismatch. |
| v19 `ዘደትገበር` | `ዘደትገበር` (R2 revert) | **AMBIGUOUS-PARCHMENT** (not re-zoomed separately; the cross-column area for v19 was verified for `ወአልሕምት` join, not for this token). HONEST flag — R2's reading not refuted by my evidence but not independently confirmed at 16x+. |

**`ይ`/`ደ` reverts: 1 CONFIRMED, 4 R1-WAS-RIGHT (catastrophic over-correction class), 1 PARTIALLY-CONFIRMED with minor first-char issue, 1 AMBIGUOUS.**

### Other smoothing reverts (6)

| Token | Current JSON | R3 verdict | Evidence |
|---|---|---|---|
| v17 `ሮዓ ባሕር` | `ሮዓ` (R2 revert) | **CONFIRMED-INK** — f030v_L_v17_roa.png at 8x line 2 clearly inks `ሮዓ` with `ሮ` open-circle topology + `ዓ` 4th-order ʕa. NOT `ኆጻ`. R2 right; R1's `ኆጻ` was a smoothing-fabrication. |
| v17 `ብዝናሙ` | `ብዝናሙ` (R2 revert) | **CONFIRMED-INK** — same crop line 2, 3rd char clearly `ና` (4th-order n with right-pointing horizontal stroke), NOT `ኆ` (6th-order labialised h). R2 right. |
| v21 `ጋዘ` | `ጋዘ` (R2 revert) | **AMBIGUOUS-PARCHMENT** — not re-zoomed at 16x+ for this exact token; not refuted by my evidence. HONEST flag. |
| v25 `ወበሕ ስም` | `ወበሕ ስም` (R2 revert) | **AMBIGUOUS-PARCHMENT** — not re-zoomed at 16x+ for this exact token. HONEST flag. |
| v28 `ይከምዑ` | `ይከምዑ` (R2 revert) | **AMBIGUOUS-PARCHMENT** — verb-form not separately verified at 16x+, but the leading `ይ` (matching v17 `ይበል`) is confirmed. R2's `ከ` not refuted. |
| v28 `ወይነሥኤ` | `ወይነሥኤ` (R2 revert) | **CONFIRMED-INK** — f030v_M_v28.png at 8x line 1 shows `ወይነሥኤ` with clear Y-triangle `ይ` and final 5th-order `ኤ` with right-side forward-leaning mark. R2 right (and R1's `እ` and fix-round-1's `ኡ` were both wrong). |

**Other reverts: 3 CONFIRMED, 3 AMBIGUOUS-PARCHMENT.**

### Numeral reverts (4)

| Token | Current JSON | R3 verdict | Evidence |
|---|---|---|---|
| v23 `ቯ ቯ ኤፍራስ` | `ቯ ቯ` (R2 revert) | **AMBIGUOUS-PARCHMENT leaning `ሺ`-shape** — f030v_M_v23_redglyphs.png at 16x shows the red glyphs as vertical stem with a leftward HORN/HOOK at the top, with 2-3 small horizontal crossings near the top, and a small foot at the base. This shape is more consistent with the **`ሺ` (sha/thousand-multiplier suffix)** topology than with the **`ቯ` (qwā)** fidel. However, without a same-scribe known-`ቯ` reference glyph elsewhere in this manuscript, I cannot definitively rule out the fidel reading. HONEST-FLAG. **Lean: R1's `ሺ`/digit decomposition may have been closer to the inked form than R2's revert.** |
| v23 `ቯ ዐ፪ ኤፍራስ` | `ቯ ዐ፪` (R2 revert) | **AMBIGUOUS-PARCHMENT leaning `ሺ`-shape** — same red-glyph pattern as preceding. HONEST-FLAG. The `ዐ፪` middle is also unusual; `፲ወ፪` (12) would make more canonical sense and the R1 reading was likely closer. |
| v26 `ቯ ፫ ኤምሳለ` | `ቯ ፫` (R2 revert) | **AMBIGUOUS-PARCHMENT leaning `ሺ`-shape** — same red-glyph pattern (line 2 of f030v_M_v26_arz.png crop). |
| v26 `ቯ ፪ ወ፮ መጾል ያ` | `ቯ ፪ ወ፮` (R2 revert) | **AMBIGUOUS-PARCHMENT leaning `ሺ`-shape** — same pattern. |

**Numeral reverts: 0 CONFIRMED, 4 AMBIGUOUS-PARCHMENT (with my R3 verdict leaning that R1's `ሺ` interpretation was probably closer to the inked form than R2's `ቯ` revert). This is a HONEST resolution-limited area; both readings are defensible without a known-form reference.**

---

## Round-2 new applies (3) — verification

| Token | R2 fix | R3 verdict |
|---|---|---|
| v6 `ካፋን` (Eliab's patronymic; was `ኒፋን` in impl, `ኒፋን` after R1-which-didn't-touch-it) | `ካፋን` | **CONFIRMED-INK** — f030r_R_v6_fan.png at 18x clearly inks `ካፋን`. The first char is `ካ` (1st-order k with right-turning top hook), NOT `ኒ` (which has tail-down-right topology). R2's new-defect catch is verified. |
| v7 drop `ጻ` in `ይትመጻውሙ` → `ይትመውሙ` | drop `ጻ` | **R2 WAS WRONG — `ጻ` IS ON THE PARCHMENT.** f030r_R_v7.png at 8x line 2 unambiguously shows `እለ ይትመጻ` at the line-end, with the line-1-of-v7-tail `ጻ` glyph (4th-order ts with characteristic curl-and-cross topology) clearly inked between `ይትመ` and the line break. Line 3 then begins `ው ሙ ግብሮሙ`. Combined word = **`ይትመጻውሙ`** (6 fidels), the ORIGINAL implementer was right; the disciplined non-change in fix-round-1 was correct; R2's overrule was a fabrication-by-omission. **Revert: restore the `ጻ`** so v7 reads `ይትመጻውሙ`. |
| v26 `አርዝ` (was `ኤርዝ` in impl, kept by fix-round-1) | `አርዝ` | **CONFIRMED-INK** — f030v_M_v26_arz.png at 10x line 2 inks `እምዕፀ አርዝ ዘውስት ሊባኖስ` with clear 1st-order `አ` (rectangular alef-form, not 5th-order `ኤ` with diagonal forward-leaning mark). R2's overrule is verified. |

**New applies: 2 CONFIRMED, 1 R2-WAS-WRONG (the `ጻ` is on the parchment, not a fabrication).**

---

## v18 restoration of `እክከ ደወስ` — verification of exact inked form

f030v_L_v18_fasag.png at 14x shows between the `❈` cross (after `ኤሱፍሊ`) and `ግብጽ` (on the next line) the inked tokens:
- First token: 3 fidels `እ-ክ-ከ` (alef-family + k + kə). Spacing makes this a discrete token before the next.
- Second token: 3 fidels `ደ-ወ-ስ` (asymmetric-D + waw + s-pair with foot).
- Combined interlude: `እክከ ደወስ` — **CONFIRMED-INK.**

The R2 restoration is verified. The fix-round-1 drop (when it harmonized toward `ሰብአ ግብጽ`) was a CARDINAL-RULE violation correctly reverted.

---

## v22 re-read — verification

f030v_M_v22_v23.png at 10x and f030v_M_v22_full.png at 12x cover this region:

- Line 1 (in this crop's row 1): `እስ ይጻወልዎ ❈ ወእስ ባውዱ ወእን` — end of v21 + start of v22.
- Line 2: `ብሩ ይሁዳ ወእስራኤል ❈ ኹሎሙ ተ` — note `ይሁዳ` (Y-triangle ይ as expected), `ኹሎሙ` (with `ሎ` 6th-order o-mark), then `ተ` at line-end.
- Line 3: `አምነዊ ታሕተ ወደኖሙ ወበስስ` (or `እምነዊ ታሕተ ወደኖሙ ወበስ`) — the first char of line 3 is ambiguous between `አ` (1st-order alef, joining with line-2's `ተ` to make `ተአምነዊ`) and `እ` (6th-order alef, starting a new word `እምነዊ`). At 16x (f030v_M_v22_line3_start.png) the first char looks more like `እ` (with the small mark on the right), but with `ተ`-at-end-of-line-2 the `ተአምነዊ` reading is also defensible.
- Line 4: `ሙ ይበልዑ ወይስትዩ እምነ እስከ` — confirms `ይበልዑ` here in v22 (the `ይ` is unambiguous).

**Critical finding on v22:** the JSON has `...ኹሎሙ ተአምነዊ ታሕተ ወዶዕ ሙ ወበስሙ ይበልዑ...` but the parchment ACTUALLY inks `...ኹሎሙ ተ | አምነዊ ታሕተ ወደኖሙ ወበስ | ሙ ይበልዑ...`. The token **`ወዶዕ ሙ` (2 tokens, JSON) is WRONG — parchment inks `ወደኖሙ` (1 token, with `ደ-ኖ` middle)**. f030v_M_v22_wedono.png at 12x is unambiguous on this: `ወ-ደ-ኖ-ሙ` with asymmetric-D `ደ` + 6th-order labialised `ኖ` + m-with-u-mark `ሙ`.

The `ኹሎሙ ተአምነዊ` re-read by R2 is plausible (Y-triangle reading of nothing in this word) but the segmentation `ኹሎሙ ተ | አምነዊ` requires reading line-2-end `ተ` as the prefix of `ተአምነዊ`. The `እምነዊ` alternative reading (start-of-line `እ`) is also defensible. AMBIGUOUS at the segmentation level.

**Recommended fix for v22:** change `ወዶዕ ሙ` (2 tokens) to `ወደኖሙ` (1 token). The `ኹሎሙ ተአምነዊ` cluster stays as-is (HONEST flag for segmentation ambiguity).

---

## Kept disciplined non-changes (7) — re-verification

| # | Token / claim | R3 verdict |
|---|---|---|
| 1 | v14 `በይሰክር` (impl kept) | **CONFIRMED** — f030v_L_v15.png line 1 inks `❈ በይሰክር ❈` with the Y-triangle `ይ`. The scribe genuinely keeps `ይ` in this token. R2 right. |
| 2 | v15 `ዘአዘዝሙ` (impl kept) | **CONFIRMED** — f030v_L_v15.png line 3 inks `ዘአዘዝሙ ወይሰርዑ` with clear `አ` between the two `ዘ` glyphs. R2 right. |
| 3 | v16 `ሀስው` (impl kept) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+ in this R3 pass. Not refuted by my evidence; R2's prior confirmation accepted by inheritance. HONEST flag. |
| 4 | v21 `እምራፉ` (impl kept with `ፉ`) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+ in this R3 pass. Accepted by inheritance from R2. |
| 5 | v25 `እውናን` (impl kept) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+. Accepted by inheritance from R2. |
| 6 | v25 `ወደራል`/`ደቂት ሰግድ` (impl kept) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+. Accepted by inheritance from R2. |
| 7 | v4 `ዮዳሐ` (impl kept) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+ in this R3 pass. Accepted by inheritance from R2. |

**Kept-disciplined-non-changes: 2 explicitly re-confirmed by R3, 5 carried-by-inheritance under HONEST flag.**

---

## AMBIGUOUS-PARCHMENT items — R3 verdict at high zoom

| Locus | R3 verdict at 16x+ |
|---|---|
| `ሰ`/`ስ` distinction (chapter-wide) | **PARTIAL-RESOLUTION.** R3 was able to discriminate using the v23 `ስስሎምን` doubled-`ስ` reference (with clear foot-at-right-base) versus v23 `ሰረገስተ` opening `ሰ` (with bare right vertical). v1 chapter-opening: confirmed **`ሰ` (bare)** — R1/R2/fix-round-2 right. But v15 `ሰሎምን` first instance also looks bare at 6x — possibly should be `ሰሎምን` not `ስሎምን`. NOT systematically re-verified per-instance — many Solomon-instances may flip-flop. HONEST flag retained chapter-wide. |
| `ቯ`/`ሺ` in v23/v26 red glyphs | **LEANING `ሺ`-shape** at 16x; not definitive without same-scribe known-reference. HONEST flag. (R1's interpretation may have been closer to the inked form than R2's revert.) |
| `መማግሥታት` doubled-`ማ` (v18) | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+. The crop f030v_L_v17_v18.png at 6x shows `መማግሥታት` plausibly — middle glyph is ambiguous between `ማ` and `ን`. HONEST flag retained. |
| v24 leading noun `ኆ`/`ኖ` (in `ኆጻ` "sand") | **NOT-INDEPENDENTLY-REVERIFIED** at 16x+. HONEST flag retained. |

---

## Independent new-defect scan

### CRITICAL — R2 reverts that were WRONG and got applied (need RE-REVERSION in fix-round-3)

These are the headline findings — R3's main contribution:

- **v7 `ይትመውሙ` (R2 dropped `ጻ`) → should be `ይትመጻውሙ`.** The `ጻ` is ON THE PARCHMENT, clearly visible at 8x in f030r_R_v7.png line 2. R2's "fabrication" claim was itself a fabrication-by-omission. **Restore the `ጻ`.**

- **v14 `ደሁዳ` → should be `ይሁዳ`.** Y-triangle `ይ` at f030v_L_v14_yhuda_max.png 16x.

- **v15 `ወደሰርዑ` → should be `ወይሰርዑ`.** Same Y-triangle `ይ`.

- **v17 `ደሁዳ` → should be `ይሁዳ`.** Same Y-triangle `ይ` at f030v_L_v16_v17.png 6x.

- **v17 `ደበልዑ` → should be `ይበልዑ`.** Same Y-triangle `ይ`.

- **v22 `ወዶዕ ሙ` (2 tokens) → should be `ወደኖሙ` (1 token).** f030v_M_v22_wedono.png at 12x unambiguously shows `ወ-ደ-ኖ-ሙ` joined.

- **v23 `እስ ይስሕቡ` → should be `እለ ይስሕቡ`.** Curve-stroke `ለ` at f030v_M_v23_es.png 18x.

- **v25 `እስ ዋዉደ` → should be `እለ ዋዉደ`.** Curve-stroke `ለ` at f030v_M_v25_es_max.png 18x.

- **v28 `እስ ሰምዑ` → should be `እለ ሰምዑ`.** Curve-stroke `ለ` at f030v_M_v28.png 8x line 2.

### MAJOR — minor first-character corrections

- **v18 `ህደወቱ` → should be `ሕደወቱ`.** First character is 6th-order `ሕ` (two-bar topology), not 1st-order `ህ`. The word straddles the f030v-L line 4→5 break: line 4 ends with `ሕ`, line 5 starts `ደወቱ`. Same-scribe `ሕ` reference: line-4 `ሕ` matches `ሕ` in `ሕይወቱ`-class words. (Note: parchment's word is `ሕደወቱ` not standard Ge'ez `ሕይወቱ` — this is the recensional reading R2 correctly caught on the second-char `ደ` axis, but R2 missed the first-char `ሕ`/`ህ` correction.)

### MINOR / vowel-mark / segmentation

- (None individually called out in this round beyond the above CRITICAL-class list; the marathon's accumulated minor flags are at the noise floor of the parchment resolution.)

### HONEST (resolution-limited)

- **Numeric red glyphs v23/v26.** R3 leans toward `ሺ`-shape but cannot definitively rule out `ቯ` without a same-scribe known reference. AMBIGUOUS-PARCHMENT — accept either as defensible.
- **`መማግሥታት` doubled-`ማ`** v18 — middle glyph identity unresolved.
- **v24 leading noun `ኆ`/`ኖ`** — labialised consonant family unresolved.
- **`ሰ`/`ስ` chapter-wide** — most instances at the limit of resolution; v1 confirmed `ሰ`, v23 confirmed `ስ` for `ስስሎምን`, others HONEST.
- **v22 segmentation `ኹሎሙ ተአምነዊ` vs `ኹሎሙ + እምነዊ`** — line-break ambiguity on whether `ተ` at end of line 2 prefixes the next word.

---

## Systematic-class summary

- **Round-2 reverts confirmed: 8 of 24** (v18 `ፈስግ`, v22 `ወእስ`, v18 `ወተቀንደ`, v17 `ሮዓ ባሕር`, v17 `ብዝናሙ`, v28 `ወይነሥኤ`, plus v21 `እስ ይጻወልዎ` AMBIGUOUS-leaning-CONFIRMED, plus 2 `ፈስግ` carry-forward).
- **Round-2 reverts that R3 thinks were WRONG (R1-was-right after all): 7** — the systematic `ይ`/`ደ` over-correction class (v14, v15, v17×2) and the `እለ`/`እስ` over-correction class (v23, v25, v28). This is the canonical Round-3 over-correction pattern.
- **Round-2 reverts AMBIGUOUS-PARCHMENT: 9** — including all 4 red-glyph numerals (lean `ሺ`-shape), v21 `ጋዘ`, v25 `ወበሕ ስም`, v28 `ይከምዑ`, v19 `ዘደትገበር`, v22 segmentation ambiguity. R3 does NOT force changes on these.
- **Round-2 new applies confirmed: 2 of 3** (v6 `ካፋን`, v26 `አርዝ`).
- **Round-2 new apply OVERRULED: 1 of 3** — v7 drop `ጻ` was wrong; `ጻ` is on the parchment.
- **Round-2 restoration confirmed: 1 of 1** (v18 `እክከ ደወስ` interlude).
- **Round-2 re-read partially confirmed: 1 of 2** (v22 `ኹሎሙ ተአምነዊ` — defensible at segmentation level; but `ወዶዕ ሙ` is WRONG and should be `ወደኖሙ`).
- **New defects discovered by R3 not in prior 2 rounds: 1** (v22 `ወዶዕ ሙ` → `ወደኖሙ`; also minor v18 `ህደወቱ` → `ሕደወቱ` first-char correction).
- **AMBIGUOUS-PARCHMENT items resolved by R3: 1 of 4** (`ሰ`/`ስ` at v1 confirmed bare `ሰ`; others still HONEST).

---

## Approval verdict

**NEEDS_FIX (round-3).**

Specific fix scope for fix-round-3:

1. **REVERT `ይ`/`ደ` over-corrections (R2 was wrong):**
   - v14: `ደሁዳ` → `ይሁዳ`
   - v15: `ወደሰርዑ` → `ወይሰርዑ`
   - v17 (first): `ደሁዳ` → `ይሁዳ`
   - v17 (second): `ደበልዑ` → `ይበልዑ`

2. **REVERT `እለ`/`እስ` over-corrections (R2 was wrong):**
   - v23: `እስ ይስሕቡ` → `እለ ይስሕቡ`
   - v25: `እስ ዋዉደ` → `እለ ዋዉደ`
   - v28: `እስ ሰምዑ` → `እለ ሰምዑ`

3. **RESTORE the `ጻ` in v7:** `ይትመውሙ` → `ይትመጻውሙ` (R2's drop was wrong; the original implementer was right, fix-round-1's disciplined non-change was right, R2 overruled in error).

4. **FIX v22 `ወዶዕ ሙ` (2 tokens) → `ወደኖሙ` (1 token).** This was missed by both R1 and R2. Parchment unambiguously inks `ወ-ደ-ኖ-ሙ` as a single word.

5. **MINOR: v18 `ህደወቱ` → `ሕደወቱ`** (first char correction).

6. **KEEP-AS-IS (R2 reverts confirmed by R3):** v6 `ካፋን`, v26 `አርዝ`, v18 `እክከ ደወስ` restoration, v18 `ፈስግ`, v21 `ፈስግ` ×2, v22 `ወእስ`, v18 `ወተቀንደ`, v17 `ሮዓ ባሕር`, v17 `ብዝናሙ`, v28 `ወይነሥኤ`, v1 `ወሰሎምን`.

7. **AMBIGUOUS / HONEST (no forced change):** the 4 red-glyph numerals at v23/v26 (R3 leans `ሺ`-shape but accepts R2's `ቯ` revert as defensible without a same-scribe known reference), v21 `ጋዘ`, v25 `ወበሕ ስም`, v28 `ይከምዑ`, v19 `ዘደትገበር`, v22 segmentation `ኹሎሙ ተአምነዊ`/`ኹሎሙ + እምነዊ`, `መማግሥታት` doubled-`ማ` (v18), v24 `ኆ`/`ኖ`, `ሰ`/`ስ` chapter-wide.

8. **KEEP-AS-IS (R2's kept disciplined-non-changes that R3 inherits):** v14 `በይሰክር` (RE-CONFIRMED), v15 `ዘአዘዝሙ` (RE-CONFIRMED), v16 `ሀስው`, v21 `እምራፉ`, v25 `እውናን`, v25 `ወደራል`/`ደቂት ሰግድ`, v4 `ዮዳሐ`.

---

## Method-note compliance

- **CARDINAL RULE — VIOLATED by R2 IN THE OPPOSITE DIRECTION.** R2 correctly identified that R1 had over-harmonized toward standard Ge'ez in many places, but then OVER-CORRECTED — reverting tokens to recensional-looking forms (`ደ` for `ይ`, `እስ` for `እለ`) in places where the parchment actually inks the standard form. This is the canonical Round-3 over-correction pattern. Fix-round-3 must apply the focused list above to swing the pendulum back to the actual inked glyphs. **The CARDINAL rule for fix-round-3: read the inked glyph topology at 14x+, not the in-context narrative.** v22 `ይሁዳ` and v17 `ይሁዳ` are the SAME scribe at the SAME zoom — both inks `ይ`. If R2 confirmed v22 `ይ`, then v17/v14 are also `ይ`.

- **METHOD NOTE 2 — INTACT.** The f030v-L → f030v-M column boundary at `ወአል | ሕምት` is correctly joined (verified at 14x on f030v_L_end.png). No mid-word column-boundary word-drop.

- **`ጻ`-axis at line break (v7):** R2's drop of `ጻ` is the same class as a column-boundary token drop, just at a line break within a column. The `ጻ` is on the parchment at the line-2 end before line-3 begins with `ው ሙ`. R2 inadvertently violated the CARDINAL rule here by trusting their own re-zoom over the actual parchment ink. R3 catches this.

---

## Honest notes

- **R2's overall direction was correct** (R1 had over-harmonized; many R2 reverts ARE confirmed by R3). The pattern is that R2 went TOO FAR on two specific axes (`ይ`/`ደ` and `እለ`/`እስ`) — applying the revert pattern systematically without per-instance verification. R3 catches this opposite-direction correction.
- **Convergence assessment: STABLE with focused residual defects.** Defect counts: R1 found ~110, fix-round-1 closed many, R2 found 22 critical + 17 R1-fix-misapplications, fix-round-2 closed most, R3 finds ~9 critical (re-revert list) + 1 new mid-class (v22 `ወዶዕ ሙ`). This is a CONVERGENCE trajectory — the residual list is focused and addressable. Not DIVERGING.
- **The v23/v26 red numerals** remain a genuinely unresolved AMBIGUOUS-PARCHMENT class. Without a same-scribe known-`ቯ` or known-`ሺ` reference elsewhere in the manuscript (e.g. a 2 Chronicles passage that mentions 40,000 chariots), neither R1 nor R2 nor R3 can definitively decide. Accept current JSON `ቯ` as the defensible HONEST reading.
- **The `ሰ`/`ስ` chapter-wide** remains noisy. R3 did confirm v1 `ሰ` (bare) vs v23 `ስስሎምን` (doubled `ስ` with foot) — but the body-text `ሰሎምን`/`ስሎምን` instances are at the perennial resolution limit. Accept current JSON readings.
- **R3 did not exhaustively re-verify the 7 kept disciplined-non-changes** at 16x+. Five are accepted by inheritance from R2's prior work under HONEST flag. If fix-round-3 has cycles, recommend a focused glyph-adjudicator pass on those five.
- **R3 commendation:** R2's discipline of writing column-strip crops and using same-scribe calibration is the right method; R2 just executed inconsistently per-instance. R3's same-method, same-zoom pass exposes the systematic-axis over-correction.

---

**End of R3 review.**
