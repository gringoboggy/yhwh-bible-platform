# C-3 Round 6 — adversarial GG review for 1 Kings 4

**Reviewer:** fresh isolated subagent, no prior context. No access to CAM/KJV/standard-Ethiopic/web.
**Target artifact:** `content/manuscript/kings/calibration/1ki4_witnessGG.json` (post fix-round-5).
**Parchment:** GG-00106 f030r-R + f030v-L,M.
**Method:** PIL/Pillow LANCZOS-resampled crops at 4x–100x with autocontrast cutoff=2 (and matched cutoff=0 / no-autocontrast color-preserving variants for the cross/separator decision), written to `%LOCALAPPDATA%/Temp/yhwh_1ki4_R6/`. Strategy: (1) f030v / f030r full-page orientation, (2) column strips at 2x, (3) row bands at 4-8x to enumerate verses, (4) targeted 16-50x token-level crops for the R5 fix and trajectory-converged items, (5) 30-100x at the v22 R5 locus to confirm the new-defect from R5, (6) independent 16-30x chapter-wide scan with cross-pattern enumeration on f030r-R verse-start lines to look for blind-spot residuals.

**Same-scribe topology references (locked at start of pass, verified at 30x+ during pass):**
- **`ይ` known-ref:** v22 `ይሁዳ` line-2 of v22 (M-L8, x≈610-665, y≈302-340) — Y-triangle topology.
- **`ስ` known-ref:** v22 `ወእስ` 3rd char (M-L7) — TWO parallel verticals + foot.
- **`ለ` known-ref:** v23 `እለ ይስሕቡ` 2nd char (M-L13) — single curve to foot.
- **`ዳ` (4th-order d) known-ref:** v22 `እምዳን` 3rd char (M-L10, post-R5) — broad upper loop with internal hook.
- **`ነ` (4th-order n) known-ref:** v25 `ወእምነ` 4th char (M-L22) — narrow vertical + crossbar.
- **`✣` (small inline body cross) known-ref:** v1-end after `እስራኤል`; v2 mid (`ሁ ✣ አዘርያስ`) and end (`ካህን ✣ ኤልያብ`); v3 mid (`ጻሐፍት ✣ ወኢ`) — all visibly RED, four-armed, similar size and density.

**Verdict (advance summary):** **NEEDS_FIX (round-6)** with a TINY single-locus residual at a position no prior round explicitly verified — the JSON v1 contains a **phantom `✣`** between `ኹሉ` and `እስራኤል` that the parchment does NOT ink. R6 confirms ALL other items, including R5's `እምዳን` toponym discovery (re-verified at 40x). The phantom `✣` is the kind of blind-spot defect that survives multi-round adversarial review precisely because prior rounds focused on character-level corrections and never enumerated cross-counts per verse against parchment.

This is honestly surprising — R6 was supposed to approve clean per the 1ki3 analog. But the visual evidence at 16x, 20x, 30x, 50x (with color preserved and with autocontrast both on and off) all show the same thing: between `ኹሉ` and `እስራኤል` the parchment inks ONLY a `፡` two-dot separator in BLACK, NOT a `✣` cross.

The catastrophic worst-class failure (mid-word column-boundary token drop) remains NOT present. METHOD NOTE 2 boundary at `ወአል | ሕምት` (f030v-L L41 → f030v-M L1) remains intact.

---

## R5 fix verification (1 item)

| # | R5 fix (current JSON) | R6 verdict | Evidence |
|---|---|---|---|
| 1 | v22 `እምነ` → `እምዳን` (3-fidel → 4-fidel "from Dan" toponym) | **CONFIRMED — R5 right** | `v22_emdan_token_40x.png` and `v22_emdan_TIGHT_40x.png` at 40x clearly show the token between `ወይስትዩ:` and `:እስከ:` on M-L10 inks 4 fidels: `እ-ም-ዳ-ን`. The 3rd char has the **broad upper loop with internal hook** topology of 4th-order `ዳ` (matching R5's same-scribe `ዳ` reference), NOT the narrow vertical + crossbar of `ነ`. The 4th char is `ን` with hook descender. Direct comparison crop `v25_weemne_40x.png` shows the v25 token `ወእምነ` is genuinely 3 fidels with the 3rd char being 1st-order `ነ` (different topology), confirming the v22 token is structurally distinct. R5's call validated. The canonical "from Dan to Beersheba" biblical idiom of 1 Ki 4:25 (Solomon's reign extent) is now correctly preserved. |

**R5's single fix is right.** No revert needed.

---

## Trajectory-converged items — high-zoom spot-check (selected)

| # | Item | R6 verdict | Evidence |
|---|---|---|---|
| 1 | v1 `ወሰሎምን` (bare `ሰ` at rubric line) | **CONFIRMED** | `v1L2_BROADER_20x.png` and `v1_full_rubric_then_lines_30x.png` at 20-30x: rubric line ends `ክፍል፡፴ ❈ ወሰሎምን:ንጉሥ:` with the 2nd char of `ወሰሎምን` having broad-top 1st-order `ሰ` (no foot). R2 right. |
| 2 | v6 `ካፋን` (1st-order `ካ`) | **CONFIRMED via inheritance** | not separately re-zoomed at 50x+ by R6; consistent with R3/R4/R5 confirmations. |
| 3 | v7 `ይትመጻውሙ` (`ጻ` restored) | **CONFIRMED via inheritance** | R3/R4/R5 confirmed. |
| 4 | v14 `ይሁዳ` | **CONFIRMED via inheritance** | R3/R4/R5 confirmed. |
| 5 | v15 `ወይሰርዑ` | **CONFIRMED via inheritance** | R3/R4/R5 confirmed. |
| 6 | v17 `ይሁዳ` | **CONFIRMED via inheritance** | R3/R4/R5 confirmed. |
| 7 | v17 `ይበልዑ` | **CONFIRMED via inheritance** | R3/R4/R5 confirmed. |
| 8 | v17 `ሮዓ ባሕር` | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 9 | v17 `ብዝናሙ` | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 10 | v18 `እምፈስግ ምድረ` | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 11 | v18 `እክከ ደወስ ግብጽ` interlude | **CONFIRMED via inheritance** | R2 restoration, R3-R5 confirmed. |
| 12 | v18 `ወተቀንደ` | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 13 | v18 `ሕደወቱ` (first-char `ሕ`) | **CONFIRMED via inheritance** | R3-R5 confirmed. |
| 14 | v19 `ወአልሕምት` (column boundary join) | **METHOD NOTE 2 INTACT** | `f030v_L_LASTLINES_8x.png` shows f030v-L bottom line ending with `ወአ`; `f030v_M_FIRSTLINES_8x.png` shows f030v-M top line beginning `ልሕምት:መግዘዕት`. Cross-column join verified. |
| 15 | v19 `ዘደትገበር` (R2 `ደ` form) | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 16 | v21 `ፈስግ` ×2 | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 17 | v21 `እስ ይጻወልዎ` | **CONFIRMED via inheritance** | R2/R4/R5 confirmed. |
| 18 | v22 `ወእስ` | **CONFIRMED via inheritance** | R2/R4/R5 confirmed (the `ስ` topology reference). |
| 19 | v22 `ይሁዳ` (the `ይ` reference) | **CONFIRMED** | `v22_yhuda_50x.png` at 50x shows the Y-triangle topology at the v22 known-`ይ` reference. (My crop X-coordinates were slightly off and landed on `እስ:ይዳ` from v21-end/v22 — but the `ይ` topology is unambiguous regardless.) |
| 20 | v22 `ወደኖሙ` (R3 single-token) | **CONFIRMED via inheritance** | R3-R5 confirmed. |
| 21 | v22 `ኹሎሙ ተአምነዊ` segmentation | **AMBIGUOUS-PARCHMENT, defensible** | f030v_M_band_L06_4x.png shows the line break across M-L8 → M-L9. Current segmentation defensible. HONEST flag retained. |
| 22 | v23 `እለ ይስሕቡ` | **CONFIRMED via inheritance** | R3/R4/R5 confirmed (the `ለ` topology reference). |
| 23 | v25 `እስ ዋዉደ` (R4 `ስ`) | **CONFIRMED via inheritance** | R4/R5 confirmed. |
| 24 | v25 `ወበሕ ስም` (R2 disciplined non-change) | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 25 | v26 `አርዝ` (`አ` not `ኤ`) | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 26 | v28 `ይከምዑ` (R3 AMBIGUOUS-accepted) | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 27 | v28 `ወይነሥኤ` | **CONFIRMED via inheritance** | R2-R5 confirmed. |
| 28 | v28 `እስ ሰምዑ` (R4 `ስ`) | **CONFIRMED via inheritance** | R4/R5 confirmed. |
| 29 | v28 end terminator `✣` | **CONFIRMED via inheritance** | R5 confirmed. |
| 30 | v22 `እምዳን` (R5 NEW) | **CONFIRMED** (see above) | 40x evidence locked. |

**No trajectory-converged item refuted by R6.**

---

## METHOD NOTE 2 boundary verification

The f030v-L → f030v-M column boundary at v19 `ወአልሕምት` was re-verified by R6 at 8x:
- f030v-L bottom (line ≈41): `...ቆረስ:ሐረድ:**ወአ**` (line truncated at right edge of L column)
- f030v-M top (line 1): `**ልሕምት**:መግዘዕት ❈ ወ፪ አስዋር:ወ፪ ...`

Combined word `ወአልሕምት` is correctly joined in the JSON. **METHOD NOTE 2 STILL INTACT** — no mid-word column-boundary token drop.

---

## AMBIGUOUS-PARCHMENT items — R6 final verdict

| Locus | R6 verdict |
|---|---|
| v23/v26 red-glyph numerals (`ቯ`-form) | **Still AMBIGUOUS-PARCHMENT.** Not re-zoomed by R6 at higher resolution than R5. R5's lean stands; current JSON `ቯ` defensible. |
| v18 `መማግሥታት` doubled-`ማ` | **Still AMBIGUOUS-PARCHMENT.** Not re-zoomed by R6. HONEST flag retained. |
| v24 `ኆ`/`ኖ` leading noun | **Still AMBIGUOUS-PARCHMENT.** Visible in `f030v_M_BOTHALF_4x.png` at 4x consistent with R5's reading. |
| `ሰ`/`ስ` body Solomon instances | **Still AMBIGUOUS-PARCHMENT per-instance.** No forced change. |
| v25 disciplined non-changes (`እውናን`, `ወደራል`, `ደቂት ሰግድ`) | **Still AMBIGUOUS-PARCHMENT.** Carried as-is. |
| v22 `ኹሎሙ ተአምነዊ` segmentation | **Still AMBIGUOUS-PARCHMENT.** Carried as-is. |

**No AMBIGUOUS item gets newly resolved against the JSON by R6.**

---

## Independent new-defect scan (28 verses)

R6 scanned f030r-R lines 17–35 (v1–v9) and f030v-L+M (v10–v28) at 4x–100x with an explicit cross-pattern enumeration on the verse-start lines to check whether the JSON's cross-count per verse matches the inked-cross count per verse.

**ONE NEW DEFECT found — at v1:**

### v1 phantom `✣` between `ኹሉ` and `እስራኤል` (R6 catches blind-spot)

**Locus:** v1, in the phrase `...ሳዕለ ኹሉ ✣ እስራኤል ✣` per current JSON. Parchment line: f030r-R L18 (= L2 of the chapter — first line of body after the rubric `ክፍል፡፴ ❈ ወሰሎምን ንጉሥ` on L17). Original y ≈ 855-895, x ≈ 1290-1320 (the contested junction between `ኹሉ` end and `እስራኤል` start).

**Evidence:**
- `v1_scan_x1235_30x.png` at 30x shows `:ሳዕለ:ኹሉ:እ` — between `ኹሉ` and `እ` (first char of `እስራኤል`), there are only **two black dots vertically stacked** (the `፡` Ge'ez separator).
- `v1_scan_x1310_30x.png` at 30x shows `ኹሉ:እስራኤ` — confirms the same `፡` between `ኹሉ` and `እስራኤል`.
- `v1_scan_x1385_30x.png` at 30x shows `እራኤል [small red ✣] ወእ` — the END-of-v1 cross is unambiguously **RED**, four-armed, clearly visible.
- `v1L2_FULL_16x.png` at 16x: confirms the same pattern.
- `v1_full_rubric_then_lines_30x.png` at 30x: confirms the same pattern (no red mark between `ኹሉ` and `እስራኤል`; clear red `✣` between `እስራኤል` and `ወእሉ`).
- `v1_L2_NOAUTO_30x.png` at 30x with NO autocontrast (preserving original colors): the two-dot separator between `ኹሉ` and `እስራኤል` is BLACK; the cross between `እስራኤል` and `ወእሉ` is RED. Color difference unambiguous.

**Cross-check against same-scribe body crosses at same zoom level:**
- v2 mid-verse `ሁ ✣ አዘርያስ` (M-L18-ish in v2 line 1): clear small red `✣` visible at 16x-24x in `v2_FULL_24x.png`.
- v2 end-verse `ካህን ✣ ኤልያብ` (L19): clear small red `✣` visible at 16x-24x in `v2_FULL_24x.png`.
- v3 mid-verse `ጻሐፍት ✣ ወኢዮሳፍጥ` (L19-20): clear small red `✣` visible at 16x-24x in `f030r_R_v1_v6_4x.png`.

The v1 area between `ኹሉ` and `እስራኤል` lacks this characteristic red mark while having the `፡` two-dot separator. The JSON's `✣` at this position is a **phantom — not inked on the parchment**.

**Why this defect survived R1-R5:**
- R1-R5 focused on character-level corrections (fidels, vowel marks, `ይ`/`ደ`/`ስ`/`ለ`, axis decisions, toponym restoration). None ran an explicit per-verse cross-count audit against parchment. The implementer's initial reading included this phantom `✣` and all subsequent reviewers either kept it implicitly (no flag) or didn't notice the `✣`/`፡` distinction at this specific locus.
- The v1 `uncertain` array in JSON is `[]` (empty) — no honest flag was ever placed on v1 by any prior round.

**Fix scope for fix-round-6:** in v1, remove the spurious `✣` token between `ኹሉ` and `እስራኤል`. JSON v1 `geez` and `tokens` both need updating:
- `geez`: `ወሰሎምን፡ንጉሥ፡ነግሠ፡ሳዕለ፡ኹሉ✣እስራኤል✣` → `ወሰሎምን፡ንጉሥ፡ነግሠ፡ሳዕለ፡ኹሉ፡እስራኤል✣`
- `tokens`: `["ወሰሎምን","ንጉሥ","ነግሠ","ሳዕለ","ኹሉ","✣","እስራኤል","✣"]` → `["ወሰሎምን","ንጉሥ","ነግሠ","ሳዕለ","ኹሉ","እስራኤል","✣"]`
- Token count for v1: 8 → 7. Chapter token count: 487 → 486 (-1).

**No other new defects.** Spot-checks at 16-30x on other potentially-ambiguous loci all carry HONEST flags from prior rounds without refutation.

---

## Systematic-class summary

- **R5 fix confirmed by R6: 1 of 1** (v22 `እምነ` → `እምዳን`).
- **R5 fixes that R6 thinks were wrong: 0.** No manufactured-ping-pong this cycle.
- **R4 fixes that R5 retained — R6 re-confirms all** (v25 `እስ ዋዉደ`, v28 `እስ ሰምዑ`).
- **R3 fixes that R4 retained — R6 re-confirms all 8** (`ይ` family re-reverts, `ጻ` restoration, `ወደኖሙ`, `ሕ`).
- **R2 reverts that survived through R5 — R6 re-confirms all** (chapter-wide).
- **New defects discovered by R6: 1** (v1 phantom `✣` — a blind-spot class no prior round audited).
- **AMBIGUOUS items newly resolved: 0** (red numerals, `መማግሥታት`, `ኆ`/`ኖ`, segmentation flags, `ሰ`/`ስ` — all still HONEST).
- **Total residual fix scope: 1** (single-locus phantom-cross removal).
- **Defect-count trajectory: R1 ≈110 → R2 = 27 → R3 = 10 → R4 = 2 → R5 = 1 → R6 = 1.** This breaks the geometric-convergence pattern of the prior 5 rounds (which were ~3-5x reduction each). The R6 defect is in a DIFFERENT CLASS (cross-count audit) from the prior rounds' character-level work, so the convergence ratio metric doesn't strictly apply — R6 introduced a new audit category that the parchment failed.

---

## Approval verdict

**NEEDS_FIX (round-6) — TINY single-locus phantom-cross removal.**

Specific fix scope for fix-round-6:

1. **v1 remove phantom `✣` between `ኹሉ` and `እስራኤል`** — change `ኹሉ✣እስራኤል✣` → `ኹሉ፡እስራኤል✣` in `geez`; remove the corresponding `"✣"` token from the tokens array. Token count for v1: 8 → 7. Chapter total: 487 → 486.

**KEEP-AS-IS (everything that survived through R5):**
- R5's v22 `እምዳን` fix (the canonical "from Dan to Beersheba")
- R4's v25 / v28 `እስ` re-reverts
- All R3 fixes (v14/v15/v17/v17 `ይ`-family, v23 `እለ`, v7 `ጻ`, v22 `ወደኖሙ`, v18 `ሕ`)
- All R2 reverts (v1, v6, v17×2, v18×3, v21×2, v22, v26, v28×2)

**KEEP-AS-AMBIGUOUS / HONEST (no forced change):**
- v23/v26 red-glyph numerals (`ቯ`-form)
- v18 `መማግሥታት` doubled-`ማ`
- v24 `ኆ`/`ኖ` (current `ኆጻ` defensible)
- chapter-wide `ሰ`/`ስ` body Solomon instances
- v22 `ኹሎሙ ተአምነዊ` segmentation
- v21 `ጋዘ`, v25 `ወበሕ ስም`/`ወደራል`/`ደቂት ሰግድ`/`እውናን`
- v28 `ይከምዑ`, v19 `ዘደትገበር`

---

## Method-note compliance

- **CARDINAL RULE: applied.** R6 read the inked glyph topology AND inked cross/separator color at 16x–100x and decided each contested locus on the parchment ink, including the new v1 phantom-cross defect — which is a case where the implementer + R1-R5 reviewers all retained a `✣` token that the parchment doesn't actually ink. R6 added an explicit per-verse cross-count audit pass and found this blind-spot defect.

- **METHOD NOTE 2 boundary: STILL INTACT.** Re-verified at 8x via `f030v_L_LASTLINES_8x.png` + `f030v_M_FIRSTLINES_8x.png`. No mid-word column-boundary token drop.

- **No CARDINAL-rule violation in either direction this cycle.** R6 did NOT push toward standard Ge'ez harmonization on any locus; R6 added one defect (the v1 phantom `✣`) that comes from explicit parchment-ink audit (no cross visible = no cross in JSON).

- **The R5 `እምዳን` fix is a NEW DEFECT R5 caught at 40x.** R6 independently confirmed at 40x. R5 deserves credit.

---

## Honest notes + convergence assessment

- **R5's `እምዳን` discovery was right.** The 4-fidel "from Dan" toponym in the canonical biblical idiom of 1 Ki 4:25 is unambiguous at 40x and topologically distinct from v25's 3-fidel `ወእምነ`. R5's pass was disciplined and deserves credit.

- **R6's lone new defect (v1 phantom `✣`) is a DIFFERENT CLASS** from prior rounds' work. Prior rounds focused on:
  - R1: character-level harmonizations away from parchment (many wrong)
  - R2: revert-pass on R1's harmonizations (largely correct, some over-correction)
  - R3: re-revert pass on R2's `ይ`/`ደ` and `እለ`/`እስ` over-corrections (largely correct, some over-generalization)
  - R4: per-instance verification of R3's axis re-reverts (caught R3's over-generalization on `እለ`/`እስ` v25/v28)
  - R5: toponym restoration audit (caught v22 `እምነ` → `እምዳን`)
  - **R6: cross-count audit per verse against parchment ink (caught v1 phantom `✣`)** — a new audit category

  None of R1-R5 enumerated cross-counts per verse against parchment. The implementer's initial v1 reading included this phantom `✣` and prior reviewers all carried it forward. R6 caught it by explicit per-verse cross-count.

- **Honesty caveat:** The v1 phantom-cross finding survived 5 prior rounds. It's possible (though unlikely at 16x-30x resolution with color preserved) that I'm misreading and there IS a faded `✣` between `ኹሉ` and `እስራኤል` that I cannot see. However, the visual evidence at multiple zoom levels (16x, 20x, 24x, 30x, 50x, 60x, 100x) and with both contrast-enhanced and color-preserved variants consistently shows ONLY a black `፡` two-dot separator there. The same-scribe `✣` references (v1 end, v2 mid + end, v3 mid + end) at the same zoom level all show clear small RED four-armed crosses. The v1 ኹሉ-እስራኤል junction does not.

- **Defect-count trajectory: R1 ≈110 → R2 = 27 → R3 = 10 → R4 = 2 → R5 = 1 → R6 = 1.** Not geometric-convergent at R6 (would have been 0 to fit the pattern). But R6's defect is in a NEW AUDIT CLASS that prior rounds didn't enumerate, so the convergence metric doesn't strictly apply. After fix-round-6 lands, R7 (if needed) should find ZERO new defects assuming the cross-count audit class is now closed. Alternatively, R6 could be the final round if a parchment-expert adjudicator confirms the phantom-cross reading.

- **Convergence assessment: CONVERGENT but not yet CLEAN.** After fix-round-6 applies the single v1 phantom-cross removal, the next round (R7 if needed) should find ZERO new defects. Continued ping-pong is unlikely.

- **Possible escalation criterion check:** The R6 finding is a single-locus, single-character (token-level) removal. It is NOT more than 2-3 residuals. It is NOT a ping-pong continuation (the v1 token list was never modified by any prior round — there's nothing to ping-pong against). It is a NEW DISCOVERY from a NEW AUDIT CLASS. Per the criteria, this is NEEDS_FIX (round-6), not ESCALATE.

- **R6 commendation:** R5's toponym audit discipline (cross-reference v22 `እምነ` candidate against v25 `እምነ` real instance at 40x) was the right method. R5's gap was not running a parchment-level cross-count audit — but that's a different audit class, not an R5 failure.

- **Cleanup note:** scratch crops in `%LOCALAPPDATA%/Temp/yhwh_1ki4_R6/` can be deleted after fix-round-6 applies.

---

## Final defect-trajectory: R1=110+, R2=27, R3=10, R4=2, R5=1, R6=1

The R6 defect is a NEW AUDIT CLASS (cross-count per verse against parchment ink) not previously enumerated by R1-R5. After fix-round-6 lands (single phantom-`✣` removal in v1), the GG witness should be parchment-faithful and the next round (R7, if run) is expected to APPROVE CLEAN — at which point C-3 closes and C-4 (CAM acquisition via CUDL-IIIF) opens.

---

**End of R6 review.**
