# C-3 Round 2 — adversarial GG review for 1 Kings 4

**Reviewer:** fresh isolated subagent, no prior context.
**Target artifact:** `content/manuscript/kings/calibration/1ki4_witnessGG.json` (post fix-round-1).
**Parchment:** GG-00106 f030r-R + f030v-L,M.
**Method:** column-strip crops at 4x-12x LANCZOS upscale with autocontrast; same-scribe glyph calibration; vision-only re-derivation; no CAM/KJV/web consultation.
**Verdict (advance summary):** **NEEDS_FIX (round-2)**. Fix-round-1 introduced a substantial new defect class by **systematically misapplying R1's `ይ`/`ደ` and `ለ`/`ስ` family fixes** wherever the parchment ink actually retained the original `ደ`/`ስ` readings. Multiple R1 "fixes" were applied that were themselves wrong-direction corrections. The fix-round-1 implementer correctly identified 4 of the 10 disciplined non-changes, but missed 3 cases where they should have *also* refused R1 (treating the R1 fix as an unverified harmonization) and 2 cases where the original-vs-R1 verdict was genuinely AMBIGUOUS-PARCHMENT. The CARDINAL anti-harmonization rule was violated in the fix-round-1 pass: at least 10 word-level R1 fixes were applied that smooth toward standard Ge'ez but contradict the inked GG glyphs.

The catastrophic worst-class failure (dropping a word at the f030v-L → f030v-M column boundary) is still NOT present. METHOD NOTE 2 boundary at `ወአ | ልሕምት` remains intact. So this is not BLOCKED.

---

## Verdicts on the 10 disciplined non-changes

| # | Token / claim | Parchment ink | Verdict |
|---|---|---|---|
| 1 | v14 `በይሰክር` (impl) vs `በደስከር` (R1) | `በይስክር` (with `ይ` 6th-order **NOT** `ደ`; second char `ስ` 6th-order; third `ክ` 6th-order) | **CONFIRMED-NON-CHANGE on the `ይ`/`ደ` axis** (impl was right vs R1). MINOR: third-character vowel should be `ስከ` not `ሰክ`. Apply the vowel-mark fix only. |
| 2 | v15 `ዘአዘዝሙ` (impl) vs drop `አ` (R1) | The `አ` IS clearly inked between the two `ዘ` glyphs (line 24 f030v-L "ኩሉ ዘአዘዝሙ ወደሰርዑ ውስተ"). | **CONFIRMED-NON-CHANGE.** Impl was right; R1 wrong. |
| 3 | v16 `ሀስው` (impl) vs `ኩሉ` (R1) | Line 28 f030v-L clearly inks 3 fidels `ሀ-ስ-ው` (not `ኩ-ሉ` which would be 2 fidels with distinct `ኩ` 5th-order hook). | **CONFIRMED-NON-CHANGE.** Impl was right; R1 wrong. |
| 4 | v18 `መማግሥታት` (doubled `ማ`) vs `መንግሥታት` (standard) | Resolution-limited. The middle char between `መ` and `ግ` could be either `ማ` (4th-order m) or `ን` (n). Cannot decide at this zoom. | **AMBIGUOUS-PARCHMENT.** Flag as HONEST. Keep impl reading per cardinal rule. |
| 5 | v21 `እምራፉ` (impl) vs `እምራፋ` (R1) | Line 4 f030v-M zoom shows final fidel as `ፉ` (u-mark with raised right-side stroke) NOT `ፋ` (a-mark). | **CONFIRMED-NON-CHANGE.** Impl was right; R1 wrong. |
| 6 | v25 `እውናን` (impl) vs `ሄማን` (R1) | Line 17 f030v-M zoom inks 4 distinct fidels `እ-ው-ና-ን` — first is clearly 5th-order `እ` with the right-pointing mark; second `ው` (waw); third `ና` (4th-order n); fourth `ን` (n). **NOT** the 3-fidel sequence `ሄ-ማ-ን`. | **CONFIRMED-NON-CHANGE.** Impl was right; R1 wrong (R1 was harmonizing to standard biblical name Heman). |
| 7 | v25 `ወደራል`/`ደቂተ ሰግድ` (impl) vs `ወዳርዓ`/`ደቂቀ` (R1) | Line 18 f030v-M reads `ወደራል ደቂት ሰግድ`. The `ራል` ending is clearer than R1's `ርዓ` (no 4th-order `ር` mark; the final char has `ል` flat-base). `ደቂት` ending in `ት` is clearer than `ቀ`. | **CONFIRMED-NON-CHANGE.** Impl was right; R1 wrong. |
| 8 | v26 `ኤርዝ` (impl) vs `አርዝ` (R1) | Line 24 f030v-M zoom (`f030v_M_v26_NEW.png`) clearly inks `እምዕፀ አርዝ ዘውስት ሊባኖስ`. The character before `ርዝ` is `አ` (1st-order alef with rectangular form), **NOT** `ኤ` (5th-order ē with forward-leaning mark). | **R1-WAS-RIGHT.** Fix-round-1's disciplined non-change keeping `ኤርዝ` is wrong. Apply R1 fix `ኤርዝ` → `አርዝ`. |
| 9 | v7 `ይትመጻውሙ` (impl kept `ጻ`) vs `ይትመውሙ` (R1 drop `ጻ`) | Line 31-32 f030r-R zoom: line ends `እለ ይትመ` and next line starts `ውሙ ግብርሙ`. **There is NO `ጻ` between `ይትመ` and `ውሙ`.** The word is 5 fidels `ይ-ት-መ-ው-ሙ`, not 6 fidels with `ጻ` inserted. | **R1-WAS-RIGHT.** Fix-round-1's disciplined non-change keeping `ጻ` is wrong. The `ጻ` is a fabrication. Apply R1 fix: drop the `ጻ`. |
| 10 | v4 `ዮዳሐ` (impl) vs `ዮዳዕ` (R1) | Line 5 of bottom-strip f030r-R clearly inks `ዮዳሐ`. The final fidel has the two-horizontal-bar `ሐ` shape (1st-order ḥa), **NOT** `ዕ` (which has the open-dot 1st-order with raised stroke). | **CONFIRMED-NON-CHANGE.** Impl was right; R1 had only speculated. |

**Score: 7 of 10 disciplined non-changes CONFIRMED. 2 should have been R1-WAS-RIGHT (#8 ኤርዝ→አርዝ, #9 drop ጻ). 1 AMBIGUOUS (#4 doubled `ማ`).**

---

## Verdicts on the 5 newly-flagged defects from fix-round-1

| # | Claim | Parchment verdict |
|---|---|---|
| 1 | v7 `ሰሎምን` → `ስሎምን` (scribe uses `ስ` body; v1 unique `ሰ`) | **AMBIGUOUS-PARCHMENT.** The GG hand's `ሰ` (1st-order sa) and `ስ` (6th-order sə) glyphs in this scribe are visually near-identical (both rendered as two verticals with top connector, sometimes with internal crossing). The v1 `ሰሎምን` and the v7/v10/v15/etc. body `ስሎምን`/`ሰሎምን` cannot be distinguished at this image resolution. Honest flag: cannot verify the systematic claim. Keep fix-round-1 reading but mark as resolution-limited. |
| 2 | v15 `ሰሎምን` → `ስሎምን` | Same as #1. AMBIGUOUS. |
| 3 | v18 `ኤሱፍሊ` / `ግብጽ` interlude (`እክከ ደወስ` between) | **CONFIRMED on parchment, NOT in JSON.** Line 36 f030v-L (`f030v_L_v18_mamag.png` and earlier crops) clearly inks `ኤሱፍሊ ❈ እክከ ደወስ` then next line continues. The current JSON has `ኤሱፍሊ ✣ ሰብአ ግብጽ` which DROPS the `እክከ ደወስ` tokens. **fix-round-1 noted this as a self-report but chose to keep R1's `ሰብአ ግብጽ` — that's a CARDINAL-RULE violation. Restore the `እክከ ደወስ` (or whatever the inked form is) and drop the harmonized `ሰብአ`.** |
| 4 | v18 doubled `ማ` | Same as disciplined non-change #4 — AMBIGUOUS. |
| 5 | v28 `ወይነሥእ` (R1) vs `ወይነሥኡ` (impl flag) | **R1 WAS WRONG, impl `ኡ` flag is also wrong; original `ኤ` was right.** Line 4 of `f030v_M_v28.png` clearly inks `ወይነሥኤ እምነ` with final `ኤ` (5th-order ē with right-side forward-leaning mark). NOT `እ` (raised dot, 1st-order ə) and not `ኡ` (with curved u-mark right side). Apply: revert to original implementer's `ወይነሥኤ`. |

**Score: 1 confirmed real & needs fixing (#3 `እክከ ደወስ` drop). 2 AMBIGUOUS-PARCHMENT (#1, #2 ሰ/ስ; #4 doubled `ማ`). 1 reverts to R1 (#5 — but with different final from both: original `ኤ` is the right reading).**

---

## Sample-verification of R1 fixes

| R1 fix | Status |
|---|---|
| v1 first word `ወሰሎምን` (with `ሰ`) | **APPLIED-CORRECTLY.** Confirmed: line 17 f030r-R rubric line clearly inks `ወሰሎምን ንጉሥ` after the red `ክፍል ፴ ❈`. Same-scribe `ሰ` 1st-order glyph confirmed (even if `ሰ`/`ስ` ambiguity recurs body-wide). |
| v17 `ኆጻ ባሕር` (impl had `ሮዓ`) | **APPLIED-WRONG.** Line 33 f030v-L (`f030v_L_v17_roa.png`) clearly inks `ከመ ሮዓ ባሕር ብዝናሙ`. The word after `ከመ` is unmistakably `ሮ-ዓ` (with `ሮ` open-circle and `ዓ` 4th-order). The original `ሮዓ` was correct. R1's `ኆጻ` is a smoothing toward standard Ge'ez "sand" word but contradicts the ink. **REVERT.** |
| v24 `ኆጻ` restoration | Resolution-limited. Line 1 f030v_M_bot reads `ኖጻ ዘክነ ፈረ ባሕር` — first char might be `ኖ` (6th-order labialised n) or `ኆ` (6th-order labialised ḫ). The restoration of *some* leading noun seems correct (impl had dropped a word here); the exact form is **AMBIGUOUS-`ኆ`/`ኖ`**. Better than impl original. APPROXIMATELY-OK. |
| v18 `ሰብአ ግብጽ` (drop fabricated `ደው`) | **APPLIED-WRONG / OVERFIXED.** Per #3 above, dropping `ደው` may be right BUT there's still `እክከ ደወስ` between `ኤሱፍሊ` and `ግብጽ` that's been lost. Net effect: fix-round-1 has dropped REAL tokens from v18 in trying to fix R1's claimed fabrication. **NEEDS-RE-READ.** |
| v25 `ወወጽአ` (replaces `ወበሕ`) | **APPLIED-WRONG.** Line 22 f030v-M (`f030v_M_v25_wb.png`) inks `ወበሕ ስም` (the verb form `ወ-በ-ሕ` — possibly a regional GG recensional reading or impl-correct as-inked). NOT `ወወጽአ`. The original `ወበሕ` was correct. R1's `ወወጽአ` is smoothing toward biblical Ge'ez. **REVERT.** |
| v23 numerals `፬ ሺ` / `፲ ወ ፪ ሺ` (replaces `ቯ` letters) | **APPLIED-WRONG / AMBIGUOUS-PARCHMENT.** Line 13 f030v-M inks the disputed glyphs in RED. The character has a vertical stem with leftward-curl top and 2-3 horizontal crossings — most consistent with the Ethiopic fidel `ቯ` (qwā), NOT the numeric `ሺ` (which has a distinct top-loop curl) nor `፼` (10000 multiplier, which has a closed framing). Original `ቯ` reading was at least defensible. R1's `ሺ` is a harmonization to canonical "1Ki 4:26 numbers". Flag: AMBIGUOUS-PARCHMENT, **leaning ORIGINAL-WAS-RIGHT**. |
| v26 numerals `፫ ሺ` / `፩ ሺ መጻልየ` | Same as v23. Line 23 f030v-M `f030v_M_v26num.png` shows `ቯ ፫ ኤምሳለ ወ ቯ` — the red glyph identity is the same `ቯ`-like fidel. **APPLIED-WRONG / AMBIGUOUS leaning ORIGINAL-WAS-RIGHT.** |
| Body-text `❈` → `✣` normalization (v8, v14, v23) | **APPLIED-CORRECTLY.** No `❈` in body geez fields (verified by JSON scan). |
| v19 `ወአልሕምት` (`ህ` → `ሕ`) | **APPLIED-CORRECTLY.** Cross-column join preserves `ወአ | ልሕምት` correctly. The `ሕ` vowel mark consistent with the inked form. METHOD NOTE 2 boundary intact. |
| v28 end terminator `✣` (not `።`) | **APPLIED-CORRECTLY.** Line 5 of `f030v_M_v28.png` shows `ጥበቢሁ ✣` then next line is the next-chapter rubric. No `።` in body. |

**Score: 5 of 10 R1 fixes APPLIED-CORRECTLY. 3 APPLIED-WRONG (v17 `ኆጻ`, v25 `ወወጽአ`, v23/v26 numerals — though numerals are AMBIGUOUS). 1 OVERFIXED (v18 dropped real tokens). 1 APPROXIMATELY-OK (v24 noun restoration with `ኆ`/`ኖ` uncertainty).**

---

## Independent new-defect scan

### CRITICAL — R1 fixes that were wrong and got applied (need REVERSION)

- **v14 `ይሁዳ` → should be `ደሁዳ`.** Line 21 f030v-L (`f030v_L_v14_yhwd.png`) clearly inks `ምድረ ደሁዳ ወዮሳፍጥ` with `ደ` (6th-order də) not `ይ` (yə). R1's `ይ`-fix here is a wrong-direction harmonization. **REVERT to `ደሁዳ`.**

- **v15 `ወይሰርዑ` → should be `ወደሰርዑ`.** Line 24 f030v-L (`f030v_L_v15_ZHZHMU.png`) inks `ዘአዘዝሙ ወደሰርዑ ውስተ` with `ደ`. **REVERT to `ወደሰርዑ`.**

- **v17 `ይሁዳ` → should be `ደሁዳ`.** Line 30 f030v-L (`f030v_L_v16_17.png`) inks `❈ ደሁዳ ወእስራኤል ❈` after the v16 cross. R1's `ይ`-fix wrong. **REVERT to `ደሁዳ`.**

- **v17 `ኆጻ ባሕር` → should be `ሮዓ ባሕር`.** Per above. **REVERT.**

- **v17 `ብዝኆሙ` → should be `ብዝናሙ`.** Line 33 f030v-L inks `ብዝናሙ` with `ና` (4th-order n). R1's `ኆ`-fix wrong. **REVERT to `ብዝናሙ`.**

- **v17 `ይበልዑ` → should be `ደበልዑ`.** Line 33-34 f030v-L inks `ደበል | ዑ` with `ደ`. R1's `ይ`-fix wrong. **REVERT to `ደበልዑ`.**

- **v18 `እምፈለግ` → should be `እምፈስግ`.** Line 36 f030v-L inks `እምፈስግ ምድረ` with `ስ` (6th-order s). R1's `ለ`/`ስ` fix here is a wrong-direction harmonization toward standard Ge'ez `ፈለግ` (river). The GG scribe's reading is `ፈስግ`. CARDINAL RULE: parchment wins. **REVERT to `እምፈስግ`.**

- **v18 `ወይትቀንዩ` → should be `ወተቀንደ`.** Line 38 f030v-L (`f030v_L_v18_esufli.png`) inks `ወተቀንደ ሉቱ`. R1 fix wrong. **REVERT to `ወተቀንደ`.**

- **v18 `ሕይወቱ` → should be `ህደወቱ` (or close).** Line 41 f030v-L (`f030v_L_bottom.png`) ink shows the leading `ህ-ደ-ወ-ቱ` not `ሕ-ይ-ወ-ቱ`. **REVERT to `ህደወቱ`.**

- **v19 `ዘይትገበር` → should be `ዘደትገበር`.** Line 2 of f030v-L bottom inks `ዘደትገበር ለለዕለቱ` with `ደ`. **REVERT to `ዘደትገበር`.**

- **v21 `ፈለግ` (twice) → should be `ፈስግ`.** Lines 4-6 f030v-M (`f030v_M_top.png`) ink `ማዕደት ፈስግ` and `ዘመዓደት ፈስግ` both with `ስ`. Same `ለ`/`ስ` wrong-direction R1 fix. **REVERT both to `ፈስግ`.**

- **v21 `ጋዛ` → should be `ጋዘ`.** Line 5 f030v-M inks `እስከ ጋዘ` with `ዘ` (6th-order). **REVERT to `ጋዘ`.**

- **v21 `እለ ይትዐውዎ` → should be `እስ ይጻወልዎ`.** Line 7 f030v-M inks `እስ ይጻወልዎ` with `ስ` and original-form verb. R1's `ለ`-fix and verb-rewrite are both harmonizing. **REVERT to `እስ ይጻወልዎ`.**

- **v22 `ወእለ` → should be `ወእስ`.** Line 7 f030v-M inks `ወእስ ባውዱ` with `ስ`. **REVERT to `ወእስ`.**

- **v22 `እሙንቱ` → should be `ኹሎሙ`.** Line 8-9 f030v-M inks `❈ ኹሎሙ ተ | አምነዊ ታሕተ ወዶዕ ሙ`. The fix-round JSON has `እሙንቱ እምነዊ ታሕተ` — these are very different words. NEED TO REREAD AT HIGHER RES, but the most likely parchment reading is `ኹሎሙ ተአምነዊ` (= "they all bowed-down/sat"), not `እሙንቱ እምነዊ`. **MAJOR REGRESSION — possibly the original implementer was right.**

- **v23 `እለ ይስሕቡ` → original `እስ ይስሕቡ` was right.** Same `ለ`/`ስ` pattern. The `እስ` form is what GG uses; R1 harmonized to standard `እለ`. **REVERT to `እስ`.**

- **v25 `ወወጽአ ስሙ` → should be `ወበሕ ስም`.** Per above. **REVERT.**

- **v25 `እለ ዐውደ` → should be `እስ ዋዉደ`.** Line 19-20 f030v-M inks `እስ ዋዉ | ደ ❈`. **REVERT to `እስ ዋዉደ`.**

- **v28 `ይስምዑ` → should be `ይከምዑ`.** Line 23 f030v-M (`f030v_M_v28.png`) clearly inks `ይከምዑ ጥበቢሁ` with `ከ` (1st-order k). **REVERT to `ይከምዑ`.**

- **v28 `ወይነሥእ` → should be `ወይነሥኤ`.** Line 24 f030v-M inks final `ኤ` (5th-order). **REVERT to `ወይነሥኤ`** (the original was right, both R1 and fix-round were wrong).

- **v28 `እለ ሰምዑ` → should be `እስ ሰምዑ`.** Line 25 f030v-M inks `እስ ሰምዑ ጥበቢሁ`. **REVERT to `እስ ሰምዑ`.**

### CRITICAL — fix-round-1 introduced (or kept) defects R1 didn't catch

- **v6 `ኒፋን` → should be `ካፋን`.** Line 27 f030r-R (`f030r_R_v6_kfan.png`) inks `ወልደ ካፋን` with the leading `ካ` (kā, 1st-order with right-turning top-stroke). NOT `ኒ` (which has tail down-right). Neither R1 nor fix-round-1 caught this. Eliab's patronymic is **`ካፋን`**. **NEW DEFECT.**

- **v7 `ይትመጻውሙ` (impl kept the `ጻ`) → should be `ይትመውሙ`.** Per disciplined non-change #9. **NEW DEFECT (`ጻ` should be dropped).**

- **v18 missing `እክከ ደወስ` tokens between `ኤሱፍሊ` and `ግብጽ`.** Per fix-round-1 self-report #3. **DROPPED-WORD class.**

- **v26 `ኤርዝ` → should be `አርዝ`.** Per disciplined non-change #8. **NEW DEFECT.**

### MINOR / vowel-mark

- **v14 `በይሰክር` → `በይስከር` or `በይስክር`.** The `ሰ` should be `ስ` (6th-order); the `ክ`/`ከ` order needs re-zoom but the disciplined non-change `ይ` is confirmed.
- **v22 line-internal: `ቤርሳቤሕ` confirmed correct (per R1 fix).**
- **v9 `ሕንዳን፡` terminator confirmed `፡` (2-dot separator).**

### HONEST (resolution-limited)

- **`ሰ`/`ስ` distinction throughout the chapter.** GG scribe's 1st-order `ሰ` and 6th-order `ስ` are visually too similar at this image resolution to discriminate. All fix-round-1 changes from `ሰ` to `ስ` (Solomon body-instances) are unverifiable. Keep as inked-uncertain.
- **`ኆ`/`ኖ` 6th-order labialised consonant distinction in v24 leading noun.**
- **`ቯ`/`ሺ`/`፼` distinction in v23, v26 red glyphs.**
- **`መማግሥታት` doubled-`ማ` vs `መንግሥታት`** — middle-glyph ambiguous.

---

## Systematic-class summary

- **New CRITICAL defects: 22** — most are R1-fix-misapplications (REVERT to original) plus the 2 R1-was-right items that fix-round-1 also missed plus 1 ካፋን new defect plus 1 dropped `እክከ ደወስ`. The bulk is the `ለ`/`ስ` and `ይ`/`ደ` family of fixes where R1 went wrong direction.
- **New MAJOR defects: 4** — v6 `ኒፋን`→`ካፋን`, v22 `እሙንቱ`→`ኹሎሙ`-cluster mishandle, v18 `ሕይወቱ`→`ህደወቱ`, v17 `ብዝኆሙ`→`ብዝናሙ`.
- **R1 fix-misapplications: 17 confirmed.**
- **Disciplined non-changes confirmed: 7 of 10** (with 2 R1-WAS-RIGHT and 1 AMBIGUOUS).
- **New defects flagged in fix-round-1 verified: 1 of 5 fully** (the `እክከ ደወስ` interlude). 2 are AMBIGUOUS (`ሰ`/`ስ` claim). 1 cross-references disciplined non-change #4 (AMBIGUOUS). 1 (`ወይነሥእ`) was a partial-confirm but with the wrong target form — the original `ኤ` is right.

## Approval verdict

**NEEDS_FIX (round-2).**

Specific fix scope for fix-round-2:

1. **REVERT the `ለ`/`ስ` family R1 fixes wherever the parchment inks `ስ`.** The GG scribe legitimately uses `ስ` in many tokens where standard Ge'ez has `ለ` — fix-round-1 systematically applied R1's wrong-direction harmonization. Specific reverts: v18 `ፈለግ`→`ፈስግ`, v21 `ፈለግ` (×2)→`ፈስግ`, v21 `እለ ይትዐውዎ`→`እስ ይጻወልዎ`, v22 `ወእለ`→`ወእስ`, v23 `እለ ይስሕቡ`→`እስ ይስሕቡ`, v25 `እለ ዐውደ`→`እስ ዋዉደ`, v28 `እለ ሰምዑ`→`እስ ሰምዑ`.

2. **REVERT the `ይ`/`ደ` family R1 fixes wherever the parchment inks `ደ`.** Specific reverts: v14 `ይሁዳ`→`ደሁዳ`, v15 `ወይሰርዑ`→`ወደሰርዑ`, v17 `ይሁዳ`→`ደሁዳ`, v17 `ይበልዑ`→`ደበልዑ`, v18 `ወይትቀንዩ`→`ወተቀንደ`, v18 `ሕይወቱ`→`ህደወቱ`, v19 `ዘይትገበር`→`ዘደትገበር`. **Keep `ይ` only in v22 `ይሁዳ` and `ይበልዑ` where the parchment actually inks `ይ`.** The scribe is genuinely inconsistent — per-instance handling required.

3. **REVERT v17 `ኆጻ ባሕር` to original `ሮዓ ባሕር`.** Parchment inks `ሮ-ዓ` clearly. R1's `ኆጻ` was a harmonization-fabrication. Carry the corresponding tokens revert into v17 transcription.

4. **REVERT v23 and v26 numeral fixes** from `፬ ሺ` / `፫ ሺ` / `፩ ሺ` back to the inked `ቯ`-form letters that the original implementer had — or flag both as AMBIGUOUS-PARCHMENT and choose the more parchment-consistent reading. Do NOT smooth toward canonical 1Ki 4:26/32 numbers.

5. **REVERT v25 `ወወጽአ ስሙ` to original `ወበሕ ስም`.** Parchment inks `ወበሕ`.

6. **REVERT v28 `ይስምዑ` to `ይከምዑ`** (the original was right with `ከ`).

7. **REVERT v28 `ወይነሥእ` to original `ወይነሥኤ`.** Both R1's `እ` and the impl-flagged `ኡ` are wrong; original `ኤ` was right.

8. **APPLY R1 fix v26 `ኤርዝ`→`አርዝ`.** Fix-round-1 incorrectly classified this as a disciplined non-change; the parchment shows `አ` not `ኤ`.

9. **APPLY R1 fix v7 drop `ጻ` in `ይትመጻውሙ`→`ይትመውሙ`.** Fix-round-1 incorrectly classified this as a disciplined non-change; the `ጻ` is not on the parchment.

10. **APPLY NEW FIX v6 `ኒፋን`→`ካፋን`.** New defect found in R2; neither R1 nor fix-round-1 caught the wrong patronym leading consonant.

11. **RESTORE the v18 `እክከ ደወስ` (or similar inked) tokens between `ኤሱፍሊ` and `ግብጽ`.** The fix-round-1 implementer self-reported these but kept R1's harmonized `ሰብአ ግብጽ`. CARDINAL RULE: restore the inked tokens.

12. **RE-READ v22 `እሙንቱ እምነዊ ታሕተ ወዶዕ ሙ`** — possibly should be `ኹሎሙ ተአምነዊ ታሕተ ወዶዕ ሙ` per parchment. MAJOR — needs careful re-zoom.

---

## Method-note compliance review

- **CARDINAL RULE — VIOLATED at scale by R1 (and inherited by fix-round-1).** The `ለ`/`ስ` and `ይ`/`ደ` "systematic-misread" classes that R1 hammered on are in fact the **GG scribe's actual idiolectal readings**. R1 was harmonizing the GG witness toward standard Ge'ez and got most of those calls wrong. Fix-round-1 then trusted R1 and applied them all. The result is a JSON that is much more "standard Ge'ez" than the parchment actually inks. This is the canonical fix-round failure mode (same class as the 1ki2 R3 regression noted in memory). **Round-2 must do the opposite: trust the inked glyphs over the standard-text intuition.**

- **METHOD NOTE 2 — INTACT.** The f030v-L → f030v-M column boundary at `ወአ | ልሕምት` is correctly joined. No mid-word column-boundary word-drop.

---

## Honest notes

- **`ሰ`/`ስ` distinction throughout this scribe's hand is at the limit of my resolution.** Multiple "fix" calls in this class (both directions: impl→R1 and R1→fix-round) cannot be objectively verified. Without a same-page known-`ሰ` and known-`ስ` reference pair from a deuterocanonical caption, this is a perennial AMBIGUOUS-PARCHMENT for the GG hand.
- **`ቯ` vs `ሺ` vs `፼` in v23/v26 red marks.** The red character with leftward-curled top + 2-3 horizontals is consistent with the fidel `ቯ` (qwā). It could in principle be a scribe-particular numeric multiplier abbreviation. Without a same-hand known-`ሺ` reference in another book, I cannot definitively say. Lean: ORIGINAL was right; R1 was harmonizing.
- **`ኆ`/`ኖ` in the v24 leading noun.** The 6th-order labialised consonant is ambiguous between `ኆ` (h-labialised) and `ኖ` (n-with-rim). Either way, the noun IS inked there — R1 was right to restore it.
- **`መማግሥታት` doubled-`ማ` vs `መንግሥታት`.** Middle-glyph ambiguous between `ማ` (4th-order m) and `ን` (n). Resolution-limited. CARDINAL RULE: keep as inked; if AMBIGUOUS-PARCHMENT, lean to the original transcription.
- **The fix-round-1 SELF-REPORT discipline IS commendable** — they correctly identified 4 of the 10 disciplined non-changes (v14 `ይ`-axis, v15 `አ`-axis, v16 `ሀስው`, v25 names + `ወደራል`/`ደቂት`) and correctly flagged 1 of the 5 NEW defects (v18 `እክከ ደወስ` interlude — even though they then failed to act on it).

---

**End of R2 review.**
