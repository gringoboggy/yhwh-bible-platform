# 1Ki5 GG C-3 Round 1 review

**Date:** 2026-05-20
**Witness:** `content/manuscript/kings/calibration/1ki5_witnessGG.json` (commit pending)
**Reviewer:** independent fresh subagent, opus, isolated (no CAM, no KJV, no parallel-PDF, no other Bible witnesses; only the JSON + the 2 GG image folios + GG_topology.md)
**Source images:** `GAPS/2_Kings/GG-00106/1-Kings/1-Kings_f030v.jpg`, `GAPS/2_Kings/GG-00106/1-Kings/1-Kings_f031r.jpg`
**Method:** column-strip crops at 3x-12x LANCZOS upscale, read against the JSON. Crops created in `%TEMP%`, deleted on report.

## Verdict

**NEEDS FIX** — 5 CRITICAL defects (parchment-anchored), 4 MAJOR, 6 MINOR/AMBIGUOUS. The boundary identification (rubric `ክፍል ፡ ፳ ወ፬` opens v1, end at `ወእባነ ፡ ፫ ዓመተ` followed by `ክፍ ፡ ?` rubric introducing 1Ki6:1 `ወእምዝ ፡ አመ ፡ ኮነ`) is **VERIFIED CORRECT**. No mid-chapter scripture omission detected at column or folio turns. The defects are mostly mis-parsing of body `✣` cross-glyphs as fidel `እ`, one line-break-split mis-merge (`ጠቢበ ላዕለ` → wrong `ጠቢላሰ`), the documented `ለ`/`ስ` and `ፈ`/`ረ` family errors, and the verse-segmentation/kefl-numbering questions deferred to C-7.

## Validator / screens

- `validate_witness`: **OK**
- `non_ethiopic_screen`: **CLEAN**
- `screen_witness_for_class_failures(NARRATIVE)`: **[]** (empty)

All three pass. The defects below are NOT caught by the screens.

## CRITICAL defects (must fix this round)

### C-1. v6 — line-break-split mis-merge: `ጠቢላሰ` is `ጠቢበ ፡ ላዕለ`

- C-2 transcribed: `…ለዳዊት ፡ ወልደ ፡ ጠቢላሰ ፡ ዝንቱ ፡ ሕዝብ ፡ ብዙኅ` (and honestly-flagged `ጠቢላሰ` as suspicious vowel-variant of `ጠቢበ`).
- **Parchment** (f030v-R, lines around y=1040-1080 in raw px): the word ends `ጠቢበ` at the end of one line, the next line begins `ላዕለ ፡ ዝንቱ ፡ ሕዝብ ፡ ብዙኅ`. The two-token `ጠቢበ ፡ ላዕለ` = "wise over" is the correct reading. C-2 conflated the line-break-split `ጠቢበ|ላዕለ` into one mis-token `ጠቢላሰ` AND dropped the `ላዕለ` token entirely.
- This is the exact **METHOD NOTE 2 / topology §4 line-break failure class** previously documented in 1Ki4 (`ሳዕ | ለ` → mis-merged `ሳቦሉ`).
- **Fix**: replace `ጠቢላሰ` token with `ጠቢበ`, insert `ላዕለ` token between `ጠቢበ` and `ዝንቱ`. Update geez accordingly. Remove the corresponding "vowel-variant" uncertain entry.

### C-2. v7-v8 — systematic `✣` body-cross mis-parsed as fidel `እ`

C-2 systematically misread the small red body `✣` (per topology §1 inline phrase divider) as the fidel `እ` when it appeared word-finally adjacent to a 2nd-person-suffix `ከ`. Confirmed parchment instances:

- **v7** `ዘበእ ፡ ከእ ፡ ዓቢየ` — parchment reads `ዘበእከ ✣ ዓቢየ` (one word `ዘበእከ` + body cross).
- **v7** `መፍቅደ ፡ ከእ ፡ ዕወወለ` — parchment reads `መፍቅደከ ✣ ዕወወ ፡ ዘድሮና` (one word `መፍቅደከ` + body cross; also note `ዕወወለ` mis-token, see C-3).
- **v7** `እምሊባኖስከእ ፡ እስከ ፡ ባሕር` — parchment reads `እምሊባኖስከ ❈ እስከ ፡ ባሕር` (or `፡`); the trailing `እ` is a body cross, not a fidel.
- **v8** `ፈቀደከእ ፡ ወትህብሊ` — parchment reads `ፈቀደከ ❈ ወት ፡ ህብ` (or `ወትህብ ✣`); the trailing `እ` is a body cross. Also `ወትህብሊ` itself is dubious (see MAJOR-v8 below).

C-2's transcription_notes admits topology §1 ("small red marks appear between every word position … read as ፡ wordspace") — but this conflates the wordspace `፡` (small black two-dot) with the DISTINCT RED `✣` body cross. The `✣` is a real glyph that should appear in the geez string, not collapsed to `፡` and not promoted to `እ`. C-2 did BOTH — collapsed some, promoted others, inconsistently.

- **Fix**: remove every `ከእ`/`ኖስከእ`/`ደከእ` token-split; reattach the `ከ` suffix to the preceding word; insert `✣` (U+2723) in the body geez where the body cross genuinely appears. The witness validator already whitelists `✣`. Do NOT use `❈` in body (topology §1 says `❈` is rubric-only).

### C-3. v7 — multiple words mis-spelled (`ዕወወለ`, `ዕወወ` should resolve)

- C-2 wrote `ዕወወለ ፡ ዘድሮን ፡ ዘፀውቄና` (honestly-flagged: "cedars + cypress (?) — token spellings differ from standard").
- **Parchment** (f030v-R after the v7 `ዘበእከ` line): I read `ዕወወ ፡ ዘድሮና ፡ ዘጸውቄና` — three tokens not two. The C-2 `ዕወወለ` is actually `ዕወወ ✣` (with the trailing `ለ` being a body-cross artifact) followed by `ዘድሮና` (4th-order `ና`, not 1st-order `ን`).
- Same C-2 ambiguity in v9: `ዕወዕ ፡ ቀድሮና ፡ ወጸውቄና` — the `ዕወዕ` form is different from v7's `ዕወወለ`/`ዕወወ`; need parchment re-check that both forms genuinely differ on parchment OR were mis-read.
- **Fix**: re-parse v7 tokens accordingly. Note: `ዕወወ`/`ዕወዕ` (=cedars) and `ዘድሮና`/`ቀድሮና` may be legitimate scribal variants — flag as orthographic uncertainty, NOT push toward printed-Bible form.

### C-4. v10 — `ስኪራም` (×2) is parchment `ለኪራም` (`ለ`/`ስ` topology family)

- C-2 transcribed `ወሰሎምን ፡ ወሁ ፡ ስኪራም ፡ ፳ ሺ ፡ …` and `…ወሁቦ ፡ ሰሎምን ፡ ስኪራም ፡ በበ ፡ ዓመት`.
- **Parchment** (f030v-R bottom lines): the word-initial character before `ኪራም` is the **wide-topped curved `ለ`**, NOT the narrower `ስ`. This is the topology §2 `ለ`/`ስ` family — exactly the documented failure class.
- The standard Ge'ez "to Hiram" is `ለኪራም` (la-Kiram). C-2's `ስኪራም` is non-idiomatic.
- **Fix**: both `ስኪራም` instances → `ለኪራም`. This is consistent with 1Ki4 R1's `ፈስግ`→`ፈለግ`, `እስ ይስሕቡ`→`እለ ይስሕቡ`, `ጋላዓድ`→`ጋላዓድ` corrections.

### C-5. v12 — `ወረነወ` first character is `ፈ`, not `ረ` (parchment-anchored)

- C-2 transcribed `ወረነወ ፡ ሰሎምን ፡ ንጉሥ ፡ ዐወርት` (honestly-flagged: "ወረነወ may be ወፈነወ (and he sent) with ፈ→ረ variant. Not corrected — transcribed as written.").
- **Parchment** (f031r-L, line just after the v12 kefl-30 rubric): hyper-zoom (12x LANCZOS) of the first word shows the second fidel has the distinctive **two-loop / two-curl glyph structure of `ፈ` (fa)**. The character `ረ` (ra) is a single vertical with a small loop at the bottom — completely different glyph. The parchment unambiguously inks `ፈ`.
- C-2's "transcribed as written" judgement is **wrong**: the parchment is NOT what C-2 inked. This is a straightforward mis-read, not a scribal variant. (Note: `ፈ`/`ረ` is NOT in topology §2's documented confused-fidel families — this is a new class for the topology to absorb.)
- **Fix**: `ወረነወ` → `ወፈነወ`. Remove the corresponding "ፈ→ረ variant" uncertain entry.

## MAJOR defects (fix this round but lower urgency)

### M-1. v1 — first word `ወፈነወሙ` likely `ወፈነሙ` (one character shorter)

- C-2 transcribed `ወፈነወሙ ፡ ኪራም ፡ ንጉሠ ፡ ጢሮስ`.
- **Parchment** (f030v-M, after the v1 kefl-24 rubric): hyper-zoom (8x LANCZOS) shows what appears to be `ወፈነሙ` — 4 characters: `ወ`-`ፈ`-`ነ`-`ሙ`. C-2 has 5 characters with an extra `ወ` between `ነ` and `ሙ`.
- **HONEST CAVEAT**: this may be resolution-limited — a tiny extra `ወ` could be present and getting visually merged. AMBIGUOUS-PARCHMENT pending re-check.
- **Fix**: re-read at higher zoom. If parchment confirms `ወፈነሙ`, drop the phantom `ወ`; if `ወፈነወሙ`, keep as-is and add an `uncertain` entry with marker `damaged` and note about merge.

### M-2. v1 — `ንጉሠ` may be `ንጉሥ` (vowel-order topology micro-confusion)

- C-2 transcribed `ንጉሠ ፡ ጢሮስ` (= "king of Tyre" with 1st-order final `ሠ`).
- **Parchment** (f030v-M, line 1 of v1): the final character of `ንጉሠ` looks like `ሥ` (sa-6th-order) with a small descender — not the broad-topped 1st-order `ሠ`. Standard absolute-state form in Ge'ez is `ንጉሥ` (`ሥ` 6th-order); construct state would be `ንጉሠ`.
- This is the topology §2 `ሰ`/`ስ` Solomon-family micro-confusion (1st vs 6th order).
- **Fix**: re-read; if parchment shows 6th-order `ሥ`, correct to `ንጉሥ`. Mark this as AMBIGUOUS-PARCHMENT and surface in transcription_notes.

### M-3. v6 — first word `ወከበ` likely `ወሶበ`

- C-2 transcribed `ወከበ ፡ ሰምዓ ፡ ኪራም ፡ ቃለ ፡ ሰሎምን`.
- **Parchment** (f030v-R, line just after v6 kefl-26 rubric): the second character of `ወከበ` looks like it could be `ሶ` (sa-7th-order, "and when") — the standard Ge'ez idiom is `ወሶበ ፡ ሰምዓ` ("and when he heard"). C-2's `ወከበ` is non-idiomatic.
- This may be `ሶ`/`ከ` confusion — not in topology §2 explicitly, but compatible with the `ኆ`/`ኁ` micro-confusion class.
- **HONEST CAVEAT**: resolution-limited; could be `ወከበ` if MS uses unusual variant.
- **Fix**: re-read; mark AMBIGUOUS-PARCHMENT and surface in transcription_notes if not confidently resolved.

### M-4. v8 — `ወትህብሊ` likely `ወትህብ ✣ ሊ?` or similar

- C-2 transcribed `…ፈቀደከእ ፡ ወትህብሊ ፡ ሲሳየ ፡ ለስብእ ፡ ቤትየ`.
- **Parchment** (f030v-R, end of v8 region): `…ፈቀደከ ❈ ወት ፡ ህብ ፡ ሲሳየ ፡ ለሰብእ ✣ ቤትየ` — I see `ወት` then space then `ህብ` then space then `ሲሳየ`. C-2 conflated `ወት ፡ ህብ` (or `ወትህብ ✣`) into single `ወትህብሊ` — adding a phantom `ሊ` (or absorbing a body cross as `ሊ`).
- **Fix**: re-parse word boundaries. The expected idiom `ወትህብ ፡ ሲሳየ` ("and you give food") suggests `ሊ` is spurious. Surface as AMBIGUOUS-PARCHMENT.

## MINOR / NIT defects (optional this round)

### N-1. v2 — `ተከእና` likely `ተከ + ✣ + ና?` (`ከእ` mis-parse class C-2)

- C-2 has `ከመ ፡ ተከእና ፡ ነዲቅ` in v2. Same `✣`→`እ` mis-parse pattern as C-2 defect.
- Re-check parchment; word boundaries likely differ.

### N-2. v11 — `ጥበባ` may be `ጥበበ` (vowel-order topology micro-confusion)

- C-2 transcribed `ለሰሎምን ፡ ጥበባ`.
- **Parchment** (f031r-L, line 3-4 of L column): the word `ጥበ` ends one line; next line begins with `በ` (1st-order ba, not 4th-order `ባ`). So parchment reads `ጥበበ` (1st-1st-1st), not `ጥበባ`.
- Topology §2 `ቤ`/`በ` micro-confusion family. Both forms mean "wisdom" — minor.
- **Fix**: optional; surface in transcription_notes as a vowel-order honest variant.

### N-3. v8 — `እለ ፡ ህየ` may be `እ ✣ ህየ`

- C-2 has `ወእነብር ፡ እለ ፡ ህየ` in v8. Parchment shows `ወእነብር ፡ ለ` at end of one line, `እ ፡ ህየ ❈` at start of next. So `እለ` is split across line-break OR there's a body cross between `እ` and `ህየ`.
- **Fix**: re-parse line-break; surface if ambiguous.

### N-4. v2 — `አበ ፡ ሰምዓ` and v2 `ዕሊከ` non-idiomatic

- v1 has `አበ ፡ ሰምዓ ፡ ከመ ፡ ቀብዕም` ("father heard that they anointed [him]"). The standard idiom would be `እስመ ፡ ሰምዓ` ("because he heard"). C-2's `አበ` may be an alternate-spelling but is non-standard. Resolution-limited.
- v2 has `ተአምር ፡ ዕሊከ ፡ ለዳዊት ፡ አቡየ` — `ዕሊከ` is non-standard (should be `ለዐልየ`/`ላዕለ`/`ዕለ`). Resolution-limited.
- Both are AMBIGUOUS-PARCHMENT honestly. Surface in transcription_notes.

### N-5. v5 — `እምሊባናስ` should likely be `እምሊባኖስ` (a/o vowel)

- C-2 has `እዕወዕ ፡ እምሊባናስ` in v5 (the cedars from Lebanon).
- Parchment likely shows `እምሊባኖስ` (7th-order final `ኖስ`, the standard form). C-2's `ሊባናስ` has 4th-order `ና` instead of 7th-order `ኖ`. Topology vowel-mark micro-confusion.
- But note v7 has `እምሊባኖስ` (with 7th-order `ኖ`) AND v12 has `ሊባናስ` (4th-order `ና`) — internal inconsistency in C-2's own transcription suggests a real parchment variation OR systematic mis-read. Flag both for re-check.

### N-6. v8 — `ለስብእ` may be `ለሰብእ` (`ሰ`/`ስ` Solomon-family per topology §2)

- C-2 has `ለስብእ ፡ ቤትየ` in v8.
- Parchment likely `ለሰብእ` (1st-order `ሰ`). Topology §2 micro-confusion. Minor.

## AMBIGUOUS-PARCHMENT items (per METHOD NOTE 4)

C-2's transcription_notes lists 5 AMBIGUOUS items. My re-review:

| C-2 item | My verdict |
|---|---|
| `ሲደና` final ና vowel-order (v5) | ACCEPT AMBIGUOUS — defer to CAM |
| `ጠቢላሰ` as vowel-variant of `ጠቢበ` (v6) | **REJECTED** — actually a line-break-split mis-merge `ጠቢበ ፡ ላዕለ` (see CRITICAL C-1). Not AMBIGUOUS, this is a hard defect. |
| `ዕወወለ ዘድሮን` orthography (v7) | DOWNGRADE — partly mis-parse (the `ለ` is a body cross), partly genuine scribal variant. Re-parse first, then assess. |
| Oil-measures `፳ ... ወ፪ ወደነፀ` (v10) | ACCEPT AMBIGUOUS — defer to CAM |
| Three `፴` rubrics at v10/v12/v13 | ACCEPT AMBIGUOUS — likely `፴`/`፴ ወ፩`/`፴ ወ፪` with 2nd digit at resolution limit. The single-digit `፴` reading is internally consistent with the topology's "no kefl 27/28/29 between 26 and 30" observation if you assume scribal sub-numbering. Defer. |
| `ማእየወኪያሲቃን` (v13, Greek-derived?) | ACCEPT AMBIGUOUS — orthography genuinely at limit; defer. |
| 1Ki6:1 boundary rubric (v13 tail) | **VERIFIED** — parchment shows `❈ ክፍ ፡ [?] ❈ ወእምዝ ፡ አመ ፡ ኮነ ፡ [፬]፻ ወ[?] ዓመት` (the `ክፍ ፡ ?` rubric is abbreviated from `ክፍል` and DOES exist between v13 end and 1Ki6:1). C-2's claim that this rubric introduces 1Ki6:1 is correct. |

### New AMBIGUOUS items I add

- **v1 `ለደቁ`** at f030v-M line 1 of v1 body — the `ቁ` could be 2nd-order `ቱ` (vowel-order micro-confusion). Defer to CAM.
- **All three `ክፍል ፡ ፴` rubrics** at v10/v12/v13 — the SHAPE of the single-digit is consistently 3-stroke between bars (matching `፴`), but the kefl-numbering scheme (jumps 26→30, then three identical-looking `፴` rubrics) is internally inconsistent unless the MS allows kefl re-numbering OR the 2nd digit `ወ?` part is fully invisible at this resolution. Defer to CAM cross-ref + higher-res CUDL pull if available.

## Verse-count + boundary verdict

**Boundary identification: VERIFIED CORRECT.** 
- v1 starts after rubric `ክፍል ፡ ፳ ወ፬ ❈` on f030v-M (preceded by the chapter-4 tail `…እምኩሉ ፡ ነገሥት ፡ ምድር ፡ እለ ፡ ሰምዑ ፡ ጥበቢሁ ❈`).
- v13 ends with `…ወእባን ፡ ፫ ዓመት` on f031r-L mid-bottom, IMMEDIATELY followed by `❈ ክፍ ፡ [?] ❈ ወእምዝ ፡ አመ ፡ ኮነ ፡ [፬]፻ ወ[?] ዓመት` (= 1Ki6:1 opening "And it came to pass at the 480th year").

**Verse-count: 13 verses is LOW vs LXX/MT (~30-32 verses) but the kefl-segmentation matches** — the MS uses `ክፍል` (sub-section) numbering rather than verse-level numbering. C-2 expanded 6 `ክፍል` units into 13 verses by semantic inference. This is reasonable but may miss intermediate verse-breaks (the MS has no `።` end-of-sentence marks on these folios that I could identify — segmentation is inferred from `✣` body crosses + narrative shifts).

**No mid-chapter scripture omission detected.** I traced every column-bottom→column-top and folio-turn boundary:
- f030v-M (end v1 line `ኪራም ፡ ለዳዊት ፡ በኩሉ ፡ መዋዕሊሁ ❈`) → f030v-M (next line, v2 rubric)  — clean (no column turn).
- f030v-M (end v2 line: `ኪራም ፡ እንዘ ፡ ይብል ፡ ተአምር ፡ ዕሊከ`) → f030v-R (line 1: `ለዳዊት ፡ አቡየ`) — clean column-break; word boundary at wordspace, no mid-word split. C-2 correctly flagged this.
- f030v-R (end v10 line: `ቅብዓ ፡ ቅዱሰ ፡ ወ`) → f031r-L (line 1: `[፪] ፡ ወደነ[ፀ] ❈ ወከመዝ`) — clean folio-turn; `ወ` ends f030v-R and `፪` begins f031r-L. The numeral `፪` is at the column-start (potentially METHOD NOTE 2 risk!) but the joined reading `ወ ፡ ፪ ፡ ወደነፀ` is what C-2 has and matches the parchment.

**No verse-count downgrade defects detected. The 13-verse count is C-2's reasonable semantic segmentation choice, not an omission defect.**

## Round-bound escalation check

For the controller `escalate_if_unbounded(chapter_class="NARRATIVE", current_round=1, hard_defects=N_open, new_ambiguous=N_new_ambig)`:

- **hard_defects (CRITICAL + MAJOR)**: 5 + 4 = **9 open**
- **new_ambiguous (items I added beyond C-2's 5)**: 2 (`ለደቁ` 2nd-order; the three `፴` rubrics taken as a single bounded class)

NARRATIVE class typically converges in 1-3 rounds with ≤20 defects per round. 9 hard defects R1 is on the lower-tail of typical R1 counts (1Ki4 R1 had 110+); this should converge in R2 if the fix-round agent is thorough.

## Topology updates (if any newly-confirmed facts to APPEND to GG_topology.md after approval)

After APPROVE these would be APPENDED to `_reviewer_context/GG_topology.md` §2 and §5:

### Append to §2 (confused fidel families)

- **`ፈ`/`ረ` family** — NEW class confirmed by 1Ki5 R1 defect C-5. The `ፈ` (fa, 1st-order) has a distinctive **two-loop / double-curl top** structure; `ረ` (ra, 1st-order) has a **single vertical with a small bottom loop**. The transcriber mis-read `ፈነወ` ("sent", verb stem) as `ረነወ` on the page (1Ki5 v12). **Confirmed instance**: 1Ki5 GG v12 `ወፈነወ` ("and he sent") was transcribed `ወረነወ`; corrected after parchment re-read at 12x.

### Append to §1 (cross-glyph protocol)

- **Body `✣` cross adjacent to 2nd-person-suffix `ከ`** — high-risk mis-parse class. A word ending in `…ከ` followed by `✣` (`<word>ከ ✣ <next>`) is routinely mis-parsed by the transcriber as `<word> ፡ ከእ ፡ <next>` (split at the `ከ`, with the `✣` promoted to fidel `እ`). **Confirmed instances**: 1Ki5 GG v7 `ዘበእከ ✣`, `መፍቅደከ ✣`, `እምሊባኖስከ ✣`; v8 `ፈቀደከ ✣`. Re-verify every `ከእ` token at C-3 against the parchment — most are `ከ ✣` mis-parses.

### Append to §5 (resolution-limited classes)

- **`ክፍል ፡ ፴` vs `ክፍል ፡ ፴ ወ?` rubrics** — when a rubric has a single visible digit `፴` followed by a large empty space, the `ወ?` second-digit-part may be invisible at JPG resolution OR genuinely absent. Discriminated only by CUDL hi-res pull OR CAM collation. (Surfaced by 1Ki5 R1 v10/v12/v13 all reading `ክፍል ፡ ፴ ❈` with three consecutive identical rubrics — unusual unless `ወ፩`/`ወ፪` invisible.)

## Spot-check rigor log (rounds C-2 may want to know I did)

- Validated witness JSON via the 3 standard scripts: OK / CLEAN / [] (no defects caught at the screen layer — all defects below are vision-resolved).
- Generated 30+ cropped images at 3x-12x LANCZOS upscale to verify: f030v full + 6 column strips of M + 6 column strips of R + 6 column strips of f031r-L + 8 hyper-zoomed rubric/digit crops + 6 hyper-zoomed word crops.
- Verified all 6 chapter-internal rubrics on parchment (kefl 24, 25, 26, 30, 30/31?, 30/32?) with digit-level zoom.
- Traced every column-break and folio-turn against C-2's word-split claims.
- Cross-checked C-2's 13 `uncertain[]` entries against the parchment, sustaining most and rejecting one (the `ጠቢላሰ` → CRITICAL C-1 line-break).
- Examined the topology's documented confused-fidel families systematically against each verse's high-risk word positions.
- Crops created in `%TEMP%` (per harness rule, OS temp dir outside repo). **Deleted before return.**
