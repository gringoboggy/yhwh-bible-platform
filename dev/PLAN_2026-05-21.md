# Master plan + re-audit — 2026-05-21

**Supersedes `dev/PLAN_2026-05-09.md`** (that plan was structured around the
path to v1.0 and is now stale — it cites 1,371/51,394 notes and a pre-recovery
build). This is the re-arranged forward sequence, ordered for productivity, with
a current health re-audit. Companion: `dev/CLAUDE_PROJECT_RULES.md` (rules),
`dev/MATRIX_MAP.md` (data-flow), `dev/IN_FLIGHT.md` (live tracker).

---

## 0. Bootstrap pointer

```
1. dev/CLAUDE_PROJECT_RULES.md   rules + §9 mental models
2. dev/SESSION_STATE.md          live snapshot (trimmed 2026-05-21)
3. dev/PLAN_2026-05-21.md        THIS FILE — sequence forward
```
Optional: `dev/IN_FLIGHT.md`, `dev/MATRIX_MAP.md`, `dev/CHANGELOG.md`.

---

## 1. Re-audit — current health (verified 2026-05-21)

| Dimension | State | Evidence |
|---|---|---|
| **Corpus** | 67,715 notes · 71 kinds · 15 categories · 87 books | `validate_taxonomy` 100% attributed, schema sound |
| **Build / inject** | **99.76% placed** (66,173 + 1,381 / 67,715); 161 residual | `inject --all-books`; valid EPUB via `build_one` smoke (24 tests) |
| **Integrity** | `ebible verify` **errors=0 / 24,015 paired**; `trace_matrix` **0 unresolved refs** | this session |
| **Coordinate quality** | **0 invalid-coordinate notes** corpus-wide; guard at promote boundary | `audit_base_html` + `scope` scan |
| **Lint / format** | ruff-format clean (606 files); `lint_rules` 12·2·0 | pre-commit, both commits |
| **Git** | clean tree at `a935701` (3 commits this session: `d3acc4f`, `38b80d3`, `a935701`) | `git status` |

**Verdict:** the demo build is solid and the corpus is the deepest + cleanest it
has been. No build/matrix/integrity defects open. The work ahead is *additive*
(more content, the paused manuscript marathon) + *editorial/render* residual +
low-risk cleanup.

---

## 2. North star — unchanged

The **builder demo** (CLAUDE_PROJECT_RULES §1): open `/wizard`, pick an edition,
step the cards, BUILD → a themed EPUB downloads with the chosen notes + verse
popups. Free public app (CC0; no ISBN/sales). **Hard deadline 2026-06-07.**

---

## 3. Tracks — re-arranged for productivity

Ordered so the highest-leverage, deadline-relevant, lowest-risk work comes first.
Each track is independent; pick by session budget.

### TRACK A — DEMO READINESS (priority: deadline) — mostly DONE
- ✅ Build pipeline live; 99.76% inject; valid EPUB; verify clean.
- **[USER] Visual QA pass** — open built EPUBs in a browser + an e-reader; spot-check
  popups, themes, note markers, TOC. The one thing I can't self-verify.
- **[OPTIONAL] epubcheck** — needs Java; opt-in `ebible build --epubcheck`. Cert only.
- Residual 161 notes are base-HTML coverage, NOT demo-blocking (see Track D).

### TRACK B — MANUSCRIPT MARATHON (the other major track — PAUSED)
The Kings/Samuel Geʽez dual-witness collation + the two standalone Bibles.
- **Resume point:** `1ki5 R2` spec-compliance review (held); then `1ki6`, `1sa2`,
  onward (`1ki:5-22` + `2ki:1-25` ≈ 43 ch; Samuel similar). Per
  `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
- **Constraints:** HEAVY work → **script-based** (`run_manuscript_*_at_scale.py`,
  NOT agents — retires the OOM crash class), single-chapter, user check-in cadence
  (`feedback_marathon_pacing` + `feedback_concurrent_agent_cap`).
- **Then:** constitute the two standalone Geʽez/Amharic Bibles (τ.G) — their own
  base HTML + English back-translation in their own popups (SCOPE_2026-05-16).

### TRACK C — CORPUS EXPANSION (opportunistic; user-fed)
Reusable pipeline (`dev/MATRIX_MAP.md` "Reference-corpus ingestion"): clean CCEL
PDF → `extract_<work>_ccel.py` → validated index → `batch_insert_notes`.
- ✅ Nave's (rebuilt), Easton's (added).
- **Candidates** (need the user to supply a clean source): Matthew Henry / JFB /
  Barnes commentary; Torrey's New Topical Textbook; Vincent's Word Studies.
- Opportunistic depth — NOT the deadline north-star.

### TRACK D — CLEANUP / UPGRADES (low-risk, do between bigger work)
Done this session: ✅ SESSION_STATE trimmed (896 KB → lean) · ✅ matrix.py docstring
(71 kinds / 67,715) · ✅ coord guard · ✅ dead-injector dispatch repointed.
Open:
- **Editorial note-key review** — the coord guard now blocks *out-of-range* coords,
  but *in-range* mis-bookings from OCR'd sources are undetectable without a clean
  re-extract. Low stakes (topic/dict notes are reviewer drafts). Optional pass.
- **1 Enoch 37-108 render gap** (~25 residual notes) — flagship Tewahedo book
  truncated at ch36 in the base; rendering needs a 1En source (ties to Track B).
- **aes WEB↔KJV concordance** (73 residual) — Additions-to-Esther versification
  re-key; editorial, do not guess. See `dev/AUDIT_2026-05-21-inject-tail-residual.md`.
- **matrix vestigial ψ.35 layering** (MATRIX_MAP finding #4) — `Matrix.enabled/
  potential/per_book` are derived projections; a slice could drop them for
  `@cached_property`. Pure refactor; behavior-neutral.
- **editions.yaml comment drift** (MATRIX_MAP finding #2) — section headers caption
  the wrong edition's trailing block. Cosmetic.
- **ruff-check lint debt** (~88 in `test_scripts.py` etc.) — NOT gated by pre-commit
  (which runs ruff-format + lint_rules only); auto-fixable subset can be `ruff --fix`'d.

### PARKED — design call needed (do not implement unscoped)
- Strategy-B non-spill irregular layouts (mq1/2/3, sir, jub; ~98 residual) — need a
  per-book multi-file verse index or a base re-render; larger effort.
- Anything from the old `PLAN_2026-05-09.md` LONG/PARKED tracks still relevant
  (most SHORT/MEDIUM phases there already shipped; verify against git before scoping).

---

## 4. Sequencing recommendation (pick by session budget)

- **Short / unattended:** Track D cleanups (each is bounded, verifiable, low-risk).
- **Medium, demo-focused:** confirm the demo end-to-end (build each edition, smoke
  the EPUBs) + tee up the [USER] visual-QA checklist.
- **Long, content-focused:** resume Track B (one chapter, script-based, check-in) OR
  Track C (if the user supplies a new clean source).
- **Always:** verify before commit (`ebible verify` errors=0 + build smoke + ruff +
  lint_rules); "continue" ≠ "save"; don't break the tree.

---

## 5. What changed vs PLAN_2026-05-09

That plan's RELEASE/SHORT/MEDIUM tracks (ψ.*, ν.*, ω.* console/template/matrix
phases) are overwhelmingly **shipped**; its corpus-floor framing (35-40K target,
51,394 actual) is obsolete (now 67,715). The live forward work collapsed to the
four tracks above. The old plan is retained in `dev/` for phase-history only.
