# Project audit — 2026-05-16 DEEP-5 (parallel-subagent sweep, post-τ.6.x.1.E + τ.6.x.5-T1-3 + brainstorm/spec/plan arc)

**Trigger:** user "major audit -> fix -> commit" after a very large
session arc: τ.6.x.1.E parser hardening + honesty gate shipped; the
full brainstorming→spec→plan superpowers flow (colometric-merge
spec, external-source spec, implementation plan); τ.6.x.5 plan
Tasks 1-3; a mid-session honest correction (the earlier "NT
unblocked / small fix" framing was refuted by a real Matthew
dry-run); memory + ledger rewrites. ≥10-phase arc → the DEEP
(parallel-subagent) variety per memory `feedback_audit_cadence`.

**Form:** DEEP-class — 3 parallel adversarial investigation
subagents: (A) code & test integrity (independent full regression
+ ruff + lint, Fix A/B/C + parser + write_book_module scrutiny,
weak/tautological-test hunt), (B) documentation & honesty-claim
audit (every ledger/spec/plan/memory for residual over-claims +
contradictions + staleness — does the "NT honestly blocked, NOT
fixed" position hold EVERYWHERE), (C) git & repo hygiene (per-
commit scoping, secrets/binaries, .gitignore, CRLF-benign).
Investigation-only; this pass synthesized + fixed + committed.

---

## 0. TL;DR

**NOT a "clean" pass — the audit did its job and caught real
findings, including over-claims THIS SESSION introduced (the
honesty system working as designed).** Git/hygiene: **CLEAN** (all
7 session commits correctly scoped, no conflation, no secrets, the
193 MB PDF + hacohen cache untracked, CRLF benign). Code: Fixes
A/B/C + the HaCohen parser are sound; one genuine over-claim + one
regression-breaker + doc/memory contradictions. **9 findings
(1 CRITICAL, 4 HIGH, 2 MEDIUM, 2 LOW) — ALL FIXED this pass.**

## 1. Findings → fixes

**F-DEEP5-1 (CRITICAL) — memory preserved a refuted over-claim.**
`feedback_reverify_conservative_nogo.md` stated the NT cause is
"~2 filter rules + one regex character" and "unifies with Ge'ez-
Psalms" as settled fact, with NO mention that the Matthew dry-run
(1178→1117, still >1071) **refuted** the "resolves NT" framing.
A persistent-memory over-claim (outlives the session) — the exact
failure the file's own rule warns against.
→ **FIXED:** rewrote the NT bullet + the How-to-apply line: A+B
*reduce* but do NOT resolve NT; Fix C = honest hard-fail; NT
honestly blocked, NOT "fixed"; added the double-lesson (re-verify
your OWN optimistic re-scope, not just the prior pessimistic one).

**F-DEEP5-2 (HIGH) — "byte-identical" over-claim.**
`write_book_module` is NOT byte-identical for default callers: the
provenance docstring line generalized ("Extracted from the
parallel-Bible EOTC PDF (" → "Extracted/ingested from source (").
The Task-2 commit message + spec/plan said "byte-identical"; the
test `test_defaults_byte_identical` only asserted 3 surviving
substrings (weak/misnamed — would pass even though output differs).
Data constants + VERSES ARE unchanged; existing committed modules
are static (not regenerated).
→ **FIXED:** test renamed `test_defaults_preserve_data_and_
provenance`, strengthened to pin data/provenance preservation AND
the one intentional generalized line (explicitly NOT byte-
identical); colometric §1 + external-source spec wording corrected
(see F-DEEP5-6). Commit `8a0ed7f`'s message is immutable history —
this audit doc is the correction-of-record (project mechanism).

**F-DEEP5-3 (HIGH) — τ.6.x.1.E phase-tag collision in PLAN.**
PLAN had the shipped `τ.6.x.1.E Structure-aware parser hardening
✓` AND a stale old `τ.6.x.1.E Truncated-keyword chapter recovery
⬜ OPTIONAL` — two definitions, the pending one falsely implying
τ.6.x.1.E is unshipped.
→ **FIXED:** the old OPTIONAL item renumbered τ.6.x.1.E→τ.6.x.1.F
(it is a DISTINCT residual — keyword truncation, not the `!`/`|`
terminator Fix A handles) with an explicit repurpose note.

**F-DEEP5-4 (HIGH) — full regression 1 failed.**
`test_parallel_bible_tau7xa.py::test_in_flight_idle_after_pilot`
asserted `TRACKER-STATE: idle`; it is legitimately `active`
mid-τ.6.x.5 (CLAUDE_PROJECT_RULES §4 — active is correct for a
checkpoint). A brittle share-pin the same test class had already
milestone-converted for its siblings.
→ **FIXED:** converted to `test_in_flight_has_valid_tracker_state`
— the durable invariant (a valid marker, idle OR active) per
`feedback_share_pin_pattern`. Forcing IN_FLIGHT to "idle" mid-task
would have been the DISHONEST option (rejected).

**F-DEEP5-5 (MEDIUM) — PLAN τ.6.x.2.i lines stale.**
`τ.6.x.2.i-z` / `τ.6.x.2+` still said "blocked … natural next once
τ.7.x.i validated" with no τ.6.x.5/Option-C/HaCohen mention.
→ **FIXED:** updated to UNBLOCKED + τ.6.x.2.i RE-ROUTED to τ.6.x.5
external-source; narrative books continue parallel-PDF; colometric
retained as fallback.

**F-DEEP5-6 (MEDIUM) — colometric spec body vs its own banner.**
§1/§9/§10 still framed the spec as the active Ge'ez-Psalms path
("shippable", "unblocks τ.6.x.2.i") and §1's "verified" line
omitted the honest reduce-not-resolve qualifier — contradicting
the SUPERSEDED-as-primary banner.
→ **FIXED:** §1 gained the honest A+B-reduce-not-resolve / NT-
honestly-blocked qualifier + RETAINED-FALLBACK framing + "Ge'ez
Psalms is NOT this path (ships from τ.6.x.5)"; §9/§10 reworded to
fallback-role-only.

**F-DEEP5-7 (LOW) — MEMORY.md index gaps.** Missing the
`feedback_reverify_conservative_nogo` pointer (20 files / 19
indexed); the GAPS-folder hook was stale ("Patrologia still
pending", "open Samuel/Kings").
→ **FIXED:** pointer added (honest hook, no over-claim); GAPS hook
updated (Patrologia present, Samuel/Kings calibration UNBLOCKED).

**F-DEEP5-8 (LOW) — dead import.** `from pathlib import Path` in
`scripts/ingest_hacohen.py` unused (Task 3 parser is re+html only;
Task 4 re-adds it).
→ **FIXED:** removed (Task 4's plan step re-adds the imports it
needs).

**F-DEEP5-9 (LOW) — over-stating test name.**
`test_verse1_is_blessed_is_the_man_unicode_geez` pins structure,
not content (fixture is a tiny hand-trim per spec §8).
→ **FIXED:** renamed `test_verse1_structure_and_unicode_decode`
with an honest comment.

## 2. Verified clean (no fix needed)

- **Git/hygiene (subagent C):** working tree clean; all 7 session
  commits correctly scoped (no phase conflation; doc-only commits
  doc-only; τ.6.x.1.E not bundled with τ.6.x.5); commit messages
  match diffs; NO commit claims "NT unblocked/fixed"; the 193 MB
  source PDF + `hacohen-geez/cache/` untracked & gitignored; no
  secrets/binaries/junk; `_source.yaml ingest_records: {}` + no
  premature `_meta`/stats bump; CRLF on ledgers is benign LF-
  normalization (real content changes not masked).
- **Fix A/B/C (subagent A):** chars/regex/threshold correct; the
  Fix C threshold has no off-by-one (Matthew 1117 vs 1071 → 46 >
  21 tol → raises, correct); characterization tests genuinely pin
  the behaviors (not tautological); no code-level over-claim.
- **HaCohen parser (subagent A):** matches the real HTML structure
  (spec §2/§4); verse-span strip / continuation-cola join / entity-
  decode / superscription-skip all correct.
- **SESSION_STATE / CHANGELOG / IN_FLIGHT / external-source spec +
  plan / reference_gaps_folder.md (subagent B):** consistent &
  honest — all say "NT honestly blocked, NOT fixed"; the demoted
  τ.7.x.v block is acceptably historical under the ✅-RESOLVED
  banner; IN_FLIGHT "executing the τ.6.x.5 plan" is accurate.

## 3. Verification (post-fix)

- **ruff-format:** clean (the 3 touched Python files already
  formatted).
- **lint_rules:** clean (confirmed by this commit's pre-commit
  hook — `[pre-commit] ... lint_rules.py ... ok`).
- **Full regression (post-fix):** **5867 passed, 1 skipped,
  0 failed** (641s). The pre-fix run was `1 failed / 5866 passed`
  (F-DEEP5-4); post-fix is `0 failed / 5867 passed` — the
  converted pin turned green and the renames + strengthened test +
  dead-import removal introduced ZERO new breakage. Count delta
  +1 pass / -1 fail, no skips changed.

## 4. Carry-forward

None new. Prior carry-forwards (F-DEEP3-2 CRLF partial-by-design,
the deferred editions_path refactor) untouched — out of scope for
this arc. The τ.6.x.5 plan (T4-T8) resumes after this audit
commit; the NT-forward decision (deeper NT-structure work vs
external-source NT) remains flagged, not blocking.
