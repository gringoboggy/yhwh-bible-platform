# IN_FLIGHT — rotated journal archive

> Older `> **➤➤➤` journal entries rotated out of the live `dev/IN_FLIGHT.md` by
> `scripts/rotate_truth_records.py` to keep the always-read bootstrap lean.
> Newest archived batch first. Full history; not read at session start.

<!-- BATCHES (newest first) -->

<!-- archived: 8 entries, 2026-06-24..2026-06-25 (rotate_truth_records.py) -->

> **▶ 2026-06-24 (Windows, autonomous Mac-helping session) — IN PROGRESS.**
> **★ NEW PROGRAM (2026-06-24, user-triggered autonomous) — Kobo deep-audit: `dev/audit/kobo-deep-audit-program-2026-06-24.md` (READ FIRST).**
> **▶▶▶ WS1 mid-verse fix ✅ DONE+VERIFIED both lanes (62→0, epubcheck 0/0/0/0, staged; Mac `39799498` PASS incl. byte-stability eink-gate).**
> **▶▶▶ WS2 note-cascade de-dup ✅ LANDED+BUILD-VERIFIED (2026-06-25, WIN, `8115876f`).** Per Mac's `note-redundancy-findings.md`:
> Class 1 — drop the per-note leaf `<a class="note-sym">` in the grouped `_emit_cascade_sections` (vn-cat-head shows the glyph once);
> Class 2 — strip `xref-citation` "Cross-references." + `text-witness` "Manuscript witness." body lead-ins in
> `_strip_redundant_body_boilerplate` (exact-kind, s1_dedup-gated). **Built catholic-study eink: leaf note-sym 0 · xref lead-in 0 ·
> text-witness lead-in 0; survivors intact (10,717 vn-cat-sym headers · 12,994 vn-item leaves · 4,218 xref groups).** 6 TDD pins +
> 96 cascade green. Deliberate grouped re-baseline (s1_dedup editions). ⏳ epubcheck (running) · ⏳ Mac cross-OS byte-diff (handed off).
> **NEXT (WIN):** WS1 mixed-translation (18 ¶ + co-located KJV brackets → WEB; source CACHED `content/translations/sources/web/eng-web_vpl.txt`)
> → then WS3 popup (Mac research). **Discrete Prayer-of-Azariah ToC ✅ (`83391827`).**
> **▶▶▶ WS3 Kobo run-on popup fix ✅ LANDED+BUILD-VERIFIED (2026-06-25, WIN).** Per Mac's
> `dev/audit/kobo-popup-formatting-research.md`: the study/cascade `verse-notes` popups separated category heads /
> source bylines / note rows ONLY with hidden U+2028 `.vn-sep` spans, which Kobo's tag-stripped Footnote overlay
> DROPS → the run-on. Gave the family the translation-family's device-proven chrome — visible `·` (U+00B7) +
> `<br class="kobo-vn-br">`, **EINK-ONLY**. New eink constants `_VN_SEP_{ITEM,CAT,BYLINE}_EINK` + a `br.kobo-vn-br`
> rule in `_EINK_READER_CSS`; threaded a kw-only `eink=False` through `_emit_cascade_sections` /
> `_badge_aside_inner_to_row` / `_chunk_vn_item_row` + the budget-pack chain (`_chunk_row_to_budgets` /
> `_split_popup_units`) + the backmatter-glossary chain (`_study_glossary_category_body` / `_study_glossary_footnote` /
> `_emit_backmatter_glossary_inner`) + 4 main-loop call sites (`eink=eink_target`). **TDD 9 pins**
> (`tests/test_ws3_popup_separators.py`, incl. default-path byte-stability) + **224 caller tests green**
> (popup_split / marker_style / note_rehaul / ws2_cascade / marker_badge_style / kobo_device_qa). **Built catholic-study
> eink: kobo-vn-br 22,897 · visible-middot cat-heads 10,717 · bylines-with-break 9,903 · 0 stale hidden cat-heads ·
> 12,994 leaves; epubcheck 0/0/0/0.** **Non-eink build: 0 kobo-vn-br, hidden `.vn-sep` separators retained** (eink-gated
> by construction → 9-KJV byte-stable). ⏳ **Mac cross-OS byte-diff + the device A/B** (user gate: `·` renders in Cardo,
> worst-case unit POPs not crashes) — handed off (`dev/LANE_HANDOFF.md` P6 + the WS3-implemented section).
> **▶▶▶ Flagship-eink build OOM — tier-1 frees DONE (2026-06-25, WIN), per Mac's P1 profile.** `del pre_badge_texts`
> (~131 MB) + `del repair_texts` (~16 MB) + `stats.pop("_study_backmatter_entries", None)` (~489 MB, the biggest) +
> docstring fix (73 MB → ~480 MB). All determinism-neutral (free-after-use; grep-verified no downstream read).
> **Byte-identical PROOF: catholic-study eink rebuilt → 453/453 entries identical vs the pre-free build, zero drift.**
> ~636 MB of the ~2.9 GB peak freed. **▶▶▶ #1 STREAMING DONE (2026-06-25, WIN):** `split_study_glossary_document` →
> generator `_iter_study_glossary_pieces` + `apply_file_split` writes pieces to disk / keeps only `plan_names` (id +
> nav scans re-read from disk) → the ~480 MB glossary is held ~1× not ~5×; + 2 glossary-read skips
> (`_merge_scripture_base_files` remap loop, `retarget_demoted_toc_anchors` toc-book scan — structurally no-ops, the
> glossary has no scripture-filenames / no `toc-book`). **Byte-identical (catholic-study eink 453/453; `test_file_split`
> 54/54). Flagship-eink monitored peak 2937 → ~885 MB.** ⚠ **Flagship FULL build still OOMs at ONE more site**
> (~1.4 GB post-retarget, `✗`/MemoryError, not yet diagnosed) — the WS1+WS2+WS3 flagship re-stage waits on it.
> catholic-study + the canon-filtered editions build clean.
> **★ NEW PROGRAM (2026-06-24, user-triggered autonomous) — Kobo deep-audit: `dev/audit/kobo-deep-audit-program-2026-06-24.md` (READ FIRST).**
> **▶▶▶ WS1 FIX LANDED (2026-06-25, WIN, `b7721a4f`) — eink-gated mid-verse-break MERGE.** Poetry decision = user "keep"
> (HUMAN_DECISIONS resolved). New `_merge_mid_verse_breaks(tmp)` (`build_edition.py`, EINK-only, called after
> `_merge_scripture_base_files` in `apply_file_split`): re-joins a narrative verse's tail-prose that the calibre base split
> across a `<p class="verse-p">` boundary into the verse's own paragraph (between-verse paragraphing preserved; ids relocate,
> all #frags resolve). NARRATIVE/prose canon only — `_MIDVERSE_BREAK_KEEP_BOOKS` keeps poetry/wisdom/poetic-prophet + irregular
> apocrypha verse-per-line. Auditor mirrors it (new `POETRY_BOOKS` → WARN; `poetry_break` bucket). **9-KJV/tablet/default base
> UNTOUCHED (eink-gated → no re-baseline)**, same pattern as the page-break + font fixes. TDD: 23 pins (`test_mid_verse_merge` 8
> + `test_audit_verse_formatting` 15) + `test_file_split` 54 green. **Real-data (base copy): 26/27 within-file narrative breaks
> merged; poetry(35)/irregular(12) untouched; lone residual = `est 10:2`→`10:3` at the Esther/Additions boundary.** ⏳ Flagship
> `ethiopian-tewahedo --target-reader eink` rebuild RUNNING (background) → verify auditor narrative-ERROR→0 + epubcheck 0/0/0/0 +
> kepubify + stage for the user's Kobo eyeball; then resolve the est residual + WS1 mixed-translation (18 ¶ + co-located brackets).
> **★ NEW PROGRAM (2026-06-24, user-triggered autonomous) — Kobo deep-audit: `dev/audit/kobo-deep-audit-program-2026-06-24.md` (READ FIRST).**
> **▶▶ WS1 AUDITOR RE-ARCHITECTED + scope CORRECTED (this session, WIN).** The first auditor cut measured the WRONG
> thing (472 = 1 Clement strategy-B chapters + psalm superscriptions + Song rubrics + apocrypha headings; it MISSED the
> real gen/exo narrative breaks). Rewrote `dev/audit_verse_formatting.py` (TDD **14/14**): break = ALPHABETIC PROSE before a
> paragraph's first verse marker; ¶/bracket attributed to the nearest preceding marker; irregular-apocrypha/strategy-B/
> superscription classified out of the ERROR gate. Verified the BUILD preserves paragraph structure (base gen19 == kepub
> gen19 == 2 paras) ⇒ **fix target = base HTML `epub_working/`**. **TRUE scope (built flagship kepub):** **62 regular-canon
> mid-verse breaks** (psa 13·gen 4·job 4·sng 4·num 3·1ch 3·pro 3·isa 3·jer 3·… ; all 4 user cases present) + **18 ¶**
> (gen 46:13/49:14 + lev/exo/num/jer/dan…) ; mixed-translation = the ¶-co-located bracket set (man/1en brackets are
> legit Charles editorial brackets → WARN). **Discrete Prayer-of-Azariah ToC = verified DONE** (`83391827`, pinned, wired
> unconditionally; `test_nav_toc_short_titles` 7/7). **NEXT (WIN):** WS1 fix (62 breaks; decide poetry psa/sng) at the base
> + deliberate re-baseline + device-verify → then mixed-translation normalize. Mac: cross-OS verify the corrected auditor.
> Device eyeball of the newest flagship eink kepub (built fresh from HEAD, loaded to G:): **✅ NO PAGE BREAKS Genesis→Revelation**
> — the page-break defect is RESOLVED on-device. New findings → 3 workstreams + discrete fixes, planned + run + fixed
> autonomously with Mac, neither lane stops till done. **WS1** scripture-body formatting — ROOT-CAUSED: mid-verse line breaks =
> a verse split across multiple `<p class="verse-p">` blocks (gen 19:1 etc.); "weird symbol" = the pilcrow `¶` on KJV-text verses
> (gen 46:13, 49:14) MIXED into WEB/modern text → a mixed-translation defect; badge-trail spaces → `dev/audit_verse_formatting.py`
> + fix + deliberate re-baseline. **WS2** study-note redundancy/contradiction → cascade rework. **WS3** Kobo run-on popup
> formatting research (Mac) → fix. **Discrete:** Prayer of Azariah ToC title reverted to the long form (→ `[+]` truncation) —
> re-shorten + pin. Lane split + loop-until-done protocol in the program doc; Mac's slice in `LANE_HANDOFF.md`. **Earlier today:**
> **✅ Slice 1 DONE (committed):** the 2 WIN-surface standalone build-bugs Mac flagged in
> `dev/audit/spine-breaks-all-editions.md` — (1) `build_one` summary-print `KeyError:'enabled_kinds'`
> that crashed the CLI *after* a successful standalone build (new `_print_edition_build_summary` +
> `build_one` raises on standalone error for contract parity); (2) Amharic standalone misnamed
> `Geez_Standalone_*` (new `_output_filename` derives the script label; Ge'ez filename byte-stable).
> +9 TDD pins; `test_build_standalone` 52/52. No EPUB-byte change. CHANGELOG 2026-06-24.
> **✅ Page-break Part 1 DONE (drop `_VN_LINK_RE`):** the packer now cuts ONLY at book/chapter
> boundaries (removed the verse-level cut candidate + the obsolete K-R15b re-merge + the dead regex).
> **Verified on real data: catholic-study eink 111 mid-chapter → 1** (`audit_spine_breaks.py`); the lone
> `psa 119:88→89` is the documented BASE calibre-split artifact (`index_split_035`), not a packer cut →
> Part 2 fixes it for free. +2 TDD pins (`TestNoMidChapterSplit`); `test_file_split` 46/46. Companion: fixed
> a pre-existing glossary-test breakage from the wrap's backmatter WIP (`min(target, default)`; byte-safe).
> **✅ Page-break Part 2 DONE (per-book base-file merge):** new `_merge_scripture_base_files` concatenates the
> scripture base files (EINK target only) so a book the calibre base split across files is contiguous; then shards
> at `FILE_SPLIT_CEILING` (8 MB, device-validated) → one spine file per book (over-ceiling books chapter-split, never
> mid-chapter). **Verified end-to-end: catholic-study eink 111 mid + 163 chapter → 0 + 0** (only 71 intended
> book-title breaks; 238→72 pieces); `audit_spine_breaks.py` PASS; **epubcheck 0/0/0/0**; +5 TDD pins incl. eink-merge
> determinism (the byte-stability guard); `test_file_split` 54/54. The cross-file opener pop is skipped post-merge.
> **★ The weeks-long page-break defect is RESOLVED on eink (Parts 1+2).** eink-only → tablet/default/KJV untouched.
> **✅ FLAGSHIP VERIFIED (the user's exact 130-break case):** `ethiopian-tewahedo --target-reader eink` rebuilt →
> **130 mid + 40 chapter breaks → 0 + 0** (77 intended book-title breaks; scripture pieces → 77; 29.69 MB);
> `audit_spine_breaks.py` PASS; **epubcheck 0/0/0/0**; kepubified → `YHWH-koboQA.kepub.epub` (39.1 MB).
> **⏳ STAGED for the user's device eyeball at `C:\Users\bogda\YHWH-device-staging\YHWH-koboQA.kepub.epub`** (the Kobo
> `G:` is NOT mounted — when the user connects it, DELETE the old `G:\YHWH-koboQA.kepub.epub` then copy this one).
> **✅ Page-break Part 2b DONE (standalone per-book merge — completes the defect across ALL editions):** the standalones
> (`standalone-geez`/`standalone-amharic`) use a SEPARATE path (`build_standalone`) that emitted one spine file PER
> CHAPTER → chapter-per-page on Kobo (Mac's audit: geez 161 / amharic 125). New pure `pack_book_chapters(book, chapters,
> ceiling)` merges each book's chapter fragments into one spine file (shards at `be.FILE_SPLIT_CEILING`=8 MB, chapter
> boundaries only, never mid-chapter); per-chapter `#ch-{book}-c{ch}` anchors keep TOC nav; noterefs stay same-file.
> **Verified on real builds: standalone-geez 4 books/165 ch → 4 spine pieces 0 mid + 0 chapter (was 161); standalone-amharic
> psa/126 ch → 1 piece 0+0 (was 125)**; `audit_spine_breaks.py` PASS + **epubcheck 0/0/0/0** on both; +5 TDD pins
> (`TestPerBookMerge` + `TestStandaloneSpineMerge`); `test_build_standalone` 57/57. UX-only (standalones are not
> byte-stable-pinned). ⏳ Mac cross-OS verify queued in `LANE_HANDOFF.md`.
> **▶ NEXT (WIN, in priority order):**
> 1. **[USER] device eyeball** of the staged flagship kepub on the color Kobo (confirms the page-break fix on-device);
>    **Mac cross-OS verify Parts 1+2+2b + re-baseline `dev/audit/spine-breaks-all-editions.md`** across all editions incl.
>    the standalones (instructed in `LANE_HANDOFF.md`; Mac already pushed `spine-breaks-post-part1.json`).
> 2. **✅ Hebrew/Greek/Ge'ez popup-font fix DONE (eink CSS, byte-safe):** added `!important` `font-family` rules for
>    `.vnote-hebrew/greek/greek-nt/geez/amharic` to the eink-only `_EINK_READER_CSS` (Hebrew/Greek = embedded Cardo;
>    Ge'ez/Amharic = embedded `"Noto Serif Ethiopic"`, not the stale wrong `"Noto Sans Ethiopic"`). **Verified on a real
>    eink build:** all 3 rules ship in the built `stylesheet.css`, **epubcheck 0/0/0/0**; `test_kobo_device_qa` 16/16
>    (+2 pins). eink-only → 9-KJV untouched. **⏳ DEVICE GATE (`dev/HUMAN_DECISIONS.md`):** does the author `!important`
>    beat Kobo's firmware override? — the user's "Cardo" vs "Publisher Default" A/B decides (the staged kepub carries it).
>    **DEFERRED follow-ups:** (a) embed **Noto Naskh Arabic** for `.vnote-arabic` (a global `EMBED_FONT_PATHS` add changes
>    KJV bytes → needs an eink-only embed path; Arabic is tofu today); (b) the eink "Publisher Default" front-matter page
>    (Kobo's only guaranteed lever + the only fix for the native footnote-PREVIEW overlay). Plan: `kobo-font-override-research.md`.
> 3. Device-QA **E** (study-note back-link navigate, `_study_verse_return_link` → cross-file noteref) + **F** (drop redundant
>    per-note `note-sym` in `_emit_cascade_sections`; supersedes B-1c). round-13 merge remainder (char-vs-byte · 1en 71/90
>    — Mac fetched the PD Charles source `dev/audit/1en-charles-source-71-90.md` · 5 Mac mediums I remediate / Mac verifies).
> Plan: `dev/audit/page-breaks-root-cause-2026-06-23.md`. **⏳ Mac cross-OS verifies the re-cut when Part 2 lands.**
>

<!-- archived: 1 entries, 2026-06-23..2026-06-23 (rotate_truth_records.py) -->

> **▶▶ FRESH AUTONOMOUS SESSION — START HERE (2026-06-23 wrap).** Two live workstreams:
> 1. **Kobo device-QA B-1/C/D — ✅ FIXED + verified + LOADED (awaiting the user's on-device eyeball).** The user
>    QA'd the `ethiopian-tewahedo` eink kepub on his color Kobo (Cardo font) → 3 actionable clusters, ALL fixed at
>    the REAL emitter (ground-truthed live base HTML + empirical Cardo cmap, NOT trace line-numbers; prior "didn't
>    land" = build-cache masking, `--force` bypasses it), grep-verified against the BUILT kepub + **epubcheck
>    0/0/0/0** + `verify_kr2_build` GREEN (36,350 noterefs all-resolve) + **14 pins** (`tests/test_kobo_device_qa.py`),
>    new kepub LOADED to `G:` (old deleted). **B-1 = 5 Cardo-safe surfaces via the new shared
>    `scripts/core/eink_glyphs.py`** (inline badge `◇→◊` · cascade header · note-sym · legend page · book-page
>    ornament `❖→❦`); **C** = dict/topic body-boiler strip (lossless); **D** = eink popup CSS (caught+fixed a real
>    CSS-001 `direction` en route). All eink-only → 9-KJV byte-stable editions untouched. Detail: `dev/CHANGELOG.md`
>    (2026-06-23) + `dev/audit/kobo-device-qa-2026-06-23.md`. **▶ NEXT: user ejects/reconnects + eyeballs on-device;**
>    if good, fold device-QA into the round-13 merge. **▶▶ WRAPPED 2026-06-24 for a fresh MAC-HELPING session —
>    handoff `.remember/remember.md`.** **A (page breaks) — 🔴 ROOT-CAUSED + MEASURED, merge VIABLE:** packer
>    cuts spine files BETWEEN VERSES (`_VN_LINK_RE`) → **130 mid-chapter + 40 chapter breaks**; finder
>    `dev/audit_spine_breaks.py`; **measurement DONE — not sub-splitting kills 129/130, Kobo renders ≤6.2 MB
>    merged files w/o lag → per-book merge is GO** (1 WIP in build_edition.py:~5213, byte-safe, KEEP). **3 NEW
>    device-QA issues:** E back-link popups instead of navigates (K-R12) · F category symbol still redundant
>    (drop per-note `note-sym`; supersedes B-1c) · **Hebrew — Cardo does NOT trigger Hebrew on Kobo** (re-embed
>    a Hebrew font). Plans = `page-breaks-root-cause-2026-06-23.md` + `kobo-device-qa-2026-06-23.md`; Mac tasks
>    in `LANE_HANDOFF.md`. byte-stability-critical (golden re-baseline). **B-2**/**B-3** await repro / `dev/HUMAN_DECISIONS.md`.
>    Kobo at `G:` (`KOBOeReader`), kepubify on-box; STANDING: always DELETE the old `YHWH-koboQA.kepub.epub` before
>    copying. Font pack settled = Cardo + Geʽez + Arabic (3 fonts / 5 files), pushed.
> 2. **Round-13 grand-audit joint merge:** WIN OPEN #5/#6/#9 done+pushed (`11b86baf`/`42da11c3`).
>    **✅ Mac half DONE + pushed (2026-06-23):** `deep-audit LANE=mac` (18 dims, Opus) → `round13-mac-*`
>    (`333e7366`: 33 survivors — 5 med / 17 low / 11 info; 12 refuted; no high/crit survived) + platform-research
>    briefs (`804d8d95`); **#7 `audit_popup_formula` FIXED + pushed** (`00b2de3d`: root-cause dead aside-index +
>    K-R9c/K-R13 by-design exemptions + device-proven floors POP_FLOOR/GLOSSARY_DECLINE_HI, TDD).
>    **Remaining for the merge** = char-vs-byte #2 (WIN, the big all-edition re-cut) · the `1en` 71/90 base fix
>    (needs the PD Charles source) · the 5 Mac mediums (corpus_index gap-4 race `scripts/core/corpus_index.py` ·
>    M4b ↩-link drop, 371 entries `scripts/core/kindle_post.py` · Play-Books QA-artifact-mislabel ×3) ·
>    6 completeness gaps. Tracker = `dev/audit/round13-remediation.md`.
>
> **Last arc CLOSED (2026-06-21): the Grok-revert cleanup — DONE + pushed** (4 commits →
> GitLab + GitHub + E: + F:, HEAD `b45a9ff1`). Loop machinery removed, rules/docs de-bloated,
> truth records reconstructed, **115 notes + 4 kinds restored** (corpus 91,712), editions junk
> fixed. Bible verified intact. Rollback branch `pre-grok-cleanup-snapshot`. Details:
> `dev/CHANGELOG.md` (2026-06-21) + memory `project_grok_cleanup` (task `ww7ughmf7`).
>
> **Mac follow-up DONE (2026-06-21):** the Mac-local Grok footprint is also removed — `~/.grok`
> CLI (1.2 GB → Trash), `~/.config/kilo/`, the `~/.zshrc` PATH block, repo `.grok/` + orphaned
> loop logs, and the tracked `.vscode/extensions.json` + `TOOLCHAIN.md §Grok` (now a decommission
> note). Cover-art Grok-image feature preserved. Claude is the sole coding agent.

<!-- archived: 1 entries, 2026-06-15..2026-06-15 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-15 (🪟 Windows, turn 92 — K-R8 INGESTED + K-R7-2e POPUP MODE).** Round-8 QA ingested (`docs/superpowers/notes/2026-06-15-kobo-round8-device-qa.md`). K-R7-2d confirmed; shipped K-R7-2e (hidden anchors default) + K-R7-4b eyebrow spans + font refresh docs. Pass → Mac re-fan M3 45 + catalog path. Baton **windows**; mode=parallel.
>

<!-- archived: 6 entries, 2026-06-14..2026-06-14 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-14 (🖥️ Mac, turn 90 — M3 FAN-OUT RUNNING).** Pipeline shipped: `build_format_matrix` kepubify post-process + `dev/M3_Kobo_Assets_v0.1.0.txt`. catholic-study smoke 5/5 green. Autonomous `build/m3_fanout.sh` building 45 → `build/matrix-m3/` (7/45 at commit; ethiopian-tewahedo active). Next after 45/45: SHA256SUMS + external drive `m3-kobo-v0.1.0/` handoff for WIN attach. User Kobo taps gate catalog live. Baton **windows**; mode=parallel.
>
> **▶ 2026-06-14 (🪟 Windows, turn 90 — ★ v1.0.0 RELEASE PLAN ACTIVE).** Plan: `docs/superpowers/plans/2026-06-14-v1.0.0-release-plan.md`. Parallel tracks: A audit round 8 · B M3 Kobo · C M5 Play · D docs · E content (opportunistic). M4 DONE (turn 89). Tag blocked until §8 DoD. WIN: audit + orphan gate. Mac: M3 fan-out. User: device QA rounds. Baton **windows**; mode=parallel.
>
> **▶ ✅ 2026-06-14 (🪟 Windows, turn 89 — ★ M4 CATALOG + WEBSITE LIVE).** Attached 45 Kindle EPUBs to v0.1.0 release; merged SHA256SUMS; `gen_release_catalog` → live columns everywhere/apple/kindle (188 assets); website deployed (`yhwh-website` c8c87d5). **M4 arc COMPLETE.** Baton **windows** (truth_owner); mode=parallel.
>
> **▶ ✅ 2026-06-14 (🖥️ Mac, turn 87 — ★ M4 LIVE ON DEVICE + WIN handoff ready).** 45/45 M4 fan-out built+gated; external drive handoff complete. **User STK 6/6 PASS** (ethiopian-tewahedo superset, jewish-study, catholic-study brown, evangelical-reformed, scholarly-academic, eastern-orthodox). EREADERS + truth triad updated; pushed for WIN attach/deploy. Baton **windows** (truth_owner); mode=parallel.
>
> **▶ ✅ 2026-06-14 (🖥️ Mac, turn 86 PULL) — WinGrok M4_Kindle_Assets + kindle_post + 1ki10 + dead-variant + round-7 mirrored on Mac; all viable (exact 45 gen via .venv+export, hooks .githooks, ruff/lint 0f, 308v 1ki10); AGENTS + asset header updated for future Groks (cross-platform format, no mangling); fresh session prep + truth rotation done; pushed both remotes (radar clear).** See SESSION_STATE top for full Mac verification details. Baton **windows** (truth_owner); mode=parallel.
>
> **▶ ✅ 2026-06-14 (🪟 Windows, turn 86 — round-7 `test_marker_style` + `test_note_rehaul` 6 fixes closed + full verification green; push clears red main; dead-variant consolidation STARTED).** All 6 fixes in (marker_style + note_rehaul sweeps). Re-ran full `test_marker_style.py` + `test_note_rehaul.py` (background) — confirmed the 6 pass AND the `_badge_counts` changes (both copies, in the two test classes) introduced no regressions. Triad (LANE_HANDOFF + IN_FLIGHT + CHANGELOG) updated for freshness. Committed + pushed; this clears the red on GitHub `Tests` / main. **Now taking up ② dead-variant consolidation (per LANE_HANDOFF turn-85/86 agreement):** retire the `--target-reader kindle` FAIL variant (`apply_kindle_safe_css` / `apply_kindle_toc_rows` / `apply_kindle_unhide` / `apply_kindle_strip_hidden` + gate-5 wiring + tied CSS/comments in `build_edition.py`) to the single production `kindle_post` path (`scripts/core/kindle_post.py` + `build_kindle.py` + matrix `post_process: kindle_safe`). Using `mac-kindle-pre-rebase` (0d0f0cb8) as the verified-clean removal reference (byte-identity at the time + 308 tests). is_kindle_target + K-KIN (B/C cap/compaction) emitter logic (still used for base + matrix) kept; only the dead in-pipeline apply fns + call sites + variant-specific gate removed. Production M4 fan-out / kindle_post / 45 artifacts untouched (additive cleanup, no perturb to running overnight build). **NEXT (after this entry):** M4 45-artifact fan-out (Mac autonomous) → user STK re-confirm → WIN attaches catalog + deploys (gen_release_catalog + website). Triad updated; ② marked DONE with test sweeps. Baton **windows** (truth_owner); mode=parallel.
> Mac mirrored M4_Kindle_Assets_v0.1.0.txt (45 assets + Win/Mac Python gens + hygiene for catalog attach; first-class committed artifact). 1ki10 integrated (ch10 evidence). Rotation mirrored.

<!-- archived: 1 entries, 2026-06-14..2026-06-14 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-14 (🪟 Windows, turn 85 — ★ M4 KINDLE PRODUCTIZED + Node-24 CI bumps; OVERNIGHT AUTONOMOUS run).** Productized the device-proven Send-to-Kindle "june10" recipe (NOT the disproven `--target-reader kindle` variant): NEW `scripts/core/kindle_post.py` — `make_kindle_safe` (strip `display:none`/`visibility:hidden` CSS+inline · KEEP `.vn-sep` (june10 keeps all 132,949) · collapse `dc:language`→single `en-US` · leave `hidden=""` · OCF re-zip mimetype-first/stored) + `verify_kindle_safe` conformance gate; driver `scripts/build_kindle.py`; `build_format_matrix` builds the Kindle column as the everywhere base + post-process (FORMAT_MATRIX `kindle` row gains `post_process: kindle_safe`; `target_reader` stays `kindle`). **PROVEN on Windows:** catholic-study → **~25.3 MiB, epubcheck 0/0/0/0, `verify_kindle_safe` clean, K-R2 gates green** (7 CSS hides stripped, 132,949 `.vn-sep` KEPT, 6 `dc:language`→1) — byte-faithful to the Mac's device-confirmed artifact. ⚠ **Mac turn-85 caught + corrected an initial vn-sep-DROP→KEEP** (`a6efc4bb`; the proven june10 KEEPS all 132,949 — dropping = FIXED.epub/FAIL behavior); reconciled, corrected tests green, **45-artifact fan-out re-running** with the corrected recipe. 15 unit tests; 147 targeted green; lint + ruff clean; the dormant variant + gate-5 + the 9 KJV editions untouched. **Node-24 CI bumps (Grok-delegated, diff-reviewed):** `actions/checkout@v5` · `setup-python@v6` · `setup-java@v5` + `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` (for `upload-artifact@v4`, no Node-24 major) across `tests.yml` / `format-matrix.yml` / `build-linux.yml` (hard Node-20 cutoff 2026-06-16). EREADERS Kindle row + section rewritten to the true state. **NEXT (running overnight, no prompts):** build all 9 Kindle catalog artifacts via the matrix + gate each → catalog regen → other backlog; lane-ping the Mac and auto-pull on BEHIND. Baton **windows** (truth_owner); mode=parallel.
>

<!-- archived: 2 entries, 2026-06-14..2026-06-14 (rotate_truth_records.py) -->

> **▶ ✅ 2026-06-14 (🖥️ Mac, turn 84 — ★★ K-KIN RESOLVED ON THE REAL CHANNEL: `june10recipe` DELIVERED via Send-to-Kindle, USER-CONFIRMED).** The reproduced **June-10 test-2 recipe** — `~/Desktop/Ethiopian Bible - Catholic Study (Kindle) june10recipe.epub` (standard everywhere build + `display:none` physically stripped + single `en-US`; 24.1 MB, 299 spine, epubcheck 0/0/0/0; **NONE of FIXED's shipshape/split/attr-strip extras**) — **WORKED on Send-to-Kindle** (fast upload, delivered; user-confirmed). ⇒ **the KP3-oracle-driven extras (shipshape compaction + 189-way file-split + attr-strip) were what BROKE Send-to-Kindle**; the minimal recipe (strip `display:none` + single lang, nothing else) is the proven-good shape. **PROVEN RECIPE (to productize):** `scripts/build_edition.py catholic-study` (standard) → post-process: strip `display:none`/`visibility:hidden` (CSS+inline) · collapse `dc:language`→single `en-US` · leave `hidden=""` · OCF re-zip (mimetype-first stored). **NEXT (fresh session):** (1) productize this as the clean kindle build mode (it's currently standard-build + a deterministic post-process script) → then WIN lights FORMAT_MATRIX **M4**; (2) v1.0.0 laundry list `notes/2026-06-14-mac-v1.0.0-laundry.md`. Detail: e999 note CORRECTION + WIN sections. Baton **windows** (truth_owner); mode=parallel.
>
> **▶ 🔄 2026-06-14 (🪟 Windows, turn 83 — CI health swept + Mac v1.0.0 laundry delivered + decommercialize-tail docs).** Bootstrapped; fixed BOTH CIs. (1) **GitLab** per-push pipelines were all `ci_quota_exceeded` (monthly CI minutes blown ~Jun 6 — NOT code; jobs never start) → `.gitlab-ci.yml` `workflow:rules` stops per-push pipeline creation (ends the failure-email spam; GitHub Actions stays the real per-push CI; manual/scheduled survive). (2) **GitHub** fast-gate green (Mac's KDP reword `7bec299b` pulled) + 2 stale `test_scripts.py` pins fixed (`bcp47` multi-lang/non-kindle + `enable_ai_notes` frozenset import) — verified pass; `popup_split` 52/52. (3) **Mac v1.0.0 laundry** = `notes/2026-06-14-mac-v1.0.0-laundry.md` (5-dim code-verified). (4) **Decommercialize-tail docs (2nd milestone):** free-public `RELEASE_NOTES_v1.0.0.md` rewrite + `HANDOFF_README_v7.md` pre-pivot banner. **K-KIN:** WIN's "restart the Kindle arc against the STK oracle (Mac)" directive is **DONE — see turn-84 (RESOLVED on Send-to-Kindle).** Next WIN session: confirm the GitHub `Tests` run greened, then pick the next v1.0.0 item. Baton **windows** (truth_owner); mode=parallel.
>

<!-- archived: 2 entries, 2026-06-12..2026-06-13 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-13 (🪟 Windows, turn 80 — ★ROUND-7 K-R6-2 BYTE-CAP POPUP-SPLIT SLICE SHIPPED + GROK/KILO SECOND-AGENT FULLY WIRED.** ① **Grok Build drove the byte-cap fix HEADLESS** (`grok --prompt-file … --model grok-build --effort max --check --permission-mode bypassPermissions`): `DEFAULT_NOTE_POPUP_SPLIT_BYTE_CAP` 8,000→**8,858** (proven-open floor; estimator dominance = the margin) · `_POPUP_UNIT_SHELL_BYTES` 512→**600** (full emitted `<aside>` est ≤ cap, so gate 4n never trips on a splitter-passed unit) · single-unit control verse gen-1-3→**gen-1-8** (gen 1:3 is markup-dense → legitimately splits) · test_popup_split assertions/docstrings updated. Its `--check` pass self-flagged 2 residuals (an E501 it fixed; the stale `/customize` "8000"). I reviewed the diff + **`tests/test_popup_split.py` 52/52 GREEN** (no regressions) + finished the stale-8,000→8,858 CLASS (customize UI · api/editions.py ×2 · verify_kr2_build.py comment); ruff format clean. Grok stopped after it looped on `run_terminal_command` tool-output errors post-fix (work done+recorded; child tree killed, 0 orphans). **✅ SHIPPED (this milestone):** `-s1` pin sweeps in `test_marker_style.py` + `test_note_rehaul.py` (ruff clean) → round-7 eth + catholic-study builds (r7-pin-sweep artifacts in build/round7/) + kepubify → `dev/verify_kr2_build.py` **ALL K-R2 GATES GREEN** (4m/4n) on eth .epub/.kepub + catholic r7(s) → epubcheck 0/0/0/0 on the artifacts. Remaining user action: NEW-filename device load + leg-4 retaps on the fresh r7 builds.** ② **SECOND-AGENT SETUP** (full recipe = `dev/TOOLCHAIN.md §Grok`): Grok Build CLI (`~/.grok/bin`, OAuth sub) + Kilo Code (xAI connected via bundled `kilo auth login --provider xai`; model `kilo/x-ai/grok-build-0.1`; MCP=chrome-devtools, broken git MCP dropped) + Grok Build sidebar; removed broken `erikkralj.vscode-grok`; `AGENTS.md` + `.vscode/extensions.json` tracked; SSH push verified (GitLab+GitHub). Baton **windows** (truth_owner); mode=parallel.**
>
> **▶ 🔄 2026-06-12 (🖥️ Mac, turn 78 FINAL — ★BOTH LANGCAP ORACLES FAIL ⇒ THE BYTES-ONLY MODEL IS DEAD: full-graph threshold ∈ (28.6, 40.5)MB — bytes × intact-popup-graph INTERACT · (B)+(C) in-build SHIPPED + byte-identity PASS · milestone push on user command ("bandwidth almost up").** ★VERDICTS: cap=2 Heb+Grk @ 48.7MB = FAIL (generic internal error) AND cap=1 Hebrew-only @ 40.5MB raw (MEASURED, epubcheck 0/0/0/0, full 33,969-popup graph intact) = FAIL — while the 0:39 DOC-SUBSET probe @ 45.3MB PASSED and vnotegut (full graph, 28.6MB) PASSED. ⇒ Raw bytes alone do NOT drive the KFX error: with the FULL popup/link graph the ceiling sits somewhere in (28.6, 40.5)MB; with a ~40-doc subset 45.3 clears. Single-volume kindle via language-capping alone is NOT provably shippable (zero-loss compaction tops ~47-49; cap=1 lands 40.5 = still over the full-graph ceiling). **TWO-VOLUME = the only PROVEN option** (halves P/P, full apparatus, live links). ★β RAN SAME SESSION (user GO): composed probe easton-trim (KINDTRIM rung, 3,773 items, −3.7MB) + cap-1 hebrew + compaction = 36.8MB raw, full 33,969-popup graph, epubcheck 0/0/0/0 → **FAIL (same generic error)** ⇒ FULL-GRAPH CEILING ∈ (28.6, 36.8)MB. Single-volume is now effectively CLOSED: getting under ~33MB means cutting Hebrew popups or most remaining notes — below the product's apparatus floor. **RECOMMENDATION → (α) TWO-VOLUME KINDLE** (proven P/P, 100% apparatus, live links; build leg = spine split at the proven seam + per-volume TOC/OPF/cover; the (B)+(C) cap machinery stays as a builder option, not the kindle ship lever). ★PINPOINT LADDER RUNNING (user-directed 'pinpoint as close as we can', push per verdict): rung BYTEDIAL (TDD 61/61) — per-aside child-boundary byte dial, global remainder carry, full graph constant (f=0 = the vnotegut shape; text-only dial was floored by 39.9MB markup mass). Run 1 @ 32.70MB (24.0% tail, 174,259 children dropped, epubcheck 0/0/0/0) = **PASS (Success, ET Supported)** — the first full-graph pass above vnotegut ⇒ ceiling ∈ (32.7, 36.8). Run 2 @ 34.75MB (27.7%) = **PASS** ⇒ ceiling ∈ (34.75, 36.8). Run 3 @ 35.80MB (29.6%) = **PASS** ⇒ ceiling ∈ (35.8, 36.8). Run 4 @ 36.30MB (30.5%) = **PASS** ⇒ vs the foreign anchor the window is (36.3, 36.8). Run 5 (THE FAMILY TEST, byte-matched to the easton+cap1 FAIL within 58 B) = **PASS** ⇒ ★THE CEILING IS NOT BYTES — IT IS (with high consistency) TOTAL ELEMENT COUNT. Census across every verdict artifact (`<[a-zA-Z]` opens, regenerated on disk): vnotegut 363,105 P · dial32.7 501,920 P · dial36.82 645,786 P · easton+cap1 712,219 F · cap1 742,463 F · full 1,055,054 F — ONE threshold separates ALL verdicts, bytes separate NONE (36.8MB sits on both sides); historical fits: halves ≈527k P/P, 0:39 ≈640k P, 0:49 ≈780k F. ⇒ ELEMENT CEILING ∈ (645,786, 712,219). Run 6 @ 679,614 elements (37.69MB) = **PASS** · Run 7 @ 693,700 (38.27MB) = **PASS** ⇒ element ceiling ∈ (693,700, 712,219) — 18.5k window. Run 8 @ 716,025 elements (39.51MB, ABOVE the foreign 712,219 FAIL anchor) = **PASS** ⇒ ★TOTAL-ELEMENT MODEL ALSO FALSIFIED — the boundary is MULTI-DIMENSIONAL (trim family keeps whole multi-KB Hebrew paras per popup and fails ≥712k; the within-popup-thinning dial family passes ≥716k; subsets with FULL popups pass ≤~640k and fail ≥~780k). ⚠ UNPROVEN QUADRANT: no probe has EVER passed >39.5MB — the full-apparatus ship shape is ~636k elements but ~70MB ⇒ its verdict is NOT implied. ★★RUN 9 = THE ANSWER: rung SHIPSHAPE (TDD 67/67; un-em popups + bidi-isolated FSI/PDI text-prefix labels + classed bold headers; the vn-sep-span label form caught fires-on-defect — first build left 756k elements, fixed merge lands 516,921 @ 64.7MB, epubcheck 0/0/0/0) = **PASS (Success, ET Supported, 0 errors)** ⇒ ★SINGLE-VOLUME FULL-APPARATUS KINDLE IS PROVEN at the artifact level — 100% content, zero loss, full 33,969-popup live graph, at 64.7MB (the high-byte/low-element quadrant ALSO exonerated: bytes never decide — passes span 28.6→64.7MB, fails span 36.8→74.7MB; the true driver = popup MARKUP DENSITY: word-<em>s 264,914 + label paras+spans ~239k + header strongs 33,969). FINAL LADDER (12 runs this session): dial passes 363k/501k/645k/679k/693k/716k elements · trim fails 712k/742k · shipshape PASS 516,921@64.7MB. ▶ SHIP LEG (next session, Mac): implement the 3 levers kindle-gated at the step-3 emitter (b9ae8bcc site; TDD + byte-identity off-kindle) — NOTE: levers make the kindle=2 cap default UNNECESSARY for the blocker (cap stays builder option; the ratified kindle=2 default REOPENS — user re-pick) → rebuild build_one(target_reader=kindle) → gates+epubcheck → Previewer clean → ONE STK (user). Probe artifacts: shipshape-full staged on Desktop; all 16 verdicts in ~/kp3-runs/. ★SHIP MATH (zero content loss, kindle-gated markup substitutions at the step-3 emitter shipped today): popup word-<em>s → plain text (−264,914) · label paras → text-prefix/CSS (−119,625) · header <strong> → CSS bold (−33,969) ⇒ **FULL APPARATUS = 636,546 elements** (57k under the proven floor; cap-2 shape = 453,955). ⇒ CANDIDATE ENDGAME: single-volume kindle with 100% apparatus — no cap default needed, no two-volume; cap stays a builder option (re-opens the ratified kindle=2 default — user re-pick at the seam). NEXT CODE LEG: extend the kindle emitter with the 3 levers (TDD) → rebuild → oracle → ONE STK (user). Verdicts in ~/kp3-runs/bytedial-*+elemdial-*/. Probe artifacts: rung-langcap-hebrew (40.5MB) staged on Desktop; verdicts in ~/kp3-runs/nosplit-langcap{,-hebrew}/. kindle_bisect: langcap `keep_groups` parameterized + `--cap-keep` (TDD 5 new, 51/51). ② Oracle #1 ran as the session's first action per the turn-77 plan; FAIL path executed (cap-1 measured + oracled in-session). ② The plan-② implementation went IN-BUILD while the oracle ran, all TDD: `resolve_popup_language_cap`/`resolve_popup_language_pick` (explicit `max_popup_languages` 1..4 > kindle-default 2 > uncapped; groups hebrew=wlc · greek=lxx-greek+greek-nt · latin=vulgate · arabic=arabic; pick key `popup_languages_capped`, default Heb+Grk, dedupe+raise-on-junk/over-cap), BIBLE-WIDE early-return in `_resolve_popup_languages` (tiers 1-3 bypassed under cap; **kjv rides along** when the bible-wide baseline carries English — capped catholic-study resolves exactly {wlc, lxx-greek, greek-nt} = the staged candidate), `_vnote_pass_needed` extracted + cap-aware (bare kindle edition can't skip the pass), kindle-gated compaction step 3 in `_apply_popup_languages_and_translation` (labels → `Heb`/`Grc`/`Lat`/`Ara`/`Eng` via COMPACT_SOURCE_LABELS keyed on content class — unknown classes keep full labels; header → `<Book> N:M` from book_native_names english — UPGRADES the rung's comma surgery that left 'or Ezra Sutu'el 1:1'/'translated from the Greek Septuagint 1:1' on 1,149 popups; unknown book code keeps header + headers_kept stat), stats labels_compacted/headers_compacted wired into build stats. ③ API: validator runs the build's OWN resolvers on the MERGED record (cross-field: a target flip that makes a stored pick over-cap REFUSES atomically — pinned by the test whose RED run actually leaked `target_reader: kindle` into editions.yaml, caught+reverted); ★in-passing DEFECT CLASS fixed: clone whitelist was hand-typed and dropped 19 newer fields (target_reader, marker_badge_style, …) → `EDITABLE_TEXT_FIELDS`/`EDITABLE_BOOL_FIELDS` module registry now feeds BOTH api_save and `_append_cloned_edition` (+ `popup_languages_capped` in clone list fields; CLONE_SCALAR_SPECIAL={title,cover_image}). ④ UI: /customize cap select + 4-group checkbox pick (hidden-input sync, standard dirty path) + amber BYPASS note on the per-book navigator when a cap applies; wizard TARGET_CAPS gains `max_popup_languages` (kindle 2 / others null) + honest kindle note (Heb+Grk default). Forensics note RESOLUTION updated. ⑤ ★BYTE-IDENTITY PROOF = PASS: catholic-study (everywhere) rebuilt at HEAD vs pre-change worktree (4291bfe7) — identical normalized sha256 `fd1ce649…` (URN/modified/date/rights-year scrubbed, the byte-stability-gate normalization; cache key includes build_edition.py source so both builds were REAL); worktree + /tmp proof dirs removed. Post-rebase onto WIN round-7 checkpoint (6126035b): 2 conflicts union-merged (api/editions.py — their `note_popup_split_byte_cap` joined the NEW module-level EDITABLE registry + both validator blocks kept; IN_FLIGHT both turn-78 entries kept) — **160 cross-slice tests green post-rebase** (their 3 recorded-open calibration failures untouched, WIN-owned). Desktop staging now: nosplit source + rung-langcap + rung-langcap-hebrew (8 spent probes deleted on user command; all verdicts preserved in ~/kp3-runs/*/Summary_Log.csv — imgstrip verdict was ✗ Error/ET-Not-Supported, recorded there). Board: pulled 2f552b89+4291bfe7+fb98e2e8 (WIN turn-76 wrap: Kobo popup root cause = Nickel cap, round-7 = WIN); pull rules mirrored to Mac memory (auto-pull-on-BEHIND + 'pull' command word + stash→pull→pop). STK upload: HOLD until the in-build rebuild converts clean.**
>

<!-- archived: 1 entries, 2026-06-12..2026-06-12 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-12 (🪟 Windows, turn 78 — ROUND-7 K-R6-2 LEGS 1-3 IN FLIGHT, checkpoint push on user command ("bandwidth almost up").** TDD slice ~85% in: ① **calibration probe vs the round-6 epub↔kepub pair (66,880 asides byte-matched)**: per-segment koboSpan delta 43.1–81.3 B (p50 52.9) ⇒ estimator `_estimate_kepub_aside_bytes` = raw + segs×85 (DOMINATES corpus max); exposure RE-VERIFIED: 98/98 family heads over cap ✓, **201 units over the 8,000 B target** (97 oversized singles — vs the verdict's ~80), and the "20 accidental pairs" reconcile EXACTLY (118 adjacent prefix pairs = 98 family heads + 20 cross-verse digit-extensions); vnote (translation) namespace: only 1 adjacent pair corpus-wide (1en-100-1<100-11) → left un-renamed, WARN-monitored. Probe: `%TEMP%\yhwh-forensics\r7_calibration.py`. ② **SHIPPED in-tree (this push):** leg 1 — ALWAYS-suffixed `-s1..-s9` vnotes/vbadge namespace (singles included; provably prefix-free; ≤9-unit family guard raises with coord) · leg 2 — `note_popup_split_byte_cap` (default 8,000; resolver + estimator + two-budget `_split_popup_units` + density-driven `_chunk_row_to_budgets`) · leg 3 — gates **4m** (prefix-free + `-s<1..9>` shape; adjacent-vnote WARN) + **4n** (per-aside kepub bytes ≤ 8,858 proven-open floor; vnote WARN; skips plain epubs) wired into verify_kr2_build main · API validator + /customize field + 22 new/updated test pins (RED→mostly GREEN). ③ **OPEN (next session, BEFORE round-7 build): 3 test failures** — (a) `test_under_cap_verse_single_unit_carries_s1` + (b) `test_every_emitted_unit_fits_the_byte_budget`: the K=85-dominating estimator + 8,000 target double-counts margin ⇒ over-splits near-cap verses (gen 1:3 control splits); FIX DIRECTION: anchor the default byte cap at the 8,858 proven-open floor (dominance = the margin; actual ≤ est ≤ 8,858) OR re-derive shell/chunk allowances from the probe — re-measure gen-1-3's actual kepub bytes first; (c) `test_book_last_verse_badge_stays_before_next_book_title` in test_popup_split FIXED (trailing-quote pin) — the SIBLING pins in test_marker_style.py (~12 sites: `vbadge-gen-1-\d+"` patterns, 31-badge counts) + test_note_rehaul.py (×5 `vbadge-gen-1-1"` presence) are NOT yet swept to `-s1`; test_orphan_vnote_asides/test_kindle_safe tolerant (suffix-stripper handles -sN). ④ THEN: round-7 eth build (+ kepubify) + catholic-study canon-filtered gate run + epubcheck + stage kepub under NEW filename → user leg-4 retaps (17 spots + discriminators gen 1:2 ◈2 · gen 1:26 ◈3 · gen 2:18 ◈1 · gen 1:26 ◈2 + controls 1sa 16:13 / gen 35:19). ruff clean on touched files (pre-existing ungated C901 main remains). ⚠ Mac: build_edition.py splitter/emitters + verify_kr2_build are WIN-owned this arc — file-disjoint check before any build slice (board ping stands).**
>

<!-- archived: 1 entries, 2026-06-11..2026-06-11 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-11 (🖥️ Mac, turn 77 close — K-KIN blocker #2: ★VNOTEGUT = PASS ⇒ BYTES DRIVER CONFIRMED (count exonerated at all 33,969 popups) · ★SHIP-CANDIDATE staged: rung LANGCAP @ 48.7MB raw, epubcheck 0/0/0/0 → its Previewer run = NEXT SESSION's FIRST action (machine left quiescent per the new STANDING rule).** **VNOTEGUT (33,969 popups intact, 28.6MB raw): "Success", ET Supported, KPF — the per-popup-count hypothesis is DEAD; only BYTES drive the KFX error.** Threshold ∈ (45.3, 55.2)MB raw (`0:39` 45.3 ✓ / `0:49` 55.2 ✗; halves ~36 ✓×2; full 71.2 ✗ in every config). **USER PICK (pre-recorded via the board, WIN turn-76 addendum): GO (B) compaction + (C) per-reader `max_popup_languages` cap — kindle=2, builder picks any 2 of Heb/Grk/Lat/Ar, ready-made default Hebrew+Greek, BIBLE-WIDE (per-book/chapter tiers bypassed under a cap), through the existing `_resolve_popup_languages` machinery (one resolver, no second path; TARGET_CAPS surfacing in /customize).** **★MEASURED, not modeled: the exact user pick simulated zip-level = rung LANGCAP (TDD 46/46): Lat+Ar content paras dropped, labels → compact `Heb`/`Grc` re-inserted before kept content, header → "Book N:M" — lands 48.7MB raw, INSIDE the unknown band; zero-loss compaction tops out ~47-49MB, so NOTHING reaches the proven 45.3 floor without content choices ⇒ the candidate's OWN oracle verdict is the ship gate.** Artifact staged + epubcheck 0/0/0/0: `~/Desktop/Ethiopian_Bible_catholic-study_kindle-nosplit_2026-06-11T213913Z_rung-langcap.epub`. **▶ NEXT SESSION: ① `mkdir -p ~/kp3-runs/nosplit-langcap && "$HOME/Applications/Kindle Previewer 3.app/Contents/MacOS/Kindle Previewer 3" ~/Desktop/Ethiopian_Bible_catholic-study_kindle-nosplit_2026-06-11T213913Z_rung-langcap.epub -convert -locale en -output ~/kp3-runs/nosplit-langcap` (~30-70 min; verdict = Summary_Log.csv). ② CANDIDATE PASS ⇒ implement in-build (TDD): `resolve_popup_language_cap` resolver (explicit `max_popup_languages` > kindle-default 2; language groups hebrew=wlc · greek=lxx-greek+greek-nt · latin=vulgate · arabic=arabic; capped pick key + Heb+Grk default; bible-wide early-return in `_resolve_popup_languages`; api/editions.py validator + /customize TARGET_CAPS + wizard surfacing) + the (B) compaction pass kindle-gated at the emitter; byte-identity proof for non-kindle editions; rebuild --target-reader kindle → gates+epubcheck → Previewer clean → ONE STK upload (user). ③ CANDIDATE FAIL ⇒ threshold <48.7 — USER OPTIONS: cap=1 language (~39-40MB proven territory) · trim kindle chapter-notes sources (Easton 3.7MB chars) · two-volume (proven ✓✓); surface with the verdict.** **★APPARATUS CENSUS REFINED (chars; raw bytes ~17% higher): 60.8MB content = 12.4MB scripture+frames + 37.8MB per-verse PARALLEL-SOURCE popups (33,969 `vnote` asides: source-labels 10.1MB — "Hebrew (Masoretic / WLC)" ×119,625 verbatim repeats! — Hebrew 6.0 · Arabic 5.2 · Vulgate 4.6 · Greek 3.9+1.1NT · headers/wrappers 6.9) + 10.6MB chapter-end notes-sections (Easton 3.7 · xref 2.1 · ethiopian 1.0 · comm 0.7 · lang 0.8).** Ship options (presentation-configurable, user pick at the seam; full brief in the forensics note RESOLUTION section): **(A) two-volume kindle** — proven passing with 100% apparatus + live popups; **(B) zero-loss popup compaction** — compact source labels + verse headers (~12-17MB chars); **(C) per-language popup knob** — drop Arabic/Vulgate from kindle popups (~7-8MB each incl. labels); B+C compose ⇒ ~71→~40MB raw, clears only if threshold >40 — the 0:39 verdict decides. New tooling this turn: **rung IMGSTRIP** (TDD 36/36, `91d765b5`) + nesting-aware apparatus/language censuses + image-recompress headroom (9.78→2.56MB @ q70/1200px — moot for the blocker, kept for size hygiene). Forensics note `docs/superpowers/notes/2026-06-11-kindle-stk-failure-forensics.md` gained the full RESOLUTION section (`f4efb40c`). (Ops: transient ENOSPC on the harness tmp during conversion — converter spike, 409GB actually free, probes unaffected.) Prior context (turn 74, still true): blocker #1 CLOSED in-build (splitter `_BP_TITLEPAGE_RE` class-keying + `retarget_demoted_toc_anchors`; gates 4k/4l; 163704Z = ALL GATES GREEN + epubcheck 0/0/0/0 + zero TOC errors) · `resolve_file_split_target` shipped (kindle 2MB; explicit override wins) · gate-5 void-element fix · halfspine hardening ×2 (31/31) · K-R6-6 chip badges · `dev/build_kindle_safe.py` · MCP_TIMEOUT=120000 VERIFIED (3/3 servers auto-connect). ④ STK upload: **HOLD until Previewer converts clean**. HOLD: K-R4-2 cap re-tune (user taps pending). Desktop staging: 163704Z + 2 half probes + nosplit + nosplit-imgstrip; kp3-runs verdicts: half-first ✓ / half-second ✓ / bigpiece-202205 ✗ / shipfix2-163704 ✗ / **nosplit ✗** / nosplit-imgstrip ⏳. **Blocker #1 (the demoted-frame TOC class) is FULLY FIXED in the build, two organs:** (a) the splitter force-isolates ONLY real `book-title-page` divs (`_BP_TITLEPAGE_RE`) — demoted `appendix-section` frames flow with parent-book content, husk pieces can't be minted; (b) **`retarget_demoted_toc_anchors`** — ship-fix rebuild #1 (160847Z) proved KFX refuses the frame TAG itself even merged mid-content (E24010 + W14001 ×3; the E24010 message named `display:none` targets), so nav.xhtml + toc.ncx + the in-book TOC now point at each demoted book's FIRST CONTENT anchor (`#ch-b45-c1` etc.; frame keeps its bp id). **Rebuild #2 (163704Z, via the new `--target-reader` path): ALL GATES GREEN + epubcheck 0/0/0/0 + Previewer shows ZERO TOC errors (E24010/E24001/W14001 all gone).** NEW gates, both fires-on-defect-proven on real artifacts: **4k** (no frame-only husk piece) + **4l** (no TOC ref targets a demoted frame); 4h + piece-geometry re-keyed to the `book-title-page` CLASS (bare-bp-id keying = the splitter's own defect class). **Blocker #2 ISOLATED: the generic no-E-code internal error persists on the fully-current 163704Z** (orphan-vnotes dropped, husks gone, TOC clean) — chained delink-on-tochusk FAILED identically ⇒ link graph exonerated. **★HALFSPINE VERDICT = P/P — BOTH halves "Book converted successfully" (Enhanced Typesetting Supported, 0 errors; the arc's FIRST passing conversions ever, full live link graph intact)** ⇒ blocker #2 is an AGGREGATE limit, not a content construct. Evidence matrix so far: 297 docs/25MB FAIL · 300 docs delinked (26.7k links, asides→divs) FAIL · **149/148 docs (~half bytes + halved TOC) PASS ×2** · **182 docs/25MB (kindle 2MB split target) FAIL** — so the threshold is BELOW 182 docs OR the driver is total bytes/converter work; nav/ncx are TINY (84 lis / 76 navPoints — TOC-scale driver DEAD); composition = HTML 72.8MB raw / images 10.3MB / fonts 1.4MB. SHIPPED for this: `resolve_file_split_target` (kindle → `FILE_SPLIT_TARGET_KINDLE` 2MB; explicit `reader_file_split_target` wins; 4 tests) + gate-5 `_hidden_text_chars` VOID-ELEMENT fix (self-closing `<hr class="notes-rule"/>` counted as an unclosed container → 98,016 phantom chars on the big-piece build; real hidden text ~927; 2 tests) + halfspine probe hardening ×2 (fragment-less back-matter lis removed whole + `<li\b` so `<link>` in the nav head can't be matched + emptied `<guide>` dropped; 31/31). **▶ NEXT (decisive): the NO-SPLIT probe — `~/Desktop/Ethiopian_Bible_catholic-study_kindle-nosplit_2026-06-11T213913Z.epub` (63 docs, FULL 25MB, epubcheck 0/0/0/0, built via temp `reader_file_split: false` yaml patch, byte-restored; note: `reader_file_split` is NOT api_save-editable — "no updates supplied")** → epubcheck → Previewer (`-convert -locale en -output ~/kp3-runs/<dir>`; ~25-40 min): **PASS ⇒ doc-count driver, threshold ∈ (63, 182), ship = no/large split for kindle (resolver already in) → rebuild → ONE STK confirmation (user). FAIL ⇒ total-bytes/work driver → next probes: image-strip (10.3MB) zip-level, then content-volume threshold (`--keep 0:N` ladder: 0:223 etc.); if bytes-driven, the kindle column may need a lighter-apparatus kindle profile (presentation-configurable, user pick).** Also SHIPPED this turn: **K-R6-6 chip badges** (`marker_badge_style: chip|glyph+count`, eink→chip via ONE resolver — ◈ never renders on Kobo; emitter + CSS + API validator + /customize, 13 tests) · `dev/build_kindle_safe.py` → `build_one(target_reader="kindle")` (yaml flip retired; stamp verified in-zip) · rebased onto WIN M1 (6702af52), 111 cross-slice tests green · board ACK: deletion-gate mirrored to Mac memory. ④ STK upload: **HOLD until Previewer converts clean** (blocker #2 open). HOLD: K-R4-2 cap re-tune (user taps pending). Desktop staging: 163704Z (current full artifact) + its 2 half probes + the no-split probe; kp3-runs verdicts: half-first ✓ / half-second ✓ / bigpiece-202205 ✗ / shipfix2-163704 ✗(generic).
>

<!-- archived: 1 entries, 2026-06-11..2026-06-11 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-11 (🪟 Windows, turn 76 — SIGNATURE-COVERS CATALOG REDESIGN SHIPPED in-tree; the Addendum-A migration protocol is the open tail).** User-directed: per-edition signature covers (no "— black"; each edition its own design+colour; horizontal cover-card grid; "Read the Bible — Choose Your Edition" merged band; index cover-showcase retired) + the `.band-alt` desktop layout fix (every beige band). Code+tests+review DONE (SESSION_STATE top has the anatomy; adversarial review 6/6 findings addressed; committed catalog.json/html deliberately HELD at the live state until the columns re-light — never-remove-live). **MIGRATION TAIL:** ① 5-leg `signature-covers-shipped` (f2250dcd) ✓ → ② CI 27375212586 ALL-GREEN (11/11, 20:25→20:37Z) ✓ → ③ freshness ✓ (4 colliding names re-uploaded post-dispatch; 39 assets) → ④ regen (guard ✓) + commit b70817fb + deploy `yhwh-site-publish` bba372f + **LIVE-VERIFIED** (9 cards · 18 signature links · covers 200) ✓ → ⑤ user said "delete" → the 14 stale old-model black assets DELETED from v0.1.0 + their 14 SHA256SUMS lines pruned (38→24) + sums re-uploaded ✓ → ⑥ final regen: 25 assets, columns still lit, catalog.json variant-clean (70 stale-cell lines dropped; catalog.html byte-identical ⇒ no redeploy) ✓ — **MIGRATION COMPLETE.** ★User also RATIFIED the signature cover map as-is (04_minimal_lines intentionally unused — do not re-flag). **★M2 COLOUR VARIANTS SHIPPED + DEPLOYED (same session):** 45 own-design composites committed (297a0512; 2 spot-checked visually) · driver variant leg (`cell_asset_names` signature-first + `variant_composite_path` + swap-per-variant, 10 gated assets/edition) · renderer "Other colours" no-JS `<details>` picker (per-cell gating; colour = plain text, never link text) + `.alt-colours` CSS · tests 14+27 green, lint_rules 32/0 · 5-leg `m2-variants-wired` → CI 27383502749 **ALL-GREEN** (11/11, 23:16→23:33Z) → regen (97 assets; 72 variants per-cell lit; 9 pickers; signature buttons unchanged) → deployed `yhwh-site-publish` 84a2a1e + LIVE-VERIFIED (9 pickers · 90 links · variant HTTP 200). **★EVENING ARC (same turn, into 06-12): Kobo round-6c/6d device QA (user) + ROOT CAUSE NAMED.** r6 on-device confirmed; the decisive mid-piece taps landed (1sa 16:12 + act 23:6: units 1+6 teleport, middles open; gen 35:18 single teleports) → forensics + a 2-finder+synthesis workflow → **K-R6-2 CLOSED-IN-ANALYSIS: Nickel popup cap ∈ (8,858..9,273] serialized bytes, measured by forward-scan to the next NON-PREFIX-EXTENDING anchor — our split-family head ids are strict prefixes of their siblings (head slice swallows the family, 98/98 heads over cap; tails fail the same group bookkeeping) + no per-unit byte budget (~80 oversized). Fix = 4 legs (prefix-free `-s1` ids · ~8,000 B split driver · 2 lints · retap list) — FULL PRESCRIPTION in the round-6 QA note.** ▶ **WIN NEXT SESSION: implement legs 1-3 (TDD) → round-7 Kobo build (+ chip badges, U+2028 separators, eyebrow nbsp — all already in code) → load device under a NEW filename → user retap (leg 4) → M3 calibration closes via ROOT CAUSE, not cap-tuning.** Also this evening: the 14 stale assets deleted + sums pruned (user "delete") · cover map RATIFIED · K-KIN user picks recorded ×3 (trim/compact GO — vnotegut PASSED, bytes-driver confirmed; per-reader language cap, any 2 of 4; Bible-wide switches; Heb+Grk kindle default) · STANDING rules added: auto-pull-on-BEHIND + "pull" command word + no-background-runs-at-wrap (mirrored + ACKed both lanes) · MCP_TIMEOUT=120000 mirrored into Windows settings (user-approved). THEN: M4 Kindle (Mac LANGCAP oracle = its next session's first read) · M5 Play (user phone-QA) · Phase D1b PO Esther (paused ~p35).
>

<!-- archived: 1 entries, 2026-06-11..2026-06-11 (rotate_truth_records.py) -->

> **▶ ✅ 2026-06-11, closed same-day (🪟 Windows, turn 75 — MATRIX M1 SHIPPED; CI dispatch + catalog go-live remaining).** All 5 M1 items DONE + e2e-proven (SESSION_STATE top has the full anatomy): FORMAT_MATRIX one-home (+COVER_COLOURS+catalog_asset_name) · 18 committed composites (CI never composites in-runner) · swap_epub_cover + build_format_matrix driver + format-matrix.yml (proof: catholic-study ×2 cells, epubcheck --strict 0/0/0/0 ×2, ALL K-R2 GATES GREEN ×2) · gen_release_catalog (full-count gating, paginated, legacy-Kobo cell; proven vs the live release) · releases.html catalog band (visual QA passed; dark state deploys quiet). **★M1 CLOSED — CATALOG LIVE (same turn):** ① 5-leg sync `matrix-m1-shipped` (6702af52) ✓ → ② CI run 27364486873 dispatched (user-authorized after an auto-mode soft-deny) → **completed/success — all 9 edition jobs + fan-in green; 18 assets + SHA256SUMS on v0.1.0 (25 assets total)** → ③ catalog regenerated (everywhere+apple live ×9, sha256s resolved) + site deployed (`yhwh-site-publish` 01049a8→9f2332a) + **LIVE-VERIFIED on www.yhwhyaway.com/releases.html** (both format panels, 18 links, Kobo legacy note). **Open:** ④ next lanes — M2 colour variants (the 225-composite commit-vs-pin decision) · M3 Kobo column (K-R4-2 cap, user taps pending) · M4 Kindle column (Mac kindle_safe) · M5 Play Books (user phone-QA) · Phase D1b PO Esther (paused ~p35).
>

<!-- archived: 1 entries, 2026-06-10..2026-06-10 (rotate_truth_records.py) -->

> **▶ 🔄 2026-06-10 (🪟 Windows, turn 69 — ② mint 3.1/3.3 TRUTH-RECORD ROTATION SHIPPED (`3fd07450`) + ★NEW USER DIRECTIVE captured: the WEBSITE FORMAT MATRIX (all 9 full canon editions × 5 formats [Apple Books · Kobo · Kindle · Play Books · standard-EPUB] × the 25-cover 5×5 with colour choice; spec `docs/superpowers/specs/2026-06-10-website-format-matrix-design.md` — CI-built artifacts, catalog UI on the Downloads page, phases M1–M5; M3 Kobo gated on K-R4-2 taps, M4 Kindle = Mac's kindle_safe, M5 Play = user phone-QA); ✅ ③ ROUND-5 COMPLETE on the merged tree — eth 26.30 MB + kepub 34.14 MB, ALL GATES GREEN (verifier EPUB+kepub: 393 pieces / 86 singletons / 66,498 noterefs all-resolve / 0 promoted-dup-spilled; epubcheck 0/0/0/0; kepubify 1/0/0); **the r5 kepub is ON the Kobo** (`G:\YHWH-Ethiopian-Bible-koboQA-r5.kepub.epub`; v0.1.0 copy archived `E:\kobo-archive`; fonts staged) + the **K-R4-2 TAP-LIST is READY** (`notes/2026-06-10-kobo-round5-tap-list.md`, 7 taps via NEW `dev/kobo_tap_calibration.py`; only 195/66,683 asides over the pop floor) + Mac's kindle_safe push MERGED (`1cf257b5`+`6f6dc704`) + NEW `dev/EREADERS.md` e-reader tracker (2nd user directive); ✅ ④ ASSESSMENT DECIDED: v1.0.0 = matrix M1(+M2) + θ.4 + the K-R4-2 fix + Kindle acceptance; M5 Play = v1.1. ✅ ⑤ ROUND-5 QA INGESTED + FORENSICS: NO regression (r5 ≡ v0.1.0 byte-identical on every probe); tofu = per-book FONT RESET (K-R5-1; swaps reuse the device filename now); decline bracket NARROWED pops ≤4,498 / declines ≥5,500 → cap ≤~4,400 (gen 35:18 = the 1 anomaly, re-tap pending; fallback = piece-top navigate CONFIRMED); NEW K-R5-3 ×38 book-title badge spill (pre-existing) — **K-R4-2 + K-R5-3 fixes → MAC** (board turn-70); K-R5-4 break options for Boggy. ▶ NEXT WIN = M1 impl (CI matrix workflow + gen_release_catalog + catalog UI). AWAITING USER: Cardo font re-set + 35:18 re-tap + Send-to-Kindle. Milestone 5-leg at this wrap.** Rotation slice (TDD, 17 new pins, 70 green in the two files): `rotate_truth_records.py` now ALSO rotates IN_FLIGHT's `> **▶` entries (both marker families; bold continuation lines ride with their entry; date-range from entry-start lines only) **and the LANE_HANDOFF board** (`plan_board_rotation`: frontmatter + newest 2 turn sections + every "do NOT rotate" STANDING section live; older sections → the pre-existing `dev/archive/LANE_HANDOFF_LOG.md`, sentinel-migrated; live board carries ONE archive-pointer line). NEW `count_entries` = the single entry-count resolver shared by lint + fixer. Lint: `_TRUTH_BUDGETS` += LANE_HANDOFF (soft 40k/hard 120k/2 entries); **`_ENFORCE_HARD_TRUTH_BUDGET` flipped TRUE** (hard breach now FAILs). `save-all.ps1` = the standing rotation ACTOR (post-leg-1 `--apply` + auto-commit of the rotation diff, pre-push). Website overhaul (turn 68) + this slice ride the next milestone 5-leg.**
>

<!-- archived: 1 entries, 2026-06-10..2026-06-10 (rotate_truth_records.py) -->

> **▶ ✅ DONE 2026-06-10 (🖥️ Mac, turn 69) — the kindle_safe variant SHIPPED end-to-end (board ①; plan `2026-06-10-kindle-safe-variant.md`, TDD, 8 local commits `099993dd…fbbf1a4a`) + the convergence red wall cleared in passing (board ③) + K-R4-2 design prep answered (board ④).** Ships: the one-resolver `TARGET_READERS`/`resolve_target_reader`/`is_kindle_target` (kindle = 5th target across api/wizard/customize/web_editions) · `apply_kindle_safe_css` (E3013 visible endnotes — artifact hidden text 486,188→955 chars; K-KIN-3 seam CSS; vn-sep visible) · `apply_kindle_toc_rows` (K-KIN-2; 75 rows/0 pills in-zip) · `patch_opf` `yhwh:target-reader` stamp (additive-only; ★post-wrap user GO: Temurin 21 JRE installed `~/.local/opt` → **epubcheck 0/0/0/0 ON the kindle artifact by execution** — Mac epubcheck-capable now) · verifier gate 5 `kindle_safe_checks` (≤10K effective display:none + single dc:language; fires-on-defect both ways) · **byte-proof: before/after catholic-study SHA-identical w/ field unset** · the kindle artifact gates-green (gate 5 ACTIVE) + **STAGED `~/Desktop/Ethiopian_Bible_catholic-study_kindle-safe_2026-06-10T224859Z.epub` → the user's Send-to-Kindle re-verify = acceptance (K-KIN-1..4)**. Red wall (P3's own fallout, run 27308009548 6F+1E→expect ordering-pair-only next push): audit_caches ×3 (`_web_anchors` whitelisted — Phase C post-dated Phase A's fix) · chi4 legacy Gospel set → canonical mrk/jhn · 2 stale `#vnote-` pins → `#v-`. K-R4-2: hist ≈19K = ONE Easton note in both worst verses ⇒ (a)+(b), (b) splits within a note body (QA note updated). editions.yaml byte-restored (diff EMPTY); temp byte-proof artifacts cleaned. **▶ MAC NEXT = item-② ordering-writer hunt (the editions.yaml session-END 2-line comment drop + work_cache/TestEbibleAudit guard-attribution trio) + arms (W1 AB① · user eyeballs · K-R4-2 post-taps).** Baton **windows** (truth_owner); mode=parallel; board turn 70 at this push.**
>

<!-- archived: 1 entries, 2026-06-10..2026-06-10 (rotate_truth_records.py) -->

> **▶ ✅ WRAPPED 2026-06-10 (🪟 Windows, turn 67) — the round-7 P3 FIX PASS SHIPPED END-TO-END (phases A–E + the red wall; 8 local commits, milestone 5-leg at this wrap). ▶ NEXT WIN SESSION (fresh, user-directed) = ① the WEBSITE OVERHAUL (user directive, 8 points — screenshots `C:\Users\bogda\OneDrive\Desktop\kobo_img\web1..web8`: two-line mobile titles → smaller/cleaner page titles · capitalization consistency (proposal: FULL-CAPS headings + Title-Case continuations) · refresh/clean the site's symbols · rename "How it was made" (it's a usage guide) + DEEPEN the guide per desired EPUB output product · de-negativize the "honestly what this can do" passages (professional, plain) · replace the gaudy "A Bible that tells the truth about itself" heading · dedupe repeated info site-wide; propose as options where presentation-doctrine applies) → ② mint 3.1/3.3 rotation → ③ round-5 rebuild + gates + kepub + calibration tap-list (the K-R4 §K-R4-2 method: 5-6 real badges at ~3.5k/4.5k/5.5k/6.5k/7.5k stripped) → ④ v1.0.0 assessment (midnight target SLIPPED — quality bar held; remaining: K-R4-2 post-calibration fix · kindle_safe variant [Mac] · θ.4 decision · round-5 user taps).** This session's ships: verify-first triage (46 verdicts, wf_81c9dea3) → fixes A (incl. the RED-main HIGH) · agent lane (8 routes + Torrey escape ×104) · B build-path (vnote regex, vrefs husk, sorted ×8, filter_html hardening, topic-span uncap, **xref hidden-target retarget ×3,502**, kindle dc:language) · C **117-site verse-boundary sweep COMPLETE** (WEB fixture; FIX=117 FLAG=0; pin test) · D public-copy sweep + site DEPLOY (114d231) · E truth records (epubcheck Java-8 refutation BY EXECUTION) · red wall (schema description FieldSpec · HOME-first console pins · manuscript pre-staged-folios contract · bcp47 single-lang pin · ★in-passing PRODUCTION fix: target_reader save clobbered chapter_number_format). **PARKED (Mac board has the evidence):** editions.yaml 2-line comment drop by a session-END writer in test_scripts (catholic-study reader_toc comment; per-test hash probe saw nothing → finalizer/last-test class) · TestEbibleAudit + work_cache ordering flakes (pass isolated) · ⛔ settings.local.json:135 `PowerShell(pip install *)` wildcard — auto-mode blocks Claude editing the permission file; ONE-LINE MANUAL DELETE for Boggy. Baton **windows** (truth_owner); mode=parallel; board turn 69.**

<!-- archived: 74 entries, 2026-05-28..2026-06-10 (rotate_truth_records.py) -->

> **▶ ✅ DONE 2026-06-10 (🖥️ Mac, turn 66b) — round-7 laundry (Mac share) SHIPPED + the per-push test signal MOVED to GitHub Actions + 7 main-suite reds fixed in passing (`01e6cb4e`+`b25446b0`+`f8847cd8`, pushed both remotes).** ① `build-linux.yml`: v0.1.0 default · fail-fast `gh release view` pre-build · proven header · appimagetool **pinned 1.9.1 + sha256**. ② GitLab `tests` verify-first KILL: NEVER green (1-h `job_execution_timeout` every run — `ci.py` runs the suite TWICE) + those runs burned the June quota (`ci_quota_exceeded` since ~06-06; private = 400 min/mo) → split `tests-core` (BLOCKING when run, not-slow, 55m) / `tests-full` (allow_failure til first green, 3h), schedule/manual-only + monthly schedule (id 4283714, first run Jul 2); **NEW `.github/workflows/tests.yml` = the per-push BLOCKING signal** (repo PUBLIC = free; gates + `pytest -m "not slow and not done_gate"`). ③ `lane-transfer/{audit,rules}` deleted origin+github+local (Boggy approved live). ④ NEW `tests/test_versification.py`: 3 MATRIX_MAP pins LIVE-green (real `VerseMap.xml` **provisioned to Mac `_acquire/`** from openscriptures — was Win-only). **★Pre-proof = full not-slow suite on Mac (1h53m, 21F+1E) → 7 FIXED:** covers ×3 (hardcoded `C:\Windows\Fonts` title font → candidate-resolve + in-repo NotoSerifEthiopic appended; Win-byte-neutral) · `build_edition` K-R2 `zip` → `strict=False` (lint) · pi0 "Sans"→"Serif" stale pin · posix backslash gate · **NEW `done_gate` marker** (red-by-design milestone pins; samkings folios pair; CI deselects) · samkings-images GAPS-absence CI skip. **★First `tests.yml` run MEASURED (27301013153, 39:51): 10F+1E / 7884 pass / 21 skip / 135 desel — gates green on ubuntu, ALL Mac fixes held; the reds = exactly WIN's P3 set** (audit_caches ×3 [round-7 HIGH] · validate_schemas ×3 + strict_unknown [CI log: eth `description` unspec'd] · consoles ×3 [CI log: nav missing `/` HOME] · manuscript_kings track_aware · **work_cache persists_to_disk errors on CI too → ordering-sensitive, NOT a Mac flake — P3 +1**); the run = WIN's convergence meter (first green = main clean). Mac-only reds (board): samkings images[samuel] (74 CAM hi-res jpgs Win-only — sync or accept?) · tau6x1 p1318 amh tuples=0 (tesseract version-sensitive; CI skips). New memory `reference_ci_architecture`. **▶ MAC NEXT = item-5 Kindle stand-by (WIN's v0.1.1 `kindle_safe`) + arms (W1 AB① · user eyeballs · K-R4-2 threshold).** Baton **windows** (truth_owner); mode=parallel; board turn 68.**

> **▶ 🔄 2026-06-10 (🪟 Windows, turn 67 — P3 EXECUTING).** Pulled Mac turn-66a (`41302ba9`, env-parity done). **Verify-first triage RUNNING** as workflow `wf_81c9dea3-f98` (task `wmb8i3ab9`; 12 read-only clusters over the WIN-share round-7 survivors; Mac Phase-3 items struck per `73edc815`; Mac keeps CI batch + allow_failure + dead branches + test_versification). Controller meanwhile: RULES §0.3 plugin baseline NAMED + corrected (live `claude plugin list` = **15 @official**, not 16 — the pre-expansion 15 was 14 official + gitkraken; 15+1+14=30 ✓) · memory items 8.11–8.14 done (reference_save truth_owner wording · external-tools Voyage key = EMPTY placeholder, treat pasted key revoked · Azure GUIDs → live-lookup · reference_lane_coordination CREATED). **★ TWO NEW USER DIRECTIVES (this session):** ① future deep audits = PROJECT CODE/PRODUCT ONLY, occasionally Claude-optimization (memory'd in reference_deep_audit_tool); ② **soft target: v1.0.0 OUT BY MIDNIGHT TONIGHT** ("fine if not realistic, never rush") — sequence release-blocking work first, quality bar unchanged (memory'd in project_deadline). ▶ After triage: fix phases safest-first (local commits; byte-proofs for Phase 5) → Phase-7 copy sweep + ONE site deploy → mint 3.1 rotation (compressed) → P4 round-5 rebuild + gates + kepub + calibration tap-list → milestone 5-leg + Mac board.

> **▶ ✅ DONE 2026-06-10 (🖥️ Mac, turn 65) — the round-7 MAC CLUSTER shipped (the synth plan's Phase-3 Mac items, verify-first, behavior-proven; `73edc815`) + the GitKraken plugin install FINISHED; milestone push (Mac legs 1–3) at this wrap.** ① `save_mac.sh` HIGH+MED: leg-1 now `git add -A` + `--cached` dirty check + `-m` commit — untracked session notes/specs are no longer silently dropped (scratch-repo proof: untracked file IS committed; dry-run mutates nothing) — and a pull-failure now `git rebase --abort`s + exits 1 (mirrors save-all.ps1; never leaves the repo mid-rebase). ② notary HIGH: `notary_autofinish.sh` derives REPO from `BASH_SOURCE` + the dmg name from the VERSION file (build_dmg.sh's parse) and the `STATE: DONE` idempotence guard is SCOPED to the current dmg basename — sandbox-proven (a v0.1.0 DONE record does NOT swallow a v0.1.1 run → rc 2; live single-check → rc 0); class swept to both siblings (`notary_ensure_agent.sh` path + scoped guard · `notary_launchd_runner.sh` path) + `[Mac-only]` headers ×3. ③ `NOTARIZATION_STATUS.md` LOW → records v0.1.0 (submission `27aedc8a` Accepted, SHA `916d8820…6c4e1b`, `stapler validate` re-verified LIVE this session; beta.1 history preserved below). **★ WIN P3 NOTE: strike the 4 Phase-3 Mac items from the triage list — DONE; and the finding's "wiped Mac path" premise is WRONG (`/Volumes/MacHD2/yhwh-bible-platform` IS this Mac's live repo, re-provisioned 2026-06-03) — the portability fix was right anyway.** ④ GitKraken (user-driven, user-run cmds): `@gitkraken/gk@3.1.68` now GLOBAL (the npx cold-fetch 30s-timeout hazard is gone) + CLI authed (gringoboggy) + plugin MCP ✔ Connected; Mac memory updated + MEMORY.md index trimmed 27.4→24.3KB (was over the load limit). Also riding this push: the kindle round-1 QA note (`4ed67ccd`, K-KIN-1…4 → v0.1.1 kindle_safe variant). **▶ MAC NEXT = await WIN's P3 board** (arms: W1 AB① contingency · user-driven Kindle/Books eyeballs). Baton **windows** (truth_owner); mode=parallel.**

> **▶ ✅ WRAPPED 2026-06-10 (🪟 Windows, turn 64) — the round-7 audit COMPLETED (75 survivors / 0 unverified; 5 HIGH) + results captured (`notes/2026-06-10-round7-findings.md` + `…-round7-audit-raw.json`); SESSION_STATE rotated 109→8KB; milestone 5-leg push at this wrap. ▶ NEXT SESSION = P3: VERIFY-FIRST triage of the 75 (the synth plan is embedded in the findings note — re-check every item vs live code first) → fix phases safest-first → Fable-5 mint impl (`notes/2026-06-10-fable5-system-mint-design.md`; un-gated now) → round-5 rebuild (K-R4-1 is in the tree, ships on next build) + calibration tap-list → G:\ + Mac board. Mac's overnight Kindle E999 arc (pulled): fold its v0.1.1 prescription into the same fix pass. The HIGH `test_audit_clean_state_on_real_tree` is RED in main right now — fix it first (whitelist 2 lru_cache singletons).**

> **▶ 🔄 2026-06-10 (🪟 Windows, turn 64 — the AUDIT-PROGRAM session, superseded by the WRAP entry above) — ROUND-4 device QA ingested (`docs/superpowers/notes/2026-06-10-kobo-round4-device-qa.md`: K-R4-1 vnote popups have NO separators [the K-R3-2 fix covered only the study cascade — artifact-confirmed]; K-R4-2 preview-decline CONFIRMED + threshold bracketed 3,313 < T ≤ 7,748 stripped chars [Gen 1:3 pops / 1:26 jumps / 1:1 "nothing" = jump-to-self]; class = 67 asides ≥5k / ~600 worst-case, whole Bible). PROGRAM PLAN = `docs/superpowers/plans/2026-06-10-full-project-audit-program.md` (①–⑤). NOW EXECUTING: engine round-7 upgrade (+7 dims: claude-setup · lane-system · github-gitlab · popup-integrity · stack-review · future-work · decommission; v0.1.0 facts; Fable inherit) → ✅ overnight audit LAUNCHED (run `wf_1468532d-6f8`, task `wlyv6mrle`; recovery = `Workflow({scriptPath, resumeFromRunId:"wf_1468532d-6f8"})`; if STOPPED, `taskkill /F /IM python.exe /T` the orphaned pytest children — NEVER node) → parallel controller: K-R4-1 TDD fix + MEMORY.md trim + ① mint design doc. K-R4-2 fix = post-round-5 calibration (deliberate sequencing, not deferral). Morning = verify-first triage → fixes → round-5 matrix + rebuild → milestone save + Mac board.**

> **▶ 🚀 RELEASED 2026-06-10 (🪟 Windows, turn 62/63) — v0.1.0 IS LIVE (GitHub release published+latest, all 6 assets + AppImage workflow run 27257694787 in flight) + site DEPLOYED + Kobo carries the v0.1.0 kepub. The K-R3 sweep executed IN FULL per Mac's review re-sequence (badge clamp [+ a splice-collision fix the first draft needed — gen 27:46, RSC-016, now tripwired in gate 4d/4e/4f] → C1 div-close → C16 reopen-strip → C12 idmap → ¶◦• separators → 3×-gated artifacts: epubcheck 0/0/0/0, 0 promoted/dup/spilled, 66,498 noterefs). Counts streamlined to the SHIPPED 91,553 (site ×4, both repo descriptions, release body). θ.4 → v0.1.1. Mac RETIRED till morning. ▶ NEXT SESSION (fresh): PLAN then RUN the overnight FULL PROJECT AUDIT (user; memory `project_full_final_audit_program` — read the topic file first; deep-audit.js Opus, sleep Never, terminal open). Round-4 tap matrix + item-7 WEB-fixture base fix + item-8 threshold + item-9 toc datum + X posts (user posts; F-series) = v0.1.1 lane.**

> **▶ ✅ DONE 2026-06-10 (🖥️ Mac, turn 62) — the turn-61 6-item backlog is ALL DONE; ★ the splitter review FOUND K-R3-4's real mechanism and it REDIRECTS WIN's deep sweep (read `notes/2026-06-10-splitter-arc-review.md` BEFORE starting the sweep — its §priority SUPERSEDES the round-3 step list and the turn-61b NEXT-SESSION ① below).** **(1) Splitter-arc adversarial review** (23 agents, 19 confirmed / 0 refuted, real-base + fresh-artifact probes): ★ **K-R3-4's "cross-file-promoted badge hrefs" theory is REFUTED on artifacts** (0 promoted out of ~109K noterefs across eth+catholic, EPUB+kepub; structurally impossible — the badge tag carries both the anchor id and the only prose href) — **the real mechanism: `apply_badge_markers` places a chapter-last-verse's badge at its LAST marker position = INSIDE the next chapter's first paragraph (305 eth / 74 catholic chapter-starts; the user's "first 5" = `vbadge-gen-1-31` title="5 notes"); the piece begins at the chapter boundary, so Kobo's failed-pop fallback to the piece top IS the "teleport to ch-1 start". Fix = a badge CLAMP in apply_badge_markers (before any intervening ch/bp boundary) + a badges-after-heading==0 artifact pin — NOT a splitter aside-attribution change (that would be a no-op); this likely also closes K-R3-3.** Plus: **C1 HIGH (ships in r3): forced title singletons TEAR the Jubilees/AddEsther intro blurbs mid-sentence** (artifact-confirmed — eth piece 018_02 ends "…the Book of Jubilees?), "; fix = end the title atom at the title-div CLOSE) · reopen prefixes replay open tags WITH ids → cross-piece duplicate ids (page_639/1468/1913…; strip ids from reopens, then harden the gate warn to FAIL) · idmap `\bid="` matches `data-*-id` (tighten `\sid="`) · orphan-aside + whitespace-piece latents · 2 test gaps. The pop regex/step-1b = VERIFIED SOUND on all 5 real donors (do not touch for K-R3). **Gate upgraded this commit:** `verify_kr2_build.py` now FAILS on promoted cross-file noterefs (attr-order-insensitive matching — the old regex was blind to the exact K-R3-4 form), asserts bp- ids exactly-once (the prior count check was tautological — incl. my own turn-61 canon-aware version), warns on cross-piece dup ids (kepub wrapper/span ids excluded); validated GREEN on catholic EPUB + eth EPUB + kepub. **(2)+(3)** canon-filtered catholic-study gate run + kepub deep-verify DONE: 75/75 singletons, epubcheck 0/0/0/0, 279 pieces survive kepubify identically, NO dummy-titlepage injection (caution retired), 0 unresolved/0 promoted post-transform. **(4) TARGET_CAPS research DONE** (`notes/2026-06-10-target-caps-research.md`): all four toc_expandable values CONFIRMED — incl. a **live Apple Books test on this Mac** (full `<details>` click round-trip); wizard gate copy upgraded to evidence-based strings (13/13 pins green); Play Books/Android = the one closed-and-stuck reader; ⚠ **cut-checklist: the eink wizard note advertises the font pack — verify the pack is LIVE in the v0.1.0 release or soften the note before cutting.** **(5) STAGE-F re-true #2 DONE** (HOME/editor bullets in, title-page bullet → the file-split mechanism [round-3 device-confirmed], reader-targeting/83-card/one-Colophon added, X thread F6+F7 appended — F7 double-gated on the pack). **(6)** expandable-Contents device recipe + a flagged test EPUB staged (`build/toc-qa/` + Desktop; 75/75 `<details>` in-zip verified; editions.yaml flip reverted). Kindle: the Mac app has NO local import (Send-to-Kindle conversion only — it silently MOVED our file into its container; restored); user logged in; datum deferred. **▶ WIN NEXT (the K-R3 sweep, re-sequenced):** badge clamp (C8) → title-atom div-close (C1) → reopen-id strip (C16) + gate-warn→FAIL → idmap tighten (C12/C15) → K-R3-2 popup separators → rebuild+gates+kepub+G:\ → round 4 → K-R3-5 research. **▶ MAC:** W1 AB① contingency + M3 dmg armed; Kindle/Books user-driven eyeballs pending. Baton **windows**.
>
> **▶ 🔄 2026-06-10 (🪟 Windows, turn 61b — round-3 results ingested, session ends here; user restarting). USER VERDICT: "OMG BIG WIN — maybe 4-5 fixes needed now."** Title bleed FIXED (kobo3), chapters flow, numerals centered, original scripts RENDER in popups (kobo1/7 — likely the font pack working; confirm which reading font was active at round 4). **READ `docs/superpowers/notes/2026-06-10-kobo-round3-device-qa.md` (K-R3-1…5).** NEXT SESSION = ① the K-R3-4 **deep sweep** (seam verses' merged asides landed in neighboring pieces → badge hrefs got cross-file-promoted → Kobo navigates instead of popping ["teleport to ch1"] + repeat clusters at Gen 2 start; extend `dev/verify_kr2_build.py` with global id-uniqueness + promoted-noteref count, fix the splitter badge↔aside same-piece invariant — likely also resolves K-R3-1 [Gen 1:1 ◈15 still silent] + K-R3-3 [end-of-chapter spill]) → ② K-R3-2 popup run-on (emit preview-honored separators per `.vn-item`/`.vn-cat-head`; inspect Greek letter-spreading) → ③ rebuild + gates + kepub + `G:\` → round 4 → ④ K-R3-5 research (the Azariah [+]/Read modal = Kobo nested-nav; user wants a use-case evaluation — chapter drill-down per book as an eink-gated option). Mac's turn-61 splitter review may independently catch K-R3-4 — reconcile.
> **▶ ✅ MILESTONE 2026-06-10 (🪟 Windows, turn 61) — the K-R2 arc CLOSED on the artifact: round-3 kepub ON THE KOBO (`G:\YHWH-Ethiopian-Bible-koboQA-r3.kepub.epub`, 34.6 MB) + all 5 fonts on `G:\fonts` (Naskh added, user-directed). ▶ NEXT SESSION = the USER's round-3 eyeball, then θ.4 → STAGE D icons → STAGE F v0.1.0 cut.** Artifact gates ALL GREEN (epub `kr2c` + the kepub post-koboSpan, via the NEW `dev/verify_kr2_build.py`): 374 pieces · 86 title singletons · max 405 KB/mean 233 KB (was 881/666) · ToC standalone · NO stranded openers (piece + FILE seams) · 66,498 noterefs resolve · OPF 83 · ONE Colophon · epubcheck 0/0/0/0 · kepubify 1/0/0. The real-artifact verify CAUGHT a 6th class (5 pre-existing FILE-seam orphans: Gen 27/1Ch 3/Ps 73/Isa 33/Jer 25) → cross-FILE opener pop (`e7685d4b`; first regex draft lazy-span-defeated on real data — tempered-dot fix; the synthetic had masked it). Mac's AST guard caught the dead version param post-rebase (`16c976b0`). Font pack BUILT staged (`dist/yhwh-kobo-font-pack.zip`; upload gated on the user's font experiment). **USER round-3 checklist:** ① every book title on its own page? ② Gen 1:1 ◈15 pops (no ToC jump)? ③ numerals centered, no stranded "27"/"3"? ④ library card says 83? ⑤ ONE Colophon in the ToC? ⑥ set Cardo as reading font → does the Footnote-preview dialog render Hebrew/Greek (the font-pack ship gate)? **▶ MAC turn-61 board (6 items):** splitter adversarial review · canon-filtered catholic-study seam gate · kepub deep-verify · TARGET_CAPS capability research · STAGE-F copy re-true #2 · expandable-Contents device recipe. Baton **windows** (truth_owner); mode=parallel.
> **▶ ✅ DONE 2026-06-09 (🖥️ Mac, turn 60) — the turn-57/58 adversarial review RECOVERED (power-cut mid-wrap) + ALL findings fixed; K-R2 R1 research ANSWERED (vendor-confirmed: split at book boundaries); R2 font-pack prep landed. Mac backlog turn-59 = FULLY CLEAR.** The kids cut power right after the review workflow finished — the result survived in the journal (`wf_12a4c922-823`), recovered intact. **Review verdict (`notes/2026-06-09-turn57-58-win-review.md`):** both WIN commits FAITHFUL; 13 confirmed (3 med, rest low/info; dedup → 9 actionable + a verified-correct map), 2 refuted (the CSP fixed-window "survivor" is structurally sound; the artifact-gate provenance is proper lane truth-record). **All actionable fixed this commit:** ① Ethiopic **Extended-B** (U+1E7E0-1E7FF) appended at **7 sibling sites** (stylesheet.css + style_config + app skin `_design.py` + website style.css + dist + LICENSES + ATTRIBUTIONS; the K② recipe's 4-*ranges* were misread as 4-*blocks* — font cmap-verified glyph-backed everywhere incl. the website woff2, re-parsed this session) + `_ETHIOPIC_RANGE` pin re-trued; ② K① numbers-mode sibling: `.note-ref + .vn-link { margin-left: 0.4em }` + kepub `#book-inner` 0.7em twin (adjacency-only — a blanket right-margin would loosen mid-verse typography) + pin; ③ W4 **class** swept: dead `version` dropped from the 5 render_* siblings + the 3-function cascade it exposed (`inject_copyright/dedication/symbol_legend` + `_write_topical_page`; the inject family was never uniform) + an AST self-enforcing guard `TestNoDeadVersionParams`; ④ OPF pin asserts the Noto item's `media-type="font/ttf"` itself; ⑤ fonts README rewritten to shipped reality (was prescribing "acquire Noto SANS Ethiopic via apply_style"); ⑥ W3 emitter-side stays-dead twin; ⑦ spec §4.1 fifth-stack note + test-comment re-true; ⑧ count corrections: b96320a0's "9 stale reds" = **8** (the `_send_json` CSP pin was GREEN at the parent — proactive hardening, not a repair) and W4 swept **11** call sites, not 10. **Gates:** font/marker/topical/skin/phi1 batch **123 green incl. the e2e built-EPUB font gate** (new range + OPF item shape proven in-zip on a real eth build) · presentation-polish 41 · TestBuildEdition+ψ.19.1 46 · topical re-run 62/62 · mypy clean · ruff = baseline · lint 29✓/3 pre-existing warns (the 1 "fail" = this tracker's freshness check, cleared by this very entry). **▶ R1 ANSWERED (`notes/2026-06-09-kepub-pagebreak-research.md`, 4-angle sweep + adversarial re-fetch):** Kobo's OWN epub-spec lists **N for all 12 page-break properties on e-ink kepub** — a decade-persistent, Kobo-acknowledged limitation; **a new spine file is the ONLY guaranteed break** (explains breaks firing at `index_split` boundaries but never mid-file; nesting/`#book-inner` is NOT the variable — the failure is categorical; kepubify is break-neutral). **WIN prescription:** `apply_file_split` forces a piece boundary before every `<section class="book-title-page">` (also collaterally helps K-R2-2's "jump to ToC"), keep the CSS for Apple Books, verify kepubify's dummy-titlepage heuristic post-split, gate on canon-filtered catholic-study. **▶ R2 PREP DONE (`notes/2026-06-09-kobo-font-pack.md`):** `yhwh-kobo-font-pack.zip` shape (flat ttf ×5 + README.txt + LICENSE-OFL.txt — standalone font redistribution MUST carry the OFL text, unlike in-EPUB embeds), Noto Naskh Arabic registered in LICENSES/ATTRIBUTIONS (version/size placeholders pinned at WIN pack-build), Guide copy DRAFTED in-note but **held off the live site until the artifact exists** (never-over-claim). Ship gate = the user's staged-fonts eyeball (does the preview dialog follow the reading font?). **▶ NEXT (win, K-R2 sequence):** repro K-R2-2 → splitter-at-book-boundaries (R1 note = the recipe) → numerals → metadata sweep → rebuild+reload G:\. **▶ MAC:** W1 AB① contingency armed (fires on the user's Apple Books re-test) · M3 armed (dmg on the v0.1.0 cut). Junk swept: a stray empty `uv.lock` stub deleted.
>
> **▶ 🔄 2026-06-09 (🪟 Windows, turn 60) — the K-R2 Kobo round-2 FIX ARC: structural + targeting + sweep slices SHIPPED locally; Mac turn-60 PULLED + RECONCILED (its R1 research independently prescribes the splitter fix I shipped — vendor-confirmed; its font-pack note = the task-6 recipe); NEXT = rebuild eth + kepub → reload `G:\` → milestone push → user round 3.** Shipped (local commits): **K-R2-1/2/4 structural (`6cb498ee`)** — forced book-title singleton pieces in `split_html_document` (a fresh spine file = the only break Kobo kepub honors; also bounds the K-R2-2 navigate-fallback blast radius — the ToC now sits in its OWN piece), ch-heading-CLASS cut candidates (the real base's `page_N` ids gave NO hard candidates → 700-880 KB over-cap pieces; mean piece should now drop ≤ ~target), no-trailing-opener pop rule (kobo9/13's stranded "3" = the ch3 heading at piece 000_00's tail — exactly this seam). **K-R2 reader targeting (`1107ded1`, user-directed mid-session)** — wizard step-1 "Where will you read it?" (everywhere/eink/tablet/computer) + TARGET_CAPS gating (expandable Contents = tablet-only, grayed elsewhere with reasons); `target_reader` field through schema/API/customize; `reader_toc_collapsible` = STRICT opt-in (the books_only coupling + the never-surfaced api_customize_data fields had silently killed the /customize checkbox on every edition). **K-R2-4 (`5e91a742`)** — numerals center on the inner `.section-heading` block (Kobo justification stomps `<p>` text-align). **K-R2-5/6/7/8 sweep (this commit)** — 3,477 alt-name replacements (books.yaml/nav/ncx/h1s/title-attrs; nested-anchor green); OPF 88→83 + per-edition `dc:description` (patch_opf) + eth description; ONE Colophon (front = "Copyright"; closing drops Generated/URN, `closing_colophon` option); K-R2-5 audit = lang markup already correct, NO action. **K-R2-3:** font pack STAGED on `G:\fonts` (user-approved as a website/release artifact — task open: pack + LICENSES for Noto Naskh Arabic [Mac was prepping] + Guide step). **▶ REBUILD GATES (task 5):** eth `--force` → epubcheck 0/0/0/0 · nested-anchor 0 · noteref in-piece resolution · NEW piece-size distribution check (expect ToC its own piece + title singletons + mean ≤ ~530 KB kepub) · kepubify → swap `G:\` (Move-Item, never delete) → 5-leg milestone push. Mac `bbdd9b9` pulled: its R1 research note (`notes/2026-06-09-kepub-pagebreak-research.md`) CONFIRMS the shipped approach (Kobo e-ink kepub honors NO page-break CSS — a new spine file is the only break) and adds two post-split checks for the rebuild gates: kepubify's dummy-titlepage heuristic + a canon-filtered catholic-study gate; its font-pack note (`notes/2026-06-09-kobo-font-pack.md`) = the task-6 artifact recipe (flat ttf ×5 + README + OFL text; Naskh registered in LICENSES).
> **▶ ✅ DONE 2026-06-09 (🪟 Windows) — Mac's M2 finding RESOLVED: the 10 η.1 "sample" seed notes PURGED from Genesis (user GO'd "remove all 10"). The "nothing invented / named PD sources" promise holds again.** Mac's device-QA M2 PASSED (the cascade survives backgrounds-off) and flagged ~10 gen notes shipping with the placeholder attribution _"Reference sample note (η.1) — seeded so every symbol has a displayable example"_ — AI-seeded bodies (ANE context, Gen 1/Gen 2 harmonization, Isaac→Christ typology, the Babel chiasm, the 1 Enoch parallel, …) contradicting the project's core promise. **The user chose REMOVE.** ★ Discovered the seeds live in TWO places (base drift): the live store `content/notes/gen.py` (10 tuples removed; loads 4903→4893) AND the **BAKED base** `epub_working/index_split_000.html` + `_001.html` (10 marker+aside pairs surgically removed — NOT a re-bake, so no drift regression; backed-up + id-matched exactly 1:1). Removing only from gen.py first left the baked bodies as **unattributed orphans** (the live-attribution-lookup architecture) — caught it, then removed from the base too. **Verified:** base nested-anchor 0 + marker/aside balanced (000: 556→551, 001 balanced); eth rebuild → all 10 bodies + "Reference sample note" GONE (0), Gen 1:1 **◈18→◈15**, cascade intact (40,594→40,585 groups); **epubcheck 0/0/0/0**; **175 tests green** (note-rehaul + corpus-index + build-my-bible); integrity sweep = no other placeholder/seed/η.1 attribution in any note store or the base. Corpus 91,733→91,723 (count refs are snapshots, not pins). Shared-base change ⇒ affects every edition's Genesis (intended). **▶ NEXT (win):** CDN-free HOME + rich-text editor + Win `.exe` frozen-verify + θ.4 + D/F. **▶ MAC:** the device behaviours M2-1 (popup-fires) / M2-10 (pagination) ← user's Apple Books + Kobo.
>
> **▶ ✅ DONE 2026-06-09 (🪟 Windows) — eth NOTE-REHAUL RE-BASELINE COMPLETE + fully verified (post-reboot); STAGE-C EPUB staged for device-QA; Mac flipped to M2. PARALLEL (Mac back up); baton windows.** The reboot cleared the 54 GB `AppXSvc` leak (8.4 GB free). Flipped the 3 eth flags `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True in `content/editions.yaml` (schema CLEAN; `git diff` = eth block only). **All gates GREEN:** built `ethiopian-tewahedo --force` (25.85 MB; cascade emitted — 40,594 category groups, `vn-group`==`vn-cat-head`==`vn-cat-sym`) → **byte gate ✓** (catholic-study byte-IDENTICAL before/after the flag flip, SHA `8e0fe3b5…dcdfea7`; the 9-KJV invariant holds empirically + by construction) → **epubcheck 0/0/0/0** (EPUB 3.3, no errors/warnings) → **nested-anchor 0** (`<a>` balanced 190,248/190,248) → **render-verify ✓** per Mac's S2-review gate: (a) comm-ethiopian (jhn 1:1) — group `vn-source-byline` SUPPRESSED (0), the father byline (Cyril/Athanasius `<strong>…</strong> <em>…</em> <small>(date)</small>`) in the BODY only ⇒ **no double-attribution** (BYLINE-1 proven on shipped data); (b) Gen 1:1 = **◈18**, 8 category groups, full `vn-group→vn-cat-head(glyph+label)→vn-source(byline once)→vn-item` cascade with the survivable C1–C6 cues; (c) jhn Cyril byline grammatical. **Ragged-byline failure signatures all 0** (dangling `Bk` 0 / `NPNF Series N` 0 / any `NPNF` 0 / `vol.` 0 — SK-2/POLISH-1 confirmed). note-rehaul suite **49 green**; validate_schemas CLEAN. **STAGE-C eth EPUB staged → `E:\epub-stage-c-eth\` for device-QA** (Mac can also rebuild deterministically from the committed source). **▶ AA MED/LOW arc COMPLETE — the loved η.1 skin is now WCAG-AA + manuscript-cohesive across every console (release-gating for v0.1.0):** ✅ M6 (heat ramp) · M7 (wizard stepper) · M8 (matrix sticky) · M9 (toggle) [prior MED batch `7c673614`] · **M5/M16** (cards → defined gold-line perimeter + soft shadow; the blanket alert-red top-stripe is dropped, now opt-in `.ms-card-accent`) · **M12** (welcome overlay → manuscript: parchment card, ink/sepia serif, gold CTA) · **M13** (greek/hebrew "Look up" → gold primary, unified across console families) · **M14** (apihelp method badges → a coherent indigo/gold/red manuscript trio) · **L4** (header `bg-white/25` chip → gold tint) · **L5** (sources highlight warm) · **L6** (covers slot states warm + the pre-existing placeholder AA fix) · **L7** (publisher focus glow → indigo) · **L9** (EB Garamond self-hosted via a sandboxed `/fonts/` route + `@font-face` — serves 200 `font/woff2`, traversal-guarded). **26/26 `test_skin_aa.py` pins**; visually verified (wizard / build_tracker / apihelp / greek render warm + cohesive, 0 console errors). ACCEPTED as-is (all pass contrast): M10 (4 distinct status-card border/text colours; the indigo info-card is on-brand) · L1 (deliberate amber/emerald caution callouts) · L8 (note-editor status colours — deferred per the editor-separation north star) · L2 (refuted) · L3 (decorative). **▶ NEXT (win):** CDN-free HOME + rich-text editor (Mac's idiot-proof design) + Win `.exe` frozen-verify + θ.4 + D/F. **▶ MAC (turn 48):** device-QA M2 on the STAGE-C EPUB (backgrounds-off matrix) ‖ items 3 (STAGE-F copy) + 5 (dmg recipe). `LANE_HANDOFF` turn 48.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ S2-cascade 2 HIGH fixes (+coupled) DONE + verified; ⛔ eth re-baseline BLOCKED on a 54 GB `AppXSvc` commit-leak (reboot needed); I'm SOLO over BOTH lanes (user: "take over any mac work").** Applied Mac's S2-cascade-review fixes (`notes/2026-06-09-S2-cascade-review.md`), TDD, pinned against BAKED structure: **BYLINE-1** (HIGH) — `_is_self_attributing_comm_ethiopian` now detects the SURVIVING baked shape (the `note-comm-ethiopian` wrapper class + the `<strong>/<em>/<small>` father→work→(date) triad) instead of the stored `<aside>` prefix the sanitizer strips; fixed at BOTH dead sites (S1 `:1987`, S2 `:2488` — fix-the-class) · **SK-2** (HIGH) — `_SOURCE_LOCATOR_RE` absorbs a preceding `Bk`/`Book`/`Hom.`/`Homily` (no dangling `Bk`) · **POLISH-1** — series-strip loops to a fixpoint (no dangling `NPNF Series N`) · **BYLINE-4** — `any()`→`all()` (a self-attributing row can't hide a co-bucketed plain byline) · **S2-GUARD-3** — leaf count hardened to the `<div class="vn-item note-` wrapper via `_count_cascade_leaves` · **S2-GUARD-1** — documented the leaf-count downgrade (construction proof) · **SK-4** — spec count refreshed (22 over-collapse keys = 19 comm-ethiopian-suppressed + 3 rendering, all intended/lossless; verified vs corpus). 7 new/updated tests in `test_note_rehaul.py`; **full suite 49 green** + `TestNoteRehaulS1Helpers/S2SourceKey/S2Cascade` 24 green; ruff format+check clean. **NOT yet committed when the leak was found.** **⛔ BLOCKER: system commit at 99% (`AppXSvc` svchost PID leaking ~54 GB committed — Windows Store-deploy bug; non-elevated restart/taskkill = Access Denied).** This is what OOM-crashed the box earlier + MemoryError'd `test_marker_style`'s build at `build_edition.py:2647`. **A reboot (or elevated `net stop AppXSvc`) frees it; the eth re-baseline + any full build MUST wait for that.** **▶ POST-REBOOT (win, solo):** re-run the pre-commit gate → **eth re-baseline** (add `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` `true` to the ethiopian-tewahedo block in `content/editions.yaml` → build `ethiopian-tewahedo --force` → byte gate [9-KJV byte-identical; eth changes intended] → epubcheck 0/0/0/0 → nested-anchor → render-verify per the review gate: comm-ethiopian verse no double-attribution/no ragged `Bk`/`NPNF`, Gen 1:1 `◈16` cascade, jhn Cyril grammatical) → STAGE-C EPUB. Then AA MED/LOW + HOME + rich-text editor + Win `.exe` + θ.4 + D/F. Baton **windows (solo)**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ app-UX skin AA arc (HIGH set + invisible-data MEDIUMs) SHIPPED (`1ed22783`); pulled Mac turn-45; MAC OFFLINE for the night → I'M SOLO (no Mac planning until the user says so).** Crash-recovery: an OOM hit before push left tonight's work uncommitted but intact — verified (482 skin/console-presentation tests green; `lint_rules.py` 31/0; ruff format clean), rebased onto Mac's turn-45 (`00fc05b0`), committed. **AA arc** (per the user's keep-gold + lighter-hover `#C49A2E` + indigo accent decision, from `docs/superpowers/notes/2026-06-09-eta1-skin-adversarial-review.md`): closed **all ship-blocking HIGHs** — H1 gold-hover 3.46→`#C49A2E` (6.01:1) · H2 dark-input 1.08:1 → theme-aware `--ms-field-bg` · H3/M11 dark-theme-complete (`:root[data-theme=dark]` block + light grounds scoped behind `:root:not([data-theme=dark])`) · H4 hint-text slate-400 ×176 → `#6E5840` · H5/H6 emerald CTAs → gold primary — **plus the invisible-data MEDIUMs** M1 (book-rows) · M2 (matrix counts) · M3 (compare verse-num/missing) · M4 (input border solid `--ms-gold-line`) · M15 (header double-rule). New `tests/test_skin_aa.py` computes true WCAG luminance + pins every pair (14 green). **Pulled Mac turn-45 (`00fc05b0`):** S2-cascade adversarial review (**2 HIGH fix-before-bake** — `docs/superpowers/notes/2026-06-09-S2-cascade-review.md`), HOME-html AA colors (`…-home-html-aa-colors.md`), M2 backgrounds-off QA matrix. **★ The eth re-baseline gate (Mac's S2 review) is now SATISFIED** → actionable SOLO. **▶ NEXT (win, solo, in order):** (1) read the S2-cascade-review's 2 HIGH fixes → apply → THEN the **eth re-baseline** (flip eth `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True → build + byte gate + epubcheck + nested-anchor); (2) remaining AA **MEDIUM/LOW** (M5 card-stripe scope · M6 build_tracker heat ramp · M7 wizard stepper · M8 matrix sticky headers · M9/M10/M13/M14 status-color policy · M12 welcome overlay · M16 card perimeter; L-series incl. L9 EB-Garamond `/fonts/` serving) + HOME AA colors per Mac's note; (3) CDN-free HOME + rich-text note editor + Win `.exe` frozen-verify + θ.4 + D/F. Milestone push = 4 commits (lane_ping fix `f830827a` · S3a `19c7f6cb` · IN_FLIGHT `ab9f45d9` · AA arc `1ed22783`). Baton **windows (solo)**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ✅ note-rehaul S2 (cascade) SHIPPED (`90ac7dc9`) + the S2 attribution-sourcing question RESOLVED (supersedes the "embed into `build_aside` + re-bake" guess a few blocks down). DECISION: a BUILD-TIME LIVE attribution lookup by note id, NOT a re-bake.** The 3-probe drift investigation (`wf_fac9b66a-9ac`) found drift is **kind=0 / ids 100% stable** (only 1,370 stale LABELS + 3 stale bodies — logged as a separate pre-existing finding, possibly the baked labels are RICHER than live's "Note" so re-deriving could regress) ⇒ a live attribution lookup is base-consistent; the re-bake path is HIGH-risk (no clean entrypoint; mutates the SHARED base ⇒ breaks 9-KJV byte-identity + the `build_aside↔rewrite_asides` parity pin + the `categorize_diff` verifier; forces a 10-edition re-release). Shipped: the cascade emission in `apply_badge_markers` + `_source_display`/`_source_key`/`_note_attribution_index`/`_emit_cascade_sections`/`apply_note_cascade_css` (15 group spines, reuse `stylesheet.css:751-791` hues + topic `#5A5F7E`) + the §4 leaf-conservation guard + comm-ethiopian byline suppression + `note_group_by_category`/`note_topic_dedup` wiring; **36 rehaul tests green**; flag absent on every edition ⇒ byte-identical (latent). Pushed as a milestone WITH a fat Mac turn-44 batch (adversarial-review S2 before the re-baseline + M2 backgrounds-off QA matrix + expand STAGE-F + finalize HOME-vs-AA + v0.1.0 dmg recipe). **✅ S3a DONE** (`a94748a8`) — vocab-aware topic union (longest-match against the 5,232-name Nave/Torrey vocab; NO comma-split; one merged Topics row, deduped, `·`-joined, sources cited in the byline); 8 InBuild green (S1 3 + S2 3 + S3a 2). Also `ef4269e0`: lane_ping `--before-push` false-BEHIND fixed (merge-base). **Local commits NOT yet pushed** (remotes at the turn-44 milestone `69a13674`; push at the re-baseline milestone). **▶ NEXT (win), in order:** (1) the **eth re-baseline** (flip eth `note_attribution_dedup`+`note_group_by_category`+`note_topic_dedup` True → build + byte gate + epubcheck + nested-anchor → STAGE-C EPUB to Mac M2) is **GATED on Mac's turn-44 adversarial review of the S2 cascade** (I asked Mac to review BEFORE I bake); (2) **the app-UX skin AA arc in parallel** (`docs/superpowers/notes/2026-06-09-eta1-skin-adversarial-review.md`: ship-blocking HIGHs — gold-hover 3.46→`#C49A2E` 6.01:1 at `_design.py:236-238`; dark-mode input 1.08:1 `:246`; a `:root[data-theme="dark"]` skin block; hint-text 2.58:1×176 `:196`/`:362`; emerald CTAs `wizard.py`; + M-series per-console hex in build_my_bible/sources/matrix/compare/build_tracker — user decision = KEEP gold buttons + `#C49A2E` hover + INDIGO `#243B6B` accents where gold fails; **must NOT break the loved skin** → careful, verify contrast). Then the CDN-free HOME + rich-text note editor (Mac's `2026-06-09-idiot-proof-app-design.md`) + Win `.exe` frozen-verify + θ.4 + D/F. Baton **windows**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — ★ NEW APP-UX ARC: shipped the η.1 manuscript skin (whole-app, loved) + v0.1.0 RE-PLANNED around an "idiot-proof app" north star. ★ READ `docs/superpowers/notes/2026-06-09-v0.1.0-app-ux-replan.md` FIRST.** A live design session with the user produced: **η.1 manuscript skin** (`82144f6a`) — whole-app console skin matching www.yhwhyaway.com, injected at `web.py _send_html` (Tailwind palette remap + accent layer: beige body, dark-brown BANNER header + gold rule, GOLD primary buttons, gold-tint defined borders, serif body) + note-editor layout (aligned capitalized 3-col headers Books/Select-a-book/Editor, wrapping book titles, example placeholders) + a lint fix (`repo_map_complete` skips gitignored runtime dirs). User-validated live ("beautiful", "clean rectangles", "there ya go"). **★ NEW NORTH STAR (user, emphatic): the SHIPPED app must be IDIOT-PROOF for end-users** — readers/builders, NOT maintainers. The note-editor (raw HTML, JSON, kind budgets) is a MAINTAINER tool → must be separated from / not the default landing; replace raw-HTML note editing with a Bold/Italic rich-text toolbar; a friendly landing with the social-card BANNER as hero. **★ v0.1.0 RE-PLAN (user-requested "replan everything + fat Mac backlog"):** WIN = S2 cascade (resolve attribution-sourcing/base-rebake first) → S3a → app-UX idiot-proof impl (after Mac's design) → verify frozen-fix on Win `.exe` → θ.4 update feature → D/F. **MAC = a FAT parallel backlog** (LANE_HANDOFF turn 42): (1) DESIGN the idiot-proof app [unblocks WIN], (2) prep EB-Garamond self-hosting, (3) draft STAGE-F outward copy, (4) adversarial review of the η.1 skin across all consoles, (5) device-QA M2 ← STAGE-C EPUB. **Local commits NOT yet pushed before this milestone save.** Baton **windows**.
>
> **▶ 🔄 2026-06-09 (🪟 Windows) — STAGE C: ✅ STAGE E landed (Mac M1 CLOSED) · note-rehaul S1 DONE locally (`f404686d`→rebased `f01f64d9`) · update-feature decided. INCOMING from Mac turn 41 (3 asks).** **STAGE E (`23e3c178`, pushed all 5 legs):** `launcher.spec` macOS pyobjc `hiddenimports` (webview.platforms.cocoa + objc/Foundation/AppKit/WebKit/Quartz/Security/CoreFoundation/UniformTypeIdentifiers — the real finding-7 fix) + `.icns` BUNDLE icon + `requirements-desktop` marker-gated pyobjc pins + explicit native→browser fallback in `launcher.py`; 129 desktop tests green. Lane board turn 39 hands Mac **M1** (native-window dmg, now actionable) + M0 fallback; ACKed Mac's 5 turn-38 follow-ups (object-fit, regression tests, docstrings, SESSION_STATE scope, Kobo) into STAGE C. **Note-rehaul S1 (`note_attribution_dedup`) DONE locally — TDD, base-consistent:** 6 pure helpers + the in-`apply_badge_markers` integration + wiring (web_editions / api·editions EDITABLE+EDITABLE_BOOL / customize checkbox); 14 S1 tests + the 6 badge-unit back-compat tests green; flag-off = byte-identical (the 26-pass eth integration + gen conservation prove it). **★Spec re-verified against the LIVE corpus (8-dim read-only workflow) — corrections found:** S3a "split topic terms on comma" is WRONG (250 topic names carry internal commas → 36.9% of topic notes would mis-parse → S3a MUST use the authoritative Nave/Torrey vocab, longest-match); the source-key PD-boilerplate strip doesn't actually clean TSK's tail; comm-ethiopian self-attribution = the unique `<aside class="note-comm-ethiopian">` structural marker (up to 4 notes/verse). **★★BASE-DRIFT FINDING:** `epub_working/` has drifted from the live notes (gen 1:2m = kind `word`/label "Note" in gen.py but renders lang-hebrew/"Hebrew." in the baked base) → S1 redesigned to operate on the RENDERED row + marker kind (NOT the live tuple) so it's correct regardless of drift. **S2 must source the attribution byline base-consistently** — likely embed attribution into `inject.build_aside` + re-bake (which ALSO fixes the drift); decided at S2. A wider `inject --all-books` + BAKE-AND-PROVE re-bake is a flagged precursor/parallel task. **Update feature (user-decided 2026-06-08):** θ.4 = wire the existing `core/updates.py` appcast data plane into the launcher (startup poll → gentle prompt + in-app release-notes); scope = adoption-signal + one-way notes, **NO telemetry** (roadmap LANE-T `update-feat`); build in the desktop stage. **▶ NEXT:** S2 (cascade emission + 15 per-category group-spine CSS [14 hues extracted; topic needs one, e.g. `#5A5F7E`] + comm-ethiopian byline suppression + the §4 `DISTINCT_OUT==DISTINCT_IN` guard) → flip eth flags True (single re-baseline) + full byte gate + Mac device-QA → S3a (vocab-aware topic union) → S3b later. **Local commits NOT yet pushed** (push at the cascade milestone). Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE C IN PROGRESS: 3 surgical fixes shipped (findings 3+2+1b); note-rehaul cascade is the next major arc.** Done + committed locally (suite green; push at the STAGE C milestone): **finding 3** title-page bleed (`d2970962` — `.bookpage-art` max-height:42vh + `.book-title-frame` display:block/break-inside:avoid; eth epubcheck 0/0/0/0 + in-EPUB content verified; on-device paginate-render = Mac M2) · **finding 2** Your-Edition table → float `.ye-row` block (`d2970962`) · **finding 1b** justify-scoping (`1ae3a2cc` — rewrote the stale 2026-05-24 `TestLeftAlign` "no-justify" contract to the real one [prose justified by default + a build GUARD that no ToC/title/heading/pill selector resolves to justify]; resolved 2 PRE-EXISTING reds; defensive ToC explicit-left CSS deferred pending Mac M2). **★Lesson:** on the cap=2 box NEVER run pytest concurrently with a build — overlapping runs starved each other to 35–48 min each (clean solo = fast). **▶ NEXT MAJOR ARC = the note-rehaul (S1→S2→S3a) — spec READ + internalized:** `docs/superpowers/specs/2026-06-08-note-presentation-rehaul-design.md` (build-time, lossless, option-gated; cascade verse→category→source→note inside `apply_badge_markers` `build_edition.py:1856-2074`; the NON-NEGOTIABLE completeness guard `DISTINCT_OUT==DISTINCT_IN`; 15 explicit per-category group-spine CSS; 4 bool fields wired across `web_editions`/`api/editions`[EDITABLE_BOOL+EDITABLE]/`templates/customize`/`build_edition`; Addendum A legend popup = a POST-`apply_file_split` per-piece pass; per-stage byte-stability proof). Sequence: S3a+S1 → S2 → re-baseline eth + Mac device-QA → S3b (default-OFF) later. Then ToC pill-toggle+incipit (Ph3 / finding 1·1c), finding 6 app top-nav (web-only, no EPUB), audit Phase 3 (splitter orphan Notes heading), Ph4/Ph5. Then D → E (unblocks Mac M1) → F (v0.1.0). **▶ Mac:** v0.1.0 plan handed (M1→M4); idle pending STAGE C EPUB / the E spec edit. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — ✅ STAGE B CLOSED + STAGE A COMPLETE; now on STAGE C.** STAGE B 3 real-build re-verifications ALL GREEN: byte-stability gate PASSED (deterministic; 9-KJV byte-identical by construction — build path + `epub_working` unchanged since baseline `b5ad8c98`, only 4 off-path `scripts/core` deltas) · epubcheck 0/0/0/0 on eth + catholic-study (no RSC-007/008; font chain reconciled — `apply_style` off-build-path, `@font-face` only in `epub_working/stylesheet.css`, `patch_opf_fonts` registers exactly `EMBED_FONT_PATHS`) · triple-seam on canon-filtered catholic-study CLEAN (audit 0 critical + scanner: spine gapless, `BOOK I..LXXII` gapless, cross-piece hrefs intact over 89,874 ids, 0 nested-`<a>`). No new defect. STAGE A clone-hoists shipped (`run_hebrew`/`run_greek` + `run_ai_notes`/`run_ai_xrefs` → `at_scale_base`; 8 tests re-homed; 85 at-scale + 6 bugcluster green; ruff/mypy/lint clean). **▶ NEXT = STAGE C** (presentation + note-rehaul: audit Phase 3 splitter-orphan · justify-default + guard · note-rehaul S1→S3a + Addendum A legend popup · ToC toggle + incipit · finding 3 title-page bleed · finding 2 Your-Edition table · finding 6 app top-nav) → D → E (unblocks Mac M1 native-window dmg) → F (v0.1.0). **▶ Mac:** v0.1.0 EXECUTION PLAN handed (`docs/superpowers/notes/2026-06-08-mac-lane-v0.1.0-execution-plan.md`, M1→M4 gated on WIN C/E/F); idle pending STAGE C/E. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE B data-validity cluster CLOSED; session-boundary milestone (user starts a NEW SESSION after this).** Candidate cleanup (114 out-of-extent removed — 39 files deleted + 44 rewritten, the audit's "111" undercount confirmed) + the `candidate_extent` lint (Phase 0 #2; ALL_CHECKS 32→33, +negative test, green) + **gap #4 answered:** promote validates at the boundary (`:368`/`:469`); the published-store real-loader sweep found only 3 out-of-extent notes (aes 10:11-13 lang-hebrew, NON-shipping parked residual) → **ZERO impossible coords ship**. All gates green. **▶ WIN NEXT SESSION (fresh start):** the 3 REAL-BUILD re-verifications (i font-embed byte/epubcheck · ii post-pass×canon-filter×file-split on built catholic-study · iii published-store DONE) → 2 STAGE-A clone-hoists → STAGE C (Mac diagnosis: title-page page-BLEED fix + stats-table fix + extra-popup Addendum A) → D (WIN `.ico`/Linux `.png`; Mac `.icns` done) → E (Mac PROVED native-window deps) → F (outward+release; absolute-sync). **▶ MAC:** idle pending WIN C/E. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — STAGE A done (−2 gate-nothing clone-hoists) + STAGE B keystone & build-free latent holes shipped; milestone pushed.** STAGE A Phase 5 finished (`a1552624`). **STAGE B Phase 2:** the `aes`/`_book_shape_cached` CLASS fix (`6045eb36` — scan-skip-empties + `html_chapter_count`→max(shape) + `TEWAHEDO`+=1cl/2en; byte-identical for the 80 contiguous books; promote guard now LIVE for aes — rejects ch17 / accepts ch11) + the build-free latent holes (`c78eb193` — edition_stats cache twin · prospect verse-gap skip · Naves/Torrey book-code normalize). All gates green (ruff/lint 30-0/mypy + targeted tests). **▶ WIN NEXT — STAGE B data-validity cluster + REAL BUILDS:** candidate-extent lint (Phase 0 #2) + 111-candidate cleanup (RECOUNT post-aes-fix — aes candidates now correctly flagged) · published-store out-of-extent sweep + promote-boundary trace (gap #4) · the 3 real-build re-verifications (font-embed byte/epubcheck · post-pass×canon-filter×file-split · published stores) → then the 2 STAGE-A clone-hoists → C/D/E/F. **▶ MAC (turn 34):** render-first diagnosis (title-page + stats-popup, for STAGE C) + macOS native-window finding-7 preflight (de-risk STAGE E); icns + Addendum A already landed. Baton **windows**.
>
> **▶ 🔄 2026-06-08 (🪟 Windows) — EXECUTING the v0.1.0 master plan (user ratified via "do"); STAGE A green slice committed + pushed; Mac handed 2 parallel tasks.** The FINDINGS-ONLY HOLD is LIFTED. **STAGE A shipped (test/doc/lint, 0 shipped-byte risk, all green):** audit **Phase 0** (extended `check_atomic_writes` to `content/notes` `.write_text`/`.write_bytes` + 3 atomic fixes incl. the audit-missed `fix_xref_targets.py:254`; PUT route coupled 10→11; +7 guard tests) + **Phase 1** (all 9 stale-test/doc realignments to v0.0.3 reality — canon 76/78/87→75/77/86, EMBED_FONT ×3, comm-reformation→jewish-study, retired-term, website 87→83, corpus-index EXACT via canon-restrict, 2es title, 4× REPO_MAP — numbers re-verified) + **Phase 5 partial** (render_coverage dup/mislabel +test; dead `book_native_names` helpers; `_append_cloned` numeric-0). Gate green: ruff + lint 30/0 + mypy + test files (incl. a 194-pass run). **▶ STAGE A Phase 5 now ALSO done (local `a1552624`):** bugcluster coord-guard sweep test exercises each driver's `_emit_extent_ok` · coverage shares `_compile_injected_id_re` + behavioral test · Greek/Easton/Torrey negative fires-on-defect lint tests. **Only the 2 at-scale clone-hoists remain in STAGE A** (`run_hebrew`/`run_greek` + `run_ai_notes`/`run_ai_xrefs` → `at_scale_base`) — they GATE NOTHING (offline tooling), so **▶ WIN NEXT = STAGE B first** (higher value: aes/`_book_shape_cached` CLASS fix · edition_stats stale-cache twin · candidate-extent lint [Phase 0 #2, moved here] · 111-candidate cleanup · 3 real-build re-verifications), then mop up the 2 clone-hoists, then C/D/E/F. **▶ MAC NOW (no WIN dep, pulled forward from release-time):** (1) `assets/icons/YHWH.icns` via `iconutil` (unblocks STAGE D); (2) design the user-sanctioned **extra note-helper / legend popup** as a note-rehaul spec addendum (native EPUB3 footnote, no-JS, reader-robust, per-edition builder option) for WIN to implement in STAGE C. Release-time Mac items unchanged (native-window dmg · v0.1.0 artifact · device-verify). **3 user directives recorded** (master plan + memory): outward-sync = ABSOLUTE everywhere incl. edit/delete-repost + DRAFTED X follow-ups; extra-popup latitude. `LANE_HANDOFF` turn 32. Baton **windows**.
>
> **▶ ✅ MILESTONE 2026-06-08 (🪟 Windows) — ROUND-6 AUDIT MERGED + v0.1.0 MASTER PLAN written; FINDINGS-ONLY HOLD (product fixes await the user ratifying the plan).** Win's 4 heavy dims (Opus) → 13 survivors; merged with Mac's 30 → **43 (0 crit/high; the 9-edition program is functionally MINT)** → `docs/superpowers/notes/2026-06-08-round6-split-audit-findings.md`. **★ Single source of truth for what's next = `docs/superpowers/plans/2026-06-08-v0.1.0-master-plan.md`** — sequences audit + device-QA 1–7 + note-rehaul S1–S4 + app icons + outward surfaces → **v0.1.0 (still beta)**, safest-first (A green/honest → B latent holes → C presentation → D icons → E mac native-window → F outward+release); lane division inside. Committed `deep-audit.js` default flipped to **Opus**. **▶ ON BOOT (either lane):** read this board → the master plan → the findings note + the device-QA note. **Win** owns shared-code impl (Stages A–F); **Mac** = the turn-30 laundry list (START the note-rehaul design spec now; release-time `.icns` + native-window dmg + v0.1.0 artifact + device-verify). **DO NOT apply product fixes until the user ratifies the master plan.** `LANE_HANDOFF` turn 30. (The Mac DONE/HOLDING entry + the stale WIN round-6 on-boot runbook below are SUPERSEDED by this.)
>
> **▶ ✅ DONE / HOLDING 2026-06-08 (🖥️ Mac) — round-6 MAC audit COMPLETE + device-QA captured + v0.1.0 target set; AWAITING winclaude's merge + v0.1.0 fix instructions. DO NOT re-run the audit.** Mac ran `LANE='mac'` (14 dims) on **Opus** → **30 survivors / 0 unverified** → `_audit-split/findings-mac.json` @ `0e1e122c` on `lane-transfer/audit` (both remotes); **win merges** (truth_owner). Then captured the user's real-device QA (8 screenshots) → `docs/superpowers/notes/2026-06-08-device-qa-and-note-presentation-rehaul.md` (7 findings, reconciled onto the existing `2026-06-06` device-QA plan/spec; note-presentation north star = pretty on ANY reader / reader-robust structure / cascade hierarchy; **release target = v0.1.0, STILL A BETA**, v1.0.0 deferred further out). Shipped the `memory_hygiene` Mac-parity fix (`cc5b4907`) + codified RULES **guard #6** (cross-lane problem hand-off). **Mac lane is IDLE (findings-only hold) — next instructions come FROM winclaude** (its 4 heavy dims → the merge → the v0.1.0 fix sequence); **finding 7 (macOS native-window / browser-fallback) is a MAC-lane build task** when that starts. `LANE_HANDOFF` turn 29. (The 🪟 round-6 boot order below is the WIN lane's — still finishing its heavy dims; not a Mac action.)
>
> **▶ ON BOOT 2026-06-08 (🪟 Windows) — RUN THE ROUND-6 SPLIT DEEP-AUDIT (FINDINGS-ONLY, BEFORE FIXES). This is the marching order; everything is wired + pushed (`c5c1ba2a`).** New standing doctrine is LIVE (RULES Guard #5 + §4: never stop to ask the user · full standing authority · bandwidth-first / bare-minimum context · **LOCAL-COMMIT during work, full 5-leg push only at a milestone**). The lane sync **ping** is wired (`scripts/lane_ping.py` → the SessionStart radar + `save-all.ps1 --before-push` auto-rebase). The auditor is **refreshed to round 6 + READY**. **Plan + Mac meantime-backlog: `docs/superpowers/plans/2026-06-08-round6-split-audit-plan.md`.**
> **WIN-LANE steps:** (1) `git pull --rebase origin main` (the boot radar flags it if Mac pushed). (2) Edit `.claude/workflows/deep-audit.js` LOCALLY, do NOT commit: `const LANE = 'win'` (that one line now also fixes REPO + agent types — parity is baked). (3) `Workflow({scriptPath:"<repo>/.claude/workflows/deep-audit.js"})`; **CONFIRM the startup log = 4 dimensions** (tests-run · opt-build · byte-stability · rx-surfaces). (4) Finders/verifiers are sonnet-pinned, cap=2 — do NOT bump to Opus or add finders (cost: ~$80/1h lesson). Sleep/hibernate OFF, terminal open. (5) Parse `JSON.parse(<result>).result.survivors`; keep locally. (6) When Mac's `findings-mac.json` lands (branch `lane-transfer/audit`), run `deep-audit-merge.js` on the N95 → write `docs/superpowers/notes/2026-06-08-round6-split-audit-findings.md`. **STOP — present findings, do NOT apply fixes.** (7) Milestone 5-leg save of the findings doc.
> **MAC LANE** runs `LANE='mac'` (14 read-only dims) per the plan, mirrors its out-of-repo halves (doctrine + ping memories · its SessionStart-hook ping block · Mac save-path `--before-push` wiring) + ACKs turn 23, and works the **meantime-backlog** if it finishes before the win heavy lane. Baton **windows**. `LANE_HANDOFF` turn 23.
>
> **▶ 🔄 2026-06-07 (🪟 Windows) — v0.0.3: full-notes Ethiopian Bible + device-QA fixes; committing + pushing + deploying.** Autonomous, user-directed (low bandwidth → local commits, push/deploy at the end). **Versioning reset (user): v0.0.1/v0.0.2 = former beta.1/beta.2 → this = v0.0.3; first full = v1.0.0.** Device-QA fixes a–i: full apparatus (91,563 notes, all popups) + cross-verse dedup + justified notes (h); Psalm superscriptions wrapped (g); empty Additions-to-Esther dropped + Daniel additions demoted to appendices + per-edition BOOK-eyebrow renumber → **83 books** (f); cleaned ", or …" names (e); in-content ToC book+chapter pills (b); front-matter blank-page/colophon fit (d); Your-Edition table fixed-layout (a); website count 87→83 + v0.0.3 + de-dup release card (i). Built + **epubcheck 0/0/0/0**; kepub regenerated. **▶ NEXT: push both remotes · `gh` release v0.0.3 (EPUB+kepub+SHA256SUMS) · build+deploy website · new X post · then the website "how to use" step-by-step page · (desktop-app rebuild to v0.0.x is a follow-up — Mac-gated).** Baton **windows**.
>
> **▶ ✅ DONE 2026-06-06 (🪟 Windows) — v1.0.0-beta.2 RELEASED + website DEPLOYED LIVE; chapter-start verse-boundary bug FIXED.** Autonomous beta-2 device-QA session. **Shipped + committed + pushed (both remotes) + bundled E:/F::** **P1–P4** (`8fb606aa`): ◈ note badges, popup dedup (audit 1043→0) + category-grouped tinted cards, justify + 1.05em + reader-native serif, centred book title pages, clickable in-content ToC, book-level native ToC, structural-audit tool (`scripts/audit_epub_structure.py`). **★Verse-boundary fix (`809f711f`):** 161 chapters re-split (empty-v1 + WEB v1&2 merged-under-v2 → correct v1/v2), KJV/JPS-anchored + confidence-gated (`scripts/_fix_chapter_verse_boundaries.py` — `--dry-run` lists the **116 FLAGGED** archaic/genealogy/apocrypha chapters left untouched). **De-hyphenation (`b0e85fbd`):** 64 OCR "word- word" breaks (`scripts/_dehyphenate_notes.py`). **Release (`2998cddf`):** `gh` v1.0.0-beta.2 prerelease = the full **ethiopian-tewahedo** EPUB (`--force`, NOT cached — verified Exo 2:1 holds its text + `◈3`) + a Kobo **`.kepub.epub`** (kepubify v4.0.4 in gitignored `.tools/`) + SHA256SUMS, clean filenames; audit 0-crit + epubcheck 0/0/0/0. **Website (`0db4a3eb`/`71fbfce` monorepo + `gringoboggy/yhwh-website` Pages):** releases.html offers the one full Ethiopian EPUB + the kepub, a capped-3 "What's changed" log, live-beta copy; **DEPLOYED LIVE at www.yhwhyaway.com (HTTP 200 verified).** **▶ MONDAY = first FULL v1.0 (NO MORE BETAS; user plans bandwidth reset → 1-2 heavy audits → fix what surfaces):** the 116 flagged verse chapters · kepub per-device font (Apple-larger plain / Kobo-smaller .kepub) · empty/blank-page (⑫) · front-matter "Edition⟷ID" stretch (⑬) · "two popups, same translations" dedup · website marker/symbol guide + single-download UX + step-by-step "how to use" page (do WITH the user's screenshots) · rebuild + re-sign desktop apps (Win Azure + **macOS notarize on the Mac** + Linux CI) · full-markers stress copy. Refs: `notes/2026-06-06-chapter-start-verse-boundary-bug.md`, `specs/2026-06-06-beta-device-qa-presentation-design.md` + plan. Baton **windows**.
>
> **▶ (superseded) 2026-06-06 (🪟 Windows) — BETA-2 POLISH P1–P4 SHIPPED + VERIFIED (`8fb606aa`); ★verse-boundary bug FIXED 161 ch (`809f711f`) + OCR de-hyphenation done (`b0e85fbd`).** **DONE + verified on a clean catholic-study rebuild (cache cleared) + live Playwright render:** ① **◈ note badges** (no more bare-number clutter), ② **dedup** (audit `scripts/audit_epub_structure.py`: 1043 dup note-rows → **0**), ③ **category-grouped tinted note cards** (topical last; soft per-category fills + accent border — confirmed `note-comm` = soft blue), ⑤ **justify** body + **1.05em** Apple-tuned size + **reader-native serif** (dropped embedded Cardo from body, kept on Heb/Grk popups), ⑨ **clickable in-content ToC** label, ⑩ **centred book title page** (root cause: `body p{text-align:left}` was directly left-aligning the `<p>` eyebrow/subtitle while the `<h1>` title inherited center → explicit `.bookpage-* {text-align:center}` fixes all 87), ⑧ **book-level native ToC** default (new `reader_native_toc_chapters` opt-in), ④ **structural-audit tool** (DUP_NOTE_ROWS/DUP_IDS/BROKEN_NOTEREF/UNBALANCED_TAGS/NEAR_EMPTY). epubcheck **0/0/0/0**, badge unit tests pass, ruff/lint/mypy clean, pre-commit green. **★BLOCKER for "in business" (user) — CHAPTER-START VERSE-BOUNDARY BUG:** at most chapter starts the base has an **empty verse-1** + WEB verse-1&2 **merged under number 2** (e.g. Exo 2: "1 ◈2 2 A man of the house of Levi…" — "A man…" is WEB 2:1 but sits under "2"). Pre-existing in the recovered base (NOT beta-2 work); ~301 chapters (over-count, needs precise scan). **Root-caused + full fix plan in `notes/2026-06-06-chapter-start-verse-boundary-bug.md`** — needs a careful, confidence-gated re-split aligning the merged WEB text against the on-disk reference translations (KJV/JPS/Douay/WLC/LXX vpl — user: "we should have every source to check"); **FIXED 2026-06-06 (`809f711f`):** repaired **161 chapters** via `scripts/_fix_chapter_verse_boundaries.py` — a content-preserving reorder + one KJV/JPS-anchored, confidence-gated split; verified exo2/gen1/exo13/gen25/lev4 correct, audit 0-crit, epubcheck 0/0/0/0, idempotent, note-count 48190 unchanged; **116 archaic/genealogy/apocrypha chapters FLAGGED** (left untouched — `--dry-run` lists them) for a follow-up. **OCR de-hyphenation DONE (`b0e85fbd`):** 64 "word- word" breaks repaired (curated, `scripts/_dehyphenate_notes.py`). All fixes bundled to **E:/F:**; **building the real `ethiopian-tewahedo` beta2 EPUB → `E:\` now** (badge mode, all fixes, for device testing). **▶ RELEASING v1.0.0-beta.2 TODAY** (the fixed Ethiopian Bible EPUB is the core; Win Azure-sign + Linux CI; **macOS dmg follows when the Mac is up** — clobber onto the tag per the beta.1 pattern). The first FULL **stable** release is ~Monday (bandwidth reset + 1-2 heavy audits → fix what surfaces). Beta2-today todo: bump VERSION → beta.2, cut the GitHub release + checksums, website update-log + releases auto-surface. Deferred to the Monday pass: website marker/symbol guide + single-download/update-log + step-by-step usage page (do WITH the user's screenshots), kepub Kobo target + per-device font (⑦), empty/blank-page (⑫), front-matter label-stretch (⑬), the full-markers stress copy. ⚠ **remotes behind** — pushed through `0c0c08dd`; verse-fix + de-hyph are local + E:/F: only (push at full-done, per user "commit to E/F to save time"). Baton **windows**.
>
> **▶ (superseded) 2026-06-06 (🪟 Windows) — BETA DEVICE-QA POLISH → v1.0.0-beta.2: spec + 8-phase plan committed; executing autonomously (user GO: "bang it all out… don't ask questions").** User device-QA (Apple Books iOS + colour Kobo) surfaced **13 polish items**; full design `specs/2026-06-06-beta-device-qa-presentation-design.md` + plan `plans/2026-06-06-beta-device-qa-presentation-plan.md`. Scope: distinct note(**◈**)/translation markers · colour-coded **tinted de-duped note cards** (grouped, topical last) · **justify** + per-device font (Apple larger plain EPUB / Kobo smaller **.kepub**) · **book-level native ToC** + kepub popups · clickable in-content ToC · **centred book title page** (eyebrow/subtitle were left, title centred) · empty/blank-page fix (splitter + title-frame) · **OCR de-hyphenation** + structural audit + lint guard · front-matter label-stretch · website **single-download + capped(3) update log** → cut **v1.0.0-beta.2** (Win Azure-sign + Linux CI + EPUB now; **macOS notarize Mac-gated** → follows). Executing 8 phases, commit per phase, final 5-leg push. Baton **windows**.
>
> **▶ ⏸ 2026-06-06 (🪟 Windows) — DEVICE-QA round-2 logged; beta LIVE + stable on all 3 platforms; ALL LANES ON A BREAK (user-directed: "we've all done enough today").** The user device-tested the live beta. ✅ **Kobo: the 2 big blockers are fixed** (no more crash / font legible). ✅ **Apple Books: "way better, very good beta."** Open polish (full detail in memory `kobo-color-ereader-end-stage-qa`, **none blocking**): **★recurring per-book title-page misalignment** ("number left, title center" — the CSS is ALREADY centered, so **render-to-pin the off element BEFORE coding**; don't blind-tweak CSS again ← the priority polish) · Kobo native-ToC too verbose (dial RX P4a-2 to book-level) · Kobo popups don't fire (EPUB3 footnote → Kobo fallback) · note-numbering vague (needs an example) · a numbers-mode (~91,733-marker) stress build (the user did this himself). **Pulled Mac's wrap** (`79634446`: socials LIVE, download buttons wired, outreach pack → `notes/2026-06-06-outreach-pack.md`). **▶ NEXT (whenever the user re-engages):** (1) title-page render-then-fix (priority); (2) Mac's 2 prepared web-submissions (faith.tools + christian_foss) with corrected all-platforms copy; (3) the user's first personal outreach send (Beta maṣāḥǝft). Baton **windows**.
>
> **▶ ✅ DONE 2026-06-06 (🪟 Windows) — `v1.0.0-beta.1` LAUNCH COMPLETE: Windows signed + in the release, site warning removed + live, Linux AppImage BUILT on CI — all 3 platforms + the Bible shipped.** Windows `YHWH.exe` SIGNED via **Azure Trusted Signing** (Valid · `CN=Bogdan Zorlescu` · timestamped — full recipe in memory `reference_windows_signing_azure` + reusable `dev/sign_windows.ps1`; ★role = **`Artifact Signing Certificate Profile Signer`** assigned **by ObjectId**, Owner ≠ signer). Uploaded the signed exe + the **Ethiopian Bible** EPUB + a merged `SHA256SUMS.txt` to the existing `v1.0.0-beta.1` tag (`--clobber`; **ONE Bible per the user**, not 2). Removed the unsigned/SmartScreen warning copy from `releases.html`/`index.html` + **deployed LIVE** (`yhwh-website` `4743f71`, www.yhwhyaway.com). **Linux AppImage BUILT on CI** — `.github/workflows/build-linux.yml` (GitHub Actions Ubuntu → PyInstaller → AppImage) ran green; hardened a 2nd pass (+ Pillow for cover rendering, pinned PyInstaller 6.20.0, self-merging checksums). `YHWH-1.0.0-beta.1-x86_64.AppImage` (301 MB) **LIVE in the release** + merged into `SHA256SUMS.txt` (all 4 assets checksummed). Socials → Mac. Repo PUBLIC; the release ships **all 3 platforms — Windows (signed) / Linux / macOS (notarized) — + the Ethiopian Bible**. **▶ OPEN TAIL (optional, not blocking):** add the catholic-study EPUB to the release if the user wants a 2nd Bible; the Linux build is web-mode (pywebview/GTK skipped) → a native-shell Linux build is a future nicety. Baton **windows**.
>
> **▶ ✅ DONE 2026-06-05 (🪟 Windows) — WIN AUDIT FIX-SLICE shipped → round-5 release gate CLEAR (both lanes done).** Mac `a49f1be7`+`30debc58`; Win this commit. Build-path slice: **Phase 1** = the HIGH `edition_stats`→`_PIPELINE_SCRIPTS` stale-EPUB cache-miss, expanded *fix-the-class* to the full build-path-core **transitive closure (25 modules → +10 covered** incl. `config`/`translations` live-bake + `corpus_index`/`sources_lexicon` behind the `matrix`/`sources` shims, **12 waived)** + a transitive-closure self-enforcing guard (Windows-owned `test_build_cache.py`, off Mac's `lint_rules.py`); **Phase 4** = badge orphan-skip stat (additive) + `filter_html` per-kind→single-alternation consolidation. **Proven byte-IDENTICAL to HEAD** (before/after full-build digests: catholic-study/jewish-study/ethiopian-tewahedo all unchanged) + determinism gate + 43+4+14 tests + ruff/lint_rules/mypy clean + a **7-agent adversarial review** that approved filter_html/badge AND caught the 2 mis-waivers (`config`/`translations`) + the transitive guard blind spot → all fixed pre-save. **▶ The release `v1.0.0-beta.1` gate is now CLEAR — public flip + release await the user's GO.** Findings doc `notes/2026-06-05-round5-split-audit-findings.md` Win boxes ticked; Mac's deferred `check_atomic_writes` lint extension is unblocked. Baton **windows**.
>
> **▶ READY 2026-06-05 (🖥️ Mac) — POST-BETA GROWTH PLAN written (PLAN-only): "eyes on the site & program."** `plans/2026-06-05-post-beta-growth-plan.md` (INDEX 68→69; workflow `wf_16ef99cc-4bb`, 8 agents). Faith-grassroots spine + SEO graft + ONE deferred Show HN; two-funnel insight (faithful/heritage on Telegram/FB/YouTube/mailing-lists vs OSS/Bible-tech on GitHub/HN/search); hero = the rare manuscript reader. Phases 0→4 + milestone-only steady-state; inviolable guardrails (never mention donations in outreach · gift-not-pitch · doctrinal humility · ~1–2 hr/wk). Claude drafts; Playwright drives posting (no API keys); the human relationships are the user's. **▶ Phase 0 content SHIPPED + DEPLOYED LIVE (user GO'd):** 3 evergreen pages live at www.yhwhyaway.com (`yhwh-website` `8190057`; monorepo `86441d89`) — `about-the-geez-bible.html` (sources/method/free-forever + correction mailto) · `ethiopian-bible-canon.html` (the 81-book canon — Enoch/Jubilees/Meqabyan) · `copy-the-bible-text.html` (the copy-paste differentiator); plain/reverent, **no donation CTAs**, linked from `geez.html`, in the sitemap. **★Also LIVE this turn — EN-indicator fix (`278c1c90`):** moved the English badge OFF the book pills (the `_en_books` ≥50-row threshold only Psalms cleared → 1ki/1sa/2sa looked English-less though every chapter has it) ONTO the per-chapter cells in the book index — honest per chapter, future-proof for Geʽez-only chapters; progress pills now show stage only. Verified live (pills 0 `pb-en`; psa index 152 `rdx-en`; 1ki 7). **✅ AI-method disclosure RESOLVED (user-directed "state our exact method") + LIVE** (`yhwh-website` `9b79846`; monorepo `ab9fba6b`): the About page now states the full method plainly — read off the images with AI vision-language models, re-read & compared, collated against a 2nd witness where one exists, **calibrated-not-harmonized** (differences flagged, never quietly corrected), reviewed + reader-corrected; the English a separately-checked literal back-translation. Vendor now NAMED — "**Claude** (Anthropic's AI)" (user already discloses Claude); LIVE (`yhwh-website` `57c0e62`, monorepo `a284804f`). **★Separately — macOS NOTARIZATION DONE this session:** the auto-finisher accepted submission `782d48b8`, stapled `YHWH-1.0.0-beta.1.dmg` (`stapler validate` ok) + regen `dist/SHA256SUMS.txt`; `dev/NOTARIZATION_STATUS.md`=DONE — the long-stuck notary launch-prep item is CLOSED. Remaining Phase 0 = must-fix QA (`releases.js` API fix, HTTPS, Giscus, email-forward). Outreach phases 1–4 run AFTER `v1.0.0-beta.1` ships. Baton **mac**.
>
> **▶ ✅ DONE 2026-06-05 (🖥️ Mac) — WEBSITE CHAPTER-READER: planned (spec+plan) → BUILT + pushed → DEPLOYED LIVE (user pivoted PLAN→BUILD this turn).** Built the Geʽez/Amharic Bibles + their literal-English ON the website — **only pills with actual transcribed work are clickable now.** Gate = the progress map's own-versified signal (◑/●), never OCR ◐; per chapter parallel (Geʽez|English) where EN exists, single-column Geʽez/Amharic otherwise (English after full release). **Today = 161 parallel chapters** (psa/1ki/1sa/2sa; Amharic = ∅), auto-grows. Free copy-paste (selectable Unicode, gutter `user-select:none`, Copy-chapter button) + Ethiopic-numeral/rubricated manuscript styling. Spec `specs/2026-06-05-website-chapter-reader-design.md`, plan `plans/2026-06-05-website-chapter-reader-plan.md` (INDEX updated). Implementing in `scripts/gen_website_progress.py` + `website/{build.mjs,style.css,partials,src/read/**}`; tests `tests/test_website_progress.py` (7, +fixed the stale `none`-stage regression). **✅ DONE this turn: 165 reader pages generated + the static pipeline wired + pushed to the monorepo; verified (ruff/mypy/lint, 0 console errors, Noto Ethiopic loaded, 0 dead links, top-level byte-stable).** **✅ DEPLOYED LIVE at www.yhwhyaway.com** (user GO'd): `yhwh-website` `1308c95`, 169 files; verified HTTP 200 + Geʽez column + clickable pill live. **The on-site Geʽez Bible (Psalms-led) is LIVE; the whole arc — plan → build → push → deploy — is DONE.** Baton **mac** (website-reader lane). ⚠ The win-lane handoff (next) marks a release-gating audit fix-slice as Mac's priority, but the USER pulled the chapter-reader forward this session — building + pushing it per direct instruction; the audit fix-slice stays queued (file-disjoint: `website/**`+`gen_website_progress.py` vs `lint_rules`/tests/migration scripts).
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — ★ROUND-5 SPLIT AUDIT COMPLETE (both lanes) → release-gating fix-session SET UP + SPLIT.** Win lane done (15 survivors: 1 HIGH · 9 MED · 5 LOW; 2 refuted); Mac's 33 already FIXED. **Merged plan + phased fix queue: `docs/superpowers/notes/2026-06-05-round5-split-audit-findings.md`** (raw `_audit-split/findings-win.json`). **▶ Fresh session(s) execute the fix-session, SPLIT file-disjoint:** 🪟 **Windows** = Phase 1 HIGH (`build_cache.py` — `edition_stats.py` cache-miss → stale EPUB; add to `_PIPELINE_SCRIPTS` + evict + coverage lint) + Phase 4 build-path (`build_edition.py` badge stat + `filter_html` opt) → **byte-stability proof per change** (regen 9 KJV + `git diff` + `test_byte_stability_gate.py`). 🖥️ **Mac** = Phase 2 atomic-write (`_reingest_eastons`/`_strip_reviewer_scaffold`/`lint_rules` blind-spot — surgical, the #18 it skipped) + Phase 3 test fixes (`validate_schemas`/`test_lint_rules`×2/`test_batch_promote_xrefs`) + `corpus_index` doc + the `needs_vnote_pass` regression test. **Disjoint** (Win=`build_cache`+`build_edition`; Mac=`lint_rules`+tests+migration scripts) → both run+push in parallel, pull before push, tick the doc checkboxes. **Phase 0** (`needs_vnote_pass`) ALREADY fixed. **✅ Mac slice DONE 2026-06-06** (`a49f1be7` + `30debc58`): atomic-write migrations (`_reingest_eastons`×2 / `_strip_reviewer_scaffold`) · `validate_schemas` `reader_toc_books_only` FieldSpec (test_validate_schemas 49✓) + cleared its 2 invisible mypy `type`-shadow errors + **added it to the mypy gate** (`[tool.mypy] files`, expand-outward) · `test_lint_rules` freshness/flaky (12✓) · `corpus_index` os.replace comment · `needs_vnote_pass` regression test (slow-tagged, verify on SSD); lint/ruff/mypy clean. **⏸ DEFERRED — the `check_atomic_writes` lint EXTENSION** (~80-site `.write_text`/`.write_bytes` sweep overlaps `build_edition.py`/marathon-core → do AFTER the win build-path lands). **▶ Only the WIN slice remains** (Phase 1 `build_cache` HIGH + Phase 4 `build_edition` badge/`filter_html`, byte-stability-proofed) before the release gate clears. **Release `v1.0.0-beta.1` GATED on the win slice** → then public flip (user GO) + release together. Baton **windows**. `LANE_HANDOFF` turn 22.
>
> **▶ NEXT SESSION (🖥️ Mac) — PLAN the website chapter-reader (turn the progress pills into an on-site Geʽez+English Bible).** User-directed 2026-06-05: a fresh session's job = **PLAN, not build** — clickable pills/chapters → on-site Geʽez + literal-English chapter reader, chapter-by-chapter progress; *"building the Bible on the website."* **▶ Full bootable brief: `docs/superpowers/notes/2026-06-05-website-chapter-reader-NEXT-SESSION-brief.md`** (vision · hard constraints incl. the never-over-claim clickable-only-where-real rule · recon list [coverage inventory FIRST — Psalms is the only ~complete one] · open questions→decisions · deliverable = a spec + phased plan, Psalms-first). Runs while the win lane finishes its audit. Don't build, don't touch the EPUB pipeline, don't re-decide versioning (app-semver vs content-milestones already settled). **Content cleanup outcome:** #13 jud/phm OCR-colophon spill removed (`93003e05`); **★#15 KEPT — the 31 `1ma`/`2ma` candidate files are real Maccabees-expansion drafts (21 substantive candidates each), NOT cruft** → register the Maccabees canon later, do NOT delete; #12 (111 impossible-coord candidates) deferred. Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — MAC-LANE ROUND-5 AUDIT FINDINGS FIXED (all fixable); a HEAD-START on the merge fix-queue.** While the win-lane audit (`wf_eeaa8368-6da`) runs on the N95, Mac fixed its lane's survivors. **`444fe8cc`** = 2 HIGH (`batch_promote_xrefs`: missing-book `1ma`/`2ma` candidates marked "promoted" w/o insert → data loss; reviewer-scaffold not stripped in the fast path) + 3 MED (`needs_vnote_pass` missing per-chapter/verse tiers; the RED `ALL_CHECKS==28` pin → 32; `corpus_index` 3 unguarded `_CACHED_CONN` closes → `_CONN_LOCK`, deadlock-checked). **`9e81ee9e`** = ~15 LOW/INFO via parallel re-verify→controller-apply (`wf_f61b5d15-d5d`): inject Psalm-119 aside ordering, RSS http(s) scheme allowlist, 7 `matrix_app` XSS escapes, lint canonical-order/bookcode/render-coverage gaps, EN-dir tracking, 4 byte-identical store escape-sequence fixes (Py3.16-safe), `sources_lexicon` topic normalization, stale REPO_MAP/SESSION_PLAYBOOK. **SKIPPED 8 (reported in-commit):** content-store deletions (#12/#13/#15 — build-harmless, defer to a user-aware cleanup), note-editor innerHTML (#2), kenyon re-index by-design (#5), atomic_write lint (#18 — 100+ violations), tau6x2 share-pins (#7), ingest confirm-optimal (#27). **Gates green:** 59+6+40+4 tests, live lint-guardrails, ruff, `ALL_CHECKS`=32, escape data byte-identical. **▶ When the win lane finishes + `deep-audit-merge.js` runs, these Mac findings are already resolved.** Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — BETA-LAUNCH PREP shipped; release GATED on the full audit; public flip pending user GO.** Mac (baton) ran the pre-public safety audit (`wf_8ba4c1c2-bc3`, 8 agents) → **GO**: code theft-protected (LICENSE airtight; 9 bad-faith reuse theories refuted) + **git history clean across 726 commits → NO secret purge needed** (⚠ the "purge a committed Voyage key + rotate" instruction is a FALSE premise — `.env` was never tracked; do NOT history-rewrite). **Shipped:** `ab5384b1` (16 CC0/commercial self-license → all-rights-reserved), `af6177f3` (LANE_HANDOFF Spaceship key-ID/secret-name scrub), **site EN-fix LIVE** (`yhwh-website` `9b236a3` — EN on Psalms only, verified). gh=`gringoboggy`; LICENSE email `yhwhyaway.com` works (kept). **▶ REMAINING:** (a) **public flip** of the monorepo (PRIVATE) — awaits explicit user GO (irreversible); (b) **release `v1.0.0-beta.1` — GATED on the full split-audit + fixes** (win lane `wf_eeaa8368-6da` still rolling on the N95 → merge via `deep-audit-merge.js` → fix → then flip + release together). LICENSE/COPYRIGHT correct (all-rights-reserved). Baton **mac**.
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows → Mac handoff) — progress-page fixes saved; beta launch handed to Mac; AUDIT ROLLING on Windows.** Windows shipped the progress-page **"not started" removal** (LIVE) + the **EN-flag fix** (saved, needs redeploy) and prepared the full beta-launch handoff (`docs/superpowers/notes/2026-06-05-beta-launch-and-en-fix-handoff.md`). **Mac finishes:** EN site deploy · beta `v1.0.0-beta.1` publish (Windows `dist/YHWH.exe` MS-signed + ready; ⚠ **gh auth per-machine — Mac `gh auth login` as gringoboggy**) · source-public AFTER a Voyage-key history purge (`54ac7493`) + rotate · CC0/commercial doc sweep. LICENSE/COPYRIGHT correct (all-rights-reserved). **▶ Audit `wf_eeaa8368-6da` (win lane) ROLLING on Windows — do NOT stop** (TaskStop orphans pytest — [[audit-orphan-processes]]); when done, win survivors merge w/ Mac's `findings-mac.json` via `deep-audit-merge.js`. Baton → **mac**. `LANE_HANDOFF` turn 20.
>
> **▶ ON BOOT — fresh 🖥️ Mac session: you HOLD THE BATON (mac). The split deep-audit MAC LANE is DONE; your job is the BETA-LAUNCH queue** (full detail `docs/superpowers/notes/2026-06-05-beta-launch-and-en-fix-handoff.md`; summary in `LANE_HANDOFF` turn 20). On boot: `git fetch origin && git pull --ff-only origin main`; the notary SessionStart backstop auto-fires; `/resume`. **The audit is DONE — do NOT re-run it.** Launch steps, IN ORDER (user-gated where noted):
> 1. **Deploy the website EN fix → live.** Source committed (`555d548e`/`178ded0e`) but not yet on the live Pages repo. Per `website/README.md`: `node website/build.mjs` → mirror `website/dist/` into `/Volumes/MacHD2/yhwh-site-publish` → commit + push. Verify `geez.html` legend = `◐ source in hand · ◑ transcribed · ● Bible-ready` and **EN shows on Psalms ONLY**.
> 2. **⚠ `gh auth login` as `gringoboggy` FIRST** — gh auth is PER-MACHINE; the Mac is NOT yet authed (the wrong `bridge4kaladin-collab` account must stay logged out). Needed to create repos / publish the release.
> 3. **★BEFORE making the monorepo public — REQUIRED secret-history purge:** commit `54ac7493` scrubbed a **Voyage API key from `.env`**; a live key may sit in git HISTORY (exposed the instant the repo goes public). Inspect `54ac7493` + surrounding history; if a live key is there, **purge with git filter-repo/BFG + ROTATE the Voyage key**, THEN flip public. (HEAD confirmed clean; `content/auth.json` never committed.) Do NOT go public until clean.
> 4. **Publish beta `v1.0.0-beta.1`:** Windows `dist/YHWH.exe` (474.9 MB, MS-signed, no SmartScreen wall) + 2 EPUBs are READY; the macOS `.dmg` AUTO-joins when Apple notarization clears (NOT a gate). `releases.js` REPO = `gringoboggy/yhwh-bible-platform` → either make the monorepo public (after step 3) + publish the release there, or use a separate public releases repo + repoint `releases.js`; then repoint the "Code" nav link + redeploy. **License = ALL RIGHTS RESERVED, source-available** (`LICENSE`/`COPYRIGHT.md` are CORRECT).
> 5. **License doc-wording sweep (cosmetic):** stale "CC0"/"commercial" wording lingers in `VERSION` ("free-public CC0 phase", "first commercial release") + a couple docs → sweep the user-facing ones to all-rights-reserved.
>
> **▶ AUDIT STATE — MAC LANE COMPLETE (2026-06-05, round 5, 12 dims):** 33 survivors / 7 refuted of 40 (**2 high · 3 med · 24 low · 4 info**) pushed to branch **`lane-transfer/audit`** → `_audit-split/findings-mac.json` (190 KB, full result). The **WIN LANE is still ROLLING on Windows** (`wf_eeaa8368-6da`) — **do NOT touch it (it runs on the N95); stopping it orphans pytest** ([[audit-orphan-processes]]). When it finishes, **WINDOWS merges** win+mac via `deep-audit-merge.js` on the N95 → `docs/superpowers/notes/2026-06-05-round5-split-audit-findings.md`; findings → the post-beta fix queue. **★2 HIGH** (both `scripts/batch_promote_xrefs.py` fast-path: marks 577 missing-book `1ma`/`2ma` candidates 'promoted' without inserting; skips the reviewer-scaffold strip). **★3 MED** incl. `tests/test_lint_rules.py::test_registry_not_silently_shrunk` pins 28 vs `ALL_CHECKS`=32 → CURRENTLY FAILING.
> **▶ NOTARY:** Apple still PENDING (3 submissions In Progress 24h+); the launchd auto-finisher (now auto-discovers via `notarytool history`) + the Mac SessionStart backstop self-staple on Accept — NO action. **Env:** `.venv/bin/python` (NOT `python3` = sys 3.9); `export TMPDIR=/Volumes/MacHD2/<dir>` for any tmp-using run; epubcheck jar at `.venv/lib/python3.14/site-packages/epubcheck/epubcheck.jar`. **[USER] minor:** `rev 1:8` "A Alpha" dup dict-easton note (de-dup or leave).
>
> **▶ ✅ DONE 2026-06-05 (🪟 Windows) — Website factual-copy pass shipped (`a1e94035`, disjoint: `website/**` + `README.md` only); `main` `4e8cf37c→a1e94035`.** A plain "story behind it" credits section (build timeline + real usage numbers + team photo, hoodie logo blurred) + GitHub Sponsors activated + the overselling/favor-framing register stripped site-wide ([[feedback_plain_professional_copy]]). **The split deep-audit is READY on the new HEAD — protocol unchanged from `LANE_HANDOFF` turn 18/19:** both lanes pull → set `LANE` locally (Mac `'mac'`=12 dims / N95 `'win'`=4 dims, confirm the startup count) → Mac pushes `findings-mac.json` to `lane-transfer/audit`; N95 merges via `deep-audit-merge.js` → `notes/2026-06-05-round5-split-audit-findings.md`. Audit = read-only (no baton contention); baton `windows` (owns the merge-commit). TRACKER-STATE idle.
>
> **▶ ✅ DONE 2026-06-05 (🖥️ Mac) — AUTO-NOTE RE-INGEST #2–5 COMPLETE; the track is finished. Mac is READY for the split deep-audit.** Both remaining commits shipped + pushed (origin + github): **#2 θεός (1,196) + #4 φῶς (76) [`cf5eefa0`]** via a curated `_GREEK_DEF_OVERRIDES` in `sources_lexicon.StrongsGreek.get()` (two malformed openscriptures Strong's entries — θεός's primary "supreme Divinity" sense lived in `derivation`; φῶς front-loaded a leaked etymology fragment; the class is exactly 2 over all 7,669 lang-greek notes) + frozen one-shot `_reingest_greek_glosses.py` lockstep; and **#3 + #5 topic-torrey (676 union, 483 distinct) [`4e8cf37c`]** — ★**#5 was a topic-nave MISATTRIBUTION: it is entirely topic-TORREY** (nave is clean). Root cause: `extract_torrey_ccel.py::parse_text` admitted 2 junk "topics" (a Tyre-block description ending in `.` + a wrapped Zechariah citation dump) that STOLE their real topic's ref block; a discriminator rejects `.`/`N:N` headings while keeping `current` so the refs flow back (n_refs preserved 55,566; 630→628 topics); one-shot `_reingest_torrey_topics.py` regenerates the index + recomputes 676 bodies via `TorreyTopicalDetector` (reproduces all 21,764 current bodies exactly) + lockstep; 0 notes dropped, 0 residual junk. **Gates green on both:** `check_nested_anchors` 0, categorize id+kind invariant (91,572/91,572), `ebible verify` errors=0 (32,263/32,263), **eth + catholic-study epubcheck 0/0/0/0**, 2 new lint guards (`greek_gloss_quality`, `no_torrey_topic_leak`) + 14 tests, ruff/format/mypy/lint clean (30 pass / 0 fail). **▶ NEXT = the user-coordinated SPLIT DEEP-AUDIT** — Mac sets `LANE='mac'` + runs the 12 read-only code-review dims of `.claude/workflows/deep-audit.js` (confirm dim count = 12) → push `findings-mac.json` to branch `lane-transfer/audit`; N95 runs the 4 build/test dims (`LANE='win'`) + merges via `deep-audit-merge.js`. On the same `main` `4e8cf37c`. Baton `mac`.
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — SPLIT END-OF-PROJECT DEEP-AUDIT prepped + ready; awaiting Mac's re-ingest #2–5 (user: "#2–5 first").** Auditor `.claude/workflows/deep-audit.js` made round-5 + current (new `rx-surfaces` dim) + split-ready (`LANE` win/mac). Plan `docs/superpowers/plans/2026-06-05-split-audit-plan.md`. Beta artifacts built + verified (2 EPUBs 0-broken-links + `dist/YHWH.exe` smoke-passed). **▶ Windows next (fresh session, after Mac signals #2–5 done):** set `LANE='win'` → run the 4 build/test dims → merge Mac's findings (branch `lane-transfer/audit`) → ship `v1.0.0-beta.1`. Meanwhile (now): pre-build the merge workflow + apply the merged memory set (`lane-transfer/rules`). Baton `mac` (#2–5).
>
> **▶ ACTIVE 2026-06-05 (🖥️ Mac) — AUTO-NOTE RE-INGEST track (Mac baton): defect #1/5 SHIPPED; #2–5 pending.** Plan: `docs/superpowers/notes/2026-06-06-auto-note-reingest-plan.md` (the authoritative spec — read §0 ship-bar + §1 before #2). The 5 defects are the two content audits' findings (`notes/2026-06-06-auto-note-quality-audit.md`).
> - **✅ #1 dict-easton un-cap (FULL articles) + `_HEAD` glue + XHTML-escape — DONE, all gates green, committed.** 1,650 store notes → full Easton article (was MAX_BODY=480 truncated). Method = a **frozen one-shot** `scripts/_reingest_eastons.py` (models `_strip_reviewer_scaffold.py`): regenerate each source entry old-style (cap+greedy `_HEAD`) AND new-style (full + `_HEAD_NEW` `(?![a-z])` + XHTML-escaped), pair by **EXACT old-body equality** (heuristic-free → 640 coord collisions auto-resolve), lockstep exact-string-replace old→new in source + base. Extractor `extract_eastons_ccel.py` fixed permanently (same 3 fixes). Verified: byte-exact reconstruction (`apply_map(HEAD)==working`, 108 files) + categorize-diff (id+kind unchanged) + check_nested_anchors 0 + **eth+cath epubcheck 0/0/0/0** + ebible verify errors=0 (32263/32263) + new `check_no_truncated_easton` guard + `tests/test_easton_reingest.py` (7) + ruff/format/mypy/lint clean. **★Lessons:** re-verify the plan's own numbers (the "2,223" was a scratch overcount; real 1,650); the epubcheck gate caught literal `<`/`>` in 2 entries (1 Tim 3:16 betacode + a `<> <>` separator) that truncation had hidden → escape the prose. Edge cases logged-not-guessed: `rev 1:8` f/g ("A Alpha" pre-existing dup — **[USER] review**, left untouched, not truncated), DALMANUTHA (dup → replace-all), ZUZIMS (source-only, never baked).
> - **▶ NEXT = defect #2 lang-greek Theós head-drop (1,196 = 100% of θεός)** — every θεός gloss dropped the primary "God" sense (greppable `θεός</em>).</strong> figuratively, a magistrate`). Find the Greek/Strong's extractor (`grep -rl 'lang-greek\|Strong' scripts/`), correct G2316, lockstep-replace the 1,196 (likely one old-body→one new-body ×1,196). Then #3 topic-torrey ref-dump (596) · #4 lang-greek Phōs paren-imbalance (76) · #5 topic-nave description-as-heading (87). One defect per commit; same §0 ship bar (incl. **build BOTH eth + catholic-study + epubcheck 0/0/0/0** — see [[feedback_gate_canon_filtered_editions]]; and **XHTML-escape any new body prose** — the #1 lesson). Mac venv now has epubcheck (`EPUBCHECK_JAR=.venv/lib/python3.14/site-packages/epubcheck/epubcheck.jar`).
> - **⚠ Coordination:** baton `mac`; **winclaude gets NEW instructions next boot** (user-directed); the Mac is being moved/unplugged. Transfer branch `lane-transfer/rules` holds the merged out-of-repo memory set for Windows to apply (then delete the branch). A fresh session: `/resume`, then continue at #2.
>
> **▶ ✅ DONE 2026-06-05 (overnight 🪟 Windows) — RX PHASE 4 COMPLETE + FULLY VERIFIED + SAVED (the LAST RX phase before the device test): build-time file-splitter (4b) + Kobo-safe TOC (4a). The RX arc's Windows work is DONE; only the [USER] device test (user-side) remains.** `apply_file_split(tmp, edition)` in `scripts/build_edition.py` (`~2241`, hooked in `build_one` just before zip; mirrors `apply_badge_markers`; base `epub_working/` NEVER touched). Splits the 2–5 MB `index_split_*.html` into ~0.4 MB pieces for colour-e-ink Kobo. **ethiopian-tewahedo: 53 files → 227 pieces, largest 472 KB (from 5 MB), 0 pieces >600 KB, epubcheck 0/0/0/0, 0 broken links.** Boundaries = `id="bp-NN"`/`id="ch-bNN-cMM"` (matches BOTH `<a class="ch-anchor">` + `<p class="ch-heading">` — matching only ch-anchor silently merged ~5 chapters/file into 2 MB units, a bug found+fixed); a giant prose chapter (one `<p class="verse-p">`) sub-splits at inline `<a class="vn-link">` verse anchors with paragraph close/reopen (`_subsplit_chapter`), skipping `<div>` poetry chapters. **★Key insight:** the base MIXES aside placement — many asides are INLINE after the verse, not in `<aside class="notes-section">` — so `_strip_notes_sections` now harvests+strips EVERY leaf note aside (`_CHILD_ASIDE_RE`) to pure prose (3181→979 KB content for Numbers), then per-piece notes-sections are rebuilt BY REFERENCE (bare `#id` footnote/popup stays same-file = native popups). Global id→piece map rewrites ~39.5 K cross-file hrefs + promotes cross-piece bare links; OPF manifest+spine + nav.xhtml + toc.ncx regenerated (preserve matter/font/css entries). Deterministic (byte gate green); cache auto-rekeys; **default ON** (`DEFAULT_READER_FILE_SPLIT=True`; opt-out `reader_file_split:false`; `reader_file_split_target` tunes). +`tests/test_file_split.py` (15: boundaries/well-formedness/notes-distribution/determinism/subsplit/OPF-nav-ncx-regen); badge tests opt out of split (fast); test_marker_style + test_nested_anchors green; ruff/mypy/lint 27✓/2⚠/0✗. Plan `plans/2026-06-05-rx-phase4-toc-and-filesplit-plan.md` (+INDEX). **✅ Phase 4a DONE (Kobo-safe TOC):** all 11 editions pinned `reader_toc_collapsible:false` in `editions.yaml` (per-edition → §6.5 no-op guard + reader-toc tests intact; ornament feature [lives in `<summary>`] undisturbed — a global default-flip would've broken `test_default_is_no_op` + `test_inserts_ornament`) so `apply_reader_toc_transforms` unwraps the `<details>` accordion → flat `<p class="toc-book-label">` + chapter `<ol>` (chapters KEPT); `epub_working/stylesheet.css` `.toc-wrap ol.toc-chapters` drops `display:flex` (inline-block pills wrap + per-pill margin). **Verified (ethiopian-tewahedo build): 87/87 books unwrapped, 0 flex, epubcheck 0/0/0/0**, composes w/ splitter; +`test_shipping_editions_pin_kobo_safe_flat_toc`. Book-list compaction = [USER]-decision (loses 1-tap chapter nav). **★GATE CAUGHT A CANON-FILTER BUG → FIXED (unified stack-aware splitter):** catholic-study (canon-filtered) failed epubcheck — 2 FATAL RSC-016 (unclosed `<p>`/`<body>`) + RSC-005 + 29 cascade RSC-012. A chapter anchor can nest INSIDE the previous chapter's trailing `<p class="verse-p">` (Acts 21→22); the old coarse-unit + `_subsplit_chapter` net-`<p>`-count splitter cut there and split the `<p>` (the superset packed those chapters together so it passed — packing-luck). **Rewrote the splitter unified + stack-aware:** cut candidates = book/chapter starts + EVERY verse anchor; at each piece boundary `_stack_at_positions` (one tag scan) gives the open-element stack → the piece reopens what it starts inside + closes what's still open at its end, so ANY cut (inside `<p>`/`<div>`) is well-formed; this subsumes verse sub-splitting (`_subsplit_chapter` removed). Verified: base-wide sweep **338 pieces / max 445 KB / 0 unbalanced**; 15 unit tests (+`TestStackAwareSplit` Acts-shaped fixture); ruff/check/mypy clean. **✅ ALL GATES PASSED + SAVED:** re-gate **catholic-study + ethiopian-tewahedo epubcheck 0/0/0/0** (211/216 pieces, max ~475 KB); determinism/regression suite **35 green** (byte-stability gate + popups-build + smoke + zip — confirms full-build determinism + that split-default-ON + the popup contract survive); base sweep 338/0-unbalanced. **Saved 5-leg `6c98f76f`** (P4a + the splitter fix) atop `37a10673` (P4b base) + `f5ecd48b` (MATRIX_MAP RX-currency). **Mac signalled build-ready** (`LANE_HANDOFF.md` turn 13). Lesson → memory `feedback_gate_canon_filtered_editions`. **▶ The ONLY remainder = the [USER] device test** (sideload on the COLOR Kobo + Apple Books; user-side, batched at the very end). Standalones don't use `index_split_*` (1.76 MB EPUB; not a Kobo problem) — no splitter needed there. Post-RX backlog (USER-gated re-ingest · LANE T code-debt/doc-coherence/test-coverage) when the user directs it. Baton: `windows`. **TRACKER-STATE idle — RX arc COMPLETE; between tasks.**
>
> **▶ ACTIVE 2026-06-05 (🪟 Windows) — EPUB READING-EXPERIENCE OVERHAUL (Kobo device-QA arc) — Layer A DONE · Phases 1–3 + 5 SHIPPED · only P4 (Kobo TOC + file-splitter) remains.** User loaded the v27 EPUB on the COLOR Kobo + shared Apple Books crops (`E:\epub-kobo-crops` → staged `C:\Users\bogda\AppData\Local\Temp\kobo-crops`; the v27 EPUB extracted at `…\Temp\kobo-epub-x`). **All root-caused:** (1) **`[Reviewer:…]` scaffold in 88,773/91,733 notes (96.8%)** — `scripts/core/detectors.py` embeds `<em>[Reviewer:…]</em>` in `body` at 8 sites (clean pattern = its own `reviewer_notes=` field, already used at 178/346); `promote.py` writes it verbatim; baked in source+base; (2) **'too cluttered' = the deferred `badge` mode** (`specs/2026-05-24-epub-presentation-polish-design.md` §4.1; user CONFIRMED = one count-badge per verse → tap→note-list); (3) Kobo perf (99 MB uncompressed / 2–3.4 MB per `index_split`); (4) Kobo no popups; (5) Kobo no title-art (full-bleed `position:absolute`); (6) Apple title-art misaligned; (7) tiny Heb/Grk (no embedded font); (8) Apple note-box width overflow (no `overflow-wrap` on `.note`/`.vnote`); (9) Apple blue-"│" artifact (`.vn` still `vertical-align:super;line-height:0`); (10) Apple TOC expand→page-break; (11) Kobo TOC broken (`<details>`+flex). **★Constraint (user): ALL notes + features ON** — declutter via display, not deletion. **Decisions:** badge=vision; strip professionally; all-on. **Master plan** `plans/2026-06-05-epub-reading-experience-overhaul.md` — Layer A (D1 badge-injection · D2 cross-reader popups · D3 Kobo TOC/title · D4 file-size · D5 font-embed · D6 leak-surface) → Layer B (P1 strip+guard · P2 CSS · P3 fonts · P4 Kobo structural · P5 badge) → Layer C Mac compat-research. **✅ Phase 1 SHIPPED** (`a6e989a7`, rebased onto Mac `979bde50`): 88,773 `<em>[Reviewer:…]</em>` spans stripped source (79 files) + base (55 files); 8 `detectors.py` body-embeds→`reviewer_notes=` + `extract_eastons_ccel.py` + `promote.py` defensive strip + new `lint_rules.check_no_reviewer_scaffolding_in_bodies` guard (88,773→0); one-shot `scripts/_strip_reviewer_scaffold.py`. Gates: nested 0/61, ebible 32263/32263, epubcheck 0/0/0 ×2, categorize-diff = only scaffold changed, lint 27/2/0, mypy clean, 164 touched-test asserts green. **✅ Layer A discovery** (`wf_95b6739a-c67`, 6 investigators) → `notes/2026-06-05-rx-discovery-findings.md`: D1 badge = per-verse native popup-footnote (NO JS); D2 popup contract already correct (Kobo fail = size/hidden-nesting); D3 Kobo TOC = `reader_toc_collapsible:false` flip + framed title + `.toc-wrap details{page-break-inside:avoid}`; **D4 NO splitter exists → build ~300 LOC semantic splitter (61@2–3.4MB → ~350@0.4MB)**; D5 embed **Cardo** (OFL) via existing `patch_opf_fonts`/`EMBED_FONT_PATHS` + Abyssinica SIL (standalones); D6 leak surface (5 patterns/8 sites). Mac handed the disjoint compat-research (`LANE_HANDOFF.md` turn 9; baton stays windows). **✅ Phase 2 SHIPPED:** `.note`/`.vnote` `overflow-wrap:break-word` (#8) + `.vn` baseline-shift `vertical-align:0.35em` replacing `super;line-height:0` (#9 blue-"│") + `.toc-wrap details{page-break-inside:avoid}` (#10) + **`title_page_style` default full-bleed→framed** (`build_edition.py:2891` + `web_editions.py` + `customize.py` select; #5/#6 — v27 used 66 full-bleed `position:absolute` overlays = the Kobo no-art ROOT cause, now 87 framed). +`TestPhase2CrossReaderCSS` (4); gates test_presentation_polish 38✓ · test_title_page_style 12✓ · nested 10✓ · epubcheck 0/0/0/0 ×2 · lint 27/2/0 · mypy clean. **Cardo (Reg/Ital/Bold) + NotoSerifEthiopic staged** in `content/assets/fonts/` for P3 (full-font embed — `fonttools` NOT installed so NO subsetting; +~1.4 MB acceptable). **✅ Phase 3 SHIPPED (font embed):** `style_config.EMBED_FONT_PATHS` = 3 Cardo faces + Noto Serif Ethiopic (range-scoped U+1200-137F); fonts in `epub_working/fonts/` (bytes ship via `build_one` copytree; `patch_opf_fonts` only patches OPF — **added it to `build_standalone.py`**, was missing → would've been an epubcheck "undeclared resource"); 4 `@font-face` (outside managed region); `.vnote-hebrew`/`.vnote-greek` lead with Cardo; ATTRIBUTIONS+LICENSES OFL notices. +`tests/test_font_embed.py` (10✓ incl. e2e zip-has-bytes+OPF-item) · test_nested 10✓ · test_parallel_bible_phi1 34✓ · **epubcheck 0/0/0/0 on ethiopian-tewahedo + catholic-study + standalone-geez** · lint 27/2/0 · mypy clean. Fonts in all 3 zips (+~0.5 MB). Fixes #7 (device-confirm pending). **✅ Phase 5 SHIPPED (badge DEFAULT):** `apply_badge_markers(tmp, edition)` build-time per-edition post-pass in `build_one` (after `apply_bilingual_toc`, after kind/canon/tradition filters + `renumber_markers`; gated, `DEFAULT_MARKER_STYLE=badge`) — verse-grouped BY POSITION (`v-{code}-{ch}-{v}` anchor-first + `<span class=vn>` fallback), collapses each verse's `note-ref` markers to ONE `verse-notes-badge` (`vbadge-{code}-{ch}-{v}`, noteref) + MERGES that verse's asides into ONE `<aside class="verse-notes" id="vnotes-{…}" epub:type="footnote">` of `.vn-item` rows (native popup, NO JS). **Gen 1: 225 markers → 31 badges (conserved); ethiopian-tewahedo Gen 1:1 = 11.** `build_edition:1707/1712/1797/3776`; /customize select badge-default. **★base `epub_working` UNTOUCHED** (0 badge markers; only stylesheet.css gained rules). Gates: test_marker_style 13✓ · nested 10✓ · **check_nested_anchors on the BUILT badge EPUB 0/61** · **epubcheck 0/0/0/0 badge+numbers** · ebible 32263/32263 · **determinism/byte-stability gate PASSED** · lint/ruff/mypy clean. Edge-fixes: anchor-first grouping for spill (1ch3); last-verse extends past `</p>` to chapter-end (1ki7:51); tradition rows wrap balanced inner (RSC-016). **▶ NEXT (fresh session) = Phase 4 (Kobo structural — the LAST RX phase):** (a) **Kobo-safe TOC restructure** — drop `<details>`+flexbox (Kobo-broken #11); pick a COMPACT cross-reader form (NOT a giant always-expanded pill list — `reader_toc_collapsible:false` at build_edition:1955 yields a huge flat TOC; consider book-list-only / per-book linking); (b) **build-time file-splitter** (`apply_file_split` post-pass ~300 LOC) — split the 2–3 MB `index_split_*` into ~0.4 MB pieces for Kobo speed (#3); ★hard part = rewrite EVERY cross-file `index_split_NNN.html#id` href (TOC page alone has 1,864; thousands total) via an id→new-file map + regen OPF manifest/spine + nav.xhtml; epubcheck must resolve every link; do it build-time (base canonical) like badge. Then the **[USER] DEVICE TEST** (sideload on the COLOR Kobo + Apple Books). **Full handoff: `.remember/remember.md`.** Also open (post-RX, user-greenlight): the auto-note INGEST-defect backlog from Mac's audit (`notes/2026-06-06-auto-note-quality-audit.md`: dict-easton 37.9% truncated · lang-greek Theós 1196 · topic-torrey 596 · etc.). Master plan `plans/2026-06-05-epub-reading-experience-overhaul.md`. Baton: `windows`. TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (🪟 Windows) — σ ARC PLANNED: "Edition Cover + Truthful Front Matter" spec + impl plan written; Mac lane PAUSED → Windows runs both streams.** Brainstormed w/ the user (visual companion): the cover becomes **"HOLY BIBLE" + a builder-chosen subtitle** (overflow-proof fitter — fixes the title-over-border bug); a new **"Your Edition" first page** (name + the builder's notes + an auto, **always-truthful** "what's inside" + total + per-book counts + popup languages); and — the crux the user flagged — the counts + the symbol **glossary must honor the new ρ.3 per-book/chapter/note choices** (today they use the edition-wide matrix and would lie once a builder overrides), via a single build-accurate `resolved_note_counts` reused from the build's own filter sets. Ge'ez/Amharic standalone Bibles get default covers (`05_missal_central_red` / `01_ornate_leafy_brown`, Ethiopic titles), builder-overridable. Spec `specs/2026-06-04-edition-cover-and-truthful-front-matter-design.md` (APPROVED, `3c0988bc`); plan `plans/2026-06-04-edition-cover-and-truthful-front-matter-plan.md` (σ.1 counter → σ.2 cover → σ.3 front matter → σ.4 /customize identity → σ.5 Ge'ez/Amharic covers → σ.6 live-console reconcile). Per-chapter counts stay live in the console (not printed) — user-approved.
> - **★LANE PLAN (user-directed; Mac paused).** The user asked for a concerted Windows+Mac plan but **macclaude is paused + finished its side (website + Apple notarization); nothing new from it.** So **Windows owns BOTH lanes**, baton `windows`. Ready-to-activate split if the Mac resumes (file-disjoint): **CONTENT lane** = the Ge'ez transcription marathon + P0 (`content/manuscript/**`, `content/translations/sources/patrologia/**`, the own-vers store, manifests) — Windows-only (needs the local GAPS/CUDL assets + the strong machine; the cloud/pod was dropped); **BUILD lane** = the σ cover/front-matter plan (`generate_edition_covers.py`, `matter_pages.py`, `edition_stats.py`, `build_edition.py`, `build_standalone.py`, `editions.yaml`, `customize.py`, `api/*`, `matrix.py`) — portable code, the Mac's natural pickup. The only overlap (σ.5 touches `build_standalone.py`+`editions.yaml`; transcription touches them only at a book-completion) is baton-sequenced. Existing content plans: `plans/2026-05-28-geez-patrologia-vision-plan.md` (Esther p35) + `plans/2026-05-17-kings-manuscript-collation.md`.
> - **✅ σ.1 COMPLETE (build-accurate counter; foundation):** σ.1.1 extracted `build_edition.compute_edition_filter_sets` from `build_one` (pure refactor, determinism-gate-proven, reviewed) · σ.1.2 `scripts/core/edition_stats.py::resolved_note_counts` (total/per-book/per-category/per-kind/popup-languages, honoring the ρ.3 hierarchy by reusing those filter sets + a base-HTML coverage gate). **Its cross-check pin caught + fixed 2 real pre-existing bugs:** (a) a `filter_books_for_canon` **leading-spillover leak** — 2 Esdras' continuation shipped ~339 orphan note markers (epubcheck RSC-012 risk) into canons that drop it; new leading-spillover guard strips it only when ALL the leading region's books are dropped (conservative; +2 fast guard tests; ethiopian-tewahedo keeps 2es untouched), (b) a stray duplicate base marker `ref-1ch0103` removed from `epub_working/index_split_015.html`. **★Intentional output change** for editions that drop 2es (catholic-study etc.) — they stop shipping invalid orphans; NOT a regression (determinism gate PASSED 185s; nested-anchors 0/61). **Cross-check pin: resolved == built EPUB exactly** (catholic-study 41,792; ethiopian-tewahedo 43,382). Adversarial build-path review: APPROVE. Commits `a8ae68dc`·`be9b60e1`·`54ecd7d9`.
> - **✅ σ.2 COMPLETE (HOLY-BIBLE cover; the user's overflow bug fixed):** `cover_main_title`(default "HOLY BIBLE")+`display_name` fields; `cover_text_for_edition`→(main,subtitle); **overflow-proof `fit_text_block`** (wrap-then-shrink, hard-break floor — guarantees every line ≤ width); `_compose_cover` draws main+rule+subtitle in the safe band; 9 covers regenerated (byte-deterministic; byte-gate green); clean `display_name`s set. **★Visually confirmed** (catholic-study + ethiopian-tewahedo: "HOLY BIBLE"+subtitle cleanly inside the gold border). Review APPROVE; the vertical-overflow edge (pathological custom input) scheduled σ.4.4 (where the /customize input lands). Commits `1b2eb667`·`5467c4e4`.
> - **✅ σ.3 COMPLETE (the "Your Edition" first page + truthful glossary):** glossary (`_legend_categories_for_edition`) repointed matrix→`resolved_note_counts` (honors the ρ.3 hierarchy); new `render_your_edition_page`/`inject_your_edition_page` (name + builder notes + auto truthful "what's inside" + total + per-book table, all from the counter), placed FIRST after the cover; old About retired; front matter reordered `titlepage→your-edition→dedication→copyright→legend→scripture`. **epubcheck 0/0/0/0** (catholic-study), determinism gate PASS, nested-anchors 0/61; Your-Edition total == counter == 41,792. Review APPROVE; one count-consistency item (the copyright page still counts via the matrix) scheduled σ.6.2 (the count-reconcile phase). Commits `87293637`·`1b86871f`·`a6415eaa`.
> - **✅ σ.4 COMPLETE (`/customize` edition-identity control):** a "Your edition's name & notes" card — a name `<select>` of **smart suggestions derived from the build** (canon + `enabled_categories`; "Study Bible" offered ONLY when study families are on — never spurious) + "✏ Custom…" (capped 48) + a notes textarea (`description`); single hidden `display_name` field (no dup payload); recompose-on-save (empty-stem→factory template); `display_name`/`cover_main_title` added to `EDITABLE_TEXT` + `api_customize_data` (+`enabled_categories`) + clone-carry + the preview EDITABLE set (review fix) + an 80-char server cap. **σ.4.4 vertical-overflow guard** (`_compose_cover_layout`: clamp top_y + re-fit subtitle/main to the safe band; pixel-bounds test) — **byte-neutral for the 9 real covers** (verified 0 hash mismatches). **★Visual QA (Playwright, 0 console errors):** the card renders per-edition with correct suggestions (catholic→"Catholic Study Bible…", jewish→"Jewish Study Tanakh…"); Custom hidden until picked; notes box present. Review APPROVE after the preview-EDITABLE fix. Commits `819923ff`·`822c0b92`·`6408a5dc`·`44f3fb70` + review fix.
> - **✅ σ.5 COMPLETE (Ge'ez/Amharic default covers):** Ethiopic-capable cover font (`_font_for_text`: Nyala→Ebrima→PIL-default when text has U+1200–U+137F; threaded through `fit_text_block`+`_compose_cover_layout` so measure==draw — overflow guarantee holds; **Latin path byte-neutral → 9 standard covers untouched**). `standalone-geez` = `05_missal_central_red` / `መጽሐፍ ፡ ቅዱስ` / "Ge'ez Tewahedo Bible"; `standalone-amharic` = `01_ornate_leafy_brown` / `መጽሐፍ ቅዱስ` / "Amharic Tewahedo Bible"; both covers composed + committed; `build_standalone` ships them (`apply_edition_cover`, was a no-op on empty cover_image). **epubcheck 0/0/0/0** on a built standalone-geez; tofu empirically disproven (Times = identical .notdef boxes; Ebrima = distinct glyphs). **★Visually confirmed by the controller** — both covers show real, well-formed Ge'ez/Amharic script (gold-on-red / gold-on-brown), builder-overridable defaults. Review APPROVE (+stale-comment fixes). Commits `4bb03ba6`·`1c5ecd8a`·`911974df` + review fix `77757702`.
> - **✅ σ.6 COMPLETE → σ ARC COMPLETE (all 6 phases shipped).** Rerouted the last count surfaces through the build-accurate counter: `resolved_note_counts` extended with `per_book_chapter`; `/build-tracker` (total/per-book/heat-grid/category/kind/popups) + the **copyright page** (annotation total + category count) now read it; **removed the redundant mint-9 #8 `_annot_override` block + `_count_in_scope_disabled_ref_ids`** (resolved_note_counts subsumes it); `api_export_preview` `notes_shipping`/`category_breakdown` repointed; fixed the `matrix.py` "actually-shipping"→"potential" docstrings. **All 5 "what you built" surfaces now agree + equal the real EPUB.** epubcheck 0/0/0/0 (catholic-study), determinism gate + the σ.1 cross-check pass, nested-anchors 0/61, lint 26✓/2⚠/0✗. Review APPROVE (no issues ≥80-conf). Commits `92a52d6e`·`7c78e6ee`·`663987af`.
> - **σ ARC SUMMARY (spec+plan SHIPPED):** σ.1 build-accurate counter (+caught/fixed the 2es orphan-spillover + a stray base marker) · σ.2 HOLY-BIBLE cover + overflow-proof fitter (the user's bug, fixed) · σ.3 "Your Edition" first page + glossary→counter · σ.4 /customize name+notes control + vertical guard · σ.5 Ge'ez/Amharic Ethiopic-script default covers · σ.6 count reconcile. Subagent-driven, per-task spec+code review + visual QA throughout. Plan `plans/2026-06-04-edition-cover-and-truthful-front-matter-plan.md` (SHIPPED). The Mac lane shipped a website cover-system showcase in parallel (file-disjoint, rebased in).
> - **▶ NEXT SESSION OPENS WITH: the Kobo device-QA issues.** The user loaded a build on the COLOR Kobo (2026-06-04) → several issues (fewer on Apple Books); crops at **`E:\epub-kobo-crops`** (nested `epub-kobo-crops\` subfolder; 30 `epub-NN.jpg` reader screenshots [mostly clean macOS/Apple Books] + `linked-photo.jpg` = a Kobo device photo). Read those FIRST next session (copy to a temp dir — Read can't reach `E:\`). Also seen in passing: a `[Reviewer: …before promoting]` editorial placeholder leaked into a shipped Greek "Pistis" note popup → content-QA sweep. Memory [[kobo-color-ereader-end-stage-qa]] updated. Other open lanes: ρ.3 C3 polish residue · Phase D1b Esther (paused p35) · the reading-plans dirty-check backlog item (below). Baton: `windows`. TRACKER-STATE active.
> - **↻ Backlog (pre-existing, found in the σ.4 review — NOT σ-introduced):** `/customize` reading-plans dirty-check never re-baselines `readingPlansState.original` after a successful save → the Save button stays "dirty" post-save for reading-plan changes (ψ.19). Small fix; out of σ scope.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE C2 COMPLETE: the `/build-my-bible` navigator FRONTEND, byte-neutral, shipped.** The 4-level drill-down console (Bible→book→chapter→verse) toggling note-symbol families/kinds + translation-popup languages at every level. Subagent-driven (implementer + spec + code-quality review per task; all approved after fixes), **8 commits `ce591d93`→`48d0f21f`** (rebased onto Mac's `2064b86d`), plan `plans/2026-06-04-hierarchical-customize-phaseC2-navigator.md` (C2-2…C2-6 all done). **C2-2** shell + route + §6.2 cross-link (console 18→19). **C2-3** lazy drill-down JS (clickable breadcrumb, BOOK/CHAPTER caches + stale-guards, spinner, XSS-safe). **C2-4** interactive symbol tri-state (book+chapter, category→kind) + popup checklists (book/chapter/verse) → `PUT /api/edition-meta/<id>` (absolute-replace from JS `WORK`; bulk-clears-finer §3.2; debounced+serialized autosave; +`popup_languages_per_book` overrides fix). **C2-5** verse note "ships?" checkbox + atomic `PUT /api/edition/<id>/note-override` (`default|on|off`; `enabled_note_ids` force-on / `disabled_note_ids` force-off, mutually exclusive, force-on wins) + `ships`/`kind_enabled` read flags. **Bible level read-only** w/ /matrix + /customize cross-links. **★BYTE-NEUTRAL** (no build_one/resolver/config/`epub_working`; byte-stability gate PASSED; `git diff epub_working` empty; 9 KJV byte-identical). **★VISUAL QA live (Playwright, 0 console errors):** 4 levels render+navigate; book-xref-OFF→`note_families_off_per_book.gen=xref` + verse note unchecked; verse Hebrew popup→`popup_languages_per_verse[gen:1:1]`; editions.yaml restored byte-identical post-QA. **▶ NEXT = Phase C3 polish** (/build-tracker resolved-state annotation · website "how you make it yours" copy · reconcile /customize+/sources to surface the new chapter/verse levels). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🖥️ Mac lane) — WEBSITE LAUNCH-SWAPS #3/#4/#6 SHIPPED LIVE; #5 owed to user.** User-directed (website items 3–6 + the nav fix), file-disjoint from Windows' ρ.3 Phase C. **Deployed to GitHub Pages** (`yhwh-website` `6ca90da`→`3d03836`, live at www.yhwhyaway.com): **#3 Giscus comments LIVE** — enabled Discussions on `yhwh-website`, installed the **giscus GitHub App** (user did the passkey sudo), pre-created the mapped **"Website feedback"** discussion (#1, Announcements cat `DIC_kwDOSwgvcM4C-gWD`), wired `data-repo`/`data-repo-id` (`R_kgDOSwgvcA`)/category in `feedback.html`; **#4 donations LIVE** — Ko-fi (`ko-fi.com/gringoboggy`, browser-verified) + PayPal (`paypal.me/gringoboggy`, HTTP 200) → real `<a>` buttons; **GitHub Sponsors kept PENDING** (`gh: user.hasSponsorsListing == false` — not yet approved; placeholder span retained, swap when it goes public); **nav** `Support`→**`Give`** (read as tech-support); **#6 copy** — feedback page flipped future→present (comments live), customization "coming soon" line aligned to "being built now". **▶ STILL OWED: #5 email forwarding** for `gringo.boggy@yhwhyaway.com` — passkey-gated Spaceship/Spacemail, **user is setting it up** (mailto links work meanwhile). Monorepo source committed + pushed (both remotes). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE B COMPLETE (ρ.3 popup engine), byte-stable, shipped.** Per-coordinate translation-popup-language resolution across Bible→book→chapter→verse. Subagent-driven TDD, 6 commits (`3969e1a6`→`45091d77`); plan `plans/2026-06-04-hierarchical-customize-phaseB-popup-engine.md`. **New fields** `popup_languages_per_chapter`/`_per_verse`; **engine** `_resolve_popup_languages(edition, book, chapter, verse)` 5-tier most-specific-wins (is-None cascade for explicit-empty; backward-compat invariant — 2-arg + no-override-4-arg identical) + the single call site threads ch/vs; **stale-cache fix** in `build_cache._referenced_translations` (walks per-chapter/verse, +4 tests). **★Byte-stability PROVEN:** gate PASSED 187s · `epub_working/` untouched · nested-anchors 0/61 · epubcheck 0/0/0/0 · suites green (popups 12 · build_cache 40 · symbols 21); 9 KJV byte-identical. +24 tests (1 slow e2e: per-verse override strips Greek from gen 1:1, keeps it in 1:2). Folded in the 2 Phase-A review nits. **Headless** (UI/API = Phase C). **▶ Phase C1 (API write path) ✅ SHIPPED** (`68f1598b`: 6 new fields via a DRY `_validate_keyed_list_field` helper + `_KEYED_LIST_FIELDS` dispatch table in `api_save_edition_meta`; EDITABLE + clone-carry + `api_customize_data` decode; +56 tests; byte-neutral). **▶ Phase C2-1 (navigator read API) ✅ SHIPPED** (`eabd00bb`: `api_build_my_bible`, lazy 3-level drill-down feed; 27 tests). UX **locked = drill-down + breadcrumb** (user-chosen); C2 plan written (`plans/2026-06-04-hierarchical-customize-phaseC2-navigator.md`, 6 sub-tasks; C2-1 done). **▶ NEXT = C2-2…C2-6** = the navigator FRONTEND (console shell + §6.2 cross-link · drill-down JS · the symbol tri-state + popup-checklist toggle controls + save via `api_save_edition_meta` · the verse/note level + `enabled_note_ids` force-on write path · byte-backstop + visual QA) → C3 polish (cross-link · /build-tracker resolved-state · website "how you make it yours" copy). Baton: `windows`. TRACKER-STATE active.
>
> **▶ ✅ DONE 2026-06-04 (🪟 Windows) — HIERARCHICAL CUSTOMIZATION PHASE A COMPLETE (ρ.3 symbol engine), byte-stable, shipped.** Per-coordinate note-symbol resolution across all 4 levels (Bible→book→chapter→individual) + `enabled_note_ids` force-on. Subagent-driven TDD, 6 commits (`8a751bc5`→`ea1f824e`); plan `plans/2026-06-04-hierarchical-customize-phaseA-symbol-engine.md` (all 8 tasks done). **New fields** `note_families_{on,off}_per_{book,chapter}` + `enabled_note_ids`; **engine** `config.enabled_kind_codes_for` (most-specific-wins; phase/AI hard gates; no-override == `enabled_kind_codes` for every real edition) + `_iter_note_ref_symbols` (91,733 = exact corpus) + `compute_symbol_disabled_html_ref_ids` (mirrors tradition filter, short-circuits empty) wired into `build_one` (force-on subtracted LAST; `disabled_kinds_for_filter = disabled − overridden_kinds`; `filter_html` unmodified). **★Byte-stability PROVEN:** gate PASSED 187s · `epub_working/` untouched · nested-anchors 0/61 · flagship epubcheck 0/0/0/0 · affected suites green; 9 KJV byte-identical. +25 tests. **Headless** (UI/API = Phase C). **▶ NEXT = Phase B** (popup-language hierarchy: `_resolve_popup_languages(edition, book, chapter, verse)` + per-chapter/per-verse fields, spec §6) **→ Phase C** (`/build-my-bible` navigator console). The superseded line below (my prior "Phase A plan ready") is now realized. Baton: `windows`. TRACKER-STATE active.
>
> **▶ DONE 2026-06-04 (🖥️ Mac lane — Apple signing + website overhaul + Geʽez; user-authorized commit/push, file-disjoint from Windows) — ★FIRST NOTARIZED macOS BUILD in flight; website overhaul shipped LIVE.** **Apple Developer signing wired** (Mac's #1 task): `dev/launcher.spec` platform-branched to a **onedir** notarizable `.app` on macOS (onefile stays on Win/Linux — onefile-in-.app is deprecated, "clashes with macOS security"); `dev/YHWH.entitlements` (hardened-runtime for the frozen-Python app); `build_dmg.sh` passes `--entitlements`; `build_desktop.sh` uses the repo `.venv` python (not system 3.9) + no silent install; PyInstaller 6.20 declared in `requirements-dev.txt`; `VERSION`→`1.0.0-beta.1`. User created a Developer ID Application cert (`Bogdan Zorlescu (AAHZNDCGFW)`; I installed Apple's G2 intermediate to complete the chain) + a `yhwh-notary` keychain profile. **`dist/YHWH-1.0.0-beta.1.dmg` built + signed (`codesign --verify` OK) + submitted** (`0c0d10c1`) → **notarization STUCK ~8h = an APPLE NOTARY-SERVICE OUTAGE 2026-06-04** (submission `0c0d10c1` still "In Progress"; repeated request-timeouts on Apple's endpoint; our build is FINE — `codesign --verify` OK, Developer-ID + secure timestamp). The build_dmg `--wait` AND a 6×30m re-attach BOTH timed out — Apple never returned a verdict. Commit `4bf2ebf8` (pushed both remotes). **⚠ FINISH WHEN APPLE RECOVERS (manual; the signed dmg is on disk at `dist/`):** `xcrun notarytool wait 0c0d10c1-5e3b-4c6c-a418-368edae22eea --keychain-profile yhwh-notary && xcrun stapler staple dist/YHWH-1.0.0-beta.1.dmg && spctl -a -vv -t open --context context:primary-signature dist/YHWH-1.0.0-beta.1.dmg && .venv/bin/python scripts/gen_checksums.py dist --out dist/SHA256SUMS.txt` — don't clear `dist/` until stapled (else re-notarize); stapling changes the dmg so checksums regen AFTER. (Or re-run `dev/build_dmg.sh` with `CODESIGN_IDENTITY`+`NOTARIZE_KEYCHAIN_PROFILE` set.) **Website (all LIVE on `yhwh-website` + committed to monorepo):** first-person mission/About reword (his real "I just wanted one free English Ethiopian study Bible" origin) + reworked hero creed; whole-site **de-gaud** pass (5-agent critique workflow → hand-applied); the "fully customizable" **Bible-shaped cascade** copy (notes AND popups at whole→book→chapter→verse, framed "coming" — matches Windows's hierarchical-customize feature); **11→9 starting-editions honesty fix** (2 standalone not buildable yet); **Beta page collapsed into a Releases download hub** (`beta.html`→redirect, Beta dropped from nav); honest "almost here" beta state; wordmark `ኪዳን`(covenant)→`እግዚአብሔር`(God, matches brand); feedback page no longer promises a phantom comment box; footer socials show handles not platform names. Commits `846223e7`/`1a9dbd28`/`dedd36d6`; deploys `87b1bb4`→`6ca90da`. **Geʽez emphasis (accuracy):** README headline bullet for the Geʽez/Amharic manuscript Bibles + honest 9-editions + cross-platform quick-start; refreshed GitHub+GitLab+website repo descriptions (Geʽez-led); profile README elevated Geʽez + dropped stale "coming soon". Commit `5aac549e` + profile `2c76649`. **▶ NEXT: when Apple's notary service recovers → finish the notarization (re-attach + staple + verify + regen checksums — command above); then the public-launch swaps (public source repo + post the v1.0.0-beta.1 release + flip download links + Giscus go-live).**
> Baton: `windows` (Mac worked file-disjoint; user OK'd this commit/push). TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (Windows, evening) — ☠ VM/CLOUD APPROACH DROPPED (pod `tvdzqrdpyw95bf` TERMINATED + confirmed gone) · Sam/Kings → LOCAL agent-path marathon (LANE M) · ▶▶ FOREGROUND = HIERARCHICAL EDITION CUSTOMIZATION (Phase A plan ready).** User-directed: *"drop all the vm plans, we're doing it all the way we have been."* **Pod bulk FAILED** — `1sa2` ran 2h10m then CAM error (`rc=2`, no CAM); chapters 2–51 all failed instantly (`rc=1`, 2–5s each) → **0 usable witness JSONs** (only artifact = 1sa2 GG raw passes; not finalizable without CAM). Pod terminated via RunPod API (`podTerminate`, verified — pods list empty); ~$16 credit stays on the account for future. Sam/Kings continues on the **local marathon** (`plans/2026-05-17-kings-manuscript-collation.md`) — NOT the cloud. The 2 cloud docs marked DROPPED.
> - **★ FOREGROUND (the real user #1) — HIERARCHICAL EDITION CUSTOMIZATION:** navigate the edition *like a Bible* (Bible → book → chapter → individual), toggling BOTH **note symbols** (families + kinds) AND **translation popups** at every level; most-specific-wins; byte-identical when untouched. **Tier-1 (edition-wide, `/matrix`) and individual-note (`/sources`, Phase ρ.1/ρ.2) ALREADY SHIP** — the genuinely new work is the book+chapter family levels, per-chapter/verse popups, force-on, and a `/build-my-bible` navigator console. Spec **APPROVED**: `docs/superpowers/specs/2026-06-04-hierarchical-edition-customization-design.md`. **Phase A (symbol engine) plan WRITTEN**: `docs/superpowers/plans/2026-06-04-hierarchical-customize-phaseA-symbol-engine.md` — per-coordinate resolver `config.enabled_kind_codes_for(edition, all_kinds, book, chapter)` + `build_edition.compute_symbol_disabled_html_ref_ids` (mirrors the tradition filter; ref-ids already encode book+chapter+verse → no new strip plumbing) + `enabled_note_ids` force-on; 8 TDD tasks; byte-stability gate = ship bar. **▶ NEXT: execute Phase A** (subagent-driven), then Phase B (popups), Phase C (the navigator). Decisions locked: granularity = category+kind; precedence finest-wins (per-book ON overrides edition disable); force-on absolute; bulk-toggle clears finer in scope (UI).
> - **★KINGS CAM-FOLIATION BUG still stands** (data-integrity — independent of the VM drop): `D:\yhwh-folio-crops\1ki_b3\CAM\` f135–140 are physically 1Ki22→2Ki~7, NOT 1ki13–18; §17 CAM 1ki 13–18 + the prior CAM 1ki 7–12 fill are SUSPECT; GG 1ki 13–18 stands; re-map CAM 1ki by vision from f126=1ki1 when the local marathon resumes Kings.
> Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior — SUPERSEDED; the pod has since FAILED + been TERMINATED, VM approach dropped) 2026-06-04 (Windows) — ☁ POD SAMUEL TRANSCRIPTION BULK RUNNING (autonomous, resumable) · website reworked+deployed+HTTPS · ★KINGS CAM-foliation BUG found · ▶▶ NEXT SESSION #1 = BUILD PER-BOOK NOTE SELECTION (user-directed), before any manuscript.** Full resume how-to in `.remember/remember.md`.
> - **#1 NEXT (user-directed, BEFORE manuscript): per-book note-family selection.** Today note families are EDITION-WIDE only — `scripts/core/config.py:enabled_kind_codes(edition, all_kinds)` has NO per-book dimension; the build applies one enabled-kinds set to every book. User requires per-book AS AN OPTION (a family on in Genesis, off in Exodus). Clean additive build mirroring the existing per-book pattern (`traditions_per_book`/`popup_languages_per_book`/`book_covers`): add `enabled_kinds_per_book`/`disabled_kinds_per_book` (+categories) to `editions.yaml` (default empty = back-compat, byte-identical) → give `enabled_kind_codes` an optional `book=` arg layering the per-book override → resolve per-book in the inject/filter loop (BUILD-PATH → prove 9 KJV byte-stable) → extend the `/customize` per-book matrix → tests → THEN update the website "How you make it yours" copy to promise it. RULES §6.5 + §9.
> - **☁ POD Samuel dual-witness bulk — RUNNING autonomously** on RunPod `tvdzqrdpyw95bf` "yhwh-cloud" (`root@205.196.17.156 -p 11489` — EPHEMERAL, changes on restart). Agent path (Claude Code on the pod via Max sub, NOT paid API): pod driver runs 2 blind `claude -p` vision passes/witness reading **native-res COLUMN TILES** (`manuscript_folio_crop.py` 3×3) → `manuscript_finalize_chapter.py` (converge→assemble→collate). ★Whole-folio 1568px downsample = ILLEGIBLE (3–5px/char → model honestly returns `[illegible]`); **column tiles FIXED it** (GG faithful: Hannah's prayer 1sa2). ★Pod is RATE-LIMIT-bound not core-bound (shared Max-sub limit; concurrency-4 → garbage/throttle → run concurrency-1 sequential, slow ~30–50min/pass, CAM denser → 4800s timeout). Budget $16.2 ÷ $0.21/hr ≈ 77hr ≈ **~25–30 of 51 ch** (user: "run till money runs out"). **Resume:** ssh → `cat /root/bulk_samuel.log` (per-ch START/END/rc) + `ls /root/out/*_witnessCAM.json` (done); **VERIFY 1sa2 CAM quality** (the live CAM-tile check — if `[illegible]`/garbage, CAM tiles failed → stop+retune; if faithful, let it run); `scp root@…:/root/out/*.json` → `content/manuscript/samuel/calibration/` + commit from N95 (**pod NEVER pushes** — single-committer=N95); RESUMABLE (skips done). Stop/start via RunPod API (key `~/.runpod/config.toml`; podStop/podResume GraphQL). Pod tooling: `/root/yhwh/_pod_transcribe_driver.py` + `/root/_pod_bulk_samuel.sh` (copies in repo-parent `…/YHWH-v2.4-full/`); ⚠ formalize the driver into `scripts/` (+tests) next session.
> - **★KINGS CAM-FOLIATION BUG (data-integrity — do NOT trust Kings CAM until re-mapped):** a vision pass found the `D:\yhwh-folio-crops\1ki_b3\CAM\` crops (f135–140) are physically **1 Kings 22 → 2 Kings ~1–7**, NOT 1ki 13–18 (penned folios 135–140 ARE correct; the §17 chapter→folio ASSUMPTION was wrong; 1ki/2ki book-seam = f135v→f136r). So §17's CAM 1ki 13–18 acquisition is wrong AND the prior **CAM 1ki 7–12 manifest fill is SUSPECT** (likely shifted). GG 1ki 13–18 (§17 table) is independent + stands. **Re-map CAM 1ki by vision from the f126=1ki1 anchor forward; correct 7–12; fill 13–22.** The manifest was NOT written with the bad 13–18 data (no corruption — just don't trust §17's CAM column).
> - **Website (Mac lane; Windows stepped in, user-directed):** removed the per-book-limitation callout + tightened the hero creed in `website/src/index.html`; deployed from Windows to GitHub Pages (`yhwh-website` `54c3544`); **cert provisioned + Enforce HTTPS ENABLED**; verified live (`https://www.yhwhyaway.com`, note gone + new creed). Mac handoff: `dev/LANE_HANDOFF.md` (turn 7, ★pull publish copy before deploying) + `website/README.md`. More copy rewording owed. ⚠ **rotate the Claude Code OAuth token** (`/root/.claude_oauth_token`) — it was partially echoed into a transcript.
> Baton: `windows`. Pod API key + Claude OAuth token stored locally (gitignored). TRACKER-STATE active.
>
> **▶ (prior — superseded by the pod run above) 2026-06-03 (Windows lane) — P0 KINGS: 1ki 7–12 mapped (both witnesses); 1ki 13–18 GG done + CAM acquired/cropped, CAM pass PENDING; Samuel transcription wired. [PAUSED for a reboot.]** Samuel COMPLETE (prior). This session: mapped **1ki 7–12 both witnesses HIGH** (filled `kings/manifest.yaml`; kings pending 41→35), mapped **GG 1ki 13–18 HIGH**, IIIF-acquired + cropped **CAM f135–f141** — but the **CAM 1ki 13–18 vision pass was interrupted by the reboot.** **★Wired the P4 Samuel transcription** (paid-API `run_manuscript_transcribe_at_scale.py`; `anthropic` 0.105.2 installed, dry-run OK; needs `ANTHROPIC_API_KEY` → `~/.config/yhwh/anthropic.key`). **▶ RESUME (fresh session) — read `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` §17 FIRST** (it has the GG 13–18 table + the exact CAM-13–18 resume + transcription how-to): (1) run CAM 1ki 13–18 (crops at `D:\yhwh-folio-crops\1ki_b3\CAM\`) → fill 13–18 both witnesses → gate; continue 1ki 19–22 → 2ki. (2) Drop the API key in → kick off Samuel transcription in the background (parallels Kings mapping, no contention). Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior) 2026-06-03 (Windows lane) — P0 Sam/Kings: 1 SAMUEL COMPLETE (1–31, both witnesses); NEXT = 2 Samuel.** Added 1sa 18–31 for both witnesses (GG companion + CAM passes; **first CUDL-IIIF acquire** of CAM f114–f117 — needs `PYTHONPATH=<repo>`). Recension: GG omits 18:1–5; CAM ch18 = 18:1 covenant; 1 Samuel ends mid-folio (GG f017v / CAM f117r), 2 Samuel 1 right after. `samuel/manifest.yaml` 1sa 1–31 filled (boundary-generous, status `pending`); anchor index §14. Gate: image-existence GREEN; samuel now only 2sa pending (23). Committed `ab86dd87` (+ documents Mac's `brand/` in REPO_MAP). Pulled Mac turn-3 (brand kit + GitHub Sponsors tiers published). **Full 5-leg save (PUSHED) this session-end; baton → windows.** **▶ NEXT (fresh session): P0 2 Samuel 1–10, 12–24** — GG on disk (f017v–f028); **CAM-IIIF acquire** the 2sa folios (only f120 local; continue the view sequence past f117 — read penned numbers to verify); same method → then 1ki 7–22 → 2ki 1–25. READ `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` FIRST. Baton: `windows`. TRACKER-STATE active.
>
> **▶ (prior) 2026-06-03 (Windows lane) — P0 Sam/Kings folio-index: 1 SAMUEL 1–17 COMPLETE (both witnesses, vision-verified); dense-section wall RETIRED.** Unique-event anchoring + recension awareness (GG=LXX-compressed, CAM=MT-fuller) fixed the wall that gave the prior session geometrically-impossible 1sa 7–16 assignments. Three cross-checked HIGH-confidence vision passes (CAM keystone 7–17 · GG companion 2–17 · CAM 1–6 re-verify; penned folio numbers confirm the view↔folio arithmetic). **Corrected a real bug:** the prior 1sa 4–11 CAM map was shifted (on-disk filename labels ~+3 off) — re-mapped; filled 2,12–16. Rewrote `samuel/manifest.yaml` 1sa 1–17 (both witnesses, boundary-generous, status pending) via a deterministic generator; added reusable `_reviewer_context/SAMKINGS_FOLIO_ANCHOR_INDEX.md` (method + 4-book anchor tables + availability: **GG 100% on disk; CAM partial → CUDL-IIIF**). Gate: image-existence GREEN; has-folios 43→37 samuel. Commits `8b6cb947`+`683bf66e` (synced this session-end). **▶ NEXT (fresh session): P0 1sa 18–31** (GG on disk f011v+; **first CAM-IIIF acquire** via `scripts/acquire_cudl_master.py`, anchor f106r=view215, 2 views/leaf) **→ 2sa 1–24 → 1ki 7–22 → 2ki 1–25** — same unique-event method, MAX-1 heavy vision, sub-batch check-ins, run the manifest gate per batch. Then P1 pod run (or local-free grind) per the user's pricing-trigger. **Baton: held by `windows`.** TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-04 (Mac website lane) — ★WEBSITE LIVE at www.yhwhyaway.com (GitHub Pages, NOT Spaceship).** All 5 pages serving (HTTP 200); HTTPS cert auto-provisioning → enable Enforce HTTPS next. Spaceship cPanel/FTP unreachable from the Mac (ports 2083/21 blocked, only 443) → pivoted: built `website/dist/` → NEW public repo **`github.com/gringoboggy/yhwh-website`** (clean, no monorepo history, GH no-reply email) → Pages enabled; the domain's product-locked apex `A`/`www CNAME` freed by **disconnecting the domain from Spaceship web hosting**; then Spaceship DNS API (443) set apex `A`→185.199.108–111.153 + `www CNAME`→`gringoboggy.github.io`. **Email (Spacemail MX/SPF/DKIM/SRV) preserved** (backup `/Volumes/MacHD2/yhwh-dns-backup-*.json`). DEPLOY = `node website/build.mjs` → push `dist/` to `gringoboggy/yhwh-website` (memory `reference_spaceship_hosting`/`project_website_launch` + `website/README.md` rewritten to this). Earlier this session: website v2 shell (`4494a129`) + `scripts/gen_checksums.py` (`b9b732bc`) + `runpodctl` on the Mac. **▶ NEXT (fresh session, USER-CHOSEN): set up Apple Developer signing → get a notarized macOS build (≥1 cert on there).** Remaining launch swaps: Enforce HTTPS · downloads-live + first beta build + `SHA256SUMS.txt` (tool ready) · Giscus go-live + pre-create thread · email-forwarding test · donation links. Spaceship web hosting now unused for the site (cancel before Jun 29 or keep; Spacemail kept for email). **Baton: `windows`** (P0 Kings — file-disjoint). TRACKER-STATE active.
>
> **✅ DONE 2026-06-03 — LANE-HANDOFF BATON SYSTEM SHIPPED (Windows↔Mac two-lane coordination).** Turn-based baton: the holder is the active worker + sole pusher + truth-record owner that turn. Built `scripts/lane_handoff.py` (pure stdlib; 8 tests; mypy+ruff clean) + `dev/LANE_HANDOFF.md` (the committed shared baton) + `.claude/commands/{handoff,resume,sync}.md` + SessionStart incoming-check on both OSes (`bootstrap-triad.ps1` + new `bootstrap-triad.sh`) + `.gitignore` carve-outs + RULES §4 bullet. Brainstorm→spec→plan→build this session; verified end-to-end (handoff→banner→mark-seen→silent). Spec/plan: `{specs,plans}/2026-06-03-lane-handoff-baton-system-*.md`. **Baton currently held by `windows`.** **▶ Mac ADOPTION pending** (Mac is on the website now): set `dev/.lane=mac`, wire the Mac local SessionStart to `bootstrap-triad.sh`, `chmod +x` it — then a `/handoff to-mac` is the system's first real use. TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-03 — P0 Sam/Kings FOLIO-INDEX on the N95 (free).** User chose N95-free P0 over the $0.26/hr RunPod RTX 4000 Ada — the pod can't transcribe pending chapters until folios are mapped (free N95 vision work); **pod stays OFF until bulk transcription P4**. Per `plans/2026-06-02-samkings-folio-index-p0-plan.md`. **Harvest (free, no vision):** backfilled 1ki6 (GG+CAM; was pending → kings 42→41) + 1sa2 GG from existing witness JSONs (`1535e74e`); harvest now exhausted. **1sa 4-6 pilot PROVEN end-to-end:** GG-walk (1 Opus vision agent, content-anchored vs KJV landmarks → 1sa4=[f004r,f004v], 5=[f004v,f005r], 6=[f005r,f005v]) + CAM IIIF acquire views 218/219/220=f107v/f108r/f108v (~40s/folio; **penned folio nums 107/108 VISION-CONFIRMED** → view→folio arithmetic holds) + manifest fill; gate image-existence GREEN, samuel both-folios 51→48. CUDL IIIF reachable from this box. ★Lesson: GG `ክፍል`/`ምዕራፍ` rubric numerals ≠ modern chapter numbers (content-anchor onsets). **▶ NEXT = user GO for the BULK** (89 ch remain: 48 sam + 41 kings, all genuinely un-transcribed → GG-walk + CAM-IIIF). Proposed bulk method: contiguous per-book runs — bulk-acquire a CAM view range + one GG-walk agent per ~6-8 folio run, ~5-ch sub-batches, gate+commit per batch, marathon-paced check-ins. ⚠ COMMIT COORDINATION: Mac lane commits/pushes FIRST; N95 local commits OK, push deferred. TRACKER-STATE active.
>
> **☁ ACTIVE 2026-06-02 — RUNPOD CLOUD LANE: PAUSED (pod STOPPED, $0.00/hr) awaiting good pricing + the user's "feed it" trigger; CORRECTED PLAN written; PHASE-0 gate started.** Deployed the A5000 → verified SSH/env (direct TCP/SCP works at `root@<IP> -p <PORT>`; 128-core host so no OOM; Python 3.12 + git present, node absent) → **STOPPED** ($0.00/hr displayed; ~$0.08 used; pod `v2ou3ui5h8iapf`, restart re-reads the connect string; Terminate to fully wipe). **★CORRECTION:** `run_manuscript_transcribe_at_scale.py` = paid-API path (`ANTHROPIC_API_KEY`, out of budget) → the cloud run must use the **AGENT path** (Claude Code on the pod via the Max subscription; vision sub-agents Read crops). Clean seam: pure `assemble_witness(model_out,…)` → a sub-agent returning `model_out` is a drop-in for the API call; downstream collate/apparatus/store unchanged. **PLAN** `plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md` (P1 agent workflow `samkings-dualwitness-batch.js` + converge gate + assemble shim, TDD on calibration · P2 lean-tarball bring-up [no GitHub cred] + setup-token + GAPS · P3 pilot+measure · P4 bounded run + budget guard; frugal lifecycle). **PHASE-0 STARTED:** `tests/test_samkings_manifest_complete.py` RED → **93 pending** (sam 51, kings 42); GG fully on-disk, CAM hires partial (CUDL-IIIF for Sam-pending + 1ki13–22 + all 2ki). Lean tarball built `D:\yhwh-pod-subset.tgz` (75 MB). Prior runbook open-items resolved: scp (not rsync/runpodctl); bundle/tarball (no PAT); direct SSH confirmed; first-connect verified. **✅ P1 COMPLETE + PROVEN end-to-end on 1ki1 (free, N95).** Built + tested (15 P1 tests, mypy clean): `manuscript_converge.py` (converge gate + `sanitize_model_out` [strip rubric ❈, clamp OOB uncertain idx, downgrade orphan illegible→damaged] + `renumber_verses` [witness contiguity]) · `manuscript_assemble_witness.py` · `manuscript_render_crops.py` · `manuscript_finalize_chapter.py` (converge→assemble→collate) · `.claude/workflows/samkings-dualwitness-batch.js` (reads run-config from a JSON file via a bootstrap agent — Workflow `args` DON'T propagate on this harness; `sequential` flag = MAX-1 heavy-vision). **1ki1 proof** (`wf_7d11849e-21a`; 4 MAX-1 blind passes; ~1.9h/603k tok): real faithful Ge'ez (✣/❈, glyph-order doubts, Adonijah `አደንያስ`/`አዶንያስ`, En-Rogel/Shunammite flagged faded); after the sanitize/renumber fixes BOTH witnesses validate + collation SUCCEEDS (base=CAM · 50 v · **witness agreement 56.97% = 417/732** · base_structured_ok=True). **★FINDING (data):** the pass-convergence gate is verse-binary → reads low (GG 11%/CAM 2%) because any 1-token diff flags the whole verse + the passes segmented 53 vs 50; the meaningful agreement is ~57% token-level → **refine the gate to token-level** (flag divergent tokens, not whole verses; propose to user — the conservative gate works, doesn't block). Also: added `HOW_I_WORK.md` (portable global rules) + the plan's foolproof **parallel-operation protocol** (single-committer=N95, pod never pushes; file-disjoint lanes; shared-quota-bounded). **▶ NEXT: USER authenticates (GitHub+GitLab) → pull repo to the Mac (2nd full lane; its Claude self-provisions) → token-level convergence refinement · P0 folio-mapping. Pod run still waits on the user's pricing-trigger.** TRACKER-STATE active.
>
> **▶ ACTIVE 2026-06-02 — PHASE D1b PO Esther vision marathon RESUMED · PAGES 28–34 DONE — ★CHAPTER 1 COMPLETE (vv1–22) + CHAPTER 2 COMPLETE (to 2:23) + CHAPTER 3 to 3:8 (3:8 cut → p35), 5-leg saved · NEXT = p35 (3:9–13 → Addition B begins).** Method this page: 2 blind Opus passes (Workflow `wf_d49c0422-6d2`, run SEQUENTIALLY = MAX-1-heavy) → A complete + French-consistent; B corroborated thru v13 then HONESTLY stopped at an image-budget limit → the faithful 2nd witness for v14–16 + all A-vs-B adjudication = the CONTROLLER reading the full strip + targeted high-zoom crux crops, cross-checked vs Pereira's French AND the printed apparatus (MNPQ). **★A 3rd fresh pass (B2) was REJECTED for HARMONIZING** to the standard LXX/Hebrew text (fabricated cut-off v16 + standard eunuch names contradicting the French) → logged as `_vision_notes (q)`; the apparatus-as-2nd-anchor lesson logged as `(r)`. Resolutions: v12/13 boundary (v13 starts `ወነገሮሙ`), 3 princes `አርቄስዮስ/ስርስቲዮስ/ማግሌ`, v10 prints 8 `ለ`-tokens vs 7 French/LXX names (8th `ወለዢ` flagged), name1 printed `ገማ` (read-the-print vs apparatus/French `ሐማ`). Accumulator pages 24–28; codepoint-clean. Background lane done: D2 source-readiness recon (1 Enoch GO parse-first / Jubilees vision-required). **p29 (1:16-tail + 1:17–22, ch1 close) was the HARDEST page** — both blind passes unreliable (pass B dittographed vv19–21) → controller multi-tile read + a dedicated ADJUDICATOR agent (rendered own crops; reconciled A/B + French + apparatus). Caught: queen `አስጢን` (both misread Esther), `መገብተ` governors, v17 onset `ወነገሮሙ` + a GENUINE doubled `አበየቶ ፡ ለንጉሥ` clause (apparatus-confirmed M/Q `o`), v19/v20 boundary fix, v22 `ይፈርህዎሙ`; new `_vision_notes` failure-classes (s) heavy-wrap margin-layout + (t) both-passes-unreliable→adjudicator + apparatus-arbitrates-doubling. Accumulator pages 24–29. **p30 (2:1-7-head, ch.2 begins) — both blind passes unreliable (A garbled/skipped v4; B wrong on interiors) → dedicated ADJUDICATOR convergence; queen `አስጢን` (vv1,4, both misread Esther) vs `አስቴር` (genuinely Esther, v7); demonstrative `ግዘንቱ` (k); Mordecai genealogy + `ናቡክድናጾር`. v7 continues → p31. Accumulator pages 24–30. **p31 (2:7-tail + 2:8-13) DONE — complete 2:7 assembled; Esther under the eunuch Gây/Hegai; ★v12 read-the-print SAVE (pass A harmonized 'myrrh' to the French; the Ge'ez says `ዕፍረት` scent — restored, extends (p)); v8 onset `ወሰበ ፡ አዘዘ`; Mordecai `መርዶኬዎስ`; Ge'ez numerals `፲ወ፪`/`፮`. v13 cut → p32. Accumulator pages 24–31. **p33 (2:19-tail + 2:20-23 + 3:1-2) DONE — ★CHAPTER 3 BEGINS; complete 2:19 assembled.** A CLEAN high-agreement page — both passes honored the (u) discipline (no MT eunuch-names; conspirators stay unnamed `፪ ኅጽዋን`) → NO adjudicator, controller-adjudicated via French + 2 crops + apparatus. ★DIVINE NAME `እግዚአብሔር` (2:20). ★Haman enters (`ሃማ` son of Hammedatha the Bougaean `ብግያዊ`, 3:1). Demonstrative mixed on one page: `ዝንቱ` (3:1) vs `ለግዝንቱ` (2:23) per (k). The (u) guard kept both passes faithful (positive counterpart to p32's catch). v3:2 cut at `ወመርዶኬ` → p34. Accumulator pages 24–33. **p34 (3:2-tail + 3:3–3:8) DONE — complete 3:2 assembled (`ወመርዶኬዎስሰ`, the contrastive enclitic -ሰ, cf. 2:20 `ወእስቴርሰ`); 3:8 cut at `…ዘኩሉ ፡ አሕዛብ` → p35.** The 2 blind passes (`wf_7c0886f0-9e5`) DIVERGED **and SHARED a misread** → CONTROLLER adjudication (crisp crux crops + French + apparatus) → **new failure-class (v): two INDEPENDENT passes can converge on the SAME wrong glyph (A=B agreement ≠ proof — verify load-bearing glyphs on a fresh crop).** ★v6 onset `ወፈቀደ` ('and he sought to destroy'; BOTH misread `ፈ`→`ረ`); ★v4 carries a genuine 2nd clause `መርዶኬዎስ ፡ አይሁዳዊ ፡ ውእቱ` ('Mordecai is a Jew') that pass B UNDER-read; ★v7 (faintest) year `በ፲ወ፪` + lot `ዕፁ` cast for the **14th of Adar** (`ዓሡሩ ፡ ወረቡዑ`; LXX, not the MT's 13th); ★v8 pass A read the full last lines (controller-confirmed — NOT an over-read, (q) holds). Mordecai refuses to bow → Haman rages → resolves to destroy all the Jews → casts the lot → opens his accusation. Accumulator pages 24–34. **▶ NEXT = render p35 (banner `III, 9 — B, 1`: completes 3:8 → 3:9–13 → Addition **B** begins, the first interleaved LXX Addition since A; same method — guard MT-plus per (u) AND verify load-bearing glyphs per the new (v) even on A=B agreement).** TRACKER-STATE active (user-paced marathon — check in per page).
>
> **✅ DONE 2026-06-02 — MINT-11 COMPLETE (the whole audit arc closes here; STOP, no mint-12 until project END per [[audit_cadence]]).** Phases 0–4 shipped earlier (5 checkpoints `257bfd51`→`d7804f13`); **Phases 5 + 6 + the deep-audit engine round-5 fix shipped this session in ONE closeout commit (5-leg), byte-stability gate PASSED 213s.** **P5a** topical `sorted()`+raw-tiebreak (DEFENSIVE — merge invariant already deterministic; cross-launch test added) · **P5b** `build_cache._referenced_translations`→`translation_id` real dirs (4/5 witnesses were `<missing>`) · **P6** annotation override scoped to canon+enabled-kind via new `_count_in_scope_disabled_ref_ids` — **the plan's canon-only fix was empirically insufficient (real-data probe: raw −49,027 → canon-only −47,362 STILL negative → canon+kind +825 ✓); byte-identical for all 11 committed editions (empty disabled sets)** · **engine** `deep-audit.js runSkepticPanel` retries null votes once then carries an empty panel as UNVERIFIED (not auto-refuted) — the round-4 false-negative fixed (node-validated). lint 26✓/2⚠/0✗; mypy clean; +6 new tests. **▶ NEXT critical path = Phase D1b PO Esther vision marathon (paused p28; parked lane below).**
>
> **▶ (prior) 2026-06-02 — MINT-11 deep-audit round 4 COMPLETE → FIXING overnight (run `wf_4b250c81-348` / task `w2wxhtf4h`).** Engine `.claude/workflows/deep-audit.js` hardened: **ROUND→4 · NOW→2026-06-02 · find+verify pinned `sonnet`** (cap=2 → ~3h not ~8h; [[audit_cadence]]); synth+completeness inherit Opus (post-barrier). 15 dims; 4 deferred-by-design fed in (ex.py→exo · aes 11–16 · compresslevel 9→6 · web.py-split). Pre-launch env: sleep/hibernate=Never(0) AC+DC, ~0.55 GB RAM reclaimed, ssh key loaded, tree clean.
> **▶ PROGRESS (2026-06-02 overnight) — Phases 0,1,2,3,4 SHIPPED (5 checkpoints `257bfd51`→`d7804f13`, all 5-leg pushed):** audit = 64 deduped → 30 survived/34 refuted (NO high/crit). **P1** 6 stale tests→200 green · **P3** HIGH-#B build_cache cover-key (`cover_image_per_book`→`book_covers`) + `core/source_dates.py` in `_PIPELINE_SCRIPTS` + `is_output_current` composes `_PIPELINE_SCRIPTS`+data + coord-guard run_xref/run_kenyon · **P4** promote within-batch dedup + corpus_index fingerprint-before-swap + update_book_floors `len(None)` guard + standalone_store atomic ×3 · **P0** corpus 67,715→91,733 ×5 + tests 186/plans 29 · **P2** skip→assert + inflight active-branch. **2 empty-panel HIGHs hand-verified: #B real (FIXED), #A LATENT** (all 87 books have id_prefix/bxx). **▶ REMAINING (next session, BUILD-PATH → run the byte-stability gate): Phase 5** (matter_pages.py:870/882 `sorted()` topical.xhtml PYTHONHASHSEED determinism + cross-launch test; build_cache popup-witness 4/5→`<missing>` resolution) **+ Phase 6** (annotation-count canon-scope @ build_edition.py:3050 — **CHANGES catholic-study output**). **DEFERRED low-value:** #3b corpus parse-warn · matrix.py:308 None/[] split · Torrey 630→628 · web_sources dead-code comment · HIGH-#A warn. **Engine round-5 lesson** (in deep-audit.js verifyDim comment + findings.md): 21/95 sonnet StructuredOutput misses auto-refuted real findings → re-run null panels / pin verify Opus. Detail: `plans/2026-06-02-mint-11-fixes-plan.md` (Status) + `notes/2026-06-02-mint-11-findings.md`.
>
> **▶ ACTIVE 2026-06-01 (cont.) — mint-10 fix Phases 1–6 SHIPPED across 3 commits; only 4 opt-tier items remain → then STOP.** SSH→GitLab + CI gate green (`9ab9989c`); Phases 1(leftovers)+2+3 (`bba99df4`, byte-gate PASSED 226s); **Phases 4+5+6 (THIS commit — NO build-path change → 9 KJV byte-stable by construction).** **P4:** 8 at-scale drivers → one `append_candidates` (status-preserving dedup; kind-replace purge removed) · `_normalize_book_code` on `--books` · naves/torrey coord guard · 5 stale candidate files deleted · 31 orphaned imports cleaned. **P5:** console XSS escaping (7+ sites) · WLC ingest fail-closed on raw `<>&` · exports version-validation. **P6:** cyril ceiling→floor + all-5-rival plurality · `corpus_index` `_CONN_LOCK` · web_helpers bxx · batch_promote mark-promoted · `_canons_index` lru_cache · conftest monkeypatch drop · REPO_MAP 66 · INDEX mint-9→Shipped. ~1,200 tests green; lint 27✓/1⚠/0✗; mypy clean.
> **▶ NEXT (fresh session) = the 4 DEFERRED opt-tier items** (per `plans/2026-06-01-mint-10-fixes-plan.md`): **#11** ρ.1 `filter_html` two-stage scan (BUILD-PATH → byte gate) · **#12** `_resolve_popup_languages` per-file hoist (BUILD-PATH → byte gate) · **#13** `render_coverage` EN-lane (+ `test_render_coverage` shape) · **#6** `candidates_written` pre-dedup stat (superseded by the P4 `append_candidates` return contract — re-confirm or drop). After they ship: **mint-10 is the LAST audit round — STOP, no mint-11 until project END** ([[audit_cadence]]). Marathon core untouched throughout.
> **▶ ALSO QUEUED (user-directed, after the mint-10 opt-items):** go through the GitHub + GitLab account/repo settings via Chrome (MCP) + add missing visible files (README/CHANGELOG/LICENSE) — see memory `project_github_gitlab_account_settings`.
>
> **▶ ACTIVE 2026-06-01 — MINT-10 round-3 re-audit COMPLETE (recovered + continued) — 62→49 survivors/13 refuted (4h·13m·22l·10i); FIXES now in progress.** Last night's run `wf_ba367edc-a4a` (15 dims, 7.4h) died on terminal shutdown before any verify/synth. Root cause: 4-core N95 → Workflow cap=2, so 2 slow finders froze everything (the "5h hang"). **Recovered 46 candidate findings** (7h·26m·12l·1i, 11 dims, incl. 11 pytest failures) from the 15 done finder transcripts → `docs/superpowers/notes/2026-06-01-mint-10-recovered-candidates.json`. **Continuation `wf_4a2d0815-8c9`** (new `.claude/workflows/deep-audit-continue.js`): verify-first (barrier) → 4 missing finders (cross-module#1 + opt-build/ingest/render) → synth; pinned sonnet (cap=2 → ~3h not ~8h). Artifacts written: findings.md + audit-raw.json + indexed `plans/2026-06-01-mint-10-fixes-plan.md`. **▶ Phase-1 stale-test green-up SHIPPED** (11 test files, 579 green). **NEXT (fresh session) = Phase-1 production (`audit_log` month-rollover · `inject` whitelist) + Phases 2–6 (build-path byte-stability)** per `plans/2026-06-01-mint-10-fixes-plan.md` (phased safest-first · TDD · byte-stable · sweep-the-class · marathon-core untouched · prefer commit-time lint guards · 5-leg save per batch) → then STOP. **mint-10 = LAST audit round (no mint-11 until project END).**
>
> **▶ (prior) 2026-06-01 — MINT-9 fix batch ALL 6 PHASES SHIPPED + engine upgraded + memory synced; round-3 re-audit + setup-build are NEXT.** Round 2 (`wf_e5e7a0d6-24a`, 99 agents · ~4.67M tokens · ~3.2h): **66 deduped → 45 survivors / 21 refuted**. All non-deferred findings fixed across 6 phases; **commit `b1a39485` = P1+P2 checkpoint; THIS commit = P3–P6 + engine + memory sync.** Artifacts: `docs/superpowers/notes/2026-05-31-mint-9-{findings.md,audit-raw.json}` + INDEXED plan.
> **✅ P1 (HIGH):** #2 tradition/time filters bxx-fallback for Strategy-B books (3 sites); #3 promote.py post-splice SyntaxError LOGS + ROOT CAUSE (`py_str` escapes backslash FIRST). **✅ P2 (stale-cache/guards):** #1 build_cache hashes all 7 pipeline scripts; #7 theme css; #17 source_dates.yaml; #23/#24 bookcode lint +3 maps (→12); fixed stale `test_changelog_documents_omega20b`. **✅ P3 (build-path, byte-gate PASSED 253s):** #11 inject precedes-order (Strategy-B); #8 matter_pages `annotation_count_override` (None→KJV byte-stable); #12 `config.category_baseline_kinds` phase/AI-gated baseline. **✅ P4 (concurrency/low-sev):** #20 corpus_index `_CACHED_CONN` reset moved INSIDE rebuild lock; #10 notes_io clears compute_matrix; #37 inject label/cat-title html.escape (byte-id on clean corpus); #36 LXX+Byzantine trusted-html ingest guard; #35 sources url_override allowlist; #21/#22 cross_refs_stripped nonlocal-count. **✅ P5 (at-scale/test-debt):** #9 naves/torrey/xref/ethiopian write_queue same-run dedup (mirror kenyon); #6 share-pin→count milestone; #25 cyril pin guards ALL challengers; #38/#39 tautological tests; #18/#19 ANSI consts→core.ui/at_scale_base; + un-stuck a stale `ch_count` test (refactor-orphaned). **✅ P6 (docs/opt):** REPO_MAP counts (182 tests/27 plans/notes dir/SESSION_PLAYBOOK); render_coverage Patrologia=rendered; is_output_current watches notes/*.py. **+4 mint-9 test files (test_mint9_phase{1,3,4,5}.py); lint 27✓/1warn/0fail; mypy clean; ruff clean.**
> **⚙ ENGINE UPGRADED (`deep-audit.js`, USER ask):** 5 meta-fixes from how rounds 1-2 missed their own findings — prior-round DEFERRED_BY_DESIGN memory (refutes re-litigation), sibling-site sweep, a `tests-run` dim that runs pytest, pinned authoritative counts, args-visibility log → now **15 dims, node-validated, ROUND default→3**. **🧠 MEMORY SYNCED (USER ask):** new `feedback_fix_the_class_not_the_instance`; updated deep-audit-tool/bookcode/W-W1/bootstrap×4-plan-name/project_overview/slow-test(+flaky-lint-test note).
> **▶ RUNNING NOW (2026-06-01) — mint-10 = round-3 re-audit LAUNCHED in background:** `Workflow` run `wf_ba367edc-a4a` / task `wdhpdk0y1` (engine `.claude/workflows/deep-audit.js`, 15 dims, ROUND=3, NOW=2026-06-01, 4 deferred-by-design fed in). **Engine hardened pre-launch:** `REPO` default changed from the relative `'YHWH v2.4'` to the ABSOLUTE `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4` to remove a cwd-ambiguity risk (this session's tools resolved relative paths against the subdir, which would have made every finder read a non-existent `YHWH v2.4/YHWH v2.4/…`). On the completion notification: parse `result` (survivors/dropped/counts/fixesPlanMarkdown/completeness), write `docs/superpowers/notes/2026-06-01-mint-10-{findings.md,audit-raw.json}` + (if survivors) an INDEXED fixes plan, then fix ALL survivors (TDD, byte-stable, sweep the whole class not one site, marathon core untouched), update the systems the round teaches (engine `deep-audit.js` / RULES / memory / truth-record), and SAVE (5-leg) at each coherent batch. **⛔ USER DIRECTIVE 2026-06-01: mint-10 is the LAST audit round — do NOT re-run the audit or the convergence loop afterward. Once findings are fixed + systems updated + everything saved, STOP. No round-4, no re-audit-to-confirm.**
> **▶ NEXT (after the audit):** (1) **round-3 re-audit** with the upgraded engine → CONVERGED (zero new verified) or another fix pass; (2) **build the 4 USER-approved setup items** (auto-format PostToolUse hook · truth-gate Stop hook · `/save` + `/audit` slash commands · Layer-1 doctrine doc) — all LOCAL/login-free, honoring [[no-external-hooks]]. **DEFERRED-by-design (engine now refutes; do NOT fix early):** `ex.py`→`exo` (→τ.G; additive `_book_path` alias is the only safe early action) · `aes` ch11–16 residual · compresslevel 9→6 · the 2 perf hoists (filter_html / _resolve_popup_languages — punted as build-path-risk-vs-low-value). **Known FLAKY:** `test_lint_rules…test_two_back_to_back_runs_equivalent` (time-sensitive freshness msg; passes in isolation — see [[slow-test-files]]).
>
> **✅ RESOLVED 2026-05-31 — bootup/env-health rule made EXPLICIT (was the user-flagged session-end TODO).** RULES §0's post-triad step is now an explicit **4-point checklist** (RAM-clear · git/remote state · plugin/MCP baseline incl. the `gitkraken-hooks` WANTED exception · version-freshness as best-effort-NOT-a-hard-gate since "latest" isn't offline-verifiable). Decision was **explicit + realistically-scoped** (not a hard gate — freshness can't be gated offline). Side-find during the review: the "23 min" slow-test claim (audit + `feedback_slow_test_files` memory) was **STALE** — re-measured `test_matrix_psi35` ~87 s / `test_web_filesplit` ~45 s; corrected the comments, pyproject marker desc, memory, PLAYBOOK §4, and SESSION_STATE.
>
> **✅ MINT-8 ROUND 1 COMPLETE 2026-05-31 — the three TODO blocks below are now DONE.** Plan authored + reusable `.claude/workflows/deep-audit.js` built + RUN (106 agents · ~4.80M tokens · ~3.25 h · `wf_1b2fdc14-4fd`) + synthesized. **70 findings → 57 verified / 13 refuted** (high 6·med 20·low 26·info 5). Artifacts: `docs/superpowers/notes/2026-05-31-mint-8-findings.md` (+ raw JSON) + phased plan `docs/superpowers/plans/2026-05-31-mint-8-fixes-plan.md` (indexed). **▶ NEXT SESSION = implement the phased fixes (TDD, byte-stable, durable guards) → RE-RUN `deep-audit` until a round is CLEAN (convergence).** Original TODO detail retained below for provenance.
> **⚠ (DONE) MINT-8 AUDIT — author the PLAN before the final mint-7 close-out (user-directed 2026-05-31).** When mint-7 D + E are done, write `docs/superpowers/plans/<date>-mint-8-audit-plan.md` — **broader + DEEPER** than mint-7's 6-dimension/15-agent sweep — for NEXT session to **create + run** (not run this session). **Standing convergence process:** audit → implement fixes → **re-audit** → REPEAT until an audit returns CLEAN (nothing found after that round's fixes). mint-8 is hopefully the last for a while, but the loop runs to convergence. The plan should spec: the audit dimensions (deeper than correctness/security/code-debt/tests/docs — e.g. byte-stability, build/EPUB integrity, data-coordinate validity, concurrency/caching contracts, cross-module invariants, the marathon-core off-limits boundary), the find→adversarially-verify methodology, severity calibration, and the convergence/exit criterion.
> **→ TWO artifacts (user idea 2026-05-31): make the deep audit a REUSABLE TOOL, not hand-rolled each time.** (1) the audit PLAN (above); (2) a **reusable named Workflow** `.claude/workflows/deep-audit.js` (parameterized over scope/dimensions/depth — find→adversarially-verify, severity-calibrate, synthesize) invocable on demand via `Workflow({name:"deep-audit"})`, optionally wrapped in a slash command. NOTE what already exists (don't rebuild): STATIC audit is reusable today — `scripts/ci.py`, `ebible audit` (caches/dead-code/types/deps), `lint_rules.py` (26 checks), `/preflight`. The GAP is only the DEEP multi-agent audit (mint-N style), which `deep-audit.js` fills. Next session: author `deep-audit.js` + the mint-8 plan, run it, then the convergence loop reuses the tool each round.
> **→ OPTIMIZATION DIMENSION (user-directed 2026-05-31) — a REQUIRED audit dimension, not just bug-finding.** The audit must also re-evaluate whether the project's **task-execution plans + tools are still the most logical / optimized approach given TODAY's capabilities** (Opus 4.8 1M-context, ultracode, Workflow orchestration, parallel agents) — because several plans were designed BEFORE 4.8/ultracode (e.g. the Patrologia + Kings/Samuel **vision-transcription marathon** method with its OOM-era MAX-1-heavy-agent / tight-crop / per-step caps; the ingest → detector → candidate → promote pipelines; the build/inject + render-coverage pipelines). For EACH remaining real project task: is the current method still optimal, or is a workflow-orchestrated / 1M-context / parallel-agent approach now strictly better (faster / cheaper / higher-fidelity)? Output = confirm-it's-optimal OR a concrete better plan. **Boundary:** this targets the PROJECT'S WORK (transcription, ingest, build, etc.), NOT the meta-tooling / env / plugin setup. Fold this into the deep-audit dimensions + the mint-8 plan.
>
> **➤➤➤ 2026-05-31 (prior) — ✅ MINT-7 ARC COMPLETE (A–E) · mint-8 audit (above) was the next step, now RUN.** Mint-cleanup arc (0–6) + the mint-7 audit were done. **Phase A (★book-code BUGCLUSTER):** completed `_BOOK_CODE_ALIASES`; canonicalized TSK/NAVES/Kenyon maps; rebuilt `tsk_xrefs.json` by a deterministic LOCAL key-remap (17,716 remaps; proven canonical + lossless) → 1,525 TSK xrefs reachable + detector emits canonical `#vnote-eze-…`; 56 stale legacy candidate files deleted; +D4 (catholic `mar`→`mrk`) + bonus `render_coverage` `mar`→`mrk`; new commit-time **`bookcode_canonical` lint check** + `TestBookCodeCanonicalization`; 4-lens workflow (`wf_f17387e2-96e`) verified. **Phase C (security):** C1 `_render_sample_html` now `sanitize_html`s the note body (stored-XSS); C2 `content/auth.json` (+distribution/press_kit) gitignored; C3 Build-All `All_Editions_*.zip` added to the download allowlist (`application/zip`). Lint **26✓/1⚠/0✗**; 9 KJV byte-stable. **DEFERRED (separate human-reviewed content run):** generating + promoting the now-reachable xref candidates. **Phase B (at-scale):** B1 both AI drivers now read `ch_count` (was `"chapters"` → always 50; Psalms now iterates all 150 — the content fill-run is a SEPARATE metered run, code fix only); B2 `run_xref` write_queue now append-merges. **Phase D (code-debt):** D1 new `core/at_scale_base.py` dedups `candidate_to_dict`(×10)/`NT_BOOKS`(×4)/ANSI across drivers+prospect+detectors (guard test pins single-source `is`-identity; write_queue stays per-driver); D2 the 2 legacy `_send_json`-bypass error paths fixed; D3 `_split_web_html`→archive, 2 orphan helpers deleted, `_replace_verse_popup_translation` marked not-yet-wired, `audit_caches`→`ci.py`. **Phase E (tests/doc) DONE:** E1 `ALL_CHECKS` meta-test (`TestAllChecksMetaContract` — all 26 checks run + shape + no-fail + registry-size pin); E2 `slow` marker + tagged the 2 ~23-min files (`-m "not slow"` deselects); E3 byte-stability **determinism** gate (`tests/test_byte_stability_gate.py`, behind `slow`; verified 205 s — builds 3 multi-canon editions valid+distinct + flagship rebuild byte-deterministic; **one build = ~133 s** → all-9 + the slow-build logged for mint-8 optimization); E4 3 doc-ref fixes. **▶ mint-7 ARC COMPLETE & SYNCED (A·C·B·D·E).** **▶ NEXT (next session) = mint-8 DEEP AUDIT** — plan `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md`; BUILD + RUN the reusable `.claude/workflows/deep-audit.js` (plan §5; the TODO block above has the full dimensions + the OPTIMIZATION spec) → adversarially verify → fix → RE-AUDIT until CLEAN. mint-7 plan boxes all checked. **⚠ Save = full 5-leg to ALL sources EVERY time (`save-all.ps1`); Claude self-triggers — RULES §4 unified 2026-05-31.**
>
> **➤➤➤ PARALLEL LANE (paused) — Phase D1b: PO Esther vision marathon, PAUSED at p28** (continues canonical 1:8 `…ፈቃደ` tail → 1:9..). Method: controller renders `render_body_for_vision(doc[28], …, dpi=230, geez_top_fraction≈0.45 — re-check per page)` to OS temp → 2 blind Opus transcribers + adjudicator + calibration (per `_vision_notes` (h)–(p)); assemble 1:8 + 1:9..; Ethiopic-only codepoint-validate; write accumulator page `'28'`; commit. MAX 1 heavy vision agent; per-unit commits. Accumulator = pages 24+25+26+27. After all PO Esther units: Task-4 `est_patrologia.py` via `po_vision_store.write_po_vision_module(...)` → xref sidecar → standalone wiring (5 books, epubcheck 0/0, 9 editions byte-stable) → EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `content/translations/sources/patrologia/_vision_notes.md`. (mint-7 and this Esther lane are independent — RULES §2.5 never-single-thread; pick either to advance.)
>

<!-- archived: 63 entries, 2026-05-20..2026-05-29 (rotate_truth_records.py) -->

> **➤➤➤ 2026-05-29 (prior) — Tracker ACTIVE — Phase D1b Task 4: PO Esther p26 DONE (Addition A A13-completion → A17 END + canonical 1:1–1:4) — RECOVERED from a crashed session + a controller re-verification pass; accumulator page `26` written (codepoint-clean); committed + E:/F: bundle THIS turn.** The crash (OOM; public-wifi RAM spike) hit AFTER the p25 commit `1f2c2393`, mid-p26 — but the heavy passes had completed: the crashed session's subagent transcripts (`79204e1c/subagents/agent-*.jsonl` = 2 blind transcribers + 1 adjudicator) + the surviving OS-temp renders held the 2 blind transcriptions (complete) + the adjudicator's resolved lines 1–5; so the blind passes were recovered (not redone) and the unresolved lines 6–14 were finished by ONE finishing-adjudicator pass + a controller re-verification (own high-zoom crops of the contested glyphs + Pereira's French) that caught 2 adjudicator over-reads. French-anchored: 1:1 = numeral `፻ወ፳ወ፯` (127); A17 = the long head (Haman the Bougaean); Mordecai `መርዶኬዎስ`; India `ህንደኬ`. Corrected 2 adjudicator over-reads (`ተገክረ`→`ተዝካረ`; head `ግዝንቱ`→`ዝንቱ`). Demonstrative ግ-onset at A15/A16 (cf. p24 A4 `ወገዘንቱ`), plain `ዝንቱ` at the heads (_vision_notes (k)). A13 joined `ወ`+`ሰምያሙ`=`ወሰምያሙ`; 1:4 cut at `ዕለ`→p27. Accumulator = pages 24+25+26; 5 `uncertain` flags. **▶ RESUME: p27** — render `render_body_for_vision(doc[27], out, dpi=230, geez_top_fraction≈0.45 — re-check per page)` to OS temp (controller renders; subagents Read the PNG) → 2 blind Opus transcribers + adjudicator + a calibration check (per (h)–(m)) → assemble 1:4 (concatenate its p27 head onto the p26 `…ዕለ` tail) + 1:5.. → Ethiopic-only codepoint-validate → write accumulator page `'27'` → commit. MAX 1 heavy agent; per-unit commits. **After all PO Esther units:** Task-4 final write `est_patrologia.py` via `po_vision_store.write_po_vision_module(slot='est_patrologia', book='est', verses=<assembled; DROP the p24/p25 A5 + p25 A13 partials, use the complete verses in their completing pages>, po_book='est', page_range=(24,65))` → `ruff format` it → Task 5 xref sidecar → Task 6 standalone wiring (build 5 books, epubcheck 0/0, 9 editions byte-stable) → Task 7 EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `_vision_notes.md`.
>
> **➤➤➤ 2026-05-29 (prior) — Tracker ACTIVE — Phase D1b Task 4: PO Esther p25 DONE (Addition A, A5-tail through A13) via 3-pass convergence + calibration; the calibration-reading method codified; committed + E:/F: backed up THIS turn.** Accumulator `content/translations/sources/patrologia/est_patrologia_vision_transcript.json` now = pages 24+25 (Ethiopic-only codepoint-clean). A5 joined across the break = `ክልኤሆሙ`; **A6 begins `ወበቃል`** (corrects the first-pass `ወበቀለ`); A6–A12 converged; **A13 partial `ወ` → continues p26**. Divine name `እግዚአብሔር` (ሔ) confirmed by all 3 passes. Tsade calibrated edition-wide ((j)): only 6th-order ጽ is marked (descending leg), NO distinct 4th-order ጻ → 1st-order ጸ in 'righteous' (decided by glyph). Standing method ((i), user-directed): read sibling pages + the editor's French + grammars FIRST to read better (= calibration, not harmonizing). **▶ RESUME: p26** — render `render_body_for_vision(doc[26], out, dpi=230, geez_top_fraction≈0.55 if a heading else 0.45 — re-check per page)` to OS temp (controller renders; subagents Read the PNG) → 2 blind Opus transcribers + adjudicator + a calibration check (per (h)–(j)) → assemble A13 (concatenate its p26 tail onto the p25 `ወ`) + A14.. → Ethiopic-only codepoint-validate → write accumulator page `'26'` → commit. MAX 1 heavy agent; per-unit commits. **After all PO Esther units:** Task-4 final write `est_patrologia.py` via `po_vision_store.write_po_vision_module(slot='est_patrologia', book='est', verses=<assembled; DROP the p24/p25 A5 partials, use the complete A5 in pages['25']>, po_book='est', page_range=(24,65))` → `ruff format` it → Task 5 xref sidecar → Task 6 standalone wiring (build 5 books, epubcheck 0/0, 9 editions byte-stable) → Task 7 EN back-translation. Plan `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions `_vision_notes.md`.
>
> **➤➤➤ 2026-05-29 (prior) — Tracker ACTIVE — Phase D1b Task 4: p25 boundary RESOLVED, glyph-CONVERGENCE pending; `/reload-plugins` rule shipped; committed + E:/F: backed up THIS turn.** **p25 (PO Esther, Addition A) findings:** A5 spans the p24→p25 break — p24 cut word `…መጽአ ፡ ክ` (6th-order ክ) + p25 `ልኤሆሙ` = `ክልኤሆሙ` ("both of them"); `ልኤሆሙ ፡ ይትጋደሉ ፡ ወከነ ፡ ቃሎሙ ፡ ዐቢይ ።` is A5's TAIL; real **A6 begins at `ወበቀለ`**. Two INDEPENDENT Opus passes diverged on ~10 glyphs (incl. `እግዚአብሔር`/`እግዚአብሐር`; `በሌሊት`/`በሰሊጥ`) + the first reviewer APPROVE-CLEAN'd a wrong segmentation → a CONVERGENCE step is required (`_vision_notes` (f)–(h)). Accumulator: p24 A5 glyph fixed ከ→ክ + p25-status note; **A6–A13 NOT yet written.** Renders staged in OS temp: `est_p25_058.png` (p25 body, 0.58), `est_p26_055.png` (p26 staged), `est_p24_band{A,B}.png` (may be cleared between sessions — re-render via the plan's fitz one-liner if absent). **▶ RESUME: the p25 convergence pass** — dispatch ≥2 INDEPENDENT Opus transcriptions of `C:\Users\bogda\AppData\Local\Temp\claude\est_p25_058.png` (method: text before `ወበቀለ` is A5's tail; delimit A6–A13 by the left-margin line-numbers 6–13, NO in-text numerals; `✻` = footnotes; EXCLUDE the apparatus; watch ሔ/ሐ, ኡ/አ, ው/ወ, ከ/ክ, ለ/ል) → adjudicate disagreements glyph-by-glyph at high zoom, flag genuine ambiguities `uncertain` → write A5 (complete: p24 part + tail, joined `ክልኤሆሙ`) + A6–A13 to a `"25"` page entry in the accumulator → codepoint-validate → commit. THEN p26 (opens with A14 `ወ…` tail). MAX 1 heavy agent; per-unit commits. Plan: `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`; decisions: `_vision_notes.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker ACTIVE — Phase D1b Task 4 in-progress: Esther p24 calibration unit DONE (transcribed + adversarially reviewed + scribed to disk); committed THIS turn; E:/F: backup cleared.** The heavy vision lane is proven on live output: controller renders the body strip (`render_body_for_vision`) → 1 blind Opus transcriber → 1 INDEPENDENT Opus adversarial reviewer → 1 Opus scribe re-reads the image + writes the files (Geʽez goes image→agent→disk, NEVER hand-typed; a hand-typed JSON was caught corrupting glyphs + discarded; a codepoint check now guards it). p24 = title + Addition A vv.A1–A5 (A5 `_partial` → continues p25), apparatus excluded, canonical 1:1 NOT on p24; 3 reviewer corrections applied (A1 qi→qē; A5 ʾu→ʾä; A2 printed wäʾətu, flagged); clean Ethiopic-only codepoint check. Accumulator `content/translations/sources/patrologia/est_patrologia_vision_transcript.json` (in-progress, per-page, split-verse flagged); findings + failure-classes in `_vision_notes.md`. NO store/base/build/test change (`est_patrologia.py` untouched). **▶ RESUME: render p25** (`render_body_for_vision` — use `geez_top_fraction ≥0.55` if a heading/title is present, else ~0.45; re-check per page) → blind Opus transcribe (CONCATENATE A5's p25 tail onto its p24 part "…two great dragons came, ready…", then A6.. + the start of canonical ch1 read from the margin numerals) → independent Opus adversarial review → controller codepoint-validate + append to the accumulator → commit (MAX 1 heavy agent; per-unit commits). Continue through Addition A + canonical ch1, then the remaining Esther reading-order units. **After ALL units:** Task-4 final write `est_patrologia.py` via `po_vision_store.write_po_vision_module(slot="est_patrologia", book="est", verses=<assembled; split verses concatenated>, po_book="est", page_range=(24,65))` → `ruff format` it → Task 5 (xref sidecar `est_patrologia_apparatus.json`; Additions → confidence "none") → Task 6 (add to `build_standalone._STANDALONE_BOOKS` + `_canonical_book` resolver + build standalone 5 books + epubcheck 0/0 + 9 editions byte-stable) → Task 7 (EN back-translation). **Self-upgrading-matrix lessons (apply next page):** title/heading pages need a taller strip (≥0.55); verses can split a page boundary (assembly concatenates); watch low-contrast glyph pairs qi/qē · ʾu/ʾä · wə/wä; preserve as-printed oddities, never harmonize. Plan: `docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE — Phase D1b (Patrologia vision lane): calibration GO + scaffolding (plan Tasks 1-3) shipped + committed THIS turn; E:/F: backup DEFERRED (drives unmounted, user packing up); clean /clear boundary.** User chose the Patrologia vision proof (own-versify the printed PO Ge'ez Esther → standalone Bible). Calibration LOCKED the Additions encoding: canonical chapters stay int (1-10); each LXX Addition's verses live INSIDE the host int-chapter at source position with STRING labels (`A1..F11`) — verified renderer-safe (`render_chapter_body` never int-maths the verse #; `sorted(by_ch)` only sees ints; verse source-order preserved) + byte-stable. Full record + the PO page→content map: `content/translations/sources/patrologia/_vision_notes.md`. Scaffolding (TDD inline, 74 tests green): `render_body_for_vision` (extract_patrologia_pdf), `write_book_module(versification=, filename=)` + `{ch!r},{v!r}` serialization, NEW `scripts/core/po_vision_store.py`. **▶ RESUME (post-/clear) = plan Task 4** (`docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`): the HEAVY per-chapter vision transcription of PO Esther (`GAPS/5_Esther/...Pereira_1913.pdf`, body = PDF pp.24-65) — **MAX 1 Opus vision agent**; per unit: render the body strip via `render_body_for_vision` → blind transcribe (body + margin verse-numerals + Addition letters, apparatus EXCLUDED) → adversarial reviewer (same image) → controller validate + accumulate; **CALIBRATE on canonical ch1 + Addition A [PDF p24] first**; per-chapter commits; controller never holds image bytes (subagents `Read` the PNG). Then write own-vers `est_patrologia.py` via `po_vision_store.write_po_vision_module(slot="est_patrologia", book="est", po_book="est", page_range=(24,65))` → Task 5 (`est_patrologia_apparatus.json` xref via `geez_kjv_xref` vs `kjv/est.py`; Additions → confidence "none") → Task 6 (add `"est_patrologia"` to `build_standalone._STANDALONE_BOOKS` + `_canonical_book(slot)` resolver + `_BOOK_TITLES["est_patrologia"]="Esther"` + build standalone 5 books + epubcheck 0/0 + 9 editions byte-stable) → Task 7 (EN back-translation). **⚠ E:/F: backup OWED at next mount** (this commit + the 3 prior recon/plan commits this session).
>
> **➤➤➤ 2026-05-28 (prior) — Tracker ACTIVE — W0 + D2 RECON DONE (a 2nd feasibility finding reshapes the proof); committed + E:/F: backed up THIS turn; PAUSED for user break + a proof-approach decision.** **W0** (HaCohen Wisdom recon, verified firsthand vs the cached `Sap 1.html`): the site HAS Wisdom (clean Unicode Ge'ez, 19 ch, `Apocryphi/Sap%20{n}.html`, Dillmann 1894 plain-text digitization) BUT it has **NO per-verse markup** — the whole chapter sits in ONE `<p>` with no verse-number spans (unlike the verse-numbered Ludolf Psalms). So HaCohen Wisdom CANNOT give source-authoritative verse numbers → the "fast clean verse-numbered HaCohen proof" premise is **REFUTED**. (Greek-derived deuterocanon — Wisdom/Sirach — also has no own-vs-canonical divergence; own ≈ LXX ≈ KJV-apocrypha.) **D2** source-scan: 1 Enoch (Charles 1906) + Jubilees (Charles 1895) + 4 Baruch (Dillmann 1866) are on archive.org but as page scans with garbled Ethiopic OCR → **NEEDS-VISION**; 1 Enoch chs 1-71 partial clean Unicode at pseudepigrapha.org; **Meqabyan = NO-GO** (no PD Ge'ez critical edition). **Reshaped picture:** the ONLY clean source-verse-numbered Ge'ez = Psalms (done) + the marathon Kings/Samuel; EVERY other Phase-D book needs either **vision-transcription** (Patrologia 6 + distinctive 1En/Jub/4Ba — the TRUE own-vers path, marathon-proven) or is a **HaCohen-apocrypha text-upgrade** (Wisdom/Sirach — clean Dillmann text, chapter-block or split-to-canonical-floor, honest tier; not recension-divergent). Two calibrate-first recons have now caught two false premises BEFORE any wasted ingest. **▶ DECISION FOR USER'S RETURN:** pick the proof/first-lane — (rec) a **Patrologia vision-transcription** book (Esther or Job) to validate the dominant lane · OR the lighter **HaCohen Wisdom text-upgrade** · OR continue the **Kings/Samuel marathon**. W1-W6 (HaCohen Wisdom verse-parser tasks) are ON HOLD pending this; the plan needs a matching revision. Spec §11; plan `docs/superpowers/plans/2026-05-28-geez-phase-d-reingest-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker ACTIVE — Phase D PLAN WRITTEN + spec §11 corrected; ready to EXECUTE the Wisdom proof.** Pre-plan feasibility check (read `scripts/extract_patrologia_pdf.py` + the shipped `est_patrologia.py`) found the PO PDFs are scans Tesseract-OCR'd → the **margin Ethiopic verse-numerals are LOST** + the French apparatus bleeds into the verse text → own-versification NOT recoverable from them by a flag-bypass. Re-scope: the 6 Patrologia books → a **vision-transcription lane (D1b, own plan)**; the clean **HaCohen** path (proved Psalms) = D1a + the proof; **proof book flipped Esther → Wisdom of Solomon (`wis`, 19 ch)** (user-approved). Plan `docs/superpowers/plans/2026-05-28-geez-phase-d-reingest-plan.md`: generalize `ingest_hacohen.py` (per-book registry) → `parse_hacohen_wisdom` (TDD) → fetch/calibrate(GO-NO-GO)/ingest own-vers `wis.py` (`VERSIFICATION=own`) → `wis_apparatus.json` (geez→kjv xref; `kjv/wis.py` exists) → add `wis` to `build_standalone._STANDALONE_BOOKS` (→ standalone 5 books/180 ch) → epubcheck 0/0 + 9-editions byte-stable → EN following lane → repeat Sirach. Scale-out (own plans): D1b Patrologia vision + D2 distinctive acquisition (1En/Jubilees/Meqabyan/4Baruch). **▶ NEXT: execute Task 0 (HaCohen Wisdom recon, calibrate-first) — via subagent-driven-development OR inline (user picks).**
>
> **➤➤➤ 2026-05-28 (prior) — Tracker ACTIVE (Phase D design RESOLVED; writing the implementation plan next). ✅ Spec §11 appended — committing THIS turn (doc-only).** Phase D = own-versification re-ingest of the 32 KJV-renumbered `geez-tewahedo` books, generalizing the Psalms `source_authoritative` precedent. Scope (brainstormed + user option B): **D1** 8 ready (6 Patrologia — `1ch`/`2ch`/`ezr`/`neh`/`est_patrologia`/`job`, PO PDFs already on disk in `GAPS/` + `sir`/`wis` via HaCohen) · **D2** 6 Tewahedo-distinctive to acquire (1 Enoch + Jubilees [Charles] first, then Meqabyan I-III + 4 Baruch — calibrate-first GO/NO-GO) · **18 deferred** (OCR-only narrative + `paz` + NT, no clean source; tally 8+6+18=32). One mechanism: generalize `source_authoritative` (NEVER `renumber_against_floor`; emit `VERSIFICATION="own"`). Order (RULES §3): Esther (`est_patrologia`) proof end-to-end → batch rest of D1 → D2 acquisition background lane → EN back-translation following per-book lane. Honesty: 0 fabrication, calibrate-first per book, 9 KJV editions byte-stable, Samuel goldens + witnesses immutable. Spec: `docs/superpowers/specs/2026-05-27-geez-own-versification-design.md` §11. **▶ NEXT: writing-plans → `docs/superpowers/plans/2026-05-28-geez-phase-d-reingest-plan.md`, then execute the Esther proof (TDD, subagent-driven-development).**
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE — ✅✅ PSALMS EN COMPLETE (all 151 ch / 2,531 v). CLEAN /clear BOUNDARY: committed + E:/F: backed up THIS turn.** Batch 5 (Ps 118-151, 32 ch / 484 v incl. the 176-v Ps 118) closed the Psalter. `geez-tewahedo-en/psa.py` = 151/151 chapters. Whole-Psalter method: translator + independent reviewer subagents per group, faithful to the LXX Ge'ez (not KJV); ~92 faithfulness drifts caught + fixed across 6 runs. Verify: total standalone EN paras 2,855 (324 KS + 2,531 psa), epubcheck 0/0/0/0, `epub_working/` diff empty (9 KJV editions byte-stable), tests 38. All batch scratch + the reusable `_assemble_psa_batch.py` cleaned from `exports/`. **▶ NEXT (post-/clear, user-paced):** Phase D own-vers re-ingest (~36 KJV-renumbered geez books) · OR the Amharic standalone Bible · OR the [USER] real-reader presentation eyeball. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`; spec §10.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Psalms EN Batch 4 shipped — clean boundary). ✅ Committed (ac2baae9) + E:/F: backed up.** Ps 90-117 (26 ch / 467 v) → `geez-tewahedo-en/psa.py` now 119/151 chapters / 2,047 verses (Ps 1-117 + 144/150). 4 translator + 4 reviewer subagents (13 fixes). Verify: standalone EN paras 1,904→2,371 (324 KS unchanged + 2,047 psa), epubcheck 0/0/0/0, `epub_working/` diff empty, tests 38. **▶ NEXT (continuous, the LAST batch): Batch 5 = Ps 118-151** (32 ch incl. the 176-verse Ps 118) → ALL 151 Psalms EN done → /clear prep (final commit + E:/F: backup + truth-record). Merge: `exports/_assemble_psa_batch.py`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Psalms EN Batch 3 shipped — clean boundary). ✅ Committed (7a5efc4a).** Ps 61-89 (27 ch / 529 v) → `geez-tewahedo-en/psa.py` now 93/151 chapters / 1,580 verses (Ps 1-89 + 101/115/144/150). 4 translator + 4 reviewer subagents (16 fixes). Verify: standalone EN paras 1,375→1,904 (324 KS unchanged + 1,580 psa), epubcheck 0/0/0/0, `epub_working/` diff empty, tests 38. **▶ NEXT (continuous to the end, user-approved): Batch 4 = Ps 90-117 → Batch 5 = Ps 118-151 (incl. the 176-verse Ps 118) → /clear prep (final commit + E:/F: backup).** Merge: `exports/_assemble_psa_batch.py`. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Psalms EN Batch 2 shipped — clean boundary). ✅ Committed (d7c1b49b).** Ps 32-60 (27 ch / 437 v) → `geez-tewahedo-en/psa.py` now 66/151 chapters / 1,051 verses (Ps 1-60 + 68/71/101/115/144/150). 4 translator + 4 independent reviewer subagents (caught + fixed 22 faithfulness drifts incl. 2 fabrications removed). Verify: standalone EN paras 938→1,375 (324 KS unchanged + 1,051 psa), epubcheck 0/0/0/0, `epub_working/` diff empty, tests 38. **▶ NEXT (user-chosen steady batches): Batch 3 = Ps 61-100** (~28 ch) via 4 translator + 4 reviewer subagents → merge (`exports/_assemble_psa_batch.py`) → rebuild → commit → check in. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Psalms EN Batch 1 shipped — clean boundary). ✅ Committed (0bb5b164) + E:/F: backed up.** Ps 2-31 (28 ch / 393 v) → `geez-tewahedo-en/psa.py` now 39/151 chapters / 614 verses. 4 translator + 4 independent reviewer subagents (caught + fixed 20 faithfulness drifts). Verify: standalone EN paras 545→938 (324 KS unchanged + 614 psa), epubcheck 0/0/0/0, `epub_working/` diff empty, tests 37→38 (added a generic source-order invariant pin). **▶ NEXT (user-chosen steady batches): Batch 2 = Ps 32-67** (~28 ch) via 4 translator + 4 reviewer subagents → merge (`exports/_assemble_psa_batch.py`, gitignored) → rebuild → commit → check in. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Psalms EN PROOF BATCH shipped — clean boundary). ✅ Committed (7c97970d).** 11 chapters / 221 verses → `content/translations/geez-tewahedo-en/psa.py` (Ps 1, 21, 23, 36, 46, 68, 71, 101, 115, 144, 150; tier `ai-back-translation-reviewed-tier3`, VERSIFICATION=own, source order mirrors the Ge'ez store exactly incl. dups + gaps). Proves the occurrence-index keying on real data: Ps 36's two v24 + two v25 each render DISTINCT English in the EPUB. Method: 3 translator subagents (Opus; faithful to the LXX Ge'ez not KJV; "Yahweh"; [brackets]) + 3 independent reviewers (caught + fixed 13 faithfulness drifts). Verify: EN paras 324→545 (324 KS unchanged + 221 psa), epubcheck 0/0/0/0, `epub_working/` diff empty, tests 33→37. **▶ NEXT (user-paced): remaining ~140 Psalms** (≈2,300 v) via the same method — pace TBD at check-in (e.g. batches of ~25 with reviews); optional 2nd convergence-review pass per chapter for max rigor. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (source-order EN-keying fix shipped — clean boundary). ✅ Committed (27d10ccb).** Fixes the dup-verse EN collision (spec §10.6): `build_standalone.en_occurrence_map()` keys the back-translation by `(verse, occurrence)` in source order; `render_chapter_body` looks up `(gv, seen[gv])`. Ps 36-style 24/25/24/25 chapters now attach each distinct English to its own verse. Byte-identical for shipped content (1ki/1sa/2sa EN have 0 dup-occurrence keys → occ always 1): rebuild 324 EN paras unchanged, 1ki6=33, epubcheck 0/0/0/0, `epub_working/` diff empty. TDD 31→33 (`tests/test_build_standalone.py`). **▶ NEXT (user-chosen, in-session): Psalms EN proof batch** — translator+reviewer subagents for the 8 irregular/dup-verse Psalms (21, 36, 46, 68, 71, 101, 115, 144) + a few normal → `content/translations/geez-tewahedo-en/psa.py` (SOURCE ORDER, mirroring `geez-tewahedo/psa.py` exactly incl. its irregular numbering — do NOT "fix" numbering, that's Phase D) → rebuild → confirm distinct EN on dup verses in a real EPUB → check in before the remaining ~140. Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`; spec §10.6.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (EN lane SCALED to all collated Kings/Samuel; clean boundary). ✅ Committed (dbe13575) + E:/F: backed up.** Following the 1ki6 proof, the EN now covers all 10 collated own-vers chapters (324 verses): 1ki 1-6 + 1sa 1/3/17 + 2sa 11. Per chapter: a translator subagent (Opus) wrote the draft store → an INDEPENDENT reviewer subagent (read the Ge'ez fresh, fixed in-place). The review caught + corrected real **MT-drift** the translators introduced — KJV/Masoretic NUMERALS not in the Ge'ez (1ki 5:15 `፸፻`=7,000 not 70,000; 5:11→2,000; 4:23→6; removed fabricated 5:14 "10,000" + 5:16 "3,300 over the people") and MT-smoothed corrupt NAMES re-anchored to the Ge'ez token + "cf." note (1sa 17:12 Geth/cf.Bethlehem; 2sa 11:20 Jerubbaal/Thebez; kept the Ge'ez 2sa 11:3 "Eliab"). Stores: `content/translations/geez-tewahedo-en/{1ki,1sa,2sa}.py`. Rebuilt → every collated chapter's popups carry EN (vnote-text == verse count: 1ki1→52 … 1sa17→58 … 2sa11→26; EN-less chapters → 0), **epubcheck 0/0/0/0**, `epub_working/` untouched (9 editions byte-stable). Tests +1 (`TestEnglishScaledKingsSamuel` → file 31). **▶ NEXT (post-/clear, user-paced): Psalms EN** — FIRST add source-order-position EN keying for dup-verse chapters (Ps 36 etc.): the `(ch,v)` en_map collides on duplicate verse numbers, and `build_standalone` renders via `chapter_verses_in_source_order`, so the EN lookup must align to that source-order iteration index (spec §10.6). THEN translate+review the 151 own-vers Psalms (≈2522 verses — big; check in first). OR Phase D (own-vers re-ingest; NO cheap flag-only wins — all books incl. the 6 patrologia need real re-ingest). Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`; spec §10. **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (EN back-translation lane — 1 Kings 6 PROOF COMPLETE). ✅ The standalone Ge'ez Bible's 1ki6 popups now carry a faithful, adversarially-reviewed English back-translation of the ACTUAL Ge'ez wording — committed (E1-E4) + ready for E:/F: backup THIS checkpoint.** **E1** registered tier `ai-back-translation-reviewed-tier3`; **E2** wired an optional `<p class="vnote-text">` English para into `build_standalone` (graceful — EN-less popups render exactly as Phase C); **E3** a translator subagent (Opus) + an INDEPENDENT reviewer subagent (cross-checked the Swete LXX + Douay/Vulgate, this Ge'ez text's ancestors) produced the 33-verse 1ki6 English → `content/translations/geez-tewahedo-en/1ki.py`, faithful to the LXX/3-Kingdoms recension (NOT KJV; "Yahweh" for the divine name; `[brackets]` mark genuine textual uncertainty in the corrupt cherubim/door section); reviewer verdict "ship after 2 fixes" → v7 (dropped tool) + v21 (over-supplied numeral) applied to convergence; **E4** rebuilt → **33 `vnote-text` paras in 1ki6** (0 in EN-less chapters), the English ADDS to (doesn't replace) the KJV xref, **epubcheck 0/0/0/0**, `epub_working/` still untouched (9 KJV editions byte-stable). Tests +7 (`tests/test_build_standalone.py` → 30). Commits 92f91a46 (E1) → fe4be746 (E3) + this E4 truth-record. **▶ NEXT (user-paced):** scale the EN lane — rest of Kings/Samuel (≈324 verses) then Psalms (≈2522; Psalms needs source-order-position EN keying for dup-verse chapters per spec §10.6) — OR Phase D (own-vers re-ingest; NO cheap flag-only wins — all books incl. the 6 patrologia need real re-ingest). Plan: `docs/superpowers/plans/2026-05-28-geez-en-backtranslation-plan.md`; spec §10.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Phase C COMPLETE — the standalone Ge'ez Bible render path ships + a first proof EPUB validates 0/0/0/0). ✅ Done via subagent-driven TDD (C1→C4 + a review-caught faithfulness fix); committed (10 commits) + ready for E:/F: backup THIS checkpoint.** Shipped: per-book `VERSIFICATION` attr (C1) · own-vers Kings/Samuel store + apparatus sidecars from the 10 v2 collations, tier `manuscript-collation-tier2` (C2) · `scripts/build_standalone.py` greenfield body-gen + OPF/nav/ncx assembly + `build_standalone()` orchestrator reusing `build_epub.build`/`patch_opf`/`apply_edition_cover` (C3a-c) · `build_one` standalone guard, no-op for the 9 KJV editions (C3d) · Psalms LXX→KJV xref (interpolated) + proof EPUB (C4). **PROOF EPUB:** standalone-geez = 4 books / 161 chapters (1ki1-6 + 1sa1/3/17 + 2sa11 + Psalms 1-151); popups = KJV xref + manuscript apparatus, **NO English** (next lane, never faked from KJV); **epubcheck 0/0/0/0**. **BYTE-STABLE PROVEN:** `epub_working/` untouched since pre-Phase-C (git-diff empty); catholic-study (KJV) builds + epubcheck 0/0/0/0; lint 15✓/1⚠(active-marker)/0✗. Tests +23 (`tests/test_build_standalone.py`). SDD final review caught a faithfulness bug (Ps 36 non-adjacent dup verses 24/25/24/25 were sorted→scrambled by `get_chapter`) → render in SOURCE order via `chapter_verses_in_source_order` (`89e3b59b`). **⚠ psa-data flag (Phase D):** HaCohen Ge'ez Psalter store has irregular numbering in 8 chapters (Ps 36 missing v21/v23 + dup 24/25; Ps 21/46/68/71/101/115/144 adjacent dup verse #s) — pre-existing ingest artifacts, now rendered faithfully; data fix = Phase-D/psa-ingest. **▶ NEXT (two open lanes, user-paced):** (1) **EN back-translation** lane — faithful English of the actual Ge'ez wording (agent path + adversarial review, generalize Track F) for the own-vers content → fold into popups; (2) **Phase D** own-vers re-ingest of ~36 KJV-renumbered geez books (the ~30 ocr-tier3 + the 6 patrologia-tier1). **Correction (verified 2026-05-28):** the 6 patrologia books (1ch/2ch/ezr/neh/job/est_patrologia) are stored KJV-renumbered (COLOMETRIC-MERGE), so they too need a real re-ingest via the `source_authoritative` bypass — NOT just a `VERSIFICATION="own"` flag (the "cheap flag-only" claim is REFUTED). No cheap Phase-D wins. Plan: `docs/superpowers/plans/2026-05-28-geez-standalone-render-plan.md`; spec §9. Commits 5e8afdc8 (C1) → 89e3b59b (C4 fix). **[USER]** real-reader presentation eyeball still open.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Phase B COMPLETE; clean boundary before Phase C). ✅ Ge'ez→KJV cross-ref tool SHIPPED + folded into the 10 v2 collations — committed (B1–B5) + E:/F: backed up + RAM cleared.** NEW pure `scripts/core/geez_kjv_xref.py` (B1 Ge'ez numeral parser · B2 KJV English number parser · B3 curated proper-noun map+matcher · B4 `build_kjv_xref` score-weighted monotonic anchor + interpolation, confidence anchored/interpolated) + driver `scripts/apply_kjv_xref.py` (B5) → every v2 now has `kjv_xref` (keyed by str(geez_v)) + `metrics.kjv_coverage`. 1ki6 v1→KJV 6:1 anchored, v2→6:2. Tests +19 (geez_kjv_xref 8 + base-structured B5 11). Engine UNCHANGED (45/45); witnesses + Samuel goldens byte-untouched (0 calibration paths in any B commit). Coverage honest 14–61% anchored/chapter. B4 caught + fixed a length-maximizing filter that displaced strong anchors (→ max-total-score). Commits B1 `7d8de28d`→B5 `4200979a`. **▶ NEXT: Phase C** — standalone render path (versification attr on translations.py → own-versification store from v2 → standalone:true build branch → first standalone Ge'ez Bible EPUB; 9 KJV editions byte-stable). Own detailed plan at phase start. Plan: `docs/superpowers/plans/2026-05-27-geez-own-versification-plan.md` (A+B done; C/D next). Engine/tool: `scripts/core/{manuscript_collation,geez_kjv_xref}.py`; outputs `content/manuscript/{kings,samuel}/collation/*_collation_v2.json`.
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Phase A FULLY complete; 1ki2 decision RESOLVED → CAM; clean boundary before Phase B). ✅ A5 `351a7df8`: `_pick_base` material-extent now by TOKEN count not verse-unit count — only 1ki2 restructured (GG 46-verse → CAM 27-verse base); the 9 other chapters + 4 Samuel goldens keep base=CAM (R5 green / `manuscript_qa.py` exit 0; only their `base_rationale` wording shifted units→tokens). +2 tests (file now 18). Committed + E:/F: backed up.** ▶ NEXT: Phase B (geez→kjv partial-anchoring cross-ref tool — expand to its own detailed plan at phase start). Engine: `scripts/core/manuscript_collation.py`; driver `scripts/recollate_base_structured.py`; outputs `content/manuscript/{kings,samuel}/collation/*_collation_v2.json`. Plan `docs/superpowers/plans/2026-05-27-geez-own-versification-plan.md` (Phase A done; B/C/D next).
>
> **➤➤➤ 2026-05-28 (prior) — Tracker IDLE (Phase A COMPLETE; clean phase boundary, awaiting the 1ki2 base decision + Phase B kickoff). ✅ Ge'ez own-versification engine re-arch + 10-chapter re-collation SHIPPED + committed (A1–A4) + E:/F: backed up + RAM cleared.** A1 `collate_base_structured` (base-witness units = primary verses); A2 per-verse `apparatus` via ONE whole-chapter Ge'ez `align_verse` + `tokens_conserved` (every GG+CAM token incl. ⟦illegible⟧); A3 Ge'ez-internal `metrics` + `base_structured_ok` gate + KJV `_SEMANTIC_PASS_FLOOR` demoted fail→informative (Samuel goldens via the UNCHANGED `collate()`: 45/45 + `manuscript_qa.py` exit 0); A4 driver `scripts/recollate_base_structured.py` → 10 `<ref>_collation_v2.json` (4 Samuel goldens byte-stable; fixed a latent base=CAM assumption in `tokens_conserved`). Author email → `gringo.boggy@icloud.com` (global config + A1 amended). **⚠ OPEN DECISION (1ki2 base=GG):** GG 46 units vs CAM 27, but ≈equal content (777 vs 803 tokens) — a SEGMENTATION not extent difference; `_pick_base` keys on unit-count. Old KJV-spine 1ki2 ALSO base=GG (not new). Pending: keep GG, or refine `_pick_base`→token-extent (→ base=CAM project-wide; re-verify R5 first, then re-run the A4 driver). **▶ NEXT: resolve 1ki2 → Phase B** (geez→kjv cross-ref tool; expand to its own detailed plan at phase start). Phase-A artifacts: engine `scripts/core/manuscript_collation.py` (`collate_base_structured`/`tokens_conserved`/`base_structured_ok`); driver `scripts/recollate_base_structured.py`; outputs `content/manuscript/{kings,samuel}/collation/*_collation_v2.json`; tests `tests/test_manuscript_collation_basestructured.py` (16). Plan: `docs/superpowers/plans/2026-05-27-geez-own-versification-plan.md` (Phase A done; B/C/D next).
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (marathon REDESIGNED; paused at a /clear checkpoint). ✅ Ge'ez collation REDESIGN approved + spec/plan/rules SHIPPED — committing + E:/F: backing up THIS turn; READY for /clear → fresh session executes Phase A.** The KJV-spine collation is superseded by **Ge'ez-OWN-versification** (base-witness sense-units = the verses; other witness = apparatus; KJV = secondary cross-ref). NO collation code executed yet beyond the redesign docs — the witnesses are UNCHANGED + immutable; next-session re-collation is MECHANICAL from them (no re-transcription). Spec `docs/superpowers/specs/2026-05-27-geez-own-versification-design.md`; plan `docs/superpowers/plans/2026-05-27-geez-own-versification-plan.md`; RULES §2 principles 4 (no-shortcuts) + 5 (never-single-thread). **▶ NEXT (post-/clear): execute Phase A** (in the plan): A1 `collate_base_structured()` → A2 apparatus via Ge'ez `align_verse` → A3 Ge'ez-internal metrics + retire the KJV `_SEMANTIC_PASS_FLOOR` fail → A4 re-collate the 10 done chapters to `<ref>_collation_v2.json`, keeping the 4 Samuel `*_collation.json` goldens byte-stable. Use subagent-driven-development; per-chapter re-collation is parallel; keep a background lane busy (CAM pre-pull / next re-collation) per RULES §2.5. CAM buffer staged through f130v (1ki8). Manifest: 1ki:1-6 witnesses done; 1ki:7-22 + 2ki:1-25 + Samuel 55 pending.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (1ki6 mid-chapter, UNCHANGED — now proceeding into C-7). ✅ Aggressive session-start RAM-clear CODIFIED into the bootstrap protocol — committed + E:/F: backed up THIS turn; ▶ CONTINUING into 1ki6 C-7 (user: "just continue after save/backup").** Process/rule/hook + memory only; NO code/corpus/base/build/test change. User-directed "make this part of bootstrap protocol": every session start now ends every process not needed for Windows/network/Claude/toolchain (PROTECT-list = the safety boundary — `claude`/`node`-MCP/the `pwsh`+`powershell`+`WindowsTerminal`+`explorer` tree/`MsMpEng`-AV/`svchost`-network; AV stays ON), not just leaked python/java + not gated on RAM pressure. 4 layers: RULES §0 + PLAYBOOK §1.4 + `bootstrap-triad.ps1` (tracked `dev/cc-hooks/` source + runtime `.claude/hooks/` copy, identical) + memory `session-hygiene`. First run freed ~1.2 GB (8.24→9.47 GB free; 21 process groups; Edge 0-window). **▶ NEXT: 1ki6 C-7 (collate)** — unchanged from the prior entry below (the still-live marathon truth): run the Phase-2 C-7 command (book=1ki, chapter=6) on the two reviewed-clean witnesses `content/manuscript/kings/calibration/1ki6_witness{GG,CAM_hires}.json` + the KJV skeleton → `content/manuscript/kings/collation/1ki6_collation.json`. EXPECT base=CAM (fuller), the GG-18 × CAM-33 asymmetry (distinct-recension signal, NOT a defect), the year-dating-placement divergence (CAM head / GG end); resolve AMBIGUOUS carry-forwards via METHOD NOTE 4 (v1 480-numeral `፬፻` vs `የ`; v12 verb `ወአቀውም`; v17 `ክ/ኩ`) → C-8 honesty → C-9 flip manifest `1ki:6` calibrated + ledger + commit + backup. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (1ki6 mid-chapter; paused at a USER /clear checkpoint between C-6 and C-7). ✅ 1ki6 C-6 CAM WITNESS REVIEWED-CLEAN (R1+R2+R3 adversarial → APPROVE CLEAN; 31→33 v) + ✅ SAMUEL scope-ahead dossier SHIPPED (background, concurrent) — committed (2 separate commits) + E:/F: backed up THIS turn; READY FOR /clear → resume 1ki6 C-7 (collate).** **C-6:** R1 caught a CRITICAL dropped-scripture omission — the divine promise (canonical vv11-12, `ወኮነ ፡ ቃሉ ፡ እግዚእብሔር…`) was physically on the L-column foot after the red `ክፍል ፴ወ፭` rubric and the blind C-5 pass had SKIPPED it (the M-top `ከ` = the tail of `አቡከ`, not an omission residue); restored + ink-reconfirmed → 31→33 v. R2 caught 2 more v10 defects (the inked `ወእስር`/`በማእስር` mis-read as `ወአሰሬ`/`በማእሰሬ` — a feet-cropped `እ` read as printed `አ`; + a 2nd dropped clause, the cedar-fastening 6:10b `ወእስር ፡ ለቤት ፡ በዕፀ ፡ አርዝ`) and certified the rest clean; fix-agent applied + ink-reconfirmed. R3 APPROVE CLEAN: both restorations + the v10 fix genuine, divine name 1st-order `እግዚእብሔር` (×5, independently verified on f128r), `validate_witness` OK 33 v, boundaries clean (START `ወእምዝ` after `ክፍል ፴ወ፭`+`ምዕ ፮`; STOP after `፯`/7-yrs, excludes `፲ወ፫`/1ki7). `CAM_topology.md` appended (1ki6 findings: the twice-fired column-foot skip class, the `እ`/`አ` descender-band rule, `ር`/`ሬ` finals, the GG↔CAM year-dating-placement divergence). **SAMUEL SCOPE (background sonnet, text-only, ran concurrent with R3):** `dev/MARATHON_LOOKAHEAD.md` now carries 1sa 1-31 + 2sa 1-24 (55 rows, Kings format) + 4 calibrated folio anchors + estimates + TOP RISKS — headline = the **1sa17-18 LXX SHORT TEXT** (ch17 ≈35-40v, not KJV 58). **Kings + Samuel are BOTH fully scoped-ahead now.** ▶ **RESUME (post-/clear) = 1ki6 C-7 (collate):** run the plan's C-7 command (substitute book=1ki, chapter=6) on the two reviewed-clean witnesses `content/manuscript/kings/calibration/1ki6_witness{GG,CAM_hires}.json` + the KJV skeleton → writes `content/manuscript/kings/collation/1ki6_collation.json`. EXPECT: base=CAM (fuller); a large GG-18 × CAM-33 asymmetry (the distinct-recension signal, NOT a defect — see MARATHON_LOOKAHEAD + memory `no-reassert-ratified-bar`); and the year-dating-placement divergence (CAM head / GG end). Resolve the AMBIGUOUS carry-forwards via METHOD NOTE 4 paths: v1 480-numeral (`፬፻` vs `የ`), v12 verb `ወአቀውም`, v17 `ክ/ኩ`. → C-8 honesty review → C-9 flip manifest `1ki:6` calibrated + ledger row + commit + backup. ▶ **THEN — 2-LANE ACCELERATED ENGINE (1ki7→2ki25, then Samuel 55):** Lane 1 = foreground chapter C-1…C-9 (MAX 1 heavy vision agent; drain before next); Lane 2 = BACKGROUND CAM hi-res PRE-PULL of chapter N+1 (`scripts/acquire_cudl_master.py` → IIIF tiles → stitch → disk; QC by dimensions/size ONLY, NEVER read tiles into context; RAM-gated, fall back to sequential if free RAM <~3 GB; 1ki7's CAM f128v already pre-pulled). Both scope-ahead dossiers feed C-1 per the BLINDNESS RULE. Cadence: NARRATIVE continuous, LIST/REGNAL check-in; NO corner-cutting (deadline extended ~1 mo, user 2026-05-27). Manifest: 1ki:1-5 calibrated, 1ki:6 in-progress (closes at C-9), 1ki:7-22 + 2ki:1-25 + Samuel 55 pending. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (1ki6 mid-chapter, paused between C-5 and C-6). ✅ 1ki6 C-4 (CAM locate) + C-5 (blind CAM transcribe) DONE + committed; ▶ resume C-6 (adversarial CAM review). USER AWAY ~2h — autonomous run: commit each step + periodic E:/F: backup; CANNOT self-`/clear` → RAM-guarded via subagent isolation (all hi-res bytes stay in subagents, never the controller), MAX 1 heavy agent, tight ≤1568px crops; RAM held ~8.3 GB free across both steps.** **C-4 (vision-locate via 1 isolated opus agent):** CORRECTED the stale "f128v=1ki6/7" pre-pull label — **CAM 1ki6 is ENTIRELY on f128r** (L from ~line 9 → M full → R through ~line 16-17, ends `…ወሐነፀ ፡ በ፯ ፡ ዓመት`); **f128v is 1ki7** (Hiram of Tyre / bronze / pillars, ክፍል ፴፩) — retained as the 1ki7 pre-pull. No new CUDL pull needed (f128r hi-res already on disk). Stray `.sonar` in the hires dir removed. **C-5 (blind opus transcribe, f128r ONLY, 1 heavy agent):** `content/manuscript/kings/calibration/1ki6_witnessCAM_hires.json` = **31 sense-unit verses**; `validate_witness` OK · non-Ethiopic screen CLEAN · NARRATIVE self-check 0 · 32 uncertain / 3 damaged / **0 illegible**. Opens at red `ክፍል ፴` + marginal `ምዕ ፮`, `ወእምዝ`, with the **480-year dating** (`፬፻ ፡ ወ፹`, printed/MT-closer, year-FIRST) — diverges from GG's LXX house-first incipit (GG dates at chapter END). Divine name **1st-order `እግዚእብሔር`** (the transcriber caught a near-harmonization to printed `አ` and cross-checked the 1ki5 CAM hand — **blindness caveat: C-6 must re-verify the divine name independently on the f128r ink**). **⚠⚠ C-6 MAKE-OR-BREAK:** CAM jumps **v10 (`…ኑኁ`, L-foot) → canonical v13 (`ወአኃድሬ ፡ በማእከለ`, M-top)** with the **divine-promise of canonical vv11-12 ABSENT**; transcriber claims genuine (re-traced ×2) but left an **orphaned overlined `ከ` at M-top** = the classic homoeoteleuton-skip red-flag (CAM_topology §2 worst class; 1ki2 precedent = "claimed minus was actually on the page"). C-6 MUST re-trace the L-foot→M-top column turn glyph-by-glyph and prove vv11-12 genuinely absent vs skipped — and note **GG HAS this passage at its v6**, so if CAM's minus is genuine, GG is the FULLER witness here (inverts the usual pattern). Column continuity verified at both turns; foxing band R-lines ~8-12 (vv29-30); flags concentrated on the v1 year-numeral + divine-name + the omission junction + foxing band + temple loanwords. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (1ki6 mid-chapter, paused between C-3 and C-4). ✅ 1ki6 C-3 GG WITNESS REVIEWED-CLEAN (R1+R2 adversarial) + scope-ahead method WIRED in — committing + E:/F: backing up THIS turn → READY for /clear → resume C-4 (CAM hi-res pull).** R1 found **2 CRITICAL homoeoteleuton omissions** (v5 `ወእሠርዮ→ወእሠርዎ`; the v15→v16 nave-doors passage across the f031r-R→f031v-L FOLIO TURN) + 5 major glyph/harmonization fixes (v4 `መፍ→መፍደሕ`; v4/v7 `ወሦሉ→ወኩሉ`; v11/v15 `ዘዴት→ዘይት`; v14 `ወእሥዓላ`; v17 `መዓርጉ→መደርክን`). A fix-agent RESTORED both omissions (each re-confirmed genuinely on the parchment) → witness now **18 v** (nave-doors became its own verse). R2 independently UPHELD all 6 fixes — **zero substantive defects, zero fabrication**, v6 `⟦illegible⟧` genuine, the **1ki6:1 LXX house-first incipit confirmed a genuine recension feature**; STOP boundary confirmed (ends `…ወሐነጸ ፡ በ፯ ዓመት`, no bleed into the red `ክፍል ፴`/1Ki7). `validate_witness` OK · screen CLEAN · 18 v. AMBIGUOUS → C-7 CAM cross-ref: v17 `ክ/ኩ` + minor v16 glyphs. `GG_topology.md` appended (ኩ/ሠ + ይ/ደ-in-`ዘይት` refs + 2 homoeoteleuton worked examples + ክ/ኩ §5). Manifest **1ki:6 in-progress** (close = C-9). **SCOPE-AHEAD shipped + WIRED (validated heavy+light concurrency):** rolling text-only agents built look-ahead dossiers for ALL of **1ki7–22 + 2ki1–25** → `dev/MARATHON_LOOKAHEAD.md` (class/rounds · KJV ceilings · known LXX divergences [1ki12 Miscellanies · 1ki14 minus · 1ki20↔21 swap · 2ki14–17 REGNAL · 2ki17 expansion · 2ki18–20 Isaiah∥] · folio bands); **Samuel scope still PENDING.** Wired into the collation plan: C-1 now consults MARATHON_LOOKAHEAD; new **METHOD NOTE 6 = the BLINDNESS RULE** (recension-content → controller + reviewers, NEVER the blind C-2/C-5 transcribers; class-screens → transcribers via `chapter_profile`); + a **bounded-concurrent low-buffer CAM hi-res pre-pull** design (disk-only, RAM-gated — first live test NEXT session during 1ki6's heavy CAM steps, pre-pulling 1ki7's CAM). Concurrency validated: 1 heavy vision + ≤2 light text-only = ~8 GB free, flat (memory `feedback-concurrent-agent-cap`). Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (marathon unchanged, paused mid-flight between C-2 and C-3). ✅ CLAUDE CODE ENV CLEANUP + a §0 session-start health-check rule — committing + E:/F: backing up THIS turn.** No code/corpus/base/build/test change. Global `~/.claude` plugins trimmed ~100→62 (login-free local only; removed all remote/token MCP + noisy SessionStart-hook plugins + learning/explanatory styles); sonar fully removed (CLI + User-PATH + `.claude/hooks/sonar-secrets/` + `.sonar`/`.scannerwork`); repo-parent SaaS plugins dropped (bootstrap-triad hook KEPT). Auth = subscription/OAuth (no API key needed; "org cancelled" wall was pre-login). In-repo (this commit): `dev/CLAUDE_PROJECT_RULES.md` §0 NEW env-health rule (update-awareness + `/mcp` check; tokenless-by-design; NO forced auto-update). Env changes apply NEXT launch — start in the repo-PARENT `…\YHWH-v2.4-full\`. **▶ NEXT: 1ki6 C-3** = adversarial GG review (after /clear; marathon UNCHANGED — the prior banner below is the live marathon truth). Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.

> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE (marathon unchanged, paused mid-flight between C-2 and C-3). ✅ ACCOUNT-SWITCH + PORTABILITY HARNESS — committed this turn + E:/F: backed up.** Process / harness / docs only; no code / corpus / base / build / test touched. New Anthropic account on the same Windows user (`bogda`) — old account locked out. The new org has DISABLED Claude Code subscription auth → API-key path (`setx ANTHROPIC_API_KEY "..."` once for fresh terminals; the live session inherits via parent-process env). `~/.claude/settings.json` cleaned: removed both sonar hooks (PreToolUse + UserPromptSubmit, both 404'd internally); set `sonarqube@` + `cockroachdb@` plugins to `false` (cockroachdb's PostToolUse `check-sql-files.py` fired on every Write). `.env` Voyage key scrubbed (`VOYAGE_API_KEY=` empty; `.env` gitignored). NEW tracked `dev/cc-hooks/` (bootstrap-triad source + idempotent installer + README) — rehydrates the gitignored runtime hook + `.claude/settings.json` registration on any fresh clone / machine / account. Memory note `reference_account_switch.md` + `MEMORY.md` index entry added for future-Claude orientation. Key audit clean (no `sk-ant-`/`ghp_`/`AKIA`/`AIza`/`xox*`/generic `*_API_KEY=…` in tracked files). **▶ NEXT: 1ki6 C-3** = adversarial GG review (after /clear, per the prior entry below — marathon state UNCHANGED). Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.

> **➤➤➤ 2026-05-27 (prior) — Tracker ACTIVE. ▶ MARATHON 1 Kings 6 IN-FLIGHT — C-1 (locate+classify) + C-2 (blind GG transcribe) DONE + committed + E:/F: backed up; ▶ resume at C-3 (adversarial GG review) after /clear.** C-1: 1ki6 = NARRATIVE (2–4 rounds, continuous cadence); located by vision = span **f031r-L(L27) → f031r-M → f031r-R → f031v-L(~L13)**, ~3.25 cols. A first sonnet locator put the end at f033v — a ~3× density overshoot (1ki5 = 13 v in ~1.65 cols); REFUTED + corrected by an opus boundary-finder (found the temple-completion "በ፯ ዓመት"=7yr, then the red ክፍል ፴ rubric + "ቤዶ ሰሎምን በ፲ወ፫ ዓመት"=own house 13yr=1ki7). C-2: blind opus GG transcription → `content/manuscript/kings/calibration/1ki6_witnessGG.json`, **17 v** (LXX/3-Kingdoms compressed recension; cf. 1ki5 13-vs-KJV-18); `validate_witness` OK + non-Ethiopic screen CLEAN + NARRATIVE self-check 0; 44 uncertain + 1 ⟦illegible⟧ (v6), mostly resolution-limited temple cubit-numerals + Greek-derived architectural terms; ✣ not harmonized to እ. **⚠ C-3 MUST adjudicate the 1ki6:1 incipit:** C-2 read it "ወውእቱ ፡ ቤት ፡ ዘሐነፀ ፡ ሰሎምን…" (LXX house-description-first; year-dating moved to chapter END), DIFFERING from the 1ki5 boundary-note's MT expectation "ወእምዝ አመ ኮነ ፬…" — verify on parchment (likely a real LXX recension feature, prime review locus). Manifest: 1ki:1–5 calibrated, **1ki:6 in-progress**. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-27 (prior) — Tracker IDLE. ✅ PROCESS FIX (triad-read SessionStart hook + memory reconciliation + RULES §0 clarification + a RULES coherence pass) — committed + E:/F: backed up; READY for /clear. ▶ Resume the marathon at 1 Kings 6 C-1 next session.** No code/corpus/base/build/test change. Fixed the recurring "continue → skipped the triad" miss: a project SessionStart hook (`.claude/hooks/bootstrap-triad.ps1`, in `.claude/settings.json`) now forces the triad read every session; the contradictory `feedback-terse-bias-to-action` memory was reconciled (terseness applies AFTER the triad, not instead of it); RULES §0 + the §10/§11/§12/§15 staleness fixed. 1ki5 stays calibrated (manifest 1ki:1–5); 1ki6 C-1 (GG transcribe) is the resume point — CAM f128v pre-pulled (it is 1ki6/7). Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-26 (prior) — Tracker IDLE. ✅ 1 Kings 5 CALIBRATED — full C-1…C-9, both witnesses reviewed-clean; chapter-close committed + E:/F: backed up; READY for /clear. ▶ Resume the marathon at 1 Kings 6 C-1.** GG C-3 R2 reverted 3 of 4 R1 harmonizations (`67b1330`); CAM C-4 pull + C-5 transcribe (`abdf503`) + C-6 R1/R2 = 5 fixes (`5cdbc30`); C-7 collate semantic 100 % / base CAM / 0 fabrication / lacuna 0; C-8 PASS; C-9 manifest flip + ledger. CAM 1ki5 was on f127v-R + f128r-L (NOT the arithmetic guess f128r+f128v; f128v = 1ki6/7, pre-pulled). Self-upgrading-matrix done: CAM/GG topology appended + 2 memories (CAM folio-location by vision; Glob/Grep skip gitignored). Manifest 1ki:1–5 calibrated; 93 ch left (42 Kings + 51 Samuel; Samuel deferred). Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-26 (prior) — Tracker ACTIVE. ▶ MARATHON IN-FLIGHT: 1ki5 C-3 R2 DONE, committed; next = C-4 CAM hi-res pull.** Per-step cadence (user 2026-05-26): commit + clear-all-the-system-RAM-Claude-can after EACH agent-step, automated, looping through to chapter close (1ki5 = the check-in boundary; the user runs `/clear` at their own discretion — Claude does not self-invoke it). **C-3 R2 GG adversarial review (opus; 1 heavy agent; tight ≤1568 px f030v crops; scratch in OS temp, outside repo):** R1 had over-harmonized 3 of the 4 AMBIGUOUS-PARCHMENT MAJORs toward the printed Bible — reverted to the inked glyphs per f030v: M-1 v1 ወፈነሙ→ወፈነዎሙ (real medial ዎ present), M-2 v1 ንጉሥ→ንጉሠ (inked 1st-order ሠ, not ሥ), M-3 v6 ወሶበ→ወከበ (inked ከ, not ሶ); M-4 v8 ወትህብ CONFIRMED (no trailing ሊ; R1 fix stands). M-2 + M-3 carried to C-7 CAM cross-ref (METHOD NOTE 4). `validate_witness` OK + non-Ethiopic screen CLEAN; working tree had only `1ki5_witnessGG.json` changed (10±). **▶ NEXT: C-4 — locate + pull the Cambridge Add. 1570 hi-res CAM folio(s) for 1ki5 from CUDL IIIF** (memory `cudl-iiif-access`; the ToC mislabels Reigns → locate by VISION against the GG narrative), then C-5 CAM blind transcribe → C-6 CAM review → C-7 collate → C-8 → C-9 flip `1ki:5` calibrated. Manifest: 1ki:1–4 calibrated, 1ki:5 in-progress, 1ki:6–22 + 2ki:1–25 pending. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`.
>
> **➤➤➤ 2026-05-26 (prior) — Tracker IDLE. ▶ TIER-3 marathon RESUMED: method ratified + paid-API de-scoped; doc-only commit; ready for `/clear`.** Audit/planning only — no vision run, no API spend; tree was clean (no stray files from this session). **Paid script-path OUT OF SCOPE** (no API budget; `anthropic` NOT installed + `ANTHROPIC_API_KEY` NOT set, both verified on disk; `manuscript_vision.py` + `run_manuscript_*_at_scale.py` stay inert, not deleted). **Marathon runs on the AGENT path** (Claude Max). **Crash root-cause refined:** NOT source size (GG folios 0.7 MB / CAM hi-res ~10 MB) — it was whole-folio 4–8× LANCZOS upscale (30–60 MB/agent) × 3 stacked → OOM; vision downsamples >1568 px so whole-page upscale was doubly wasteful. **RATIFIED:** tight region crops (≤1568 px, one column/band — sharper + ~1–3 MB) NOT whole-folio upscale · **MAX 1 heavy agent** · **auto-commit each step** · **`/clear` between heavy steps** · **unit = ONE agent-step, not a chapter**. **▶ RESUME: 1ki5 GG R2** (adversarial review of the R1-fixed GG witness; the 4 M-1…M-4 ambiguous loci may carry to C-7 CAM cross-ref) → C-4 f128r CUDL pull → C-5/C-6 CAM → C-7/C-8 collate → C-9 flip `1ki:5→calibrated`. Manifest: 1ki:1–4 calibrated, rest pending. Method: `docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md` (EXECUTION METHOD note).
>
> **➤➤➤ 2026-05-26 (prior) — Tracker IDLE. ✅ IN-DEPTH AUDIT (cycle 2) COMPLETE — full-suite caught + FIXED 10 stale pins; all gates green; healthy. COMMITTED `b0508789` + E:/F: backed up #2; full-suite re-run **OBSERVED 7,368/0** + this doc-update committed; READY for the pre-TIER-3 /clear.** First full-suite since 2026-05-25 = 10 failed / 7,358 passed; all 10 = stale pins/uncatalogued-data from the 3 intervening ships (Torrey, web-split, Phase E), ZERO regressions, ZERO from the de-dup. Fixed: psi35 markers→`web_matrix.py` ×3; web_filesplit api_covers→`web_covers`; vulgate book-count per-store (77/74); `torrey_topical` whitelisted ×3; Torrey catalogued in `source_dates.yaml` ×2. Verified 331 (touched files) + 10 node-ids green; then **OBSERVED** a full clean serial sweep = 7,368/0 (27:37). Fresh gates: ci.py CLEAN · taxonomy 91,733/100% · verify errors=0/32,264 paired. Findings: `dev/AUDIT_2026-05-26-FINDINGS.md`; 2026-05-23 ledger reconciled (~95% done). **▶ AFTER /clear: TIER-3 (user-paced)** + the fabrication-deferred Phase-E chapters + a low-severity audit tail (findings doc; none blocking). Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `c45ca38`) — Tracker IDLE. ✅ Post-de-dup cleanup (Phase-E re-verify · B1.6 note · F1 junk).** Phase-E deferred chapters RE-VERIFIED → deferral CONFIRMED; B1.6 documented; F1 untracked+gitignored `content/.refactor_log.yaml`. Gates: `test_refactor` 29 · `ci.py --no-tests` CLEAN.
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `6e5adad`) — Tracker IDLE. ✅ De-dup B1.8 + B1.9 COMPLETE — shared bases for the AI clients + the 6 commentary loaders.** Behavior-PRESERVING (TDD: characterization test first → consolidate → still-green); all public names/fields/messages/defaults byte-identical, hub + callers untouched. **B1.9** `sources_commentary.py`: 6 loader near-clones → `_CommentaryCorpus` base + 6 thin subclasses (6 dataclasses KEPT distinct). **B1.8** `sources_ai_clients.py`: the 2 sibling clients → `_AnthropicClient` base (shared `__init__`/`_valid_codes`/`_default_completion_fn`/cache; distinct `attribution`/`propose_xrefs`/`draft_note` kept). `manuscript_vision` untouched (out of scope). NEW `tests/test_commentary_loaders.py` (18) + 2 SDK-wiring pins; gates green (908+67+10+44 · `ci.py --no-tests` CLEAN · mypy ✓ · lint net-neutral PROVEN). NO corpus/base/build change. **▶ NEXT (user-direction):** deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess) · TIER-3 (user-paced). Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior) — Tracker IDLE. ✅ Session wrap: rules-hygiene meta-rule added (§12 mistakes→preventive-guard); committed + E:/F: backed up this turn. Ready for /clear.** Session shipped + committed + backed up: `inject.py` nested-anchor root-fix (`8bbb516`) · god-module splits `sources.py` 1/2 (`774246f`) + `web.py` 2/2 (`bae92e4`) · this rules commit. All truth-gate verified; both splits AST-symbol-equality faithful. **▶ NEXT (user-direction):** B1.8/B1.9 de-dup · deferred Phase-E chapters · TIER-3. Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `bae92e4`) — Tracker IDLE. ✅ God-module splits COMPLETE — (2/2) `scripts/web.py` → 8 domain modules + 2,028-line server-core/re-export hub. UNCOMMITTED, awaiting "save".** Pure relocation of `api_*`/helpers; Handler + route tables + main stay in web.py. Verified faithful by AST symbol-equality (104==104, 0 logic mismatch) + import smoke + `test_web_routetable` 89 + 352 web/api tests + `lint_rules` 16/0/0 + `ruff format` clean. Caveat: ~87 standalone divider/historical comment lines dropped in the hub-slim (inline comments + docstrings preserved; acceptable). **▶ NEXT (user-direction):** B1.8/B1.9 de-dup · deferred Phase-E chapters · TIER-3. Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `774246f`) — Tracker IDLE. ✅ God-module split (1/2): `scripts/core/sources.py` → 5 modules + 89-line re-export hub — UNCOMMITTED, awaiting "save".** Pure relocation (re-export-shim pattern); callers unchanged. Verified faithful by AST symbol-equality (57==57 symbols, 0 logic mismatch, comments preserved) + import smoke + 157 tests + `lint_rules` 16/0/0 + `ruff format` clean. B1.8/B1.9 de-dup deferred. **▶ NEXT:** god-module split (2/2) = `web.py` (5,214 LOC). Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior — COMMITTED `8bbb516`) — Tracker IDLE. ✅ Root-fixed `inject.py` nested-`<a>` cause (`find_verse_region_b` region-end) — UNCOMMITTED, awaiting "save".** The Strategy-B verse region now ends BEFORE the next verse's `<a class="vn-link">` (not at its span), so verse-level markers can't nest inside it (the asymmetry with Strategy A). TDD `tests/test_inject_wellformed.py` +2 (RED→GREEN); no re-bake (committed base already clean, fix is preventive). Gates: nested-anchor base gate **0/61** · `lint_rules` 16/0/0 · `ruff format` clean · inject/nested/spill/marker suites green. Closes the Phase-E ship's deferred follow-up. **▶ NEXT (user-direction checkpoint):** deferred Phase-E chapters (1es 5/8, 2es 14 — no-guess) · the 2 god-module splits (user-deferred) · TIER-3 (user-paced). Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior) — Tracker IDLE. TIER 2 #7 code-debt — tasks 1-4 done + COMMITTED (4 checkpoints) + E:/F: backed up; #5 god-module splits DEFERRED (user's call).** **#1 tooling:** vulture+pip-audit declared (`requirements-dev.txt`) + installed + wired into `scripts/ci.py` as report-only checks (+2 tests); shipped deps 0 CVEs (the 10 found are dev-env only). **#2 safe fixes:** F811 fixed — `TestCustomize` defined twice (`test_scripts.py` 715+1569) silently shadowed 8 customize.py tests → renamed first to `TestCustomizeScript`, all 8 recovered + green; E401 fixed; audit micro-items A3/B1.3/A7 already resolved; the 6 W605 are TIER-3 OCR data noise → folding into #3. Test collection +10. **#3 ruff backlog:** excluded `content/` data from `ruff check` code-style rules (`pyproject.toml` per-file-ignore for `content/translations/**/*.py`) → backlog **118,725 → 268**; applied 6 safe autofixes (UP045/SIM300/SIM910/UP035/UP037) + cleaned the one resulting F401. Remaining 268 = the project's intentional honest-backlog (C901 66 inherent-complexity · naming 70 mostly-intentional · scattered SIM105 49 mostly in tests) — left as DOCUMENTED backlog (C901 partly reduces via #4/#5). **#4 de-dup:** A8 done — `_editions_index()` across 4 api modules now delegates to `config.editions_by_id()` (160 tests pass); B1.8 (AI-client) + B1.9 (6 commentary loaders ~540 LOC) FOLD INTO the deferred `sources.py` split (same code). **▶ DEFERRED = #5 the two god-module splits:** `sources.py` (2,950 LOC → lexicon/commentary/ai_clients/ai_prompts + absorb B1.8/B1.9) + `web.py` (5,214 LOC → extract clusters) — riskiest #7 work; need full-suite + epubcheck + byte-compat proof (re-export-shim pattern = the `build_edition`→`matter_pages` precedent); non-functional → a focused fresh-session task. **▶ OTHER OPEN:** surface Torrey in the EPUB topical-index (`scripts/matter_pages.py` merge `naves_topical`+`torrey_topical`); TIER 3 parallel-Bibles (user-paced). Bundle-backed-up to E:/F: this session (commits `48b5bd8` · `1cc1c2d` · the A8 commit).
>
> **➤➤➤ 2026-05-26 (prior) — Tracker WAS IDLE. ✅ Torrey COMMITTED (`13462720`) + E:/F: bundle-backed-up; + session-end COMMIT/BACKUP TRUTH GATE added (PLAYBOOK §0 + new §6.7 · RULES §12 5-point audit + §14 resume backstop).** A resume-time `git status` caught the Torrey ingest sitting UNCOMMITTED despite the prior session's "committed + backed up / safe to /clear" claim — nothing lost (`/clear` doesn't touch the tree), uncommitted was rule-correct ("continue" ≠ "save"), the false claim was the bug. Committed it + backed up + closed the rule gap (the audits verified work was *recorded*, never that a commit *happened*). **▶ NEXT:** TIER 2 #7 code-debt (vulture/pip-audit/ruff — DECLARE deps in `requirements-dev.txt`, auto-mode OFF) → surface Torrey in EPUB topical-index. Full account: SESSION_STATE + CHANGELOG 2026-05-26 (cont.).
>
> **➤➤➤ 2026-05-26 (prior — now COMMITTED `13462720` + backed up) — TRACK C: Torrey FULL ingest DONE + VERIFIED — was READY TO COMMIT + BACKUP (post-/clear).** Corpus 69,969 → **91,733 (100% attributed)**, +21,762 `topic-torrey`; baked into `epub_working/`; flagship **epubcheck 0/0/0/0**; `ebible verify` errors=0/32,264; `lint_rules` 16/0/0; 20 torrey tests + `record_count` 72; `ruff` clean. NEW χ-cluster pipeline + `batch_promote --by-book` perf fix (44 s vs ~46 min) + legacy book-code normalization. **⚠ NOT YET surfaced in EPUB** (topical-index is Nave's-only — follow-up AFTER TIER 2 #7). **▶ POST-/CLEAR ORDER:** (1) COMMIT (`git add -A` — all intended; 0 junk) + bundle-backup E:/F: (NEVER C:; no push); (2) TIER 2 #7 code-debt (vulture/pip-audit/ruff backlog); (3) surface Torrey in the topical-index. **Full handoff — commit msg · file scope · backup cmd = `SESSION_STATE.md` 2026-05-26 LATEST.**
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 3 STARTED: `verse_popup_style` + `note_popup_style` (CSS-only popup-layout settings) SHIPPED + verified. Tracker IDLE.** Re-bake-FREE slice of Wave 3. Two per-edition popup-layout toggles, each applied in `build_one` by APPENDING a variant CSS block to the edition stylesheet (same mechanism as the theme override) — popup/note HTML unchanged, **no base re-bake**. **§4.2 `verse_popup_style`** (`cards` default/`stack`): `cards` → tinted witness-card chrome (gold/purple spines on `.vnote-hebrew`/`.vnote-greek`); `stack` → flat base. **§4.4 `note_popup_style`** (`chip` default/`pills`): `chip` → tinted rounded label-chip (`.note .note-label`); `pills` → bordered pills on in-note cross-refs (`.note a:not(.note-back)`). NEW `build_edition.py` consts + `_*_CSS` + `apply_verse_popup_style`/`apply_note_popup_style`; §7-wired in `api/editions.py` (allowed-lists + 2 enum validators), `web.py api_customize_data` (defaults cards/chip), `templates/customize.py` (2 selects). `tests/test_popup_styles.py` 16; 86 build-level regression green; `lint_rules` 16/0/0; ruff clean; catholic-study → epubcheck 0/0/0/0; shipped-stylesheet cascade-verified. **▶ NEXT — the re-bake-DEPENDENT remainder of Wave 3** (prereqs first): `marker_style`→numbers · symbols-INTO-notes (+ glossary link) + drop stray `‖` · widened/drop-KJV popups · Nave's back-matter. Prereqs: MATRIX_MAP update · `resync_marker_glyphs.py` + categorize-diff verifier · split `build_edition.py` (~3,860 lines). Plan `dev/PLAN_2026-05-24-end-scope.md` Wave 3; full handoff = SESSION_STATE 2026-05-25 LATEST.
>
> **➤➤➤ 2026-05-25 (prior) — EPUB Wave 2 COMPLETE: §4.6 cover picker SHIPPED + title-overflow fix + 3 pre-existing bugs fixed (browser QA). Tracker IDLE.** §4.6 = `COVER_TEMPLATES` (25 stems) + `cover_template_catalog()` in PIL-free `scripts/core/covers.py`; `cover_template` field §7-wired (`api/editions.py` validator + `web.py api_customize_data` default+catalog); `api/covers.py::api_apply_cover_template` recompose endpoint (`POST /api/covers/<ed>/template`); `/customize` "Cover" panel = 25-thumbnail picker (immediate recompose) + main/per-book upload affordances; `generate_edition_covers.title_for_edition`. 23 tests (`tests/test_cover_templates.py`); verified live in-browser. **Title-overflow fix (user-reported):** `_fit_title_font` auto-shrink + 150px safe-zone → 4 long-title covers regenerated at 64pt. **3 fixes:** (1) `/customize` rebind loop wired ONLY the first edition (preview button also has `data-edition`; unguarded `btn.addEventListener` threw, aborting the loop) → `if (!btn) return` guard + test — was broken for 10/11 editions; (2) `corpus_index` SQLite cross-thread 500 on `/customize`+`/matrix` → `check_same_thread=False` + test; (3) 5 `*_red` template PNGs were JPEGs misnamed `.png` (415 on the cover route) → re-encoded + guard test. Gates: 221 tests · lint 16/0/0 · ruff clean. **▶ NEXT — Wave 3 (EPUB Presentation Phase 2 = BASE RE-BAKE):** remaining 3 enum settings (`marker_style`→numbers · `verse_popup_style` · `note_popup_style`) · symbols-into-notes · widened/drop-KJV popups · Nave's back-matter; prereqs = MATRIX_MAP update + `resync_marker_glyphs.py` companion + split `build_edition.py`. Plan `dev/PLAN_2026-05-24-end-scope.md` Wave 3; full handoff = SESSION_STATE 2026-05-25 LATEST.
>
> **➤➤➤ 2026-05-25 (prior — §4.6 now DONE, see top) — EPUB Wave 2: §4.5 per-book title-page art DONE + COMMITTED; §4.6 cover picker NEXT. Tracker IDLE.** §4.5 `title_page_style` (full-bleed default / framed): `build_edition.apply_title_pages(tmp, edition, canon_books)` (in `build_one` after the canon filter) injects each KEPT book's art (`book_covers` override → `content/covers/_book_defaults/<code>.jpg` → text-only) into its `book-title-frame` + `patch_opf_book_images` registers them in the OPF; §7-wired setting + `/customize <select>`; CSS in `epub_working/stylesheet.css`. catholic-study epubcheck **0/0/0/0**, 66 art images packaged, Genesis full-bleed **visually confirmed** (Playwright); `tests/test_title_page_style.py` 12 + 170 customize/editions regression green. Also committed this session: title-only covers + universal PIL-anchor placement (`6e58570`). **▶ NEXT — §4.6 (task #13):** `cover_template` field (25 stems = 5 families × 5 colors; §7-wire like `title_page_style`) → 25-thumbnail `/customize` picker (thumbs from the sandbox-served `/content/covers/templates/<stem>.png`) → on pick recompose via `generate_edition_covers._compose_cover(stem, title)` + set `cover_image`; surface the EXISTING `/api/covers/<ed>/main` + `/api/covers/<ed>/book/<code>` upload endpoints as UI affordances. Plan `dev/PLAN_2026-05-24-epub-presentation-polish.md` §2E; full handoff = SESSION_STATE 2026-05-25 LATEST.
>
> **➤➤➤ 2026-05-25 (prior — COMMITTED `c83c743`) — nested-`<a>` (746 RSC-005) + a surfaced RSC-001 FIXED; catholic-study epubcheck 0/0/0/0. Tracker IDLE.** The #1 CRITICAL handoff item. Inherited root cause (a `generate_verse_popups` `vchunk` over-scope) was WRONG — re-verified ([[reverify-conservative-nogo]]): the 746 are a **static recovered-base artifact** (`note-ref` nested in `vn-link`) across **8 files** (psa 033/034/035 · sir 038 · isa 040 · jer 042 · 2co+phi 058 · 1ti+jam 059), fixed by NEW `scripts/check_nested_anchors.py` (byte-exact repair → 746→0, byte-multiset conserved, only those 8 files) + a JVM-free `nested <a>` gate in `scripts/ship-check.py` + `tests/test_nested_anchors.py` (10). Clearing them **unmasked** an RSC-001 (`_drop_placeholder_introduction` manifest regex `[^/]*`→`[^>]*` — `introduction.xhtml` left dangling) → fixed + unit-tested + the integration test strengthened. Full account: SESSION_STATE + CHANGELOG 2026-05-25. **COMMITTED `c83c743`.**
>
> **➤➤➤ 2026-05-24 (prior) — `test_scripts.py` RUNNABLE: 976 tests, ~2.9 min, GREEN (D.hang + D.slow fixed). Tracker IDLE.** D.hang (deadlock): 9 socket tests → `ThreadingHTTPServer` + `test_ops` mocks `api_preflight`. D.slow (the ~1 hr a full run then took): conftest session fixture `_stub_exports_epubcheck` stubs the real epubcheck/Java over the 12 `exports/` EPUBs — only for the real `exports/` dir (`TestEpubcheckWrapper`'s tmp tests stay real). Fixed 2 stale pins exposed by the now-runnable suite (themes "all classic" → registered-theme check; popup-registry `douay`/`vulgate` now baked=True). **"Never run full test_scripts.py" rule RETIRED** (§4). **ALSO DONE:** web.py cleanup A1/A6/A9/A12 (`fb50001`); `test_web_filesplit.py` (~11 s) + `test_matrix_psi35.py` (~12 s) confirmed FAST (D.slow conftest stub covers them). **▶ DEFERRED (owner said stop — context budget):** `C1.run` §7.1 (`content/notes/__init__.py::load_notes` uses `exec_module`; swap to `notes_io` ast-loader) · `audit_*` → CI / `ebible audit` target (not the dashboard) · P3/P4 (C2.phase4 · C3a/b dead-code · D.pins · preview.py `_NOTE_LABEL`/`_NOTE_TITLE` swap). See `dev/AUDIT_2026-05-23-DEEP.md` + PLAN §4.1.
>
> **➤➤➤ 2026-05-24 (prior) — C1.chap HEBREW BACKFILL SHIPPED `507bf7a`. Tracker IDLE.** Re-ran the at-scale hebrew generator (now `ch_count`-correct) at the original **`--min-confidence 0.65`** for the four >50-chapter OT-KJV books, promoted chapters 51+ only (dedup-skips 1-50): **+2,151 `lang-hebrew`** (psa 840→2502, isa 1031→1406, jer 1332→1422, sir 1134→1158). **Corpus 67,818 → 69,969.** Only `lang-hebrew` rose; only the 4 books' 6 epub split files changed (additive `inject --book`). Flagship carries it (22,897 asides == master), epubcheck 0/0; ebible verify errors=0/24,015; taxonomy 69,969/100%; TDD `TestLongBookHebrewBackfill`.
>
> **➤➤➤ 2026-05-24 (prior) — autonomous backlog (3 commits): phi/jam `309c0f7` · G1/G2 security `0892270` · a11y→preflight `1ae0d23`.** G1: `core/http.py` `{http,https}` scheme allowlist (`file://` SSRF/LFI closed). G2: `core/preview.py` `sanitize_html`s note bodies on the unauth `/api/preview` (XSS closed). a11y: `check_a11y.run_all()` composed into `/preflight` (the 3 subprocess `audit_*` left off the dashboard on purpose). **⚠ corpus-regen method note still applies (below): for note REMOVAL use SURGICAL removal, NOT bare-base regen (lossy); for ADDITION use additive `inject --book`.**
>
> **➤➤➤ 2026-05-24 (prior) — phi/jam DATA-cleanup SHIPPED + committed `309c0f7`. Tracker IDLE.** The ★BUGCLUSTER follow-up is DONE: 165 spurious `lang-hebrew` stripped from phi.py(76)+jam.py(89) (AST-span, 1,815 pure deletions) + **270 `lang-greek`** generated/promoted (phi 156, jam 114; `--min-confidence 0.65`). Corpus **67,818** (+105). All gates green (lint 16/0/0 · taxonomy 67,818/100% · ebible verify 0/24,015 · catholic-study epubcheck 0/0); TDD guard `TestNTBookLanguageInvariant`; tests +2 (→7,066).
>
> **⚠ KEY METHOD NOTE for ANY corpus regen (learned this session — supersedes the bare-base step in the recipe below + SESSION_PLAYBOOK §7):** `git checkout <base> -- epub_working/` → full regen is **LOSSY** — it drops `harvest_existing_langs`-preserved popup content the resolver can't rebuild (PROVEN: it wiped `vnote-paz-1-30`'s Douay+Vulgate) AND re-qualifies unrelated xref hrefs (~88). Inject is additive (won't prune orphaned asides), so to drop notes from the build do it **SURGICALLY**: from HEAD's `epub_working`, regex-remove the orphaned `note-<kind>` markers (`<a class="note-ref note-<kind>" …>…</a>`) + asides (`<aside class="note note-<kind>" …>…</aside>`) from the affected book's split file(s), then `inject --book <code>` to add the new notes. Verify CONTENT-level (aside-by-id diff), never raw line-diff. Only the changed book's split file(s) should differ.
>
> **➤➤➤ 2026-05-24 (prior — phi/jam was the NEXT TASK, now DONE above). Tracker IDLE.** The book-code CODE-fix is shipped (`c41e6d2`, recurrence prevented) and the program is renamed **YHWH Ya' Way**. The one remaining piece was the **phi/jam corpus data-cleanup — do it ATOMICALLY** (one coherent change → re-cert → save; a half-done state breaks source↔build consistency, so don't commit partway). **FULL RECIPE: `dev/SESSION_PLAYBOOK.md` §7.** Summary of the steps + the hard-won gotchas:
> 1. **Strip** the **165 spurious `lang-hebrew`** notes from `content/notes/{phi,jam}.py` (phi 76 + jam 89 — Hebrew word-studies the old book-code bug wrongly attached to two Greek NT books). Use AST-span removal → **pure deletions** (proven clean this session: 1,815 lines, 0 reformat churn; verify `git diff` shows only lang-hebrew tuples gone).
> 2. **Generate Greek:** `run_greek_at_scale.py --books phi,jam --min-confidence 0.65` → **270 candidates** (phi 156, jam 114). The original χ.1 threshold is **0.65**, NOT the 0.7 default (at 0.7 you get 0). ⚠ **DEBUG THE PROMOTE PATH FIRST** — `promote.py --promote-top` marked candidates `promoted` but did NOT insert them into the notes file, and a Hebrew-re-add quirk surfaced. ALSO: `content/candidates/{phi,jam}_ch_*.json` are git-tracked and were left STALE from the original buggy run — clear/regenerate them before promoting. Read `promote.promote_candidate` to find why it returns ok without inserting (likely a `note_already_exists`/anchor guard).
> 3. **Regen `epub_working` from base** — `inject` is **ADDITIVE** (it won't prune the now-orphaned hebrew asides), so the build won't reflect the strip until: `git checkout <base-commit> -- epub_working/` → `inject --all-books` → `generate_verse_popups` → `resync_marker_glyphs`. (Split files are shared between books — regen all, don't per-file.)
> 4. **Re-cert + save:** `validate_taxonomy` (new total ≈ 67,713 − 165 + greek), `ebible verify` 0/paired, `trace_matrix` 0, `lint_rules` 16/0/0, build `catholic-study` + epubcheck 0/0/0/0.
> Related (same regen family): the **C1.chap >50-chapter backfill** — Psalms 51-150 / Isaiah / Jeremiah may lack auto greek/hebrew (the runner key-bug, now code-fixed; re-run the at-scale generators for >50-ch books). Audit ledger: `dev/AUDIT_2026-05-23-DEEP.md`.
>
> **➤➤➤ 2026-05-24 — DEEP AUDIT COMPLETE; tracker IDLE (committed `637e793`).** Full ledger `dev/AUDIT_2026-05-23-DEEP.md`; forward QUEUED backlog `dev/PLAN_2026-05-21.md` §4.1. Shipped: **17 verified safe-fixes** + **matrix mint-cert** (catholic-study epubcheck 0/0/0/0; lint 16/0/0; 7,064 tests). Audit QUEUED backlog (PLAN §4.1): P0 ★BUGCLUSTER (code-fix done; phi/jam DATA-cleanup = the NEXT-TASK banner above) · P1 **G1** `file://` SSRF + **G2** preview-XSS (open) + **CC0**→all-rights-reserved DONE · P2 wire the dead audit-checks into preflight. The Track-B marathon (1Ki5 R2 →) remains the critical-path calendar spine.
>
> **➤➤➤ 2026-05-23 — DOUAY + VULGATE SHIPPED + baked; PHASE-2 TRANSLATION SPINE COMPLETE (COMMITTED `42a59e0`).** Table-driven `vulgate_to_kjv` (segments generated from the Copenhagen `vul.json`, composed vul→org→KJV, then driven to SHIFTS=0 vs the real Douay via `_vg_verify.py`); `_vulgate_cross` for the Daniel additions (paz/sus/bel); thin `extract_{douay,vulgate}.py`; both stores 74 bk / 33,343+33,344 v; baked (+31,867 `vnote-douay` + 31,866 `vnote-vulgate`, 0 other-content mismatches); `ebible verify` 0/24,015 paired; epubcheck catholic-study+anglican-bcp 0/0/0/0; `lint_rules` 16/0/0; 153 vulgate + 333 LXX tests. Full record: `dev/CHANGELOG.md` 2026-05-23. **NEXT (fast-follow, NOT yet started): Phase E — Clementine appendix Latin for `man`/`1es`/`2es`** (CLEAN-DIGITAL-FIRST — la.wikisource/Bibliotheca Augustana; the eBible + Tweedale digital sources both omit the post-NT appendix; `2es`'s Greek is lost so the Latin is its primary witness), as step 1 of a **shared vision-OCR engine** arc (generalize `manuscript_vision.py` to printed PDFs — also powers the Ge'ez/Amharic bulk-ingest); plus a prior-translation (WLC/LXX/Arabic/JPS) re-verify-vs-table pass. **The "D/V GRIND IN PROGRESS" map below is now HISTORICAL (superseded by this ship).**
>
> **Shipped this session (on disk, NOT yet committed):**
> - **NEW reusable infra — `extract_translation.apply_remap(by_project_book, remap)`** (+ optional `remap=` param on `extract()`): a `(code,ch,vs)->(code,ch,vs)|None` callback that remaps each verse to canonical KJV and CONCATENATES same-coord collisions in source order (mirrors `extract_lxx_swete.build_verses`). `remap=None` = byte-identical to before (PROVEN: re-extracted kjv → all 81 book .py files byte-identical; only `_meta` `fetched:` date moved). This is the foundation all four VPL translations use.
> - **Arabic (Van Dyck, `arabic-vandyke`) — COMPLETE.** Probe found Van Dyck = KJV versification across ALL 66 books / 1189 ch EXCEPT two tail-splits (content-aligned vs real text): AVD 1Ti 6:21+6:22 = KJV 6:21; AVD 3Jn 1:14+1:15 = KJV 1:14. `versification.arabic_to_kjv` = identity + those 2 same-book merges. `scripts/extract_arabic_vandyke.py` (thin driver over `extract`+`arabic_to_kjv`). 66 books / **31,102 verses**. Baked (`arabic` in `_BAKED_NOW`): categorize-diff +31,100 `vnote-arabic`, **0 non-arabic content mismatches** across 33,704 asides (originals byte-identical to HEAD). `ebible verify` errors=0/24,015 paired; **epubcheck 0/0/0/0** on coptic-orthodox (shows arabic, RTL) + ethiopian-tewahedo (arabic stripped). `tests/test_arabic_vandyke_ingest.py` (20). **Arabic already SURFACES in coptic-orthodox** (its `popup_languages_default` had `arabic`).
> - **JPS (1917, `jps`) — COMPLETE. FINDING: eBible `engjps` is ALREADY KJV-renumbered, NOT Masoretic — the runbook's "reuse `wlc_to_kjv_map`" is REFUTED** (applying it would double-remap/corrupt). Probe: 0 divergent chapters across 39 Tanakh books / 929 ch; total **23,145 = the KJV OT verse count**; every Masoretic locus is KJV-numbered (Ps 51=19 not 21; Joel=3ch not 4; Mal=4ch not 3; Gen 32=32 not 33). So PURE IDENTITY: plain `extract_translation jps` (no remap). Baked (`jps` in `_BAKED_NOW`): categorize-diff +23,145 `vnote-jps`, 0 other-content mismatches. `ebible verify` errors=0. `tests/test_jps_ingest.py` (9, incl. pins that fail under any Masoretic remap). Test pin updated in `tests/test_popup_versions.py` (arabic+jps now bake).
>
> **➤ NEXT = Douay + Vulgate (Task 3) — the HARDEST; scope MEASURED + de-risked this session.** WIP scaffold landed: `versification.vulgate_to_kjv` (Psalms reuse `_psalm_map` + identity elsewhere + empty `_VULGATE_SEGMENTS`/`_VULGATE_PSALM_FIXES`/`_VULGATE_CROSS` hooks) — DORMANT (douay/vulgate NOT in `_BAKED_NOW`; nothing in production calls it). Verification harness `_vg_verify.py` (repo parent): applies the map to the ENGLISH Douay and word-overlap-compares every KJV verse vs mapped-Douay-English — high overlap = content-correct (catches count-matching mis-maps the extent-guard can't). Findings:
>   - **Psalms — reuse PROVEN.** The LXX `_psalm_map` maps the Vulgate Psalms with 0 collisions; content-verified (psa 3/23/117 align). Only **3 psalms need a per-psalm fix** (psa 20/44/56 — Latin verse-split differs; missing the last KJV verse). Encode in `_VULGATE_PSALM_FIXES`.
>   - **~25 narrative books = tractable single-chapter offsets** (gen/exo/num/jos/gospels/acts/…): 1–3 chapters each where the Vulgate splits/merges one verse; the harness low-overlap cluster pinpoints each. Content-align Douay-English↔KJV-English, encode `_VULGATE_SEGMENTS[code]`.
>   - **Daniel/Esther additions:** Dan 3:24-90→`paz`, Dan 13→`sus`, Dan 14→`bel`, Dan 4 offset; Esther 10-16→`aes` (or omit, per LXX). Cross-book via `_VULGATE_CROSS`.
>   - **⚠ tob / jdt (and likely sir / lje) are a DIFFERENT RECENSION — CONFIRMED (`_probe_recension.py`):** Douay Tob 1:1 "Tobias of the tribe and city of Nephtali" vs KJV "The book of the words of Tobit, son of Tobiel…"; verse-by-verse correspondence is BROKEN (Jerome's Aramaic-based recension vs the Greek the KJV Apocrypha follows). Mapping them onto the KJV skeleton would attach WRONG text to every verse. **Correct handling = OMIT them from the Douay/Vulgate popups (return None), documented — exactly the LXX-aes precedent.** Those KJV verses keep their KJV + LXX-Greek popups; just no Douay/Vulgate column. NOT a gap — the only correct option (anything else = guessing).
>   - **14 chapters Douay≠Vulgate** (per-source verse-split diffs) → per-source override in the two drivers (`extract_douay.py` / `extract_vulgate.py`).
> Comparable in size to the whole LXX-Swete ingest. Then bake both, categorize-diff, epubcheck catholic-study + anglican-bcp, Phase-2 spine COMPLETE.
>
> **➤➤ D/V GRIND IN PROGRESS (2026-05-23) — resume here. `versification._VULGATE_SEGMENTS` + `_VULGATE_OMIT` are PARTIALLY populated (all harness-gated SHIFTS=0 unless noted); douay/vulgate still NOT in `_BAKED_NOW` (bake is ALL-OR-NOTHING — finish the WHOLE map first).**
> - **DONE + gated (segments encoded in `versification._VULGATE_SEGMENTS`):** NT count-mismatch — mat17, mrk8/9 (cross-ch), jhn6, act7/14, 2co13, rev12 (cross-ch). Prophets — jon2, hag2 (cross-ch date-verse), mic5, amo6, hos6/14 (hos14 cross-ch), jer37; eze2 = trailing-combine (identity, no seg). Genesis — gen49/50. Recension OMIT (`_VULGATE_OMIT`): tob, jdt, sir (different texts; return None — documented).
> - **STILL TO DO (≈50-80 careful alignments; use `_vg_win.py <book>` worksheet → verify → encode → `_vg_verify.py <book>` gate to SHIFTS=0):**
>   - **NT count-MATCHING shifts not yet encoded:** mat5 (Beatitudes SWAP: D5:4↔K5:5 → `[(1,3,5,1),(4,4,5,5),(5,5,5,4),(6,_HI,5,6)]`), act8 (D8:8=K8:7 tail), rev20 (D20:8=K20:9), jhn10:42 (verify). (eze2:9 SHIFT is the benign trailing-combine — leave.)
>   - **OT light:** jos4/5/21, jdg5/21, lev = identity (lev26 trailing-combine, NO seg; lev15:21/25:14 isolated = wording), 1ch (3/11/28 — verify), bar3 (verify), neh3/7/12, 1ki22 (tail offset).
>   - **OT heavy (cross-chapter + internal cascades — read carefully):** num (12/13 spies + 29/30 boundary + more, 41 shift-verses), 1sa24 (D24:1=K23:29 cross-ch), job16/39/40/41, ecc5 boundary (4:17/5:1) + 4/6/7, sng1 (Vulgate numbering, 28 shifts), exo38/39 (tabernacle reorder — the LXX DEFERRED exo36-39; consider omit), 1ma1+ (28 shifts), 2ma2+ (16 shifts), lje (whole-book offset, 59 shifts), hos2 (D24 vs K23 — verify last-verse).
>   - **wis** (69 shifts / 0.50 overlap) — ASSESS: borderline recension; if it won't align cleanly, OMIT like tob/jdt/sir.
>   - **Psalms:** reuse works; 3 per-psalm fixes in `_VULGATE_PSALM_FIXES` (psa 20/44/56 — Latin verse-split differs, missing last KJV verse).
>   - **Daniel/Esther additions (`_VULGATE_CROSS`):** Dan 3:24-90→paz, Dan13→sus, Dan14→bel, Dan4 offset; Esther 10-16→aes (or omit per LXX).
> - **THEN:** build `extract_douay.py` + `extract_vulgate.py` thin drivers (`extract(remap=vulgate_to_kjv)` + the 14 Douay≠Vulgate per-source overrides) → add `douay`+`vulgate` to `_BAKED_NOW` → regen → categorize-diff → ebible verify → epubcheck catholic-study+anglican-bcp → tests → docs → save. Tooling: `_vg_win.py` (worksheet), `_vg_verify.py` (SHIFTS gate — the objective check), `_vg_align.py` (proposer; verify each, it errs ~50%).
>
> **➤ EXECUTION FINDING (2026-05-23, calibrated — DON'T re-derive): the auto-aligner (`_vg_align.py`) is a PROPOSER, not an oracle.** Douay(Latin-derived)↔KJV(Hebrew-derived) share only ~0.5 word-overlap even when correctly aligned, so overlap can't reliably pick the right split vs an adjacent one, AND a per-chapter aligner misses cross-chapter boundary shifts. Calibration (`_vg_cal.py`): act 7 ✓ + gen 49 ✓ but **mat 17 ✗** (aligner put the gap at KJV 14; truth is KJV 15 — the Vulgate folds "Lord have mercy on my son" into v14) and **num 30 ✗** (cross-chapter: Vulgate 30:1 = KJV **29:40** "Moses told Israel"). So **each of the ~75 count-mismatch chapters needs eyeball Douay↔KJV verification** (like the LXX did) — use the aligner to PROPOSE the split, then READ to confirm/correct, then encode. **Recension decision FINAL: OMIT `tob`/`jdt`/`sir`** (book-identity-overlap 0.16/0.16/0.24, every chapter mis-aligns — different texts); **`hag`/`lje` are NOT recension** (single-offset, align well post-DP — include them). Per-book count-mismatch chapters + the proposed (UNVERIFIED) segments are in the `_vg_align.py` output; the harness `_vg_verify.py` is the post-encoding structural+overlap gate. Tooling kept in the repo PARENT (`_vg_align.py`, `_vg_verify.py`, `_vg_cal.py`, `_probe_vulgate.py`, `_probe_vps*.py`, `_probe_recension.py`).
>
> **AVAILABILITY model (clarified by user 2026-05-23): the 9 non-geez/amharic editions should let the BUILDER pick any/all/none of the translations in `/customize` before building — full customizability (§2.1), not fixed per-edition defaults.** This is ALREADY the design: `api_customize_data` offers ONE universal pool (`ALL_POPUP_LANGUAGES`) to every edition, the save-path accepts any registry id, and the build honors the picks (`_resolve_popup_languages`→strip). **FIXED this session:** the `has_data` flag in that registry was HARDCODED to `{english,hebrew,greek}` (web.py) so the freshly-baked arabic/jps/greek-nt/lxx-greek wrongly showed grayed-out "(no data)" in the picker — now data-driven via `popup_versions.bakes_now(resolve_version_id(lid) or lid)`, so every baked version shows live + pickable for all 9 editions, and douay/vulgate flip live automatically when they bake. Test `tests/test_scripts.py::...popup_languages_registry` rewritten data-driven. So the full chain (offer→pick→save→build-keeps) now works for arabic/jps end-to-end.
> **DEFAULTS (`popup_languages_default`) — left as-is per tradition (user: "decide later").** Each edition keeps its current default popup set (arabic stays default-on in coptic-orthodox; jps/douay/vulgate are PICKABLE-but-not-default elsewhere). Tune defaults whenever the user wants. NOTE: **greek-nt (Byzantine NT) is baked but is default-on in NO edition** ("greek"→lxx-greek only) — now correctly shows has_data=true in the picker (so it's pickable), but no edition defaults it on; flag for the defaults pass.
>
> **GOTCHAS unchanged** (Python full path + `$env:PYTHONUTF8=1`; never full `test_scripts.py`; epubcheck Java 8 off-PATH + jar in site-packages, ONE JVM at a time; categorize-diff by aside-id not line-diff). Throwaway probes live in the repo PARENT (`_probe_*.py`, `_count_classes.py`, `_aside_compare.py`, `_probe_editions.py`) — outside git, delete when done.
>
> **[Below: the prior 2026-05-23 banner — superseded for current state, retained for the deutero/cover/spine-prep ships it records.]**

> **➤➤➤ 2026-05-23 (prior session) — COMMITTED. Shipped + committed: (1) Deuterocanon LXX reorder pass COMPLETE (Judith + 1 Esdras + Manasseh + Azariah), (2) the per-edition COVER fix (build_edition now ships each edition's `cover_image`), and (3) PREPARED the spine arc — eBible VPL data for Douay/JPS/Vulgate/Arabic acquired into `sources/` + runbook at `docs/superpowers/specs/2026-05-23-translation-spine-arc.md`.**
>
> **Committed earlier:** WLC Hebrew (`fcd6217`) · LXX-Greek Swete OT (`3ea1b24`) · Greek-NT Byzantine (`4770256`) · Deuterocanon LXX 5 books wis/sus/bel/bar/lje (`ee62924`) · Sirach + Tobit + LXX-split-concatenation (the prior deutero-reorder batch). **Committed THIS session (one commit):** deutero — `scripts/core/versification.py` (+`_JDT_SEGMENTS` / `_1ES_SEGMENTS` / `_PAZ_FROM_DAT3` / new `_cross_book` for man+paz), `scripts/extract_lxx_swete.py` (_meta notes), 4 new `content/translations/lxx-swete-greek/{jdt,1es,man,paz}.py` + `_meta.yaml`, 4 `epub_working/index_split_*.html` (baked Greek), `tests/test_lxx_swete_ingest.py` (+94); cover fix — `scripts/build_edition.py` (`apply_edition_cover`) + new `tests/test_covers.py`; **spine prep** — 4 new `content/translations/sources/{douay-rheims,jps,vulgate-clementine,arabic-vandyke}/` (eBible VPL + provenance) + `docs/superpowers/specs/2026-05-23-translation-spine-arc.md`; docs (CHANGELOG/SESSION_STATE/PLAN/VISUAL_QA_CHECKLIST/this file).
>
> **Deuterocanon LXX status: COMPLETE** — all 11 books: wis · sus (Theodotion Sut) · bel (Theodotion Bet) · bar · lje · sir · tob · **jdt** (15/16 song boundary + ch16 merge) · **1es** (scattered combines; ch8 = 5 merges + 1 split) · **man** (Swete **Ode 8** — the documented Rahlfs Ode-12 was WRONG, Ode 12 here is the Nunc Dimittis; clean 15v identity) · **paz** (Theodotion Dat 3:24-90; cross-book; reordered Benedicite + 2 source-empty verses). LXX-Swete now **50 books / 26,126 verses** (+849). Every mapping content-verified vs the real Greek↔KJV; **byte-compat proven** (46 existing books byte-identical); flagship + catholic-study epubcheck **0/0/0/0**; `ebible verify` errors=0 / 24,015 paired; `lint_rules` 16/0/0; **333 LXX tests**. **⚠ GREP IS UNRELIABLE FOR GREEK** in these files (Unicode NFC mismatch) — verify Greek by Read, not Grep. **⚠ Shared split files**: a raw `git diff`/`generate_verse_popups --dry-run` shows neighbor books (e.g. est) + "61 books" as touched — that is a shared-file artifact (regen a neighbor alone → no change); trust a content-level aside-by-id compare, not the line-diff.
>
> **➤ RESUME PER `dev/PLAN_2026-05-21.md` §4.0 (ratified deadline-sequencing strategy, 2026-06-07):**
> - **MARATHON = priority calendar track — needs USER go-ahead, paced (`feedback_marathon_pacing`).** Resume the Kings/Samuel Geʽez dual-witness collation; exact held state is in the **τ.6.x.4.c marathon banner below** (≈ 1Ki5 R2 → 1Ki6 → 1Sa2 …). It is the critical path + a north-star goal (the two standalone Geʽez/Amharic Bibles), so earliest start = most chapters by the deadline.
> - **BOUNDED backlog = AUTONOMOUS ("continue" suffices), cleared in the gaps:** (1) ✅ **cover bug DONE** 2026-05-23 (`build_edition.apply_edition_cover`); (2) ✅ **deuterocanon LXX COMPLETE** (all 11 books); (3) **NEXT = the Phase-2 translation spine: Douay / JPS / Vulgate / Arabic — PREPARED 2026-05-23, ready to start.** Full runbook + verified scope in **`docs/superpowers/specs/2026-05-23-translation-spine-arc.md`**. **Data already ACQUIRED** (eBible VPL, in `content/translations/sources/{douay-rheims,jps,vulgate-clementine,arabic-vandyke}/`, git-tracked + dry-run-validated). Verified scope: **Arabic** (66-book, Ps 9=20=KJV, RTL) = near-identity → do FIRST; **JPS** (Tanakh, 23,145v≈WLC) = reuse `versification.wlc_to_kjv_map` → SECOND; **Douay + Vulgate** (Ps 9=39, Vulgate numbering) = shared `vulgate_to_kjv` adapter (content-align like the LXX `_psalm_map`) → LAST, together. Wire each via `popup_versions._BAKED_NOW`. (4) **Phase 3/4** (per-book version-selection UI + per-note curation). Translation pattern is proven: extractor → `versification.<x>_to_kjv` → driver → regen → categorize-diff → epubcheck → docs → save. **Per memory `feedback_extensive_answers`: do the COMPLETE scoped work — never omit/defer a fiddly tail to save effort (omit only for true no-guessing correctness).**
>
> **GOTCHAS:** Python = `C:\Users\bogda\AppData\Local\Python\pythoncore-3.14-64\python.exe` + `$env:PYTHONUTF8="1"`. Do NOT run the full `tests/test_scripts.py` (HANGS on build/socket smokes — use targeted `-k`). `_acquire/` is one level ABOVE the repo (gitignored; cloned sources there: morphhb, LXX-Swete-1930, byzantine-majority-text). epubcheck = Java 8 at `C:\Program Files\Java\jre1.8.0_491\bin` (off PATH); `ebible build <ed> --epubcheck` runs the full build-all + batch validate (~40 min). FASTER: build ONE edition (`build_edition.py <ed> --force --output-dir <tmp>`, ~3 min) then `epubcheck.py --jar <site-packages/epubcheck/epubcheck.jar> <epub>` with java on PATH (~1 min). **Do NOT run two epubcheck JVMs concurrently — memory contention crashes HotSpot (exit 2, drops a `replay_pid*.log`/`hs_err_pid*.log` in repo root → delete it before `git add`).** "continue" ≠ "save".
>
> **[Everything below is HISTORICAL — superseded by this banner for current state.]**

> **➤➤➤ 2026-05-22 (historical) — IDLE / READY FOR A FRESH SESSION. Nothing in flight.** NEWEST (this session): all 11 editions epubcheck-verified individually (0/0/0/0 — closes "5 canon shapes → 11 editions") + Track D ruff cleanup **1071 → 646** in verified batches; the `scripts/web.py` + `scripts/templates/*` F401 re-export regressions were caught by the full suite and repaired, then **marked clean** (`__all__` on 17 templates + PEP-484 redundant-alias on web.py); a real `check_a11y.py` missing-`collections.Counter` bug was found + fixed. Then **Tiers 1+2** cleared 135 more (mechanical UP035/SIM114/B011/B007/E731 + bug-hunt B023/B904/F841/B905 — **no real product bug found, all benign**) → ruff **1071 → 511**. **Tier 4** (config) per-file-ignored E501 in `scripts/templates/*` (inherent HTML strings) → **511 → 243**; conservative (C901/SIM105/N8xx/19 non-template-E501 kept VISIBLE as honest backlog). **Session: ruff 1071 → 243** over 4 commits; all 11 editions epubcheck-clean. Track D lint sequence COMPLETE. `ruff format` clean · `lint_rules` 16/0/0 · full `pytest tests/` **6462 passed**. Detail: `dev/CHANGELOG.md` + `dev/SESSION_STATE.md` 2026-05-22. **⏭ NEXT (post-/clear): VISUAL QA → `dev/VISUAL_QA_CHECKLIST.md`** (5 representative editions; popups/glyphs/themes/TOC/canon — the last demo item, user-only). **Matrix audit 2026-05-22 re-confirmed CLEAN**: `trace_matrix` 0 unresolved · `validate_taxonomy` 100%/67,713 · `ebible verify` errors=0/24,015 paired — no integrity fixes needed.
>
> Arcs shipped this session (newest first; full detail in `dev/CHANGELOG.md` + `dev/SESSION_STATE.md`): **(NEWEST 2026-05-22)** **ALL editions epubcheck-clean** — fixed the last 2 RSC-005 OPF errors (catholic-study/anglican-bcp): `patch_opf` emitted a BISAC `dc:subject` with `property="authority"` but no paired `property="term"`; added the `term` meta. catholic+anglican now 0/0/0/0. Every edition across all 5 canon shapes is epubcheck-clean. `tests/test_canon_splice.py` (+bisac test). **(2026-05-22)** canon-spliced editions: **footnotes cleaned** — smaller-canon editions had ~2,373 orphaned note-ref markers (RSC-012); `build_edition.filter_books_for_canon`'s `_BOOK_SEGMENT_RE` let a dropped LAST book's segment swallow the shared per-file notes-section, removing KEPT books' asides. Fixed: segments stop at the shared notes/verse-refs sections + Pass 3 drops orphaned asides. All 4 canon shapes 0 RSC-012 (reformed 2,373→0 + 0/0/0/0, jewish/orthodox 0/0/0/0, catholic 0 RSC-012). `tests/test_canon_splice.py`. FLAGGED: catholic/anglican have 2 separate RSC-005 OPF dc:subject errors (next). **(2026-05-22)** flagship EPUB **epubcheck-clean (0/0/0/0)** — Java installed → first real `epubcheck` found 50 pre-existing validity errors beyond well-formedness: 25 RSC-005 (illegal nested `<p>`) + 25 RSC-012 (broken xref links). Fixed both: `build_aside` uses `<div>` for block-content bodies (CSS mirrors `> div >`); normalized 667 xref codes (`php/jas/ezk/nam/jol`→`phi/jam/eze/nah/joe`) + `fix_xref_targets` (unresolved=0). Regenerated master HTML (0/61 malformed), rebuilt flagship → `epubcheck` EPUB-3.3 **0 fatals/errors/warnings/infos**. 9+17+19 tests + lint 16/0/0 green. **(2026-05-22)** malformed-XHTML inject bug fix — a flagship-EPUB structural validation for the [USER] e-reader check found the master HTML was **58/61 files malformed XML** (1,322+ split `<aside>`/marker tags; hidden because `ebible verify` checks pairing not well-formedness). Git-bisected to the first inject; root-caused to 3 unsafe insertion offsets in `scripts/inject.py` (aside `m.end()` mid-tag · chapter-region end at `id=` attr / run-to-EOF · verse-end fallback raw edge), all fixed to land at safe tag/`</body>` boundaries. Regenerated `epub_working/` from clean base `5ee2ad1` → **0/61 malformed**, feature-identical to `ffa20c7` (67,555 notes · 36,556 vn-links · 104,151 asides, delta 0); rebuilt flagship validates **PASS** (0 orphan markers). Guards: `tests/test_inject_wellformed.py` (7) + `audit.py` **B8** well-formed-XML gate. **(1)** end-of-session audit — all green (`ebible verify` errors=0, `trace_matrix` 0, `lint_rules` 16/0/0, `test_scripts.py` green after fixing the build-all socket flake); **(2)** symbol-system + tooltip fix — `inject.glyph_for` and `html_title_for` made data-driven so all **15** marker symbols + their tooltips render (topic-nave `◇`→`✦`, etc.); **(3)** verse-popup regeneration (below) 24% → 90.5%. The popup summary is retained below for context.
>
> The demo-critical verse-popup gap is CLOSED. `scripts/generate_verse_popups.py` (+ `tests/test_verse_popups.py`, 19 tests) regenerated verse popups across all KJV-covered books: coverage **24% → 90.5%** (9,689 → 36,556 verses; 77 books at ~100%). KJV English floor + harvest-merge-preserved Hebrew/Greek (the 11 originally-wrapped books kept their he/gr); old Gen 1:1/1:2 offset corrected; idempotent; `ebible verify` errors=0; build smoke green. Committed through `70ac7d3` (9 TDD tasks) + the finishing commit. Full story: CHANGELOG 2026-05-22 + spec/plan in `docs/superpowers/`. Deferred: acquire full he/gr/Greek-NT PD datasets to upgrade the English floor; Ge'ez popups for the 7 no-KJV books (Meqabyan I-III, 2 Enoch, Jubilees, 4 Baruch, 1 Clement).

> **➤➤➤ 2026-05-21 (LATEST) REFERENCE-CORPUS REBUILD + EXPANSION — READ FIRST; supersedes every banner below for state. COMMITTED `38b80d3`; later arcs (re-audit + PLAN_2026-05-21, Track-D cleanups, base-HTML coverage audit, repo/rules maps) committed through `3c4a173`, plus one uncommitted map/rules batch — full chronology in `dev/CHANGELOG.md`.**
>
> Corpus **52,859 → 67,715 notes**; inject **99.76%** (66,173 injected + 1,381 base-baked / 67,715). The ~161-note residual is **verse-level versification, NOT coverage** — base-HTML coverage is COMPLETE (0 chapter gaps; all 87 books / 1,702 chapters; `audit_base_html.py --coverage`). `ebible verify` **errors=0 / 24,015 paired**; valid EPUB; `trace_matrix` **0 unresolved refs**; `validate_taxonomy` 100% attributed; ruff + lint_rules clean.
>
> 1. **Nave's Topical REBUILT clean** from a user-supplied CCEL PDF → `content/sources/naves_ccel_source.txt` (NEW). The old index was OCR-noisy + under-extracted. New parser `scripts/extract_naves_ccel.py` (CCEL abbrevs, carry-forward `expand_refs`, "Jud"=Judges) → validated `naves_topical.json` (4,604 topics / 100,983 refs) → **26,335 `topic-nave` notes** (was 15,258), 0 garbled topics, 0 invalid coords, all 66 books. Fixed a 5-book code mismatch (`eze/joe/nah/jam/phi` had silently never received Nave's notes).
> 2. **Easton's Bible Dictionary INGESTED** (new dimension) from a user-supplied CCEL PDF → `content/sources/eastons_ccel_source.txt` (NEW). `scripts/extract_eastons_ccel.py` (• headwords, full book-name refs, primary-verse anchor) → **3,779 `dict-easton` notes**. New kind `dict-easton` (category `hist`) in `content/kinds.yaml` + inject glyph ⌂.
> 3. **Data-quality guard** `fetch_sources._naves_coord_in_extent` (TDD) rejects out-of-canonical-extent coords at the index boundary; earlier in this arc it + a prune removed 114 impossible-coordinate topic-nave notes.
> 4. **Optimize: `promote.batch_insert_notes`** (TDD) — one read+write per book vs per-note O(n²); reused by both ingests.
> 5. **`dev/MATRIX_MAP.md` updated** (71 kinds / 67,715 notes; ingestion pipeline; base-HTML gap marked resolved). The inject-tail spill resolver `inject.find_verse_region_b_spill` + `scripts/audit_base_html.py` shipped earlier in this arc (99.21%→99.48% pre-rebuild) — committed `d3acc4f` (spill) + uncommitted residual audit.
>
> **Source PDFs** (`Naves Topical Bible.pdf`, `eastons_bible_dictionary.pdf`) sit in the OUTER folder, OUTSIDE the git repo → not tracked. The committed `*_ccel_source.txt` extracts are the reproducible in-repo sources.
>
> **FLAGGED ITEMS FIXED (follow-up commit, after `38b80d3`):** (a) the 2 invalid-coordinate `text-witness` notes (gen 23:24, jos 16:59) pruned; a corpus-wide scan confirms **0 invalid-coordinate notes of any kind**. (b) New boundary guard `canonical_verse_counts.coord_in_canonical_extent`, enforced in `promote.promote_candidate` + `promote.batch_insert_notes`, rejects out-of-canonical-extent coordinates from ANY source/detector (prevents recurrence; the Kenyon text-witness defect was the same class as the Nave's one). (c) The `run.py` + `add_note.py` dead-injector dispatch (lost `source_archive/add_commentary.py` + `kings_session/strategy_b_inject.py`) repointed to the unified `scripts/inject.py`; `set_reader_toc.py` stale build_toc advice updated. Remaining lost-path mentions are docstring/migration-history only (cleanup.py, README.md).

> **➤➤➤ 2026-05-21 (LATER) INJECT-TAIL → 99.48% via boundary-aware spill resolver — READ FIRST; supersedes the 99.21% banner below.**
>
> **State correction:** the 99.21% inject-tail work the banner below calls "UNCOMMITTED — awaiting save" IS committed (`e05df31 build.inject-tail-99`); the working tree was clean at that commit before this session. (The banner was written pre-commit and never updated.)
>
> **This session executed `docs/superpowers/plans/2026-05-21-inject-tail-completion.md` Phases 1–3 (UNCOMMITTED — on disk, awaiting user "save"):**
> - **Phase 1 — `scripts/audit_base_html.py`** (NEW, re-runnable detector): `classify_book` flags chapters whose `ch-{bxx}` anchor's region holds zero verse spans → 5 fully-stranded chapters (1ch:3, isa:33, jer:25, mq1:30, psa:73). +2 tests `TestAuditBaseHtml`.
> - **DEVIATION FROM PLAN (evidence-driven, safer):** the plan's Phase-2 `build_verse_index_b` (full-document vn-span walk + reset-on-v1 chapter assignment) is **unsound** — split files are SHARED between books (verified: `index_split_015.html` holds all 25 of 2 Kings b11 + 1ch b12 ch1-3), so the walk would ingest the previous book's verses and mis-number chapters → silent mis-placement that `ebible verify` can't catch. Replaced with a **boundary-aware spill resolver** `inject.find_verse_region_b_spill(files, file_texts, host_fname, bxx, ch, v)`: the chapter anchors are reliable + ordered; the only defect is a chapter whose anchor ends file N while its verses open file N+1. When the conventional lookup fails, it reads only the head of the next file (start → its first `ch-{bxx}` anchor), with a **mis-placement guard** — only the chapter whose anchor is physically LAST in host_fname may spill, so a versification miss on an earlier chapter can't borrow the spilled verses. +5 tests `TestVerseRegionBSpill` + `TestInjectIrregularLayout`.
> - **Phase 3 — wired into `inject_book` Strategy-B branch** (spill fallback when `find_verse_region_b` returns None). Real re-inject result: **placement 52,553 → 52,696 / 52,973 = 99.48% (+143)**; misses 420 → 277. Fully resolved: **jer (57→0), psa (24→0)**; mostly: isa (43→4), 1ch (29→5). `ebible verify` **errors=0 / 15790 paired** (was 15766); 24 smoke tests green; ruff clean; `build_one` → valid EPUB with the new notes. NO regression (placement strictly up; every prior note still placed). `epub_working/` is currently DIRTY with the 143 new notes (regenerable: `git checkout -- epub_working/` then `inject --all-books`).
>
> **Phase 5 DONE (documentation, no content change):** `verse_absent_report()` + `--verse-absent` added to `scripts/audit_base_html.py` (+1 test); full 277-note residual enumerated + adjudicated in `dev/AUDIT_2026-05-21-inject-tail-residual.md`.
>
> **Phase 4 (aes render) — PREMISE REFUTED, NOT executed (verified against real data):** the base ALREADY renders aes (`b25`, `index_split_028.html`) as chapters **1–10** (WEB narrative ordering of the Greek Additions: `b25 c1` = Dream of Mordecai, `b25 c10` = canonical Esther 10). The 82 aes **notes** are keyed to the **KJV/Vulgate appendix scheme** (ch 10,11,13,14,15,16), so the 73 misses are notes on ch11–16 that the base renders under DIFFERENT numbers. Rendering would DUPLICATE the Additions. Correct fix = editorial WEB↔KJV verse concordance (re-key notes) — NOT a render, NOT a guess. Plan Phase 4 annotated ⛔; deferred to editorial.
>
> **REMAINING tail = 277, all NON-mechanically-addable (documented, deferred):**
> - **Editorial (do not guess):** aes 73 (WEB↔KJV Esther-Additions concordance) · ~44 out-of-range/mis-keyed Strategy-A note coords (deu 81, gen 87, num 82, …) needing source `content/notes/*.py` fixes.
> - **Larger render efforts:** 1 Enoch 37-108 base-render gap (~25 verse-absent) · Strategy-B non-spill irregular layouts (98: mq1/2/3 Tewahedo, sir, jub, rom, mat, act, jhn) — per-book multi-file index or base re-render.
> - Full breakdown + verse list: `dev/AUDIT_2026-05-21-inject-tail-residual.md`.
>
> **NET (inject-tail) — COMMITTED `d3acc4f`:** Phases 1–3 + 5; placement **99.21% → 99.48% (+143)** via the boundary-aware spill resolver; `scripts/inject.py` + `scripts/audit_base_html.py` (NEW) + 8 tests + `epub_working/` 5 files re-injected + AUDIT + plan annotation.
>
> **THEN (Nave's data-bug fix) — UNCOMMITTED, awaiting "save":** root-caused the `topic-nave` corpus-quality defect to `fetch_sources._build_naves_indices` accepting OCR-noisy out-of-range coordinates (Genesis 87, Deut 81, …) with no validation. **Fixed:** added `_naves_coord_in_extent` range-guard (TDD, +2 tests in `tests/test_scripts.py`); rebuilt `content/sources/naves_topical.json` (−118 refs / −6 garbage topics, 0 invalid remain); pruned **114** invalid `topic-nave` notes from 37 `content/notes/*.py` (ast-span removal, each file delta-verified). Corpus 52,973 → 52,859; placement **99.48% → 99.69%** (52,696 / 52,859); misses 277 → 163. Verified: 7+21+24 tests green, ruff-format + lint_rules clean. Full record: `dev/AUDIT_2026-05-21-inject-tail-residual.md` "Nave's data-bug fix". **Left for the owner:** 2 invalid `text-witness` notes (gen+jos, different provenance — flagged, not touched); the 163 residual stays editorial/large-render (aes versification, 1En 37-108 render, sir/jub/mq versification). Commit on user "save" (memory `feedback_continue_not_save`).

> **➤➤➤ 2026-05-21 INJECT-TAIL CLOSED to 99.21% — READ FIRST (supersedes the CRASH #4 + RESUME banners below for NEXT-ACTION; ALL their work is committed — c701ae7 vision-pipeline, e0ac20e 1ki5 R1, 7a1aecf full inject — working tree was CLEAN at 7a1aecf before this session).**
>
> **Shipped this session (UNCOMMITTED — awaiting user "save"):** three inject-tail fixes in `scripts/inject.py` took note placement from 44,020 / 83.1% → **52,553 / 52,973 = 99.21%** (+8,533). All TDD-pinned in `tests/test_build_smoke.py` (16 tests, was 3), ruff-clean (added `scripts/inject.py`=["E402","C901"] per-file-ignore on the build_edition.py precedent + fixed pre-existing SIM102/F841), `ebible verify` errors=0 / 15766 fully-paired, build smoke = valid EPUB.
> 1. **anchor-fallback (+7,633):** new `resolve_marker_insertion()` — when a note's word-anchor isn't in the WEB base text (KJV-vs-WEB divergence 'LORD'→'Yahweh', 'firmament'→'expanse', or a verse-merge empty slot, Gen 1:1 style), place the marker at verse-end instead of dropping the note. New `anchor_fallback` stat in the CLI/TOTAL output.
> 2. **notes-section locator (+855):** `find_notes_section_for_chapter` matched chapter ids only on `<a class="ch-anchor">`, missing the `<p class="ch-heading">` form (deu/1sa/jdg/jos/num/lev/rut ch1 etc.) → those chapters dropped notes although their section EXISTED. Broadened to match the id on ANY tag + scope by `bxx`; extracted `_aside_inside_range` helper.
> 3. **split-boundary section creation (+45, gen 27):** new `ensure_notes_section_a` synthesizes a notes-section (tagged `id="notes-{bxx}-c{ch}"` — NOT a duplicate `ch-` anchor, which collided cross-file and failed verify) when a chapter's verses spill into the next split file away from its heading. Idempotent.
>
> **(b) FINDING — the residual 420 notes (0.79%) are NOT addable content; they are base-HTML structural/versification issues needing a base re-render (a larger separate effort, deliberately NOT hacked near the deadline against the verified 99.21% state):**
> - **241 verse-unparseable (Strategy B: jer 57, isa 43, 1ch 29, psa 24, mq1-3, jub, sir, rom…):** for these books the base HTML SEPARATES scripture from the chapter-anchored notes blocks across split files — the only `id="ch-{bxx}-c{ch}"` anchor sits in the NOTES region (0 verse spans) while the chapter's verse text is in a different file with no consistent chapter anchor, so `find_chapter_region_b` lands a verse-less region. Real fix = base-HTML re-render that anchors every chapter's scripture region consistently (this would unlock all 241); NOT an inject tweak.
> - **110 chapter-absent-B:** aes 73 (Additions to Esther) + small pro/isa/1jn/rev — chapters not present/anchored in the base.
> - **69 verse-absent (Strategy A):** versification mismatches — the note's verse number doesn't exist in WEB's rendering of that chapter (gen 13, deu 8, num 7, 1en 28…).
>
> **NEXT (toward 2026-06-07):** either (1) a base-HTML re-render pass with consistent per-chapter scripture anchoring (unlocks the 241; own session, real risk), or (2) resume the PAUSED manuscript marathon (1ki5 R2 held; pointer below). FLAG: `dev/SESSION_STATE.md` is bloated to 874KB (bootstrap-triad design target ~900 lines) — trim is a separate hygiene item.

> **➤➤➤ 2026-05-21 EPUB-BUILDER REVIVED — base scripture HTML recovered + committed; first valid build since the 05-08 re-init. (Superseded for NEXT-ACTION by the inject-tail banner above; retained for the base-HTML recovery story.)**
>
> The build was broken because the base HTML (`epub_working/index_split_*.html` — the World English Bible text that notes inject into) was never committed after the 2026-05-08 repo re-init, so it was lost. RECOVERED from the v28a-50 (2026-05-07) snapshot + COMMITTED so it can't be lost again. Verified: `ebible build ethiopian-tewahedo` → a 6.2 MB structurally-valid EPUB; `inject` lands ~83% of the 52,973 notes. Full story: `dev/CHANGELOG.md` (2026-05-21) + `dev/AUDIT_2026-05-21-smoother-running.md` + `dev/MATRIX_MAP.md`.
>
> **NEXT (audit-prioritized, toward the 2026-06-07 deadline):** ~~(1) de-commercialize the tooling~~ **✅ DONE 2026-05-21** · ~~(4) add an `ebible build` smoke test~~ **✅ DONE 2026-05-21** · (2) fix the 290 unmatched note-refs (build_edition filtering); (3) close the inject tail (8,236 anchor-miss + 808 no-section — OT auto-note anchoring + chapters absent from the 05-07 base, e.g. 1 Enoch 37-108); (5) install Java for epubcheck cert. **The manuscript-marathon track below is PAUSED** — the demo build is the priority for the deadline.
>
> **➤ 2026-05-21 audit P1 COMPLETE (uncommitted — awaiting user "save"):** de-commercialized the LIVE tooling path (the Ω.0-quarantined modules were bannered dead but `ship-check`/`doctor`/`status` still invoked them): removed `ship-check.py` step 6 (`build_onix`) + the `--skip-onix` flag; renamed the commercial `--retail` gate → `--epubcheck` (epubcheck is still valid post-pivot — opt-in, needs Java); stripped `cmd_status`/`cmd_doctor` ONIX-TODO blocks + the "Submit to Apple Books/KDP/Kobo" advice (doctor now points at `ebible build` + the free CC0 EPUB). DEVNULL-guarded the `run_script`/`git`/`run()`/`cmd_test` subprocess helpers (Windows WinError-6, memory `feedback_w_w1_subprocess_devnull`). `.gitignore`'d `epub_working/onix/` (build-output side-effect; base scripture HTML stays tracked) + fixed the pre-pivot "ONIX metadata tracked" header. New tests: `tests/test_build_smoke.py` (base-HTML present-pins + `build_one()`→valid-EPUB, no Java) + 5 `TestLiveToolingDecommercialized` pins in `test_omega0_free_public_pivot.py`. Touched: `.gitignore`, `scripts/ship-check.py`, `scripts/ebible.py`, `tests/test_omega0_free_public_pivot.py`, `tests/test_build_smoke.py`. Verified: 5+4+32 targeted tests green; CLI smokes (`ship-check --help`, `ebible status`, `ebible help ship`) clean. **Remaining:** ~~(2) unmatched refs~~ **✅ DONE 2026-05-21** (commit `a54d793`: `fix_xref_targets --apply` repaired 6043 broken cross-file `#vnote-` refs in 59 `content/notes/*.py` — 1059 exact-verse + ~4984 verified Strategy-B chapter-fallbacks; rendered HTML needed 0 changes; 190 residual = absent Ezekiel targets = item 3); (3) inject tail (8224 anchor-not-found + 729 no-section + the 190 absent xref targets — base-HTML per-verse-popover/anchor coverage; OT auto-note anchoring + chapters absent from the 05-07 base e.g. 1 Enoch 37-108); ~~stale-gate tangle~~ **✅ DONE 2026-05-21** — root-caused: the 4 `verify.py` ERRORs came from `audit.py` (root; `verify.py` wraps it) which still imported the lost `kings_session/book_meta.py` for book metadata + listed the lost scripts in its `SCRIPTS` inventory. `books.yaml` (header: "generated from `kings_session/book_meta.py`") is the migrated successor, so repointed `audit._load_book_meta()` → `config.books_by_code()` and dropped the dead `SCRIPTS` entries → **verify errors 4→0** (still 1365/1365 paired, no flood of real B-errors — the recovered base HTML has the structural anchors). Also de-staled `ship-check`: removed the dead `1354/1354` hardcode (verify exits 0 now) + made step-5 fail only on *resolvable-pending* xrefs (the 190 absent-target are a known base-HTML gap → warn, not block). Pin: `tests/test_audit_modernization.py`. **NOTE — still latent (NOT in the integrity path):** `run.py:60` + `add_note.py:227` dispatch to the lost `source_archive/add_commentary.py` / `kings_session/strategy_b_inject.py` injectors; superseded by the unified `inject.py` + `new_note.py` (the `ebible` CLI surface) but would fail if invoked. Cleanest fix = retire or repoint to `inject.py` (needs their output-summary parsers updated — `inject.py` emits a different format); deferred. **Remaining after this:** (3) inject tail (ship-check step 4 honestly fails: 42,639 source notes not yet injected into the recovered base; + 8224 anchor-miss + 729 no-section); then P2 (delete the quarantined commercial modules + their tests/routes — destructive, confirm first).

> **➤➤➤ 2026-05-21 CRASH #4 RECOVERY [RESOLVED — the manuscript-vision pipeline IS committed (c701ae7); this banner's "verify the pipeline + commit" NEXT-ACTION is DONE. Retained below for the OOM agent-image-buffer root-cause analysis, which still governs the script-not-agent marathon rule.]**
>
> **Disk truth (verified this session, read-only):** HEAD = `e0ac20e` (1ki5 R1 fix-round) — committed & SAFE. All 17 marathon review files intact on disk. Clean `main`, no stash, zero committed-work loss.
>
> **The un-bannered work is the FIX for the crash — not its cause.** A session AFTER the 20:55 recovery commit (file mtimes 21:28–22:23, 2026-05-20) built a standalone manuscript-vision pipeline and never committed/bannered it. Uncommitted delta: NEW `scripts/core/{parallel,work_cache,manuscript_vision,manuscript_index}.py`, `scripts/build_manuscript_index.py`, `scripts/run_manuscript_{transcribe,review}_at_scale.py`, 7 new `tests/test_*`; MODIFIED `scripts/core/sources.py` (→116KB), `scripts/run_ai_{notes,xrefs}_at_scale.py` (adopt `parallel_map`). **All 17 byte-compile clean.**
>
> **Corrected diagnosis (read the code, not guessed):** the recurring OOM crash class is **agent-based** manuscript vision — Task/Agent subagents read LANCZOS-*upscaled* PNG crops, buffering 30–60 MB of image bytes in the parent harness for the agent's full ~20-min life; stack 2–3 heavy ones and the Rust allocator panics (`memory allocation of N bytes failed` — matches crashes #1–#4). `scripts/core/manuscript_vision.py` is the documented root-cause fix: run vision from a **standalone script** so image bytes go straight to the API and never enter the harness buffer, with hard caps — **never upscale**, longest edge ≤1568px (`MAX_IMAGE_EDGE`), ≤8 folios decoded (`lru_cache`). The runners are **single-chapter / one API call / ~2–4 capped images** — bounded and safe to run. The `parallel_map` adoption in AI-notes/xrefs is a separate I/O optimization (result-buffering already existed in the serial path; only adds ≤8 concurrent API calls).
>
> **NEXT-ACTION:** (1) verify the pipeline's tests ONE FILE AT A TIME (no broad sweep / no parallel subprocesses — memory `local_test_memory_pressure`) + `--dry-run` the runners (zero API/image); (2) commit the pipeline as a WIP checkpoint ONLY on user go-ahead; (3) the manuscript marathon then resumes via the **SCRIPT path** (`run_manuscript_transcribe/review_at_scale.py`), NOT the agent path — that retires the crash class. Until committed: no heavy AGENT dispatch. The RESUME banner's "dispatch R2 alone in BG" is HELD — R2 should re-run as a SCRIPT, not an agent.

> **➤➤➤ 2026-05-21 RESUME-AFTER-ACCIDENTAL-CLOSE (user "superpowers on and continue. think i closed you by mistake") — 1ki5 R1 fix-round LANDED ON DISK in the prior session (witness mtime 20:43, ~10s after `_r1_fix_apply.py` helper). Validated this session: schema PASS (13 verses), NARRATIVE-class screen `[]` PASS, 103 manuscript tests green. Committing the R1 work + the prior session's trim/archive in one atomic crash-recovery commit. Then dispatching R2 spec-compliance review alone in BG per the workload-tiered cap (HEAVY = MAX 1 concurrent).**
>
> Landed on disk (now being committed):
> - **τ.6.x.4.c 1ki5 R1 fix-round** — 5 CRITICAL + 4 MAJOR applied per `dev/marathon_reviews/1ki5/REVIEW_2026-05-20-1ki5-GG-R1.md`. Witness `content/manuscript/kings/calibration/1ki5_witnessGG.json` re-written via `write_witness()` (canonical schema; 13 verses; +112 / -31 lines incl. uncertain-marker rationale per fix). Helper script `dev/marathon_reviews/1ki5/_r1_fix_apply.py` retained as the audit-trail of which fix maps to which review item.
> - **Phase-2 re-verification helpers** — `dev/marathon_reviews/1ki5/_p2_crop.py` + `_p2_overview.py` (LANCZOS crop generators for the AMBIGUOUS-PARCHMENT loci flagged by R1; output to `%TEMP%\1ki5_r1_p2\`, never repo-internal).
> - **IN_FLIGHT.md trim** — 386KB → ~11KB; superseded banners archived to `dev/IN_FLIGHT_archive_2026-05-20.md` (6,681 lines of historical context preserved for git-blame).
>
> Next (dispatched after this commit; HEAVY alone in BG):
> - **τ.6.x.4.c 1ki5 R2 spec-compliance review** — fresh adversarial reviewer reads the R1-applied witness, re-checks every CRITICAL + MAJOR vs parchment evidence, produces `dev/marathon_reviews/1ki5/REVIEW_2026-05-20-1ki5-GG-R2.md` with verdict (APPROVED / NEEDS_FIX / BLOCKER). MUST also Phase-2 re-verify the 4 `AMBIGUOUS-PARCHMENT` loci the R1 fix-round self-flagged (M-1 ወፈነወሙ→ወፈነሙ, M-2 ንጉሠ→ንጉሥ, M-3 ወከበ→ወሶበ, M-4 ወትህብሊ→ወትህብ) — without Phase-2 evidence, these MUST be downgraded to BLOCKER. **HEAVY class.** No other agents until this drains and commits.
>
> Held-back queue (sequential per cap; NOT dispatched until R2 lands + user check-in per `feedback_marathon_pacing`):
> 1. **τ.6.x.4.c 1sa2 fresh C-2 re-pass** — full blind re-transcription with corrected methodology (≥6× LANCZOS, post-rubric `❈`, multi-illegible honesty, NEW `ይ`/`ግ`/`ን` + `ሰ`/`ል` + `ደ`/`ድ` families, v29 fabrication watchout); overwrite `content/manuscript/samuel/calibration/1sa2_witnessGG.json`.
> 2. **τ.6.x.4.c 1ki6 C-1+C-2 blind GG** — next Kings chapter advance; NEW `1ki6_witnessGG.json` from f031r 1Ki6:1 boundary.
>
> **[Resumes / supersedes — the 23:30 3RD-CRASH hand-off banner below documents the cap rewrite; that rule remains in force. The PRIOR resume banner (which dispatched the now-LANDED R1 fix-round) is superseded by this LANDED note.]**

> **➤➤➤ 2026-05-20 ~23:30 EST — 3RD CRASH (same day, parallel-agent OOM AT THE NEW 3-CAP) + RULE REWRITTEN + NO RE-DISPATCH THIS SESSION (read FIRST; supersedes the 22:30 2ND-CRASH banner below for state, but its commit 7a20eca remains valid).**
>
> Crash signature: `memory allocation of 30982500 bytes failed` (Rust panic in the harness process). At-time-of-crash state: **3 in-flight BG agents** (the "safe" new cap from the 22:30 banner), each running 19–20 min, downstream token counts **132.2k + 153.0k + 126.1k = ~411k tokens of streamed agent output buffered in parent** — same buffer pressure as crash #2's 7-agent wave, with less than half the agent count. **All 3 agents died with zero disk delta** (no commits landed; witness files on disk unchanged from their pre-dispatch state).
>
> **Root cause (corrected):** The "3-agent cap" rule from the 22:30 banner mistook agent count for the OOM driver. **Real driver is cumulative buffered output × time.** Heavy manuscript-vision agents (CUDL+LANCZOS+verse-by-verse blind transcription, R1 fix-rounds applying 5+ critical defects) routinely emit 100–150k tokens per agent. Three of those = the same parent-buffer pressure as seven small NT-pre-pass / BT-smoke / OT-Ge'ez-gap-fill agents from the 22:51 wave (which landed 16 ships cleanly).
>
> **NEW RULE (memory `feedback_concurrent_agent_cap` rewritten):** workload-tiered, not count-flat:
> - **Heavy** (>100k tokens/agent: manuscript-vision, R1/R2 fix-rounds with all-criticals, deep-evidence multi-chapter sweeps, full-doc constitutional reviews) → **MAX 1 concurrent**, drain before next.
> - **Medium** (30–100k: standard chapter extraction, Patrologia book-ship, multi-chapter NT pre-pass) → MAX 2 concurrent.
> - **Light** (<30k: single-chapter NT epistles, single-chapter Ge'ez gap-fills, BT smoke, hygiene sweeps) → up to 4 concurrent.
> - Mixing heavy+light still counts the heavy's full budget; buffer is one bucket.
> - Drain before re-dispatching; never queue on top of in-flight.
> - Between batches: commit landed work + `/clear` or fresh session to release accumulated parent context.
>
> **Secondary mitigation surfaced by this crash:** IN_FLIGHT.md is currently **386KB**. The crash-recovery loop reads it in full at every fresh session, eating ~50–80k tokens of parent budget BEFORE any agent dispatch. Heavy waves on top of that baseline are unsafe by design. **Action item for the next clean session: trim IN_FLIGHT.md aggressively** — keep the latest banner + the active task section, archive all superseded banners (the 22:30 / 22:00 / 16:45 / 2026-05-19 prior-resume blocks) to `dev/IN_FLIGHT_archive_2026-05-20.md`. Target <50KB.
>
> **This session takes NO action beyond:**
> 1. Writing this banner (you are reading it now)
> 2. Rewriting memory `feedback_concurrent_agent_cap.md` and updating MEMORY.md index
> 3. Committing both (one atomic commit)
> 4. Handing off to user for fresh-session restart
>
> **The 3 agents that died (no disk delta — re-dispatch unchanged in next session, but ONE AT A TIME per the new rule):**
> - τ.6.x.4.c **1ki5 R1 fix-round** — apply 5 CRITICAL + 4 MAJOR per `dev/marathon_reviews/1ki5/REVIEW_2026-05-20-1ki5-GG-R1.md`; overwrite `content/manuscript/kings/calibration/1ki5_witnessGG.json`. **HEAVY class.** Run first, alone.
> - τ.6.x.4.c **1sa2 fresh C-2 re-pass** — full blind re-transcription with ≥6× LANCZOS + corrected methodology + v29 fabrication watchout; overwrite `content/manuscript/samuel/calibration/1sa2_witnessGG.json`. **HEAVY class.** Run second, alone, after 1ki5 lands.
> - τ.6.x.4.c **1ki6 C-1+C-2 blind GG** — next Kings chapter advance; new `1ki6_witnessGG.json` from f031r 1Ki6:1 boundary. **HEAVY class.** Run third, alone, after 1sa2 lands.
>
> **Resume pointer (next session):** read this banner → read updated `feedback_concurrent_agent_cap` → start a fresh `/clear`-ed session → dispatch 1ki5 ALONE in background, await completion notification, commit witness, then 1sa2 ALONE, then 1ki6 ALONE. Total wall-clock per chapter ~20 min × 3 sequential = ~60 min, but zero crash risk vs ~0 min of "parallel" that loses 100% of the work.

> **[ARCHIVE NOTE 2026-05-20 23:30 EST]:** Prior crash banners (22:30 2ND-CRASH, 22:00 RECOVERY, 16:45 EOD-hygiene) and ALL historical/closed arcs were moved to `dev/IN_FLIGHT_archive_2026-05-20.md` during this trim (386KB → ~15KB). Read this file first; consult the archive only when chasing historical context.
