# Mint-10 fixes plan — round-3 re-audit (recovered + continued)

**Status:** OPEN — 49 verified survivors (4 high / 13 medium / 22 low / 10 info) from mint-10 round 3. Source: killed run `wf_ba367edc-a4a` (find phase recovered) + continuation `wf_4a2d0815-8c9` (verify + 4 missing finders + synth). 62 deduped -> 49 survived / 13 refuted. **mint-10 is the LAST audit round (no mint-11 until project end).**

**Implementation status:** Phase-1 *stale-test green-up* SHIPPED 2026-06-01 (11 test files: DELETE/MULTIPART/PUT route-counts 8→6 / 4→3 / 12→10; `test_omega0` un-removes the live `api/distribution.py`; `test_lint_guardrails` state-aware; `url_override` allowlist host; 7 parallel-bible `SCOPE`→`dev/archive/` paths — 579 tests green). **Phase-1 production leftovers + Phase 2 + Phase 3 SHIPPED 2026-06-01** (byte-stability gate PASSED 226s; +5 regression tests; lint 27✓/1⚠/0✗; mypy clean): P1 = `audit_log` month-rollover chain fix (+`_prior_month_file`, regression test) + `inject._aside_existing_re` whitelist; P2 = `_PIPELINE_SCRIPTS` += popup_versions/traditions, `_referenced_translations` alias-resolve, topical-JSON cache keys, `is_output_current` inputs, `EBIBLE_VPL_TO_PROJECT` lint; P3 = `_iter_note_ref_attribution_years` `<9`→`<8`, `_append_cloned_edition` kind-gate fields, promote drop-warn. **Phases 4 + 5 + 6 SHIPPED 2026-06-01** (no build-path change → 9 KJV byte-stable by construction; ~1,200 tests green; lint 27✓/1⚠/0✗; mypy clean): P4 = 8 at-scale drivers → one `at_scale_base.append_candidates` (status-preserving dedup; kind-replace purge removed) + `_normalize_book_code` on `--books` + naves/torrey coord guard + 5 stale candidate files deleted; P5 = console XSS escaping (7+ sites) + WLC ingest fail-closed on raw `<>&` + exports `version` validation; P6 = cyril ceiling→floor + all-5-rival plurality + `corpus_index` `_CONN_LOCK` + web_helpers `bxx` + batch_promote mark-promoted + `_canons_index` lru_cache + conftest monkeypatch drop + REPO_MAP 66 + INDEX mint-9→Shipped. **REMAINING (fresh session) — 4 opt-tier items only:** **#11** ρ.1 `filter_html` two-stage scan (BUILD-PATH → byte gate) · **#12** `_resolve_popup_languages` per-file hoist (BUILD-PATH → byte gate) · **#13** `render_coverage` EN-lane (+ test shape) · **#6** `candidates_written` stat (superseded by P4's `append_candidates` return contract — re-confirm or drop). After these, **mint-10 is the LAST audit round — STOP (no mint-11 until project END).**

---
I'll synthesize the plan directly from the verified findings provided. The data is complete and authoritative; no file reads are needed.

## Executive summary

Round 3 of the deep audit deduped 62 findings into 49 verified survivors (13 refuted), spanning {high: 4, medium: 13, low: 22, info: 10} by severity and split 34 bug/correctness/security/debt/test/doc to 15 optimization. The codebase is healthy: every one of the 4 high-severity findings is either latent (gated behind an unset `time_filter_ceiling`) or scoped to a non-KJV path (cloned editions, topical-index back-matter), so no currently-shipping KJV EPUB is wrong on bytes today. The most serious correctness issue is the `len(tup) < 9` guard in `_iter_note_ref_attribution_years` (build_edition.py:341), which silently defeats `time_filter_ceiling` for the entire legacy 8-field corpus the moment any edition activates it; the most operationally dangerous is `_append_cloned_edition` (api/editions.py) shipping 0 notes because it omits the kind-gate fields. A cluster of build-cache key omissions (`popup_versions.py`, `traditions.py`, the two topical JSONs, legacy popup-language aliases) can serve stale EPUBs after a data/registry edit. The remaining body is dominated by stale tests (paths/route-counts that drifted past their phase), incomplete-pattern fixes (the `★BUGCLUSTER` normalize + write_queue dedup missed on 4 of 8 at-scale drivers), and low-risk XSS hardening on the local console.

## Phased fixes

Phases are ordered safest/most-foundational first: stale-test green-up and lint-guard widening (no behavior change) → cache-key completeness (additive, forces correct rebuilds) → silent-data-loss + clone correctness → ingest defense-in-depth + escaping → docs/cleanup. None of the bug fixes touch the marathon core. Build-path findings carry a byte-stability proof obligation (regen + `git diff` over the 9 KJV editions, or `tests/test_byte_stability_gate.py`).

### Phase 1 — Stale-test green-up (test-only; no production code; byte-safe)

- [ ] **[medium] Stale `_DELETE_ROUTES` count expects future routes** — `tests/test_web_routetable.py:1068`. Change `== 8` to `== 6`; remove the `/distribution/`+`/license/` pattern asserts (lines 1071-1072); keep the `/sources/cache/` assert; update the comment block (1063-1065) to "6 entries, A.8 added the 6th." **Touches build path:** no.
- [ ] **[medium] Stale test references deleted commercial module that is actually live** — `tests/test_omega0_free_public_pivot.py:270-276`. Remove `"scripts/api/distribution.py"` from the `REMOVED` tuple (it is the live mint-6 archive.org rollup module; do not move it to `KEPT`). **Touches build path:** no.
- [ ] **[medium] Audit-chain verify fails on month-rollover (pre-ξ.17 records)** — the bug is in `scripts/core/audit_log.py:104-123` (`_last_line_hash`), not the test. When the target monthly file does not exist, fall back to the most-recent prior `*.ndjson` sibling's last-line sha256 before `_CHAIN_GENESIS` (full body in the finding). Additive: `verify_chain` unchanged; only changes the `prev_hash` written into a new month's first line. **Add guard:** keep the existing `test_security_xi_late.py:1176` case green. **Touches build path:** no.
- [ ] **[low] Stale `_MULTIPART_ROUTES` count** — `tests/test_web_routetable.py:1195`. `== 4` → `== 3`; drop the `sales/import` docstring line (1188); fix comment (1192) to "sales/import removed in mint-4 decommercialize; floor is 3." **Touches build path:** no.
- [ ] **[low] Stale `_PUT_ROUTES` count** — `tests/test_web_routetable.py:1410`. `== 12` → `== 10`; update comment (1406-1407) to "6 from A.5 + 3 from A.10 + 1 from ε.7 = 10; ε.6/ξ.26 not yet implemented." **Touches build path:** no.
- [ ] **[low] 7 scope-doc tests point at un-archived path** — change `REPO/"dev"/"SCOPE_2026-05-14-parallel-bible.md"` → `REPO/"dev"/"archive"/"SCOPE_2026-05-14-parallel-bible.md"` in all 7 sites: `test_parallel_bible_tau6x0b.py:72`, `tau6x0.py:88`, `tau6x0c.py:313`, `tau6x1.py:688`, `tau6x2d.py:64`, `pi1.py:652`, `pi2prep.py:51`. Content already satisfies every assertion; only the path constant is wrong. **Touches build path:** no.
- [ ] **[low] `url_override` test uses non-allowlisted host (test bug, not impl)** — `tests/test_scripts.py:8063`. Change `"https://my-mirror/example.json"` → `"https://ebible.org/example.json"` (any allowlist host). Do NOT weaken the SSRF guard in `scripts/api/sources.py`. **Touches build path:** no.
- [ ] **[low] `TestStrayArtifacts.test_flags_junk` not state-aware** — `tests/test_lint_guardrails.py:231-239`. Replace `assert r["status"] in {"warn","fail"}` with `assert r["status"] == ("fail" if lint_rules._ENFORCE_STRAY_ARTIFACTS else "warn")` (matches siblings at 148/178/216). **Touches build path:** no.
- [ ] **[low] Unwhitelisted `lru_cache` false-positive** — `scripts/inject.py:610` (`_aside_existing_re`). Add a whitelist entry in `scripts/.cache_audit_whitelist.py` ("Pure-function compiled-regex caches" block); pure function, no `cache_clear()` needed. Resolves the `audit_caches.py` `no_clear_path` false-positive. **Touches build path:** the cache itself is on the inject path but is unchanged — fix is whitelist-only, byte-stable.

### Phase 2 — Lint-guard / cache-key completeness (additive; commit-time enforcement preferred; forces-correct-rebuild, not output-changing)

- [ ] **[high→med] Build cache key omits `popup_versions.py`** — `scripts/core/build_cache.py:62-70`. Add `"core/popup_versions.py"` to `_PIPELINE_SCRIPTS`. Drives `POPUP_LANGUAGES`/language-stripping across all 9 popup editions. **Add guard:** extend the `_PIPELINE_SCRIPTS`-completeness check (or add one) so any new build-path import must be registered. **Touches build path:** yes — additive; invalidates cache only when the file changes; run byte-stability gate.
- [ ] **[med] Build cache key omits `traditions.py`** — `scripts/core/build_cache.py:62-70`. Add `"core/traditions.py"`; `CANONICAL_TRADITIONS` labels are injected for `catholic-study`. Update the comment at lines 55-61 to mention the traditions build-path edge. **Touches build path:** yes — additive; byte-stability gate.
- [ ] **[med→low blast-radius] Build cache key omits topical-index JSONs** — `scripts/core/build_cache.py:277`. After the `source_dates.yaml` hash, add `naves_topical.json` and `torrey_topical.json` hashes (full snippet in finding). Re-running an extractor must bust the cache. **Touches build path:** yes (back-matter page only) — additive; byte-stability gate.
- [ ] **[low] Cache key resolves legacy popup-language aliases to missing dirs** — `scripts/core/build_cache.py:111-136` (`_referenced_translations`). Lazily `from scripts.core import popup_versions as _pv` and map each `popup_languages_default`/`popup_languages_per_book` id through `_pv.resolve_version_id(...) or id` before `refs.add`. Registry IDs self-resolve → 9 editions byte-stable. Update the "gracefully degrades" comment. **Touches build path:** yes — additive; byte-stability gate.
- [ ] **[low] `is_output_current` mtime-cache misses several build inputs** — `scripts/build_edition.py:1953-1980`. Append `matter_pages.py`, `epub_utils.py`, `content/kinds.yaml`, `content/categories.yaml`, `content/books.yaml`, and `content/themes/*.css` to the watched `sources` list to reach parity with `_PIPELINE_SCRIPTS`. Only bites on cold content-cache. **Touches build path:** yes — additive; byte-stability gate.
- [ ] **[low] `EBIBLE_VPL_TO_PROJECT` book-code map absent from canonical-codes lint** — `scripts/lint_rules.py:1995-2012`. After the `OSIS_BOOK_TO_CODE` entry add `("scripts.extract_translation", "EBIBLE_VPL_TO_PROJECT")` with the mint-10 comment. **This is a commit-time `lint_rules` check — preferred over a pytest-only guard** for an invariant (canonical book codes) that recurs every translation ingest. **Touches build path:** no (guard-only).

### Phase 3 — Silent-data-loss + clone correctness (behavior-changing; high priority; latent-but-contract-violating)

- [ ] **[high] `_iter_note_ref_attribution_years` skips all 8-field legacy notes → `time_filter_ceiling` silently defeated** — `scripts/build_edition.py:341,346`. Change guard to `len(tup) < 8`; change attribution read to `attribution = (tup[8] if len(tup) > 8 else "") or ""` (or call `note_attribution(tup)`). Mirrors `_iter_note_ref_traditions:144` and `NoteSpec.from_tuple`. This is the SOLE `< 9` iteration guard — confirmed single site. **Add guard:** a pytest that builds with a positive `time_filter_ceiling` and an 8-field note and asserts the note's ref-id lands in `disabled_html_ref_ids`. **Touches build path:** yes — but unreachable for the 9 KJV editions (none set a ceiling) → byte-stable; still run the gate to prove it.
- [ ] **[high] `_append_cloned_edition` ships 0 notes (omits the kind-gate fields)** — `scripts/api/editions.py:120-194`. Add `max_phase` (and `enable_ai_notes`) to `scalar_fields`, and conditionally append `enabled_categories`, `enabled_kinds`, `disabled_kinds` to `list_fields` using the existing `popup_languages_default`/`traditions_default` serialization pattern (full snippet in finding). **Add guard:** a pytest that clones a populated edition and asserts `enabled_kind_codes(clone)` is non-empty (== source). Schema is purely additive (new fields only on newly-cloned editions). **Touches build path:** no KJV-edition change; the clone path produces a correct edition. Byte-stable for the 9.
- [ ] **[med] `batch_insert_notes` silently drops out-of-extent notes** — `scripts/promote.py:354-358`. Add a `dropped` counter, increment at both silent `continue`s (canonical-extent guard + base-HTML-extent guard), and `warnings.warn(...)` once after the loop when non-zero (matches the existing missing-ch/v `warnings.warn` at 346-350). No caller/schema change. Lower-priority follow-up: DEBUG-log the `promote_candidate` single-note silent return (441-450). **Touches build path:** no.

### Phase 4 — Incomplete-pattern (★BUGCLUSTER) sweeps + ingest defense-in-depth (fix the whole class)

- [ ] **[med] `_normalize_book_code` missing from `--books` path in 4 of 8 at-scale drivers** — sweep all 4 unguarded sites: `run_ai_xrefs_at_scale.py:269`, `run_ai_notes_at_scale.py:272`, `run_hebrew_at_scale.py:138-143`, `run_greek_at_scale.py:135-140`. For the `resolve_books` drivers, add `books = [sources._normalize_book_code(b) for b in books]`; for the `.split(",")` drivers, normalize each token (full per-file snippet in finding). The 4 already-guarded drivers (xref/naves/torrey/ethiopian) are correct. **Add guard:** a `lint_rules` check (commit-time, preferred — recurs every at-scale driver added) asserting every `run_*_at_scale.py` `main()` normalizes explicit `--books`. **Touches build path:** no.
- [ ] **[med] `write_queue` copy-pasted across 4 accumulator drivers; `at_scale_base` docstring stale** — extract `at_scale_base.append_candidates(out_path, book, chapter, candidates)` using the narrow `except (json.JSONDecodeError, OSError)` and the triple-key `seen` dedup; adopt it in `run_xref/naves/torrey/ethiopian_at_scale.py`. Kenyon (full re-index) and the kind-replace drivers keep local impls. Update `at_scale_base.py:8` docstring to current reality. **Add guard:** a pytest that the 4 accumulator drivers call the shared helper. **Touches build path:** no.
- [ ] **[med] 4 kind-replace drivers purge-and-replace own-kind candidates, resetting `promoted`→`pending`** — `run_hebrew_at_scale.py:46-53`, `run_greek_at_scale.py:45-52`, `run_ai_xrefs_at_scale.py:104-111`, `run_ai_notes_at_scale.py:102-109`. Replace the kind-filter purge with the same `seen`-set dedup (preserves prior status), payload `= existing + new_dicts` (full snippet in finding). **Note:** this competes with the Phase-4 `write_queue` extraction; coordinate so the kind-replace drivers adopt status-preserving dedup rather than re-introducing the purge. **Touches build path:** no.
- [ ] **[med] `run_naves`/`run_torrey_at_scale` lack `coord_in_canonical_extent` guard** — `run_naves_at_scale.py:101-121` + byte-identical `run_torrey_at_scale.py:120-141`. Add `from scripts.core.canonical_verse_counts import coord_in_canonical_extent` (top of file) and `if not coord_in_canonical_extent(book, chapter, verse): continue` in each inner verse loop. Both sites, same change. **Cleanup:** delete the 5 stale candidate files below. **Touches build path:** no.
- [ ] **[low] Delete 5 stale impossible-chapter candidate files** — `content/candidates/{1ch_ch_038,1co_ch_035,1jn_ch_006,1sa_ch_034,1ti_ch_008}.json` (pending, NaveTopicalDetector, coords exceed canonical chapter count; promote guard already blocks them). Optional: extend `check_content.py` candidates check to validate `chapter <= canonical_chapters(book)` to prevent re-accumulation. **Touches build path:** no.

### Phase 5 — Console XSS hardening (local single-user app; defense-in-depth; template-only)

- [ ] **[low] Server error strings reflected into `innerHTML` unescaped** — sweep all 7 sites: `customize.py:1364,1441,1791`, `export.py:502`, `sources.py:485`, `matrix_app.js:890,1535`. Wrap each interpolated `error`/`msg` in `escapeAttr(...)`/`escapeHTML(...)` (verify/add the one-liner in `export.py`'s script block). **Touches build path:** no.
- [ ] **[low] Corpus-sourced `suffix`/`anchor`/`kind`/`category_*` interpolated unescaped** — `audit.py:234` (escape `n.suffix` via the already-defined `escapeHTML` at 247) and `sources.py:553-555` (escape `n.suffix`, `n.anchor`, `n.category_label` via `escapeAttr@586`, `n.kind`, `n.category_symbol`). **Touches build path:** no.
- [ ] **[low] WLC trusted-HTML ingest missing the `<>&` guard its 2 siblings have** — `scripts/extract_wlc_morphhb.py:216-235`. Add the guard to the RAW token text inside `verse_to_em_html` (before the `<em>` wrap) and to `seg_text`, raising `ValueError` on `<`,`>`,`&` — matching `extract_lxx_swete.py:164-170` and `extract_byzantine_nt.py:87-93`. Do NOT guard the assembled `<em>` string (it always contains `<`/`>`). All 3 `_TRUSTED_HTML` sites checked; only WLC lacked it. **Touches build path:** no.
- [ ] **[info] Unvalidated `version` reaches subprocess arg + glob pattern** — `scripts/api/exports.py:120-165,225`. After the `edition_id` check, validate via the existing `_validate_version` (`_VERSION_NAME_RE = ^[a-z0-9][a-z0-9._-]{0,40}$`); add the same guard to `api_build_all_editions` before the zip filename (line ~378). List-form subprocess already blocks shell injection; this fixes glob-metachar file mis-discovery. **Touches build path:** no.

### Phase 6 — Test-robustness, concurrency, docs (low/info; non-output-changing)

- [ ] **[med] Cyril ≤700 ceiling has only 32 headroom; brittle vs authorized expansion** — `tests/test_ethiopian_gamma4.py:1171-1183`. Remove the ceiling assertion (RULES §8.1 prefers count-floor milestone pins, not ceilings); rely on the durable plurality-leader guard at 7014. If a ceiling is insisted on, raise to ~1500 and re-anchor the comment. **Touches build path:** no.
- [ ] **[med] γ.4.8.E Cyril plurality test checks only 2 of 5 rivals (★incomplete-fix site)** — `tests/test_ethiopian_gamma4.py:8174-8197`. Upgrade `test_cyril_remains_plurality_leader_at_meqabyan_arc_close` to the `Counter`+`_voice()` all-5-rival pattern from the γ.4.9.D upgrade (7014); optionally add the ≥2× ratio guard from γ.4.8.F (8536-8538). **Touches build path:** no.
- [ ] **[low] `corpus_index.connection()` unguarded check-then-set leaks a `sqlite3.Connection`** — `scripts/core/corpus_index.py:659-680`. Add module-level `_CONN_LOCK = threading.Lock()` (+ `import threading`) by `_CACHED_CONN` (642-646); wrap the path-mismatch close block AND the None-check-and-create together in `with _CONN_LOCK:`. Use a plain `threading.Lock`, NOT `_acquire_rebuild_lock`. Fix the stale comment at 670-672. (This and the opt-vision duplicate are one fix.) **Touches build path:** no.
- [ ] **[opt-med] `html_ref_id_from_note_id` missing the Strategy-B `bxx` fallback** — `scripts/web_helpers.py:262`. Change `prefix = book.get("id_prefix")` → `prefix = book.get("id_prefix") or book.get("bxx")` to match `build_edition.py:139/336/2666` and `inject.py:691`. Without it the web editor cannot navigate to notes in 2ch/ezr/neh etc. **Touches build path:** no (editor path).
- [ ] **[opt-low] `batch_promote_xrefs.promote_by_book` never marks promoted candidates** — `scripts/batch_promote_xrefs.py:55-90`. After the per-book insert loop, reload each queue JSON and mark the batch's candidates `"promoted"` via `atomic_write` (Option (a): conservative-mark-all is correct because `skip_existing` means already-in-corpus). Root cause of re-run pending re-attempts. **Touches build path:** no.
- [ ] **[opt-info] `candidates_written` inflated (pre-dedup count)** — `run_naves_at_scale.py:117-121` + `run_xref_at_scale.py:120-121`. Return `(out_path, len(new_dicts))` from `write_queue` and add `out[1]` to `candidates_written`; gate `files_written` on `out[1] > 0`. Cosmetic stat only. **Touches build path:** no. (Fold into Phase-4 `write_queue` extraction.)
- [ ] **[info] `conftest.py` autouse fixture requests unused `monkeypatch`** — `tests/conftest.py:237`. Drop the `monkeypatch` param (dead since the TTL=0 removal); avoids spinning monkeypatch for ~200+ tests. **Touches build path:** no.
- [ ] **[info] `_canons_index()` docstring claims "Cached load" but has no cache** — `scripts/web_helpers.py:371-379`. Add `@functools.lru_cache(maxsize=1)` (ensure `import functools`) and fix the docstring per S7.1 (singleton; restart to reload); tests swapping `canons.yaml` call `.cache_clear()`. **Touches build path:** no.
- [ ] **[info] REPO_MAP `scripts/core/` count off-by-one** — `dev/REPO_MAP.md:41`. `(65)` → `(66)` (`manuscript_records.py` unaccounted). **Touches build path:** no.
- [ ] **[info] INDEX + mint-9 plan Status stale ("Phases 3–6 in progress")** — `docs/superpowers/INDEX.md:13` + `docs/superpowers/plans/2026-05-31-mint-9-fixes-plan.md:3`. Set both to "COMPLETE — all Phases 1–6 shipped 2026-06-01"; move the INDEX row from "In progress" to "Shipped" (update both together so `check_superpowers_coherence` stays in sync). **Touches build path:** no.

### Deferred-to-τ.G (do NOT apply now — atomic with the `ex.py`→`exo.py` rename)

- [ ] **[opt-low] `lint_rules.check_render_coverage_no_regression` hardcodes legacy `"ex"`** — `scripts/lint_rules.py:977,1006`. At τ.G (same commit as the 4-dir rename), change `"ex"`→`"exo"` in both `expected_geez` and `expected_amharic`. Applying it early causes an immediate false-FAIL. **Touches build path:** no.

## Optimization decisions

| Area | Verdict | Recommendation |
|---|---|---|
| `filter_html` ρ.1 disabled-ref-id removal (`build_edition.py:1046-1062`) | **change** | Replace the per-ID `re.compile` loop with a two-stage scan: `present = set(re.findall(r'id="(ref-[^"]+)"', text)); for ref_id in present & disabled:` — kills ~3.7M unique compilations per tradition-filtered build. Byte-identical output. **Byte-stability gate required.** |
| `web_helpers.html_ref_id_from_note_id` bxx fallback | **change** | One-line fallback (Phase 6). Fixes broken editor nav for Strategy-B books. |
| `corpus_index.connection()` threading lock | **change** | Add `_CONN_LOCK` (Phase 6). Eliminates per-race fd leak under ThreadingHTTPServer. |
| `_resolve_popup_languages` per-aside re-decode (`build_edition.py:781,955`) | **change** | Hoist `decode_per_book_languages` once per file; pass pre-decoded dict into `_resolve_popup_languages`. Additive, byte-stable. |
| `is_output_current` watched-sources parity | **change** | Add missing pipeline scripts/configs (Phase 2). |
| `build_cache._PIPELINE_SCRIPTS` (`traditions.py`+`popup_versions.py`) | **change** | Add both (Phase 2). |
| 4 kind-replace + 4 accumulator at-scale `write_queue` dedup/status | **change** | Status-preserving `seen`-set dedup + shared helper (Phase 4). |
| `batch_promote_xrefs` queue-status update | **change** | Mark promoted candidates (Phase 6). |
| Whole-build pipeline (copytree → per-file filter → zip @ compresslevel 9; `ThreadPoolExecutor(5)` for `--all`; content-addressable cache) | **confirmed-optimal** | No new parallel/streaming/shared-base speedup without breaking byte-stability. `compresslevel 9→6` **DECLINED** (quality > ~30s). Shared pre-filtered base impossible (per-edition kind/canon/popup/tradition divergence). Only the ρ.1 fix above changes anything. |
| Vision-transcription marathon method (Patrologia Esther + Kings/Samuel) — **MARATHON CORE, READ-ONLY** | **confirmed-optimal** | MAX-1 heavy agent, ≤1568px crops, per-step commits, 3-pass convergence — root-cause-correct for the 16 GB RAM ceiling (1M context would worsen OOM, not help). **No paid API.** Optional additive enhancement only: `scripts/recover_vision_transcript.py` (reads existing jsonl; no new dispatches). |
| `build_standalone.py` source-order + `en_occurrence_map` — **MARATHON CORE, READ-ONLY** | **confirmed-optimal** | Faithful dup-verse source order + `(verse_number, occurrence_index)` back-translation alignment is spec §10.6-compliant. No redesign. |
| `render_coverage.run_all()` EN-lane coverage (`render_coverage.py:239-241`) | **change (low)** | Add `geez-tewahedo-en`+`amharic-tewahedo-en` to the dir tuple + 2 summary keys; update `test_render_coverage.py` shape assert. Do NOT pin a no-regression set for the still-growing EN dirs. |

## Constraints carried

- **Never touch the marathon core** (`build_standalone.py`, `core/manuscript_*.py`, `core/po_vision_store.py`, `content/manuscript/**`, `content/translations/sources/patrologia/**`, `GAPS/`). The two `confirm-optimal` marathon items are read-only context; flagging them as defects is itself out of scope unless an outright crash.
- **9 KJV editions byte-stable.** Every build-path fix (Phase 2 cache keys, Phase 3 `_iter_note_ref_attribution_years`, the ρ.1 filter optimization, `is_output_current`, per-aside decode) carries a byte-stability proof obligation: regen + `git diff` over the 9 editions, or `tests/test_byte_stability_gate.py` rebuild+digest, must pass before shipping each phase.
- **Additive schema only.** `_append_cloned_edition` fields and any new note-field reads must be byte-identical when unset; cloned-edition fields only appear on newly-cloned editions going forward.
- **Atomic writes.** All queue/notes/JSON rewrites (Phase 4/6) go through `notes_io.atomic_write` / `ensure_backup`.
- **No paid API; KEEP PYTHON; no database.** At-scale driver fixes use no new dispatches; the marathon recovery helper only reads existing jsonl.
- **Prefer commit-time `lint_rules` checks over pytest-only guards** for invariants that recur every ingest (the `EBIBLE_VPL_TO_PROJECT` canonical-codes guard and the `--books` normalize guard).
- **5-leg save per phase:** local commit (`save.ps1`, PowerShell) + `git push origin` (GitLab) + `git push github` (GitHub) + `git bundle` to E: + copy to F: — verify each leg. `ruff format` every touched/regenerated `.py` before commit or the pre-commit hook blocks.
