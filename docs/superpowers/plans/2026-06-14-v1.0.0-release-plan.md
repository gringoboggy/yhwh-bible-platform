# v1.0.0 Release Plan — deep audit + all readers proven + mint tag

**Status:** ACTIVE 2026-06-14 — the single authoritative gate plan for cutting tag `v1.0.0`.
**Supersedes for release purposes:** `plans/2026-06-08-v0.1.0-master-plan.md` (that arc shipped
`v0.1.0`; this plan governs the **next** milestone only).

**Coordination contract (both lanes, every session):**
1. On `/resume` or fresh session: read this file + `dev/LANE_HANDOFF.md` your-task line.
2. Pick work only from **your lane column** below unless the user redirects.
3. Update truth triad (`LANE_HANDOFF` + `IN_FLIGHT` + `CHANGELOG`) when a phase slice lands.
4. **No `v1.0.0` tag** until §8 Definition of Done is fully checked (user runs the tag command).

**Pointers:** format matrix spec §6 (`specs/2026-06-10-website-format-matrix-design.md`) · reader
evidence (`dev/EREADERS.md`) · audit engine (`.claude/workflows/deep-audit.js`, round 7 ran
2026-06-10 — **round 8 required** post-M4) · Mac laundry (`notes/2026-06-14-mac-v1.0.0-laundry.md`)
· full-project arc after tag (`dev/PLAN_2026-05-29-roadmap.md` LANE D/M/P/T).

---

## 1. Honest current state (2026-06-14)

| Area | Status | Evidence |
|---|---|---|
| Builder per-reader profiles | **Built** — `target_reader` + `FORMAT_MATRIX` + wizard `TARGET_CAPS` | `scripts/build_edition.py`, `scripts/templates/wizard.py` |
| v0.1.0 release (beta track) | **Live** — desktop binaries + partial catalog | GitHub `releases/tag/v0.1.0` |
| M1 everywhere column | **LIVE** (45 EPUBs) | `catalog.json` `live: true` |
| M2 Apple Books column | **LIVE** (45 EPUBs) | Device QA rounds 2026-06-08..10 |
| M4 Kindle column | **LIVE** (45 EPUBs) | STK 6/6 PASS; WIN turn 89 attach+deploy |
| M3 Kobo column | **NOT LIVE** — legacy flagship kepub only | Round 6 device QA partial; taps pending |
| M5 Play Books column | **NOT LIVE** | Zero device QA |
| Deep audit on current tree | **STALE** — round 7 pre-M4/kindle_post/dead-variant | Needs round 8 + triage |
| Public docs for v1.0.0 | **Partial** — `RELEASE_NOTES_v1.0.0.md` rewritten; `HANDOFF_README_v7.md` obsolete | Laundry list §E |
| CI / hooks | **Green on WIN**; Mac hooks fixed turn 85 | `pre-commit` ruff+lint+mypy |

**Verdict:** The program is **not** v1.0.0-ready. v0.1.0 is a honest beta; v1.0.0 requires audit +
all five reader columns device-proven + desktop re-cut at tag.

---

## 2. Release goal (what v1.0.0 means)

A user can open `/wizard`, build a Bible for **any supported reader**, download a **catalog artifact**
or custom build, and have it work as documented — with every claim backed by device QA or an explicit
honest limitation in `EREADERS.md`.

**Non-goals for v1.0.0** (defer to post-tag master roadmap):
- Finishing the Ge'ez manuscript marathon (LANE M)
- Phase-D Esther vision marathon completion (LANE D)
- Parallel-Bible end-state (LANE P)
- Full LANE T depth backlog

---

## 3. Phase map (sequenced gates)

| Phase | Name | Blocks tag? | Owner |
|---|---|---|---|
| **P0** | Plan sync + push truth | yes (coordination) | Both |
| **P1** | Deep audit round 8 + triage | yes | WIN runs · Mac reviews/fixes |
| **P2** | Reader columns M3 + M5 | yes | Parallel (below) |
| **P3** | Audit fix sweep | yes | Parallel file-disjoint |
| **P4** | Full gate run | yes | Either lane |
| **P5** | Catalog complete (5/5 live) + site deploy | yes | WIN |
| **P6** | Desktop binaries re-cut + upload | yes | WIN exe · Mac dmg · CI AppImage |
| **P7** | Tag ceremony | — | **USER** runs tag command |
| **P8** | Post-tag | no | Master roadmap resumes |

Phases P1–P4 may overlap in **parallel tracks** (§4) but **P4 must be green before P5–P7**.

---

## 4. Parallel work matrix (WIN ↔ Mac)

**Mode:** `parallel` (default). File-disjoint unless noted. Pull before touching shared files.

### Track A — Deep audit (P1 → P3)

| Step | Windows (Grok) | Mac (Grok/Claude) |
|---|---|---|
| A1 | Bump `deep-audit.js` to **round 8** (post-M4 facts: 188 assets, kindle_post-only, M4 live, 91,723 notes, 21 consoles) | Pull + ACK |
| A2 | Run round 8 audit (win-solo OK; cap=2) + optional `dishonest-code-audit` + `stub-audit` sweeps | Mirror orphan-process protocol; review findings |
| A3 | Triage → `notes/2026-06-14-v1.0.0-audit-triage.md` (HIGH/MED/LOW) | Add Mac-owned items to board |
| A4 | Fix WIN-owned defects (build/website/catalog/tests) | Fix Mac-owned defects (dmg/hooks/content-disjoint) |
| A5 | Re-run targeted tests per fix | Verify hooks + `.venv` parity |

### Track B — Kobo M3 (P2)

| Step | Windows (Grok) | Mac (Grok/Claude) | User |
|---|---|---|---|
| B1 | **Orphan vnote gate** (`gate 4j`): drop asides whose anchor is absent post-canon-filter; TDD in `tests/` | — | — |
| B2 | Verify K-R6 fixes in tree (U+2028 seps, chip badges, BOOKII nbsp) — file tickets if missing | Same on Mac `.venv` | — |
| B3 | — | **`build_format_matrix --phase M3`** → 45 `.kepub.epub`; gate each (epubcheck + verify_kr2 + kepubify) | — |
| B4 | Copy 45 + SHA256SUMS to external drive OR scp staging for WIN | Handoff per M4 pattern (`YHWH-v2.4-releases/m3-kobo-v0.1.0/`) | Plug drive if needed |
| B5 | Attach 45 to release when Mac hands off | — | — |
| B6 | — | — | **Kobo tap round** (scripted list below) |

**Kobo user tap list (minimum before M3 goes live):**
1. Gen 1:1 — all split units (◈2/◈5/◈8) — confirm which pop open
2. Gen 35:18 — re-tap (anomalous decline in round 5)
3. Publisher Default + font pack — Arabic popup spot-check (round 6 regression)
4. One title page — "BOOK II" eyebrow spacing (K-R6-4)
5. One long chapter — no badge teleport at chapter boundary

### Track C — Play Books M5 (P2)

| Step | Windows (Grok) | Mac | User |
|---|---|---|---|
| C1 | Document Play QA protocol in `dev/EREADERS.md` §Play (upload steps) | — | — |
| C2 | Stage one M1 `everywhere` artifact (ethiopian-tewahedo navy) for upload | — | — |
| C3 | — | — | Upload to Play Books library on phone; test: popups, fonts, chapter nav, collapsible ToC behavior |
| C4 | Record verdict → decide: keep `everywhere` profile OR add `play` target | — | — |
| C5 | If pass: build M5 fan-out (9×5) OR alias everywhere until profile diverges | Mac can build if WIN busy | — |

### Track D — Docs + hygiene (parallel, non-blocking until P5)

| Step | Windows (Grok) | Mac (Grok/Claude) |
|---|---|---|
| D1 | Finish `HANDOFF_README_v7.md` → archive pointer or free-public rewrite | — |
| D2 | Final polish `dev/RELEASE_NOTES_v1.0.0.md` against live catalog | — |
| D3 | Push turn 89 commit + milestone `save-all` | Pull + ACK plan in handoff |
| D4 | — | DMG recipe dry-run (`dev/build_dmg.sh`); confirm notarize profile |
| D5 | Grok/ping tooling from stash (turn 87 WIP) — commit if complete | Mirror ping `.sh` + AGENTS.md |

### Track E — Content (opportunistic, does NOT block tag)

| Windows | Mac |
|---|---|
| Phase-D Esther only if idle | Kings/Samuel marathon · Esther vision resume (~p35) |

---

## 5. Build commands reference

```bash
# Full format matrix (from repo root, PYTHONUTF8=1)
py -3 scripts/build_format_matrix.py --phase M3   # Kobo
py -3 scripts/build_format_matrix.py --phase M5   # Play (after QA)
py -3 scripts/build_format_matrix.py --phase M4   # Kindle (DONE)

# Catalog + site (WIN)
py -3 -m scripts.gen_release_catalog --tag v0.1.0
node website/build.mjs
# deploy → C:\Users\bogda\Documents\yhwh-site-publish (WIN) or Mac publish clone

# Gates (before any "done" claim)
py -3 scripts/ci.py
py -3 dev/trace_matrix.py
```

---

## 6. Lane handoff snippets (copy to board each milestone)

**Windows active tasks (pick one until done, then next):**
1. A1–A2: audit round 8
2. B1: orphan vnote gate
3. P5: catalog attach when Mac hands M3/M5 artifacts
4. D1–D3: docs + push

**Mac active tasks:**
1. Pull + ACK this plan
2. B3–B4: M3 Kobo fan-out + external handoff
3. A4: Mac-owned audit fixes
4. D4: DMG tag-time prep
5. Track E only when A–D idle

**User tasks (cannot be agent-delegated):**
- Kobo tap round (Track B6)
- Play Books phone QA (Track C3)
- Run `git tag -a v1.0.0` when §8 complete

---

## 7. Risk register

| Risk | Mitigation |
|---|---|
| Kobo popup decline not fully understood (K-R6-2) | Ship M3 with documented limitation OR hold column until taps close |
| Play Books needs own profile | Phone QA first; add `target_reader: play` only if evidence demands |
| Audit finds build-path defect post-M4 | P3 fix sweep blocks tag; no tag on known HIGH |
| Lane collision on `build_edition.py` | WIN owns B1 orphan gate; Mac owns M3 builds; ping before shared edits |
| GitLab CI quota | GitHub Actions = real CI; GitLab manual only |

---

## 8. Definition of Done — v1.0.0 tag checklist

Every box must be checked before the user cuts the tag.

### Audit & code health
- [ ] Deep audit round 8 complete; triage doc published
- [ ] Zero OPEN **HIGH** from audit triage (MED/LOW may ship with documented debt)
- [ ] `py -3 scripts/ci.py` green (ruff, lint_rules, mypy, pytest, coverage)
- [ ] `py -3 dev/trace_matrix.py` → 0 unresolved
- [ ] `py -3 -m scripts.ebible verify` errors=0
- [ ] Dishonest-code + stub audits run; no HIGH UX lies in production paths

### All five reader columns
- [ ] M1 everywhere: 45 assets on release, epubcheck 0/0/0/0 on spot-check
- [ ] M2 Apple: 45 assets; device regression spot-check (1 edition)
- [ ] M3 Kobo: 45 `.kepub.epub` on release; user tap round complete; `EREADERS.md` updated
- [ ] M4 Kindle: 45 assets (DONE); STK spot-check documented
- [ ] M5 Play: 45 assets OR honest "everywhere alias" with phone QA verdict recorded
- [ ] `gen_release_catalog --tag v0.1.0` → **5 live columns**
- [ ] Website deployed; Downloads matrix matches release

### Desktop & release assets
- [ ] Windows `.exe` rebuilt at v1.0.0 version string, signed
- [ ] macOS `.dmg` rebuilt, notarized, stapled, uploaded
- [ ] Linux `.AppImage` rebuilt, uploaded
- [ ] `SHA256SUMS.txt` complete for all assets
- [ ] `dev/RELEASE_NOTES_v1.0.0.md` matches reality

### Coordination
- [ ] Both lanes ACK'd plan in `LANE_HANDOFF`
- [ ] `dev/SESSION_STATE.md` top entry = v1.0.0 ready or honest blocker list
- [ ] `dev/IN_FLIGHT.md` TRACKER-STATE idle at tag time

**Tag command (user only):**
```bash
git tag -a v1.0.0 -m "v1.0.0" && git push origin v1.0.0 && git push github v1.0.0
```

---

## 9. After v1.0.0 — full project completion

Resume `dev/PLAN_2026-05-29-roadmap.md`:
- **LANE D** — Phase-D own-vers ingestion (Esther → batch)
- **LANE M** — Kings/Samuel manuscript marathon
- **LANE P** — Parallel-Bible standalone end-state
- **LANE T** — Depth/correctness backlog (bookcode, security, coverage, etc.)

Schedule **audit round 9** at the next major milestone per
`plans/2026-06-10-full-project-audit-program.md` §P5.

---

## 10. Session quick-reference

| Question | Answer |
|---|---|
| What plan am I on? | This file |
| What shipped since v0.1.0? | M4 Kindle column (turn 89) |
| What's blocking v1.0.0? | Audit round 8 + M3 + M5 + full gates |
| Can I tag now? | **No** |
| Mac idle task? | M3 fan-out + audit fix review |
| WIN idle task? | Audit round 8 + orphan vnote gate |