# Mac Second-Lane Bring-up + Next Steps

> **For agentic workers:** the operational plan for the Mac (2017 iMac) lane. Steps use `- [ ]` checkboxes. This machine is a **2nd lane** — work on files DISJOINT from the N95's active lane and **never push** (Windows/N95 = the single committer, per the parallel-operation protocol). Orientation: read memory `reference_mac_dev_env` first.

**Status:** ready — 2026-06-03. Mac provisioned as a full 2nd lane **WITH push** (SSH → GitLab `origin` + GitHub `github`; Windows pulls+combines, does NOT push). GAPS + OCR PDFs in + verified; toolchain COMPLETE incl. **Tesseract** (precompiled via conda-forge — Homebrew is unsupported on macOS 13). Captures the bring-up, the Mac-made cross-platform fixes (now committed FROM the Mac), and the GAPS-unblocked work (Windows owns P0 folio-mapping).

**Context (done this session, 2026-06-02→03):** uv-managed **Python 3.14** venv (`.venv`); terminal `claude` CLI (`~/.local/bin`, on PATH); VS Code + Claude/Ruff extensions (interpreter pointed at the venv via gitignored `.vscode/settings.json`); **Homebrew**; **Pillow + PyMuPDF** installed in the venv; `git core.fileMode false` (Windows committed scripts as 100644); restored exec bits on `ebible`/`build.sh`; **GAPS extracted to `<repo>/GAPS/` (697 files / 1.1 GB — verified: all 34 referenced Sam/Kings folios resolve, `missing=0`)**; full KoolJam drive copied to `/Volumes/MacHD2/KoolJam/`. Full suite ≈ 7,490 pass; the only non-expected reds are environment/timing (below).

---

### Task 1 — Finish toolchain verification
- [x] **Tesseract** — DONE 2026-06-03, but **NOT via brew**: macOS 13 is now Homebrew **Tier-2/3 (unsupported)** → it force-builds from source and FAILED (glib/meson "compiler cannot compile programs", though clang itself is fine). Installed **precompiled via conda-forge/micromamba** instead: `micromamba` (static binary in `~/.local/bin`) → env `ocr` → **`tesseract 5.5.2`** at `~/micromamba/envs/ocr/bin/tesseract`, symlinked to `~/.local/bin/tesseract` (on PATH). `--list-langs` shows **`amh`** + **`script/Ethiopic`** (upgraded to `tessdata_best`) + 124 more. `paths.tesseract_binary()` resolves it via **`TESSERACT_BIN`** (set in `~/.zshrc` with `TESSDATA_PREFIX`). **VERIFIED** OCRing printed Ge'ez/Amharic from `Bible_Amharic_and_Geez.pdf` (which also has an embedded text layer → the parallel-Bible lane can extract text directly). ⚠ Do not use brew for native deps on this macOS 13 box — prefer conda-forge precompiled.
- [ ] **perf tests** — re-run `tests/test_perf.py` on a QUIET machine (nothing compiling) to confirm the 2 budget fails (`test_api_matrix_cold_under_budget`, `test_api_customize_data_under_budget`) were CPU contention / cold-cache, not a regression. If they persist on a quiet run it's the slower iMac hardware vs the N95-calibrated budgets — record, don't chase.

### Task 2 — Remaining input data (user provides)
- [x] **OCR source PDF** → `content/translations/sources/parallel-bible-eotc/Bible_Amharic_and_Geez.pdf` (gitignored, 202.7 MB). DONE 2026-06-03 — placed from the N95 Mac-handoff zip `KoolJam/yhwh-mac-handoff_p0-folio-index_ocr-pdfs_2026-06-03.zip` (had a `README_PLACEMENT.txt`); `%PDF` header verified, correctly gitignored (`.gitignore:169`). The same zip also placed `project_maccabees_expansion/Meqabyan_Geez_Amharic_parallel.pdf` (6.7 MB, gitignored) + the repo-parent reference PDFs (Nave's / Easton's / psalmslinks) one level above the repo at `/Volumes/MacHD2/`.
- [x] GAPS image tree — DONE (extracted + verified this session; the handoff zip's 577 GAPS files all matched existing paths → union still 697, a source-of-truth refresh, no new folios).

### Task 3 — Land the Mac-made fixes (committed FROM the Mac, 2026-06-03)
~~The Mac does NOT push~~ — superseded 2026-06-03 (Task 5): the Mac now pushes, so these 3 cross-platform fixes are committed + pushed from the Mac; Windows pulls them and combines its P0 work:
- [ ] `tests/test_omega4x_hygiene.py` — hardcoded Windows `"py"` launcher → `sys.executable` (2 calls). Fixes the 2 omega4x `FileNotFoundError: 'py'` fails on Mac; also correct on Windows.
- [ ] `tests/test_mint11_phase4.py` — `test_corpus_index_fingerprint_computed_before_swap` now strips full-line comments before `.index()`, so the comment mentioning `tmp.replace(path)` can't false-match ahead of the real call (production order is already correct; this was a fragile-test false-positive).
- [ ] `requirements-dev.txt` — declared **Pillow** + **PyMuPDF** (assumed-but-unpinned feature deps for cover/manuscript/OCR; needed for the full suite to pass). Consider promoting **Pillow** to `requirements.txt` (it is also a runtime cover-generation dep).
- [ ] After landing, re-run the affected tests; a full `pytest` on a quiet machine should leave only the expected reds: the 2 `both_witness_folios` P0-gate (Task 4) + any genuine perf-hardware delta.

### Task 4 — GAPS-unblocked project work (file-disjoint, user-paced)
Marathon rules apply (memory `feedback_marathon_pacing`, `feedback_concurrent_agent_cap`): MAX-1 heavy vision agent, tight region crops ≤1568 px, per-step commits via the committer, user check-ins per page/chapter.
- [ ] **P0 folio-mapping** — per `plans/2026-06-02-samkings-folio-index-p0-plan.md`: map folios for the **51 samuel** (1sa 2,4–16,18–31; 2sa 1–10,12–24) + **42 kings** pending chapters into `content/manuscript/{samuel,kings}/manifest.yaml`. NOTE the live gate lists **1ki 6** as lacking folios (so pending kings = 1ki 6–22 + 2ki 1–25), though docs list 1ki 1–6 as calibrated — reconcile 1ki6 during mapping. Drives `test_samkings_manifest_complete::test_every_chapter_has_both_witness_folios` → green. GG folios are on disk under `GAPS/`; CAM hi-res for pending chapters needs CUDL-IIIF acquisition (`scripts/acquire_cudl_master.py`). Keep `status: pending` so collation can't mis-trigger.
- [ ] **Token-level convergence refinement** — per `plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md`: refine `scripts/core/manuscript_converge.py` from a verse-binary gate to token-level (flag divergent tokens, not whole verses — the 1Ki1 proof was ~57% token-level vs 2–11% verse-binary). Non-blocking; the conservative gate already works.

### Task 5 — Standing lane-coordination rules
- Mac = **2nd lane, NOW WITH PUSH** (user-directed 2026-06-03): SSH push to GitLab `origin` + GitHub `github` is wired (memory `reference_mac_dev_env`). **Push model:** the Mac commits + pushes its adjustments to `main`; **Windows does NOT push** — it pulls the Mac's commits and combines its own P0 work locally. Keep files DISJOINT from Windows' active work (Windows owns P0 Sam/Kings folio-mapping). The Max subscription quota is SHARED — don't run heavy agent fan-outs concurrently. Always `git fetch` before pushing.
- RunPod cloud lane stays paused awaiting the user's pricing-trigger (`plans/2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md` P2–P4).

**Cross-refs:** memory `reference_mac_dev_env`, `reference_gaps_folder`, `reference_ssh_git_remotes` (Windows-only), `feedback_marathon_pacing`, `feedback_concurrent_agent_cap`; plans `2026-06-02-samkings-folio-index-p0-plan.md`, `2026-06-02-samkings-cloud-agent-workflow-and-run-plan.md`.
