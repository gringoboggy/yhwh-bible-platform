# v0.1.0 Master Plan — round-6 audit + device-QA + note-rehaul + icons → release

**Status:** READY 2026-06-08. The single top-level sequencing + lane-division plan for the
v0.1.0 (still-beta) release. **Findings-only until this plan is ratified; no fixes applied yet.**
This plan STITCHES three detailed input docs — it does not duplicate them:

1. **Audit (43 findings):** `docs/superpowers/notes/2026-06-08-round6-split-audit-findings.md`
   (the merged win+mac round-6 phased fix plan — Phases 0–5 + optimizations + 7 completeness gaps).
2. **Device-QA + note-rehaul:** `docs/superpowers/notes/2026-06-08-device-qa-and-note-presentation-rehaul.md`
   (findings 1–7 + the note-presentation north star + staged S1–S4).
3. **Existing presentation plan to UPDATE in place (do NOT duplicate):**
   `docs/superpowers/plans/2026-06-06-beta-device-qa-presentation-plan.md` (8 phases) +
   its spec `…/specs/2026-06-06-beta-device-qa-presentation-design.md`.

**Release target (user-directed):** **v0.1.0 — STILL A BETA** (a milestone bump from v0.0.3; v1.0.0 is
intentionally deferred further, until the v0.1.0 upgrade is validated in real use). Supersedes the
06-06 plan's "v1.0.0-beta.2."

**Verdict from the audit:** 0 critical / 0 high; the 9-edition program is functionally MINT. Every audit
finding is test-only, a latent guard that never fires on current data, reader-cosmetic, dev-tooling, or
stale docs — none corrupts shipped bytes, none touches the marathon core.

---

## Guiding order (RULES §3): safest / most-foundational first → polish → release-with-outward-surfaces last

The whole program is honest + correct only AFTER the suite is green and the latent holes are closed; do
that BEFORE the user-facing presentation work, so every presentation byte-stability proof runs against a
verified-clean baseline. The public-facing surfaces (site / update-log / roadmap / repo) ship LAST, with
the release, so they reflect finished work and stay internally consistent.

### STAGE A — Green + honest + guarded *(audit Phases 0,1 + the test-only parts of Phase 5)* — WIN
Lowest risk; test/doc/lint only; **no build-path, no byte obligation.**
- Audit **Phase 0** (3 commit-time lint guards: `check_atomic_writes` → `.write_text/.write_bytes`;
  candidate-extent lint; route-table count coupling) — ship FIRST so later phases can't regress.
- Audit **Phase 1** (the red suite: canon counts 87→86/75/77, `EMBED_FONT_PATHS`, comm-reformation,
  date-rolled term, **website-progress 87→83**, PUT route 10→11, corpus-index equivalence, 2es title,
  4× REPO_MAP doc fixes). Source is correct; only stale tests/docs move.
- Audit **Phase 5** test-honesty/debt items that are test-only/off-build-path (dead promote guard,
  bugcluster sweep test, coverage regex, lint negative tests, at-scale clone hoists, dead helpers,
  `_append_cloned 0→""`, `TEWAHEDO_DISTINCTIVE`, render_coverage dup/mislabel).
- **Gate:** full suite GREEN; lint guards live; ruff/mypy clean.

### STAGE B — Close latent holes + run the completeness re-verifications *(audit Phase 2 + the 7 gaps)* — WIN (N95 SSD)
Additive; promote-time / cache / authoring; **no shipped bytes.** Do the real-build re-verifications here
so we have a proven-clean build baseline before touching presentation.
- Audit **Phase 2**: aes coord-guard no-op **+ the `_book_shape_cached` CLASS fix** (completeness gap #3 —
  any non-1-start/gapped book, not just aes); `edition_stats` stale-cache twin; 2 re-ingest bare writes;
  `prospect` truncation; `sources_lexicon` book-code normalize; 111 out-of-extent candidate cleanup.
- **Completeness re-verifications (N95 real builds — the 3 that need a real tree):**
  (i) **font-embed byte-stability/epubcheck** — build eth + catholic-study + the 9 KJV twice, diff bytes,
  epubcheck for RSC-008 from the populated `EMBED_FONT_PATHS` woff2/Cardo `@font-face`; reconcile
  `epub_working/stylesheet.css` vs `apply_style.py`.
  (ii) **post-pass × canon-filter × file-split triple-seam** — build catholic-study, scan ONE artifact for
  spine gaps, gapless `BOOK <roman>` eyebrows, cross-piece href integrity, orphan Notes shells,
  nested-anchors.
  (iii) **real published stores out-of-extent sweep** — load each `content/notes/<book>.py` via the real
  loader (not regex), run `coord_in_canonical_extent` after the aes/class fix; trace whether `promote.py`
  validates on the way IN.
- **Gate:** byte-identical 9 KJV; epubcheck 0/0/0/0; the 3 gaps answered (findings-only → fold any new
  defect here).

### STAGE C — Presentation polish + note-rehaul *(UPDATE the 06-06 plan; + audit Phase 3)* — WIN build / MAC design
Build-path → **byte-stability proof obligation** on touched surfaces (regen + per-member zip-digest diff,
normalize `dc:date`/`dcterms:modified`/filename ts; 9 KJV byte-identical unless intentionally re-baselined).
Map onto the 06-06 phases (do not re-plan):
- Audit **Phase 3** (file-splitter orphaned "Notes" heading/`<hr>`) — pairs with 06-06 splitter work.
- **06-06 Ph1 + finding 1b** — justify as the EPUB default, **scoped via a prose-container WHITELIST**
  (verse/note/intro prose; NEVER titles/headings/ToC/pills/tables/captions) + `hyphens:auto` + **a build
  guard** that no heading/title/ToC selector resolves to `justify`; body size; native serif; in-content
  ToC link affordance.
- **06-06 Ph2 = note-rehaul S1→S2→S3a** under the **NORTH STAR (reader-robust structure FIRST):** carry
  the cascade **verse → category → source → note** with primitives that survive everywhere (semantic
  headings, border-rules/indents, dividers, weight, the existing icons ◇⌂⌘▌⚖○✦); tinted cards are
  progressive enhancement ONLY (acceptance: backgrounds off → still clearly structured). S1 attribution
  de-dup (Ephrem ×3→×1) → S2 group by category→source (no repeated prefixes) → S3a topic-term dedup +
  Nave's/Torrey union → S3b near-dup bodies (careful, high threshold) → **S4 semantic combine DEFERRED**.
- **06-06 Ph3 + findings 1 + 1c** — native ToC book-level default + the **expandable in-EPUB pill ToC as a
  `/customize` ON/OFF toggle (default ON)** + native per-chapter toggle (default book-only) **with
  user-facing "reader-dependent" instructions** + **incipit chapter labels** (derive from first verse) +
  kepub Kobo target.
- **06-06 Ph4** (structural audit + lint guard); **06-06 Ph5** (dup-tuple prune + OCR de-hyphenation —
  overlaps audit Phase 2's 111-candidate cleanup; reconcile); **06-06 Ph6 = finding 3** (book title-page
  bleed — **render-first**, do NOT blind re-center).
- **NEW finding 2** (Your-Edition stats popup full-page / book-name column off-screen) — render-then-
  diagnose the modal + table CSS; relates to `edition_stats`.
- **NEW finding 6** (desktop-app top-nav prettify — `scripts/web.py` nav template + app CSS: real app-bar,
  grouped routes Build·Edit·Inspect·Publish, hover/active states; behavior unchanged).
- **Design latitude — an EXTRA popup is sanctioned (user-directed 2026-06-08):** *"if we need to add an extra
  popup to help the program I am open to that."* If the note-rehaul / reader UX is genuinely improved by a
  NEW popup surface (e.g. a symbol/category **legend** popup, a glossary/help popup, or splitting an
  overloaded note into its own popup), adding one is approved. Constraints: follow the popup/aside conventions
  — a **native EPUB3 footnote-popup, NO JS, with a reader-robust fallback** per the note north star (Kobo's
  partial footnote support → degrade gracefully), and surface it as a **builder option with a sensible
  default** (RULES §2 / [[feedback_presentation_configurable]]), not a hardcode.

### STAGE D — App icons (3 platforms) — WIN (.ico/.png + spec) / MAC (.icns)
Root cause: `dev/launcher.spec:160` `icon=None`; no `.icns` exists. Assets are all present in
`assets/icons/`.
- **WIN:** make `launcher.spec` `icon=` **platform-conditional** — Windows → `assets/icons/program_icon.ico`;
  Linux AppImage → drop `assets/icons/icon_512.png` (replace the `build_appimage.sh:80` placeholder).
- **MAC:** generate `assets/icons/YHWH.icns` via `iconutil` from the PNG ladder (16→1024) and commit it
  (the spec's darwin branch references it).

### STAGE E — macOS native window (finding 7, HIGH) + dmg rebuild — MAC (+ WIN shared spec edit)
- **WIN (shared edit):** add `pyobjc-core`/`pyobjc-framework-Cocoa`/`pyobjc-framework-WebKit` to
  `dev/requirements-desktop.txt` + `launcher.spec` hiddenimports (`webview.platforms.cocoa` + the pyobjc
  modules); make the native→browser fallback EXPLICIT (a clear message, not a silent browser open).
- **MAC:** rebuild the `.dmg` with those deps + the `.icns`, re-notarize + staple, **verify it opens its
  OWN native window with the chosen icon** (dock + window + about). Also confirm Win `.exe`/Linux AppImage
  native-window paths.

### STAGE F — Outward-facing surfaces + v0.1.0 release (LAST, ships together) — WIN (+ MAC dmg)
Apply the count/claim-sweep rule **as ABSOLUTE SYNC across EVERY surface the program is connected to**
(user-directed 2026-06-08) — not just the page body. Sweep ALL metadata (`<meta>`/og/twitter,
`manifest`/`sitemap.xml`/`robots.txt`, **social-card PNG re-render**), the GitHub+GitLab repo
descriptions/topics/About/README, the release notes, the in-app/tracker copy, AND every external listing
the project is registered on (faith.tools, christian_foss, any directory/socials). Where a surface already
PUBLISHED stale content, **edit it — or delete-and-repost when an edit can't fix it** (an outdated release
card, a superseded X post), and **DRAFT follow-up X posts for the user to copy-paste** announcing the
update (the user posts X manually — the standing convention; Claude only drafts). Nothing connected is
left on an old count/claim/version.
- **Site (06-06 Ph7 + audit Phase 4):** single current-download block (Win/Mac/Linux v0.1.0) + capped
  3-entry update/fix log; **home-page "almost here" → present-tense "live"** (audit Ph4); mobile layout;
  `gen_checksums.py` `.epub` fix; `notary_autofinish.sh` + `build-linux.yml` retired-tag fixes;
  `gen_website_progress.py` EN-regex → `ast` (audit Ph4 / completeness gap #6).
- **"What we're working on":** update the site roadmap/status page to the live v0.1.0 track.
- **Fix/update log:** website "what's changed" (capped) **and** `dev/CHANGELOG.md` + release notes in lockstep.
- **GitLab + GitHub:** repo descriptions, README, topics synced (83 books, v0.1.0, current state) —
  dovetails the queued "walk both repos' settings + add missing visible files" task.
- **Release (06-06 Ph8 → v0.1.0):** bump `VERSION` → `0.1.0`; rebuild ALL THREE binaries (Win `.exe`
  Azure-signed; Linux AppImage CI; **macOS `.dmg` MAC** — with native window + icon) **+** EPUB/`.kepub`
  **+** website **+** social-card; merge `SHA256SUMS.txt`; GitHub release (new tag, prerelease, supersede
  v0.0.3); re-render + re-scrape the social card; verify live URLs HTTP 200 + previews show 83.
- **Re-render/re-scrape note:** today's `og:image?v=20260608` cache-bust is live; v0.1.0 bumps the card +
  version again → bump the `?v=` token with it.

---

## Lane division (mode = parallel where file-disjoint; truth_owner = windows)

**WIN owns ALL shared-repo code/test/doc/config edits** (audit Phases 0–5; device-QA build impl; note-rehaul
S1–S3 build; the file-splitter fix; `launcher.spec` platform-conditional icon + pyobjc hiddenimports;
`requirements-desktop`; website + release tooling + outward surfaces; GitHub/GitLab repo updates; the Win
`.exe` + Linux AppImage rebuilds). This keeps the shared tree single-writer = no lane conflict.

**MAC owns the macOS-build-only + design + verify items** (file-disjoint from win's code edits):
see the laundry list below.

**Sequencing across lanes:** A → B → C run on WIN (a fix at a time, each its own commit/gate). MAC's
note-rehaul SPEC (a NEW doc) can be authored in PARALLEL with STAGE A/B (feeds STAGE C). STAGE E/F's dmg is
MAC and runs at release time after WIN lands the spec/requirements + the EPUB/website.

---

## macclaude laundry list (dispatch now; START the design spec; the dmg/build items are release-time)

1. **(START NOW, parallel) Note-rehaul follow-on DESIGN SPEC** — turn the device-QA doc §4+5 (S1–S4 + the
   reader-robust north star) into a build-time design spec under `docs/superpowers/specs/` that WIN
   implements in STAGE C: exact cascade markup (verse→category→source→note) using reader-robust primitives,
   the category→source grouping rule, S1/S2/S3a/S3b dedup predicates + completeness guard (never drop a
   distinct point), and how tinted cards layer ON TOP as enhancement only. Mac-led design; build impl = WIN.
2. **(release-time) macOS `.icns`** — `iconutil` an `.iconset` from `assets/icons/` (16→1024) →
   `assets/icons/YHWH.icns`, commit it (WIN's `launcher.spec` darwin branch references it).
3. **(release-time, HIGH — finding 7) macOS native-window dmg rebuild** — after WIN lands the
   `requirements-desktop` pyobjc deps + `launcher.spec` cocoa hiddenimports + the explicit-fallback change:
   rebuild the `.dmg`, re-notarize + staple, **verify it opens its OWN native window with the chosen icon**
   (dock/window/about). Confirm the fallback message path.
4. **(release-time) v0.1.0 macOS artifact** — build `dist/YHWH-0.1.0.dmg`, notarize/staple, upload to the
   v0.1.0 release, merge into `SHA256SUMS.txt`, point the site's macOS button at it.
5. **(verify) Device-QA confirmation** — once WIN's STAGE C EPUB lands, re-check on Apple Books / the user's
   device that the note-rehaul, justify, ToC toggle, title-page, and stats-popup fixes render as intended.
6. **Mac-domain audit cross-checks (read/confirm, fixes are WIN's):** the `aes`/`_book_shape_cached` and
   `edition_stats` fixes are WIN code edits — Mac need not touch them; just confirm them in review when WIN
   pushes STAGE B.

Findings-only HOLD lifts only when this plan is ratified; until then Mac may proceed on **item 1 (the design
spec)** since it's a new doc that blocks nothing and feeds STAGE C.

---

## Constraints carried (from the audit + project rules)
- **Never touch the marathon core:** `build_standalone.py`, `core/manuscript_*`, `core/po_vision_store.py`,
  `content/manuscript/`, Patrologia stores, `GAPS/`.
- **9 KJV editions byte-stable:** every build-path fix (STAGE C + the STAGE B re-verification builds) carries
  a regen + per-member zip-digest proof, normalizing timestamps. Test/doc/CLI/promote/authoring fixes carry
  no byte obligation.
- **Additive only; KEEP PYTHON / no DB; no registry edits** — sources are correct; only stale tests/docs move.
- **Atomic writes** for every notes-store write (`notes_io.ensure_backup`+`atomic_write`); enforced by the
  STAGE A lint extension.
- **No paid API; no rented cloud VM** — both owned machines only (N95 worker cap = 2; Mac dmg-build + design +
  read-only review).
- **Save cadence:** LOCAL COMMIT during work; full 5-leg (`save-all.ps1`, `lane_ping --before-push`) at each
  STAGE milestone. `ruff format` regenerated stores before commit.
- **Geez/Amharic standalones = a FUTURE update, out of scope** (the standalone-EN alignment item is
  recommend-only, deferred to when that lane goes live).

## Standing planning rule (user-directed 2026-06-08)
Every plan ALWAYS includes the outward-facing surfaces, not just internal code: **site implementation/updates ·
the fix/update log (site + CHANGELOG) · the "what we're working on" roadmap/status · GitLab+GitHub repo
descriptions/README/topics** — synced together on each release via the count/claim-sweep rule.

**ABSOLUTE SYNC (user-directed 2026-06-08, stronger):** *"when updating the website and all that phase, I
also mean metadata and everything — absolute sync everywhere the program is connected, including edits (or
delete/reposts) when needed and followup messages for X."* The sweep covers EVERY connected surface + ALL
metadata (page bodies, `<meta>`/og/twitter, social card, manifest/sitemap/robots, repo descriptions/topics/
About, release notes, in-app copy, external listings/directories, socials), and it INCLUDES **editing — or
deleting & reposting — already-published content** when a change makes it stale (a stale X post, an old
release card, an outdated listing), plus **follow-up X posts DRAFTED by Claude for the user to copy-paste**
(the user posts X manually). After any release/claim/version change, audit the full connected footprint and
leave nothing on an old value.
