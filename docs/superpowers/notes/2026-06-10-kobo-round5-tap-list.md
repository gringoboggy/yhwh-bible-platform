# Kobo round-5 — the K-R4-2 calibration tap-list

**Status:** READY 2026-06-10 — the round-5 kepub is ON the Kobo
(`G:\YHWH-Ethiopian-Bible-koboQA-r5.kepub.epub`, 34.14 MB, all gates green:
verifier EPUB+kepub, epubcheck 0/0/0/0, kepubify 1/0/0). Seven taps pin the
popup-decline threshold T; the K-R4-2 fix (split-by-category + in-note split)
sets its cap from the result. Generated by `dev/kobo_tap_calibration.py`;
re-runnable on any artifact.

## What this answers

Kobo's e-ink footnote preview DECLINES big popups and NAVIGATES instead (the
round-4 "teleport"/"nothing"). Round 4 bracketed the cutoff between Gen 1:3
(popped) and Gen 1:26 (jumped). These seven taps narrow that bracket to one
gap so the fix's cap is data-set, not guessed.

## The protocol (same reading conditions as round 4)

Open each verse below, tap the **translation badge** once, and record one
letter per row:

- **P** — the preview pops at the bottom of the page
- **J** — the screen jumps somewhere else
- **N** — nothing visible happens (that IS a jump to the start of the
  current section — counts as J)

| # | Verse | Expected | Result (P / J) |
|---|---|---|---|
| 1 | 1 Chronicles 24:10 | P (control) | |
| 2 | Genesis 35:18 | ? | |
| 3 | Joshua 11:1 | ? | |
| 4 | Genesis 2:18 | ? | |
| 5 | Genesis 41:51 | ? | |
| 6 | Genesis 1:26 | J (round-4 control) | |
| 7 | 1 Samuel 1:20 | J (control) | |

Rows are ordered smallest → largest note. The threshold lands between the
largest P and the smallest J — e.g. "1–4 popped, 5–7 jumped" pins T between
5,500 and 6,442 stripped characters.

## The measurements behind the rows (maintainer detail)

| Verse | aside id | stripped chars | piece |
|---|---|---|---|
| 1ch 24:10 | `vnotes-1ch-24-10` | 3,300 | index_split_016_04 |
| gen 35:18 | `vnotes-gen-35-18` | 3,509 | index_split_002_02 |
| jos 11:1 | `vnotes-jos-11-1` | 4,498 | index_split_009_05 |
| gen 2:18 | `vnotes-gen-2-18` | 5,500 | index_split_000_02 |
| gen 41:51 | `vnotes-gen-41-51` | 6,442 | index_split_002_04 |
| gen 1:26 | `vnotes-gen-1-26` | 7,417 | index_split_000_02 |
| 1sa 1:20 | `vnotes-1sa-1-20` | 8,074 | index_split_010_07 |

Artifact facts: 66,683 popup asides scanned; only **195** exceed the
proven-pop floor (3,313) — the K-R4-2 fix class is small. Distribution:
p50 382 · p90 844 · max 19,432 (the Easton DAVID/PAUL single-note giants —
Mac's design prep: the fix needs (a) split-by-category AND (b) splitting
within a single note body). Sizes measured tag-stripped/entity-decoded/
whitespace-collapsed; EPUB and kepub measure identically (koboSpan adds no
text — verified by execution on both round-5 artifacts).

## After the taps

1. Record the seven letters here (or just tell the lane "1-4 P, 5-7 J" style).
2. T is pinned → the K-R4-2 cap is set just under T; the fix implements
   (a)+(b) per `notes/2026-06-10-kobo-round4-device-qa.md`; gate S2 (stripped
   sizes, `verify_kr2_build.py` gate 4g) flips warn→fail at the calibrated cap.
3. `dev/EREADERS.md` §Kobo gets the pinned T (same commit as the fix).
