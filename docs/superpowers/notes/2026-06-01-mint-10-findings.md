# Mint-10 deep-audit findings — round 3 (recovered + continued)

> **Provenance:** killed run `wf_ba367edc-a4a` (mint-10 round-3, 15 dims, ran 7.4h then died on terminal shutdown before any verify/synth) — its 15 completed finder transcripts were recovered (46 candidates). Continuation `wf_4a2d0815-8c9` (`deep-audit-continue.js`) verified all 46 + ran the 4 finders that never completed (cross-module#1 + opt-build/ingest/render) + synthesized. Verify+find on sonnet (4-core N95 -> Workflow cap=2).

**Counts:** 62 deduped -> **49 verified survivors / 13 refuted**. By severity: high 4 / medium 13 / low 22 / info 10.

Full structured detail (evidence + panel reasoning) in `2026-06-01-mint-10-audit-raw.json`. Phased plan in `../plans/2026-06-01-mint-10-fixes-plan.md`.

## Verified survivors (49)

### byte-stability [4]
- **[high] Build cache: scripts/core/popup_versions.py missing from _PIPELINE_SCRIPTS — stale EPUB served after version-registry edit** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/build_cache.py:62-70`)
  - **Fix:** Add `"core/popup_versions.py"` to `_PIPELINE_SCRIPTS` in `/scripts/core/build_cache.py` lines 62-70: ```python _PIPELINE_SCRIPTS = ( "build_edition.py", "matter_pages.py", "epub_utils.py", "resync_marker_glyphs.py", "build_epub.py", "style_config.py", "inject.py", "core/popup_versions.py", # drives  …
  - Evidence: `_PIPELINE_SCRIPTS` lists 7 scripts under `scripts/` but omits `scripts/core/popup_versions.py`, which is imported at module-level in `build_edition.py` (line 749: `from scripts.core import popup_versions as _pv`) and whose `VERSION_REGISTRY` dict is merged into `POPUP_LANGUAGES` at lines 751-760. T …
- **[high] Build cache key omits topical-index source data files; stale EPUB served after naves/torrey JSON rebuild** (`scripts/core/build_cache.py:62-70, 277`)
  - **Fix:** Immediately after line 277 in C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/build_cache.py, add: ```python # 9c. Topical-index source data consumed by inject_back_matter -> # _write_topical_page -> sources.naves_topical() / sources.torrey_topical(). # Re-running extract_naves_ccel.p …
  - Evidence: The `compute_cache_key` function explicitly hashes `source_dates.yaml` at line 277 with the rationale 'not under epub_working/, so hash it directly or a … edit serves a stale … EPUB' (comment lines 274-276). The same class of input is missed for the two topical-index data files. Every EPUB build cal …
- **[medium] Build cache: scripts/core/traditions.py missing from _PIPELINE_SCRIPTS — stale EPUB served for tradition-filtered editions after label/ID change** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/build_cache.py:62-70`)
  - **Fix:** Add `"core/traditions.py"` to `_PIPELINE_SCRIPTS` in `scripts/core/build_cache.py`. The tuple becomes: ```python _PIPELINE_SCRIPTS = ( "build_edition.py", "matter_pages.py", "epub_utils.py", "resync_marker_glyphs.py", "build_epub.py", "style_config.py", "inject.py", "core/traditions.py", # CANONICAL …
  - Evidence: `scripts/core/traditions.py` is imported at build time in three places in `build_edition.py` (lines 126, 227, 459) and its `CANONICAL_TRADITIONS` tuple provides the display labels injected by `apply_tradition_labels_to_html` into editions that declare `traditions_default`. `catholic-study` has `trad …
- **[low] build_cache._referenced_translations resolves legacy popup-language aliases to non-existent directories — stale cache for any edition using 'english'/'hebrew'/'greek' aliases** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/build_cache.py:111-136`)
  - **Fix:** The finder's fix is correct in logic but incomplete — `popup_versions` is not yet imported in `build_cache.py`. The safe, complete fix: In `scripts/core/build_cache.py`, add the import near the top of the module (alongside other `scripts.core` imports, if any, or lazily inside the function to avoid  …
  - Evidence: `_referenced_translations` collects raw language IDs from `popup_languages_default` and `popup_languages_per_book` and hashes `translations/<id>/_meta.yaml` + per-book files. But `build_edition._resolve_popup_languages` maps legacy aliases through `_pv.resolve_version_id` at runtime (line 795): `'en …

### code-debt [2]
- **[medium] write_queue copy-pasted verbatim across 4 at-scale drivers — mint-9 fix applied to 4 of 8 sites, stale at_scale_base docstring** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/run_xref_at_scale.py:40-83`)
  - **Fix:** Confirm: extract the shared logic into `at_scale_base.append_candidates(out_path: Path, book: str, chapter: int, candidates: list[dict]) -> Path | None` using `except (json.JSONDecodeError, OSError)` throughout (no `except_broad` kwarg — the narrow form is correct for all 4 sites and ethiopian alrea …
  - Evidence: After mint-9 #9 added the triple-key dedup `seen = {(c.get("verse"), c.get("kind"), c.get("draft_body")) for c in existing}` guard, the `write_queue` bodies in run_xref_at_scale.py (lines 40–83), run_naves_at_scale.py (lines 41–86), run_torrey_at_scale.py (lines 41–82), and run_ethiopian_at_scale.py …
- **[medium] _normalize_book_code missing from --books CLI path in 4 of 8 at-scale drivers (★BUGCLUSTER incomplete fix)** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/run_ai_xrefs_at_scale.py:269`)
  - **Fix:** The finder's fix is correct. For each of the 4 unguarded drivers: **`scripts/run_ai_xrefs_at_scale.py` (line 269) and `scripts/run_ai_notes_at_scale.py` (line 272):** Both already import `resolve_books` from `at_scale_base`. After `books = resolve_books(args.books)`, add: ```python from scripts.core …
  - Evidence: mint-7 added `books = [sources._normalize_book_code(b) for b in books]` in the `main()` of run_xref_at_scale.py (line 149), run_naves_at_scale.py (line 154), run_torrey_at_scale.py (line 190), and run_ethiopian_at_scale.py (line 159) — each with the comment 'Defense-in-depth: canonicalize legacy cod …

### concurrency-caching [3]
- **[low] corpus_index.connection(): unprotected check-then-set race leaks a sqlite3.Connection on ThreadingHTTPServer** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/corpus_index.py:669-679`)
  - **Fix:** The proposed fix is mechanically correct. One improvement: move `_CONN_LOCK = threading.Lock()` to module level alongside `_CACHED_CONN` and `_CACHED_CONN_PATH` (lines 642-646 in corpus_index.py), and add `import threading` to the imports if not already present. Also correct the stale comment at lin …
  - Evidence: ```python if _CACHED_CONN is None: _CACHED_CONN = sqlite3.connect(current_path, check_same_thread=False) _CACHED_CONN.row_factory = sqlite3.Row _CACHED_CONN_PATH = current_path ``` The `if _CACHED_CONN is None:` check and the subsequent `_CACHED_CONN = sqlite3.connect(...)` assignment are NOT protec …
- **[info] conftest.py: _disable_corpus_index_fingerprint_cache fixture requests `monkeypatch` but never uses it — dead parameter wastes fixture overhead per test** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/tests/conftest.py:237`)
  - **Fix:** Remove the unused `monkeypatch` parameter: ```python @pytest.fixture(autouse=True) def _disable_corpus_index_fingerprint_cache(): import sqlite3 try: from scripts.core import corpus_index except ImportError: return if corpus_index._CACHED_CONN is not None: try: corpus_index._CACHED_CONN.close() exce …
  - Evidence: ```python @pytest.fixture(autouse=True) def _disable_corpus_index_fingerprint_cache(monkeypatch): import sqlite3 try: from scripts.core import corpus_index except ImportError: return if corpus_index._CACHED_CONN is not None: ... corpus_index._CACHED_CONN = None corpus_index._CACHED_CONN_PATH = None  …
- **[info] _canons_index() docstring claims 'Cached load' but function has no cache and re-reads canons.yaml on every call** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/web_helpers.py:371-379`)
  - **Fix:** The most complete fix per S7.1 is to add @lru_cache(maxsize=1) and correct the docstring. The function has no mutable runtime callers (canons.yaml is never written at runtime via any API endpoint), so no cache_clear() concern in production. Tests that exercise this path should call _canons_index.cac …
  - Evidence: ```python def _canons_index() -> dict: """Cached load of canons.yaml — returns {canon_id: {label, description, books}}.""" canons_path = REPO / "content" / "canons.yaml" if not canons_path.is_file(): return {} import yaml data = yaml.safe_load(canons_path.read_text(encoding="utf-8")) or {} return da …

### correctness [5]
- **[high] `_iter_note_ref_attribution_years` skips all 8-field (legacy) notes — `time_filter_ceiling` silently defeated for bulk corpus** (`scripts/build_edition.py:341`)
  - **Fix:** In `scripts/build_edition.py`, change the guard at line 341 and the attribution read at line 346: Line 341: `if not isinstance(tup, tuple) or len(tup) < 8:` Line 346: `attribution = (tup[8] if len(tup) > 8 else "") or ""` This mirrors `NoteSpec.from_tuple` (config.py line 88-90) and `_iter_note_ref_ …
  - Evidence: ```python for tup in notes: if not isinstance(tup, tuple) or len(tup) < 9: continue ...attribution = tup[8] or "" ``` The minimum valid note tuple is 8 fields (the legacy form, used by the majority of the 67,715-note corpus during the migration phase). The guard `len(tup) < 9` silently skips every 8 …
- **[high] `_append_cloned_edition` omits `enabled_categories`, `enabled_kinds`, `disabled_kinds`, and `max_phase` — cloned edition ships 0 notes** (`scripts/api/editions.py:120-194`)
  - **Fix:** In _append_cloned_edition (scripts/api/editions.py), add the missing fields using the existing serialization patterns already in the function. For scalar fields, append to the scalar_fields list (around line 148, before the closing bracket): ("max_phase", src.get("max_phase", "")), ("enable_ai_notes …
  - Evidence: `_append_cloned_edition` copies only scalar and popup/tradition list fields (lines 135–189). It never copies `enabled_categories`, `enabled_kinds`, `disabled_kinds`, or `max_phase` from the source edition. `enabled_kind_codes` (the canonical resolver for both the matrix and the EPUB build) returns a …
- **[medium] time_filter_ceiling silently ignores all legacy 8-field notes (they always survive any temporal filter)** (`scripts/build_edition.py:341`)
  - **Fix:** Change line 341 in `scripts/build_edition.py` from: `if not isinstance(tup, tuple) or len(tup) < 9: continue` to: `if not isinstance(tup, tuple) or len(tup) < 8: continue` Change line 346 from: `attribution = tup[8] or ""` to: `attribution = (tup[8] if len(tup) >= 9 else "") or ""` This mirrors the  …
  - Evidence: In `_iter_note_ref_attribution_years`, line 341: `if not isinstance(tup, tuple) or len(tup) < 9: continue`. Any 8-field legacy note tuple (which has no attribution field) is silently skipped — it is never yielded, so it is never added to the `disabled_html_ref_ids` set inside `compute_time_filtered_ …
- **[medium] batch_insert_notes silently drops notes that fail coord_in_canonical_extent with no log or count** (`scripts/promote.py:354-358`)
  - **Fix:** The finder's fix is correct and safe. Concrete implementation to make it match the existing pattern precisely: In `batch_insert_notes` (C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/promote.py, inside the `for n in new_notes:` loop starting at line 342), add a `dropped` counter before th …
  - Evidence: In `batch_insert_notes`, lines 354-358: `if not coord_in_canonical_extent(book_path.stem, ch, v): continue # boundary guard: drop impossible coordinates` `_html_chs = html_chapter_count(book_path.stem)` `if _html_chs and ch > _html_chs: continue # base-HTML extent guard` Both guards silently `contin …
- **[info] run_naves_at_scale and run_xref_at_scale write candidate counts as candidates_written += len(chapter_candidates) but that counts pre-dedup candidates, not the number actually appended** (`scripts/run_naves_at_scale.py:117-121`)
  - **Fix:** In both `write_queue` functions, change the return type to return `len(new_dicts)` instead of `out_path`. For example in `run_naves_at_scale.py` line 85-86 and `run_xref_at_scale.py` line 82-83, replace `return out_path` with `return len(new_dicts)`. Then in the callers, change the guard to `if out: …
  - Evidence: In `run_naves_for_book`, lines 117-121: `out = write_queue(book, chapter, chapter_candidates) if out: files_written += 1 candidates_written += len(chapter_candidates)` `write_queue` returns `None` when all candidates deduplicate against the existing queue ("nothing new to add"). In that case `if out …

### cross-module [2]
- **[medium] Cyril plurality test at γ.4.8.E arc-close only checks 2 of 5 rival voices — incomplete-fix pattern missed in mint-9** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/tests/test_ethiopian_gamma4.py:8174-8197`)
  - **Fix:** In `test_cyril_remains_plurality_leader_at_meqabyan_arc_close` (line 8174), replace the two-rival hand-counted loop with the same Counter+_voice() pattern used in the γ.4.9.D upgrade at line 7014. Specifically: import Counter, define _voice() collapsing the five stems (Cyril of Alexandria, Athanasiu …
  - Evidence: ```python # test_cyril_remains_plurality_leader_at_meqabyan_arc_close (line 8174): cyril_count = 0 jubilees_count = 0 for verse_entries in self.ec._by_verse.values(): for e in verse_entries: if e.father == "Cyril of Alexandria": cyril_count += 1 elif e.father.startswith("Jubilees"): jubilees_count + …
- **[low] EBIBLE_VPL_TO_PROJECT book-code map absent from check_book_codes_canonical lint guard** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/lint_rules.py:1995-2012`)
  - **Fix:** In scripts/lint_rules.py after line 2011 (the OSIS_BOOK_TO_CODE entry), add one line to map_specs: ("scripts.extract_translation", "EBIBLE_VPL_TO_PROJECT"), With the comment: # mint-10 — translation-extraction backbone (KJV/Douay/Vulgate/Arabic/WEB); # a legacy alias here would silently misname a pe …
  - Evidence: The `check_book_codes_canonical` guard screens 11 book-code maps/lists for legacy-alias values (joh/ps/jas/mar/jol/ezk/nam/php). `scripts.extract_translation.EBIBLE_VPL_TO_PROJECT` — the single map that drives ALL translation-extraction pipelines (KJV, Douay, Vulgate, Arabic Van Dyck, WEB) — is not  …

### data-validity [2]
- **[medium] run_naves_at_scale.py and run_torrey_at_scale.py lack coord_in_canonical_extent guard — defense-in-depth gap lets impossible-chapter candidates be written** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/run_naves_at_scale.py:101-121`)
  - **Fix:** Add the import and guard to both files identically. The finder's proposed fix is correct and safe with one small improvement: place the import alongside the existing imports at the top of each file (not inside the function), and apply to both drivers. In C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH  …
  - Evidence: In run_naves_at_scale.py lines 101-121 (and the byte-identical loop in run_torrey_at_scale.py lines 120-141), the driver iterates `book_data.items()` — the `_verses` reverse index from the cached JSON — and calls `detector.detect(book, chapter, verse, ...)` with no `coord_in_canonical_extent` check: …
- **[low] Stale impossible-chapter candidate files in content/candidates/ from pre-guard Nave's run (5 confirmed)** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/content/candidates/1ch_ch_038.json:`)
  - **Fix:** Delete the five stale files directly. They are inert artifacts with no live consumer that bypasses the promote guard. The optional lint addition (`check_content.py` candidates_json check extended to validate chapter <= canonical_chapters(book)) is a reasonable improvement to prevent re-accumulation, …
  - Evidence: Five candidate JSON files have chapter numbers that exceed their book's canonical chapter count: `1ch_ch_038.json` (chapter=38, 1 Chr has 29), `1co_ch_035.json` (chapter=35, 1 Cor has 16), `1jn_ch_006.json` (chapter=6, 1 Jn has 5), `1sa_ch_034.json` (chapter=34, 1 Sam has 31), `1ti_ch_008.json` (cha …

### docs [2]
- **[info] REPO_MAP scripts/core/ count is 65, actual is 66 (manuscript_records.py unaccounted)** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/dev/REPO_MAP.md:41`)
  - **Fix:** Change `(65)` to `(66)` on line 41 of `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/dev/REPO_MAP.md`. No other change is needed; adding `manuscript_records.py` to the `etc.` list is optional and low-priority.
  - Evidence: Line 41 reads: `- **`scripts/core/`** (65) — the engine: ...`. A direct glob of `scripts/core/*.py` returns 66 files: the 65 previously counted plus `scripts/core/manuscript_records.py` (docstring: 'Witness-record schema + honesty-contract validator (Phase-2 Unit C)'). The Phase 6 doc-truth pass cor …
- **[info] docs/superpowers/INDEX.md and mint-9-fixes-plan.md Status both stale: claim 'Phases 3–6 in progress' when all 6 are complete** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/docs/superpowers/INDEX.md:13`)
  - **Fix:** In `docs/superpowers/plans/2026-05-31-mint-9-fixes-plan.md` line 3, replace the **Status:** value with: `COMPLETE — all Phases 1–6 shipped 2026-06-01 (b1a39485 + HEAD); byte-stability gate PASSED; lint 27✓/1warn/0fail. H4/H5 ex.py→exo + aes ch11–16 + M8 compresslevel deferred-by-design. NEXT = round …
  - Evidence: INDEX.md line 13 (In progress table): `EXECUTING 2026-05-31 — round-2 re-audit found 45 survivors; Phases 1–2 shipped, 3–6 in progress; then re-audit (round 3) to convergence`. The corresponding plan file `plans/2026-05-31-mint-9-fixes-plan.md` line 3 reads: `EXECUTING 2026-05-31 — Phase 1 (data-los …

### opt-build [5]
- **[low] filter_html ρ.1 path compiles O(N_ids × N_files) unique regexes — quadratic for tradition/time-filtered editions** (`YHWH v2.4/scripts/build_edition.py:1046-1062`)
  - **Fix:** Inside `filter_html` (build_edition.py around line 1044), replace the per-ID compile loop with a two-stage scan: ```python if disabled_html_ref_ids: # Stage 1: find which ref-ids actually appear in this file. # O(file_text) single scan — avoids O(N_disabled) re.compile() calls # for IDs that cannot  …
  - Evidence: ```python for ref_id in disabled_html_ref_ids: m_re = re.compile( rf'<a class="note-ref [^"]*" id="{re.escape(ref_id)}"[^>]*>.*?</a>', re.DOTALL, ) new_text, n = m_re.subn("", new_text) ... a_re = re.compile( rf'<aside class="note [^"]*" id="{re.escape(note_id)}"[^>]*>.*?</aside>', re.DOTALL, ) new_ …
- **[low] `_resolve_popup_languages` re-decodes `popup_languages_per_book` on every vnote aside — redundant O(n_entries) work per aside** (`YHWH v2.4/scripts/build_edition.py:955 (called from _process closure at line 946), 781`)
  - **Fix:** In `_apply_popup_languages_and_translation` (build_edition.py ~line 934), before `def _process(m):` is defined, add one line: _per_book_decoded = decode_per_book_languages(edition.get("popup_languages_per_book")) Then change `_resolve_popup_languages` to accept an optional pre-decoded dict: def _res …
  - Evidence: ```python def _resolve_popup_languages(edition: dict, book_code: str) -> set[str]: per_book = decode_per_book_languages(edition.get("popup_languages_per_book")) # line 781: re-decodes every call ... ``` `_process` (the regex substitution callback at line 946) calls `_resolve_popup_languages(edition, …
- **[low] `is_output_current` mtime-cache misses code changes to `matter_pages.py`, `epub_utils.py`, `kinds.yaml`, `categories.yaml`, `books.yaml`, `themes/*.css`** (`YHWH v2.4/scripts/build_edition.py:1953-1980`)
  - **Fix:** In is_output_current (build_edition.py lines 1966-1976), add the missing pipeline scripts and config files to the sources list after the existing entries: sources.append(REPO_ROOT / "scripts" / "matter_pages.py") sources.append(REPO_ROOT / "scripts" / "epub_utils.py") sources.append(REPO_ROOT / "con …
  - Evidence: ```python sources = list(EPUB_DIR.glob("*.html")) sources.append(EPUB_DIR / "content.opf") sources.append(EPUB_DIR / "nav.xhtml") sources.append(EPUB_DIR / "stylesheet.css") sources.append(REPO_ROOT / "content" / "editions.yaml") sources.append(REPO_ROOT / "scripts" / "build_edition.py") sources.ext …
- **[low] `build_cache._PIPELINE_SCRIPTS` omits `traditions.py` and `popup_versions.py` — code changes to these serve stale cached EPUBs** (`YHWH v2.4/scripts/core/build_cache.py:62-70`)
  - **Fix:** In `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/build_cache.py` at line 62, extend `_PIPELINE_SCRIPTS` to include the two core dependency files: ```python _PIPELINE_SCRIPTS = ( "build_edition.py", "matter_pages.py", "epub_utils.py", "resync_marker_glyphs.py", "build_epub.py", "sty …
  - Evidence: ```python _PIPELINE_SCRIPTS = ( "build_edition.py", "matter_pages.py", "epub_utils.py", "resync_marker_glyphs.py", "build_epub.py", "style_config.py", "inject.py", ) ``` `scripts/core/traditions.py` is imported at build time via `compute_tradition_disabled_html_ref_ids` → `note_tradition` (line 155  …
- **[info] OPTIMIZATION: Build pipeline is CONFIRM-OPTIMAL for all-9 loop except the ρ.1 filter fix; no new parallel/streaming/shared-base speedup available without breaking byte-stability** (`YHWH v2.4/scripts/build_edition.py:3161-3189`)
  - **Fix:** No action needed beyond the ρ.1 fix (finding #1). The build pipeline is correctly optimized for its constraints: cache-first fast path, parallel --all, deterministic compresslevel-9 zip, and a content-addressable cache that spans all 9 editions. Byte-stability proof obligation: any cache change must …
  - Evidence: Measured: single-edition build ~133s (mint-7 E3). The pipeline: `shutil.copytree` (~23 MB, fast) → per-file filter+renumber+vnote-pass+retitle (61 files) → canon filter → matter pages → OPF/nav/ncx patch → `build_epub.build()` (compresslevel=9 zip of ~23 MB, dominant cost). Five findings on current  …

### opt-ingest [3]
- **[medium] 4 write_queue drivers purge-and-replace own-kind candidates, silently discarding prior promotion status on re-run** (`YHWH v2.4/scripts/run_hebrew_at_scale.py:50-53`)
  - **Fix:** Apply the seen-set dedup pattern (already in run_xref, run_naves, run_torrey, run_kenyon, run_ethiopian from mint-9 #9) to all four missed drivers. In each write_queue, replace the kind-filter purge with: ```python existing: list[dict] = [] if out_path.exists(): try: existing = json.loads(out_path.r …
  - Evidence: In run_hebrew_at_scale.write_queue (line 50): `existing_candidates = [c for c in existing.get('candidates', []) if c.get('kind') != 'lang-hebrew']` — then new dicts are produced by `candidate_to_dict` which hardcodes `status: 'pending'` (at_scale_base.py:143). A re-run strips every prior lang-hebrew …
- **[low] build_edition._iter_note_ref_traditions uses :02d for chapter in ref_id, which diverges from the actual HTML id for chapters >= 100** (`YHWH v2.4/scripts/build_edition.py:154, 352, 2672`)
  - **Fix:** In `build_edition.py` at line 341–346, change: ```python if not isinstance(tup, tuple) or len(tup) < 9: continue ch = tup[0] vs = tup[1] suffix = tup[2] or "" attribution = tup[8] or "" ``` to: ```python if not isinstance(tup, tuple) or len(tup) < 8: continue ch = tup[0] vs = tup[1] suffix = tup[2]  …
  - Evidence: inject.py:730 writes `full_id = f"{prefix}{ch:02d}{v:02d}{suffix or ''}"` — which for ch=100 produces `prefix10001suffix` (correct, since `:02d` pads but does not truncate). build_edition.py:154 writes `ref_id = f"ref-{prefix}{ch_i:02d}{vs_i:02d}{suffix}"` — same `:02d` format. These are consistent. …
- **[low] batch_promote_xrefs.promote_by_book does not update candidate status to 'promoted' in the queue JSON after insertion** (`YHWH v2.4/scripts/batch_promote_xrefs.py:55-90`)
  - **Fix:** In `promote_by_book`, after the per-book insert loop completes, add a second pass over the original `files` list. For each file, reload its JSON, mark as `'promoted'` any candidate whose `(chapter, verse, kind, body)` tuple was included in the `by_book` batch that was successfully inserted (track in …
  - Evidence: The `promote_by_book` fast path (lines 55-90) inserts notes into `<book>.py` via `batch_insert_notes` but never calls `update_queue_status` to mark the candidates as `promoted` in the `content/candidates/<book>_ch_NNN.json` queue files. The slow `--per-candidate` path (line 149 via `promote_candidat …

### opt-render [4]
- **[low] lint_rules.check_render_coverage_no_regression uses legacy code 'ex' in both expected sets — will false-FAIL on the correct τ.G rename** (`YHWH v2.4/scripts/lint_rules.py:977, 1006`)
  - **Fix:** At τ.G, in the same commit that renames ex.py → exo.py in all four translation dirs (geez-tewahedo, amharic-tewahedo, geez-tewahedo-en, amharic-tewahedo-en), also update C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/lint_rules.py line 977 and line 1006: change "ex" to "exo" in both expec …
  - Evidence: expected_geez (line 977) and expected_amharic (line 1006) both contain `"ex"`. The check compares these against `{p.stem for p in ed_dir.glob('*.py')}`, so it passes today because `ex.py` exists. When `ex.py` is correctly renamed to `exo.py` at τ.G, the check will report `"ex"` as missing and FAIL — …
- **[low] _iter_note_ref_attribution_years silently skips 8-field (unattributed) notes — they escape time_filter_ceiling entirely** (`YHWH v2.4/scripts/build_edition.py:341`)
  - **Fix:** At build_edition.py line 341-346, change: if not isinstance(tup, tuple) or len(tup) < 9: continue ch = tup[0] vs = tup[1] suffix = tup[2] or "" attribution = tup[8] or "" to: if not isinstance(tup, tuple) or len(tup) < 8: continue ch = tup[0] vs = tup[1] suffix = tup[2] or "" attribution = tup[8] if …
  - Evidence: `_iter_note_ref_attribution_years` at line 341: `if not isinstance(tup, tuple) or len(tup) < 9: continue`. An 8-field (unattributed) note is never yielded, so it is never added to the `out` set in `compute_time_filtered_html_ref_ids` regardless of the edition's `time_filter_ceiling`. The docstring a …
- **[info] render_coverage.run_all() omits geez-tewahedo-en and amharic-tewahedo-en — EN back-translation lane coverage is invisible to preflight** (`YHWH v2.4/scripts/render_coverage.py:239-241`)
  - **Fix:** In `scripts/render_coverage.py` lines 239-258, extend the loop and summary block: Loop (line 240): change the tuple to `("kjv", "geez-tewahedo", "amharic-tewahedo", "geez-tewahedo-en", "amharic-tewahedo-en")`. Summary block (lines 243-256): add two keys after the existing ones: `"geez_tewahedo_en_re …
  - Evidence: Lines 239-241: `run_all()` scans only `('kjv', 'geez-tewahedo', 'amharic-tewahedo')`. The `geez-tewahedo-en` dir has 7 books (gen, ex, lev, 1ki, 1sa, 2sa, psa) and `amharic-tewahedo-en` has 3 books (gen, ex, lev) — both active per REPO_MAP. These are entirely absent from the coverage summary and fro …
- **[info] OPTIMIZATION: render_coverage + build_standalone lanes — CONFIRM-OPTIMAL; standalone EN back-translation lane — CONFIRM-OPTIMAL** (`YHWH v2.4/scripts/render_coverage.py:`)
  - **Fix:** No changes needed for algorithmic correctness or architecture. The one improvement available (adding -en dirs to run_all) is reported separately as a low-severity finding.
  - Evidence: render_coverage.py: The _CANONICAL_BOOKS list is correct (87 books in canonical order), _BOOK_ALIASES correctly maps legacy stems, _calibration_count correctly reads manifest.yaml via yaml.safe_load (integer chapter keys parsed correctly), and _per_edition_report correctly classifies marathon_pendin …

### opt-vision [3]
- **[medium] web_helpers.html_ref_id_from_note_id silently returns None for Strategy-B books (missing bxx fallback), while every other ref-id builder applies the bxx fallback** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/web_helpers.py:262-265`)
  - **Fix:** At web_helpers.py:262, change `prefix = book.get("id_prefix")` to `prefix = book.get("id_prefix") or book.get("bxx")`. This is a one-line change that mirrors the identical pattern at build_edition.py:139, 336, 2666 and inject.py:691, all of which carry explicit comments that the bxx fallback is requ …
  - Evidence: Lines 262-265: `prefix = book.get('id_prefix'); if not prefix: return None`. All other ref-id builders in the codebase apply the Strategy-B bxx fallback: `build_edition.py:139` `prefix = book.get('id_prefix') or book.get('bxx')`, `build_edition.py:336` same, `build_edition.py:2666` same, `inject.py: …
- **[low] corpus_index.connection() has an unguarded TOCTOU that leaks a SQLite connection per concurrent request pair** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/corpus_index.py:659-680`)
  - **Fix:** Add a module-level `_CONN_LOCK = threading.Lock()` (add `import threading` if not already present — it is not currently imported in corpus_index.py). Wrap both the path-mismatch close block AND the None-check-and-create block together in a single `with _CONN_LOCK:` in `connection()`, so the entire r …
  - Evidence: Lines 669-679: `if _CACHED_CONN is None: _CACHED_CONN = sqlite3.connect(current_path, check_same_thread=False)`. Under ThreadingHTTPServer two worker threads can both pass the `_CACHED_CONN is None` check simultaneously, both call `sqlite3.connect()`, and one connection is stored in `_CACHED_CONN` w …
- **[info] CONFIRM-OPTIMAL: Vision-transcription marathon method (Patrologia Esther + Kings/Samuel) is still optimal under today's constraints** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/content/translations/sources/patrologia/_vision_notes.md:`)
  - **Fix:** Confirmed optimal: maintain MAX-1 heavy vision agent, tight ≤1568px crops, per-step commits, 3-pass convergence. Optional additive: create scripts/recover_vision_transcript.py that implements the note-(n) recovery procedure (walk .claude/projects/.../subagents/*.jsonl, find tool_use with name==Struc …
  - Evidence: The OOM crashes (documented in _vision_notes.md (n), plan 2026-05-17-kings-manuscript-collation.md crash-fix section) were caused by stacked heavy agents with whole-folio upscale buffering ~400k parent tokens — not context window limits. Today's 1M-context models (Sonnet 4.6/Opus 4) do NOT change th …

### security [4]
- **[low] WLC trusted-HTML ingest missing the <>&-character guard present on sibling sources** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/extract_wlc_morphhb.py:216-235`)
  - **Fix:** The guard must check the raw word/segment text BEFORE HTML assembly, not the finished `<em>`-wrapped string. In `verse_to_em_html`, add a helper that validates individual token text (before the `<em>` wrap): ```python def verse_to_em_html(verse_el: ET.Element) -> str: ... for child in verse_el: tag  …
  - Evidence: The `write_book_module` function writes WLC verses that are passed raw (unescaped) into the EPUB because `popup_versions._TRUSTED_HTML` contains `"wlc"`. Both sibling extractors that also live in `_TRUSTED_HTML` enforce an ingest-time guard: `extract_lxx_swete.py` lines 164-170 and `extract_byzantin …
- **[low] Server error messages containing user-supplied field values reflected into innerHTML without escaping** (`scripts/templates/customize.py:1364, 1441, 1791`)
  - **Fix:** Apply escaping consistently at all unescaped innerHTML-error sites — both the ones the finder listed and the two additional sites in matrix_app.js that follow the same pattern: 1. C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/templates/customize.py L1364: `${result.error}` → `${escapeAtt …
  - Evidence: Line 1364: `status.innerHTML = \`<span class="text-red-600">✗ ${result.error}</span>\`` and line 1441: `status.innerHTML = \`<span class="text-red-600">✗ ${msg}</span>\`` where `msg = data.error`. Server-side handlers in `scripts/api/editions.py` generate error strings that embed user-supplied value …
- **[low] Corpus-sourced note fields (suffix, anchor) interpolated into innerHTML without HTML-escaping in audit and sources consoles** (`scripts/templates/audit.py:234`)
  - **Fix:** The finder's proposed fix is correct and safe. Apply it as written: In `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/templates/audit.py` line 234, change: `${n.book} ${n.chapter}:${n.verse}${n.suffix || ''}` to: `${n.book} ${n.chapter}:${n.verse}${n.suffix ? escapeHTML(n.suffix) : ''}`  …
  - Evidence: Line 234 in renderIssues(): `<span class="verse-anchor text-xs text-slate-500">${n.book} ${n.chapter}:${n.verse}${n.suffix || ''}</span>` — `n.suffix` comes from corpus notes tuple field[2] via `api_attribution_audit` → `_compute_attribution_audit_uncached`. The `escapeHTML` function is defined in t …
- **[info] version parameter in api_export_build is unvalidated before being passed to subprocess and embedded in glob pattern** (`scripts/api/exports.py:120-165`)
  - **Fix:** At the top of `api_export_build` in `C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/api/exports.py`, after the `edition_id` check at line 144, add: ```python from scripts.core.snapshots import _validate_version try: _validate_version(version) except ValueError: return {"error": "invalid v …
  - Evidence: `api_export_build(edition_id, version)` at line 120 accepts `version` from `payload.get('version', 'v28a')` with no validation. Line 165: `'--version', version` passes it as a CLI argument (list form — no shell injection). Line 225: `pattern = f"Ethiopian_Bible_{edition_id}_{version}_*.epub"` — a `v …

### tests [2]
- **[medium] Cyril absolute-count ceiling (≤700) leaves only 32 entries headroom — future authorized Cyril expansion will cause spurious test failures** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/tests/test_ethiopian_gamma4.py:1171-1183`)
  - **Fix:** Remove the ceiling assertion entirely from `test_voice_rebalance_achieved` (lines 1171-1183 of tests/test_ethiopian_gamma4.py) and rely solely on the durable plurality-leader guard (`test_cyril_remains_plurality_leader_at_arc_close` at line 7014). The ceiling is an anti-pattern per RULES §8.1 (which …
  - Evidence: ```python assert len(cyril) <= 700, f"γ.4.2 wave-1 Cyril ceiling: expected ≤700 Cyril entries; found {len(cyril)}" ``` The comment on line 1180 states 'Live count at pin-time (2026-05-31) = 668', leaving only 32 entries before the ceiling trips. RULES §1 explicitly authorizes future Cyril expansion  …
- **[low] TestStrayArtifacts.test_flags_junk is not state-aware: accepts 'warn' even though _ENFORCE_STRAY_ARTIFACTS is True** (`C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/tests/test_lint_guardrails.py:231-239`)
  - **Fix:** Replace `assert r["status"] in {"warn", "fail"}` with the state-aware pattern consistent with the sibling tests: ```python assert r["status"] == ("fail" if lint_rules._ENFORCE_STRAY_ARTIFACTS else "warn") ``` This matches the exact pattern at lines 148, 178, 216 of the same file and would correctly  …
  - Evidence: ```python def test_flags_junk(self, monkeypatch): monkeypatch.setattr( lint_rules, "_git_candidate_files", lambda: ["scripts/x.py", "scratch.tmp", "dev/notes.bak"] ) r = lint_rules.check_no_stray_artifacts() # FAIL-tier once the tree is verified clean (_ENFORCE_STRAY_ARTIFACTS); # WARN beforehand. T …

### tests-run [8]
- **[medium] Stale test: test_delete_table_has_eight_entries_now expects future routes** (`tests/test_web_routetable.py:1068`)
  - **Fix:** In tests/test_web_routetable.py, update test_delete_table_has_eight_entries_now as follows: (1) change the assertion on line 1068 from `assert len(web._DELETE_ROUTES) == 8` to `assert len(web._DELETE_ROUTES) == 6`; (2) remove or comment out lines 1071-1072 that assert /distribution/ and /license/ pa …
  - Evidence: assert len(web._DELETE_ROUTES) == 8 but actual count is 6. Test comment references ε.6 (distribution/<edition>/<channel>) and ξ.26 (license/<edition>) routes that don't exist. These are future phases not yet implemented.
- **[medium] Test checking for deleted commercial module still fails** (`tests/test_omega0_free_public_pivot.py:1`)
  - **Fix:** In `tests/test_omega0_free_public_pivot.py` lines 270-276, remove `"scripts/api/distribution.py"` from the `REMOVED` tuple. The file is a live, non-commercial mint-6 API module (archive.org free-distribution rollup, backed by `scripts/core/distribution.py`) that post-dates Phase 4 decommercialize. I …
  - Evidence: FAILED test_deleted_modules_gone[scripts/api/distribution.py] — Test expects scripts/api/distribution.py to be deleted, but file exists or test is checking wrong location.
- **[medium] Audit log chain verification fails on pre-ξ.17 records** (`tests/test_security_xi_late.py:1176`)
  - **Fix:** The bug is in `_last_line_hash` (C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/audit_log.py, lines 104-123), not in `verify_chain`. When the target monthly file does not exist yet (month rollover), `_last_line_hash` must look back at the most recently dated prior `*.ndjson` file in  …
  - Evidence: assert result["status"] == "ok" but got "broken". Test creates a pre-ξ.17 ndjson line (without prev_hash) then appends a new line. The verify_chain function reports broken instead of ok.
- **[low] Unwhitelisted lru_cache in inject.py without clear path** (`scripts/inject.py:610`)
  - **Fix:** Add to scripts/.cache_audit_whitelist.py under the "Read-once singletons over immutable manuscript / canonical data" block (or a new "Pure-function compiled-regex caches" block): _._aside_existing_re # scripts/inject.py — compiled regex from deterministic formula over `prefix`; pure function, no I/O …
  - Evidence: @functools.lru_cache(maxsize=256) def _aside_existing_re(prefix: str) -> re.Pattern: ... — No cache_clear() site found in codebase, and not in whitelist.
- **[low] Stale test: test_multipart_table_has_four_entries expects future routes** (`tests/test_web_routetable.py:1195`)
  - **Fix:** In tests/test_web_routetable.py: (1) change line 1195 from `assert len(web._MULTIPART_ROUTES) == 4` to `assert len(web._MULTIPART_ROUTES) == 3`; (2) remove the sales/import line from the class docstring (line 1188: `- /api/sales/import/<channel> → api_sales_import (ε.3)`); (3) update the comment at  …
  - Evidence: assert len(web._MULTIPART_ROUTES) == 4 but actual count is 3. Test comment references ε.3 (sales/import/<channel>) route not yet implemented.
- **[low] Stale test: test_put_table_has_twelve_entries expects future routes** (`tests/test_web_routetable.py:1410`)
  - **Fix:** In tests/test_web_routetable.py at line 1410, change the assertion to `assert len(web._PUT_ROUTES) == 10` and update the comment on lines 1406-1407 to reflect the current state: "6 from A.5 + 3 from A.10 + 1 from ε.7 (press-kit save) = 10. ε.6 (distribution mark) and ξ.26 (license set) not yet imple …
  - Evidence: assert len(web._PUT_ROUTES) == 12 but actual count is 10. Test comment references ε.6 (distribution mark), ε.7 (press-kit save), and ξ.26 (license set) routes not yet implemented.
- **[low] Multiple stale scope document tests fail with missing files** (`tests/test_parallel_bible_tau6x0b.py:1`)
  - **Fix:** In all 7 test files, change the path from `REPO / "dev" / "SCOPE_2026-05-14-parallel-bible.md"` to `REPO / "dev" / "archive" / "SCOPE_2026-05-14-parallel-bible.md"`. The document was archived but the tests were not updated to follow it. The content fully satisfies every assertion already — only the  …
  - Evidence: 8 test failures in TestTau6x0bScopeDecisionBlock checking for scope doc sections that don't exist: test_scope_doc_exists, test_decision_block_present, test_option_d_authorized, test_authorized_date_recorded, test_tesseract_engine_choice_recorded, test_tesseract_not_installed_verification_recorded, t …
- **[low] Test for url_override not working in sources cache fetch** (`tests/test_scripts.py:8066`)
  - **Fix:** The defect is entirely in the test, not in the implementation. In `tests/test_scripts.py` line 8063, change the non-allowlisted URL to one whose host is in `DEFAULT_PD_SOURCES_ALLOWLIST`: Before: `url_override="https://my-mirror/example.json",` After: `url_override="https://ebible.org/example.json", …
  - Evidence: assert seen_urls == ["https://my-mirror/example.json"] but got []. The url_override parameter is not being passed through to the stub_fetch function, suggesting the implementation doesn't handle the override.

## Refuted (13)
- **[medium] _chapter_from_id parses IDs using parts[-3] which is wrong for multi-part book codes containing hyphens** (correctness, 1/1 refute) — I read the actual code at: - `scripts/promote.py` lines 494-497 (`_chapter_from_id`) and line 435 (the call site) - `scripts/core/at_scale_base.py` lines 125-143 (`candidate_to_dict`) - `scripts/core/detectors.py` lines  …
- **[medium] apply_chapter_decoration and apply_bilingual_toc write to temp-dir files without atomic_write, risking corrupt EPUB on build interruption** (correctness, 1/1 refute) — I read all three cited lines (1588, 1733, 1906) and the surrounding context in build_edition.py. The facts are: 1. All three `fpath.write_text(...)` calls operate on files inside `Path(tmpdir) / "build"` — a directory cr …
- **[low] audit_log._summarize_args redacts sensitive kwargs by name but leaves positional args unredacted** (security, 1/1 refute) — I read the actual code at the cited lines (274-295) and the wrapper at 329-364. The finding's description of the code is accurate: positional args use `_short_repr(a)` without key-based redaction, while kwargs apply `_RE …
- **[low] Content-Disposition filename in export download route sets unescaped URL-captured filename in attachment header** (security, 1/1 refute) — Verified by reading the actual code at both layers. Route regex (web.py line 1570): `[\w.-]+` captures the URL segment. The captured group is passed to `api_download_export` (scripts/api/exports.py line 400). That functi …
- **[low] test_audit_clean_state_on_real_tree carries a stale hardcoded count comment ('23 cached functions') — the actual decorator count is now ~37** (tests, 1/1 refute) — I read the test at lines 181-199 and the audit_caches.py script in full. The stale comment is confirmed real: lines 182-185 say "23 cached functions (15 with clear paths + 8 whitelisted)" but the actual decorator count i …
- **[medium] MATRIX_MAP variable-trace table claims popup_translation is 'OK' but _replace_verse_popup_translation is explicitly NOT YET WIRED INTO THE BUILD** (docs, 1/1 refute) — I read MATRIX_MAP.md line 107 and build_edition.py lines 610-629 and 917-956 and 2709-2904. The finding claims `popup_translation` has no production caller. This is incorrect. The field IS consumed in production: build_e …
- **[high] Lint rules tests fail: ALL_CHECKS count mismatch** (tests-run, 2/2 refute) — I read ALL_CHECKS in C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/lint_rules.py lines 2183–2223 directly. Counting every key entry ("6.1", "6.2", "encode_decode", "docs", "repo_map_complete", "superpowers_co …
- **[medium] Test for defaults_used_when_unset fails in publisher console** (tests-run, 1/1 refute) — I read the test at test_scripts.py:5495-5502 and the full implementation chain: api_publisher_data (web_sources.py:362-366) -> _cached_publisher_data -> _compute_publisher_data_uncached (web_sources.py:369-391). The logi …
- **[low] Test error in test_work_cache.py::test_persists_to_disk** (tests-run, 1/1 refute) — Read both the test (C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/tests/test_work_cache.py, lines 51-60) and the implementation (C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/core/work_cache.py). The test …
- **[low] check_render_coverage_no_regression in lint_rules.py checks raw .stem values but the expected sets still contain the legacy code "ex" — will silently break when ex.py is renamed to exo.py without a paired expected-set update** (opt-vision, 1/1 refute) — Both `geez-tewahedo/ex.py` and `amharic-tewahedo/ex.py` confirmed present on disk (Glob results). The lint check at lint_rules.py:1042–1043 scans actual file stems and compares against the expected sets; `"ex"` is in bot …
- **[low] run_hebrew_at_scale and run_greek_at_scale: main() builds book list by globbing kjv/ directly instead of using resolve_books, risking non-canonical ordering and bypassing the available-book filter** (opt-ingest, 1/1 refute) — I read the cited code at run_hebrew_at_scale.py lines 138-143 and run_greek_at_scale.py lines 135-140, and confirmed the glob+sorted pattern exists exactly as described. I also read at_scale_base.resolve_books() (lines 1 …
- **[info] OPTIMIZATION: ingest pipeline is CONFIRM-OPTIMAL — no unified-driver replacement is warranted** (opt-ingest, 1/1 refute) — I read at_scale_base.py (lines 1-16, 7-9 specifically) and all 12 run_*_at_scale.py driver files. The finding's primary claim is "confirmed optimal — no unified-driver replacement is warranted," but the finding simultane …
- **[low] render_coverage.py _calibration_count defers 'import yaml' inside the function body — executed up to 4 times per run_all()** (opt-render, 1/1 refute) — I read C:/Users/bogda/Documents/YHWH-v2.4-full/YHWH v2.4/scripts/render_coverage.py lines 180-199 and lines 35-40. The factual claims are all accurate: `import yaml` is at line 188 inside `_calibration_count`, and all ot …

## Completeness gaps (12) — RECORDED ONLY
> mint-10 is the LAST audit round (no mint-11 until project end). These are logged for provenance, NOT actioned this round.
- **versification.py — vulgate_to_kjv / lxx_swete_to_kjv segment-table correctness and the _vg_verify gate** — For each translation with a non-identity adapter (wlc, lxx-swete, byzantine, douay, vulgate, arabic), run: 1) the coord_in_canonical_extent gate counts (confirm no verses are silently dropped above th …
- **generate_verse_popups.py — bake path interaction with popup_versions.normalize_coord stub** — Grep for `normalize_coord` across the entire codebase to find every call site; verify that each site either intentionally uses the stub or routes through the actual per-version adapter in versificatio …
- **_append_cloned_edition drops enabled_categories, enabled_kinds, disabled_kinds, max_phase — no recovery path and no test for the zero-note scenario** — In tests: (1) call _append_cloned_edition on an edition with non-empty enabled_categories and assert the cloned record's enabled_categories survives a round-trip through config.editions_by_id(); (2) v …
- **extract_translation.py EBIBLE_VPL_TO_PROJECT map is absent from check_book_codes_canonical lint guard** — Glob scripts/extract_*.py and grep each for a dict mapping uppercase or mixed-case VPL/OSIS/SBL codes to project codes (pattern: uppercase key → lowercase value). For each found map, check whether it  …
- **batch_insert_notes silent-drop at coord_in_canonical_extent — no logging when dropping, no count returned to caller** — For each of the 8 at-scale drivers (run_xref, run_hebrew, run_greek, run_kenyon, run_ethiopian, run_ai_notes, run_ai_xrefs, run_torrey/naves already confirmed), check whether their run_*_for_book / in …
- **scripts/api/ surface — api_export_build version parameter injection into subprocess command** — Grep scripts/api/ for subprocess.run and for EXPORTS_DIR.glob, and for any pattern where a user-supplied string from an HTTP payload is interpolated into a shell command, a glob pattern, or a filesyst …
- **corpus_index.py connection() TOCTOU / connection-leak — the confirmed surviving finding has a sibling in the _CACHED_CONN module-level pattern** — Grep for _CACHED_CONN and for module-level singletons pattern `_X = None` followed by an if-check-then-set outside a lock, across all scripts/core/*.py files. Also grep tests/ for _disable_corpus_inde …
- **promote_by_book in batch_promote_xrefs — does not update candidate JSON status to 'promoted' after insertion** — Write a test that: (1) runs promote_by_book on a fixture candidate file, (2) verifies the inserted note exists, (3) runs promote_by_book again on the same file, (4) asserts no duplicate note was added …
- **lint_rules.py ALL_CHECKS count pin and the test_guard_self.py contract — exact count may have drifted since mint-9 fixes shipped** — Run `python scripts/lint_rules.py --json` and compare the count of entries in ALL_CHECKS against the TestAllChecksMetaContract pin. If there is a mismatch, identify which check was added without updat …
- **scripts/core/sources_commentary.py and sources_lexicon.py — the god-module split re-export hubs: no test that all re-exported names survive ruff F401 fix attempts** — Check whether test_at_scale_wiring.py or any other test verifies that `from scripts.core.sources import X` works for each name in sources.__all__ (or equivalently each noqa: F401 import). If no such t …
- **content/candidates/*.json — stale impossible-chapter files confirmed at 5 sites; no lint guard blocking re-introduction** — Write a lint check or one-shot script that walks content/candidates/*.json, parses each candidate's (book, chapter, verse), and calls coord_in_canonical_extent on each. Report every file with at least …
- **The _iter_note_ref_attribution_years / time_filter_ceiling interaction — the PATTERN of 8-field (legacy) notes escaping filters may extend to other filter paths beyond time_filter_ceiling** — Grep scripts/ for `tup[8]`, `t[8]`, `len(tup) < 9`, `len(tup) == 9`, `note_attribution`, and `attribution` in the context of note-tuple iteration. For each site, verify that 8-field tuples are either  …
