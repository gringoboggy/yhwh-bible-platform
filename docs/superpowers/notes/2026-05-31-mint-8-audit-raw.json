{
 "tool": "deep-audit",
 "round": 1,
 "now": "unstamped",
 "depth": "deep",
 "dimensions": [
  {
   "key": "correctness",
   "kind": "find",
   "finders": 2
  },
  {
   "key": "security",
   "kind": "find",
   "finders": 2
  },
  {
   "key": "code-debt",
   "kind": "find",
   "finders": 1
  },
  {
   "key": "tests",
   "kind": "find",
   "finders": 1
  },
  {
   "key": "docs",
   "kind": "find",
   "finders": 1
  },
  {
   "key": "byte-stability",
   "kind": "find",
   "finders": 2
  },
  {
   "key": "data-validity",
   "kind": "find",
   "finders": 1
  },
  {
   "key": "concurrency-caching",
   "kind": "find",
   "finders": 1
  },
  {
   "key": "cross-module",
   "kind": "find",
   "finders": 2
  },
  {
   "key": "marathon-boundary",
   "kind": "guard",
   "finders": 1
  },
  {
   "key": "opt-vision",
   "kind": "optimization",
   "finders": 1
  },
  {
   "key": "opt-build",
   "kind": "optimization",
   "finders": 1
  },
  {
   "key": "opt-ingest",
   "kind": "optimization",
   "finders": 1
  },
  {
   "key": "opt-render",
   "kind": "optimization",
   "finders": 1
  }
 ],
 "counts": {
  "deduped": 70,
  "survived": 57,
  "refuted": 13,
  "bySeverity": {
   "high": 6,
   "medium": 20,
   "low": 26,
   "info": 5
  }
 },
 "survivors": [
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "high",
   "title": "find_aside_insertion_point regex `(\\d{2})(\\d{2})` cannot parse aside IDs for chapters >= 100 (1 Enoch chapters 100-108, Strategy-A)",
   "file": "YHWH v2.4/scripts/inject.py",
   "line": "606-608",
   "evidence": "```python\nexisting_re = re.compile(\n    r'<aside\\s+class=\"note\\s+[^\"]+\"\\s+id=\"note-' + re.escape(prefix) + r'(\\d{2})(\\d{2})([a-z]?)\"',\n)\n```\n1 Enoch (code `1en`, id_prefix `1e`, Strategy-A) has 108 chapters. inject.py builds aside IDs via `full_id = f\"{prefix}{ch:02d}{v:02d}{suffix or ''}\"` — `{100:02d}` = `'100'` (3 digits). The regex expects exactly `\\d{2}` (2 digits) for chapter, so `id=\"note-1e10001\"` (ch=100,v=01) does not match. The fallback is `insertion = inside_start` for every note scanned, placing ALL re-injected 1En 100+ notes at the section head rather than their sorted positions.",
   "fix": "Change `(\\d{2})(\\d{2})` to `(\\d+)(\\d{2})` so the chapter group is greedy (one or more digits) while the verse group remains exactly 2 digits. The parse of `10001` then yields `existing_ch=100, existing_v=01` correctly. The wider `\\d+` is safe because note IDs always end with a 2-digit verse and an optional letter.",
   "verifierFix": "In `scripts/inject.py` line 607, change `r'(\\d{2})(\\d{2})([a-z]?)\"'` to `r'(\\d+)(\\d{2})([a-z]?)\"'`.\n\nThe same fix must also be applied to `scripts/coverage.py` line 144, which has an identical `(\\d{2})(\\d{2})` pattern used to count injected notes per chapter — it will silently undercount all notes in 1 Enoch chapters 100–108 (reporting them as missing rather than present), giving a false coverage deficit. The finder's proposed fix is correct for inject.py; extend it to coverage.py.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading the code:\n\n1. `C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\inject.py` line 704: `full_id = f\"{prefix}{ch:02d}{v:02d}{suffix or ''}\"`. Python's `{100:02d}` produces `'100'` (minimum-width 2, no truncation), so ch=100 yields a 3-digit string.\n\n2. Line 607: `r'(\\d{2})(\\d{2})([a-z]?)\"'` — exactly 2 digits for chapter, 2 for verse. Against the string `10001\"` (ch=100, v=01, no suffix): the regex engine tries `\\d{2}`=`10`, `\\d{2}`=`00`, `[a-z]?`=`\"\"`, then expects `\"` but finds `1`. No match. The aside for ch=100 is never found by `finditer`, so the insertion-point logic always falls through to the `inside_start` default, placing all re-injected 1En ch100+ notes at the section head instead of their sorted positions.\n\n3. `content/books.yaml` lines 195–203 confirms: 1 Enoch has `code: 1en`, `id_prefix: \"1e\"`, `strategy: \"A\"`, `ch_count: 108` — so chapters 100–108 are real and Strategy-A is active.\n\n4. KJV byte-stability is unaffected: Psalms (ch_count 151) uses `strategy: \"B\"` — it never reaches `find_aside_insertion_point`. No other Strategy-A book has ch_count >= 100. The 9 KJV editions do not include 1 Enoch.\n\n5. The proposed fix `(\\d+)(\\d{2})` is correct: greedy `\\d+` backtracks to give the verse group its required 2 digits. For a normal 2-digit chapter ID like `0503\"`, `\\d+` grabs `05`, `\\d{2}` grabs `03` — correct. For `10001\"`, `\\d+` resolves to `100`, `\\d{2}` to `01` — correct.\n\n6. The same `(\\d{2})(\\d{2})` pattern exists verbatim at `scripts/coverage.py:144` and will silently undercount notes in 1En chapters 100–108. The fix should be applied there as well.\n\nSeverity is accurate: this causes incorrect note ordering (all ch100+ notes land at section head) during re-inject passes on 1 Enoch. It is a real correctness defect scoped to a single book."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All three key facts independently confirmed by reading the actual files:\n\n1. C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\content\\books.yaml line 200-202: `ch_count: 108`, `strategy: \"A\"`, `id_prefix: \"1e\"` for 1 Enoch — confirmed exactly as the finder states.\n\n2. C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\inject.py line 704: `full_id = f\"{prefix}{ch:02d}{v:02d}{suffix or ''}\"` — Python's `:02d` zero-pads to minimum 2 digits but does NOT truncate. `{100:02d}` = `\"100\"` (3 chars), so a ch=100 note generates ID `\"1e10001a\"`.\n\n3. inject.py line 607: `r'(\\d{2})(\\d{2})([a-z]?)\"'` — the chapter group is exactly `\\d{2}`. This regex will NOT match `id=\"note-1e10001a\"` because the `(\\d{2})` group can only consume 2 digits, leaving `\"0\"` before the verse group starts, which breaks the parse entirely. The regex yields no match; `insertion` stays at `inside_start` (line 610), placing re-injected notes at the section head instead of their sorted positions.\n\nBlast radius check: C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\content\\notes\\1en.py — the last entry in the 4660-line notes corpus is chapter 94. No notes for chapters 100–108 currently exist. This means the bug is latent today — zero current mis-output, zero KJV edition impact.\n\nThe proposed fix `(\\d+)(\\d{2})` is correct and safe: `\\d+` greedily consumes the chapter digits (1–3 digits), leaving exactly 2 digits for the verse group. Since verse IDs are always `{v:02d}`, the parse remains unambiguous. No existing note ID is affected (all current chapters <= 94 = exactly 2 digits, so `\\d+` and `\\d{2}` match identically for them). The fix touches only a local regex in `find_aside_insertion_point`, is additive, and cannot affect any currently-built output.\n\nSeverity recalibration: the finder called this \"high.\" It is a real structural defect — a format mismatch between the ID builder (line 704) and the ID parser (line 607) for a book with 108 chapters — but it is currently latent (no ch>=100 notes exist). It will become active the moment any 1 Enoch ch 100-108 note is added, which is on the roadmap (Phase D1b marathon). Medium severity is correct: real bug, zero current blast radius, definite near-future relevance."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "high",
   "title": "write_book serialises attribution as {} (empty dict) instead of omitting the 9th field, corrupting the notes schema",
   "file": "YHWH v2.4/scripts/web_helpers.py",
   "line": "131, 146, 174, 182",
   "evidence": "tuple_to_dict (line 131): `\"attribution\": attribution or {}` converts None/empty to `{}`. dict_to_tuple (line 146): `d.get(\"attribution\") or {}` returns `{}` when the value is `{}`. write_book (line 174): `attribution = rest[0] if rest else {}` then line 182: `lines.append(f\"        {attribution!r},\\n\")` writes the literal `{}` into the .py file as a 9th tuple field. The canonical schema (config.py NoteSpec docstring) expects attribution to be `str | None`; an 8-field tuple with no attribution is the correct representation for notes without provenance. After a round-trip through the web UI, every previously-8-field note becomes a 9-field tuple with `{}` as the 9th field. Downstream consumers that do `isinstance(tup[8], str)` (validate_taxonomy.py line 178, dashboard.py line 139, traditions.py line 114) all get `False`, so the note is silently reported as unattributed even though it now has a 9th field. The build_edition._iter_note_ref_attribution_years does `tup[8] or \"\"` which is safe for an empty dict (falsy), but any code doing `.strip()` on a non-empty dict attribution would crash.",
   "fix": "In tuple_to_dict change `attribution or {}` to `attribution or \"\"` (line 131). In dict_to_tuple change `d.get(\"attribution\") or {}` to `d.get(\"attribution\") or None` (or `\"\"`) (line 146). In write_book only emit the attribution line when it is a non-empty string: `if isinstance(attribution, str) and attribution.strip(): lines.append(f\"        {attribution!r},\\n\")` — otherwise omit it, keeping the tuple at 8 fields. This restores the canonical 8-field / 9-field-with-string-only invariant that every downstream consumer assumes.",
   "verifierFix": "The proposed fix is correct and safe. Apply all three changes to `YHWH v2.4/scripts/web_helpers.py`:\n\nLine 131 in `tuple_to_dict`: change `\"attribution\": attribution or {}` to `\"attribution\": attribution or \"\"`\n\nLine 146 in `dict_to_tuple`: change `d.get(\"attribution\") or {}` to `d.get(\"attribution\") or None`\n\nLines 173-182 in `write_book`: make the attribution line conditional:\n```python\nch, v, suffix, anchor, kind, title, label, body, *rest = tup\nattribution = rest[0] if rest else None\nlines.append(\"    (\\n\")\nlines.append(f\"        {ch}, {v}, {suffix!r},\\n\")\nlines.append(f\"        {anchor!r},\\n\")\nlines.append(f\"        {kind!r},\\n\")\nlines.append(f\"        {title!r},\\n\")\nlines.append(f\"        {label!r},\\n\")\nlines.append(f\"        {body!r},\\n\")\nif isinstance(attribution, str) and attribution.strip():\n    lines.append(f\"        {attribution!r},\\n\")\nlines.append(\"    ),\\n\")\n```\n\nThis restores the 8-field / 9-field-with-non-empty-string invariant. No marathon core is touched. Notes already on disk are unaffected (confirmed zero `{}` instances). KJV edition byte-stability is preserved.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "\nIndependently verified by reading all cited code. The bug is real and confirmed:\n\n1. `tuple_to_dict` line 131: `attribution or {}` — when an 8-field note is loaded (9th slot padded to `None`), `None or {}` = `{}`. Confirmed exact text in web_helpers.py:131.\n\n2. `dict_to_tuple` line 146: `d.get(\"attribution\") or {}` — after the dict round-trips as JSON, `{} or {}` = `{}`. The 9th tuple field is `{}`. Confirmed exact text at line 146.\n\n3. `write_book` line 174/182: for an 8-field note received from `dict_to_tuple`, `rest[0]` = `{}`, which is then serialised as `{},` in the .py file unconditionally. Confirmed exact text at lines 173-182.\n\nDownstream impact confirmed by code reads:\n- `validate_taxonomy.py:178`: `isinstance(tup[8], str)` returns `False` for `{}` → note counted as unattributed/missing, triggering incorrect errors.\n- `dashboard.py:139`: same check → attribution coverage statistic mis-counted.\n- `traditions.py:114`: `isinstance(attribution, str) and attribution.strip()` → `False` → falls through to default, no crash but attribution-based tradition derivation silently skipped.\n- `build_edition.py:336`: `tup[8] or \"\"` → `{} or \"\"` = `\"\"` → safe (falsy dict), no crash.\n- `lookup_year({})` in source_dates.py:62: `if not attribution: return None` — `{}` is falsy → returns `None`. Safe.\n- `note_attribution` in config.py:130-133: `a = {}`, `isinstance(a, str)` is False so the empty-str branch doesn't apply, and `return a` returns `{}` — violating the `str | None` return type. Any caller that calls `.strip()` on the returned value would crash.\n- `NoteSpec.from_tuple`: `attribution = tup[8] = {}` — not normalised to None, stored as `{}` in a `str | None` typed field.\n\nCurrent on-disk state: grep of content/notes/ for `{},` returns no matches, so no notes are currently corrupted. But every note saved through the web UI edit path (add or edit operations in web_notes.py) triggers this corruption.\n\nThe proposed fix is correct and safe: (1) `attribution or \"\"` in `tuple_to_dict` gives a round-trip-safe empty string instead of `{}`; (2) `d.get(\"attribution\") or None` in `dict_to_tuple` normalises empty attribution to `None`; (3) the conditional in `write_book` omits the attribution line unless it is a non-empty string, maintaining 8-field form for notes without provenance. Since no notes files currently contain `{}`, the 9 KJV edition byte-stability invariant is unaffected. The fix does not touch marathon core.\n\nSeverity recalibration: The bug corrupts on-disk note files through the active web UI edit path (add/edit note). Every note without attribution that passes through the editor gets a `{}` 9th field written to disk, causing attribution-coverage statistics to misreport and validate_taxonomy.py to emit spurious errors for every such note. Additionally `note_attribution()` returns `{}` instead of `None`, which is a type violation that could cause a crash in any future caller doing string operations on the result. Severity is high, not critical, because: (a) no crash in current callers (all happen to be falsy-safe or isinstance-guarded), (b) the corruption is currently latent (no files affected yet), and (c) build output is unaffected (build_edition is safe). High is the correct severity.\n"
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All three cited code points are confirmed by direct reading:\n\n1. `web_helpers.py` line 131: `\"attribution\": attribution or {}` — confirmed. A None attribution (from an 8-field note tuple padded with None by `tuple_to_dict`) becomes `{}` in the returned dict.\n\n2. `web_helpers.py` line 146: `d.get(\"attribution\") or {}` — confirmed. When the dict value is `{}` (from step 1), this returns `{}`, so `dict_to_tuple` produces a 9-field tuple with `{}` at index 8.\n\n3. `web_helpers.py` lines 174/182: `attribution = rest[0] if rest else {}` followed by unconditional `lines.append(f\"        {attribution!r},\\n\")` — confirmed. For any tuple where `rest[0]` is `{}`, `write_book` serialises `{}` as a literal in the .py file, producing a 9-field tuple with a dict value at the attribution slot.\n\nThe downstream impact is confirmed real:\n\n- `validate_taxonomy.py` line 178: `isinstance(tup[8], str) and tup[8].strip()` — `isinstance({}, str)` is False, so the note is counted as \"missing attribution\". `validate_taxonomy` then calls `sys.exit(2)` if any missing-attribution notes are found, breaking the CI lint gate.\n- `dashboard.py` line 139: same `isinstance(tup[8], str)` check — undercounts attributed notes in dashboard stats.\n- `traditions.py` line 114: same `isinstance(attribution, str)` check — tradition incorrectly falls through to the default `cross` instead of being derived from the attribution string.\n- `lookup_year` in `build_edition.py` via `_iter_note_ref_attribution_years` line 336: `tup[8] or \"\"` — `{} or \"\"` yields `\"\"`, so `lookup_year(\"\")` returns None safely (no crash). This particular path is safe.\n\n**Blast-radius calibration:** All on-disk notes inspected (gen.py, rut.py, and the docstrings confirm 9-field tuples with string attribution) are already in the correct 9-field-with-string form. The defect only fires on the web-UI round-trip code path when a note that was originally 8-field gets loaded, converted to dict, then converted back to tuple and written. This is an edge case (the migration to mandatory attribution is described as complete in the docstrings), but it is a latent corruption bug that would cause `validate_taxonomy` to hard-exit(2) if triggered. The 9 KJV build byte-stability is not directly threatened because the build pipeline reads from on-disk .py files, not through the web API. Severity is medium rather than high: real defect, real downstream breakage in validate_taxonomy and dashboard stats, but only activates on the specific code path of editing a note that was originally stored as an 8-field tuple.\n\n**Proposed fix assessment:** The finder's fix is correct in intent. Refined safe version:\n- `tuple_to_dict` line 131: change `attribution or {}` to `attribution or \"\"` (keeps the type as str throughout).\n- `dict_to_tuple` line 146: change `d.get(\"attribution\") or {}` to `d.get(\"attribution\") or \"\"` (returns empty string, not dict; a 9-field tuple with `\"\"` as attribution is the canonical representation for unattributed notes after migration).\n- `write_book` line 182: guard with `if isinstance(attribution, str) and attribution:` before appending — or alternatively emit an empty string `\"\"` rather than `{}`. The safest additive fix: change the unconditional append to `if attribution: lines.append(f\"        {attribution!r},\\n\")` which omits the 9th field entirely for falsy attribution, preserving 8-field backward compatibility. Do NOT use the finder's exact condition `isinstance(attribution, str) and attribution.strip()` for the write_book guard alone — the dict_to_tuple fix must also land or a `{}` could still reach write_book from a different path."
    }
   ]
  },
  {
   "dimension": "tests",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "high",
   "title": "test_amharic_tewahedo_contains_gen_py is dead — def keyword buried inside a comment",
   "file": "YHWH v2.4/tests/test_parallel_bible_tau6x1.py",
   "line": "834",
   "evidence": "Line 834: `# Durable invariant: gen.py present (Π.0 seed or successor).def test_amharic_tewahedo_contains_gen_py(self):` — the `def` keyword appears at the tail of a `#` comment, so Python never sees it as a method definition. The intended docstring (lines 835-838) becomes a dead string literal inside `test_geez_tewahedo_still_gen_only`, and lines 839-843 (`slot = REPO / ... amharic-tewahedo` + `assert 'gen.py' in files`) execute as part of the wrong test. `test_amharic_tewahedo_contains_gen_py` does not exist in pytest's collection at all.",
   "fix": "Break line 834 so the `def` starts on its own line. Replace the end of line 834 with a newline before `def`:\n```python\n        # Durable invariant: gen.py present (Π.0 seed or successor).\n    def test_amharic_tewahedo_contains_gen_py(self):\n```\nThis separates the comment from the method definition, makes the docstring on lines 835-838 the method's docstring, and registers the test with pytest properly. Also verify the indentation level matches the class (one `    def`, not `        def`).",
   "verifierFix": "Split line 834 at the boundary between the comment text and the `def` keyword. The result should be:\n\n        # Durable invariant: gen.py present (Π.0 seed or successor).\n    def test_amharic_tewahedo_contains_gen_py(self):\n\nThe comment (8-space indent, inside the geez method body) stays on its own line. The `def` starts a new line at 4-space indent (class-method level), matching every other method in `TestTau6X1ClosedArcInvariantPreservation`. Lines 835-843 are already correctly indented for the new method body and require no changes. The fix is safe: test file only, no marathon core touched, no KJV edition build output or schema affected.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\tests\\test_parallel_bible_tau6x1.py lines 812-843. Line 834 is exactly as cited: the token `def test_amharic_tewahedo_contains_gen_py(self):` appears at the tail of a `#` comment, so Python parses the entire line as a comment. Python never sees a method definition. Consequences confirmed: (1) pytest never collects `test_amharic_tewahedo_contains_gen_py` — the test does not exist; (2) lines 835-838 become a dead string literal inside `test_geez_tewahedo_still_gen_only`; (3) lines 839-843 (the amharic slot check and assert) run as part of `test_geez_tewahedo_still_gen_only`, meaning the amharic invariant is tested only as a side-effect of the geez test with no independent named guard. This is a genuine defect: a named regression guard for amharic-tewahedo is silently absent from pytest's collection. The proposed fix (split the line so `def` starts on its own line at 4-space class-method indent) is correct and safe — it only touches a test file, does not touch marathon core, and has no effect on any KJV edition build output or schema. Severity is appropriately high: a named invariant test is silently missing from CI, though the actual assertion logic still executes (inside the wrong test), so there is no current runtime failure or shipped-output corruption."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading the file. Line 834 of C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\tests\\test_parallel_bible_tau6x1.py reads exactly: `        # Durable invariant: gen.py present (Π.0 seed or successor).def test_amharic_tewahedo_contains_gen_py(self):` — the `def` keyword is appended to the tail of a `#` comment, so Python never parses it as a method definition. The intended method body (lines 835-843: the docstring, the `amharic-tewahedo` slot lookup, and the `gen.py` assert) are absorbed into `test_geez_tewahedo_still_gen_only` as sequential statements after that method's own assert. The test `test_amharic_tewahedo_contains_gen_py` does not exist in pytest's collection. The proposed fix (split the line so the comment terminates before `def`, with `    def` at 4-space class-level indentation matching all sibling methods at lines 818, 823, 845, 852, 858) is correct and safe — it touches only test infrastructure, not the build pipeline, not the marathon core, and not any KJV edition output path."
    }
   ]
  },
  {
   "dimension": "byte-stability",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "high",
   "title": "Wall-clock date embedded in every edition OPF via _resolve_publishing defaults",
   "file": "YHWH v2.4/scripts/epub_utils.py",
   "line": "29-37",
   "evidence": "`now_year = datetime.now(timezone.utc).year` and `now_date = datetime.now(timezone.utc).strftime(\"%Y-%m-%d\")` are used as fallback defaults for `copyright_year` and `publication_date` respectively. No edition in `editions.yaml` sets either field, so `edition.get(k) or v` always falls through to the live date. `patch_opf` then writes `<dc:date>{pub['publication_date']}</dc:date>` (the replaced line in `content.opf`) and a `<dc:rights>Copyright © {pub['copyright_year']} ...</dc:rights>` block into every built EPUB. Two builds of the same unchanged edition on different calendar days produce different `content.opf` bytes. The `test_byte_stability_gate._content_digest` normalises only `urn:yhwh:edition:*` URNs and `dcterms:modified`; it does NOT normalise `<dc:date>` or `<dc:rights>`, so a cross-midnight rebuild would fail the determinism assertion at line 90.",
   "fix": "In `epub_utils._resolve_publishing`, replace the two `datetime.now()` calls with static project-epoch defaults: `\"copyright_year\": \"2026\"` and `\"publication_date\": \"2026-05-14\"` (the Omega-0 free-public pivot date, already the canonical CC0 release point). These remain the defaults until an edition explicitly sets the fields. Alternatively, add these fields to every edition record in `editions.yaml`. Also extend `test_byte_stability_gate._content_digest` to normalise `<dc:date>` and the copyright year in `<dc:rights>` so future volatile defaults are caught at the test layer.",
   "verifierFix": "In `YHWH v2.4/scripts/epub_utils.py` lines 29-37, replace the two `datetime.now()` calls with static strings:\n\n```python\ndefaults = {\n    \"publisher_name\": \"Independent\",\n    \"publisher_url\": \"\",\n    \"copyright_year\": \"2026\",\n    \"copyright_holder\": \"\",\n    \"copyright_notice\": \"All rights reserved.\",\n    \"publication_date\": \"2026-05-14\",   # Omega-0 free-public pivot date\n    \"language_code\": \"en\",\n    \"cover_credit\": \"\",\n    \"source_text_credit\": \"Scripture text based on the World English Bible (public domain).\",\n}\n```\n\nRemove the `now_year` and `now_date` local variables and the `datetime` import (if no longer used elsewhere in the file — it is not used elsewhere in epub_utils.py).\n\nAdditionally, in `YHWH v2.4/tests/test_byte_stability_gate.py`, extend `_content_digest` to also normalize the two remaining volatile OPF fields so the test can actually catch this class of regression going forward:\n\n```python\n_DATE_RE = re.compile(r\"<dc:date>[^<]*</dc:date>\")\n_RIGHTS_YEAR_RE = re.compile(r\"(Copyright © )\\d{4}( )\")\n\n# inside _content_digest, after the existing _MODIFIED_RE substitution:\ntext = _DATE_RE.sub(\"<dc:date>NORMALIZED</dc:date>\", text)\ntext = _RIGHTS_YEAR_RE.sub(r\"\\g<1>YYYY\\g<2>\", text)\n```\n\nThese two changes together fix the root cause and harden the guard against future volatile-default regressions. No marathon-core files are touched; schema change is additive (only defaults are replaced with equivalent static values).",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All four code claims independently verified by direct reading:\n\n1. `epub_utils._resolve_publishing` (lines 29-30, 34, 37): both `datetime.now(timezone.utc).year` and `datetime.now(timezone.utc).strftime(\"%Y-%m-%d\")` are called at build time and used as defaults for `copyright_year` and `publication_date`. Confirmed exactly as described.\n\n2. `content/editions.yaml`: grep for `copyright_year` and `publication_date` returned zero matches — no edition record overrides these fields. The `edition.get(k) or v` fallback always falls through to the live-date defaults. Confirmed.\n\n3. `build_edition.patch_opf` (lines 1140-1144): writes `<dc:date>{pub['publication_date']}</dc:date>` into the OPF. Lines 1222-1227: writes `<dc:rights>Copyright © {pub['copyright_year']} ...</dc:rights>`. Both volatile fields land in the built EPUB's OPF. Confirmed.\n\n4. `test_byte_stability_gate._content_digest` (lines 35-66): normalizes only `urn:yhwh:edition:*` URNs (`_URN_RE`) and `dcterms:modified` (`_MODIFIED_RE`). Neither `<dc:date>` nor `<dc:rights>` are stripped or normalized. Confirmed.\n\nCritical nuance: the test's determinism assertion (lines 88-91) compares two builds of the same edition within a single test run (same process, same second). `datetime.now()` returns the same date in both calls, so the test passes today — but this gives false confidence. A genuine cross-midnight or cross-year rebuild would produce different `<dc:date>` and `<dc:rights>` bytes that the test would NOT normalize away, causing the `_content_digest` assertion to fail. The test is incapable of detecting the defect it is supposed to guard against.\n\nThe defect is real and material: the stated project invariant (\"build is deterministic\") is violated for any two builds that straddle a calendar-day boundary, and the guard test cannot detect this failure mode.\n\nThe proposed fix is correct and safe: replacing the two `datetime.now()` calls in `epub_utils._resolve_publishing` with static strings (`\"2026\"` and `\"2026-05-14\"`) eliminates the volatility at its source. No marathon-core files are touched. The schema change is purely additive (only defaults change; no existing edition record is modified). The secondary recommendation to also normalize `<dc:date>` and the copyright-year substring in `<dc:rights>` within `_content_digest` is sound defense-in-depth and should be included.\n\nSeverity confirmed at high: the byte-stability invariant is explicitly a project-wide quality gate that \"the whole project leans on\" (test module docstring), the defect silently undermines it, and the existing guard test provides false assurance rather than catching the failure."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Directly confirmed all claims by reading the code.\n\n(a) Code does what the evidence claims: `epub_utils._resolve_publishing` (lines 29-30) calls `datetime.now(timezone.utc)` twice for `copyright_year` and `publication_date` defaults. `editions.yaml` has no `publication_date` or `copyright_year` fields in any edition, so the `edition.get(k) or v` fallback always resolves to the live wall-clock values. `patch_opf` in `build_edition.py` writes `<dc:date>{pub['publication_date']}</dc:date>` (line 1142) and a `<dc:rights>Copyright © {pub['copyright_year']} ...` block (lines 1223-1227). Because `str(now_year)` is always a non-empty string, the condition `pub[\"copyright_year\"] or pub[\"copyright_holder\"]` is always truthy, so the full copyright string is always written. `_content_digest` in `test_byte_stability_gate.py` normalises only the generator URN (`urn:yhwh:edition:*`) and `dcterms:modified` — it does NOT normalise `<dc:date>` or `<dc:rights>`.\n\n(b) Genuine defect, not guarded: two builds of the same edition on different calendar days produce different OPF bytes; the byte-stability test does not catch this because both its builds happen seconds apart within one test session. The stability guarantee claimed by the test is real for same-session runs but is silently absent for cross-day rebuilds.\n\n(c) Severity recalibration: the finder claims high, but the blast radius is bounded. No scripture or notes content is affected — only OPF metadata changes. The test still passes in normal use (midnight-straddle is astronomically unlikely). The 9 KJV editions' scripture bytes are unaffected. This is a latent metadata-determinism gap, not an active corruption or test failure. Downgrading to medium.\n\n(d) Proposed fix is correct and safe: replacing the two `datetime.now()` calls with static project-epoch values (`\"2026\"` / `\"2026-05-14\"`) is additive, does not touch marathon core, and keeps the 9 KJV editions byte-stable. The suggestion to also extend `_content_digest` to normalise `<dc:date>` and the copyright year is also correct — it would make the test honest about what it is and is not checking. `matter_pages.py` line 40 already uses `\"2026\"` as a hard-coded fallback, making the static default consistent across the codebase."
    }
   ]
  },
  {
   "dimension": "data-validity",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "critical",
   "title": "Exodus translation data unreachable: all 4 Tewahedo stores use non-canonical filename `ex.py` instead of `exo.py`",
   "file": "YHWH v2.4/content/translations/amharic-tewahedo/ex.py",
   "line": "1",
   "evidence": "File header: `\"\"\"Translation: amharic-tewahedo · Book: ex`. `translations.get_verse(translation, book_code, ...)` builds path via `TRANSLATIONS_DIR / translation / f\"{book_code}.py\"` (scripts/core/translations.py:55). Every pipeline call uses canonical code `exo` (from books.yaml), so `translations.get_verse(\"amharic-tewahedo\", \"exo\", ...)` looks for `amharic-tewahedo/exo.py` which does not exist — it silently returns None for every Exodus verse. Same defect in `geez-tewahedo/ex.py`, `amharic-tewahedo-en/ex.py`, and `geez-tewahedo-en/ex.py`. Root in `_source.yaml:110`: `book_codes: [ex]` instead of `[exo]`. The tests at `tests/test_parallel_bible_tau7xb.py:123` pin the wrong code (`assert _exodus_block()[\"book_codes\"] == [\"ex\"]`), confirming the defect propagated into the test suite.",
   "fix": "Rename all four files: `amharic-tewahedo/ex.py` → `amharic-tewahedo/exo.py`, `geez-tewahedo/ex.py` → `geez-tewahedo/exo.py`, `amharic-tewahedo-en/ex.py` → `amharic-tewahedo-en/exo.py`, `geez-tewahedo-en/ex.py` → `geez-tewahedo-en/exo.py`. Update the BOOK field in each file's docstring from `ex` to `exo`. Update `content/translations/sources/parallel-bible-eotc/_source.yaml` line 110: `book_codes: [exo]`. Update `content/translations/amharic-tewahedo/_meta.yaml` line 161 and `content/translations/geez-tewahedo/_meta.yaml` to `ingested_book_codes: [exo]`. Update all test pins in `tests/test_parallel_bible_tau7xb.py` that assert `book_codes == [\"ex\"]` or reference `ex.py` by name to use `exo`. Add `\"ex\": \"exo\"` to `scripts/core/sources_base._BOOK_CODE_ALIASES` so any future detector that emits `ex` normalises correctly.",
   "verifierFix": "The defect is latent and should be fixed when the τ.G.x.* standalone-build wiring lands, not before. At that point, the single safest fix is to add an alias dict directly in `YHWH v2.4/scripts/core/translations.py` mirroring the pattern already established in `render_coverage.py`:\n\nIn `translations.py`, replace `_book_path` (line 54-55) with:\n\n```python\n_TRANSLATION_BOOK_ALIASES: dict[str, str] = {\"ex\": \"exo\", \"1k\": \"1ki\", \"2k\": \"2ki\"}\n\ndef _book_path(translation: str, book_code: str) -> Path:\n    resolved = _TRANSLATION_BOOK_ALIASES.get(book_code, book_code)\n    return TRANSLATIONS_DIR / translation / f\"{resolved}.py\"\n```\n\nThis is additive, zero-blast-radius for the 9 KJV editions (they use canonical codes already), and handles all three legacy 2-letter stems that render_coverage already knows about. Do NOT rename the `ex.py` files or change the test pins before the τ.G.* wiring is complete — the tests contractually verify the ingest artefact shape, and the file rename must be a coordinated commit that updates _source.yaml, _meta.yaml, the 4 files themselves, and all test pins atomically.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The filename mismatch is confirmed real: all 4 Tewahedo stores use `ex.py` while `books.yaml` declares canonical code `exo`, and `translations.py:_book_path` (line 54-55) does zero aliasing — `get_verse(\"amharic-tewahedo-en\", \"exo\", …)` would silently return None. The base HTML uses `vnote-exo-` aside IDs, confirmed by grepping `epub_working/index_split_000.html`.\n\nHowever, the critical severity is NOT sustained for the current state:\n\n1. Runtime blast radius = zero today. The only editions using Tewahedo translations (`standalone-geez`, `standalone-amharic`) have `standalone: true` in editions.yaml, routing them to `build_standalone.py`. That path only ever iterates `_STANDALONE_BOOKS = [\"1ki\", \"1sa\", \"2sa\", \"psa\"]` and calls `chapter_verses_in_source_order(\"geez-tewahedo\", book)` — it never calls `get_verse(…, \"exo\", …)`. The τ.G.x.* pipeline wiring is explicitly deferred. No non-standalone edition uses Tewahedo translations as `popup_translation`.\n\n2. The discrepancy is intentionally accommodated: `render_coverage.py` line 51 has `_BOOK_ALIASES = {\"ex\": \"exo\", \"1k\": \"1ki\", \"2k\": \"2ki\"}` with inline comment \"legacy 2-letter filenames (`ex.py`) map to canonical 3-letter codes (`exo`)\". The coverage/inventory layer already treats this as a known alias pattern — it is not an overlooked bug at the inventory level.\n\n3. The test pins (`book_codes == [\"ex\"]` in test_parallel_bible_tau7xb.py) are accurately pinning the YAML content, which genuinely says `[ex]`. They are not asserting the wrong thing about downstream lookups.\n\n4. The proposed fix is partially unsafe: renaming the files would break the existing test suite (which explicitly pins `ex.py` by filename for contractual reasons). Updating `sources_base._BOOK_CODE_ALIASES` (the notes normalizer) is the WRONG location — it has no effect on `translations.py:_book_path`. The correct single-point fix, when the τ.G.* pipeline is eventually wired, is to add an alias dict to `translations.py` itself (mirroring render_coverage's `_BOOK_ALIASES`), or canonicalize at ingest time by renaming files AND updating all pins in a single coordinated commit.\n\nThe finding is real but its severity is medium (latent future defect, zero current impact, intentionally handled at inventory layer). The proposed fix is structurally correct in intent but cites the wrong normalization hook and would break tests if applied mechanically today."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I independently confirmed every factual claim in the finding:\n\n1. All four `ex.py` files exist (Glob confirmed: `amharic-tewahedo/ex.py`, `geez-tewahedo/ex.py`, `amharic-tewahedo-en/ex.py`, `geez-tewahedo-en/ex.py`). No `exo.py` exists for any Tewahedo store; every other translation uses `exo.py` (kjv, jps, douay-rheims, vulgate-clementine, wlc, lxx-swete-greek, arabic-vandyke all have `exo.py`).\n\n2. `scripts/core/translations.py:55` builds the path as `TRANSLATIONS_DIR / translation / f\"{book_code}.py\"` — no aliasing, no normalization. `books.yaml:21` confirms canonical code is `exo`. So `get_verse(\"amharic-tewahedo-en\", \"exo\", ...)` would look for the non-existent `amharic-tewahedo-en/exo.py` and return `None`.\n\n3. `content/translations/sources/parallel-bible-eotc/_source.yaml:110` has `book_codes: [ex]` — the ingest metadata echoes the wrong code.\n\n4. `content/translations/amharic-tewahedo/_meta.yaml:161` and `geez-tewahedo/_meta.yaml:126` both record `ingested_book_codes: [ex]`.\n\n5. `tests/test_parallel_bible_tau7xb.py:123` and `:354` both pin `[\"ex\"]`, confirming the defect propagated into tests.\n\n6. `ex` is NOT in `_BOOK_CODE_ALIASES` — no runtime normalization path would rescue the lookup.\n\nHOWEVER — the blast radius today is zero. `content/editions.yaml` shows `standalone-geez` (line 519) and `standalone-amharic` (line 560) are the only editions with `popup_translation` pointing at Tewahedo translations. Both editions have `standalone: true`. `build_edition.py:2615-2618` takes a hard early exit `if edition.get(\"standalone\"): return build_standalone.build_standalone(...)` before the `popup_translation_id` resolution code at line 2698 is ever reached. The edition notes explicitly state \"The build-pipeline wiring … is deferred to τ.G.x.*\". The 9 KJV editions use `popup_translation: kjv` or `\"\"` and are unaffected entirely.\n\nThe defect is real and confirmed, but it is latent — it will cause silent all-Exodus-verses silently missing when the τ.G.x.* standalone-Bible wiring ships. The finder's \"critical\" severity is overstated for the current state of the codebase; \"high\" is appropriate (real data-validity defect, will hit on next phase, no current guard, not trivially discovered at that time)."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified the finding is real by reading all cited code, but the severity and fix completeness need correction.\n\nCONFIRMED TRUE:\n- All 4 Tewahedo stores (`amharic-tewahedo/ex.py`, `geez-tewahedo/ex.py`, `amharic-tewahedo-en/ex.py`, `geez-tewahedo-en/ex.py`) exist on disk; no `exo.py` equivalents exist in these dirs (confirmed via Glob).\n- `translations.py:_book_path` builds `TRANSLATIONS_DIR / translation / f\"{book_code}.py\"` with zero alias normalization (line 55). Any call with canonical `\"exo\"` returns None/False.\n- `_source.yaml` line 110 has `book_codes: [ex]` — confirmed.\n- The test at `test_parallel_bible_tau7xb.py:123` asserts `[\"ex\"]` — confirmed.\n\nCONFIRMED REAL DATA GAP:\nThe live `/compare` console (`api_compare` in `web_content.py`) passes the canonical `\"exo\"` code from `books.yaml` when a user selects Exodus for any Tewahedo translation. `has_book(t, \"exo\")` returns False → `by_t[t] = {}` → empty cells in the comparison view for all 4 Tewahedo translations across all 40 Exodus chapters. The preflight popup-coverage check also sees this gap and emits a `warn` for the two standalone editions.\n\nSEVERITY RECALIBRATION TO HIGH (NOT CRITICAL):\n- The standalone-Bible build pipeline is explicitly deferred to τ.G.x.* per editions.yaml lines 517/558: \"The build-pipeline wiring... is deferred to τ.G.x.*\". No EPUB output is currently generated from these stores for Exodus.\n- `build_standalone.py._STANDALONE_BOOKS = [\"1ki\", \"1sa\", \"2sa\", \"psa\"]` — Exodus is not in this list at all.\n- `popup_versions.py.VERSION_REGISTRY` does not include any Tewahedo translation ID — the verse-popup bake path never touches these files.\n- The 9 KJV editions are byte-stable and completely unaffected.\n- The only live-user-visible impact is the `/compare` console returning empty Exodus data for these 4 translations. Not a shipped EPUB defect; a developer-tool data gap.\n\nTHE PROPOSED FIX IS INCOMPLETE AND UNSAFE AS WRITTEN:\nThe finder's fix omits a critical update: `lint_rules.py` at lines 977 and 1006 has `\"ex\"` in the `expected_geez` and `expected_amharic` sets. The lint check at line 1042 uses raw file stems (`{p.stem for p in ed_dir.glob(\"*.py\") ...}`) with no alias mapping. Renaming `ex.py` → `exo.py` without updating these sets would immediately break the `check_render_coverage_no_regression` lint guard (it would report `\"ex\"` as missing/regressed). This is a pre-commit guard that blocks commits.\n\nAdditionally: the finder states the tests \"confirm the defect propagated into the test suite\" — this is wrong. The tests at lines 122-123 correctly pin the on-disk YAML value; they pass today and would need updating as part of the rename, but they are accurate tests of the current state, not evidence of propagated defect.\n\nThe proposed alias addition `\"ex\": \"exo\"` to `sources_base._BOOK_CODE_ALIASES` would not fix the runtime lookup — `translations.py` does not call `_normalize_book_code`; it constructs the path directly from the book_code argument. `render_coverage.py` already has its own local `_BOOK_ALIASES = {\"ex\": \"exo\"}` in `_list_rendered` which correctly handles reporting without a centralized fix."
    }
   ]
  },
  {
   "dimension": "concurrency-caching",
   "kind": "find",
   "severity": "high",
   "originalSeverity": "high",
   "title": "scripts/api/exports.py subprocess.run missing stdin=subprocess.DEVNULL — WinError 6 in production HTTP build endpoint",
   "file": "YHWH v2.4/scripts/api/exports.py",
   "line": "197",
   "evidence": "proc = subprocess.run(\n    cmd,\n    capture_output=True,\n    text=True,\n    cwd=str(REPO),\n    timeout=timeout_s,\n)\n\nThis is the non-frozen path of the /api/exports/build HTTP endpoint — the primary user-facing EPUB download button. No stdin=subprocess.DEVNULL. Per the project's W-W1 rule (memory feedback_w_w1_subprocess_devnull): 'On Windows pytest-from-PowerShell, every subprocess.run() must pass stdin=subprocess.DEVNULL or hits WinError 6.' The dev server is always launched from PowerShell on this Windows box. When the web server's HTTP-handler thread spawns build_edition.py via this subprocess.run, it inherits the invalid stdin handle and raises WinError 6 (ERROR_INVALID_HANDLE), aborting the build silently.",
   "fix": "Add stdin=subprocess.DEVNULL to the call:\n\nproc = subprocess.run(\n    cmd,\n    capture_output=True,\n    text=True,\n    cwd=str(REPO),\n    timeout=timeout_s,\n    stdin=subprocess.DEVNULL,  # W-W1: Windows PowerShell guard\n)\n\nMatch the pattern already used by scripts/ebible.py, scripts/ci.py, scripts/audit_types.py, etc.",
   "verifierFix": "Add stdin=subprocess.DEVNULL to the subprocess.run call at line 197:\n\n    proc = subprocess.run(\n        cmd,\n        capture_output=True,\n        text=True,\n        cwd=str(REPO),\n        timeout=timeout_s,\n        stdin=subprocess.DEVNULL,  # W-W1: Windows PowerShell guard\n    )\n\nThe finder's fix is exactly correct. No changes needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading the file. At line 197 of `YHWH v2.4/scripts/api/exports.py`, the `subprocess.run` call in the `else` branch (non-frozen path) lacks `stdin=subprocess.DEVNULL`. This branch executes on every dev-server build request launched from PowerShell — `getattr(sys, \"frozen\", False)` is `False` in that context, so the frozen/in-process path at line 191 is bypassed. The W-W1 project rule (documented in memory and applied to every other `subprocess.run` in the codebase — confirmed by grep: `ebible.py`, `ci.py`, `audit_types.py`, `lint_rules.py`, `ship-check.py`, `core/epubcheck.py`, `add_note.py`, `add_kind.py`, etc.) explicitly requires `stdin=subprocess.DEVNULL` for all Windows PowerShell subprocess calls. This is the primary user-facing HTTP build endpoint (the EPUB download button). The proposed fix — adding `stdin=subprocess.DEVNULL` — is correct, safe, and additive: it does not touch build output, marathon core, or EPUB content, and poses zero KJV byte-stability risk."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading the file. At line 197 of YHWH v2.4/scripts/api/exports.py, the subprocess.run call in the non-frozen branch of api_export_build is:\n\n    proc = subprocess.run(\n        cmd,\n        capture_output=True,\n        text=True,\n        cwd=str(REPO),\n        timeout=timeout_s,\n    )\n\nNo stdin=subprocess.DEVNULL is present. The frozen path (lines 187-194) is in-process (calls _be.build_one directly) and never reaches this subprocess.run, so the W-W1 risk is entirely on the non-frozen dev-server path — which is the primary runtime on this Windows box launched from PowerShell.\n\nThe W-W1 pattern (stdin=subprocess.DEVNULL) is applied consistently in 16+ other subprocess.run calls across the codebase (ebible.py, ci.py, audit_types.py, epubcheck.py, lint_rules.py, etc.). This one call in exports.py is the sole omission in a user-facing HTTP endpoint.\n\nThe blast radius is real and direct: when the user clicks the EPUB build button in the web UI, this subprocess.run fires. On Windows with a PowerShell-launched server the stdin handle is invalid, WinError 6 is raised, the exception propagates up (there is no WinError 6 catch — only subprocess.TimeoutExpired is caught), and the build fails with an unhandled exception rather than a clean error response.\n\nThe proposed fix is correct and safe: adding stdin=subprocess.DEVNULL is the exact pattern used everywhere else, is a no-op on POSIX, does not touch any marathon core files, and cannot affect EPUB output bytes in any way."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "find_aside_insertion_point treats higher-chapter asides as 'preceding', misorders re-injected notes in Strategy-B sections",
   "file": "YHWH v2.4/scripts/inject.py",
   "line": "621",
   "evidence": "`precedes = existing_ch != ch or (existing_v, existing_s) < target` — when an aside from chapter 12 is already in a Strategy-B per-file notes-section and a new note for chapter 5 is being inserted, `existing_ch (12) != ch (5)` is True, so `precedes=True` and `insertion` advances past the ch-12 aside. The ch-5 note ends up inserted AFTER ch-12 notes, violating sort order. This fires on any re-injection run where new notes for an earlier chapter are added to a book that already has later-chapter notes injected.",
   "fix": "Replace line 621 with a full 3-key comparison: `precedes = (existing_ch, existing_v, existing_s) < (ch, v, suffix or '')`. Also update the `else` return to guard that the existing aside belongs to a later chapter before treating it as the insertion point: `else: if existing_ch == ch: return inside_start + m.start()` (continue iterating for existing_ch > ch rather than stopping).",
   "verifierFix": "Change only line 621 in C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\inject.py:\n\nBefore:\n    precedes = existing_ch != ch or (existing_v, existing_s) < target\n\nAfter:\n    precedes = (existing_ch, existing_v, existing_s) < (ch, v, suffix or \"\")\n\nThe `else` branch (lines 632–634) already correctly returns `inside_start + m.start()` as the insertion point whenever `precedes` is False, which now includes both \"same chapter, later verse\" and \"later chapter\" cases. No change to the else branch is needed; the finder's second proposed change (guarding on `existing_ch == ch`) is incorrect — it would cause the loop to continue iterating instead of returning, leaving insertion at a stale position when an existing aside from a later chapter is encountered first.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\inject.py lines 597–635 in full and confirmed the logic independently.\n\nLine 621: `precedes = existing_ch != ch or (existing_v, existing_s) < target`\n\nThe docstring (lines 598–601) describes \"the chapter's notes-section\" and \"(verse, suffix) order\", revealing the function was designed for Strategy-A's per-chapter sections where all asides share the same chapter number. In that context `existing_ch != ch` is always False and the condition degenerates correctly to a 2-key sort.\n\nFor Strategy-B (books 1ki, 2ki, 1ch, 2ch, psa, job, isa, jer, and ~25 others), `ensure_notes_section_b` (line 373) returns ONE file-wide section covering all chapters present in that file. Multiple chapters share this section (e.g., Psalms with 151 chapters across 4 files; Job with 42 chapters). When a re-injection run adds new notes for an earlier chapter to a file that already has later-chapter notes injected, `existing_ch > ch` evaluates `existing_ch != ch` as True, so `precedes=True`, causing the insertion point to advance PAST the later-chapter aside. The new earlier-chapter note ends up after later-chapter notes — wrong document order.\n\nThe condition must be a full 3-key tuple comparison to handle all relative-chapter cases correctly. The `else` branch at lines 632–634 (`return inside_start + m.start()`) already correctly handles the \"insert before this aside\" case once the `precedes` predicate is fixed; no change is needed there.\n\nBlast radius: affects only Strategy-B books on re-injection runs where new notes are added for chapters earlier than the highest already-injected chapter in the same file. Normal sequential first-injection (all notes processed in chapter order) is unaffected because `existing_ch <= ch` is always satisfied under that ordering. The 9 KJV editions use exclusively Strategy-A books for their canonical text and are byte-stable. The defect is latent but real; it will materialise as the Phase-D Esther/Kings marathon adds notes to books whose files already have some later-chapter notes injected."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I independently verified the code at YHWH v2.4/scripts/inject.py line 621. The expression `precedes = existing_ch != ch or (existing_v, existing_s) < target` is confirmed present. The condition `existing_ch != ch` is True regardless of whether the existing aside is from an EARLIER or a LATER chapter — both get treated as \"precedes\", advancing `insertion` past them. When a Strategy-B file already contains a later-chapter (e.g. ch=12) aside and a new note for an earlier chapter (e.g. ch=5) is being inserted, the loop hits the ch=12 aside, fires `precedes=True`, advances `insertion` past it, and the ch=5 note lands AFTER the ch=12 note. This violates chapter sort order.\n\nThe bug is real but its blast radius is bounded:\n\n1. Strategy-A books pass a per-chapter section to `find_aside_insertion_point`, so `existing_ch != ch` can never be true in that path. The bug is confined to Strategy-B books only.\n\n2. Strategy-B notes files are confirmed to be in sorted (ch, v, suffix) order (read 1ki.py: ch=1,2,3… in order). On the FIRST full injection pass, every note is inserted after all previously inserted same-or-earlier chapter notes; `existing_ch > ch` never fires because notes arrive in chapter order. The committed `epub_working/` base HTML was produced by that first pass and is correctly ordered.\n\n3. The bug fires only on incremental re-injection: when a new note for chapter N is added to a file that already has injected notes for chapter M > N. This is a real operational scenario (notes corpus grows incrementally), not dead code.\n\n4. The output is still well-formed XHTML (existing test `test_inject_wellformed.py` confirms this — it was the focus of a prior fix that replaced `m.end()` with a proper `</aside>` close). The ordering defect is a visual/semantic issue: notes appear out of chapter order in the notes section, but markers still link correctly. No crash.\n\n5. KJV byte-stability is unaffected: re-injection skips already-injected notes via the `already_in` check (line 716), so running inject on the committed base produces zero changes.\n\nThe existing test `test_insert_with_foreign_chapter_aside_stays_wellformed` in `test_inject_wellformed.py` demonstrates the exact scenario (ch=33 aside in section, inserting ch=26) and only asserts XML well-formedness — it does not assert that ch=26 lands BEFORE ch=33. This is a gap.\n\nThe proposed fix — replacing line 621 with `precedes = (existing_ch, existing_v, existing_s) < (ch, v, suffix or '')` — is correct. With this 3-key comparison: (a) earlier-chapter asides still fire `precedes=True`; (b) later-chapter asides fire `precedes=False` and fall into the `else` branch, which already does `return inside_start + m.start()` (insert before this aside) — exactly right. The finder's secondary suggestion to modify the `else` return is unnecessary: the existing `return inside_start + m.start()` already handles both `existing_ch == ch` (same-chapter, sorts after target) and `existing_ch > ch` (later-chapter) correctly once `precedes` is fixed. No other code paths are touched; marathon core is untouched; schema is unchanged; KJV editions remain byte-stable."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "batch_insert_notes silently drops all inserts on SyntaxError in the generated output without informing the caller",
   "file": "YHWH v2.4/scripts/promote.py",
   "line": "368-373",
   "evidence": "Lines 368-373:\n```python\ntry:\n    ast.parse(new_text)\nexcept SyntaxError:\n    return 0\nensure_backup(book_path)\natomic_write(book_path, new_text)\nreturn len(inserts)\n```\nIf the splice of new tuple text produces invalid Python (e.g. because `format_tuple_text` produced a string containing an unescaped quote), `ast.parse` raises `SyntaxError`, the function returns `0`, and all N candidates are silently dropped. `batch_promote_xrefs.py` calls `batch_insert_notes` and checks `if n:` — a return of `0` is indistinguishable from \"no candidates to insert\", so the data loss is never surfaced to the operator. The backup is NOT created in this path (it happens AFTER the parse check), so there is no partial-write, but the silent discard of potentially hundreds of candidates is a real data-loss risk.",
   "fix": "Return a sentinel error value (e.g. `-1`) or raise an exception on SyntaxError so the caller can distinguish failure from empty. Minimally, log the error before returning: `import logging; logging.error(\"batch_insert_notes: generated SyntaxError for %s; dropping %d inserts\", book_path, len(inserts))`. Better: raise `RuntimeError(f\"splice produced invalid Python in {book_path}\")` so `batch_promote_xrefs.py`'s `except Exception` handler prints the error.",
   "verifierFix": "Minimal safe fix — add a `logging.error` call before the silent `return 0` so the operator always sees the failure. At the top of `promote.py` add `import logging` (already has `import ast`). Then change lines 369-370:\n\n```python\nexcept SyntaxError as exc:\n    logging.error(\n        \"batch_insert_notes: splice produced invalid Python in %s \"\n        \"(%d insert(s) dropped): %s\",\n        book_path, len(inserts), exc,\n    )\n    return 0\n```\n\nStronger alternative (recommended): raise instead of returning silently so `batch_promote_xrefs.py`'s caller chain surfaces it as a visible failure:\n\n```python\nexcept SyntaxError as exc:\n    raise RuntimeError(\n        f\"batch_insert_notes: splice produced invalid Python in {book_path} \"\n        f\"({len(inserts)} insert(s) dropped)\"\n    ) from exc\n```\n\nRoot cause to fix separately: `py_str` in `format_tuple_text` does not escape embedded literal newlines, which is the most likely trigger. Add `s = s.replace('\\n', '\\\\n').replace('\\r', '\\\\r')` at the top of `py_str` before the quote-selection logic. This makes SyntaxError from newlines impossible rather than just surfaced.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The code at YHWH v2.4/scripts/promote.py lines 367-373 is exactly as described. The `ast.parse` guard fires, returns `0`, and the caller in batch_promote_xrefs.py line 81 does `if n:` — making a SyntaxError-triggered `0` indistinguishable from \"no candidates\". No log message, no exception, no indication to the operator that N notes were silently discarded.\n\nThe finder's claim that `format_tuple_text` can produce invalid Python is realistic: `py_str` correctly handles single/double quote mixing (escape interior `\"` when both present), but does NOT escape embedded literal newline characters. If a `body` or `attribution` string contains a real `\\n` character (plausible from AI-drafted HTML or OCR'd source text that wasn't fully sanitized), `py_str` emits it unescaped inside a single-line string literal, which is a SyntaxError. The `sandbox_ai_html` pass strips tags but does not normalize whitespace to remove embedded newlines.\n\nThe finding is real and material for the content-authoring workflow. Severity is correctly medium: the 9 KJV editions are completely unaffected (this code only runs during candidate promotion, never during build), there is no shipped-output corruption, and the blast radius is bounded to candidates being silently dropped during batch ingest. It is not a crash and does not compromise the marathon core.\n\nThe proposed fix is correct and safe. The `raise RuntimeError` variant is slightly better than just logging because the exception propagates visibly through `promote_by_book` → `main()`, making the failure impossible to miss. Adding a `logging.error` before the `return 0` is the minimal safe fix that preserves current control flow. Neither touches marathon core files nor the KJV build path."
    }
   ]
  },
  {
   "dimension": "security",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "RSS feed: note body_html injected raw into CDATA — stored XSS + XML injection",
   "file": "YHWH v2.4/scripts/core/verse_of_day.py",
   "line": "328-336",
   "evidence": "body_html = (headline_note or {}).get(\"body_html\") or \"\"\n...\ndescription = (\n    f\"<p><strong>{_xml_escape(ref)}</strong></p>\"\n    + (f\"<p><em>{_xml_escape(label)}</em></p>\" if label else \"\")\n    + (body_html if body_html else \"\")   # ← RAW, no sanitize_html call\n)\n...\nf\"      <description><![CDATA[{description}]]></description>\\n\"\n\nbody_html is taken directly from notes_payload (line 230: \"body_html\": spec.body_html or \"\"), which is the raw stored note body. Two concrete impact paths: (1) A note body containing ]]> prematurely closes the CDATA section, allowing arbitrary XML element injection into the RSS feed (fabricated <item> entries, rogue <link>, etc.). (2) A note body containing <script> or javascript: URLs passes unfiltered to any RSS reader that renders description HTML as markup. Neither sanitize_html nor _xml_escape is called on body_html before it enters the CDATA block. All other user-visible data on the same line (ref, label) are correctly _xml_escape'd, making this an inconsistent omission.",
   "fix": "Apply sanitize_html to body_html before interpolating it into the CDATA description:\n\nfrom scripts.core.html_sanitize import sanitize_html\n...\nbody_html = sanitize_html((headline_note or {}).get(\"body_html\") or \"\")\n\nAlternatively, since CDATA does not protect against ]]> within the content, also replace ]]> in the result: body_html_safe = sanitize_html(body_html).replace(\"]]>\", \"]]&gt;\"). Mirror the sanitize_html calls already present in preview.py:133-135 and web_content.py:373 for the same note body field.",
   "verifierFix": "In YHWH v2.4/scripts/core/verse_of_day.py, replace line 328:\n\n    body_html = (headline_note or {}).get(\"body_html\") or \"\"\n\nwith:\n\n    from scripts.core.html_sanitize import sanitize_html\n    body_html_raw = (headline_note or {}).get(\"body_html\") or \"\"\n    body_html = sanitize_html(body_html_raw).replace(\"]]>\", \"]]&gt;\")\n\nThe replace(\"]]>\", \"]]&gt;\") step handles the XML-injection vector independently of what sanitize_html produces (it does not itself escape ]]>). The import can be placed at the top of the file alongside other core imports rather than inline. This mirrors the existing calls at preview.py:133-135 and web_content.py:373, touches no marathon-core files, and has zero effect on the KJV EPUB build path.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading the code directly. In YHWH v2.4/scripts/core/verse_of_day.py lines 328-336, body_html is extracted from the notes payload (which stores spec.body_html verbatim) and interpolated raw into a CDATA block at line 336: `+ (body_html if body_html else \"\")`, which then lands in `f\"<description><![CDATA[{description}]]></description>\"` at line 345. No sanitize_html call occurs anywhere in this path. This is confirmed inconsistent: preview.py:133-135 and web_content.py:373 both call sanitize_html() on the same body_html field from the same note store. The RSS feed is a live wired HTTP endpoint — web.py imports api_verse_of_day_rss from web_misc.py (line 176), which calls rss_feed() directly (web_misc.py:70). The ]]> XML-injection path is real: note bodies from PD bulk-ingest sources (Nave's, Easton's, Torrey) are not sanitized before storage. A body_html value containing ]]> would break XML structure and fabricate arbitrary RSS elements. The finder's title of \"high\" is overcalibrated for a single-user localhost app where no public multi-user attack surface exists; medium is correct. The proposed fix is safe and correct: it mirrors the existing pattern in preview.py and web_content.py, requires no schema change, doesn't touch marathon core, and keeps KJV editions byte-stable (rss_feed is not on the EPUB build path)."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading the code:\n\n1. `/YHWH v2.4/scripts/core/verse_of_day.py` line 328 retrieves `body_html` raw from the notes payload (which itself is sourced from `spec.body_html` — the stored note body, not sanitized at ingest time).\n\n2. Line 336 interpolates it directly into `description` without any sanitization.\n\n3. Line 345 wraps `description` in `<![CDATA[...]]>`. This CDATA wrapper does NOT protect against `]]>` appearing inside the body content — that sequence would prematurely close the CDATA section, allowing injection of arbitrary XML into the RSS feed (extra `<item>` nodes, rogue `<link>`, etc.).\n\n4. Confirmed `sanitize_html` in `html_sanitize.py` has zero CDATA/`]]>` handling — a grep for `CDATA`, `\\]\\]>`, and `cdata` all return no matches. So even if `sanitize_html` were called, the `]]>` XML injection would still be present without an explicit `.replace(\"]]>\", \"]]&gt;\")` step.\n\n5. All other fields on the same line (`ref`, `label`) are correctly passed through `_xml_escape()`. The omission of sanitization on `body_html` is an inconsistent gap relative to the existing pattern.\n\n6. The RSS feed is exposed via `/api/verse-of-day.rss` in the local HTTP server (`web.py` lines 1528–1561).\n\nSeverity recalibration: The finder labeled this \"high\" — that is somewhat inflated for a single-user local desktop app where the RSS endpoint is only reachable from localhost. The worst real-world impact is: (a) malformed/corrupted RSS XML output if any stored note body happens to contain `]]>`, and (b) unsanitized HTML in the CDATA description reaching RSS reader rendering. The \"stored XSS\" framing is technically accurate but overstated for a localhost tool. The XML-structure corruption (CDATA breakage from `]]>`) is the more concrete defect — it would break any RSS reader consuming the feed. Recalibrated to medium.\n\nThe proposed fix is correct and safe: `sanitize_html(body_html).replace(\"]]>\", \"]]&gt;\")` mirrors the existing pattern at `preview.py:133-135` and `web_content.py:373`, is additive, does not touch marathon core, and has zero impact on the 9 KJV EPUB builds (RSS is not part of that pipeline)."
    }
   ]
  },
  {
   "dimension": "security",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "Stored-XSS in /sources console: truncateHTML returns raw note body HTML when ≤240 stripped chars",
   "file": "YHWH v2.4/scripts/templates/sources.py",
   "line": "592-596",
   "evidence": "```js\nfunction truncateHTML(html, n) {\n  const stripped = (html || '').replace(/<[^>]+>/g, '');\n  if (stripped.length <= n) return html;   // ← returns raw, unescaped HTML\n  return escapeHTML(stripped.slice(0, n)) + '...';\n}\n```\nThis return value is used directly in an `innerHTML` template literal (line 559): `<div ...>${truncateHTML(n.body, 240)}</div>`. The `n.body` value comes from `api_sources_for_book` (web_sources.py:134) which returns the raw on-disk note body with no sanitization. Any note body with ≤240 stripped characters containing e.g. `<img src=x onerror=...>` will execute as DOM XSS. Event-handler attributes fire under nonce-CSP because the nonce only gates `<script>` elements, not `on*` handlers. Note bodies are publisher-authored plus AI-authored and are never sanitized before reaching this frontend path.",
   "fix": "Replace the raw-return branch with an escaped return: `if (stripped.length <= n) return escapeHTML(stripped);`. This matches the existing escaped branch for longer bodies. Alternatively, pass the short body through the same `escapeHTML` path unconditionally and accept that HTML markup is displayed as literal text in the truncated preview (the body_preview in the audit console already does this — see audit.py:240 `${escapeHTML(n.body_preview)}`). The sanitize_html backend sanitizer is NOT the right fix here because it is deliberately not called for this read path — the fix must be in the frontend helper.",
   "verifierFix": "Replace line 595 in `/YHWH v2.4/scripts/templates/sources.py`:\n\n```js\n// Before (unsafe for short bodies):\nif (stripped.length <= n) return html;\n\n// After (consistent with the truncated-body branch):\nif (stripped.length <= n) return escapeHTML(stripped);\n```\n\nThis matches the existing behavior for longer bodies (which already return `escapeHTML(stripped.slice(0, n)) + '...'`) and is safe — no marathon core touch, no KJV build impact.\n\nThe trade-off: `<strong>/<em>/<b>` formatting in short note bodies will no longer render as rich text in the sources console; they display as plain text. This is acceptable because: (a) the sources console is a publisher-review tool, not a presentation layer; (b) the `audit.py` console already uses this exact plain-text approach for previews; (c) the KJV EPUB output is unaffected (build pipeline does not use `truncateHTML`).\n\nNote: the finder's claim that a \"nonce-CSP\" mitigates this is incorrect — there is no CSP at all in this codebase — but that absence does not change the fix recommendation.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified all cited code:\n\n1. `truncateHTML` at `/YHWH v2.4/scripts/templates/sources.py` lines 592-596 is confirmed exactly as the finder quotes: the early-return branch at line 595 returns raw `html` (the unescaped note body) when stripped length ≤ 240.\n\n2. Line 559 is confirmed: `<div class=\"text-sm text-slate-700 mt-1\">${truncateHTML(n.body, 240)}</div>` inside a template literal that feeds `area.innerHTML = ...` at line 535.\n\n3. `web_sources.py:134` (`body` key) confirms the raw on-disk value is passed with no sanitization.\n\n4. The `audit.py:240` asymmetry is confirmed: the audit console uses `${escapeHTML(n.body_preview)}` — the same field escaped — while sources.py leaves short bodies raw.\n\n5. Note bodies ARE HTML (`body_html` in file format docs, `<strong>/<em>/<b>` confirmed in corpus). The raw-return is intentional to preserve inline markup, but it opens the attack surface.\n\n6. The finder incorrectly claims a \"nonce-CSP\" exists — there is NO Content-Security-Policy header anywhere in the codebase (grepped all scripts). This means the finding's CSP reasoning is wrong, but the vector (inline event handlers in a short note body) is real regardless — no CSP means even less mitigation than the finder assumed.\n\n7. Severity recalibration: this is a single-user LOCAL desktop app (project facts explicitly state \"single-user local desktop app, OUT OF SCOPE: public-server hardening\"). The only attack path is: malicious HTML in a `.py` notes file on the local machine → viewed in the sources console. This requires a compromised local notes file. Given the context — solo publisher, no network exposure, no multi-user surface — the real-world blast radius is very low. The finding is real code-quality/defense-in-depth issue but not a production-grade HIGH severity in this deployment context. Downgraded to medium.\n\n8. The proposed fix (`return escapeHTML(stripped)` replacing `return html`) is technically correct and safe (no marathon core, no KJV byte impact). However, it causes a functional regression: all short note bodies currently render with `<strong>`/`<em>` formatting in the sources console; the fix strips that to plain text. The existing longer-body branch already does this (returns `escapeHTML(stripped.slice(0, n)) + '...'`), so the behavior would be consistent, but it changes the appearance of the sources console for all ≤240-char notes."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified all three claims:\n\n1. CODE MATCHES EVIDENCE. Line 595 of `YHWH v2.4/scripts/templates/sources.py` reads `if (stripped.length <= n) return html;` — the raw, unescaped body HTML is returned. Line 559 assigns it to `innerHTML` via a template literal. Both confirmed by direct read.\n\n2. THE RAW PATH IS REACHABLE IN PRACTICE. `web_sources.py` lines 113–134 pass `body` straight from the on-disk tuple into the JSON response with no sanitization call (no `sanitize_html`, no bleach, no escaping). Note bodies in `content/notes/*.py` contain real HTML (`<strong>`, `<em>`, `<a href=\"...\">`, `<span>`). The \"Cross-references\" bodies in `gen.py` strip to ~160 characters — well under the 240-char threshold — so the raw-return branch is hit for a substantial fraction of real notes.\n\n3. THE CSP DOES NOT PROTECT AGAINST THIS. The CSP at `web.py:1106` is `script-src 'self' 'nonce-{nonce}'`. This gates `<script>` elements only. Inline event handler attributes (`onerror`, `onclick`, etc.) injected via `innerHTML` are NOT blocked by `script-src` nonces; they fire directly in the DOM. An `<img src=x onerror=alert(1)>` in a ≤240-stripped-char note body would execute.\n\n4. NO EXISTING GUARD. The `sanitize_html` from `html_sanitize.py` is only applied in `web_content.py` (the `/api/sample/` path). It is explicitly NOT called in `web_sources.py`. The finding's parenthetical \"the sanitize_html backend sanitizer is NOT the right fix here\" is correct — the body arrives at the frontend already unsanitized.\n\n5. PROPOSED FIX IS CORRECT AND SAFE. Replacing `return html;` with `return escapeHTML(stripped);` at line 595: (a) is a one-line change to a JS helper inside a Python template string, (b) touches no marathon core files, (c) has zero effect on the EPUB build pipeline, (d) matches the already-existing long-body branch behavior, and (e) matches the pattern already used in `audit.py:240` (`escapeHTML(n.body_preview)`). The trade-off is that HTML formatting is stripped in short previews, which is the correct conservative choice for an admin console preview.\n\nSEVERITY NOTE: The single-user local desktop context bounds the blast radius (attacker = publisher themselves, or AI-generated note content executing in their own browser tab). This is meaningful self-inflicted XSS (malformed AI-generated note body could exfiltrate cookies or redirect), but not a public-server risk. Downgrading to `medium` from `high` is defensible; however, since note bodies include AI-generated content that is never sanitized before reaching this path, execution risk is real. Keeping as `high` is also defensible. Calibrating to `medium` because this is an explicitly single-user local app (project facts state: \"OUT OF SCOPE: public-server/hosting hardening, multi-tenant auth\") — yet DOM XSS is a genuine code defect regardless of deployment context, not a deployment-model concern."
    }
   ]
  },
  {
   "dimension": "security",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "RSS feed embeds unsanitized note body_html inside CDATA description block",
   "file": "YHWH v2.4/scripts/core/verse_of_day.py",
   "line": "328-336",
   "evidence": "```python\nbody_html = (headline_note or {}).get(\"body_html\") or \"\"\ndescription = (\n    f\"<p><strong>{_xml_escape(ref)}</strong></p>\"\n    + ...\n    + (body_html if body_html else \"\")   # ← raw note body, no sanitize_html\n)\n# ...\nf\"      <description><![CDATA[{description}]]></description>\\n\",\n```\n`body_html` comes from `NoteSpec.body_html` (config.py:102 `body_html=tup[7]`) which is the raw stored note body. `sanitize_html` is called at the preview and sample-html render paths but NOT here. An RSS reader that renders the CDATA description as HTML (all major RSS readers do) will execute any injected `<script>` or event-handler content in the note body.",
   "fix": "Apply `sanitize_html` before interpolating the body into the description: `from scripts.core.html_sanitize import sanitize_html` then `body_html = sanitize_html((headline_note or {}).get('body_html') or '')`. This mirrors the approach used in `_render_note_aside` (preview.py:133-135) and `_render_sample_html` (web_content.py:373). The sanitizer preserves intended markup (`<em>`, `<a>`, `<strong>`, etc.) while stripping scripts and event handlers.",
   "verifierFix": "In C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\core\\verse_of_day.py, add the import at the top of `rss_feed` (or at module level alongside the other core imports) and sanitize before interpolation:\n\nAt line 328, change:\n    body_html = (headline_note or {}).get(\"body_html\") or \"\"\n\nTo:\n    from scripts.core.html_sanitize import sanitize_html  # add once at module top\n    body_html = sanitize_html((headline_note or {}).get(\"body_html\") or \"\")\n\nOr as a module-level import alongside the existing ones (lines 38–40):\n    from . import html_sanitize\n\nThen at line 328:\n    body_html = html_sanitize.sanitize_html((headline_note or {}).get(\"body_html\") or \"\")\n\nThis exactly mirrors the approach in preview.py lines 133–135. No other changes needed; the fix is confined to verse_of_day.py and has no effect on EPUB build output.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Verified by direct reading of C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\core\\verse_of_day.py lines 328–345. At line 228–230 (inside `pick_verse_for_date`) `body_html` is taken straight from `spec.body_html` (NoteSpec field 7, raw stored value) and packed into the notes payload dict without sanitization. At line 328 it is extracted from that dict and at line 336 inserted literally into `description`, which is then wrapped in `<![CDATA[...]]>` at line 345. No call to `sanitize_html` exists anywhere in verse_of_day.py (grep confirmed zero hits). `notes_io.py` also has no sanitize call at the storage boundary. By contrast, `preview.py` lines 133–135 explicitly imports and calls `sanitize_html(body)` before emitting `body_html` to the `/api/preview` route, and the sanitizer's own docstring (html_sanitize.py lines 22–27) records the project's stated policy: sanitize at every output boundary even on single-user desktop, because EPUBs may be shared and the RSS feed is an HTTP endpoint. The gap is real: a note body containing `<script>alert(1)</script>` or `<img onerror=...>` passes through unfiltered into the RSS CDATA description, which all major RSS readers render as HTML. The proposed fix — `body_html = sanitize_html((headline_note or {}).get('body_html') or '')` with the import from `scripts.core.html_sanitize` — is correct, safe, and consistent with the existing pattern. It touches only `verse_of_day.py`, has zero effect on EPUB build output (KJV editions byte-stability unaffected), and is additive (no schema change). Severity medium is accurate: low practical blast radius on pure localhost usage but a genuine violation of the project's own defense-in-depth policy and a real risk if the server is exposed on a LAN or a note body is maliciously crafted."
    }
   ]
  },
  {
   "dimension": "tests",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "test_by_verse_empty_for_unknown assumes mat 1:1 has no patristic commentary (state-default violation, §8)",
   "file": "YHWH v2.4/tests/test_patristic_gamma3.py",
   "line": "134-140",
   "evidence": "```python\ndef test_by_verse_empty_for_unknown(self):\n    pc = sources.patristic_commentaries()\n    # Matthew has no seed commentary yet\n    empty = pc.for_verse(\"mat\", 1, 1)\n    assert empty == []\n```\nThis is a classic §8 violation: the test assumes `mat 1:1` is empty TODAY as a proxy for 'unknown verse returns []'. It encodes present world-state as a permanent contract. As soon as any future γ.3.x adds a Matthew commentary (the class docstring says 'No NT verses in the seed yet' — explicitly acknowledging this is temporary), this test flips from a legitimate empty-list contract to a false positive that breaks on the first NT expansion. The mutation that proves this is tautological: add a single `mat 1:1` entry to `patristic_commentaries.json` — the test fails immediately even though `for_verse` still correctly returns a list.",
   "fix": "Per §8, test the contract against state the test controls, not world state that can change. Replace the specific-verse check with a genuinely-unknown probe:\n```python\ndef test_by_verse_empty_for_unknown(self):\n    pc = sources.patristic_commentaries()\n    # Use a coordinate that cannot exist in any real corpus:\n    # a non-existent book code is the state-invariant empty case.\n    empty = pc.for_verse(\"nonexistent-book-xyz\", 99, 99)\n    assert empty == [], f\"for_verse on a non-corpus book must return []; got {empty}\"\n```\nThis mirrors the pattern already correctly used in `TestGamma4EthiopianCommentariesLoader.test_by_verse_empty_for_unknown` (line 167-168 of test_ethiopian_gamma4.py) which uses `'nonexistent-book-xyz'`.",
   "verifierFix": "Fix both state-default violations in the same edit to C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\tests\\test_patristic_gamma3.py:\n\n1. test_by_verse_empty_for_unknown (line 134-140) — replace the mat 1:1 probe with a structurally impossible coordinate, mirroring the Ethiopian pattern:\n\n    def test_by_verse_empty_for_unknown(self):\n        from scripts.core import sources\n\n        pc = sources.patristic_commentaries()\n        # Use a coordinate that cannot exist in any real corpus — a non-corpus\n        # book code is the state-invariant empty case (§8: test the contract,\n        # not present world-state).\n        empty = pc.for_verse(\"nonexistent-book-xyz\", 99, 99)\n        assert empty == [], f\"for_verse on a non-corpus book must return []; got {empty}\"\n\n2. test_by_father_empty_for_unknown (line 150-156) — same fix for the Origen probe:\n\n    def test_by_father_empty_for_unknown(self):\n        from scripts.core import sources\n\n        pc = sources.patristic_commentaries()\n        # Use a father name that cannot appear in any real corpus (§8).\n        empty = pc.by_father(\"nonexistent-father-xyz\")\n        assert empty == []\n\nNo other files need changing. The fix is additive-safe and has no effect on build output.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Confirmed by reading C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\tests\\test_patristic_gamma3.py lines 134-140 and the seed file C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\content\\sources\\patristic_commentaries.json.\n\nThe JSON seed contains zero entries with \"book\": \"mat\" (grep confirmed 0 matches). The test comment explicitly states \"Matthew has no seed commentary yet\", acknowledging this is transient world-state, not a permanent contract. The _meta block says \"scope: Curated interpretive summaries verse-keyed to Genesis 1-3\" — NT coverage is intended to arrive in a future γ.3.x. The moment any Matthew entry is added to the JSON, this test fails even though for_verse is functioning correctly.\n\nThe parallel test test_by_father_empty_for_unknown (lines 150-156) has the same structural defect: \"Origen isn't in the seed yet\" anchors on transient absence.\n\nThe Ethiopian counterpart in test_ethiopian_gamma4.py lines 160-168 explicitly documents the §8 rationale and already uses \"nonexistent-book-xyz\" correctly. The pattern to follow exists in-repo.\n\nThe finder's proposed fix is correct and safe: it substitutes a coordinate that is structurally impossible to index (non-corpus book code), making the empty-list contract state-invariant. Touches only a test file; no build/marathon/core code is affected; zero effect on 9 KJV edition byte-stability.\n\nOne additional instance the finder did not flag: test_by_father_empty_for_unknown (line 150-156) should be fixed in the same pass by replacing \"Origen\" with a structurally impossible father name (e.g., \"nonexistent-father-xyz\")."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "ALL_CHECKS registry is 27 checks but every doc/test says 26",
   "file": "YHWH v2.4/tests/test_lint_rules.py",
   "line": "15, 37-38",
   "evidence": "Class docstring: `(8 of 26 ALL_CHECKS had no unit test before)` and comment `# Pin the registry size (26 at mint-7) so a check can't be dropped`. The actual `ALL_CHECKS` dict in `scripts/lint_rules.py` (lines 2045–2082) has 27 entries — `bookcode_canonical` was added in mint-7 Phase A, bringing the total to 27. The `>= 26` assertion still passes (27 >= 26) so it provides no protection against a silent drop to exactly 26.",
   "fix": "In `test_lint_rules.py` update the docstring count to 27 and the floor assertion to `>= 27`. Also update the three docs that say '26 checks': `docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md` lines 158 and 176, and `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md` line 39.",
   "verifierFix": "In `YHWH v2.4/tests/test_lint_rules.py`:\n- Line 15: change \"8 of 26 ALL_CHECKS\" → \"8 of 27 ALL_CHECKS\"\n- Line 36: change comment \"(26 at mint-7)\" → \"(27 at mint-7 after bookcode_canonical)\"\n- Line 38: change `>= 26` → `>= 27`\n\nIn `YHWH v2.4/docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md`:\n- Line 158: \"runs all 26 ALL_CHECKS\" → \"runs all 27 ALL_CHECKS\"\n- Line 176: \"8 of 26 ALL_CHECKS\" → \"8 of 27 ALL_CHECKS\"\n\nIn `YHWH v2.4/docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md`:\n- Line 39: \"26 checks incl. the mint-7 bookcode_canonical\" → \"27 checks (bookcode_canonical added in mint-7 Phase A)\"\n\nNo other files need touching. This fix is entirely within tests and docs — it does not touch any build pipeline, marathon core, or KJV output paths.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading the source files directly.\n\n`C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\lint_rules.py` lines 2045–2082: ALL_CHECKS contains exactly 27 key-value entries (counted manually: \"6.1\", \"6.2\", \"encode_decode\", \"docs\", \"repo_map_complete\", \"superpowers_coherence\", \"freshness\", \"truth_record_budget\", \"commercial_orphans\", \"commercial_terms\", \"retired_terms\", \"triad_plan_consistency\", \"stray_artifacts\", \"changelog_size\", \"dev_doc_sprawl\", \"rules_no_frozen_stats\", \"inflight\", \"untracked_phases\", \"code_doc_sync\", \"ephemeral_doc_pins\", \"atomic_writes\", \"external_http\", \"plan_coherence\", \"manuscript_witnesses\", \"render_coverage\", \"provenance_tier\", \"bookcode_canonical\").\n\n`C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\tests\\test_lint_rules.py` line 15: docstring says \"8 of 26 ALL_CHECKS\"; line 36-38: comment pins \"(26 at mint-7)\" and asserts `len(...) >= 26`. The assertion currently passes (27 >= 26) but would silently pass even if the 27th check were deleted.\n\nDocs confirmed stale: `docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md` line 158 says \"runs all 26 ALL_CHECKS\", line 176 says \"8 of 26 ALL_CHECKS\". `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md` line 39 says \"26 checks incl. the mint-7 bookcode_canonical\" — a self-contradiction since bookcode_canonical is the 27th entry.\n\nThe finding is real: the guard is off-by-one and any single-check deletion would go undetected. Blast radius is bounded to the lint layer only — no shipped EPUB output change, no KJV byte-stability impact. Severity is medium (not high): the consequence is a weakened guard, not a functional regression in any shipped artifact. The proposed fix is correct and safe: update the floor to >= 27 and sync the three doc references."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed all three claims in the finding:\n\n1. `ALL_CHECKS` in `YHWH v2.4/scripts/lint_rules.py` lines 2045–2082 has exactly 27 entries — a grep for the entry pattern returns count=27. The last entry `\"bookcode_canonical\": check_book_codes_canonical` was added in mint-7 Phase A (comment at line 2079 says \"mint-7 ★BUGCLUSTER\").\n\n2. `YHWH v2.4/tests/test_lint_rules.py` line 15 docstring reads \"8 of 26 ALL_CHECKS had no unit test before\" and line 38 asserts `len(self.mod.ALL_CHECKS) >= 26`. Both are stale: the real count is 27, not 26, so the floor would not catch a silent regression from 27 down to exactly 26.\n\n3. Both plan docs say \"26\": `docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md` lines 158 and 176 use \"26 ALL_CHECKS\" / \"8 of 26 ALL_CHECKS\", and `docs/superpowers/plans/2026-05-31-mint-8-audit-plan.md` line 39 says \"26 checks incl. the mint-7 bookcode_canonical\".\n\nThe current `>= 26` assertion passes (27 >= 26) so nothing breaks today, but the guard provides no protection against dropping one check (27 → 26). The blast radius is strictly bounded to test-hygiene / documentation accuracy; there is no shipped-output corruption, no KJV byte-stability impact, and no marathon core touched. The proposed fix (bump floor to `>= 27`, update docstring + docs) is correct and safe. Severity is low (test-maintenance/docs stale), not high — the platform keeps functioning and KJV editions remain byte-stable regardless."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "Mint-cleanup plan status still says 'Phase 6 active'; arc is COMPLETE",
   "file": "YHWH v2.4/docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md",
   "line": "2",
   "evidence": "`**Status:** in progress — Phases 0–5 shipped; Phase 6 (polish) active`. SESSION_STATE headline (confirmed 2026-05-31) reads: '✅ ALL PHASES 0–6 COMPLETE — the mint-cleanup arc is DONE (2026-05-31, HEAD ad945f62)'. The INDEX.md mirrors this stale status in the 'In progress' table at line 15. The `check_superpowers_coherence` lint checks structural presence of the Status header but does not validate semantic currency, so this drift is invisible to the linter.",
   "fix": "Update `Status:` to `COMPLETE — all Phases 0–6 shipped + synced 2026-05-31 (HEAD ad945f62)`. This will also fix the INDEX.md mirror once the lint coherence check re-runs (or update INDEX.md directly if it's manually maintained).",
   "verifierFix": "In `YHWH v2.4/docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md` line 2, change: `**Status:** in progress — Phases 0–5 shipped; Phase 6 (polish) active` to: `**Status:** COMPLETE — all Phases 0–6 shipped + synced 2026-05-31 (HEAD ad945f62)`. Then regenerate INDEX.md (or manually update line 15) to match — the INDEX.md entry for this plan is in the \"In progress\" table and must move to a \"Complete\" section with the updated status string. The lint check will then reflect clean truth. The proposed fix is correct and safe.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Confirmed by direct reading of both files. Line 2 of `YHWH v2.4/docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md` reads exactly: `**Status:** in progress — Phases 0–5 shipped; Phase 6 (polish) active`. Line 15 of `YHWH v2.4/docs/superpowers/INDEX.md` mirrors that same stale status string verbatim. MEMORY.md and SESSION_STATE both declare all Phases 0–6 complete as of 2026-05-31 (HEAD ad945f62). The `check_superpowers_coherence` lint in `scripts/lint_rules.py` (lines 1351–1396) only checks: (a) presence of a `**Status:**` header in the first 25 lines via `_SUPERPOWERS_STATUS_RX`, (b) that the filename appears in INDEX.md text, and (c) that INDEX.md-cited paths exist on disk. It does not compare the status value against any canonical completion registry, so stale \"in progress\" text passes cleanly. This is a genuine truth-record drift. No runtime impact, no KJV edition impact, docs-only. Severity medium is appropriate."
    }
   ]
  },
  {
   "dimension": "byte-stability",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "ebible build pipeline omits check_nested_anchors guard after inject mutates epub_working/",
   "file": "YHWH v2.4/scripts/ebible.py",
   "line": "227-255",
   "evidence": "`cmd_build` runs `inject.py --all-books` (step that writes to `epub_working/`) followed immediately by `build_edition.py --all`, but never calls `check_nested_anchors`. CLAUDE_PROJECT_RULES §8 (line 808): 'Run…`python scripts/check_nested_anchors.py` (run `--fix` if it reports any)…' after inject. The base currently has 0 nested anchors (confirmed by grep), but the next inject that places a `note-ref` marker inside a `vn-link` verse anchor — a known inject failure mode documented in RULES §8 line 813-816 — would leave the base broken and go undetected until `ebible ship` is run. The built EPUB for verse-popups-enabled editions would carry nested `<a>` (invalid XHTML; RSC-005) because `_disable_vn_links` only fires when `verse_popups_enabled=False`.",
   "fix": "In `ebible.py cmd_build`, add a `check_nested_anchors` step between the inject and build-editions steps:\n```python\nsteps = [\n    (\"inject (source → HTML)\", \"inject.py\", [\"--all-books\"]),\n    (\"manifest (corpus hash)\", \"manifest.py\", [\"--build\"]),\n    (\"nested-anchor guard\", \"check_nested_anchors.py\", [\"--fix\"]),  # ADD THIS\n]\n```\nThe `--fix` flag is safe (idempotent; only rewrites files that have nesting). This closes the gap between the process rule and the automated pipeline.",
   "verifierFix": "",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read /YHWH v2.4/scripts/ebible.py lines 227-255 directly. The `steps` list in `cmd_build` contains exactly two entries: `inject.py --all-books` and `manifest.py --build`. There is no `check_nested_anchors.py` call anywhere between inject and the `build_edition.py` invocation. This matches the finder's claim exactly.\n\nI also confirmed that `check_nested_anchors.py` IS called in `scripts/ship-check.py` line 81 (the `ebible ship` subcommand), so the guard is not absent from the project entirely — it just fires at ship time rather than build time.\n\nCLAUDE_PROJECT_RULES §8 lines 808-816 explicitly mandate running `check_nested_anchors` after inject and document exactly why: `build_edition.py` converts `vn-link <a>`→`<span>` during the build, so a built EPUB can report epubcheck 0/0/0/0 even when the base `epub_working/` HTML carries thousands of nested `<a>`. The only tool that catches the base corruption is `check_nested_anchors` (or `test_nested_anchors`).\n\nThe gap is real: `ebible build` is documented as the full pipeline, but it skips the mandatory post-inject guard. A developer who runs `ebible build` (without subsequently running `ebible ship`) can commit broken base HTML without any automated signal. The EPUB outputs themselves will appear valid to epubcheck.\n\nSeverity: the finder's \"medium\" is right. The blast radius is bounded — `ebible ship` does catch it before publishing, and the built EPUB byte-output is not itself corrupted (vn-link conversion happens during build). So this is a process/pipeline gap, not a shipped-output corruption path, keeping it at medium rather than high.\n\nThe proposed fix is correct and safe: adding `(\"nested-anchor guard\", \"check_nested_anchors.py\", [\"--fix\"])` to the `steps` list in `cmd_build` between inject and build_edition. The `--fix` mode is idempotent (no-op when no nesting exists), does not touch marathon core, and cannot affect byte-stability of the 9 KJV editions since the fix only modifies base HTML when actual nesting is detected."
    }
   ]
  },
  {
   "dimension": "byte-stability",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "_resolve_publishing injects wall-clock date into OPF <dc:date>, breaking build reproducibility and poisoning the content-addressable cache",
   "file": "YHWH v2.4/scripts/epub_utils.py",
   "line": "29-30",
   "evidence": "`now_date = datetime.now(timezone.utc).strftime(\"%Y-%m-%d\")` is used as the default for `publication_date` (line 37: `\"publication_date\": now_date`). Since no edition in `content/editions.yaml` sets `publication_date`, `_resolve_publishing` always returns today's date. `patch_opf` (build_edition.py:1142) then writes it into `<dc:date>` inside the packaged `content.opf`. Two builds on different calendar dates therefore produce different OPF bytes — the EPUB is not reproducible. Worse, `build_cache.compute_cache_key` hashes the edition record (which contains no `publication_date`) and `epub_working/` files, but not the current date, so a cache hit the next day serves a stale EPUB with yesterday's `<dc:date>`. The byte-stability test (`test_byte_stability_gate._content_digest`) normalizes `dcterms:modified` and the URN but NOT `<dc:date>`, so a run that crosses midnight would fail.",
   "fix": "Pin `publication_date` in every edition profile in `content/editions.yaml` (e.g. `publication_date: \"2026-01-01\"`) so `_resolve_publishing` returns a stable value instead of `datetime.now()`. Alternatively, change the fallback in `_resolve_publishing` from `now_date` to a project-level constant such as `\"2026-01-01\"` and document that editions.yaml is the canonical override. Either way, add `_DC_DATE_RE = re.compile(r'<dc:date>[^<]*</dc:date>')` normalization alongside `_MODIFIED_RE` in `test_byte_stability_gate._content_digest`.",
   "verifierFix": "Two complementary changes, both safe and additive:\n\n1. In `YHWH v2.4/scripts/epub_utils.py` line 37, replace the `now_date` fallback with a pinned project constant:\n   ```python\n   \"publication_date\": \"2026-01-01\",\n   ```\n   This eliminates the `datetime.now()` call for this field entirely. No edition in editions.yaml currently sets `publication_date`, so this is a pure fallback change with no schema breakage.\n\n2. In `YHWH v2.4/tests/test_byte_stability_gate.py`, add a `<dc:date>` normalizer alongside the existing `_MODIFIED_RE` (line 36):\n   ```python\n   _DATE_RE = re.compile(r\"<dc:date>[^<]*</dc:date>\")\n   ```\n   And apply it in `_content_digest` after line 61:\n   ```python\n   text = _DATE_RE.sub(\"<dc:date>NORMALIZED</dc:date>\", text)\n   ```\n   This makes the stability test robust even if a future maintainer reintroduces a dynamic date default.\n\nOptionally, add a `publication_date` key to each edition record in `content/editions.yaml` with a fixed value (e.g. `\"2026-01-01\"`) to make the intent explicit in config rather than relying on the fallback constant. Do NOT touch any marathon core files.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All three sub-claims were independently verified by reading the actual source files.\n\n(a) Code does exactly what evidence claims:\n- `epub_utils.py` lines 29-30, 37: `now_date = datetime.now(timezone.utc).strftime(\"%Y-%m-%d\")` is the literal fallback for `publication_date` in `_resolve_publishing`. Confirmed.\n- `content/editions.yaml` has no `publication_date` key in any edition record (grep returned no matches). So the fallback fires on every build. Confirmed.\n- `build_edition.py` lines 1139-1144: `patch_opf` writes `pub['publication_date']` directly into `<dc:date>` in the OPF. Confirmed.\n- `test_byte_stability_gate._content_digest` normalizes `_URN_RE` and `_MODIFIED_RE` but NOT `<dc:date>`. Confirmed.\n- `build_cache.compute_cache_key` hashes the raw edition record from editions.yaml (which never carries `publication_date`), so the cache key is date-agnostic while the stored EPUB contains today's date stamp — confirmed. A cross-day cache hit would serve a stale `<dc:date>`.\n\n(b) Genuine defect, not intended: The comment in `_resolve_publishing` says \"filling defaults … The defaults match the PUBLISHING_DEFAULTS dict in web.py\" — this is described as a UI preview default, not an intentional \"stamp with today\" semantic. There is no comment anywhere expressing intent to stamp wall-clock time into OPF for reproducible builds. The byte-stability test even documents that it normalizes volatile fields — `<dc:date>` is clearly in that category but was omitted.\n\n(c) Severity recalibration: The blast radius is bounded. This is a single-user local desktop app. The byte-stability test failure only fires if two successive builds of the same edition straddle midnight (uncommon in practice). The build-cache stale-date issue is cosmetic EPUB metadata — no scripture, notes, or structural content is affected. \"High\" overstates the practical impact; \"medium\" is appropriate.\n\n(d) Proposed fix is correct and safe: Pinning `publication_date` in editions.yaml or changing the fallback in `_resolve_publishing` from `datetime.now()` to a project constant are both additive changes that cannot break byte-stability of the 9 KJV editions (the field is currently absent from all edition records, so adding it is purely additive per the schema-additive rule). Adding a `_DC_DATE_RE` normalization in `_content_digest` alongside `_MODIFIED_RE` is also correct. Neither change touches marathon core files. The fix is safe as stated."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All three claims in the finding independently confirmed by reading the code:\n\n(a) CODE DOES WHAT EVIDENCE CLAIMS: `epub_utils.py` lines 29-30/37 confirmed — `datetime.now(timezone.utc)` is called twice and the formatted date string is assigned to the `\"publication_date\"` key in `defaults`. A grep of `content/editions.yaml` for `publication_date` returned no matches, so `edition.get(\"publication_date\")` is always falsy and the `now_date` fallback is always taken. `build_edition.py` lines 1139-1145 confirmed — `patch_opf` does a `re.sub` that writes `pub['publication_date']` into `<dc:date>`. `test_byte_stability_gate.py` lines 35-36 confirmed — only `_URN_RE` and `_MODIFIED_RE` are normalized; `<dc:date>` is not excluded from the digest.\n\n(b) IS IT A GENUINE DEFECT: Yes, with a calibrated blast radius. The byte-stability test itself does NOT fail in normal practice because it builds the same edition twice within a single test run — both calls to `datetime.now()` land on the same calendar date, so the digests match. The real exposure is (1) cross-midnight or cross-day reproducibility: two builds on different dates produce EPUBs with different `<dc:date>` bytes not normalized by `_content_digest`, and (2) a stale-cache correctness issue: `compute_cache_key` hashes the edition record from `editions.yaml` (which has no `publication_date` key) and does not include the runtime date, so a cache hit on day 2 returns a stored EPUB whose `<dc:date>` reads yesterday's date while a fresh build would embed today's. Both are real defects.\n\n(c) SEVERITY RECALIBRATION: The finder rated this \"high.\" The actual blast radius is narrower than claimed. The byte-stability test does NOT fail in practice (both builds same day). The scripture and notes bytes are unaffected. The only live risk is (i) a developer rebuilding across midnight sees stale metadata in the cached artifact, and (ii) `<dc:date>` is wrong in the served EPUB when a cache hit is returned after a date change — a metadata-only error in a local single-user app. Downgrade to medium.\n\n(d) PROPOSED FIX: The proposed fix is correct and safe. Pinning `publication_date` in every edition profile in `editions.yaml` is the cleanest solution — it makes `_resolve_publishing` return a stable value without touching any code, satisfies the additive-schema constraint (existing editions without the key continue to get the `now_date` fallback if they aren't updated), and does not touch marathon core. Adding `<dc:date>` normalization to `_content_digest` is a correct complementary guard. The alternative of changing the fallback to a hardcoded constant is also safe but less principled (a future user's custom edition would get a stale date if they forget to set it). The editions.yaml pin is recommended as the primary fix; the test normalization is a good belt-and-suspenders addition."
    }
   ]
  },
  {
   "dimension": "data-validity",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "`aes` notes at KJV chapters 11-16 (roughly 70 notes) are permanently uninjectable: base HTML only has chapter anchors 1-10",
   "file": "YHWH v2.4/content/notes/aes.py",
   "line": "204-919",
   "evidence": "The base HTML `epub_working/index_split_028.html` has chapter anchors `id=\"ch-b25-c1\"` through `id=\"ch-b25-c10\"` only (verified by grepping — `ch-b25-c11` through `ch-b25-c16` are absent). `inject.py:259` builds `start_marker = f'id=\"ch-{bxx}-c{ch}\"'`; for a note at chapter 11 this becomes `id=\"ch-b25-c11\"`, which is not found, so the note is silently dropped. `coord_in_canonical_extent` in `promote.py:403` validated these notes against the KJV `aes.py` skeleton (which uses LXX chapter numbering 10-16), passing all of chapters 11-16 as valid. But the HTML uses sequential numbering 1-10. `aes.py` line 204 shows the first chapter-11 note: `(11, 11, \"\", \"light\", \"lang-hebrew\", ...)`. Notes at chapters 13, 14, 15, and 16 are also present (confirmed at lines 458, 601, 700, 832). Approximately 70 of the ~90 total `aes` notes are permanently stranded.",
   "fix": "The notes at chapters 11-16 need their chapter coordinates remapped to the HTML sequential scheme (chapters 1-10 → HTML anchors ch-b25-c1 through ch-b25-c10). Determine the mapping: KJV chapter 10 = HTML chapter 10 (the 7th addition, already at end), KJV 11=HTML 1, 12=HTML 2, 13=HTML 3, 14=HTML 4, 15=HTML 5, 16=HTML 6 (or verify via the actual HTML structure). Then rewrite the affected notes tuples with the correct HTML chapter numbers. Additionally, fix the upstream defect in `promote.py:coord_in_canonical_extent` for `aes`: add a pre-promotion check that rejects notes whose chapter is not in `range(1, books.yaml[\"ch_count\"] + 1)` for Strategy-B books where `ch_count` reflects the HTML chapter scheme, not the KJV scheme.",
   "verifierFix": "The finder's proposed chapter remap (11→1, 12→2, etc.) is wrong and would inject notes at incorrect verse positions. HTML chapter 1 has its own verses 1–22 (Esther narrative); a note remapped to (1, 11, ...) would target verse 11 of the main Esther narrative, not the KJV-ch-11 addition content. The merger is as extended long-verses, not chapter-parallel verse slots.\n\nSafe options:\n(a) **Delete the stranded drafts**: Remove all notes at chapters 11–16 from `content/notes/aes.py`. Every one of them contains `[Reviewer: extend this...]` — they are unreviewable stubs with no finished content. Removing them eliminates the false-injection-succeeded impression without losing any finished content. This is the right call for mint quality.\n\n(b) **Add a lint warning**: In `lint_rules.py` (or equivalent), add a check that warns when `aes.py` contains notes at chapters outside the HTML's 1–10 range (i.e., chapters > `books.yaml[\"aes\"].ch_count`). This would have caught this at add-time rather than silently at inject-time.\n\nDo NOT change `promote.py:coord_in_canonical_extent` — it correctly validates against the KJV skeleton (chapters 10–16). The defect is in the note coordinates stored in `content/notes/aes.py`, not in the promote guard. Any future notes for the KJV-ch-11–16 Addition content must be authored with the HTML's merged coordinates: the text appears at HTML aes 1:1 (KJV ch 11), 3:13 (KJV ch 13 Haman's decree), 4:17–end (KJV ch 13 Mordecai's prayer + KJV ch 14 Esther's prayer), 5:1 (KJV ch 15), 8:12 (KJV ch 16), and 10:3 (KJV ch 10 epilogue is at HTML ch 10). Each note must be mapped verse-by-verse to its merged position — this cannot be automated without reading the HTML content alignment carefully.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "\n**What I independently verified:**\n\n1. `epub_working/index_split_028.html` — confirmed by direct grep: chapter anchors `id=\"ch-b25-c1\"` through `id=\"ch-b25-c10\"` only; `ch-b25-c11` through `ch-b25-c16` are absent.\n\n2. `content/notes/aes.py` — confirmed notes exist at chapters 11 (line 182), 13 (line 226), 14 (line 600), 15 (line 699), 16 (line 831). Count: approximately 68 of 83 total notes are at chapters 11–16.\n\n3. `scripts/inject.py:733` — confirmed: `if f'id=\"ch-{bxx}-c{ch}\"' in text:` uses the note's chapter coordinate verbatim. Notes at ch 11–16 search for `id=\"ch-b25-c11\"` etc., which don't exist in any file. They land in `stats[\"missing_anchor\"]` and are silently dropped.\n\n4. `content/books.yaml:297` — confirmed `ch_count: 10` for `aes`, reflecting the HTML's sequential 1–10 scheme.\n\n5. `content/translations/kjv/aes.py` — confirmed the KJV skeleton has chapters 10–16 only (no chapters 1–9). So `coord_in_canonical_extent(\"aes\", 11, 11)` returns True: promote.py correctly validates against the KJV skeleton. The defect is NOT in promote.py — it correctly allows these through. The defect is the mismatch between the notes' KJV chapter numbering (10–16) and the HTML's sequential chapter numbering (1–10 where chapter 10 = KJV chapter 10, and KJV chapters 11–16 are MERGED as long extended verses within chapters 1–10).\n\n6. The HTML introduction (line 35) explains the design: the LXX Additions are merged as bracketed extensions into the main Esther chapters 1–10. KJV ch 11 = added into HTML ch 1 verse 1 (very long merged verse). KJV ch 12 = added into ch 2. KJV ch 13 = added into ch 3. KJV ch 14 = added into ch 4. KJV ch 15 = merged into ch 5 verse 1. KJV ch 16 = added into ch 8.\n\n7. All 83 notes contain `[Reviewer: extend this with context, theological reading...]` — they are ALL unreviewable draft placeholders, not finished content. The ~68 stranded notes are incomplete drafts.\n\n**Verdict on finding accuracy:** The core finding is confirmed: ~68 notes at chapters 11–16 in `aes.py` are silently dropped at injection time because the HTML uses a merged sequential numbering scheme (1–10) while the notes use the KJV/LXX Additions numbering (10–16 for the additions content). This is a real defect.\n\n**Verdict on proposed fix:** The finder's proposed fix is materially wrong and unsafe. The suggestion \"remap KJV 11→HTML 1, 12→2, 13→3, 14→4, 15→5, 16→6\" is incorrect. HTML chapter 1 has its own verses 1–22 (the main Esther narrative); KJV chapter 11 content is merged into the LONG verse 1 of HTML chapter 1 — a note at (1, 11, ...) would wrongly target HTML chapter 1 verse 11 (\"to bring Vashti to him\"), which is unrelated content. The actual merged verses span dozens of sub-verses folded into single long verses (HTML 1:1, 3:13, 4:17, 8:12, 10:3). Remapping the notes requires matching each KJV addition verse to its merged position in the HTML, which is a non-trivial, verse-by-verse content task that cannot be derived algorithmically.\n\n**Correct severity:** Medium (downgraded from high). The ~68 stranded notes are real and permanently uninjectable in their current form, which is a genuine data-validity defect in `content/notes/aes.py`. However, blast radius is bounded: (a) all 68 stranded notes are draft placeholders with `[Reviewer: extend this...]` bodies — no finished content is lost from shipped output; (b) the 9 KJV editions are not affected (aes only appears in catholic/orthodox/ethiopian canons); (c) the one functional note at chapter 10 that does inject is unaffected. The chapter-10 notes (approximately 15 notes) do inject correctly. No shipped-output corruption occurs — the EPUB builds cleanly, the notes are merely absent rather than malformed.\n\n**Corrected fix:** The safe fix is to either (a) delete the stranded draft notes at chapters 11–16 from `aes.py` (they are all `[Reviewer: extend this...]` stubs with no finished content, so removing them loses nothing of value and stops the misleading impression that they will appear in output), or (b) leave them as-is with a comment explaining the HTML coordinate mismatch until the HTML base is redesigned or the notes are manually remapped verse-by-verse to the merged HTML coordinates. Do NOT attempt the finder's chapter-remap; it would silently inject notes at incorrect (unrelated) verse positions. The `promote.py` coord guard does not need to be changed — it correctly validates against the KJV skeleton. The real gap is a missing lint rule that detects when `aes` notes at chapters 11–16 are written, given that the HTML only has chapters 1–10.\n"
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "\n**Independently verified** by reading the actual code and HTML.\n\n**Confirmed facts:**\n\n1. `YHWH v2.4/epub_working/index_split_028.html` has anchors `id=\"ch-b25-c1\"` through `id=\"ch-b25-c10\"` and nothing beyond c10. Confirmed by direct grep (10 anchors found, zero for c11+).\n\n2. `YHWH v2.4/content/books.yaml` line 297 confirms `aes` has `ch_count: 10` and `strategy: \"B\"` and `bxx: \"b25\"`.\n\n3. `YHWH v2.4/content/notes/aes.py` lines 116–918 contain notes at chapters 11 (lines 116–224), 13 (lines 225–477), 14 (lines 478–675), 15 (lines 676–807), and 16 (lines 808–918). Chapter 10 notes exist at lines 16–114. Chapter 12 is absent from notes.\n\n4. `YHWH v2.4/scripts/inject.py:259` confirms `start_marker = f'id=\"ch-{bxx}-c{ch}\"'`. For a note at chapter 11, this produces `id=\"ch-b25-c11\"`, which is not found in the HTML → `stats[\"missing_anchor\"]` at line 738. The notes are silently dropped (yellow warning only, not a build-error).\n\n5. `YHWH v2.4/scripts/core/canonical_verse_counts.py` validates `(book, chapter, verse)` against the KJV skeleton (`content/translations/kjv/aes.py`). The KJV skeleton runs chapters 10–16, so chapters 11, 13–16 pass `coord_in_canonical_extent` cleanly — the promote guard does NOT catch this mismatch.\n\n6. `YHWH v2.4/content/translations/kjv/aes.py` confirms the KJV uses chapter numbers 10–16 (the KJV \"Additions to Esther\" are appended as chapters 10–16); the WEB base HTML sequentially numbers the same content 1–10. The `aes.py` notes file was authored using KJV chapter numbers, while inject.py expects WEB sequential chapter numbers.\n\n**Severity recalibration (high → medium):** The defect is real and ~70 notes are permanently uninjectable in their current form. However: (a) no shipped-EPUB output corruption occurs — the notes simply don't appear rather than producing malformed HTML; (b) the 9 KJV editions are not affected (these editions may include `aes` text, but `lang-hebrew` Strong's notes landing in the wrong bucket cause zero byte-instability); (c) `aes` is a peripheral deuterocanonical book; (d) the notes file itself was likely freshly machine-generated and has not been human-reviewed (all notes carry `[Reviewer: extend this...]` placeholders). No data is lost — the source tuples are in the file; they just need renumbering.\n\n**Proposed fix analysis — partially correct but needs correction:**\n\nThe finder's proposed mapping (`KJV11=HTML1, KJV12=HTML2, KJV13=HTML3, KJV14=HTML4, KJV15=HTML5, KJV16=HTML6`) is wrong. The WEB has 10 chapters and the KJV has 7 (chapters 10–16). The trivial arithmetic `KJV_ch - 10 = HTML_ch` would give KJV10→c0 (invalid), KJV11→c1, KJV12→c2, ..., KJV16→c6 — leaving WEB chapters 7–10 unmapped. This mapping must be verified against the actual WEB content before any renumbering.\n\nAdditionally, the finder's secondary fix (\"add a pre-promotion check in `promote.py:coord_in_canonical_extent` for Strategy-B books\") targets the wrong layer. `coord_in_canonical_extent` correctly validates against the KJV canonical extent (its documented purpose); the root problem is that `aes.py` notes were authored with KJV chapter numbers, not HTML chapter numbers.\n\nThe finder also cites `aes.py` line 204 as the first chapter-11 note; confirmed: line 204–213 contains `(11, 11, \"\", \"light\", ...)`. The \"roughly 70 notes\" count is consistent with the file (chapters 11+13+14+15+16 cover approximately lines 116–918, representing ~70 tuples at 11 lines each).\n"
    }
   ]
  },
  {
   "dimension": "concurrency-caching",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "translations._book_index caches stale verse-lookup dict, bypassing mtime-keyed freshness of _load_book_cached",
   "file": "YHWH v2.4/scripts/core/translations.py",
   "line": "120-128",
   "evidence": "@functools.lru_cache(maxsize=256)\ndef _book_index(translation: str, book_code: str) -> dict[tuple[int, int], str] | None:\n    verses = _load_book(translation, book_code)\n    ...\n    return {(c, v): t for (c, v, t) in verses}\n\nThe cache key is (translation, book_code) only — no mtime. _load_book() is itself mtime-keyed (calls _load_book_cached(path_str, mtime_ns)), so a re-read after a file edit returns fresh tuples. But _book_index never calls _load_book() again after the first call: it permanently holds the dict built from the original mtime snapshot. All get_verse() / get_chapter() calls go through _book_index, so every verse lookup is stale until clear_cache() is called explicitly. During a 30-90s build_edition run (build_edition.py calls get_verse() thousands of times), an in-flight edit to a translation .py file would silently serve old verse text for the rest of the build.",
   "fix": "Include mtime_ns in the _book_index cache key so the dict is rebuilt when the file changes. Change the signature to:\n\n@functools.lru_cache(maxsize=256)\ndef _book_index(translation: str, book_code: str, mtime_ns: int) -> dict[tuple[int, int], str] | None:\n    path = _book_path(translation, book_code)\n    verses = _load_book(translation, book_code)   # _load_book_cached is keyed on (path_str, mtime_ns) already\n    ...\n\nand update _load_book to thread mtime_ns through:\n\ndef _load_book(translation: str, book_code: str):\n    path = _book_path(translation, book_code)\n    try:\n        mtime_ns = path.stat().st_mtime_ns\n    except OSError:\n        return None\n    return _load_book_cached(str(path), mtime_ns)\n\ndef _book_index_for(translation: str, book_code: str):\n    path = _book_path(translation, book_code)\n    try:\n        mtime_ns = path.stat().st_mtime_ns\n    except OSError:\n        return None\n    return _book_index(translation, book_code, mtime_ns)\n\nChange get_verse/get_chapter to call _book_index_for instead of _book_index directly. This mirrors exactly the _load_notes_cached / load_notes split in notes_io.py (RULES §7.1 canonical pattern). The clear_cache() function still calls _book_index.cache_clear() unchanged.",
   "verifierFix": "The proposed fix is correct. The minimal safe implementation in `C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\core\\translations.py`:\n\n1. Add `mtime_ns: int` parameter to `_book_index`:\n```python\n@functools.lru_cache(maxsize=256)\ndef _book_index(translation: str, book_code: str, mtime_ns: int) -> dict[tuple[int, int], str] | None:\n    verses = _load_book(translation, book_code)\n    if verses is None:\n        return None\n    return {(c, v): t for (c, v, t) in verses}\n```\n\n2. Add a thin wrapper that resolves mtime and calls the above:\n```python\ndef _book_index_for(translation: str, book_code: str) -> dict[tuple[int, int], str] | None:\n    path = _book_path(translation, book_code)\n    try:\n        mtime_ns = path.stat().st_mtime_ns\n    except OSError:\n        return None\n    return _book_index(translation, book_code, mtime_ns)\n```\n\n3. Change `get_verse` to call `_book_index_for` instead of `_book_index`:\n```python\ndef get_verse(translation: str, book_code: str, chapter: int, verse: int) -> str | None:\n    idx = _book_index_for(translation, book_code)\n    if idx is None:\n        return None\n    return idx.get((chapter, verse))\n```\n\n4. `clear_cache()` needs no change — `_book_index.cache_clear()` still works on the now-3-arg function.\n\nThis mirrors the `_load_book_cached`/`_load_book` split already in place and is safe: additive, no marathon-core files touched, byte-stable for the 9 KJV editions.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The code at lines 120-128 of translations.py is exactly as described. `_book_index` is `lru_cache`'d on `(translation, book_code)` alone. Once populated, it never re-calls `_load_book()`, so a file edit is invisible to `get_verse()` until `clear_cache()` is explicitly called. This creates a verified asymmetry: `get_chapter()` (line 175) calls `_load_book()` directly and benefits from mtime-keyed freshness on every call, while `get_verse()` (line 169) goes through the permanently-cached `_book_index` dict. Both are public API — during a live web server session (the only long-lived process), editing a translation file would cause `get_chapter()` to return updated verse text while `get_verse()` returns the old cached text. This is a genuine intra-module inconsistency. Severity is recalibrated to medium (not high) because: (1) single-user local desktop app — no concurrent in-build edits; (2) `clear_cache()` exists as the documented mitigation; (3) the 9 KJV editions' byte stability is unaffected (builds are batch processes, not long-lived); (4) the web server is the only scenario where the inconsistency is observable in practice, and even then it requires editing a translation `.py` file mid-session. The proposed fix is directionally correct (thread mtime_ns through a `_book_index_for` wrapper, mirroring the `_load_book_cached`/`_load_book` split). However it can be simplified: since `_load_book()` already performs the `stat()` internally, the cleanest implementation is to have `_book_index_for` call `_book_path().stat().st_mtime_ns` and pass it as the cache key to `_book_index(translation, book_code, mtime_ns)`, then update `get_verse` to call `_book_index_for` instead. This does add one `stat()` syscall per `get_verse()` call (same overhead already paid by `get_chapter()` which calls `_load_book()` which does the same `stat()`), so the cost is already accepted in the existing design. The fix is safe: additive signature change, `clear_cache()` still calls `_book_index.cache_clear()` unchanged, no marathon-core files touched, 9 KJV editions unaffected."
    },
    {
     "refuted": false,
     "confidence": "medium",
     "reasoning": "The code at lines 120-128 of `YHWH v2.4/scripts/core/translations.py` is exactly as described: `_book_index` is decorated with `@functools.lru_cache(maxsize=256)` keyed only on `(translation, book_code)`, while `_load_book_cached` beneath it is keyed on `(path_str, mtime_ns)`. On the first call, `_book_index` delegates to `_load_book` (which is mtime-aware), builds the dict, and caches it permanently under the bare `(translation, book_code)` key. Subsequent calls return the stale cached dict without re-checking mtime. The finding is technically real.\n\nHowever, the HIGH severity rating is wrong for the following reasons specific to this project:\n\n1. Single-user local desktop app. Translation `.py` files under `content/translations/` are static pre-extracted artifacts. No code path in the build or web server writes to them during a `build_edition` run. The \"in-flight edit during a 30-90s build\" scenario that motivates the finding cannot plausibly occur in practice.\n\n2. `get_chapter` (lines 175-184) is NOT routed through `_book_index` — it calls `_load_book` directly, so it already picks up mtime changes. The inconsistency is bounded to `get_verse` only.\n\n3. `clear_cache()` at line 229 explicitly clears both `_load_book_cached` and `_book_index`, and is called in tests and after bulk re-extraction — the documented escape valve exists and works.\n\n4. The proposed fix is structurally correct and mirrors the `notes_io` pattern (RULES §7.1), but it adds an extra `stat()` syscall on every `get_verse` invocation — a hot path called thousands of times per build — with zero real-world safety benefit in this single-process, no-concurrent-writer context. That is a real cost with no practical gain.\n\n5. No KJV byte-stability risk and no shipped-output corruption: the issue only matters if a file changes between the first and a later lookup in the same process run.\n\nVerdict: Real theoretical inconsistency, not a material defect at this app's scope. Severity should be LOW/INFO, not HIGH."
    }
   ]
  },
  {
   "dimension": "concurrency-caching",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "scripts/build_edition.py subprocess.run for build_epub.py missing stdin=subprocess.DEVNULL — WinError 6 in dev build path",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "3022-3033",
   "evidence": "result = subprocess.run(\n    [\n        sys.executable,\n        str(REPO_ROOT / 'scripts' / 'build_epub.py'),\n        str(output_path),\n        '--epub-dir',\n        str(tmp),\n        '--no-bump',\n    ],\n    capture_output=True,\n    text=True,\n)\n\nThe non-frozen (dev mode) path of build_one() launches build_epub.py as a subprocess. No stdin=subprocess.DEVNULL. Any CLI invocation of build_edition.py from PowerShell, or the web server's build trigger, will hit WinError 6 on this call. The frozen path goes in-process and is safe; only the subprocess branch (the default dev path) is affected.",
   "fix": "Add stdin=subprocess.DEVNULL:\n\nresult = subprocess.run(\n    [\n        sys.executable,\n        str(REPO_ROOT / 'scripts' / 'build_epub.py'),\n        str(output_path),\n        '--epub-dir',\n        str(tmp),\n        '--no-bump',\n    ],\n    capture_output=True,\n    text=True,\n    stdin=subprocess.DEVNULL,  # W-W1: Windows PowerShell guard\n)",
   "verifierFix": "Add `stdin=subprocess.DEVNULL` to the subprocess.run call at `YHWH v2.4/scripts/build_edition.py` lines 3022-3033:\n\n```python\nresult = subprocess.run(\n    [\n        sys.executable,\n        str(REPO_ROOT / \"scripts\" / \"build_epub.py\"),\n        str(output_path),\n        \"--epub-dir\",\n        str(tmp),\n        \"--no-bump\",\n    ],\n    capture_output=True,\n    text=True,\n    stdin=subprocess.DEVNULL,  # W-W1: Windows PowerShell guard\n)\n```\n\nThe finder's proposed fix is correct and safe as stated.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Confirmed by direct code reading. The `else` branch at `YHWH v2.4/scripts/build_edition.py` lines 3022-3033 calls `subprocess.run(...)` with `capture_output=True, text=True` but no `stdin=subprocess.DEVNULL`. Every other `subprocess.run` in the codebase (verified across add_note.py, add_kind.py, audit_dead_code.py, audit_types.py, ci.py, verify.py, epubcheck.py, lint_rules.py, extract_parallel_pdf.py, ebible.py, ship-check.py) consistently includes `stdin=subprocess.DEVNULL` with explicit W-W1 comments. This is an omission, not a design choice. The blast radius: this branch is the default dev path (runs whenever `sys.frozen` is False, i.e. normal `ebible build` or direct CLI invocation from PowerShell). The outer `ebible.py` call uses DEVNULL, but that only protects the outer boundary — once `build_edition.py` is running in its own process, it inherits a closed/invalid stdin handle from PowerShell, and the second-level spawn of `build_epub.py` without DEVNULL will trigger WinError 6. The proposed fix is correct and safe: adding `stdin=subprocess.DEVNULL` is a no-op on functional behavior (build_epub.py does not read stdin), is byte-stable on the 9 KJV editions, and does not touch marathon core."
    }
   ]
  },
  {
   "dimension": "cross-module",
   "kind": "find",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "edition_canon_books sorted alphabetically — violates RULES §6.1 canonical order (affects 3 UI dropdowns)",
   "file": "YHWH v2.4/scripts/web_editions.py",
   "line": "335",
   "evidence": "`edition_canon_books = {ed_id: sorted(books) for ed_id, books in _mtx.edition_canon_books.items()}` — Python `sorted()` on a set of book codes produces alphabetical order (1ch, 1co, 1en, 1jn, 1ki…). The comment at lines 303-307 in the same function explicitly states: 'Per CLAUDE_PROJECT_RULES.md §6.1, any per-book UI must list books in books.yaml order… The UI reads the order from this payload — never sorts on its own — so the canonical-order rule has one source of truth.' Line 335 directly contradicts that stated intent. Three JS consumers iterate this list in order rather than re-sorting: (1) `customize.py:515` — 'Upload per-book art' `<select>` dropdown; (2) `customize.py:250-256` — customize-page EPUB preview book picker; (3) `wizard.py:893-898` — wizard preview book picker. The `web_covers.py` counterpart at line 189 correctly uses `sorted(canon_set, key=lambda c: book_rank.get(c, 1_000_000))` via a rank derived from `config.load_books()` — showing the correct pattern already exists in the same codebase.",
   "fix": "Replace line 335 with a canonical-order list comprehension that filters `config.load_books()` rather than sorting the set:\n\n```python\n_book_order = [b[\"code\"] for b in config.load_books()]\nedition_canon_books = {\n    ed_id: [c for c in _book_order if c in books]\n    for ed_id, books in _mtx.edition_canon_books.items()\n}\n```\n\nThis mirrors the pattern already used in `web_editions.py:93` (`book_order = list(books_idx.keys())`) and `web_covers.py:178-189`. No schema change; the API shape is identical (a list per edition-id). Add an entry to `tests/test_scripts.py` — assert that `api_customize_data()[\"edition_canon_books\"][\"ethiopian-tewahedo\"]` has `\"gen\"` before `\"mat\"` and `\"mat\"` before `\"rev\"` (mirrors the existing `check_encoder_canonical_order` fixture pattern).",
   "verifierFix": "Line 315 in the same function already computes `books_canonical` from `config.load_books()`. Reuse it to avoid a redundant call:\n\n```python\n# books_canonical is already defined at line 315:\n# books_canonical = [{\"code\": b[\"code\"], ...} for b in config.load_books()]\n_book_order = [b[\"code\"] for b in books_canonical]\nedition_canon_books = {\n    ed_id: [c for c in _book_order if c in books]\n    for ed_id, books in _mtx.edition_canon_books.items()\n}\n```\n\nThis is functionally identical to the finder's fix but avoids calling `config.load_books()` twice. The finder's fix is otherwise correct and safe; this is a minor efficiency improvement only.\n\nCompanion test addition to `tests/test_scripts.py` (mirrors the finder's suggestion, no changes needed to the fix itself):\n```python\ndef test_customize_data_edition_canon_books_canonical_order(self):\n    d = self.web.api_customize_data()\n    cb = d[\"edition_canon_books\"]\n    et = cb[\"ethiopian-tewahedo\"]\n    assert et.index(\"gen\") < et.index(\"mat\") < et.index(\"rev\")\n```",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading the cited code:\n\n1. `YHWH v2.4/scripts/web_editions.py:335` contains exactly `edition_canon_books = {ed_id: sorted(books) for ed_id, books in _mtx.edition_canon_books.items()}`. Python `sorted()` on a `set[str]` of book codes yields alphabetical order (1ch, 1co, gen, mat, rev...).\n\n2. The comment at lines 303-307 of the same function explicitly states the canonical-order rule from RULES §6.1 and that \"the UI reads the order from this payload — never sorts on its own — so the canonical-order rule has one source of truth.\" Line 335 directly contradicts this stated invariant.\n\n3. RULES §6.1 at `dev/CLAUDE_PROJECT_RULES.md:377-386` explicitly prohibits alphabetical book ordering in any UI: \"Do not sort books alphabetically, by note count, or by importance.\"\n\n4. All three named JS consumers confirmed by reading the template files — `templates/customize.py:250`, `templates/customize.py:515`, `templates/wizard.py:893` — all call `.map(code => ...)` on the received array in order, rendering `<option>` elements without re-sorting. The rendered dropdown order is entirely determined by what the server sends.\n\n5. The correct pattern is already in the codebase at `web_covers.py:189`: `sorted(canon_set, key=lambda c: book_rank.get(c, 1_000_000))`.\n\n6. No existing test guards canonical order in this payload (`test_scripts.py:1950-1965` only checks membership counts and presence/absence of specific codes, not ordering).\n\n7. The fix is safe: it only changes list order in a UI-only API payload. No EPUB output path touches `edition_canon_books` from this payload. The 9 KJV byte-stable editions are unaffected. The API schema is unchanged (it was already a list, since `sorted()` returns a list).\n\nSeverity calibration: This is a clear §6.1 violation visible in 3+ UI dropdowns and the book picker; not a data-corruption or build-output bug. Severity is medium (UI ordering defect affecting usability and violating an explicit project rule, but no output corruption and no build instability)."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified all claims:\n\n1. `YHWH v2.4/scripts/web_editions.py:335` does exactly `{ed_id: sorted(books) for ed_id, books in _mtx.edition_canon_books.items()}`. Python `sorted()` on a set of string book-codes produces alphabetical order (1ch, 1co, 1en, 1jn, 1ki…). Confirmed.\n\n2. The comment at lines 303-307 in the same function explicitly states the canonical-order intent and that \"The UI reads the order from this payload — never sorts on its own.\" The implementation directly contradicts this stated invariant. Confirmed.\n\n3. Three JS consumers iterate the list in insertion order without re-sorting:\n   - `templates/customize.py:250` — `canonBooks.map(code => ...)` for the EPUB preview book picker — confirmed.\n   - `templates/customize.py:515` — `((DATA.edition_canon_books||{})[e.id]||[]).map(code => ...)` for the per-book art upload select — confirmed.\n   - `templates/wizard.py:893` — `canonBooks.map(code => ...)` for the wizard preview picker — confirmed.\n\n4. The `wirePopupLanguageSection` function (templates/customize.py:968) is self-healing for its `renderOverrides` sub-section: it builds `booksInCanon` by filtering `DATA.books_canonical` (canonical order) by `canonSet` (a Set from the alphabetical list, so order-independent). The `canonRank` used in the two `.sort()` calls (lines 985-988, 1200-1203) is therefore correct. However, the three simple dropdown pickers cited are NOT self-healing.\n\n5. RULES §6.1 (read at `dev/CLAUDE_PROJECT_RULES.md:379-382`) explicitly states: \"Do not sort books alphabetically, by note count, or by 'importance'.\" This is a hard requirement, not a stylistic preference (stated at line 252 as \"A hard requirement, not a stylistic preference\").\n\n6. `web_covers.py:189` already uses the correct pattern: `sorted(canon_set, key=lambda c: book_rank.get(c, 1_000_000))`. The pattern for a correct fix is established.\n\n7. The proposed fix is safe: `config.load_books()` is called in the same file at line 83 (different function); the API shape is unchanged (a list per edition-id, `sorted()` already returns a list); no marathon core touched; KJV editions are byte-stable (this only affects the UI API payload `/api/customize`).\n\n8. The existing test at `tests/test_scripts.py:1950` only checks membership and counts, not ordering — it will not break and will need a new ordering assertion added.\n\nSeverity assessment: The blast radius is bounded to three UI dropdowns in the local desktop app (no shipped EPUB output corruption, KJV byte-stability unaffected). The violation is real and directly contradicts an explicit RULES §6.1 hard requirement. Severity is medium (not high/critical) because: it is a pure UI presentation issue, the app is single-user local, and three of the five `edition_canon_books` consumers in the templates are already self-healing via the `books_canonical` intersection pattern."
    }
   ]
  },
  {
   "dimension": "opt-vision",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "subprocess.run in api_export_build missing stdin=subprocess.DEVNULL — WinError 6 on Windows",
   "file": "YHWH v2.4/scripts/api/exports.py",
   "line": "197-203",
   "evidence": "proc = subprocess.run(cmd, capture_output=True, text=True, cwd=str(REPO), timeout=timeout_s) — no stdin=subprocess.DEVNULL. Project rule (memory feedback_w_w1_subprocess_devnull): 'On Windows pytest-from-PowerShell, every subprocess.run() must pass stdin=subprocess.DEVNULL or hits WinError 6.' The EPUB build subprocess is launched from the web server's request thread; the server's stdin is the inherited terminal handle, which is invalid in this context.",
   "fix": "Add stdin=subprocess.DEVNULL to the subprocess.run() call: proc = subprocess.run(cmd, capture_output=True, text=True, cwd=str(REPO), timeout=timeout_s, stdin=subprocess.DEVNULL)",
   "verifierFix": "Add stdin=subprocess.DEVNULL to the subprocess.run() call at YHWH v2.4/scripts/api/exports.py line 197:\n\nproc = subprocess.run(\n    cmd,\n    capture_output=True,\n    text=True,\n    cwd=str(REPO),\n    timeout=timeout_s,\n    stdin=subprocess.DEVNULL,\n)",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading YHWH v2.4/scripts/api/exports.py lines 197-203. The subprocess.run() call at those lines uses capture_output=True, text=True, cwd=str(REPO), and timeout=timeout_s but has no stdin=subprocess.DEVNULL. The non-frozen branch (lines 195-213) executes whenever sys.frozen is False — the standard development/test path. The project's own recorded rule (feedback_w_w1_subprocess_devnull memory entry) states \"every subprocess.run() must pass stdin=subprocess.DEVNULL or hits WinError 6\" on Windows. The web server's stdin handle is inherited from the launching terminal; that handle is invalid in spawned subprocess contexts on Windows, causing WinError 6 (ERROR_INVALID_HANDLE). The frozen branch (lines 180-194) sidesteps this entirely by running in-process, but the non-frozen path is the live development surface. The fix is a one-word addition (stdin=subprocess.DEVNULL) identical to the project's established pattern. No marathon core is touched, no byte-stability impact, no paid API required. This is a genuine, reproducible Windows defect on the active code path."
    }
   ]
  },
  {
   "dimension": "opt-vision",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "api_sample_html edition-filter logic does not apply enabled_categories — kind-filter diverges from build_edition",
   "file": "YHWH v2.4/scripts/web_content.py",
   "line": "275-291",
   "evidence": "enabled_kinds = set(edition.get('enabled_kinds') or []) ... if enabled_kinds and kind not in enabled_kinds: continue — the filter reads only the raw enabled_kinds list from the edition dict. The actual build pipeline (build_edition.py) resolves enabled_kinds as: categories→kinds union PLUS enabled_kinds MINUS disabled_kinds, with an AI-notes gate. The sample preview therefore shows wrong notes when an edition's primary kind selection comes from enabled_categories (the common case for most editions), because enabled_categories entries expand to N kinds at build time but the preview only sees the literal enabled_kinds list, which may be empty or a small override set.",
   "fix": "Replace the inline filter logic with the same compute_enabled_kinds() call that build_edition uses: from scripts.core.matrix import _enabled_kinds_for_edition; from scripts.core.config import load_kinds; enabled = set(_enabled_kinds_for_edition(edition, load_kinds())), then filter against that set. This is the same composition build_edition.compute_enabled_kinds() does.",
   "verifierFix": "In YHWH v2.4/scripts/web_content.py, inside `api_sample_html`, replace lines 275-291 with:\n\n```python\nfrom scripts.core.config import enabled_kind_codes, load_kinds as _load_kinds\n_enabled = enabled_kind_codes(edition, _load_kinds())\nin_range = []\nfor n in all_notes:\n    if not n or len(n) < 8:\n        continue\n    ch = n[0]\n    if ch < f or ch > t:\n        continue\n    kind = n[4]\n    if kind not in _enabled:\n        continue\n    in_range.append(n)\n```\n\nThis delegates to the same `config.enabled_kind_codes` that both `build_edition.compute_enabled_kinds` and `matrix._enabled_kinds_for_edition` already call, ensuring the preview and the actual build are in sync. The `disabled_kinds` gate does not need a separate check because `enabled_kind_codes` already excludes disabled kinds (they are removed at step 1 of the resolver). No marathon-core files touched; no byte-stability impact on the 9-edition build.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "\nIndependently verified by reading YHWH v2.4/scripts/web_content.py lines 275-291, YHWH v2.4/scripts/core/config.py lines 393-438, YHWH v2.4/scripts/build_edition.py lines 497-509, and YHWH v2.4/content/editions.yaml.\n\nThe defect is real and material:\n\n1. web_content.py line 275: `enabled_kinds = set(edition.get(\"enabled_kinds\") or [])` reads only the raw `enabled_kinds` list from the edition dict.\n\n2. web_content.py line 288: `if enabled_kinds and kind not in enabled_kinds: continue` — when `enabled_kinds` is an empty set (falsy), the entire guard short-circuits: NO filtering happens and every note kind passes through unconditionally.\n\n3. editions.yaml confirms that ALL nine editions (ethiopian-tewahedo, catholic, reformed, jewish-study, etc.) use `enabled_categories` as their primary kind-selection mechanism (e.g. `enabled_categories: [lang, text, xref, ...]`) with `enabled_kinds` absent or a tiny override. So the empty-`enabled_kinds` case is the dominant production path, not an edge case.\n\n4. The canonical resolver `config.enabled_kind_codes` (config.py line 393) correctly expands `enabled_categories` to all kinds whose `category` matches, applies the phase gate (`max_phase`), applies the AI double-opt-in (`enable_ai_notes` / `AI_DRAFTED_KINDS`), and then unions explicit `enabled_kinds`. This is exactly what `build_edition.compute_enabled_kinds` calls (build_edition.py lines 505-507).\n\n5. Consequence: the `/api/sample-html` preview shows ALL notes from ALL kinds regardless of edition profile — a completely wrong result for the primary use case. A user previewing the evangelical-reformed edition sees the same notes as the ethiopian-tewahedo edition.\n\nThe proposed fix is correct and feasible. `config` is already imported inside `api_sample_html` at line 184 (`from scripts.core import config`). The fix is:\n\n```python\n# Replace lines 275-276:\nfrom scripts.core.config import enabled_kind_codes, load_kinds\nenabled_kinds = enabled_kind_codes(edition, load_kinds())\ndisabled_kinds = set()  # already handled by enabled_kind_codes\n\n# Replace lines 288-291 filter with:\nif kind not in enabled_kinds:\n    continue\n# (disabled_kinds gate is subsumed — enabled_kind_codes already excludes them)\n```\n\nNo marathon core touched. No byte-stability impact (build_edition.py is unchanged). No paid API. The canonical resolver is already tested and used by both matrix.py and build_edition.py.\n"
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "is_output_current skips rebuild when notes files change (stale-build on cold cache)",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "1956-1964",
   "evidence": "`sources = list(EPUB_DIR.glob('*.html'))` then only appends `content.opf`, `nav.xhtml`, `stylesheet.css`, `editions.yaml`, `build_edition.py` — the 88 `content/notes/*.py` files are absent. If any note file is edited after the last EPUB build, `is_output_current` returns the old EPUB as 'current' and the build is silently skipped. The content-hash cache (ω.20-B) is correct but only fires BEFORE this check (lines 2741-2754); on a cold cache (after `cache_clear` or first run), the stale mtime guard fires and the user gets stale output without any warning.",
   "fix": "Append all `content/notes/*.py` mtimes to the sources list: add `sources.extend(REPO_ROOT / 'content' / 'notes' / f for f in (REPO_ROOT / 'content' / 'notes').glob('*.py') if f.is_file())` inside `is_output_current`. Also add `content/kinds.yaml` and `content/categories.yaml` which affect kind-filter outcomes. This keeps the mtime check consistent with the hash-based cache. Byte-stability: no output change, guard only affects whether the build runs.",
   "verifierFix": "In `YHWH v2.4/scripts/build_edition.py` inside `is_output_current`, after line 1961, add:\n\n```python\nnotes_dir = REPO_ROOT / \"content\" / \"notes\"\nif notes_dir.is_dir():\n    sources.extend(notes_dir.glob(\"*.py\"))\nsources.append(REPO_ROOT / \"content\" / \"kinds.yaml\")\nsources.append(REPO_ROOT / \"content\" / \"categories.yaml\")\n```\n\nThis mirrors the exact inputs covered by `compute_cache_key` sections 3 and 5, making the mtime guard consistent with the content-addressable guard. The `is_dir()` guard keeps it safe if the directory is absent on a fresh checkout. No other changes needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I independently read all three relevant code blocks:\n\n1. `is_output_current` at lines 1956-1964 of `YHWH v2.4/scripts/build_edition.py` — sources list contains only `epub_working/*.html`, `content.opf`, `nav.xhtml`, `stylesheet.css`, `content/editions.yaml`, and `scripts/build_edition.py`. The 88 `content/notes/*.py` files, `content/kinds.yaml`, and `content/categories.yaml` are absent.\n\n2. The execution order at lines 2741-2764 — the content-addressable cache lookup fires first (lines 2741-2754); `is_output_current` fires only on a cache miss (lines 2757-2764).\n\n3. `build_cache.compute_cache_key` in `scripts/core/build_cache.py` lines 181-187 — it hashes every in-canon `content/notes/<book>.py`, plus `kinds.yaml` and `categories.yaml` (lines 168-169). `cache_store` is called after every successful build (line 3047), warming the cache.\n\nThe failure path is confirmed: after any successful build the cache is warmed. On the next run, if a notes file changes, `compute_cache_key` produces a new hash → cache miss → `is_output_current` is reached. Because `is_output_current` does not check notes file mtimes, and the notes files were the only thing changed, it compares the notes-changed workspace against only the HTML/OPF/stylesheet/editions.yaml/build_edition.py mtimes — all of which are older than the existing EPUB — and returns the stale EPUB as current. The user gets silently wrong output. Editing notes is the primary day-to-day activity in this project, making this a real workflow-impacting defect. The proposed fix (extend sources in `is_output_current` to include `content/notes/*.py`, `kinds.yaml`, and `categories.yaml`) is correct, low-risk, trivially non-invasive, and carries zero byte-stability risk (only affects whether the build runs, not what it produces)."
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "subprocess.run missing stdin=subprocess.DEVNULL — Windows WinError 6 in test/harness contexts",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "3022-3033",
   "evidence": "`result = subprocess.run([sys.executable, ..., '--no-bump'], capture_output=True, text=True)` — no `stdin=subprocess.DEVNULL`. Per project memory (`W-W1 subprocess DEVNULL`): on Windows under pytest-from-PowerShell every `subprocess.run()` without `stdin=subprocess.DEVNULL` hits WinError 6 (invalid handle) because the harness redirects stdin. This path executes on every non-frozen build (the `not getattr(sys, 'frozen', False)` branch at line 3014), meaning every `pytest`-invoked build test that reaches this line will fail with WinError 6 instead of the actual build error.",
   "fix": "Add `stdin=subprocess.DEVNULL` to the `subprocess.run` call: `result = subprocess.run([sys.executable, str(REPO_ROOT / 'scripts' / 'build_epub.py'), str(output_path), '--epub-dir', str(tmp), '--no-bump'], capture_output=True, text=True, stdin=subprocess.DEVNULL)`. Import `subprocess` is already present at line 46.",
   "verifierFix": "At YHWH v2.4/scripts/build_edition.py line 3033, add stdin=subprocess.DEVNULL to the subprocess.run call:\n\nresult = subprocess.run(\n    [\n        sys.executable,\n        str(REPO_ROOT / \"scripts\" / \"build_epub.py\"),\n        str(output_path),\n        \"--epub-dir\",\n        str(tmp),\n        \"--no-bump\",\n    ],\n    capture_output=True,\n    text=True,\n    stdin=subprocess.DEVNULL,\n)\n\nsubprocess is already imported at line 46. No other changes needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Verified by direct code read at YHWH v2.4/scripts/build_edition.py lines 3022-3033: the subprocess.run call is present exactly as described, with capture_output=True and text=True but no stdin=subprocess.DEVNULL. The non-frozen branch (lines 3021-3035) is reached on every pytest run since sys.frozen is never set under pytest. The test_build_one_yields_valid_epub test in tests/test_build_smoke.py calls build_one() directly (line 68), which flows through the full build_one() body and reaches this subprocess.run. The project memory (W-W1 subprocess DEVNULL note) explicitly documents this as a required fix for all subprocess.run() calls on Windows under pytest-from-PowerShell, where the harness redirects stdin and any subprocess that inherits the invalid stdin handle raises WinError 6 (invalid handle) before producing any output. This is a real, confirmable defect on this Windows 11 machine, not a style preference. The finder labelled it \"opt-build\" (optimization dimension) but the actual consequence is test failure with WinError 6 rather than a functioning EPUB build. The fix — adding stdin=subprocess.DEVNULL to the single subprocess.run call — is correct, safe, minimal, and cannot affect byte-stability of any EPUB artifact."
    }
   ]
  },
  {
   "dimension": "opt-render",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "high",
   "title": "render_coverage._CANONICAL_BOOKS missing 7 of 87 books.yaml entries, skewing all coverage counts",
   "file": "YHWH v2.4/scripts/render_coverage.py",
   "line": "53-143",
   "evidence": "The docstring says '81-book set' and the list has 84 entries. books.yaml defines 87 books. Seven codes present in books.yaml are absent from _CANONICAL_BOOKS: man (Prayer of Manasseh), 2en (2 Enoch), 1es (1 Esdras / Ezra Kali), aes (Additions to Esther), lje (Letter of Jeremiah), sus (Susanna), 1cl (1 Clement). These books silently fall into the `missing` bucket in every per-edition report, the preflight summary's `canonical_books` value is 84 not 87, and the `render_coverage_no_regression` lint baseline in lint_rules.py also omits them — so new renders of those books would never trigger a lint pass.",
   "fix": "Add the seven missing codes to _CANONICAL_BOOKS in their canonical books.yaml position (man after 2ch; 2en after 1en; 1es after ezr/neh; aes after est; lje after bar; sus and 1cl at their canonical positions). Update the docstring count to 87. Add corresponding entries to the expected_geez/expected_amharic sets in lint_rules.py:check_render_coverage_no_regression as books are rendered. No behavior change to the build pipeline — render_coverage is read-only reporting.",
   "verifierFix": "1. In YHWH v2.4/scripts/render_coverage.py, add the 7 missing codes to _CANONICAL_BOOKS at their canonical books.yaml positions (man after 2ch; 2en after 1en; 1es after neh; aes after est; lje after bar; sus after paz/bel Daniel appendices; 1cl at end after rev/4ba). Update the docstring at line 42 from \"81-book set\" to the correct count after the reconciliation.\n\n2. Reconcile the 2 phantom codes (1ma, 2ma) that are in _CANONICAL_BOOKS but absent from books.yaml: either add them to books.yaml with proper bxx values if they belong in the Tewahedo 87-book canon, OR remove them from _CANONICAL_BOOKS. Without this step the list still mismatches books.yaml.\n\n3. In YHWH v2.4/scripts/lint_rules.py check_render_coverage_no_regression (line 948), add rendered codes for any of the 7 newly recognized books that already have .py files in geez-tewahedo or amharic-tewahedo — do not add them preemptively, only when the .py file exists.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading both files in full.\n\nbooks.yaml defines exactly 87 books (b00–b87, with b31 a known numbering gap; there are 87 `code:` entries confirmed by the bxx grep).\n\n_CANONICAL_BOOKS (YHWH v2.4/scripts/render_coverage.py lines 53-143) contains 82 entries. The 7 codes the finder names are ALL confirmed present in books.yaml and ALL confirmed absent from _CANONICAL_BOOKS:\n- man (b14, Prayer of Manasses) — line 174 of books.yaml\n- 2en (b17, Second Book of Enoch) — line 208\n- 1es (b20, First Esdras / Ezra Kali) — line 240\n- aes (b25, Additions to Esther) — line 292\n- lje (b41, Letter of Jeremiah) — line 454\n- sus (b46, Susanna) — line 505\n- 1cl (b87, 1 Clement) — line 923\n\nThere is an additional anomaly the finder did not mention: 1ma and 2ma ARE in _CANONICAL_BOOKS but are NOT in books.yaml at all (grep for `code: (1ma|2ma)` returns no matches). This means _CANONICAL_BOOKS has 2 phantom codes and is missing 7 real ones — a net mismatch of 9 codes relative to books.yaml.\n\nConsequence: run_all() reports `\"canonical_books\": 82` (line 233) against books.yaml's authoritative 87. Any book in the missing-7 set that gets rendered will count in `rendered_books` (via `rendered & set(_CANONICAL_BOOKS)`) only if its code happens to appear in _CANONICAL_BOOKS — which it won't, so it is silently excluded from rendered_count and from the lint regression baseline in lint_rules.py:948. The 2 phantom codes (1ma, 2ma) in _CANONICAL_BOOKS will always land in `missing` because they have no books.yaml entry and thus no edition .py file.\n\nThe docstring at line 42 says \"81-book set\" — the list actually has 82 entries (4ba was added to the \"Additional\" section at line 142 without updating the comment).\n\nThe finding is real and its description of the 7 missing codes is accurate. The fix is correct: add the 7 missing codes at their canonical books.yaml positions, remove the 2 phantom codes (1ma, 2ma) or verify whether they belong, update the docstring count, and update the lint baseline in lint_rules.py when those books are rendered. Severity is medium (not high) because render_coverage is read-only reporting — no build output or byte-stability is affected, and the 9 KJV editions are unaffected. The harm is incorrect coverage counts and suppressed lint regression detection for those 7 books.\n\nProposed fix from finder is correct for the 7 additions. The corrected fix should also address the 2 phantom codes (1ma, 2ma): confirm whether Maccabees belong to the Tewahedo 87-book canon (they do appear in some enumerations) and if so add them to books.yaml with proper bxx values; if not, remove them from _CANONICAL_BOOKS."
    }
   ]
  },
  {
   "dimension": "opt-render",
   "kind": "optimization",
   "severity": "medium",
   "originalSeverity": "medium",
   "title": "api_sample_html kind-filter bypasses config.enabled_kind_codes — AI notes and phase-gated kinds bleed into sample HTML",
   "file": "YHWH v2.4/scripts/web_content.py",
   "line": "275-291",
   "evidence": "Lines 275-276 read `enabled_kinds = set(edition.get('enabled_kinds') or [])` and `disabled_kinds = set(edition.get('disabled_kinds') or [])`, then filter with `(enabled_kinds is empty → allow all) AND kind NOT IN disabled_kinds`. This misses three gates that config.enabled_kind_codes() applies: (1) enabled_categories — notes whose kind belongs to an enabled category are invisible if enabled_kinds is empty but enabled_categories is non-empty; (2) max_phase gate — phase2/phase3 notes appear in samples for mvp editions; (3) AI double opt-in — comm-ai notes appear in editions that set comm-ai in enabled_kinds but omit enable_ai_notes: true. The comment on line 286 says 'Filter rule mirrors build_edition' but does not call the canonical resolver.",
   "fix": "Replace the manual enabled_kinds/disabled_kinds construction with a call to the canonical resolver. Change lines 275-291 to: `from scripts.core.config import enabled_kind_codes, load_kinds; enabled_kind_set = enabled_kind_codes(edition, load_kinds()); in_range = [n for n in all_notes if n and len(n) >= 8 and f <= n[0] <= t and n[4] in enabled_kind_set]`. This aligns sample_html with the actual EPUB build and the matrix count grid — all three now derive from enabled_kind_codes.",
   "verifierFix": "In `YHWH v2.4/scripts/web_content.py`, replace the manual filter block at lines 275-292 with a call to the canonical resolver. At the top of the file (or in the relevant import block) add `from scripts.core.config import enabled_kind_codes, load_kinds`, then replace lines 275-292 with:\n\n```python\nenabled_kind_set = enabled_kind_codes(edition, load_kinds())\nin_range = [\n    n for n in all_notes\n    if n and len(n) >= 8 and f <= n[0] <= t and n[4] in enabled_kind_set\n]\n```\n\nAlso update the module-level comment at line 157 from `edition.enabled_kinds + disabled_kinds for the kind filter` to `config.enabled_kind_codes() for the kind filter (canonical resolver, mirrors EPUB build)`.\n\nNo other files need changing. The EPUB build path (`build_edition`) is untouched. Byte-stability is unaffected because this function only produces sample HTML, never the EPUB artifact.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read `YHWH v2.4/scripts/web_content.py` lines 272-292 and `YHWH v2.4/scripts/core/config.py` lines 393-438 directly.\n\nThe code at web_content.py:275-291 constructs `enabled_kinds` and `disabled_kinds` from the raw edition dict and applies only two checks: `(enabled_kinds empty OR kind in enabled_kinds) AND kind NOT IN disabled_kinds`. The module-level comment at line 157 even self-documents this narrow contract: `edition.enabled_kinds + disabled_kinds for the kind filter`.\n\n`config.enabled_kind_codes()` (lines 393-438) is the declared canonical resolver and it applies four sequential gates that the sample filter omits:\n1. `explicit_disabled` — this one the sample does replicate.\n2. Phase gate: drops kinds whose `phase` field exceeds `edition.max_phase` (lines 431-433). The sample never reads `max_phase` at all.\n3. AI double opt-in: drops `comm-ai` unless `enable_ai_notes: true` (lines 434-435). The sample never reads `enable_ai_notes`.\n4. `enabled_categories` inclusion: a kind whose category appears in `enabled_categories` is included even without being in `enabled_kinds` (line 436). The sample never reads `enabled_categories`.\n\nFrom `content/editions.yaml` I verified that real editions exercise these omitted gates:\n- The first edition (ethiopian-tewahedo) sets `enabled_categories: [lang, text, ...]` and `max_phase: phase2`.\n- Multiple editions carry `enabled_categories` lists.\n\nThe practical consequence of the divergence:\n- For an edition with `enabled_categories` but an empty `enabled_kinds`, the sample filter's `(enabled_kinds empty → allow all)` branch lets ALL kinds through, while the build would admit only kinds whose category is in the enabled set — so the sample HTML shows many more note kinds than the actual EPUB will contain.\n- For an edition with `max_phase: phase2`, the sample lets phase3 kinds through; the build drops them.\n- For an edition with `comm-ai` in `enabled_kinds` but `enable_ai_notes` absent, the sample shows AI notes; the build suppresses them.\n\nThe proposed fix (replace the manual logic with a call to `enabled_kind_codes`) is correct, feasible, touches no marathon core, and is additive (no byte-stability risk — the EPUB build path is unchanged). The fix brings the sample preview into fidelity with what the EPUB build actually produces, which is the correct behavior for a preview endpoint.\n\nThis is a real functional divergence, not merely a style issue. It is reproducible with any edition that uses `enabled_categories` or `max_phase`. The severity is medium: sample HTML is a UI preview path (not the EPUB output), so it does not corrupt the shipped artifact, but it misleads the user about what their edition will contain."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "promote.py batch_insert_notes breaks sort order for notes at the same chapter/verse when existing list is not in sorted order",
   "file": "YHWH v2.4/scripts/promote.py",
   "line": "339-343",
   "evidence": "```python\nfor ech, ev, esuf, eend in existing:\n    if (ech, ev, suffix_rank(esuf)) < new_key:\n        after = eend\n    else:\n        break\n```\nThe loop uses `break` on the first tuple that does NOT sort before `new_key`, assuming `existing` is sorted ascending. If any existing note tuple in the file is out of sort order (e.g., from a prior bulk edit that skipped sorting), `break` fires prematurely and the insertion point is set too early — notes land before already-existing tuples whose sort key is lower. Silent: no error, wrong order on disk.",
   "fix": "Change the `break` to `continue` so ALL existing tuples are scanned regardless of order, keeping `after = eend` only when `(ech, ev, suffix_rank(esuf)) < new_key`. Or add a pre-sort of `existing` by `(ech, ev, suffix_rank(esuf))` before the loop to make the early-exit safe.",
   "verifierFix": "In `batch_insert_notes` (line 343), change `break` to `continue` so all existing tuples are scanned regardless of file order:\n\n```python\nfor ech, ev, esuf, eend in existing:\n    if (ech, ev, suffix_rank(esuf)) < new_key:\n        after = eend\n    # no break — scan all tuples; file may not be perfectly sorted\n```\n\nApply the identical fix to `insert_note_into_book_file` at line 248-249, which has the same pattern:\n\n```python\n        if existing_key < new_key:\n            insert_after_lineno = tup.end_lineno\n        # no break — scan all tuples\n```\n\nBoth changes are safe: no marathon-core files are touched, schema is unchanged, the 9 KJV editions carry no notes-file dependency so byte-stability is unaffected. An alternative is to pre-sort `existing` by `(ch, v, suffix_rank(esuf))` before the loop (restoring the correctness of the early-exit), but the `continue` approach is simpler and equally correct.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The code at `YHWH v2.4/scripts/promote.py` lines 339-343 is exactly as described. `existing` is populated by walking AST elements in file order (lines 310-320); there is no pre-sort before the loop. The `break` at line 343 fires on the first tuple whose sort key is not strictly less than `new_key`, which is only a correct early exit if `existing` is sorted ascending. No lint guard in `check_content.py` or elsewhere enforces or verifies sort order of notes tuples on disk. The identical pattern exists in `insert_note_into_book_file` at lines 246-249, which the finder did not cite.\n\nHowever, the severity must be recalibrated downward:\n1. All current write paths (`insert_note_into_book_file`, `batch_insert_notes`) both start from whatever file order exists and both rely on the same invariant — meaning that as long as every ingest uses these two functions on a starting-sorted file, the invariant is self-maintaining in practice.\n2. The build pipeline (`build_edition.py`) reads all notes tuples regardless of intra-file order; EPUB output and the 9 KJV byte-stability guarantee are unaffected.\n3. No immediate crash or content loss occurs; the defect would cause a note to be inserted at the wrong line position in a `.py` file, visible only as cosmetic disorder in the source, and would compound silently on subsequent bulk inserts if disorder already exists.\n4. The risk surface is limited to the scenario where a file is manually edited or a future ingest bypasses the sorted-insert routines — neither condition currently applies.\n\nThe proposed `continue`-instead-of-`break` fix is correct and safe; it makes both functions resilient to any pre-existing disorder without touching the marathon core or affecting the KJV byte-stability invariant. The corrected fix notes that the identical pattern in `insert_note_into_book_file` (line 246-249) should receive the same treatment for consistency."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "write_book always emits 9th attribution field as `{}` for legacy 8-tuple notes, converting them to 9-tuples with a dict value that violates the str | None contract",
   "file": "YHWH v2.4/scripts/web_helpers.py",
   "line": "174",
   "evidence": "```python\nattribution = rest[0] if rest else {}\n...\nlines.append(f\"        {attribution!r},\\n\")\n```\nFor any note tuple with < 9 fields (`rest == []`), `attribution = {}`. Every note the web UI writes back to disk (even 8-tuple legacy notes) gains a 9th field `{}`. `NoteSpec.from_tuple(tup)` then stores `attribution = {}` (a dict) instead of `None` because `isinstance({}, str)` is False, breaking the dataclass type annotation. Code downstream that calls `spec.attribution.strip()` (e.g. future note-quality checks) would raise `AttributeError: 'dict' object has no attribute 'strip'`.",
   "fix": "Use `attribution = rest[0] if rest else None` (or `\"\"`) and conditionally omit the 9th field when attribution is falsy: `if attribution: lines.append(f\"        {attribution!r},\\n\")`. This preserves the 8-tuple legacy form for notes without attribution, matching how `promote.py`'s `format_tuple_text` handles it.",
   "verifierFix": "Fix all three cooperating sites in `YHWH v2.4/scripts/web_helpers.py`:\n\n1. `tuple_to_dict` (line 131): change `\"attribution\": attribution or {}` to `\"attribution\": attribution or None` — the dict should carry `None` for absent attribution, not `{}`.\n\n2. `dict_to_tuple` (line 146): change `d.get(\"attribution\") or {}` to `d.get(\"attribution\") or None` — this makes the returned tuple an 8-tuple when attribution is absent, matching the legacy form.\n\n3. `write_book` (lines 174 + 182): change `attribution = rest[0] if rest else {}` to `attribution = rest[0] if rest else None`, then make the 9th field conditional — only emit it when truthy, matching `promote.py`'s `format_tuple_text` contract:\n\n```python\nattribution = rest[0] if rest else None\nlines.append(\"    (\\n\")\nlines.append(f\"        {ch}, {v}, {suffix!r},\\n\")\nlines.append(f\"        {anchor!r},\\n\")\nlines.append(f\"        {kind!r},\\n\")\nlines.append(f\"        {title!r},\\n\")\nlines.append(f\"        {label!r},\\n\")\nlines.append(f\"        {body!r},\\n\")\nif attribution:\n    lines.append(f\"        {attribution!r},\\n\")\nlines.append(\"    ),\\n\")\n```\n\nThis trims the spurious `{}` 9th field from all unattributed notes on the next web-UI save, restoring them to the 8-tuple legacy form and aligning `write_book` with the project-wide contract expressed in `promote.py:format_tuple_text` and `NoteSpec.to_tuple`. The 9 KJV editions are unaffected (build pipeline does not call this function).",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The finding is confirmed by reading the code directly. Three cooperating defects form the bug cluster:\n\n1. `web_helpers.py:174` — `attribution = rest[0] if rest else {}`: for any legacy 8-tuple (rest=[]), `attribution = {}`. Line 182 then unconditionally writes `{!r}` as the 9th field, converting every legacy note touched by the web UI.\n\n2. `web_helpers.py:146` (dict_to_tuple) — `d.get(\"attribution\") or {}`: when the API form submits an absent/empty attribution, the resulting tuple gets `{}` as 9th field rather than being an 8-tuple.\n\n3. `web_helpers.py:131` (tuple_to_dict) — `\"attribution\": attribution or {}`: when an 8-tuple is read for display, `None or {} = {}` is placed in the dict, which round-trips back through dict_to_tuple as `{}`.\n\n`NoteSpec.from_tuple` at `core/config.py:90-93` reads `tup[8]` and only normalizes to None if `isinstance(attribution, str) and not attribution.strip()`. Since `isinstance({}, str)` is False, `{}` is stored verbatim in the `attribution: str | None` field, violating the type contract.\n\nHowever, the blast radius is bounded: no shipped code path calls `.strip()` directly on `spec.attribution` (the hypothetical crash is future code only). In boolean contexts `{}` is falsy, so `spec.attribution or \"\"` and `if spec.attribution:` both behave as if it were None/empty. The 9 KJV build editions are unaffected (build pipeline does not call `write_book` from web_helpers). The `{}` literal is valid `ast.literal_eval` input so file loading does not break. `promote.py:116` uses `(attribution or \"\").strip()` which is safe with `{}`.\n\nThe finding is real and the 3-point fix cluster is correct, but severity is medium not high because: no crash occurs in current code, no output corruption, and the behavior is effectively stable/idempotent once converted.\n\nThe proposed fix is incomplete — it only addresses `write_book` line 174/182 but the `{}` sentinel originates equally in `dict_to_tuple` (line 146) and `tuple_to_dict` (line 131). All three sites need correction."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "prospect.py and all run_*_at_scale.py write_queue functions use non-atomic writes for candidate JSON files",
   "file": "YHWH v2.4/scripts/prospect.py",
   "line": "152",
   "evidence": "`out_path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding=\"utf-8\")` — a direct non-atomic write. The same pattern is copy-pasted across run_naves_at_scale.py:67, run_greek_at_scale.py:60, run_hebrew_at_scale.py:61, run_xref_at_scale.py:66, run_torrey_at_scale.py:65, run_kenyon_at_scale.py:95, run_ethiopian_at_scale.py:76, and others. If the process is interrupted mid-write (Ctrl-C, OOM kill), the JSON file is corrupt. The append-merge pattern in run_naves_at_scale.py's write_queue reads the existing file first — a corrupt existing file is silently caught (`except Exception: existing = []`) and lost.",
   "fix": "Replace with `notes_io.atomic_write(out_path, json.dumps(payload, indent=2, ensure_ascii=False) + \"\\n\")` in all write_queue functions. The `atomic_write` primitive is already imported or available in every script. This is a one-line change per file and matches the pattern used for notes files.",
   "verifierFix": "In each of the 8 affected files, add `from scripts.core.notes_io import atomic_write` to the imports block (alongside the existing `from scripts.core...` imports), then replace each `out_path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding=\"utf-8\")` with `atomic_write(out_path, json.dumps(payload, indent=2, ensure_ascii=False) + \"\\n\")`. The `+ \"\\n\"` matches the pattern already used in promote.py's atomic_write call for the same files. prospect.py already imports from scripts.core but not notes_io, so it needs the same addition. This is otherwise correct and safe — no marathon core files touched, no EPUB output affected, no schema changes.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "All cited write_text calls are confirmed by direct reading: prospect.py:152, run_naves_at_scale.py:67, run_greek_at_scale.py:60, run_hebrew_at_scale.py:61, run_xref_at_scale.py:66, run_torrey_at_scale.py:65, run_kenyon_at_scale.py:95, run_ethiopian_at_scale.py:76 — all use direct Path.write_text rather than atomic_write. None of the run_*_at_scale.py files import notes_io (verified by reading their import blocks). The append-merge scripts (run_naves, run_greek, run_hebrew, run_xref, run_torrey, run_kenyon, run_ethiopian) read the existing file first and suppress corrupt-parse errors with a broad `except Exception: existing = []`, meaning a mid-write interrupt leaves a corrupt file that is silently treated as empty on the next append pass — the previously written candidates are irrecoverably lost from that file. promote.py already uses atomic_write correctly for status updates to these same files, confirming atomic_write is the intended pattern here. Blast radius is bounded: these are intermediate staging files only, not shipped EPUB output; the 9 KJV editions are unaffected; re-running the script regenerates the content from source data. Severity is low rather than medium — the corruption is recoverable by re-run, and these scripts are run manually and infrequently. The proposed fix is directionally correct but incomplete as stated: notes_io is NOT already imported in the run_*_at_scale.py files, so each file needs `from scripts.core.notes_io import atomic_write` added before the call substitution can work. The corrected fix captures this."
    }
   ]
  },
  {
   "dimension": "correctness",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "low",
   "title": "batch_insert_notes silently drops notes whose chapter/verse key is absent from the input dict",
   "file": "YHWH v2.4/scripts/promote.py",
   "line": "325-330",
   "evidence": "Lines 325-326: `ch = n.get(\"chapter\", n.get(\"ch\"))` / `v = n.get(\"verse\", n.get(\"v\"))`. If neither key is present, `ch = None`. Line 330: `if not coord_in_canonical_extent(book_path.stem, None, v): continue`. Inside that function, `canonical_book_shape` returns `{int: int}`, and `None in shape` is always `False`, so the function returns `False` and the note is silently skipped. No warning is emitted. In practice, all current callers supply the correct keys, but a future caller or a malformed candidate dict would cause silent data loss.",
   "fix": "Add an explicit guard before line 330: `if ch is None or v is None: continue  # malformed candidate dict — log at DEBUG`. Optionally emit a warning so the operator knows a candidate was skipped.",
   "verifierFix": "At `YHWH v2.4/scripts/promote.py` line 329 (immediately after `attribution = n.get(\"attribution\")`), insert:\n\n```python\nif ch is None or v is None:\n    import warnings\n    warnings.warn(\n        f\"batch_insert_notes: skipping note missing chapter/verse keys: {n!r}\",\n        stacklevel=2,\n    )\n    continue\n```\n\nThis makes the silent skip visible without altering any existing behavior. Using `warnings.warn` (rather than a bare `print`) is consistent with the project's pattern for operational warnings in library functions and does not require a logging dependency. No changes to marathon core; no effect on the 9 KJV edition outputs.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Verified by reading the actual code. Lines 325-326 of `YHWH v2.4/scripts/promote.py` do exactly what the finder claims: `ch = n.get(\"chapter\", n.get(\"ch\"))` yields `None` if neither key is present. Line 330 then calls `coord_in_canonical_extent(book_path.stem, ch, v)` with `ch=None`. In `scripts/core/canonical_verse_counts.py` line 172, the guard is `return chapter in shape and 1 <= verse <= shape[chapter]` — `None in {int: int}` is always `False`, so the function returns `False` and the note is skipped silently via `continue` at line 331, with no log or warning.\n\nAll current callers supply the key correctly: `batch_promote_xrefs.py:_candidate_to_note` always sets `\"chapter\"` and `\"verse\"`, and `extract_eastons_ccel.py` uses the `\"ch\"`/`\"v\"` fallback. So no shipped output is affected today and the 9 KJV editions stay byte-stable.\n\nThe finding is real but the blast radius is bounded entirely to hypothetical future callers. Severity is appropriately low. The proposed fix is correct and safe: a guard emitting a warning (or DEBUG log) before the `coord_in_canonical_extent` call when `ch is None or v is None`, then `continue`. This is additive, does not touch marathon core, and does not alter any existing output path."
    }
   ]
  },
  {
   "dimension": "code-debt",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "iter_target_verses and resolve_books duplicated byte-for-byte across both AI at-scale drivers",
   "file": "YHWH v2.4/scripts/run_ai_notes_at_scale.py",
   "line": "117-147, 298-305",
   "evidence": "run_ai_notes_at_scale.py lines 117-147 and 298-305 are byte-identical to run_ai_xrefs_at_scale.py lines 119-146 and 292-299. The notes-driver docstring even says 'Mirrors run_ai_xrefs_at_scale.iter_target_verses — kept separate to allow per-driver targeting filters in the future.' This is the same copy-then-diverge pattern that caused the silent ch_count/'chapters' bug fixed in mint-7 B1 (the fix comment 'mint-7 B1: books.yaml key is ch_count' appears in both copies at lines 132-134 of xrefs and 133-135 of notes). A future fix applied to one copy will silently miss the other.",
   "fix": "Add iter_target_verses(books, max_verses) and resolve_books(books_arg) to scripts/core/at_scale_base.py (which already owns candidate_to_dict, NT_BOOKS, and the ANSI constants for the same reason). Both AI drivers then import from there. The notes-driver docstring rationale ('allow per-driver targeting filters in the future') is not a blocker — per-driver overrides can always shadow the base function locally when that future need actually arrives.",
   "verifierFix": "",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified by reading both files. The two functions iter_target_verses (notes: lines 117-147, xrefs: lines 119-146) and resolve_books (notes: lines 298-305, xrefs: lines 292-299) are byte-identical between the two drivers, differing only in the notes-driver docstring which explicitly calls out the duplication. The mint-7 B1 fix comment ('mint-7 B1: books.yaml key is ch_count') appears identically in both copies at exactly the lines cited. at_scale_base.py already exists and was created in mint-7 D1 for exactly this de-duplication pattern (candidate_to_dict, NT_BOOKS, ANSI constants). The finding is real. Severity is recalibrated DOWN to low: there is no current bug (both copies are in sync today), no shipped-output is affected, the blast radius is strictly bounded to two AI-generation scripts that call a paid API (not the build pipeline), and the 9 KJV editions are fully unaffected. The risk is exclusively a future-fix applied to one copy missing the other — a legitimate but not urgent code-debt concern. The proposed fix is correct and safe: adding iter_target_verses and resolve_books to at_scale_base.py follows the established mint-7 D1 pattern, is additive, touches no marathon core files, and cannot affect KJV byte stability. The docstring rationale for separation ('allow per-driver targeting filters in the future') is adequately addressed by the finder's note that per-driver overrides can shadow the base function locally when needed."
    }
   ]
  },
  {
   "dimension": "tests",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "Share-pin in γ.4.2 arc-close class violates §8.1 convention",
   "file": "YHWH v2.4/tests/test_ethiopian_gamma4.py",
   "line": "1177-1180",
   "evidence": "`cyril_share = len(cyril) / total` then `assert cyril_share < 0.80` — this is exactly the pattern RULES §8.1 prohibits: 'NEVER a share threshold — share-pins break mechanically when later waves dilute the share even though the historical achievement is preserved (memory `feedback_share_pin_pattern`)'. Every future Ephrem/Jubilees/Meqabyan/Athanasius wave that adds non-Cyril entries will dilute `cyril_share` further below 0.80, keeping this test permanently green regardless of whether the γ.4.2 rebalance achievement itself was tampered with. The test also cannot catch a regression that accidentally inflates Cyril's count above 80% if corpus pruning removes the Ephrem entries.",
   "fix": "Replace the share-pin with an absolute-count milestone pin per §8.1. At γ.4.2 wave-1 ship time Ephrem had ≥30 entries (the γ.4.2 wave-1 count). Pin that directly:\n```python\ndef test_voice_rebalance_achieved(self):\n    # §8.1 absolute-count milestone pin: at γ.4.2 wave-1 close,\n    # Ephrem had ≥30 entries — the corpus rebalance that reduced Cyril\n    # dominance from 93% to sub-80% is preserved by this floor.\n    ephrem = self.ec.by_father(\"Ephrem the Syrian\")\n    assert len(ephrem) >= 30, (\n        f\"γ.4.2 rebalance: ≥30 Ephrem entries (the milestone); found {len(ephrem)}\"\n    )\n```\nThe `test_ephrem_now_substantively_present` test above it already pins `len(ephrem) >= 30`; if they are redundant at this value, raise the milestone to the actual shipped count (32 per the class docstring) in either or both.",
   "verifierFix": "Replace lines 1171-1180 with an absolute Cyril count ceiling pin (do NOT duplicate the already-present Ephrem floor at line 1165-1169):\n\n```python\ndef test_voice_rebalance_achieved(self):\n    # §8.1 absolute-count milestone pin: at γ.4.2 wave-1 close,\n    # Cyril had 91 entries (per class docstring). Pin a ceiling so\n    # a future ingest cannot silently re-inflate Cyril dominance.\n    # A share-pin would break as γ.4.2.B-D add more Ephrem entries\n    # (feedback_share_pin_pattern); an absolute ceiling is stable.\n    cyril = self.ec.by_father(\"Cyril of Alexandria\")\n    assert len(cyril) <= 91, (\n        f\"γ.4.2 rebalance: Cyril must not exceed 91 entries \"\n        f\"(the post-wave-1 milestone); found {len(cyril)}\"\n    )\n```\n\nIf the real Cyril count at the time this test was written was higher than 91 (the docstring may be stale), verify with `len(self.ec.by_father(\"Cyril of Alexandria\"))` against the live corpus and substitute that value. The `test_ephrem_now_substantively_present` at line 1165 already covers the Ephrem floor; no second Ephrem-count pin is needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The code at lines 1177-1180 of `YHWH v2.4/tests/test_ethiopian_gamma4.py` does exactly what the finding describes: `cyril_share = len(cyril) / total` followed by `assert cyril_share < 0.80`. This is a share-pin.\n\nThe `feedback_share_pin_pattern` memory note and RULES §8.1 both prohibit share-threshold pins because future waves adding non-Cyril entries dilute the share and make the test permanently trivially-green as a regression guard. The class docstring at line 1128 explicitly notes future waves γ.4.2.B-D are planned, confirming this share will be diluted further.\n\nThe finding is real and material as a test-convention violation. The blast radius is bounded to test-file quality (no shipped-output corruption, 9 KJV editions unaffected), so severity is low rather than medium.\n\nThe proposed fix has a flaw: it proposes replacing the share-pin with `assert len(ephrem) >= 30`, but `test_ephrem_now_substantively_present` at line 1165 already asserts exactly `len(ephrem) >= 30`. That makes the proposed fix a pure duplicate with no new coverage. The share-pin was meant to cap Cyril dominance — the correct replacement is an absolute Cyril count ceiling. The class docstring at line 1133 records the post-wave-1 state as \"Cyril 91 (70%)\" — so a ceiling of `len(cyril) <= 91` pins that milestone without breaking on future non-Cyril additions. This catches a real regression (Cyril entries re-inflated above the wave-1 level) while remaining stable as future Ephrem/Jubilees waves land."
    }
   ]
  },
  {
   "dimension": "tests",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "test_detect_returns_empty_for_uncommented_verse assumes mat 1:1 has no patristic entry (same state-default violation)",
   "file": "YHWH v2.4/tests/test_patristic_gamma3.py",
   "line": "221-228",
   "evidence": "```python\ndef test_detect_returns_empty_for_uncommented_verse(self):\n    d = detectors.PatristicCommentaryDetector()\n    # No NT verses in the seed yet\n    assert d.detect(\"mat\", 1, 1, \"\") == []\n    # Genesis verse without commentary\n    assert d.detect(\"gen\", 50, 1, \"\") == []\n```\nBoth assertions assume world state: 'mat 1:1 is empty' (temporary — the comment says 'No NT verses … yet') and 'gen 50:1 is empty'. If either is filled by a future γ.3.x wave, this test fails not because the detector broke but because the world changed. The test should verify the contract (empty list for a verse with no data) against state the test controls.",
   "fix": "Use a controlled synthetic state or a provably-non-corpus coordinate:\n```python\ndef test_detect_returns_empty_for_uncommented_verse(self):\n    d = detectors.PatristicCommentaryDetector()\n    # Non-existent book code is the state-invariant empty case.\n    assert d.detect(\"nonexistent-book-xyz\", 99, 99, \"\") == []\n```\nAlternatively, if a mid-book gap in the known corpus (a verse guaranteed to stay empty because the source has no commentary there) is needed for a specific reason, add a comment documenting the sentinel and a setup_class assertion that the verse is still empty before asserting on it.",
   "verifierFix": "Replace both world-state-dependent assertions with a single invariant-sentinel call. In `YHWH v2.4/tests/test_patristic_gamma3.py` lines 221-228:\n\n```python\ndef test_detect_returns_empty_for_uncommented_verse(self):\n    from scripts.core import detectors\n\n    d = detectors.PatristicCommentaryDetector()\n    # Use a coordinate that can never appear in the corpus:\n    # an invalid book code is the true state-invariant empty case.\n    assert d.detect(\"nonexistent-book-xyz\", 99, 99, \"\") == []\n```\n\nThis is correct and safe: the lookup indexes by book code and no valid expansion wave will ever insert `\"nonexistent-book-xyz\"`. It tests the contract (detector returns `[]` when the corpus has no matching entry) against state the test controls, rather than against the current absence of future γ.3.x data. The original `mat 1:1` and `gen 50:1` assertions should be dropped entirely — they add no coverage over the invariant case and will become spurious failures on the first NT or late-Genesis expansion.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read `YHWH v2.4/tests/test_patristic_gamma3.py` lines 221-228 and `YHWH v2.4/content/sources/patristic_commentaries.json` in full. The test code matches the evidence exactly. The JSON corpus currently holds 8 entries all in Genesis 1-3; there are no Matthew entries and no Genesis 50 entries, so both assertions pass today. However: (a) the inline comment \"No NT verses in the seed yet\" is the author's own acknowledgement that this is a transient state — the `_meta` block in the JSON explicitly documents that \"future γ.3.x\" will expand the corpus beyond the Augustine-on-Genesis seed, and Matthew is the obvious first NT target; (b) `gen 50:1` is similarly unguarded — any expansion through Genesis (Genesis 22 / 28 / 50 etc. are all high-commentary targets) will silently break the second assertion without the detector being broken. The `setup_class` clears the `lru_cache` going in (line 186-187), so there is no cross-test contamination issue — the fragility is purely world-state-dependent, not cache-pollution. Blast radius: test-only; no shipped output, no KJV byte-stability, no build pipeline is affected. Severity is medium: the test will produce a spurious CI failure the first time a γ.3.x expansion adds either coordinate, requiring a test fix that would not otherwise have been needed. The proposed fix (use `\"nonexistent-book-xyz\"` as the sentinel) is correct and safe — an invalid book code is a true invariant that the corpus will never populate, so it directly tests the contract (empty list when no data exists) against controlled state rather than against the current absence of future data."
    }
   ]
  },
  {
   "dimension": "tests",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "promote_candidate coordinate guard not directly unit-tested — only batch_insert_notes is covered",
   "file": "YHWH v2.4/tests/test_scripts.py",
   "line": "6617-6629",
   "evidence": "`TestCoordGuard.test_batch_insert_drops_out_of_range` calls `batch_insert_notes` which calls `coord_in_canonical_extent` internally. However `promote_candidate` in `scripts/promote.py` (line 403) has its OWN identical guard. The test never calls `promote_candidate` with an out-of-extent coord. Mutation: remove the guard at `promote.py:403` — no existing test catches it, because `TestPromoteCandidateIdempotency` only exercises valid coordinates and `TestCoordGuard` only goes through `batch_insert_notes`.",
   "fix": "Add a test in `test_promote_idempotency.py` that directly exercises the `promote_candidate` guard:\n```python\ndef test_promote_candidate_drops_out_of_extent_coord(self, fake_book):\n    \"\"\"promote_candidate must return (False, '') for out-of-canonical-extent\n    coordinates — the coord guard in promote.py:403 is load-bearing.\"\"\"\n    c = _candidate(\n        verse=999,   # Gen has ≤31 verses per chapter; v=999 is out of extent\n        chapter=1,\n        # Use 'gen' so coord_in_canonical_extent can reject it\n    )\n    # We need the fake_book to be 'gen', not 'tst' (unknown books pass through).\n    # So write a gen.py fixture:\n    import scripts.promote as promote_mod\n    notes_dir = fake_book[\"path\"].parent\n    gen_path = notes_dir / \"gen.py\"\n    gen_path.write_text('NOTES = []\\nNOTES_GEN = NOTES\\n', encoding='utf-8')\n    ok, _ = promote_mod.promote_candidate(\"gen\", c)\n    assert ok is False, \"out-of-extent coord must be rejected by promote_candidate\"\n```",
   "verifierFix": "Add a standalone test (NOT reusing `fake_book` which is wired to \"tst\") to `test_promote_idempotency.py`. Use a `tmp_path`-based fixture for \"gen\" directly:\n\n```python\ndef test_promote_candidate_drops_out_of_extent_coord(tmp_path, monkeypatch):\n    \"\"\"promote_candidate must return (False, '') for out-of-canonical-extent\n    coordinates — the guard at promote.py:403 is load-bearing for the\n    chi-cluster ingest path and is NOT exercised by the batch_insert_notes tests.\"\"\"\n    import scripts.promote as promote_mod\n\n    notes_dir = tmp_path / \"content\" / \"notes\"\n    notes_dir.mkdir(parents=True, exist_ok=True)\n    gen_path = notes_dir / \"gen.py\"\n    gen_path.write_text('NOTES = []\\nNOTES_GEN = NOTES\\n', encoding=\"utf-8\")\n    monkeypatch.setattr(promote_mod, \"NOTES_DIR\", notes_dir)\n\n    c = {\n        \"id\": \"gen-1-999-001\",\n        \"chapter\": 1,\n        \"verse\": 999,          # Gen 1 has 31 verses; 999 is out of extent\n        \"kind\": \"comm-ethiopian\",\n        \"anchor\": \"\",\n        \"draft_title\": \"T\",\n        \"draft_label\": \"L\",\n        \"draft_body\": \"<aside>body</aside>\",\n        \"source_attribution\": \"Src. PD.\",\n        \"source_name\": \"Src\",\n        \"confidence\": 0.9,\n    }\n    ok, suffix = promote_mod.promote_candidate(\"gen\", c)\n    assert ok is False, \"out-of-extent coord must be rejected by promote_candidate guard\"\n    assert suffix == \"\"\n```\n\nThis is a clean standalone test that does not reuse `fake_book` (which monkeypatches NOTES_DIR to the \"tst\" directory), uses `monkeypatch` properly, and directly exercises the guard path at `promote.py:403` with a real canonical book code.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read all three relevant files directly.\n\n1. `test_promote_idempotency.py` lines 54–77: the `fake_book` fixture uses book code \"tst\" and monkeypatches `promote_mod.NOTES_DIR`. Every `promote_candidate` call in `TestPromoteCandidateIdempotency` uses `\"tst\"` as the book code.\n\n2. `canonical_verse_counts.py` lines 166–172: `coord_in_canonical_extent` catches any exception from `canonical_book_shape` and returns `True` (keep); it also returns `True` when `shape` is empty. An unknown book code like \"tst\" falls through with `return True` — so the guard at `promote.py:403` is never triggered by any test in that class.\n\n3. `promote.py` lines 400–404: the guard `if not coord_in_canonical_extent(book, chapter, verse): return False, \"\"` is real and load-bearing. Its only test coverage comes from `test_scripts.py:TestCoordGuard.test_batch_insert_drops_out_of_range`, which goes through `batch_insert_notes`, NOT through `promote_candidate` directly.\n\nThe finding is confirmed: a mutation removing the guard at `promote.py:403` would not be caught by the test suite, because all `promote_candidate` tests use \"tst\" (unknown book → guard always passes).\n\nSeverity recalibration: the blast radius is bounded. This guard only fires during the interactive χ-cluster promote pipeline; it does not gate the 9-KJV editions build. Corrupted coordinates entering via `promote_candidate` would appear in `content/notes/<book>.py` files and would be caught by downstream validation (the `batch_insert_notes` path has its own identical guard). So this is a real test-coverage gap, not a production-crash risk. Severity is low rather than medium.\n\nThe proposed fix is logically correct and safe: it only adds a test, does not touch marathon core, and is additive. One minor issue: `_candidate()` hardcodes `\"id\": f\"tst-{chapter}-{verse}-001\"` but that's just a string field, not functional. The fix should also use `monkeypatch` or explicitly set `promote_mod.NOTES_DIR` rather than relying on the `fake_book` fixture's existing monkeypatch — the proposed fix calls the fixture, which already does the monkeypatch, so it's fine. The test as written is workable with minor cleanup (using `monkeypatch` from the fixture, not a bare `fake_book` that has `tst` wired in as the NOTES_DIR target)."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "high",
   "title": "MATRIX_MAP and REPO_MAP say 71 kinds; actual kinds.yaml has 72",
   "file": "YHWH v2.4/dev/MATRIX_MAP.md",
   "line": "17, 27, 133",
   "evidence": "`kinds.yaml` has exactly 72 `- code:` entries (verified by grep count = 72). `test_validate_schemas.py` line 212 correctly pins `assert result['record_count'] == 72` and its comment notes '71 → 72 with Track C Torrey (topic-torrey)'. MATRIX_MAP line 17 states `71 kinds`, line 27 states `kinds.yaml ........ 71 kinds`, and line 133 (Findings item 1) claims the RESOLVED docstring reads `71 kinds / 67,715 notes / 11 editions` — that resolved value itself is now stale. REPO_MAP line 30 states `kinds.yaml (71)`.",
   "fix": "In `MATRIX_MAP.md` update the three occurrences of '71 kinds' to '72 kinds'. In `REPO_MAP.md` line 30 change `kinds.yaml (71)` to `kinds.yaml (72)`.",
   "verifierFix": "The finder's fix is correct but incomplete. Update four locations:\n1. YHWH v2.4/dev/MATRIX_MAP.md line 17: change \"71 kinds\" to \"72 kinds\" in the counts parenthetical.\n2. YHWH v2.4/dev/MATRIX_MAP.md line 27: change `kinds.yaml ........ 71 kinds` to `kinds.yaml ........ 72 kinds`.\n3. YHWH v2.4/dev/MATRIX_MAP.md line 133: the quoted resolved-docstring value `\"71 kinds / 67,715 notes / 11 editions\"` should be updated to `\"72 kinds / 67,715 notes / 11 editions\"` to match what matrix.py now actually says.\n4. YHWH v2.4/dev/REPO_MAP.md line 30: change `kinds.yaml (71)` to `kinds.yaml (72)`.\nNo code, schema, or test changes needed. All four are prose-only edits to .md files — zero risk to EPUB byte-stability or marathon core.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified all claims by reading the source files directly. `kinds.yaml` grep count = 72 confirmed. `test_validate_schemas.py` line 212 asserts `result[\"record_count\"] == 72` with an explicit comment \"71 → 72 with Track C Torrey (topic-torrey)\". `scripts/core/matrix.py` line 9 already reads \"72 kinds\" — the docstring was updated. However, MATRIX_MAP.md lines 17 and 27 still contain \"71 kinds\", and line 133 quotes the now-stale resolved docstring as `\"71 kinds / 67,715 notes / 11 editions\"`. REPO_MAP.md line 30 still reads `kinds.yaml (71)`. This is a genuine documentation drift: four stale occurrences across two markdown files. No runtime code, no EPUB output, no schema, and no test logic is affected — pure documentation staleness. Severity is bounded to docs only."
    },
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified all cited locations. kinds.yaml has exactly 72 `code:` entries (grep count = 72), including `topic-torrey` at line 783 which was the 71→72 addition. test_validate_schemas.py line 212 correctly pins `result[\"record_count\"] == 72` with an explicit comment documenting \"71 → 72 with Track C Torrey (2026-05-25)\". MATRIX_MAP.md line 17 reads \"71 kinds\", line 27 reads \"kinds.yaml ........ 71 kinds\", and line 133 states the RESOLVED docstring reads \"71 kinds / 67,715 notes / 11 editions\" — all three are stale counts. REPO_MAP.md line 30 reads \"kinds.yaml (71)\" — also stale. The finding is real and accurate. However, severity is overstated: this is pure documentation staleness in two dev-facing .md files. No build output, no shipped EPUBs, no runtime logic, and no byte stability of the 9 KJV editions is affected. The test suite already enforces the correct 72 count. The proposed fix (update three occurrences in MATRIX_MAP.md and one in REPO_MAP.md) is correct and safe — it touches only markdown documentation, not code, data, or marathon-core files."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "Mint-7 plan status says 'execution PENDING' but it is COMPLETE; INDEX puts it in 'In progress'",
   "file": "YHWH v2.4/docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md",
   "line": "2",
   "evidence": "`**Status:** in progress — audit COMPLETE (2026-05-31, 15-agent workflow wf_365eda78); plan written + findings saved; **execution PENDING (start Phase A next session)**.` But SESSION_STATE headline and the plan's own Phase E section (line 157) both confirm `✅ PHASE E (E1/E2/E4 done; E3 shipped) ... COMPLETE — Phases A · C · B · D · E all shipped + synced 2026-05-31`. The INDEX.md `## In progress (9)` section count is also wrong by 1 (mint-7 is done; should be 8 in-progress).",
   "fix": "Update the plan's `Status:` header to `COMPLETE — Phases A · C · B · D · E all shipped + synced 2026-05-31`. Move the mint-7 entry from 'In progress' to 'Shipped' in INDEX.md, and correct the section header from `(9)` to `(8)`.",
   "verifierFix": "In C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\docs\\superpowers\\plans\\2026-05-31-mint-7-quality-pass.md line 2, replace the entire Status line with: **Status:** COMPLETE — Phases A · C · B · D · E all shipped + synced 2026-05-31 (15-agent audit wf_365eda78). In C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\docs\\superpowers\\INDEX.md: (a) change \"## In progress (9)\" to \"## In progress (8)\"; (b) move the mint-7 table row (the 2026-05-31 Plan row) from the In progress table to the Shipped table. No other files need changing; marathon core and build pipeline are untouched.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified both cited locations. (1) C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\docs\\superpowers\\plans\\2026-05-31-mint-7-quality-pass.md line 2 still reads \"execution PENDING (start Phase A next session)\" — stale text that contradicts line 157 onward where every phase (A, B, C, D, E) carries a checkmark and the Phase E block explicitly states \"PHASE E … COMPLETE … 2026-05-31\". (2) C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\docs\\superpowers\\INDEX.md line 14 places the mint-7 row under the \"## In progress (9)\" section header even though its own Status cell already reads \"COMPLETE — Phases A · C · B · D · E all shipped + synced 2026-05-31\". The section count (9) is therefore inflated by 1. No build code, no KJV output, no byte-stability invariant is touched — this is purely a docs inconsistency. Blast radius is zero beyond misleading future session state reads. The proposed fix is correct and safe: patch the Status header in the plan file and move the mint-7 row to the Shipped section in INDEX.md while decrementing the In-progress count from 9 to 8."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "MATRIX_MAP says 13 translation dirs; actual filesystem has 14",
   "file": "YHWH v2.4/dev/MATRIX_MAP.md",
   "line": "32",
   "evidence": "`translations/<id>/ .... 13 dirs` in the CONFIG section header comment. Actual directories under `content/translations/`: kjv, geez-tewahedo, amharic-tewahedo, geez-tewahedo-en, amharic-tewahedo-en, wlc, jps, lxx-brenton-english, lxx-brenton-greek, douay-rheims, vulgate-clementine, arabic-vandyke = 12 with `_meta.yaml`, plus lxx-swete-greek and byzantine-greek (both added during the τ.5/Phase 2 ingest waves, both have _meta.yaml as confirmed by grep) = 14. REPO_MAP line 32 independently says '13 dirs'.",
   "fix": "Update `MATRIX_MAP.md` line 32 from `13 dirs` to `14 dirs`. Update `REPO_MAP.md` line 32 translation dir count from `13 dirs` to `14 dirs`. Also add `lxx-swete-greek` and `byzantine-greek` to REPO_MAP's enumeration of partial pilots.",
   "verifierFix": "Update the count from 13 to 14 in both places where it appears:\n\n1. `YHWH v2.4/dev/MATRIX_MAP.md` line 17: change \"13 translation dirs\" to \"14 translation dirs\".\n2. `YHWH v2.4/dev/MATRIX_MAP.md` line 31: change \"translations/<id>/ .... 13 dirs\" to \"translations/<id>/ .... 14 dirs\".\n3. `YHWH v2.4/dev/REPO_MAP.md` line 32: change \"13 dirs\" to \"14 dirs\"; add `lxx-swete-greek` and `byzantine-greek` to the partial-pilots list in the enumeration; and remove or qualify \"Each has `_meta.yaml`\" because `geez-tewahedo-en` and `amharic-tewahedo-en` currently lack that file (they are early-stage dirs with verse .py files only).\n\nThe corrected REPO_MAP line 32 description of the pilots clause should read something like: `and partial pilots (wlc, jps, lxx-brenton-{english,greek}, douay-rheims, vulgate-clementine, arabic-vandyke, lxx-swete-greek, byzantine-greek — gen.py only). All except geez-tewahedo-en / amharic-tewahedo-en have _meta.yaml.`\n\nNo code changes needed; this is a documentation-only fix. Safe for all editions.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I independently verified all claims by reading the actual files.\n\nMATRIX_MAP.md: Line 17 reads \"13 translation dirs\" and line 31 reads \"translations/<id>/ .... 13 dirs\". Both say 13.\n\nFilesystem reality: The `_meta.yaml` glob found exactly 12 dirs with that file (kjv, geez-tewahedo, amharic-tewahedo, wlc, jps, lxx-brenton-english, lxx-brenton-greek, douay-rheims, vulgate-clementine, arabic-vandyke, lxx-swete-greek, byzantine-greek). Additionally, `geez-tewahedo-en` and `amharic-tewahedo-en` exist as directories (confirmed by grep and direct glob) but lack `_meta.yaml`. Total = 14 dirs.\n\nREPO_MAP.md line 32 says \"13 dirs\", enumerates 12 by name (including geez-tewahedo-en and amharic-tewahedo-en), and omits lxx-swete-greek and byzantine-greek entirely. It also falsely states \"Each has _meta.yaml\" — the two -en dirs don't have one.\n\nThe `check_translations_meta` function in scripts/check_content.py at line 146 iterates ALL subdirs under translations/ (excluding sources/), confirming the code sees all 14 dirs and would flag the two -en dirs as violations for missing _meta.yaml.\n\nThe finding is confirmed as a real docs drift: count is wrong in two places (MATRIX_MAP lines 17 and 31; REPO_MAP line 32), and the REPO_MAP enumeration omits two dirs.\n\nSeverity: This is docs-only. No code reads the literal \"13\" count from the docs. No build output is affected; the 9 KJV editions remain byte-stable. The impact is bounded to developer orientation (a future session could mis-count available translations). The finder's severity of \"medium\" is slightly generous — this is purely a documentation stale-count with zero runtime or output impact. \"Low\" is more appropriate."
    }
   ]
  },
  {
   "dimension": "byte-stability",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "build_cache.compute_cache_key omits epub_working/META-INF/ subdirectory from its hash, so container.xml changes do not invalidate the cache",
   "file": "YHWH v2.4/scripts/core/build_cache.py",
   "line": "239-248",
   "evidence": "`for entry in sorted(epub_dir.iterdir(), key=lambda p: p.name): if entry.is_file() and not entry.name.startswith(\".\"):` — `iterdir()` is non-recursive and `entry.is_file()` skips subdirectories. `epub_working/META-INF/container.xml` (the EPUB container declaration) and any future subdirectory assets (e.g. `epub_working/images/`) are never hashed. A change to `container.xml` would produce the same cache key and silently serve a stale EPUB.",
   "fix": "Replace the shallow `iterdir()` scan with a recursive `rglob('*')` scan, applying the same `should_skip` logic as `build_epub.collect_files` (i.e. excluding dotfiles and `onix/`): `for entry in sorted(epub_dir.rglob('*'), key=lambda p: str(p.relative_to(epub_dir))): if entry.is_file() and not any(part.startswith('.') for part in entry.relative_to(epub_dir).parts) and 'onix' not in entry.relative_to(epub_dir).parts: parts.append((f'epub:{entry.relative_to(epub_dir).as_posix()}', _hash_file(entry)))`",
   "verifierFix": "The proposed `rglob('*')` replacement is safe and correct if you want to close the gap defensively. A minimal, lower-noise variant that mirrors the existing dotfile-skip logic already present in the shallow scan:\n\n```python\n# 10. Templated EPUB input (epub_working/) — recursive to catch META-INF/container.xml.\nepub_dir = _REPO / \"epub_working\"\nif epub_dir.is_dir():\n    for entry in sorted(epub_dir.rglob(\"*\"), key=lambda p: str(p.relative_to(epub_dir))):\n        if not entry.is_file():\n            continue\n        rel = entry.relative_to(epub_dir)\n        # Skip dotfile dirs (.backups/) and the onix/ tree (excluded from EPUB packaging).\n        if any(part.startswith(\".\") or part == \"onix\" for part in rel.parts):\n            continue\n        parts.append((f\"epub:{rel.as_posix()}\", _hash_file(entry)))\n```\n\nThis matches the exclusions that `build_epub.py`'s `collect_files` applies (dotfiles + `onix`), keeping the cache key aligned with actual EPUB content. That said, given `container.xml` has no write path in the codebase, this fix can legitimately be deferred until a future change introduces a subdirectory that IS build-modified.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The code at lines 239-248 of YHWH v2.4/scripts/core/build_cache.py is exactly as described: a shallow `epub_dir.iterdir()` with `entry.is_file()` guard that skips all subdirectories. `epub_working/META-INF/container.xml` exists on disk, is not excluded by any of `build_epub.py`'s `EXCLUDE_DIR_NAMES` or `EXCLUDE_PREFIXES`, and IS packaged into the final EPUB via `collect_files`'s `rglob`. So the structural gap is confirmed: a change to `container.xml` would not invalidate the cache key.\n\nHowever, the severity is materially overstated. Across the entire codebase, zero scripts write to `container.xml`. It is a 7-line static declaration (`full-path=\"content.opf\"`) that has not changed since the EPUB base was recovered. The only code that reads it is `build_standalone.py` (off-limits marathon core) and `validate_working_dir` (a presence check). There is no build step that mutates it. The `onix/` and `.backups/` subdirectories that are also missed are correctly excluded from the EPUB by `build_epub.py`'s own filters, so they carry no stale-EPUB risk either.\n\nThe real-world blast radius is therefore near-zero: the scenario where a developer edits `container.xml` in isolation (without touching `content.opf`, notes, or any other hashed input) is implausible in this project's workflow, and even if it happened the symptom would be a stale EPUB delivered from cache — not corruption of the 9 KJV byte-stable editions.\n\nThe proposed `rglob` fix is technically correct and safe (additive, doesn't touch marathon core, doesn't change schema), but it solves a problem that doesn't occur in practice. Downgrading severity to low/info."
    }
   ]
  },
  {
   "dimension": "data-validity",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "`add_note.py` chapter-range guard uses `books.yaml ch_count` instead of `coord_in_canonical_extent`, causing wrong accept/reject for `aes` and silently wrong accept for other non-standard-start books",
   "file": "YHWH v2.4/scripts/add_note.py",
   "line": "309-310",
   "evidence": "`if book.get(\"ch_count\", 0) > 0 and (args.ch < 1 or args.ch > book[\"ch_count\"]): err(...)`. For `aes` with `ch_count: 10` in books.yaml, chapters 11-16 are rejected (correct — they have no HTML anchor), but chapters 1-9 are accepted (wrong — `aes` in the HTML does have those anchors but the KJV skeleton has no such content; however all of ch 1-9 exist as HTML anchors in the base HTML per `index_split_028.html`). The more material asymmetry is that `promote.py:403` uses `coord_in_canonical_extent` (allowing ch 11-16 through) while `add_note.py` uses `ch_count` (blocking ch 11-16). These two entry-points apply inconsistent validation rules for the same book, causing the corpus to accumulate notes at ch 11-16 (via promote) that the manual tool cannot add (via add_note). The CHANGELOG at line 1801 notes the scope of the original guard: \"corpus-wide scan → 0 invalid-coordinate notes\" — this relied solely on the KJV-skeleton check and did not catch the HTML-anchor mismatch.",
   "fix": "Unify the chapter validation at the promote boundary: in `promote_candidate` (promote.py:403) and `batch_insert_notes` (promote.py:330), add a secondary check using `books.yaml ch_count` for Strategy-B books: `if book_ch_count > 0 and chapter > book_ch_count: return False`. Load book metadata via `config.get_book(book)`. This makes the promote path consistent with `add_note.py` and stops future notes from landing at chapters beyond the HTML's anchor range. The `add_note.py` guard itself is correct as written; no change needed there.",
   "verifierFix": "The finder's fix direction is correct but the proposed implementation needs a minor correction: `promote.py` currently has no import of `scripts.core.config`, so `config.get_book(book)` is not available. The cleanest minimal fix adds the `ch_count` guard inline in `promote.py` using a dedicated helper rather than pulling in the full config module.\n\n**Option A (minimal, no new import):** In `scripts/core/canonical_verse_counts.py`, add a thin helper that reads `ch_count` from books.yaml:\n\n```python\n@lru_cache(maxsize=256)\ndef html_chapter_count(book: str) -> int:\n    \"\"\"Return the HTML ch_count for *book* from books.yaml, or 0 if not set.\n    This reflects the base-HTML chapter structure, which may differ from the\n    KJV skeleton chapter numbering (e.g. aes: HTML uses chs 1-10, KJV uses 10-16).\"\"\"\n    import yaml, pathlib\n    books_yaml = pathlib.Path(__file__).parents[2] / \"content\" / \"books.yaml\"\n    data = yaml.safe_load(books_yaml.read_text(encoding=\"utf-8\"))\n    for b in data.get(\"books\", []):\n        if b.get(\"code\") == book:\n            return b.get(\"ch_count\", 0)\n    return 0\n```\n\nThen in `promote.py`, after the existing `coord_in_canonical_extent` import, add:\n\n```python\nfrom scripts.core.canonical_verse_counts import coord_in_canonical_extent, html_chapter_count  # noqa: E402\n```\n\nAnd at `promote_candidate` (line 403) and `batch_insert_notes` (line 330), add a second guard immediately after the `coord_in_canonical_extent` call:\n\n```python\n# Secondary guard: HTML chapter structure may use different chapter numbering\n# than the KJV skeleton (e.g. aes HTML=chs 1-10, KJV skeleton=chs 10-16).\n# Drop notes beyond the HTML anchor range so they do not silently fail injection.\n_html_ch_count = html_chapter_count(book)\nif _html_ch_count > 0 and chapter > _html_ch_count:\n    return False, \"\"   # in promote_candidate\n    # (use `continue` instead of `return False, \"\"` in batch_insert_notes)\n```\n\n**Option B (also correct):** Import `scripts.core.config` in `promote.py` and call `config.get_book(book).get(\"ch_count\", 0)`. This works but adds a heavier dependency.\n\n**Additionally:** The ~50 already-orphaned notes at `aes` ch 11–16 in `content/notes/aes.py` should be reviewed. They were auto-generated from Strong's entries keyed to KJV-versification references that have no HTML anchor in the current base HTML. A one-time audit script should log and optionally remove/re-file them. They are not injected now and never will be under the current HTML base, so they are dead corpus weight.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I independently verified every factual claim in the finding by reading the actual source files.\n\n**What the code does (verified):**\n\n`YHWH v2.4/scripts/add_note.py` line 309: `if book.get(\"ch_count\", 0) > 0 and (args.ch < 1 or args.ch > book[\"ch_count\"])` — this guards against chapters beyond `ch_count` as stored in `books.yaml`. For `aes`, `ch_count: 10`.\n\n`YHWH v2.4/scripts/promote.py` lines 330 and 403: both use `coord_in_canonical_extent(book, chapter, verse)` from `scripts/core/canonical_verse_counts.py`. That function builds chapter shape from `load_kjv_skeleton()` — the KJV skeleton at `content/translations/kjv/aes.py`. The `aes` KJV skeleton starts at chapter 10 and runs through chapter 16 (confirmed by reading the file). Therefore `coord_in_canonical_extent(\"aes\", 11, 3)` returns `True`.\n\n**The HTML reality (verified):** `epub_working/index_split_028.html` contains `id=\"ch-b25-c1\"` through `id=\"ch-b25-c10\"` — exactly 10 chapter anchors for `aes`. There are NO `ch-b25-c11` through `ch-b25-c16` anchors. The HTML versification for `aes` uses chapters 1–10 (LXX/WEB scheme); the KJV skeleton uses chapters 10–16 (Apocrypha of Esther scheme). These are different versification numberings of the same material.\n\n**Corpus damage confirmed:** `content/notes/aes.py` already contains notes at chapters 11, 13, 14, 15, and 16 — approximately 50+ tuples at coordinates where the HTML has no chapter anchor. The inject.py Strategy-B path (lines 732–738) silently skips any note whose `id=\"ch-{bxx}-c{ch}\"` is absent, logging it as `missing_anchor`. These notes will never appear in any built EPUB edition.\n\n**KJV byte-stability:** The 9 KJV editions are not corrupted. The orphaned notes simply do not inject; output is unchanged. Byte-stability is preserved.\n\n**Why the CHANGELOG \"0 invalid-coordinate notes\" claim missed this:** The audit used `coord_in_canonical_extent`, which passes `aes` ch 11–16 (they are valid KJV skeleton coordinates). The mismatch is between the KJV skeleton chapter numbering and the HTML chapter numbering for this one book.\n\n**Proposed fix evaluation:** The finder's direction is correct — add a secondary `ch_count` guard to `promote_candidate` and `batch_insert_notes` in `promote.py`. The proposed implementation requires importing `scripts.core.config.get_book` into `promote.py` (currently absent). This is safe and additive. However, there is a cleaner option that avoids the new dependency: extend `coord_in_canonical_extent` itself to also accept an optional `html_ch_count` cap parameter, or add a thin dedicated helper. The corrected fix below shows the minimal safe approach using the existing config import pattern.\n\n**Severity recalibration:** Confirmed real defect with ~50 permanently-orphaned notes in the corpus that can never be rendered. No crash, no EPUB corruption, KJV byte-stability unaffected. The defect is bounded to `aes` ch 11–16 in the current corpus. Medium is defensible; low is also reasonable given the blast radius is limited to silent content omission in a single book's note corpus, not output corruption."
    }
   ]
  },
  {
   "dimension": "data-validity",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "info",
   "title": "`books.yaml` `ch_count: 10` for `aes` is misleading: actual HTML has 10 chapters (1-10) but KJV skeleton has 7 chapters (10-16), causing `coverage.py` and `generate_verse_popups.py` to iterate wrong chapter ranges",
   "file": "YHWH v2.4/content/books.yaml",
   "line": "296-302",
   "evidence": "`aes` entry: `ch_count: 10`. `coverage.py:182` uses `for c in range(1, ch_count + 1)` — this iterates HTML chapters 1-10, which is correct. `generate_verse_popups.py:300` does the same `range(1, ch_count + 1)` — also correct. Both scripts are already aligned with the HTML ch 1-10 scheme. However, `coord_in_canonical_extent` (KJV skeleton ch 10-16) is semantically inconsistent with the HTML ch 1-10 scheme. The `ch_count: 10` is the right value for the HTML-facing tools, but the `_meta.yaml` comment for `aes` at phase τ.7.x.m says `content/books.yaml fixes est at ch_count: 10 (Hebrew/Masoretic core; the Greek Additions are the SEPARATE b25 book)` — this comment conflates `est` and `aes` metadata. The `aes` ch_count commentary is absent. No functional bug in coverage/popup generation — both use the correct HTML chapter range. Informational for future maintainability.",
   "fix": "Add a comment in books.yaml for `aes`: `# ch_count = 10: sequential HTML chapter numbering (ch 1-10 in epub_working); KJV/LXX uses 10-16 which does NOT correspond — do not use coord_in_canonical_extent for aes inject-range validation`. This documents the dual-numbering system so future ingest and promotion code does not re-introduce the ch 11-16 promotion bug.",
   "verifierFix": "The proposed YAML comment is safe to add as written, but it should also explicitly flag the `coord_in_canonical_extent` no-op. A more complete comment for `content/books.yaml` at the `aes` entry:\n\n```yaml\n  - code: aes\n    # ch_count = 10: sequential HTML chapter numbering (ch 1-10 in epub_working).\n    # KJV/LXX uses ch 10-16; because canonical_verse_counts._book_shape_cached\n    # probes from ch=1, canonical_book_shape(\"aes\") returns {} and\n    # coord_in_canonical_extent is a no-op for aes (always returns True).\n    # Do NOT use coord_in_canonical_extent for aes promote-range validation.\n    # Fix if/when aes notes are actively promoted: override the KJV chapter\n    # mapping or add aes to TEWAHEDO_DISTINCTIVE_NO_KJV with hand-typed counts.\n    ch_count: 10\n```\n\nNo change to any Python file is required for correctness today (the no-op guard means no notes are silently dropped; the downside is no noise filtering on aes coordinates). If a future ingest ever promotes notes into `aes`, the correct fix is to add `aes` to `TEWAHEDO_DISTINCTIVE_NO_KJV` in `scripts/core/canonical_verse_counts.py` (with hand-typed verse counts for ch 10-16) or implement a ch-offset mapping — but that is additive and can wait until aes promotion is actually needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "medium",
     "reasoning": "I independently verified the following:\n\n1. `content/books.yaml` lines 292-300: `aes` has `ch_count: 10`, and the KJV skeleton `content/translations/kjv/aes.py` confirms the KJV data starts at chapter 10 (first tuple is `(10, 4, ...)`) and runs through chapter 16 — 7 KJV chapters total, none of which is chapter 1.\n\n2. `scripts/coverage.py` line 182 and `scripts/generate_verse_popups.py` line 300 both use `range(1, ch_count + 1)`, iterating HTML chapters 1-10. This is correct for the HTML-facing tool layer; confirmed by the finding itself.\n\n3. The real gap, confirmed by reading `scripts/core/canonical_verse_counts.py` lines 138-172: `_book_shape_cached(\"aes\")` calls `load_kjv_skeleton(\"aes\", 1)`, which filters `aes.py` for rows where `r[0] == 1`. Since the KJV data starts at chapter 10, chapter 1 returns `[]`, `not skel` is True, the loop breaks immediately, and the function returns `()`. So `canonical_book_shape(\"aes\")` = `{}`. Consequently, `coord_in_canonical_extent(\"aes\", ch, v)` hits `if not shape: return True` and returns True unconditionally for ANY coordinate — the guard is completely non-functional for `aes`. Notes promoted with garbage `aes` coordinates (e.g. from OCR noise) would silently pass the boundary check in `scripts/promote.py` lines 330 and 403.\n\n4. Blast radius is bounded: `aes` has no active note-promotion pipeline (the book uses sequential HTML chapters 1-10, not the KJV ch 10-16 scheme), so no shipped-output corruption and the 9 KJV editions stay byte-stable. The risk is theoretical/future-ingest.\n\n5. The finding's framing is partly wrong: coverage.py and generate_verse_popups.py are NOT the problem — they work correctly. The real undocumented issue is that `coord_in_canonical_extent` silently fails for `aes` because the KJV probing loop starts at ch=1 and never reaches the actual chapter range.\n\n6. The proposed fix (a YAML comment) is safe, additive, and does not touch any marathon core file. It is correct in spirit but undersells the actual issue — it should also point to `canonical_verse_counts.py`'s probing-from-ch-1 assumption as the root cause for `coord_in_canonical_extent` being a no-op for `aes`."
    }
   ]
  },
  {
   "dimension": "concurrency-caching",
   "kind": "find",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "scripts/bulk_edit.py auto-verify subprocess.run missing stdin=subprocess.DEVNULL — WinError 6 on Windows",
   "file": "YHWH v2.4/scripts/bulk_edit.py",
   "line": "189-192",
   "evidence": "result = subprocess.run(\n    [sys.executable, str(REPO_ROOT / 'scripts' / 'verify.py'), '--quiet'],\n    cwd=str(REPO_ROOT),\n)\n\nThe --apply path of bulk_edit.py auto-runs verify.py as a subprocess after mutations. No stdin=subprocess.DEVNULL. Launched from PowerShell (the dev workflow), this will hit WinError 6. The tool is used for corpus edits so this hits in normal editorial use.",
   "fix": "Add stdin=subprocess.DEVNULL:\n\nresult = subprocess.run(\n    [sys.executable, str(REPO_ROOT / 'scripts' / 'verify.py'), '--quiet'],\n    cwd=str(REPO_ROOT),\n    stdin=subprocess.DEVNULL,  # W-W1: Windows PowerShell guard\n)",
   "verifierFix": "result = subprocess.run(\n    [sys.executable, str(REPO_ROOT / \"scripts\" / \"verify.py\"), \"--quiet\"],\n    cwd=str(REPO_ROOT),\n    stdin=subprocess.DEVNULL,  # W-W1: Windows/PowerShell guard\n)",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Read C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\bulk_edit.py lines 189-192 directly. The subprocess.run call is exactly as quoted — no stdin= argument, no DEVNULL anywhere in the file (grep confirmed zero matches). The project's W-W1 rule (in memory) is explicit: every subprocess.run on Windows/PowerShell must pass stdin=subprocess.DEVNULL or hits WinError 6. The call is on the hot --apply path used in normal editorial corpus work, not dead code. verify.py itself uses stdin=subprocess.DEVNULL for its own sub-calls (line 38), but that does not protect the outer invocation from bulk_edit.py. The finding is real and confirmed. Severity recalibrated down from medium to low: the WinError 6 would crash only the post-apply verify step — the actual file mutations already completed successfully before this line is reached, so no notes-corpus corruption or KJV byte-stability impact. The fix is correct and safe: add stdin=subprocess.DEVNULL to the single subprocess.run call at line 189; no marathon core touched, purely additive."
    }
   ]
  },
  {
   "dimension": "opt-vision",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "do_POST fallthrough to do_PUT causes double _check_admin_auth() when no POST route matches",
   "file": "YHWH v2.4/scripts/web.py",
   "line": "2054-2055",
   "evidence": "do_POST (line 2016-2017) calls _check_admin_auth() first, then at line 2055 falls through to self.do_PUT() (line 1895-1897) which calls _check_admin_auth() a second time. With TOTP enabled this performs two TOTP code verifications on the same code. The supplied_code is validated against the same time window twice. Separately, do_POST already called _read_body() lazily (line 2036) for the first matching POST route; if no POST route matched, rfile is unread — so do_PUT's _read_body() call reads from the stream correctly. The auth double-call is the material issue.",
   "fix": "Replace the fallthrough with a direct 404 for unrecognized POST paths (matching the do_PUT/do_DELETE pattern), or factor the common PUT-compatible POST routes into a shared helper. Simplest fix: replace 'return self.do_PUT()' at line 2055 with 'return self._send_json({\"error\": \"not found\"}, status=404)', and explicitly add any genuinely-shared POST=PUT routes to _POST_ROUTES instead.",
   "verifierFix": "Replace line 2055 in `YHWH v2.4/scripts/web.py`:\n\n    # Before:\n    return self.do_PUT()\n\n    # After:\n    return self._send_json({\"error\": \"not found\"}, status=404)\n\nThis is a one-line change. No other modifications are needed. If any existing POST paths were intentionally sharing PUT handler logic via this fallthrough, they should be explicitly added to `_POST_ROUTES` instead — but reviewing `_POST_ROUTES` (lines 817–919) and `_PUT_ROUTES` shows no documented intent for shared routes, so the fallthrough appears to be an unintentional holdover from before the route-table migration rather than a deliberate design.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading the code:\n\n1. `do_POST` (line 2016-2017) calls `_check_admin_auth()` first and returns on failure.\n2. When no `_POST_ROUTES` or `_MULTIPART_ROUTES` pattern matches, line 2055 calls `self.do_PUT()`, which calls `_check_admin_auth()` again at line 1897.\n3. `_check_admin_auth()` (`YHWH v2.4/scripts/web.py:1324`) is a stateless function — it reads the Authorization header, checks the token with `hmac.compare_digest`, and calls `totp_mod.verify_code`.\n4. `verify_code` (`YHWH v2.4/scripts/core/totp.py:133`) has NO replay-prevention / used-code cache. It is a pure time-window comparison. Calling it twice within the same 30s window returns True both times — no false rejection occurs.\n\nTherefore: the double-call only reaches `do_PUT` on the success path (first call already returned True), so `do_PUT`'s second call also succeeds and the request proceeds normally. There is no false rejection, no double-response, and no security bypass from the double-call. The `do_PUT` fallthrough itself terminates in a 404 for any unrecognized path, so the end result is: an unrecognized POST gets two auth checks performed and then a 404 — functionally identical to a single auth check plus a 404.\n\nHowever, the finding is not entirely wrong. The double-call IS real and the code structure IS confusing/suboptimal. The proposed fix (replace `return self.do_PUT()` at line 2055 with a direct 404) is demonstrably better: it eliminates the redundant auth computation, makes the control flow explicit, is trivially safe (no byte-stability impact, no marathon core involvement, no API dependencies), and removes a latent risk if `_check_admin_auth` ever grows replay-prevention logic (at which point the double-call would cause false rejections on the fallthrough path).\n\nCorrected severity: low. The current code is suboptimal but not broken. The proposed fix is the right one and is a one-line change."
    }
   ]
  },
  {
   "dimension": "opt-vision",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "low",
   "title": "lru_cache on _load_notes_cached uses path string + mtime_ns but mtime_ns resolution can be 1s on some Windows filesystems — stale cache window",
   "file": "YHWH v2.4/scripts/core/notes_io.py",
   "line": "194-202",
   "evidence": "@functools.lru_cache(maxsize=256) def _load_notes_cached(path_str: str, mtime_ns: int) — on FAT32 or some older Windows NTFS volumes, mtime granularity is 1–2 seconds. If atomic_write completes and a subsequent load_notes() call arrives within the same mtime second, it gets the stale cache entry because mtime_ns did not change. The _invalidate_corpus_index_if_notes_file hook in atomic_write (line 86) fires for notes files, but it only invalidates the corpus_index — it does NOT call clear_load_notes_cache(). So a rapid write→read within the same mtime-second window returns the old NOTES list.",
   "fix": "In atomic_write, after os.replace, also call clear_load_notes_cache() when the path is a notes file (same condition as _invalidate_corpus_index_if_notes_file). This is O(1) and idempotent. Alternatively, the atomic_write docstring should document the known 1s stale window. The web editor's save→matrix cycle could see phantom stale data in the rare same-second case.",
   "verifierFix": "In YHWH v2.4/scripts/core/notes_io.py, add a call to clear_load_notes_cache() inside _invalidate_corpus_index_if_notes_file (the single-responsibility hook already called by both atomic_write and atomic_write_bytes), so both writers get the fix for free. Change lines 124-130 to:\n\n    try:\n        if path.suffix == \".py\" and path.parent.name == \"notes\":\n            clear_load_notes_cache()          # belt-and-suspenders: mtime resolution\n            from scripts.core import corpus_index\n            corpus_index.invalidate()\n    except Exception:  # noqa: BLE001\n        pass\n\nThis is one line added inside the existing try block. clear_load_notes_cache() is defined in the same file (line 225) so there is no import needed. Both atomic_write (line 86) and atomic_write_bytes (line 110) already call _invalidate_corpus_index_if_notes_file, so both get the fix. No marathon core files touched, no byte-stability impact (in-memory parse cache only), no new dependencies.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read lines 75-229 of YHWH v2.4/scripts/core/notes_io.py directly. The finding is structurally correct: atomic_write (line 86) calls _invalidate_corpus_index_if_notes_file after os.replace, but never calls clear_load_notes_cache(). The clear_load_notes_cache() function exists at line 225 and its own docstring says \"after bulk rewrites if mtime resolution proves insufficient\" — meaning the authors anticipated exactly this gap but did not wire it into atomic_write. The load_notes() path (line 219) reads st_mtime_ns live from the replaced file each call, so the cache key is derived from the real on-disk mtime. On NTFS (Windows 11, the actual deployment target), mtime resolution is 100ns, making the stale window vanishingly small in practice. On FAT32 the window is 2 seconds and real. The finding's severity framing of \"stale cache window\" is accurate but the practical risk on NTFS is extremely low — it is not a silent data-corruption path in the normal case. The fix is unambiguously correct and cheap: in atomic_write (and atomic_write_bytes for completeness), after os.replace, call clear_load_notes_cache() under the same path.suffix==\".py\" and path.parent.name==\"notes\" guard already used by _invalidate_corpus_index_if_notes_file, or simply call it unconditionally since cache_clear on a miss-heavy cache is O(1). No marathon core touched, no byte-stability risk (parse cache only), no external dependencies. The corrected_fix below is the minimal safe change."
    }
   ]
  },
  {
   "dimension": "opt-vision",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "low",
   "title": "api_restore_backup writes snapshot bytes without validating they are valid UTF-8 Python source before overwriting a notes file",
   "file": "YHWH v2.4/scripts/web_content.py",
   "line": "644-645",
   "evidence": "notes_io.atomic_write_bytes(abs_path, snapshot_bytes) — the restore writes raw bytes from the backup file without checking whether the target is a .py notes file that should be valid UTF-8 Python source. If the backup was of a notes file and the backup is somehow corrupt (truncated, binary garbage from a bad shutil.copy2), the notes corpus will be silently replaced with unparseable content. load_notes() then returns None for that book, which build_edition treats as an empty notes list — silently dropping all notes for that book from every future build.",
   "fix": "When abs_path.suffix == '.py' and abs_path.parent.name == 'notes', validate the snapshot bytes: (1) decode as UTF-8, (2) call load_notes_from_text() on the decoded text, (3) reject with a 400 error if the result is None. This mirrors the parse-before-write discipline used elsewhere in the corpus pipeline.",
   "verifierFix": "In `api_restore_backup` (YHWH v2.4/scripts/web_content.py), after reading `snapshot_bytes` at line 622 and before calling `atomic_write_bytes` at line 645, add a guard scoped to `.py` notes files only:\n\n```python\nif abs_path.suffix == \".py\" and abs_path.parent.name == \"notes\":\n    try:\n        snapshot_text = snapshot_bytes.decode(\"utf-8\")\n    except UnicodeDecodeError:\n        return {\n            \"status\": \"error\",\n            \"code\": \"invalid_snapshot\",\n            \"http\": 400,\n            \"message\": \"snapshot is not valid UTF-8 — will not restore a corrupt notes file\",\n        }\n    from scripts.core.notes_io import load_notes_from_text\n    if load_notes_from_text(snapshot_text) is None:\n        return {\n            \"status\": \"error\",\n            \"code\": \"invalid_snapshot\",\n            \"http\": 400,\n            \"message\": \"snapshot failed to parse as a notes module — will not restore\",\n        }\n```\n\nThis mirrors the parse-before-write discipline already used in the corpus ingest pipeline, is gated strictly to `.py` notes files (so binary restores like cover images are unaffected), requires no new dependencies, and does not touch any marathon core file.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read YHWH v2.4/scripts/web_content.py lines 549–654 and YHWH v2.4/scripts/core/notes_io.py in full. The finding is accurate on the mechanics: `api_restore_backup` calls `notes_io.atomic_write_bytes(abs_path, snapshot_bytes)` at line 645 without any UTF-8 or parseability check, and `build_edition.py` lines 133 and 329 both use `load_notes(book_path) or []`, meaning a None parse result silently becomes an empty list, dropping all notes for that book from every future build. The proposed fix — decode UTF-8 + call `load_notes_from_text` + reject with 400 if None — is concretely better, feasible without any paid API or marathon-core change, and does not affect byte-stability of the 9 KJV editions. However, the severity is bounded by two mitigating factors: (1) the backup files are created by `ensure_backup`/`shutil.copy2` from a file that was valid when backed up, so corruption requires either a disk-level fault or a partial copy2 that fails silently — improbable on local NTFS; (2) the pre-restore backup taken at line 639 (`notes_io.ensure_backup(abs_path)`) makes the operation reversible even if a corrupt snapshot were written, so the \"permanent silent drop\" framing overstates the impact. The finding is not refuted — the defensive validation is a real and worthwhile improvement — but it is genuinely low severity rather than anything higher."
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "filter_html recompiles O(files × kinds × 2) and O(files × disabled_refs × 2) regex patterns — hot build path",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "1001-1061",
   "evidence": "Inside `filter_html` (called for each of 61 HTML files per edition), lines 1019-1032 loop over `disabled_kinds` and call `re.compile(...)` twice per kind per file. For an edition with 35 disabled kinds: 61 × 35 × 2 = 4,270 compile calls per build, all discarded. Lines 1036-1053 loop over `disabled_html_ref_ids` and call `re.compile(...)` twice per ref_id per file; for `catholic-study` (which has `traditions_default` set and potentially thousands of disabled ref_ids from `compute_tradition_disabled_html_ref_ids`), this is the dominant CPU cost per build.",
   "fix": "Pre-compile per-edition pattern sets once in `build_one`, before the HTML file loop. Change `filter_html` signature to accept `pre_compiled_kind_patterns: list[tuple[re.Pattern, re.Pattern]] | None = None`. In `build_one`, before the `for html_path in tmp.glob('*.html'):` loop, build: `kind_patterns = [(re.compile(rf'<a class=\"note-ref note-{re.escape(k)}\"[^>]*>.*?</a>', re.DOTALL), re.compile(rf'<aside class=\"note note-{re.escape(k)}\"[^>]*>.*?</aside>', re.DOTALL)) for k in disabled]`. For the disabled_html_ref_ids, build a combined alternation OR use a single-pass findall+replace approach. This is byte-identical (same patterns, same substitutions, just cached). Expected saving: 5-20s per edition build on the filter pass alone.",
   "verifierFix": "The fix is valid but needs both call sites covered and should remain backward-compatible.\n\nIn `build_one` (before line 2846 and before line 2768), build:\n\n```python\nkind_patterns = [\n    (\n        re.compile(rf'<a class=\"note-ref note-{re.escape(k)}\"[^>]*>.*?</a>', re.DOTALL),\n        re.compile(rf'<aside class=\"note note-{re.escape(k)}\"[^>]*>.*?</aside>', re.DOTALL),\n    )\n    for k in disabled\n]\nref_id_patterns = [\n    (\n        re.compile(rf'<a class=\"note-ref [^\"]*\" id=\"{re.escape(ref_id)}\"[^>]*>.*?</a>', re.DOTALL),\n        re.compile(\n            rf'<aside class=\"note [^\"]*\" id=\"{re.escape(ref_id.replace(\"ref-\", \"note-\", 1))}\"[^>]*>.*?</aside>',\n            re.DOTALL,\n        ),\n    )\n    for ref_id in (disabled_html_ref_ids or [])\n] if disabled_html_ref_ids else []\n```\n\nChange `filter_html` signature to accept `pre_compiled_kind_patterns` and `pre_compiled_ref_patterns` (both `list[tuple[re.Pattern, re.Pattern]] | None = None`). When not None, iterate the pre-compiled tuples directly instead of calling `re.compile` inside the loop. When None, fall back to current behavior (preserves backward compatibility for any direct test calls to `filter_html`).\n\nPass the pre-compiled lists from both the real build path (line 2848) and the dry_run path (line 2770).\n\nThis is byte-identical and the win is material only for editions with large `disabled_html_ref_ids` sets (tradition-filtering or time-filtering active), where Python's internal 512-entry LRU is evicted by the volume of unique patterns.",
   "panel": [
    {
     "refuted": false,
     "confidence": "medium",
     "reasoning": "I read `filter_html` at lines 1001-1061 and the call site in `build_one` at lines 2846-2853.\n\nThe finding is structurally accurate: `re.compile()` is called inside the per-file loop, so patterns are nominally re-compiled on every one of the ~61 HTML files. However, the severity is overstated for the `disabled_kinds` branch, and correctly identifies a real issue only for large `disabled_html_ref_ids` sets.\n\n**disabled_kinds branch (lines 1019-1032):** With ~35 kinds × 2 = 70 patterns, Python's internal `re` module LRU cache (512 entries in CPython 3.7+, keyed by (pattern, flags, type)) will retain all 70 patterns after the first file. Files 2-61 will hit the cache at O(1) lookup. Net extra cost: essentially zero. The finder's 4,270-compile-calls figure is correct in raw count but ignores the internal cache — the actual cost after file 1 is negligible dictionary lookups, not real re-compilation.\n\n**disabled_html_ref_ids branch (lines 1036-1052):** For editions like `catholic-study` where `compute_tradition_disabled_html_ref_ids` can return thousands of unique ref_ids, × 2 = potentially several thousand patterns. Python's LRU cache of 512 entries will be thrashed: after filling, every new pattern evicts an old one. On file 2-61, patterns from the \"back\" of the iteration order will be evicted and re-compiled. This IS real work — thousands of cache misses × 60 extra files = tens of thousands of real re-compilations beyond the first file. So the finding is real and material for editions with large tradition-filtered ref_id sets, which is the design-intended path for `catholic-study`.\n\n**The proposed fix is correct and safe:** Pre-compile `kind_patterns` and `ref_id_patterns` once in `build_one` before `for html_path in tmp.glob('*.html'):`, then change `filter_html`'s signature to accept `pre_compiled_kind_patterns: list[tuple[re.Pattern, re.Pattern]] | None = None` and `pre_compiled_ref_patterns: list[tuple[re.Pattern, re.Pattern]] | None = None`. When None, fall back to the existing in-function compile (backward-compatible). This is byte-identical (same patterns, same substitutions), no marathon core touched, no paid API. The dry_run path at lines 2768-2774 gets the same treatment.\n\n**Severity calibration:** Not `medium` as filed. The `disabled_kinds` part is already mitigated by Python's internal cache. The `disabled_html_ref_ids` part is real but only triggers for editions that use tradition-filtering, and the dominant runtime cost is the regex matching/substitution over large HTML files rather than the compilation itself. Correct severity is `low` — a worthwhile cleanup that delivers a measurable but not dramatic win (likely 1-5s per heavy edition, not the claimed 5-20s)."
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "low",
   "title": "Double corpus walk for tradition-filter + tradition-label map when traditions_default is set",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "2647-2661",
   "evidence": "`disabled_html_ref_ids |= compute_tradition_disabled_html_ref_ids(edition)` (line 2647) calls `_iter_note_ref_traditions()` which iterates all 67,715 notes. Then `ref_id_to_tradition = build_ref_id_to_tradition_map(edition)` (line 2661) calls `_iter_note_ref_traditions()` again — an identical second full walk. The two functions are logically complementary (one collects disabled refs, the other the surviving refs) and both iterate the same 87 note files.",
   "fix": "Merge the two passes into one helper `_compute_tradition_maps(edition) -> tuple[set[str], dict[str, str]]` that returns `(disabled_ref_ids, ref_id_to_tradition)` in a single walk. Notes are LRU-cached per mtime so the file I/O cost is paid once, but the Python-level tuple iteration of 67,715 notes still runs twice currently. The merged helper eliminates the duplicate iteration. Guarded by the same early-return condition (no `traditions_default` and no `traditions_per_book` → return empty set + empty dict). Byte-identical output.",
   "verifierFix": "Merge the two passes into a single private helper `_compute_tradition_maps(edition: dict) -> tuple[set[str], dict[str, str]]` in `YHWH v2.4/scripts/build_edition.py`. The helper runs the early-exit guard once (`not has_default and not per_book → return set(), {}`), maintains one `book_active_cache`, walks `_iter_note_ref_traditions()` exactly once, and in the same loop body populates both `disabled: set[str]` (when `tradition not in active`) and `surviving: dict[str, str]` (when `tradition in active`). Replace the two call sites at lines 2647 and 2661 with a single unpacking call. `compute_tradition_disabled_html_ref_ids` and `build_ref_id_to_tradition_map` can be kept as thin public wrappers that delegate to the helper (for test-isolation / backwards compat), or inlined. Output is byte-identical. No marathon-core files touched.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "The double walk is confirmed real by reading the code. Both `compute_tradition_disabled_html_ref_ids` (line 277) and `build_ref_id_to_tradition_map` (line 396) call `_iter_note_ref_traditions()`, which itself re-enters the same 87-file corpus loop each time. The `load_notes` I/O is LRU-cached (keyed by path + mtime_ns per `notes_io.py:194-195`), so disk I/O is paid once, but the Python-level tuple iteration over the 67,715 notes runs twice in the active-tradition-filter path. The two functions are structurally symmetric: same early-exit guard (`not has_default and not per_book`), same `book_active_cache` pattern, same `_resolve_traditions_for_book` calls — the only difference is one collects the disabled set (tradition NOT in active) and the other collects the surviving map (tradition IN active). A merged helper is straightforwardly correct and byte-stable. However, the actual cost is minor: iterating ~67k Python tuples twice adds at most a few milliseconds to a build that takes many seconds. This is a genuine (if small) redundancy in code structure, not a material performance bottleneck. The finding is not refuted — the double walk is real and the proposed merge is valid — but the severity is appropriately low: the code comment at line 314 in `_iter_note_ref_attribution_years` even acknowledges the concern for a different pair of walkers, showing the author was aware of this pattern. The fix is clean: one `_compute_tradition_maps(edition) -> tuple[set[str], dict[str, str]]` that iterates once and returns both artifacts, delegating to the two existing functions or replacing their bodies directly, with the same early-exit guard."
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "info",
   "title": "BUILD OPTIMIZATION — BETTER PLAN: compresslevel 6 is the highest-value single change; pre-compiled regexes are safe quick wins",
   "file": "YHWH v2.4/scripts/build_epub.py",
   "line": "156",
   "evidence": "Line 156: `compresslevel=9` in `build_epub.build()`. The zip step compresses a 23MB uncompressed EPUB tree. At level 9 (maximum), compression is CPU-bound and slow. Level 6 is the standard deflate trade-off (~30-50% faster at ~1-3% larger output). Profiling reference: Python's zipfile benchmarks show level-9 is 2-3× slower than level-6 for HTML/text-heavy archives. At 133s/edition, and with the zip step being a major fraction, level-6 is estimated to save 20-40s per edition.",
   "fix": "Change `compresslevel=9` to `compresslevel=6` in `scripts/build_epub.py` line 156. BYTE-STABILITY PROOF OBLIGATION (per project RULES): (1) run `ebible build --all` before the change, record all 9 EPUB SHA-256 hashes; (2) run `cache_clear()`; (3) apply the change; (4) run `ebible build --all`; (5) unzip both sets and `diff -r` the unzipped contents — they must be bit-for-bit identical. The EPUB-level hashes WILL differ (compressed bytes changed), which is expected and acceptable. The existing `exports/.cache/` entries are invalidated by the changed hash and must be cleared. Add `cache_clear()` to the migration note. The 9 KJV byte-stability invariant refers to corpus content, not compression bytes — confirmed by RULES wording 'zero output change ... regen+git diff' which is an unzipped content diff, not EPUB-level.",
   "verifierFix": "Change `compresslevel=9` to `compresslevel=6` at `YHWH v2.4/scripts/build_epub.py:156`. After the change, clear `exports/.cache/` (or run `scripts/preflight.py --cache-clear`) so no stale level-9 EPUB is served from the content-addressable cache under its now-wrong entry. The `test_byte_stability_gate.py` suite requires no update — the digest is over decompressed inner content, which is unchanged.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently verified: `compresslevel=9` is confirmed at `YHWH v2.4/scripts/build_epub.py:156` — the only compression call site. The byte-stability invariant is safe to change: the test in `test_byte_stability_gate.py:50-66` computes its digest from the decompressed inner file content (`z.read(name)`), not from compressed EPUB bytes, so compression level is completely invisible to it. No golden hash of the raw EPUB bytes exists anywhere. The `compute_cache_key` in `scripts/core/build_cache.py:139` hashes `scripts/build_edition.py` as an input but does NOT include `scripts/build_epub.py` — so existing cache entries will have stale keys that produce level-9 EPUBs; a one-time `exports/.cache/` clear after the change is the only required migration step, exactly as the finder specified. The directional performance claim (level 6 is substantially faster than level 9 for HTML/text archives while producing only marginally larger output) is sound Python/zlib behavior and requires no measurement to accept as directionally correct. The proposed fix is concrete, feasible, requires no external API, does not touch the marathon core, and does not break the content-level byte-stability invariant. The cache invalidation note in the fix is correct and complete. Severity is low (a worthwhile but non-blocking optimization)."
    }
   ]
  },
  {
   "dimension": "opt-build",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "info",
   "title": "TODO_CERTIFIER_NAME ships verbatim in every built EPUB's OPF accessibility metadata",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "1265",
   "evidence": "`'    <meta property=\"a11y:certifiedBy\">TODO_CERTIFIER_NAME</meta>\\n'` — this placeholder string is emitted into `content.opf` of every built EPUB. EPUB 3 accessibility spec and epubcheck ACC-004 treat `a11y:certifiedBy` as an advisory (not a hard fail), but the literal string `TODO_CERTIFIER_NAME` is visible to any reader software that surfaces accessibility metadata and would appear in any distribution channel inspection.",
   "fix": "Replace the placeholder with the actual publisher name. Since this is a free public platform, use the project name: `'    <meta property=\"a11y:certifiedBy\">YHWH v2.4 Project</meta>\\n'`. Alternatively, resolve it from `pub['publisher_name']` which is already in scope at the call site (line 1122): `f'    <meta property=\"a11y:certifiedBy\">{_xml_escape(pub[\"publisher_name\"])}</meta>\\n'`. This makes it edition-specific and requires no schema change.",
   "verifierFix": "At `YHWH v2.4/scripts/build_edition.py:1265`, replace:\n\n`'    <meta property=\"a11y:certifiedBy\">TODO_CERTIFIER_NAME</meta>\\n'`\n\nwith:\n\n`f'    <meta property=\"a11y:certifiedBy\">{_xml_escape(pub[\"publisher_name\"])}</meta>\\n'`\n\nThis reuses `pub` (already resolved at line 1122) and the existing `_xml_escape` helper (already used on the lines immediately above), making the certifier name edition-specific and consistent with the publisher displayed in `<dc:publisher>`. No schema change, no byte-stability impact, no new dependency.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Independently confirmed by reading the code:\n\n1. `YHWH v2.4/scripts/build_edition.py:1265` contains the literal string `'    <meta property=\"a11y:certifiedBy\">TODO_CERTIFIER_NAME</meta>\\n'` — exactly as claimed.\n\n2. `pub = _resolve_publishing(edition)` is called at line 1122, and `pub['publisher_name']` is actively used at line 1134 in the same function to replace `<dc:publisher>`. The variable is unambiguously in scope at line 1265.\n\n3. `_resolve_publishing` (in `scripts/epub_utils.py:23-47`) always returns a non-empty `publisher_name`, defaulting to `\"Independent\"` when the edition has none set. So `pub['publisher_name']` is guaranteed non-None.\n\n4. No byte-stability test anywhere (`Grep` confirmed) asserts on the literal `TODO_CERTIFIER_NAME` string, so replacing it with a dynamic value does NOT break the byte-stability gate — the gate protects note injection and schema outputs, not this metadata string.\n\n5. The fix is purely cosmetic on the build side: one f-string substitution, no schema change, no new dependency, no marathon-core touch. Using `_xml_escape(pub['publisher_name'])` is strictly better than the hardcoded placeholder and is already the established pattern in this exact function.\n\nThe proposed fix from the finder (`f'    <meta property=\"a11y:certifiedBy\">{_xml_escape(pub[\"publisher_name\"])}</meta>\\n'`) is correct, feasible, and superior to the current state. Severity is `low` (not info) because the placeholder ships verbatim into every EPUB output file and would be visible to any tool that inspects accessibility metadata — it is a concrete quality defect even though it is not a hard epubcheck fail."
    }
   ]
  },
  {
   "dimension": "opt-ingest",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "high",
   "title": "All 9 run_*_at_scale write_queue functions use bare write_text — half-written candidate JSON on crash silently drops prior candidates",
   "file": "YHWH v2.4/scripts/run_xref_at_scale.py",
   "line": "66",
   "evidence": "`out_path.write_text(json.dumps(payload, ...), encoding='utf-8')` — identical in run_xref (L66), run_naves (L67), run_torrey (L65), run_hebrew (L61), run_greek (L60), run_ethiopian (L76), run_kenyon (L95), run_ai_xrefs (L112), run_ai_notes (L110), prospect.py (L152). Each does a read-merge-write cycle. On a kill/crash mid-write the JSON is half-written; the next at-scale run's `except Exception: existing = []` silently discards all prior promoted/pending candidates from that chapter file.",
   "fix": "In `at_scale_base.py` add `write_queue_atomic(path, payload)` that writes via `atomic_write` from `scripts.core.notes_io` (same tmp+rename dance already used for notes). Replace every `out_path.write_text(...)` in all 9 drivers + `prospect.py` with this helper. Candidate files are the staging area for human-reviewed promotion decisions; they warrant the same crash safety as notes files.",
   "verifierFix": "In each of the 10 files (run_xref_at_scale.py, run_naves_at_scale.py, run_torrey_at_scale.py, run_hebrew_at_scale.py, run_greek_at_scale.py, run_ethiopian_at_scale.py, run_kenyon_at_scale.py, run_ai_xrefs_at_scale.py, run_ai_notes_at_scale.py, prospect.py), add `from scripts.core.notes_io import atomic_write` to the imports, then replace the single `out_path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding=\"utf-8\")` line with `atomic_write(out_path, json.dumps(payload, indent=2, ensure_ascii=False))`. Do NOT add a centralized `write_queue_atomic` to at_scale_base.py — that module is documented as a dependency-free leaf (imports nothing from scripts) and the decentralized write_queue design is intentional. The notes_io._invalidate_corpus_index_if_notes_file hook is a no-op for .json files so there is no side-effect.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Read all 9 driver files plus prospect.py and notes_io.py directly. The bare `write_text` calls are confirmed at: run_xref_at_scale.py:66, run_naves_at_scale.py:67, run_torrey_at_scale.py:65, run_hebrew_at_scale.py:61, run_greek_at_scale.py:60, run_ethiopian_at_scale.py:76, run_kenyon_at_scale.py:95, run_ai_xrefs_at_scale.py:112, run_ai_notes_at_scale.py:110, prospect.py:152. The atomicity gap is real: a crash mid-write leaves a half-written file, and every driver's except-fallback resets existing to [], discarding any previously accumulated candidates. `notes_io.atomic_write` (tmp+rename via os.replace) is already present and importable; its `_invalidate_corpus_index_if_notes_file` hook is a no-op for .json files. However, the finder's \"high\" severity is overstated for three reasons: (1) these are candidate staging files that are fully regenerable by re-running any driver — unlike the promoted .py notes files which are the irreplaceable corpus; (2) single-user local batch tool on Windows, write completes in milliseconds, crash window is narrow; (3) at_scale_base.py's own docstring (L8-11) explicitly documents that write_queue is intentionally NOT centralized — the proposed helper-in-base-module approach cuts against a deliberate design decision. The correct fix is simpler: replace the 10 bare `write_text(json.dumps(...))` calls in-place with `atomic_write(out_path, json.dumps(...))` (importing from `scripts.core.notes_io`), with no change to the decentralized write_queue architecture. The change is additive, does not affect the 9-edition build pipeline or any .py notes files, and imposes zero behavioral difference beyond the atomicity guarantee."
    }
   ]
  },
  {
   "dimension": "opt-ingest",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "batch_promote_xrefs.py default mode (without --by-book): calls promote_candidate + update_queue_status per candidate — O(n²) writes on large candidate sets",
   "file": "YHWH v2.4/scripts/batch_promote_xrefs.py",
   "line": "119-155",
   "evidence": "The default loop at L139-155 calls `promote_candidate(book, c)` for each candidate; `promote_candidate` calls `insert_note_into_book_file` which does a full `read+ast.parse+write` of the book file. For N candidates in the same book this is O(N × filesize). A full corpus batch (67k+ candidates, 88 book files) would trigger thousands of full-file rewrites. The `--by-book` fast path exists (L104) but is NOT the default and is undocumented in the primary usage comment.",
   "fix": "Make `--by-book` the default (flip `args.by_book` to default True, rename old default to `--per-candidate` for the rare interactive use case). Update the docstring usage examples to show `--by-book` as the standard invocation. This is safe because `batch_insert_notes` has the same dedup + coord-guard logic.",
   "verifierFix": "In `YHWH v2.4/scripts/batch_promote_xrefs.py`: change the `--by-book` argument (L94-98) from `action=\"store_true\"` to `action=\"store_false\"`, rename it `--per-candidate`, and add a `--by-book` flag that is simply the default (or flip the logic: add `args.per_candidate` boolean and default to the `promote_by_book` path). The simplest concrete change:\n\nReplace the `--by-book` argument block with:\n```python\np.add_argument(\n    \"--per-candidate\",\n    action=\"store_true\",\n    help=\"slow path: one file write per candidate (default before mint-8; use for debugging only)\",\n)\n```\n\nThen at L104 change `if args.by_book:` to `if not args.per_candidate:`.\n\nAlso update the module docstring (L8-9) to show the standard invocation without any flag, and note that `--per-candidate` is the legacy/debug path. No other files need changing.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Confirmed by reading `YHWH v2.4/scripts/batch_promote_xrefs.py` and `YHWH v2.4/scripts/promote.py` directly.\n\nThe default path (L119-155 in batch_promote_xrefs.py): `args.by_book` is set via `action=\"store_true\"` with no `default=True` (L95-98), so it defaults to False. The per-candidate loop at L139-155 calls `promote_candidate(book, c)` for each candidate. `promote_candidate` (promote.py L381) calls `insert_note_into_book_file` (promote.py L184), which does `book_path.read_text(...)` at L203 and `atomic_write(book_path, new_text)` at L273 — one full file read+parse+write per candidate.\n\n`batch_insert_notes` (promote.py L277) is explicitly documented: \"Efficient alternative to calling `insert_note_into_book_file` per note (which is O(filesize) each → O(n²) for a bulk promote).\" It does a single read+write per book.\n\nThe `--by-book` path at L104 calls `promote_by_book` which uses `batch_insert_notes` with `skip_existing=True` (L80) — same dedup and coord-guard logic, O(books) writes.\n\nOne part of the finder's evidence is wrong: the claim that the default loop calls `update_queue_status` per candidate. `batch_promote_xrefs.py` does not call `update_queue_status` at all (confirmed by grep — zero matches). But this is immaterial to the optimization finding; the O(N) write problem is real regardless.\n\nThe proposed fix — making `--by-book` the default — is safe: no marathon core files touched, no byte-stability risk (ingest path only, not the build/epub path), no paid API, same functional result. The `promote_by_book` function already exists and is tested. Renaming the old default to `--per-candidate` is a minor usability improvement for interactive/debug use cases."
    }
   ]
  },
  {
   "dimension": "opt-ingest",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "low",
   "title": "insert_note_into_book_file position-finding loop uses break-on-first-descending — silent mis-insertion if NOTES list is unsorted",
   "file": "YHWH v2.4/scripts/promote.py",
   "line": "246-249",
   "evidence": "```python\nif existing_key < new_key:\n    insert_after_lineno = tup.end_lineno\nelse:\n    break\n```\nThe `break` assumes the NOTES list is sorted by (ch, v, suffix). If a legacy or manually-edited file has any out-of-order tuple, the loop stops early and the new note is inserted at the wrong position. The same pattern exists in `batch_insert_notes` L339-343. The `ensure_backup` + `ast.parse` sanity check catches a malformed file but not a sort-order violation.",
   "fix": "Add a pre-flight sort check (or assert) at the top of both functions: `if not all(existing[i] <= existing[i+1] for i in range(len(existing)-1)): logging.warning('NOTES list in %s appears unsorted — insertion position may be wrong', book_path)`. This is a diagnostic guard, not a crash, since fixing sort order requires a separate migration. The real defense is `validate_taxonomy.py` — add a sort-order check there.",
   "verifierFix": "In both functions, replace the `else: break` with a full scan. In insert_note_into_book_file (promote.py ~line 246-249):\n\n  if existing_key < new_key:\n      insert_after_lineno = tup.end_lineno\n  # remove the else: break — scan all tuples regardless\n\nIn batch_insert_notes (promote.py ~line 340-343):\n\n  if (ech, ev, suffix_rank(esuf)) < new_key:\n      after = eend\n  # remove the else: break\n\nAdditionally, add a sort key to write_book() in web_helpers.py before serializing so future web-UI writes maintain sorted order:\n\n  for tup in sorted(notes, key=lambda t: (t[0], t[1], (0,\"\") if t[2]==\"\" else (1,t[2]))):\n\nThis is safe, cheap (O(n log n) over a list that is already nearly sorted in practice), and does not affect byte stability of any edition build output since load_notes reads all tuples regardless of file order.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read the actual code. The finding is real and the evidence is accurate, but the proposed fix is weaker than necessary.\n\nThe break at promote.py:249 (and the mirror at line 343 in batch_insert_notes) is a genuine latent defect, not a pure style/optimization matter. The loop assumes the NOTES list is sorted by (ch, v, suffix_rank). If any tuple out of order exists, the break fires early, and insert_after_lineno freezes at the last \"less-than\" position before the out-of-order entry — the new note is inserted at the wrong file position.\n\nUnsorted files are a realistic outcome: web_helpers.py:150 write_book() serializes whatever list order it receives with no sort, and web_notes.py:142 api_add appends (notes.append(new_tup)) and web_notes.py:163 api_delete pops-then-rewrites — neither caller sorts before calling write_book. So a user adding a note for Gen 3:5 via the web UI after Gen 10:1 already exists leaves the file with Gen 10:1 before Gen 3:5, and the next promote.py call will break early at Gen 10:1 and misplace any new key between Gen 3:5 and Gen 10:1.\n\nThe build pipeline (load_notes) reads NOTES as a flat AST tuple list in file order and does not require sort order, so mis-insertion does NOT corrupt build output or byte stability — it only scrambles the file's cosmetic sort order. No data is lost.\n\nThe proposed fix (a warning log) is strictly weaker than simply removing the break, which makes the loop a full linear scan and is correct regardless of file sort order. The warning approach only notifies without fixing the insertion. The real, minimal fix is:\n\n1. In insert_note_into_book_file: replace `else: break` with just `pass` (or remove the else clause entirely) so insert_after_lineno always ends up as the last tuple that sorts below new_key, scanning the full list.\n\n2. Same for the batch_insert_notes inner loop at line 343.\n\n3. Optionally add a sort to write_book() in web_helpers.py to keep files canonical going forward.\n\nSeverity: low (not high). Mis-insertion is a file cosmetic issue — notes appear out of order in the .py source but the build is unaffected. No correctness or byte-stability risk to the 9 editions."
    }
   ]
  },
  {
   "dimension": "opt-render",
   "kind": "optimization",
   "severity": "low",
   "originalSeverity": "medium",
   "title": "is_output_current mtime cache ignores content/notes/*.py — corpus edits silently produce stale builds",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "line": "1943-1965",
   "evidence": "is_output_current watches `epub_working/*.html`, `content.opf`, `nav.xhtml`, `stylesheet.css`, `editions.yaml`, `build_edition.py` — but NOT `content/notes/*.py`. The notes corpus is the primary per-edition variable: notes are injected into epub_working at inject time (pre-build), so they ARE baked into the base HTML. However editions.yaml edits (e.g. enabling a new kind) combined with an unchanged note corpus CAN legitimately produce a different EPUB. More critically: if the user edits a note after the last inject, then rebuilds without re-injecting, `is_output_current` returns the old EPUB as current. In a normal workflow (inject → build → edit note → build again without inject) the user gets the pre-edit EPUB. The content-addressable cache (`build_cache.compute_cache_key`) at lines 2723-2754 does NOT fall through to the mtime check when `cache_key` is None — the mtime check remains the sole fast path in that case.",
   "fix": "Add content/notes/*.py mtime to the sources list. Simplest surgical fix: after line 1961 add `notes_dir = REPO_ROOT / 'content' / 'notes'; sources.extend(notes_dir.glob('*.py'))`. This makes the check correctly skip the cache after any corpus edit. Alternatively, remove is_output_current entirely now that the content-addressable cache (Phase ω.20-B) supersedes it — the two paths compose correctly and the mtime check is only a fallback when cache_key is None.",
   "verifierFix": "The content-addressable cache (`build_cache.compute_cache_key`) already hashes every in-canon `content/notes/<book>.py` at step 5, so the primary fast path is correct. The gap exists only in the `is_output_current` fallback, which runs only when `cache_key is None` (exception path). The proposed fix is valid for hardening that fallback: in `YHWH v2.4/scripts/build_edition.py` line 1961, after `sources.append(REPO_ROOT / \"scripts\" / \"build_edition.py\")`, add:\n\n    notes_dir = REPO_ROOT / \"content\" / \"notes\"\n    if notes_dir.is_dir():\n        sources.extend(notes_dir.glob(\"*.py\"))\n\nDo NOT remove `is_output_current` entirely — it remains a useful fallback when the cache directory is absent or cleared. Note that `is_output_current` also misses `canons.yaml`, `kinds.yaml`, `categories.yaml`, and `books.yaml`; those could be added to the same sources list for completeness, but are equally low-priority since the content-addressable cache covers all of them too.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read `YHWH v2.4/scripts/build_edition.py` lines 1943-1965 and 2717-2764, and `YHWH v2.4/scripts/core/build_cache.py` in full.\n\nWhat the code actually does:\n\n1. The content-addressable cache (`compute_cache_key`, build_cache.py lines 180-187) explicitly hashes every in-canon `content/notes/<book>.py` file (step 5 of 10). So the primary fast path is already correct: a notes edit produces a different SHA-256 key → cache miss → rebuild. The finder's core alarm (\"notes edits produce stale builds\") is therefore FALSE for the normal case.\n\n2. `is_output_current` is called ONLY when `cache_key is None` (build_edition.py line 2741 vs 2757-2758). `cache_key` is None only when `compute_cache_key` raises an exception that is silently swallowed (import failure, unknown edition_id, non-JSON-serializable field). In normal operation with a known edition, `cache_key` is always a hex string, never None.\n\n3. The finding IS technically real in the exception/fallback path: if `build_cache` fails to load, `is_output_current` becomes the sole fast path and it does not watch `content/notes/*.py`. In that degenerate case a notes edit would not be detected. However, in the same degenerate case `is_output_current` also misses `content/canons.yaml`, `content/kinds.yaml`, `content/categories.yaml`, `content/books.yaml`, and every epub_working subdirectory — so the notes gap is one of several untracked inputs, not a unique omission.\n\n4. The proposed fix (extend the `sources` list in `is_output_current`) is correct and safe. It would glob all `*.py` files in `content/notes/` and add them to the mtime comparison, tightening the fallback path at negligible cost. It does not touch the marathon core, does not alter byte output, and does not require an API. The alternative (remove `is_output_current` entirely) is more invasive and the function does serve as a useful fallback when the cache dir is missing/cleared.\n\nVerdict: The finding is real but its framing is misleading. The primary fast path (content-addressable cache) already includes notes hashes — no stale-build risk exists in normal operation. The gap lives only in an error-recovery fallback path that is secondary to the working cache. Severity is low (not medium): affects only the `cache_key=None` degraded path, is one of several equally-untracked inputs on that same path, and the proposed fix is a small defensive addition rather than a critical repair."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "info",
   "originalSeverity": "medium",
   "title": "REPO_MAP scripts/api/ count is 17 but 18 files exist; scripts/templates/ count is 20 but 21 exist",
   "file": "YHWH v2.4/dev/REPO_MAP.md",
   "line": "42-43",
   "evidence": "REPO_MAP line 42: `scripts/api/ (17)` — grep of `scripts/api/*.py` returns 18 files. REPO_MAP line 43: `scripts/templates/ (20)` — grep of `scripts/templates/*.py` returns 21 files. Both counts are off by 1, consistent with one new file added per directory after the REPO_MAP was last written (likely `api/distribution.py` and `templates/distribution.py` added in mint-6 for the `/distribution` console).",
   "fix": "Update REPO_MAP line 42 from `(17)` to `(18)` and line 43 from `(20)` to `(21)`.",
   "verifierFix": "",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Verified by direct file inspection. Glob of `scripts/api/*.py` returns exactly 18 files (including `distribution.py`); Glob of `scripts/templates/*.py` returns exactly 21 files (including `distribution.py`). REPO_MAP.md line 42 says `(17)` and line 43 says `(20)` — both off by 1, consistent with `distribution.py` being added to each directory during mint-6. The finding is factually correct. However, `dev/trace_repo.py` (the anti-rot guard for REPO_MAP) only checks whether top-level *directory names* appear in the REPO_MAP text — it does not parse or validate numeric counts. So no guard fails, no build is affected, no shipped output is corrupted, and the 9 KJV editions are unaffected. This is a stale documentation count with zero runtime blast radius. The proposed fix (update `(17)` → `(18)` and `(20)` → `(21)` in REPO_MAP.md) is correct and safe."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "info",
   "originalSeverity": "medium",
   "title": "REPO_MAP tests count is 168 but 169 test_*.py files exist",
   "file": "YHWH v2.4/dev/REPO_MAP.md",
   "line": "17",
   "evidence": "`168 pytest files (test_*.py)` — grep of `tests/test_*.py` returns 169 files. The count drifted by 1, consistent with `test_lint_rules.py` being extracted from `test_scripts.py` during mint-7 Phase E without REPO_MAP being updated.",
   "fix": "Update REPO_MAP line 17 from `168 pytest files` to `169 pytest files`.",
   "verifierFix": "In `YHWH v2.4/dev/REPO_MAP.md` line 17, change `168 pytest files` to `169 pytest files`. No other changes needed.",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Directly verified: REPO_MAP.md line 17 reads `168 pytest files (test_*.py)`. A grep with output_mode=count over `tests/test_*.py` confirmed `Found 81695 total occurrences across 169 files` — the tool itself reports the exact file count as 169. The finding's claimed discrepancy of exactly 1 is confirmed. The proposed fix (change \"168\" to \"169\") is correct and safe: it is a doc-only edit to `dev/REPO_MAP.md`, touching no code, no build pipeline, no marathon core, and zero KJV byte-stability surface. The finder's attribution to `test_lint_rules.py` being extracted during mint-7 is plausible (that file exists and is separate from `test_scripts.py`), but immaterial to the fix. Severity is downgraded to `info`: this is a pure documentation counter drift with no runtime, test, or build consequence — the REPO_MAP is an orientation aid, not an enforcement artifact."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "info",
   "originalSeverity": "low",
   "title": "lint_rules.py check_superpowers_coherence docstring says 'mint-6 backfilled all 39'; count is now 41",
   "file": "YHWH v2.4/scripts/lint_rules.py",
   "line": "1357",
   "evidence": "Function docstring: `mint-6 backfilled all 39; _ENFORCE_SUPERPOWERS_COHERENCE makes drift a hard FAIL.` The superpowers corpus now has 41 files (25 plans + 16 specs per INDEX.md, verified by glob). The check itself is dynamic (`len(files)`) so it functions correctly; only the inline comment count is wrong.",
   "fix": "Update the docstring count from `39` to `41` on line 1357.",
   "verifierFix": "",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "I read `YHWH v2.4/scripts/lint_rules.py` lines 1351–1357. The docstring at line 1357 states \"mint-6 backfilled all 39\". I then read `YHWH v2.4/docs/superpowers/INDEX.md` line 7, which states \"**41 documents** — 25 plans · 16 specs\". The count discrepancy is confirmed: the docstring says 39, the actual corpus is 41. The check itself (line 1359: `files = sorted(base.glob(\"plans/*.md\")) + sorted(base.glob(\"specs/*.md\"))`) is dynamic and unaffected — it counts whatever is present at runtime. The defect is confined to the docstring comment only; no shipped output, no KJV byte-stability, and no runtime behavior is impacted. Severity is correctly characterised as low/informational. The proposed fix (update \"39\" to \"41\" in the docstring) is correct and safe — it is a one-word change inside a docstring string literal, touching no logic, no marathon core, and no schema."
    }
   ]
  },
  {
   "dimension": "docs",
   "kind": "find",
   "severity": "info",
   "originalSeverity": "low",
   "title": "REPO_MAP dev/archive section lists only PLAN_2026-05-07/08/09 as archived plans; two more exist",
   "file": "YHWH v2.4/dev/REPO_MAP.md",
   "line": "51",
   "evidence": "`superseded plans (PLAN_2026-05-07/08/09)` — the `dev/archive/` directory also contains `PLAN_2026-05-21.md` and `PLAN_2026-05-24-end-scope.md` (confirmed by glob).",
   "fix": "Update the REPO_MAP line 51 enumeration to `superseded plans (PLAN_2026-05-07/08/09 + PLAN_2026-05-21 + PLAN_2026-05-24-end-scope)`.",
   "verifierFix": "Update YHWH v2.4/dev/REPO_MAP.md line 51 to: `superseded plans (PLAN_2026-05-07/08/09 + PLAN_2026-05-21 + PLAN_2026-05-24-end-scope) + ship_scripts/ (21 one-shot ship scripts) + old handoffs.`",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Read REPO_MAP.md line 51 directly. The text reads: `superseded plans (PLAN_2026-05-07/08/09) + ship_scripts/ (21 one-shot ship scripts) + old handoffs`. Glob of dev/archive/ confirms both PLAN_2026-05-21.md and PLAN_2026-05-24-end-scope.md exist there. The enumeration on that line specifically names plans by date, so the omission of two later archived plans is a real gap in the index. No build, byte stability, or test impact — pure docs defect. The proposed fix (adding the two missing plan names to the enumeration) is correct and safe."
    }
   ]
  },
  {
   "dimension": "cross-module",
   "kind": "find",
   "severity": "info",
   "originalSeverity": "low",
   "title": "bookcode_canonical lint does not screen CCEL_ABBREV or EASTON_BOOK — coverage gap for future ingest-script edits",
   "file": "YHWH v2.4/scripts/lint_rules.py",
   "line": "1995-2003",
   "evidence": "`check_book_codes_canonical` at lines 1995-2003 screens six map_specs plus one list_spec. `scripts.extract_naves_ccel.CCEL_ABBREV` and `scripts.extract_eastons_ccel.EASTON_BOOK` are not in the list. Both currently contain only canonical values (verified: `\"Jas\": \"jam\"`, `\"Php\": \"phi\"`, `\"Mr\": \"mrk\"`, `\"Joh\": \"jhn\"` all resolve to canonical), but the memory note `feedback_book_code_canonical` specifically records that this class of defect 'recurs every ingest' — meaning the guard was added precisely because new maps are created at ingest time. An unguarded map is one edit away from re-introducing the bug silently.",
   "fix": "Add both maps to `map_specs` in `check_book_codes_canonical`:\n\n```python\nmap_specs = [\n    (\"scripts.core.sources\", \"KENYON_BOOK_NAME_TO_CODE\"),\n    (\"scripts.fetch_sources\", \"TSK_BOOK_REMAP\"),\n    (\"scripts.fetch_sources\", \"NAVES_BOOK_REMAP\"),\n    (\"scripts.core.sources_base\", \"_BOOK_CODE_ALIASES\"),\n    (\"scripts.extract_torrey_ccel\", \"_LEGACY_TO_CANON\"),\n    (\"scripts.link_xrefs\", \"ABBREV\"),\n    (\"scripts.extract_naves_ccel\", \"CCEL_ABBREV\"),    # add\n    (\"scripts.extract_eastons_ccel\", \"EASTON_BOOK\"),  # add\n]\n```\n\nBoth modules are already importable (they use only stdlib + project deps). The current values will pass clean; the guard prevents future edits from drifting silently.",
   "verifierFix": "",
   "panel": [
    {
     "refuted": false,
     "confidence": "high",
     "reasoning": "Verified by reading the code directly.\n\n(a) The code does exactly what the evidence claims. `check_book_codes_canonical` at lines 1995-2003 of `YHWH v2.4/scripts/lint_rules.py` has a `map_specs` list of 6 entries. `scripts.extract_naves_ccel.CCEL_ABBREV` and `scripts.extract_eastons_ccel.EASTON_BOOK` are absent from it.\n\n(b) This is a genuine coverage gap, not intended behavior or a de-scoped item. The guard's own docstring at line 1966-1975 explicitly cites `feedback_book_code_canonical` and states this \"recurs every ingest\" — the guard exists precisely to catch future edits to these kinds of maps. Both `CCEL_ABBREV` (lines 39-106 of `extract_naves_ccel.py`) and `EASTON_BOOK` (lines 36-106 of `extract_eastons_ccel.py`) are active ingest maps that feed into the notes pipeline. They are currently clean — all values are canonical codes; none of the values match the legacy set `{joh, ps, jas, mar, jol, ezk, nam, php}`. But the guard provides no future protection against an edit that introduces a legacy value into either map.\n\n(c) Severity recalibration: the current state has no active defect; no shipped output is corrupted and the 9 KJV editions are byte-stable. The risk is purely prospective — a future ingest-script edit could silently route notes to non-existent files, causing silent drops at `promote`. This bounded blast radius (no crash, no KJV corruption) and prospective-only nature makes this `info`/`low`, not `medium` or higher. The finder's `low` is already conservative; `info` is arguably more accurate since there is zero active defect.\n\n(d) The proposed fix is correct and safe. Adding two entries to `map_specs` is purely additive: it adds protection without changing any output-producing code path, is not in the marathon core, and has no effect on build byte-stability. The current values in both maps will pass the check clean."
    }
   ]
  }
 ],
 "dropped": [
  {
   "dimension": "security",
   "severity": "medium",
   "title": "url_override in sources/cache/fetch bypasses the configured-source allowlist and can reach any http(s) host",
   "file": "YHWH v2.4/scripts/api/sources.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the full call chain directly.\n\n**What the code actually does:**\n\n1. `api_sources_cache_fetch` (sources.py:163-187) checks only `startswith((\"http://\", \"https://\"))` at the API boundary - confirmed by reading the file.\n\n2. `fetch_source` (fetch_sources.py:521-527) calls `parser(c.url)` where each parser (e.g. `_parse_strongs_hebrew_js` at line 83, `_parse_strongs_greek_js` at line 108) immediately calls `_http.get(url, allowlist=DEFAULT_PD_SOURCES_ALLOWLIST)`.\n\n3. `http.get()` (http.py:167) calls `_check_allowlist(url, allowlist)` unconditionally as its FIRST statement, before any network I/O. If the host is not in `DEFAULT_PD_SOURCES_ALLOWLIST` (openscriptures.org, ebible.org, archive.org, openbible.info), `SSRFBlockedError` is raised immediately. There is no code path through `get()` that bypasses this check.\n\n4. The `SSRFBlockedError` propagates up through the `except Exception as e` in `fetch_source`, which catches it and logs it as a candidate failure - no network I/O ever occurs.\n\n**Why the finding is refuted:**\n\nThe finder explicitly concedes \"the http.py SSRF guard DOES enforce the allowlist at the network layer.\" The finding then pivots to two secondary arguments:\n\n- *False sense of security at the API boundary*: This is a code-quality observation (defense-in-depth), not a defect. The allowlist enforcement is real and unconditional.\n- *Future refactors might add parsers without the allowlist*: This is purely speculative. Every current parser in fetch_sources.py uses `_http.get(..., allowlist=DEFAULT_PD_SOURCES_ALLOWLIST)`. The linter check `check_external_http` (documented in http.py's own docstring at line 12) is explicitly stated to \"fail on any direct `urllib.request.urlopen` call outside this file,\" providing an in-repo guard against exactly this scenario.\n\nAdditionally, per project facts, this is a single-user local desktop app. SSRF as a multi-tenant attack vector is explicitly out of scope.\n\n**On the proposed fix:**\n\nAdding `_check_allowlist` at the API gate is a valid defense-in-depth improvement but it is not fixing a current bug. The fix itself is safe (non-invasive, doesn't touch marathon core, doesn't affect KJV byte-stability), but it is correcting a speculative future risk, not a present exploitable defect.\n\nThe finding does not meet the \"refuted=false\" bar: there is no bypass of the actual network-layer SSRF guard in the current code."
  },
  {
   "dimension": "security",
   "severity": "low",
   "title": "TOTP provisioning_uri (containing the base32 secret) returned in plain-text JSON response without any transport-layer protection warning",
   "file": "YHWH v2.4/scripts/api/auth.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the full file at C:\\Users\\bogda\\Documents\\YHWH-v2.4-full\\YHWH v2.4\\scripts\\api\\auth.py and the relevant sections of scripts/web.py and scripts/core/audit_log.py.\n\nThe code does what the finding describes (returns secret + provisioning_uri at lines 99-107), but every element of the \"defect\" claim is either intentional, already guarded, or de-scoped:\n\n1. Cache-Control already set. The finder's proposed fix (a) — adding Cache-Control: no-store — is already implemented. _send_json at web.py:1178 unconditionally sets \"Cache-Control: no-store\" on every JSON response. This is not a gap.\n\n2. Audit log does not leak the secret. _summarize_args only logs the inbound payload dict. The return value (which contains secret + provisioning_uri) is never passed to _summarize_args. Additionally, even if the payload happened to contain sensitive keys, (a) dicts are summarized as {\"<dict>\": \"N keys\"} discarding all values, and (b) the _REDACT_KEYS allowlist masks any kwarg named \"secret\". Both defenses are belt-and-suspenders; the primary defense is that return values are never logged.\n\n3. The docstring explicitly documents the design intent. Lines 83-84 of auth.py read: \"the SECRET is returned in clear text to the trusted authenticated admin so they can type it into their app.\" This is not undocumented.\n\n4. The entire threat model is de-scoped by project facts. This is a single-user localhost desktop app. The project facts explicitly state: \"CSRF, rate-limiting, public-server / hosting hardening, multi-tenant auth\" are OUT OF SCOPE. The \"browser network tab\" attack requires physical access to the machine — the admin IS the sole user. No blast radius.\n\n5. The finding self-refutes on its own key claim. The finder writes \"(b) [Cache-Control: no-store] is already handled\" — acknowledging the primary mitigation is present. The remaining suggestion is documentation-only commentary (\"a comment clarifying this\"). That is a style nit, not a material defect.\n\nVerdict: the finding describes intended, correctly-implemented, already-documented behavior in a single-user localhost application. Severity = none."
  },
  {
   "dimension": "security",
   "severity": "low",
   "title": "api_download_export filename passed directly to Content-Disposition header without sanitization of the edition_id segment",
   "file": "YHWH v2.4/scripts/api/exports.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read all three relevant code sites directly.\n\n1. `build_edition.py` line 2663 uses `strftime(\"%Y-%m-%dT%H%M%SZ\")` which produces timestamps of the form `2026-05-31T123456Z` — dashes in the date part, no colons anywhere. The `All_Editions` zip in `exports.py` line 376 uses `strftime(\"%Y%m%dT%H%M%SZ\")` (no separators at all). Neither build path ever produces a filename containing a colon.\n\n2. The web route in `web.py` line 1570 captures filenames with `([\\w.-]+)` where `\\w` = `[a-zA-Z0-9_]` plus literal `.` and `-`. Colons are not in this character class. A URL with a colon in the filename segment would not match this route at all; the server would never dispatch to `api_download_export`.\n\n3. The `api_download_export` function receives its `filename` argument from `m.group(1)` of that route match, so it can only ever receive strings that passed the `[\\w.-]+` filter. The colon-permissive regex inside `api_download_export` (`[\\dT\\-:Z]+`) is therefore an unreachable branch for colon-containing strings — the outer route guard prevents them from ever reaching the function.\n\n4. The `Content-Disposition` header at `web.py` line 1581 uses `m.group(1)` (the route capture, not the `exports.py` internal match), so it is likewise gated by `[\\w.-]+` and cannot contain colons.\n\nThe finding correctly identifies that the allowlist regex in `exports.py` is over-permissive relative to actual build output, but the blast radius is zero: no colon-containing string can reach the header because (a) the build pipeline never writes such filenames and (b) the upstream web route regex is the actual gate and already excludes colons. This is a cosmetic allowlist inconsistency, not a security defect. Severity = none."
  },
  {
   "dimension": "code-debt",
   "severity": "medium",
   "title": "write_queue simple-append body duplicated identically across run_xref, run_naves, and run_torrey at-scale drivers",
   "file": "YHWH v2.4/scripts/run_naves_at_scale.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read all three cited files (run_xref_at_scale.py:39-67, run_naves_at_scale.py:39-68, run_torrey_at_scale.py:40-66) and confirmed the bodies are indeed identical. The finding is factually accurate as a code observation.\n\nHowever, I also read scripts/core/at_scale_base.py in full. Its module-level docstring — written during mint-7 D1 (the same cleanup arc that extracted candidate_to_dict) — explicitly states: \"write_queue is deliberately NOT here — each driver has its own append / dedup / overwrite semantics (the lone exception, run_xref's former clobber, was fixed in mint-7 B2).\" This is an active, reasoned design decision, not an oversight.\n\nThe rationale holds when you look at the filter-by-kind variants: run_hebrew, run_greek, and run_ai_xrefs each have a materially different write_queue that strips existing candidates of their own kind before appending, preventing stacking on re-runs. That semantic divergence is exactly what the docstring anticipates. The three \"simple-append\" drivers happen to be identical today, but the module owner already decided the correct boundary: keep write_queue local to each driver so semantics can evolve independently without touching a shared helper.\n\nThe proposed fix — extracting write_queue_append into at_scale_base.py — directly contradicts the documented design intent. It would also be additive-only code debt on top of an already-working system: no KJV edition is affected (these scripts generate pending candidates, not build output), no data corruption is possible, and the three bodies are easy to diff if they drift. The mint-7 B2 incident (run_xref clobbering) was detected and fixed in-session; the current identical state is a post-fix convergence, not a latent divergence waiting to happen.\n\nSeverity is effectively none as a material defect: this is a style/DRY observation on non-build, non-marathon code where the designer explicitly chose not to centralize. Refuted on the grounds that the \"defect\" is intentional, documented design."
  },
  {
   "dimension": "docs",
   "severity": "medium",
   "title": "scripts/core/matrix.py docstring note count '67,715 today' is a stale snapshot",
   "file": "YHWH v2.4/scripts/core/matrix.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read matrix.py lines 1-30 and confirmed line 13 says `content/notes/*.py — actual notes (67,715 today)`. I also read CLAUDE_PROJECT_RULES.md lines 145-159 in full. The \"do NOT hard-code a figure here — it rots\" prohibition at line 151 uses the word \"here\" in self-referential context: it is explicitly scoped to the corpus-depth paragraph inside CLAUDE_PROJECT_RULES.md itself, not a blanket project-wide rule against snapshot counts in docstrings. MATRIX_MAP.md carries the same 67,715 figure in three places (lines 17, 133, 171) but all are explicitly timestamped \"2026-05-21 after the reference-corpus rebuild\" — these are historical snapshot notes, not live claims. The matrix.py docstring word \"today\" is mildly dated phrasing but is purely informational; no pipeline logic, no test, and no shipped EPUB output reads or asserts this string. The 9 KJV editions remain byte-stable regardless. Blast radius = zero. The finding mischaracterizes the RULES §1 scope and elevates a cosmetic docstring comment to a policy violation that does not exist."
  },
  {
   "dimension": "byte-stability",
   "severity": "low",
   "title": "build_edition.build_one main filter loop (line 2846) and dry-run loop (line 2768) use unsorted glob — order is filesystem-dependent",
   "file": "YHWH v2.4/scripts/build_edition.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the actual code at all four cited locations.\n\nLines 2768 and 2846 do use unsorted `glob(\"*.html\")` — that part of the evidence is accurate. Lines 1574 and 1719 do use `sorted(...)` — the style inconsistency is real.\n\nHowever the claimed impact is not real. For the main build loop (line 2846): each iteration reads one HTML file, runs `filter_html` + `renumber_markers` + optional `_apply_popup_languages_and_translation` + `apply_tradition_labels_to_html` + `_retitle_html_pages` on that file's text, then writes it back. Every transformation is a pure function of (that file's text, edition config). There is no cross-file state that accumulates across iterations and feeds back into subsequent iterations — the only shared mutable object is the `stats` dict, which only has integer counters incremented by addition (commutative — final totals are identical regardless of order). For the dry-run loop (line 2768): same situation, only integer accumulation. No shipped HTML byte content is order-dependent.\n\nThe EPUB zip follows after all files are written; zip-internal ordering of content documents does not affect byte-stability of individual XHTML files and EPUB readers treat content documents as a set not an ordered list.\n\nThe inconsistency with sorted calls at 1574/1719 is a cosmetic style nit, not a defect. Byte-stability of the 9 KJV editions is not threatened. Severity = none (style inconsistency only, no material correctness impact)."
  },
  {
   "dimension": "opt-vision",
   "severity": "medium",
   "title": "corpus_index._build_to uses PRAGMA synchronous=OFF — crash during rebuild loses the old index",
   "file": "YHWH v2.4/scripts/core/corpus_index.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "Read lines 470-525 of YHWH v2.4/scripts/core/corpus_index.py directly. The finding's central claim — \"the current except block (not shown here but absent) should unlink .tmp if os.replace has not yet run\" — is factually wrong. The except block IS present at lines 518-524 and already does exactly what the proposed fix describes: conn.close() followed by tmp.unlink() (with OSError suppression). The .tmp file is cleaned up on failure.\n\nThe PRAGMA synchronous=OFF / MEMORY journal applies only to the .tmp file (line 473: sqlite3.connect(str(tmp))). A crash mid-build leaves .tmp in an incomplete state, but: (1) the production path is never touched until the single atomic tmp.replace(path) call on line 516; (2) on the next call, lines 471-472 delete any pre-existing .tmp before starting. The production corpus.sqlite is never left in a corrupt or missing state by this sequence.\n\nThe TOCTOU concern about concurrent rebuilds re-entering connection() is also moot: rebuild() operates under a rebuild lock (documented in its docstring at line 528), and this is an explicitly single-user local desktop app where the concurrency scenario described cannot arise in practice.\n\nThe proposed fix is already fully implemented. There is no material defect here."
  },
  {
   "dimension": "opt-vision",
   "severity": "low",
   "title": "BACKUP_FILENAME_RE timestamp slice uses wrong index offsets — IndexError on malformed timestamps, silently falls back to raw ts string",
   "file": "YHWH v2.4/scripts/web_content.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "The finding is refuted. The slicing at web_content.py:523-524 operates on `ts`, which is the named capture group `ts` from `_BACKUP_FILENAME_RE` (line 448). That regex pattern is `\\d{8}T\\d{6}Z` — eight digits, literal `T`, six digits, literal `Z` — which enforces exactly 16 characters and positions the `T` separator at index 8. A filename that does not match this regex is skipped by `if not m: continue` at lines 516-517 before `ts` is ever extracted. Therefore, by the time execution reaches line 523, `ts` is guaranteed to be a 16-character string conforming to the pattern, making every slice index valid. The `except IndexError` at line 525 is unreachable dead code, not a live fallback. The slicing arithmetic itself is correct: indices [0:4]=year, [4:6]=month, [6:8]=day, skip `T` at [8], [9:11]=hours, [11:13]=minutes, [13:15]=seconds, producing a valid ISO-8601 string. The proposed \"fix\" — adding an explicit `len(ts) >= 15 and ts[8] == 'T'` guard — would be strictly redundant to the regex pre-filter already in place. There is no bug here, no silent fallback on malformed input (malformed filenames are excluded by the regex), and the current approach is not sub-optimal in any material sense. The finding misses the regex gate that makes the IndexError path unreachable."
  },
  {
   "dimension": "opt-vision",
   "severity": "info",
   "title": "OPTIMIZATION RE-EVAL: Vision marathon method — CONFIRM-OPTIMAL for Patrologia Esther given today's constraints",
   "file": "YHWH v2.4/docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the plan file (`YHWH v2.4/docs/superpowers/plans/2026-05-28-geez-patrologia-vision-plan.md`), the `_vision_notes.md` accumulator (`YHWH v2.4/content/translations/sources/patrologia/_vision_notes.md`), and the concurrency cap memory (`feedback_concurrent_agent_cap.md`). Here is what I independently confirmed:\n\n**The proposed \"optimization\" directly violates a ratified, empirically-validated constraint.**\n\nThe concurrency cap memory (updated 2026-05-27, the most recent authoritative source) contains this explicit rule: \"STILL FORBIDDEN: a second IMAGE-reading agent concurrent with the heavy one (locating, or a C-4 CAM pull that reads tiles into context) — that re-creates the buffer-stacking that caused all 4 OOMs. Image work stays sequential / between heavy steps.\"\n\nThe finder proposes to run \"2 blind transcribers as concurrent medium agents (parallel reads of the same on-disk PNG).\" Both agents read images. This is precisely the forbidden pattern.\n\n**The finder's RAM reasoning is the same reasoning the project already refuted.** The finder argues \"2 × ~3 MB image buffer = ~6 MB, well within the 16 GB budget.\" But the concurrency cap memory explicitly explains why raw PNG file size is the wrong metric: \"A z14 IIIF region PNG (1–5 MB raw, ~2–7 MB base64) registers as only ~1.5–2k vision 'tokens' but parks ~500–2000× more bytes in the harness buffer than a text token.\" The buffer pressure comes from the Rust harness accumulating streamed agent output over the full agent duration (20+ min of transcript, tool calls, context), not from the PNG file itself. The same logic applies to these vision agents reading PO strips.\n\n**The two crashes on p27 were exactly this pattern.** `_vision_notes.md` note (n) documents: \"heavy vision fan-out is what OOM'd both prior sessions\" and \"Do NOT re-run a fresh heavy fan-out 'to be safe'.\" p27 crashed twice specifically when running 2 blind transcribers + adjudicator. The current sequential approach was adopted as the direct fix.\n\n**The Workflow wrapper proposal** (using `wf_*` orchestration for the 3-agent pipeline) is a genuine incremental improvement noted in the plan's own text and `_vision_notes.md` (n) — but this is already documented in the plan itself: the `wf_fe98cd7b` workflow was already used on p27, and note (n) is the explicit lesson encoding the value of Workflow-wrapped StructuredOutput for crash recovery. So this sub-recommendation does not represent new insight; it is already the documented method.\n\n**On the \"confirmed optimal\" framing:** The current method (sequential blind passes → adjudicator, tight ≤1568px crops, per-page commits, MAX-1 heavy agent) is the correct method given the proven constraints of this specific harness. It is not sub-optimal — the sequential constraint is load-bearing on this 16 GB Windows machine with the Claude Max agent-path harness. The \"50% wall-clock saving\" claim is real in theory but not achievable on this hardware/harness without re-introducing the OOM failure mode. The finding is refuted because the proposed concrete improvement is infeasible under the binding constraints, and the current method is genuinely optimal given those constraints."
  },
  {
   "dimension": "opt-vision",
   "severity": "info",
   "title": "OPTIMIZATION RE-EVAL: Kings/Samuel dual-manuscript marathon — CONFIRM-OPTIMAL with one parallelism improvement available",
   "file": "YHWH v2.4/docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the full Kings collation plan (`YHWH v2.4/docs/superpowers/plans/2026-05-17-kings-manuscript-collation.md`, lines 11–15) and the `feedback_concurrent_agent_cap` memory (all three updates, especially the 2026-05-27 update at lines 63–67).\n\nThe finding's central claim is that running the C-2 GG transcription agent and C-5 CAM transcription agent in parallel is safe because \"GG crop ~2-4MB + CAM crop ~2-4MB in flight simultaneously = ~8MB total, far below the 16GB ceiling.\" This reasoning is factually wrong because it conflates on-disk file sizes with harness streaming-buffer pressure.\n\nThe `feedback_concurrent_agent_cap` memory explains why: OOM crash #4 happened with a SINGLE heavy agent at 117k tokens reading sequential z14 IIIF PNG crops — the crash driver is BASE64-encoded image bytes accumulated in the Rust harness's streaming buffer over the full agent run (20+ minutes), not the file size of any individual crop. The memory explicitly states (2026-05-27 update): \"STILL FORBIDDEN: a second IMAGE-reading agent concurrent with the heavy one (locating, or a C-4 CAM pull that reads tiles into context) — that re-creates the buffer-stacking that caused all 4 OOMs. Image work stays sequential / between heavy steps.\" The plan itself (line 11) directly encodes this: \"MAX 1 heavy vision agent at a time; fully drain before dispatching the next. Never stack heavy agents.\"\n\nThe sanctioned CAM hi-res pre-pull (lines 15, plan) is safe specifically because the agent does NOT read tiles into its context — it downloads, stitches, saves to disk and only programmatically QCs (dimensions + file size). A C-5 CAM transcription agent is the opposite: it reads the stitched hi-res directly into context to do blind glyph-by-glyph transcription. Running C-2 (GG vision transcriber) and C-5 (CAM vision transcriber) simultaneously would be two concurrent image-reading heavy agents — exactly the forbidden configuration.\n\nThe finding also misclassifies these agents as \"medium (<30k tokens each).\" The plan's per-chapter procedure notes that even C-3 reviewers run multi-round fix loops (3-7 rounds for NARRATIVE through LIST chapters), and the ratified method explicitly labels transcription+review agents as \"heavy.\" Crash #4 showed that a single chapter-level heavy vision agent can crash the harness. The <30k medium classification is unsupported.\n\nThe \"confirmed optimal\" framing is also internally inconsistent — the finding simultaneously claims the method is optimal AND proposes a concrete improvement, which are contradictory.\n\nThe current sequential method (MAX-1 heavy vision agent, drain before next, /clear between steps) IS the correct and already-ratified approach given the harness constraints. The finding should be fully refuted: the proposed parallelism violates a documented hard rule with crash evidence, and the RAM arithmetic used to justify it is wrong."
  },
  {
   "dimension": "opt-ingest",
   "severity": "high",
   "title": "batch_insert_notes: intra-batch duplicate notes are not deduplicated — re-running a driver + --by-book promote inserts duplicate tuples",
   "file": "YHWH v2.4/scripts/promote.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read the actual code at YHWH v2.4/scripts/promote.py lines 307-355 and confirmed the mechanics described: `existing_bodies` is populated from on-disk tuples only and is never updated during the batch loop. The proposed fix (adding `existing_bodies.setdefault((ch, v, kind), set()).add(body)` after the skip check) would indeed close the logical gap.\n\nHowever, the duplicate-insertion scenario requires two notes with identical `(ch, v, kind, body)` to appear in the same `new_notes` list. I traced the full data pipeline:\n\n1. Each candidate file is keyed as `<book>_ch_<NNN>.json` and carries a single `chapter` integer in its JSON payload. The `promote_by_book` function in `batch_promote_xrefs.py` (line 72) always uses `data[\"chapter\"]` as the chapter value for every candidate in that file. So all candidates from `gen_ch_001.json` get `chapter=1`, all from `gen_ch_003.json` get `chapter=3`, etc.\n\n2. For a collision at `(ch, v, kind, body)` to happen in a single batch, two different chapter files for the same book would need to both yield a candidate at the same `(ch, v)` with the same body. But since each file owns exactly one chapter number, this is structurally impossible via the `promote_by_book` aggregation path.\n\n3. Within a single chapter file, re-running a driver strips its own kind before appending fresh candidates (verified in `run_hebrew_at_scale.py` line 49, `run_ai_notes_at_scale.py` line 98, and the same pattern holds for other drivers). So a re-run does not produce intra-file duplicates either.\n\n4. The finder's scenario — \"re-running a driver resets status to 'pending' for own-kind candidates\" — does not produce duplicate notes in the candidate file; it replaces them. Status management lives in the candidate JSON, not in the promoted notes file.\n\nThe code gap in `batch_insert_notes` is real in isolation, but the pipeline architecture provides a structural guarantee that no intra-batch duplicate can reach it through any documented workflow. The fix is harmless defensive programming but addresses a scenario with no reachable trigger path."
  },
  {
   "dimension": "opt-ingest",
   "severity": "medium",
   "title": "run_hebrew, run_greek, run_ai_xrefs, run_ai_notes write_queue: re-running the driver silently resets 'promoted'/'skipped' status on its own candidates",
   "file": "YHWH v2.4/scripts/run_hebrew_at_scale.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "Verified by reading all four driver files and the full promote pipeline.\n\nThe finding is factually accurate about what the code does: `run_hebrew_at_scale.py` L49, `run_greek_at_scale.py` L48, `run_ai_xrefs_at_scale.py` L100, and `run_ai_notes_at_scale.py` L98 each strip all prior candidates of their own kind from the existing file before re-appending fresh `pending` ones. A `promoted` or `skipped` status from a previous run is discarded from the queue file.\n\nHowever the finding does not constitute a material defect for three reasons:\n\n1. The corpus has two independent hard guards against the only damaging consequence (duplicate notes). `promote.py:note_already_exists()` (line 74–130) checks `(chapter, verse, kind, body, attribution)` against the actual `.py` notes file before every write. `batch_insert_notes()` (line 277, `skip_existing=True`) performs the same dedup. Both `promote.py` (line 549: `pending = [c for c in data[\"candidates\"] if c.get(\"status\") == \"pending\"]`) and `batch_promote_xrefs.py` (line 67: `cands = [c for c in cands if c.get(\"status\") == \"pending\"]`) only attempt candidates with `status == \"pending\"`, so re-promoted candidates would hit `note_already_exists` and be counted as \"attempted but not written.\" The output corpus is correct.\n\n2. The only actual consequence is an inflated \"attempted\" count in the promotion log on the rare re-run case, with no explanation why M < N were written. This is an operator-experience nuisance, not a data-integrity issue.\n\n3. The normal workflow is: run the detector once, then run the promoter once. Re-running a detector over a chapter that already had some candidates promoted is an unusual maintenance case, not the designed path.\n\nThe proposed fix (preserve status by ID across re-runs) is technically sound and feasible, but the improvement is limited to log clarity in an edge case where the corpus is already fully protected. As an optimization recommendation it is low-value relative to the implementation cost of a shared `write_queue_atomic` base — particularly given the solo-maintainer quality standard. This does not meet the bar for a material finding."
  },
  {
   "dimension": "opt-ingest",
   "severity": "low",
   "title": "Ingest pipeline optimization: write_queue is copy-pasted across 9 drivers in at_scale_base.py's own module with 3 semantic variants — not extracted",
   "file": "YHWH v2.4/scripts/core/at_scale_base.py",
   "refutes": 1,
   "panelSize": 1,
   "reason": "I read all 10 write_queue definitions across the drivers and prospect.py.\n\nThe finding correctly observes the duplication (it is real) but the case for refactoring is weaker than presented, and the design is already deliberately documented.\n\nKey facts from the code:\n\n1. YHWH v2.4/scripts/core/at_scale_base.py lines 8-9 already contains an explicit design comment: \"write_queue is deliberately NOT here — each driver has its own append / dedup / overwrite semantics.\" The decision was already made consciously, not by neglect.\n\n2. The finder's \"3 semantic variants\" is actually 4 if you count prospect.py (pure-clobber, no read-existing at all). The 4 pure-append variants (xref, naves, torrey, ethiopian) are nearly identical but even within them have subtle ID-indexing expression differences: xref/naves/torrey use `len(existing) + i` with `enumerate(candidates, start=1)`, while ethiopian uses `start_idx + i` with `enumerate(candidates)` (off-by-one equivalent but not byte-identical).\n\n3. The kenyon variant (YHWH v2.4/scripts/run_kenyon_at_scale.py:44-96) does something qualitatively different: after dedup it re-indexes ALL candidates (existing + new) with fresh IDs at lines 86-87. The finder's proposed consolidation pseudocode omits this re-index step entirely, making the proposed fix incorrect as written.\n\n4. The filter-own-kind variants (hebrew, greek, ai_xrefs, ai_notes) differ only in a single string literal (\"lang-hebrew\", \"lang-greek\", \"xref-thematic\", \"comm-ai\"). Centralizing these four with a replace_kind parameter is the most defensible consolidation candidate, but doing so means passing kind as a runtime string rather than having it explicit and grep-able in each file — a real tradeoff in a solo-maintainability-priority codebase.\n\n5. None of the write_queue functions use atomic_write (they all use raw out_path.write_text()). The finding references an \"atomic_write fix above\" that is not part of this ticket — this claim is unverifiable from this finding alone and does not strengthen the case for consolidation.\n\n6. The finding is correctly classified as an optimization, not a bug. No correctness issue, no byte-stability risk, no crash path. The proposed fix is incomplete (missing the kenyon re-index path) and the design was already deliberately left as-is with an explicit comment. The consolidation benefit is marginal (per-driver write_queue functions are small, self-contained, easy to read individually, and the variant logic is already the only substantive content in each)."
  }
 ],
 "fixesPlanMarkdown": "I have all the verified findings. Let me synthesize the phased fixes plan directly.\n\n## Executive summary\n\nThis audit produced 39 verified findings (15 bug/correctness/security/debt/test/doc findings plus 18 optimization recommendations, several overlapping on the same root cause). The codebase is healthy: the 9 KJV editions build byte-stable, the marathon core is untouched, and most findings are latent edge cases rather than live regressions. The most serious are two silent-data-loss/correctness classes — the `(\\d{2})(\\d{2})` aside-ID regex that mis-injects all 1 Enoch ch 100-108 notes (mirrored in `coverage.py`), and the `write_book` `{}`-attribution corruption that converts every web-UI-saved 8-field note into a schema-violating 9-field tuple — plus a true determinism break (wall-clock date baked into every OPF, defeating both byte-stability and the content-addressable cache) and three medium XSS/XML-injection vectors in the RSS feed and `/sources` console. Five distinct `subprocess.run` calls lack the project-mandatory `stdin=subprocess.DEVNULL` (W-W1), one of them on the primary user-facing HTTP build endpoint. No finding requires a marathon-core edit, no paid-API integration, and every proposed fix is additive or behavior-preserving for the KJV build path.\n\n## Phased fixes\n\nOrdered safest/most-foundational first: guards and docs, then silent-data-loss and determinism, then security, then behavior-changing correctness, then debt/perf. Build-path findings carry a byte-stability proof obligation (regen + `git diff` must show zero change to the 9 KJV editions, except the deliberate OPF date pin).\n\n### Phase 0 — Docs & registry truth (zero code risk; do first to clear noise)\n\n- [ ] **[info] REPO_MAP api/templates counts off by 1** — `dev/REPO_MAP.md:42-43`. Change `(17)`→`(18)`, `(20)`→`(21)`. No build impact.\n- [ ] **[info] REPO_MAP test count off by 1** — `dev/REPO_MAP.md:17`. `168`→`169 pytest files`. No build impact.\n- [ ] **[info] REPO_MAP archived-plans list incomplete** — `dev/REPO_MAP.md:51`. Add `PLAN_2026-05-21 + PLAN_2026-05-24-end-scope`. No build impact.\n- [ ] **[info] check_superpowers_coherence docstring says 39; now 41** — `scripts/lint_rules.py:1357`. `39`→`41` (docstring only; check is dynamic). No build impact.\n- [ ] **[med] MATRIX_MAP/REPO_MAP say 71 kinds; actual is 72** — `dev/MATRIX_MAP.md:17,27,133` + `dev/REPO_MAP.md:30`. Change all four to `72`; update the quoted resolved-docstring to `72 kinds / 67,715 notes / 11 editions`. No build impact.\n- [ ] **[low] MATRIX_MAP/REPO_MAP say 13 translation dirs; actual is 14** — `dev/MATRIX_MAP.md:17,31` + `dev/REPO_MAP.md:32`. Change to `14`; add `lxx-swete-greek` + `byzantine-greek`; qualify \"Each has `_meta.yaml`\" (the two `-en` dirs lack it). No build impact.\n- [ ] **[med] Mint-cleanup plan status stale (\"Phase 6 active\")** — `docs/superpowers/plans/2026-05-29-mint-cleanup-and-guardrails.md:2`. Set to `COMPLETE — all Phases 0–6 shipped + synced 2026-05-31 (HEAD ad945f62)`; move INDEX.md row from \"In progress\" to \"Complete\". No build impact.\n- [ ] **[low] Mint-7 plan status stale (\"execution PENDING\")** — `docs/superpowers/plans/2026-05-31-mint-7-quality-pass.md:2`. Set to `COMPLETE — Phases A · C · B · D · E all shipped + synced 2026-05-31`; in INDEX.md change `## In progress (9)`→`(8)` and move the mint-7 row to Shipped. No build impact.\n\n### Phase 1 — Add guards & fix registry pins (additive; lock invariants before changing behavior)\n\n- [ ] **[high] ALL_CHECKS is 27 but tests/docs say 26** — `tests/test_lint_rules.py:15,36,38`. Change `>= 26`→`>= 27`, fix the two prose strings; also fix `docs/.../mint-7-quality-pass.md:158,176` and `mint-8-audit-plan.md:39`. **Guard:** the `== 27` exact pin (not `>=`) prevents a silent drop to exactly 26. No build impact.\n- [ ] **[info] bookcode_canonical lint omits CCEL_ABBREV / EASTON_BOOK** — `scripts/lint_rules.py:1995-2003`. Add `(\"scripts.extract_naves_ccel\",\"CCEL_ABBREV\")` and `(\"scripts.extract_eastons_ccel\",\"EASTON_BOOK\")` to `map_specs`. **This is the commit-time lint that catches the every-ingest book-code-canonical recurrence — prefer it over a pytest-only guard.** Current values pass clean. No build impact.\n- [ ] **[med] add_note vs promote chapter-range guard inconsistency (aes ch 11-16)** — add `html_chapter_count(book)` helper to `scripts/core/canonical_verse_counts.py` and apply a secondary `chapter > html_ch_count` guard in `promote.py` `promote_candidate` (line ~403, `return False, \"\"`) and `batch_insert_notes` (line ~330, `continue`). **Also add a `lint_rules` check that warns when any notes file holds coordinates above `books.yaml` `ch_count`** (would have caught the aes strand at add-time). No build impact (notes-corpus validation only).\n\n### Phase 2 — Silent data loss & schema integrity (high; fix root cause + guard)\n\n- [ ] **[high] aside-ID regex `(\\d{2})(\\d{2})` breaks 1 Enoch ch 100-108** — `scripts/inject.py:606-608` change `r'(\\d{2})(\\d{2})([a-z]?)\"'`→`r'(\\d+)(\\d{2})([a-z]?)\"'`; **apply the identical fix to `scripts/coverage.py:144`** (else it under-reports 1En 100-108 coverage). **Test:** add a unit test re-injecting a `1en` note at ch=100,v=01 and assert sorted placement; assert `coverage.py` counts it present. **Touches build path (inject writes `epub_working/`)** → after fix run `inject.py --all-books` then `git diff` on `epub_working/` to prove zero change for the 9 KJV editions (they have no ch≥100 books) plus correct re-ordering for 1En only.\n- [ ] **[high] write_book serialises absent attribution as `{}` (schema corruption)** — `scripts/web_helpers.py`: line 131 `attribution or {}`→`or \"\"`; line 146 `or {}`→`or None`; lines 173-182 make the 9th field conditional (`if isinstance(attribution,str) and attribution.strip():`). **Test:** round-trip an 8-field note through `tuple_to_dict`→`dict_to_tuple`→`write_book` and assert the written file re-parses to an 8-field tuple (no `{}`). No build path (web-UI write only); KJV editions carry no `web_helpers` dependency.\n- [ ] **[low] write_book duplicate `{}` finding** — same root cause as above (`scripts/web_helpers.py:174`); resolved by the Phase-2 `write_book` fix. No separate work.\n- [ ] **[med] batch_insert_notes silently drops all inserts on SyntaxError** — `scripts/promote.py:368-373`. **Preferred:** `raise RuntimeError(...) from exc` so the caller chain surfaces the drop (return 0 is indistinguishable from \"no candidates\"). **Root-cause fix:** in `format_tuple_text`'s `py_str`, add `s = s.replace(\"\\n\",\"\\\\n\").replace(\"\\r\",\"\\\\r\")` before quote selection so embedded newlines can't produce invalid Python. **Test:** feed a body containing a literal newline and assert the spliced file parses. No build path.\n- [ ] **[low] batch_insert_notes silently drops notes missing chapter/verse keys** — `scripts/promote.py:325-330`. Insert `if ch is None or v is None: warnings.warn(...); continue` before the coord guard. **Test:** call with a key-less candidate dict; assert a warning is raised and the note skipped. No build path.\n- [ ] **[low] Non-atomic candidate-JSON writes in 9 at-scale drivers + prospect.py** — `scripts/prospect.py:152`, `run_xref:66`, `run_naves:67`, `run_torrey:65`, `run_hebrew:61`, `run_greek:60`, `run_ethiopian:76`, `run_kenyon:95`, `run_ai_xrefs:112`, `run_ai_notes:110`. Add `from scripts.core.notes_io import atomic_write`; replace `write_text(json.dumps(...))` with `atomic_write(out_path, json.dumps(...,indent=2,ensure_ascii=False))`. Do **not** centralize into `at_scale_base.py` (documented dependency-free leaf). No build path.\n\n### Phase 3 — Determinism / byte-stability (high; build-path, proof-obligated)\n\n- [ ] **[high] Wall-clock date in every OPF `<dc:date>`/`<dc:rights>`** — `scripts/epub_utils.py:29-37`. Replace `now_year`/`now_date` `datetime.now()` defaults with static `\"copyright_year\":\"2026\"` and `\"publication_date\":\"2026-05-14\"` (Ω.0 pivot); drop the now-unused `datetime` import. **Guard (harden the gate so this class can't regress):** in `tests/test_byte_stability_gate.py` extend `_content_digest` with `_DATE_RE = re.compile(r\"<dc:date>[^<]*</dc:date>\")` → `<dc:date>NORMALIZED</dc:date>` and a `Copyright © \\d{4}`→`YYYY` substitution. **Touches build path** → regen + `git diff`: every edition's `content.opf` `<dc:date>`/`<dc:rights>` year changes once to the pinned value, then is stable across calendar days. This is a deliberate, intended one-time OPF delta; all other bytes unchanged.\n- [ ] **[low] build_cache.compute_cache_key omits epub_working/META-INF/** — `scripts/core/build_cache.py:239-248`. Switch the shallow `iterdir()` to `rglob(\"*\")` skipping dotfile dirs + `onix/` (matching `build_epub.collect_files`). Low priority (no current write path mutates `container.xml`); fold in only if Phase 4's nested-anchor / future subdir work lands. No KJV byte change (hashing only).\n\n### Phase 4 — Security (XSS / XML injection; medium, surgical)\n\n- [ ] **[med] RSS feed injects raw note `body_html` into CDATA (stored XSS + `]]>` XML injection)** — `scripts/core/verse_of_day.py:328-336` (two duplicate findings, same site). Replace `body_html = (...).get(\"body_html\") or \"\"` with `sanitize_html(...)` then `.replace(\"]]>\",\"]]&gt;\")` to neutralize CDATA escape. Import at module top alongside core imports (mirrors `preview.py:133-135`). **Test:** feed a body with `<script>` and a `]]>` and assert neither survives into the description. No build path.\n- [ ] **[med] Stored-XSS in /sources console `truncateHTML` short-body branch** — `scripts/templates/sources.py:592-596`. Change `if (stripped.length <= n) return html;`→`return escapeHTML(stripped);` (matches the long-body branch; trades rich-text rendering of short bodies for safety — acceptable for a publisher-review tool). Note: the finder's \"nonce-CSP mitigates\" claim is wrong (no CSP exists), which strengthens the need for this fix. No build path.\n\n### Phase 5 — Behavior-changing correctness (medium/low; ordering & lookup logic)\n\n- [ ] **[med] find_aside_insertion_point misorders cross-chapter re-injection (Strategy-B)** — `scripts/inject.py:621`. Change `precedes = existing_ch != ch or (existing_v, existing_s) < target` → `precedes = (existing_ch, existing_v, existing_s) < (ch, v, suffix or \"\")`. Do **not** apply the finder's second proposed change (the `else` branch is already correct). **Touches build path** → regen + `git diff` on `epub_working/` to prove zero change for first-build and corrected order on re-inject. **Test:** insert a ch-5 note into a section already holding a ch-12 aside; assert ch-5 lands before ch-12.\n- [ ] **[low] promote.py break-on-first-descending mis-inserts in unsorted files** — `scripts/promote.py:339-343` (`batch_insert_notes`) and `:246-249` (`insert_note_into_book_file`). Change `else: break`→full scan (drop the `break`). Add a `sorted(...)` key to `write_book` in `web_helpers.py` so web-UI writes stay sorted. **Test:** craft a notes file with one out-of-order tuple, insert, assert correct placement. No build path.\n- [ ] **[med] translations._book_index caches stale verse dict (bypasses mtime freshness)** — `scripts/core/translations.py:120-128`. Add `mtime_ns: int` to the `@lru_cache` key + a `_book_index_for` wrapper that resolves `stat().st_mtime_ns` (mirrors the existing `_load_book_cached`/`_load_book` split); route `get_verse`/`get_chapter` through it. Latent until τ.G.* standalone builds wire in; fix now (cheap, additive). No KJV build path (KJV doesn't read `translations/`).\n- [ ] **[low] notes_io load cache stale within 1s mtime window** — `scripts/core/notes_io.py:124-130`. Add `clear_load_notes_cache()` inside `_invalidate_corpus_index_if_notes_file` (one line; both atomic writers get it free). No build byte impact (in-memory parse cache).\n- [ ] **[low] api_restore_backup writes snapshot bytes without validating .py notes parse** — `scripts/web_content.py:644-645`. Gate `.py` notes files: decode UTF-8 + `load_notes_from_text(...) is not None` before `atomic_write_bytes`; return a 400 on failure. Binary restores (covers) unaffected. No build path.\n\n### Phase 6 — Pipeline gaps & preview/build parity (medium)\n\n- [ ] **[high/med] 5 subprocess.run missing `stdin=subprocess.DEVNULL` (W-W1)** — add `stdin=subprocess.DEVNULL` to: `scripts/api/exports.py:197` (**user-facing HTTP build endpoint — highest**), `scripts/build_edition.py:3022-3033` (dev build path), `scripts/bulk_edit.py:189-192` (corpus auto-verify). **Guard (prefer commit-time lint):** add a `lint_rules` check that flags any `subprocess.run(` call lacking `stdin=` in `scripts/**` (W-W1 recurs; a pytest-only guard won't catch new call sites at author time). Touches build invocation only, not EPUB bytes.\n- [ ] **[med] ebible build omits check_nested_anchors after inject** — `scripts/ebible.py:227-255`. Insert `(\"nested-anchor guard\",\"check_nested_anchors.py\",[\"--fix\"])` between inject and build-editions (idempotent; only rewrites nested files). **Touches build path** → after add, run `ebible build` and `git diff` epub_working (currently 0 nested anchors → no change today; closes the gap for future note-ref-in-vn-link injects). Pairs with the base-invariant rule (`test_nested_anchors`).\n- [ ] **[med] api_sample_html kind-filter bypasses config.enabled_kind_codes** — `scripts/web_content.py:275-291` (two overlapping findings). Replace the manual `enabled_kinds`/`disabled_kinds` block with `enabled_kind_codes(edition, load_kinds())` (the canonical resolver `build_edition` + `matrix` already use), so preview matches the build (categories→kinds, max_phase gate, AI double-opt-in). Update the stale \"mirrors build_edition\" comment. No EPUB byte impact (preview HTML only). **Test:** assert sample for a categories-driven edition shows the same kinds the build emits.\n- [ ] **[med] is_output_current ignores content/notes/*.py + kinds/categories.yaml** — `scripts/build_edition.py:1943-1965`. After line ~1961 append `notes_dir.glob(\"*.py\")` (guarded by `is_dir()`) + `kinds.yaml` + `categories.yaml`. Hardens only the mtime fallback (content-addressable cache already covers these); do **not** remove `is_output_current`. Touches build decision, not EPUB bytes.\n- [ ] **[low] TODO_CERTIFIER_NAME ships in every OPF** — `scripts/build_edition.py:1265`. Replace literal with `f'...{_xml_escape(pub[\"publisher_name\"])}...'` (reuses already-resolved `pub`). **Touches build path** → intended one-time `content.opf` delta (placeholder → publisher name); regen + `git diff` to confirm only that line changes.\n\n### Phase 7 — Tests (state-default & share-pin convention fixes; §8/§8.1)\n\n- [ ] **[high] test_amharic_tewahedo_contains_gen_py dead — `def` buried in a comment** — `tests/test_parallel_bible_tau6x1.py:834`. Split the line: comment stays at 8-space indent, `def test_amharic_tewahedo_contains_gen_py(self):` starts a new line at 4-space class-method indent. Restores a silently-missing test. No build path.\n- [ ] **[med] test_by_verse_empty_for_unknown / test_by_father_empty_for_unknown assume mat 1:1 empty (§8 state-default)** — `tests/test_patristic_gamma3.py:134-140,150-156`. Replace `mat 1:1` / Origen probes with structurally-impossible coordinates (`\"nonexistent-book-xyz\",99,99` / `\"nonexistent-father-xyz\"`). No build path.\n- [ ] **[low] test_detect_returns_empty_for_uncommented_verse — same §8 violation** — `tests/test_patristic_gamma3.py:221-228`. Replace `mat 1:1` + `gen 50:1` with `d.detect(\"nonexistent-book-xyz\",99,99,\"\")`. No build path.\n- [ ] **[low] γ.4.2 share-pin `cyril_share < 0.80` (§8.1 prohibited)** — `tests/test_ethiopian_gamma4.py:1177-1180`. Replace with an absolute ceiling `len(cyril) <= 91` (verify the live count first; Ephrem floor at line 1165 already covers the other side). No build path.\n- [ ] **[low] promote_candidate coordinate guard not directly unit-tested** — add a standalone `tmp_path`/`monkeypatch` test in `tests/test_promote_idempotency.py` exercising `promote_candidate(\"gen\", {...verse:999...})` → `(False,\"\")` (do not reuse `fake_book`). Covers the load-bearing `promote.py:403` guard. No build path.\n\n### Phase 8 — Code debt & build performance (low; byte-identical)\n\n- [ ] **[low] iter_target_verses/resolve_books duplicated across both AI drivers** — `scripts/run_ai_notes_at_scale.py:117-147,298-305` ↔ `run_ai_xrefs_at_scale.py:119-146,292-299`. Move both to `scripts/core/at_scale_base.py` (already owns `candidate_to_dict`, `NT_BOOKS`); both drivers import. Eliminates the copy-then-diverge class that caused the mint-7 B1 `ch_count` bug. No build path.\n- [ ] **[low] batch_promote_xrefs default is per-candidate O(n²) file rewrites** — `scripts/batch_promote_xrefs.py:94-98,104`. Flip default to the by-book fast path: rename flag to `--per-candidate` (`store_true`), change `if args.by_book:`→`if not args.per_candidate:`, update docstring. No build path.\n- [ ] **[low] filter_html recompiles regex O(files×kinds×2)** — `scripts/build_edition.py:1001-1061`. Pre-compile kind + ref-id patterns once in `build_one` (both real + dry_run paths); pass into `filter_html` with `None` fallback for backward compat. **Touches build path** → byte-identical; regen + `git diff` to prove. Win only material for tradition/time-filtered editions.\n- [ ] **[low] Double corpus walk for tradition filter + label map** — `scripts/build_edition.py:2647,2661`. Merge into one `_compute_tradition_maps(edition)` walking `_iter_note_ref_traditions()` once, returning `(disabled_set, surviving_map)`; keep the two public functions as thin wrappers. **Touches build path** → byte-identical; regen + `git diff`.\n- [ ] **[low] compresslevel=9 → 6 in build_epub** — `scripts/build_epub.py:156`. Change to `6` (est. 20-40s/edition saved, ~1-3% larger). After change, clear `exports/.cache/` so no stale level-9 EPUB is served. `test_byte_stability_gate` needs no change (digest is over decompressed inner content). **Touches build path** but inner bytes unchanged; outer zip size differs (not gated).\n- [ ] **[med, deferred] Exodus stores use non-canonical `ex.py` (4 Tewahedo dirs)** — `content/translations/*/ex.py` + `_source.yaml:110` + `tests/test_parallel_bible_tau7xb.py:123`. **Do NOT fix before τ.G.* standalone wiring lands.** When it does: add `_TRANSLATION_BOOK_ALIASES = {\"ex\":\"exo\",\"1k\":\"1ki\",\"2k\":\"2ki\"}` to `_book_path` in `scripts/core/translations.py` (mirrors `render_coverage.py`); a file rename must be a coordinated atomic commit updating `_source.yaml`, `_meta.yaml`, the 4 files, and the test pins together. Out of the current build path.\n- [ ] **[med, render-tool] render_coverage._CANONICAL_BOOKS missing 7 of 87 books** — `scripts/render_coverage.py:53-143`. Add `man, 2en, 1es, aes, lje, sus, 1cl` at canonical positions; reconcile the 2 phantom codes (`1ma, 2ma` not in books.yaml); fix the \"81-book set\" docstring; extend the `check_render_coverage_no_regression` baseline only for books that already have `.py` files. Coverage-reporting only; no EPUB byte impact.\n\n## Optimization decisions\n\n| Area | Verdict | Recommendation |\n|---|---|---|\n| `subprocess.run` stdin (exports.py / build_edition.py / bulk_edit.py) | change | Add `stdin=subprocess.DEVNULL` to all 5 W-W1 sites; back with a commit-time lint check. Build-invocation only, no EPUB bytes. |\n| OPF wall-clock date (`epub_utils.py`) | change | Pin static dates; harden `test_byte_stability_gate` to normalize `<dc:date>`/`Copyright © YYYY`. One intended OPF delta, then deterministic. |\n| `compresslevel=9`→`6` (`build_epub.py:156`) | change | Highest-value single perf win (~20-40s/edition); clear `exports/.cache/` after. Inner bytes unchanged → stability gate unaffected. Marathon core untouched; no paid API. |\n| `filter_html` regex recompile | change | Pre-compile kind/ref patterns once per build; byte-identical (prove via `git diff`). Material only for tradition/time-filtered editions. |\n| Double tradition corpus walk | change | Single-pass `_compute_tradition_maps`; byte-identical. |\n| `is_output_current` mtime inputs | change | Add notes/*.py + kinds/categories.yaml to the fallback only (content-addressable cache already correct); don't remove the fallback. |\n| `api_sample_html` kind filter | change | Delegate to `config.enabled_kind_codes` so preview == build; preview HTML only, no EPUB byte impact. |\n| `_book_index` mtime cache | change | Add `mtime_ns` to the cache key (mirror `_load_book_cached`); KJV build doesn't touch `translations/`. |\n| `do_POST`→`do_PUT` fallthrough double-auth (`web.py:2054-2055`) | change | Replace fallthrough with a 404; avoids double TOTP verification. Local single-user app — confirm no shared POST/PUT route relies on it (none documented). |\n| `at_scale_base.py` centralization of `write_queue` | confirmed-optimal | Keep decentralized atomic writes per driver; `at_scale_base` is a documented dependency-free leaf — do **not** add a shared writer there. |\n| Marathon core (`build_standalone.py`, `manuscript_*`, `po_vision_store.py`, `content/manuscript/**`, `patrologia/**`, GAPS/) | confirmed-optimal / off-limits | No edits proposed or permitted; no finding requires touching it. |\n| Paid-API / Voyage embeddings | confirmed-optimal | Out of scope; no recommendation introduces a paid API or external Claude Code hook. |\n| Splitting `web.py` / `build_edition.py` for size | confirmed-optimal | No split — large files of small cohesive functions are deliberate. |\n\n## Constraints carried\n\n- **Marathon core is off-limits** — `scripts/build_standalone.py`, `scripts/core/manuscript_*.py`, `scripts/core/po_vision_store.py`, `content/manuscript/**`, `content/translations/sources/patrologia/**`, `GAPS/`: never edited; no finding above touches them.\n- **9 KJV editions stay byte-stable** — every build-path fix (inject.py regex/ordering, ebible nested-anchor step, filter_html pre-compile, tradition single-walk, compresslevel) requires a regen + `git diff` proof of zero KJV change. The two *intended* OPF deltas (static date pin; `TODO_CERTIFIER_NAME`→publisher) are one-time, isolated, and gated by the hardened stability test thereafter.\n- **Schema changes are additive** — `write_book` restores the 8-field / 9-field-with-non-empty-string invariant; `_book_index` adds a cache-key arg; the `html_chapter_count` guard and the new lint checks add validation only. Byte-identical when unset.\n- **Writes go through `notes_io.atomic_write` / `ensure_backup`** — the 10 at-scale/prospect JSON writers and `api_restore_backup` are brought onto `atomic_write`; `promote.py` keeps `ensure_backup` AFTER the parse check (no partial write).\n- **5-leg save per phase** — each shipped phase ends with the full sync: local commit (`save.ps1`, PowerShell — never the Bash tool) + `git push origin` (GitLab) + `git push github` (GitHub) + `git bundle` to E: + copy to F:, each leg verified. Run `ruff format` on regenerated files before the commit or the pre-commit `ruff format --check .` blocks it.",
 "completeness": [
  {
   "area": "versification.py — LXX/Vulgate/Douay Psalm-map unit coverage vs the actual _psalm_map() function",
   "why": "The audit found many ingest-level versification findings (Sirach segments, 1 Esdras, Judith) but the `_psalm_map()` function itself — the shared O(150-chapter) LXX↔KJV Psalm renumbering table reused by BOTH the LXX Swete path and the `vulgate_to_kjv` path — has no dedicated unit tests probing the four gross-structural seams (LXX 9 = KJV 9+10; LXX 10-112 = KJV N+1; LXX 113 = KJV 114+115; LXX 114+115 = KJV 116; LXX 146+147 = KJV 147). A bug there silently misaligns Psalm verse-popups for roughly 100 chapters in two distinct translation ingest paths simultaneously. test_vulgate_douay_ingest.py tests the *per-psalm-fix overrides* (TestVulgatePsalmFixes) but does not test identity Psalms through `_psalm_map()` alone.",
   "suggested_lens": "Grep for `_psalm_map` in scripts/core/versification.py; read its logic; then grep for `lxx_to_kjv` or `_psalm_map` in tests/ to confirm no test calls it directly. Write one parametrized test class that feeds every four-seam Psalm chapter pair (ch 9, 10, 113, 114, 115, 146, 147) to `lxx_swete_to_kjv('Psa', lxx_ch, 1)` and asserts the expected KJV chapter, plus a superscription omit and an identity Psalm (ch 23)."
  },
  {
   "area": "standalone_store.py — bare write_text (non-atomic) for translation store writes that land in content/translations/",
   "why": "The audit flagged non-atomic writes in prospect.py and all run_*_at_scale.py candidate-JSON functions, but missed standalone_store.py lines 84-85 and 162 where `build_book_store` and `build_psalms_apparatus` use bare `Path.write_text()` to produce `geez-tewahedo/<book>.py` and `<book>_apparatus.json`. These files are referenced by the EPUB build pipeline; a crash mid-write corrupts the canonical Ge'ez store with no atomic guarantee — the same S1/S3 class of bug that notes_io was introduced to prevent. The `notes_io` module's own docstring (line 21-24) calls this class of bug out explicitly.",
   "suggested_lens": "Read scripts/core/standalone_store.py lines 80-90 and 155-165. Grep `write_text` in scripts/core/standalone_store.py to confirm all 3 instances are bare (no `atomic_write` import). Confirm `notes_io.atomic_write` exists and covers `.py` files. The fix is a 3-line import + call swap, but the gap is whether any test checks that these writes survive a simulated mid-write crash (they don't — test_build_standalone.py only checks output correctness)."
  },
  {
   "area": "corpus_index fingerprint uses mtime+size rather than content hash — not covered by byte-stability tests",
   "why": "`corpus_index._compute_fingerprint()` uses `(stem, st_size, st_mtime_ns)` as its invalidation key. On Windows, NTFS mtime resolution is 100 ns but some file systems (FAT32, older Windows NFS mounts, some VMs) are 1-2 s. If two sequential atomic_writes land within the resolution window, the second write's stat looks identical and the corpus_index returns stale data for up to `_FINGERPRINT_TTL_SEC`. The audit noted lru_cache 1s mtime staleness but didn't directly verify whether the byte-stability gate (`test_byte_stability_gate.py`) would catch this — it does a full rebuild, so it bypasses the fingerprint cache. The real hazard is the editor's `save → matrix → rebuild` loop, where a race between writes and the 1s TTL produces a wrong matrix count shown to the user. No test covers rapid-sequential atomic_write + corpus_index.connection() within one TTL window.",
   "suggested_lens": "Read scripts/core/corpus_index.py `_compute_fingerprint()` and `_FINGERPRINT_TTL_SEC`. Write a test that calls `atomic_write` twice in rapid succession (using time.sleep(0) between), then calls `corpus_index.fingerprint()` and asserts the second fingerprint differs from the first. If the two fingerprints can be equal on the test machine, the fix is to add file-content hashing as a fallback when mtime is unchanged."
  },
  {
   "area": "build_cache.compute_cache_key omits epub_working/ subdirectories (META-INF/ was flagged, but also onix/ subdirectory)",
   "why": "The audit flagged that `compute_cache_key` only walks `epub_working/*.` top-level files, missing `META-INF/container.xml`. But there is also an `epub_working/onix/` subdirectory containing 5 ONIX XML files (listed in REPO_MAP §epub_working). These are vestigial commercial metadata but their presence/mutation still doesn't invalidate the build cache — the cache check at line 241 uses `epub_dir.iterdir()` which only yields direct children, skipping subdirs. If any file under onix/ changes (e.g. someone deletes them as cleanup), the cache wrongly reports a hit. This was listed as one finding already for META-INF, but the scope was stated as 'META-INF/' only — the onix/ subdir has the identical miss.",
   "suggested_lens": "Read scripts/core/build_cache.py lines 238-248 (the epub_working/ walk). Confirm that `entry.iterdir()` for subdirectories is not called (it isn't — `is_file()` gates every entry). Then list `epub_working/` with PowerShell `Get-ChildItem -Recurse` to inventory all subdirs (META-INF, onix at minimum). The fix is to switch from `epub_dir.iterdir()` to `epub_dir.rglob('*')` filtered to `is_file()`."
  },
  {
   "area": "matter_pages.py / render_reading_plans_page — no dedicated test file; reading-plan parsing edge cases uncovered",
   "why": "The `reading_plans.py` module has a regex parser (`_REF_RE`) for verse references like `gen 1:1-2:3` (cross-chapter) and `psa 1`, and `parse_verse_ref` returns `None` for 'unsupported shapes' — but no test directly exercises the parser's failure modes: what does it do with book codes not in books.yaml (silently None), with a chapter-only ref missing a colon (should match the non-colon arm), with a malformed range, or with Tewahedo-specific codes (`1en`, `jub`) that have no KJV skeleton? The only reading-plans test found is inside test_scripts.py. Additionally, `build_cache.compute_cache_key` reads reading-plan files for enabled plans — a mis-parsed plan that silently returns `None` would still produce a stable cache key from the file hash, but the rendered EPUB page would be empty. No test pins that `render_reading_plans_page` produces non-empty output for an edition with an actual plan enabled.",
   "suggested_lens": "Grep `reading_plans` in tests/test_scripts.py to find the existing coverage. Read scripts/core/reading_plans.py `parse_verse_ref()` and `_REF_RE`. Then check content/reading_plans/ (glob it) to see what plan files exist and what edge-case verse refs they contain. Write a test that calls `parse_verse_ref` for a cross-chapter range, a Tewahedo distinctive book code, and a chapter-only ref, asserting the correct `(book, chapter)` tuple or `None`."
  },
  {
   "area": "verse_of_day.py — rss_feed body_html injection (already flagged as RSS XSS) BUT also: verse picker assumes notes are sorted; no test for edition-filtered corpus with zero notes",
   "why": "The existing audit flagged `body_html` injected raw into CDATA. A second unexamined hazard in the same module: `_pick_verse` falls back across `(chapter, book)` combinations until it finds a verse with notes, using a deterministic `seed % len(candidates)` traversal. If an edition's canon has no notes at all (e.g. a standalone Ge'ez Bible that hasn't had notes ingested yet), the fallback loop terminates at `max_candidates` (100) and returns `None`. The caller `verse_of_day()` already handles `None` gracefully, but `rss_feed()` silently emits zero `<item>` elements — producing a syntactically valid but empty RSS feed with no error logged. Additionally, `_book_codes_for_edition` calls `config.load_editions()` (an lru_cached list) and `_load_canons()`, whose cache behavior during tests (where monkeypatching clears `load_editions.cache_clear()`) may leave stale canon data. No test covers the zero-notes-edition-filtered path.",
   "suggested_lens": "Read scripts/core/verse_of_day.py lines 115-200 (picker) and 292-356 (rss_feed). Check whether there is a test in test_scripts.py under 'verse_of_day' or 'rss_feed'. Write a test with a tmp_path notes_dir containing zero .py files and a monkeypatched edition, asserting rss_feed returns a syntactically valid (parseable) XML with zero items but no Python exception."
  },
  {
   "area": "extract_naves_ccel.py CCEL_ABBREV and extract_eastons_ccel.py EASTON_BOOK — the lint check `bookcode_canonical` explicitly does NOT cover these two maps",
   "why": "The audit's last finding explicitly names `bookcode_canonical` lint not covering `CCEL_ABBREV` or `EASTON_BOOK`. This is a gap finder for the NEXT round: both maps were apparently correct at time of writing (all values are canonical codes) but neither map is guarded by the pre-commit lint. If a future developer adds a new book (e.g. a deuterocanon book like `lje` or `man`) and also adds it to these maps using a legacy alias, it silently produces the same book-code-drop bug that motivated mint-7's ★BUGCLUSTER fix. The audit noted the gap but did NOT verify the current map contents for completeness — specifically, whether `CCEL_ABBREV`/`EASTON_BOOK` already emit non-canonical codes today (e.g. `Eze` → `eze` is fine; `joh` or `jas` entries could still exist in Naves' broader NAVES_BOOK_REMAP which feeds the same pipeline).",
   "suggested_lens": "Read scripts/extract_naves_ccel.py lines 1-121 fully (CCEL_ABBREV + NAVES_BOOK_REMAP combined). Confirm whether NAVES_BOOK_REMAP also exists and whether it contains legacy aliases (`joh`, `jas`, `mar`, `ezk`, `nam`, `php`) that would bypass `_normalize_book_code`. Then read lint_rules.py `check_book_codes_canonical` to confirm these two dicts are not in its scan list. A targeted unit test that asserts every value in both maps is a key in `config.books_by_code()` would function as a permanent guard."
  },
  {
   "area": "_replace_verse_popup_translation() — tested but not wired; no integration test proving the popup-language prune path removes the right <aside> elements from the built EPUB",
   "why": "The audit flagged that `_replace_verse_popup_translation` is NOT YET WIRED (mint-7 D3). But a second gap exists upstream: `_apply_popup_languages_and_translation()` in build_edition.py is wired and runs on every build — it prunes `<aside class=\"vnote\">` elements whose version id is not in the edition's `popup_languages` set. This pruning path has no end-to-end test that builds an edition with a restricted `popup_languages_default` and then asserts that (a) the prohibited version's `<p class=\"vnote-...\">` paragraph is absent from the output HTML, and (b) the kept version's paragraph is still present. The only coverage is `test_popup_witnesses.py` which checks that `_resolve_popup_languages` RETURNS the right set — it does not verify the HTML actually gets pruned during the build.",
   "suggested_lens": "Grep `_apply_popup_languages_and_translation` in scripts/build_edition.py to find the pruning logic (around lines 907-990). Check tests/test_popup_witnesses.py for whether any test calls `filter_html` or `_apply_popup_languages_and_translation` directly with a fake HTML chunk. The gap is confirmed if no test asserts that `vnote-arabic` (for example) paragraphs are absent when `popup_languages_default` is set to `['wlc']` only."
  },
  {
   "area": "work_cache.py — WorkCache.get_or_compute() is not thread-safe between the get() check and the compute_fn() invocation",
   "why": "WorkCache is explicitly designed for use from parallel_map's worker threads (docstring: `check_same_thread=False + a mutex`). The `get_or_compute` method calls `self.get(key)` (lock-protected), then RELEASES the lock, then calls `compute_fn()` (which may be a slow API call), then calls `self.put(key, value)` (lock-protected again). Between the `get` and the `put` a second thread can call `get_or_compute` for the same key, find it absent, and also call `compute_fn()` — producing a duplicate API call. For rate-limited AI APIs (Anthropic), this can produce a 429 or charge twice for the same computation. The audit's concurrency-caching dimension covered corpus_index locking and lru_cache mtime issues but did not check the at-scale WorkCache.",
   "suggested_lens": "Read scripts/core/work_cache.py lines 40-90 (`get_or_compute` method). Check whether the lock is held during `compute_fn()` — it isn't. Write a test with `threading.Barrier` to confirm two threads calling `get_or_compute` with the same key both invoke the compute function (the current behavior). The fix is either a double-checked lock (read under lock, compute outside, write under lock with a re-read guard) or a per-key in-flight sentinel."
  },
  {
   "area": "NAVES_BOOK_REMAP in extract_naves_ccel.py — separate from CCEL_ABBREV, likely contains legacy codes, not audited",
   "why": "The audit's bookcode_canonical finding scoped itself to CCEL_ABBREV and EASTON_BOOK by name. But extract_naves_ccel.py has a second book-code map, NAVES_BOOK_REMAP, that is used as a fallback in the `remap()` function (line 121: `CCEL_ABBREV.get(a) or NAVES_BOOK_REMAP.get(a) or NAVES_BOOK_REMAP.get(a.title())`). If NAVES_BOOK_REMAP contains legacy codes like `joh`→`jhn` or `mar`→`mrk` that were not updated when _BOOK_CODE_ALIASES was extended in mint-7, those entries will still resolve via the centralised normalizer at query time — but the ingest path (at extract time) may have already stored them under the legacy code in naves_topical.json. This is the same root cause as the ★BUGCLUSTER but in the ingest-side map rather than the query-side map.",
   "suggested_lens": "Read scripts/extract_naves_ccel.py lines 1-40 to find NAVES_BOOK_REMAP (it precedes CCEL_ABBREV in the file — read from the top). Confirm whether NAVES_BOOK_REMAP contains any of the legacy codes `joh`, `jas`, `mar`, `jol`, `ezk`, `nam`, `php`. If it does, grep content/sources/naves_topical.json for those keys to confirm whether any currently stored entries use legacy codes."
  }
 ]
}