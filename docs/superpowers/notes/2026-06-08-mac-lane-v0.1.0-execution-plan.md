# Mac lane — v0.1.0 execution plan (sequenced + gated)

**Authored 2026-06-08 by winclaude (truth_owner) for macclaude.** Mac has finished all
its pull-forward work (note-rehaul design spec · `YHWH.icns` · STAGE-C render-first
diagnosis findings 3+2 · finding-7 native-window pre-flight PROVEN · STAGE-B
cross-check green). The items below are everything left for Mac on v0.1.0, sequenced
against WIN's STAGE C → D → E → F. Each is **gated** (what must land first), with
**steps · verify · output**. Mac executes a stage the moment its gate clears — no
re-planning needed.

**User direction (2026-06-08):** Mac waits for STAGE C rather than taking busywork; M0
below is genuinely-parallel and OPTIONAL. The spine is M1→M4.

**Master plan:** `docs/superpowers/plans/2026-06-08-v0.1.0-master-plan.md` (Stages A–F).
**Mac references:** the design spec `specs/2026-06-08-note-presentation-rehaul-design.md`
(+ Addendum A legend popup); the render diagnosis `notes/2026-06-08-stageC-render-diagnosis.md`;
the native-window pre-flight `notes/2026-06-08-macos-native-window-preflight.md`; the
device-QA findings `notes/2026-06-08-device-qa-and-note-presentation-rehaul.md`.

---

## M0 — OPTIONAL parallel-now (file-disjoint from WIN's STAGE C code; user leans "wait for C")
No gate. Only if Mac wants forward motion before C lands; nothing here blocks WIN.
- **M0a — draft the STAGE F outward copy** (a NEW notes doc, so it never collides with
  WIN's `website/` source edits): v0.1.0 release notes; the capped 3-entry website
  "what's changed" log; the "what we're working on" roadmap/status update text; the
  DRAFTED follow-up X posts for the user to copy-paste (user posts X manually). WIN
  integrates these verbatim at STAGE F → faster release.
- **M0b — deep read-only review of WIN's STAGE B data-validity cluster** (`1bde16d9`):
  independently re-confirm the "ZERO out-of-extent coords ship" claim (the 3 aes
  parked-residual notes are non-shipping) + the candidate-cleanup count. Report on the
  board; Guard #6 anything off.

## M1 — Native-window `.dmg` de-risk (finding 7, HIGH)  ⟵ the priority Mac item
**GATE:** WIN lands + pushes the STAGE E shared edit — `launcher.spec` hiddenimports +=
`["webview.platforms.cocoa","objc","Foundation","AppKit","WebKit","Quartz","Security","CoreFoundation","UniformTypeIdentifiers"]`,
`dev/requirements-desktop.txt` = **plain `pywebview==6.2.1`** (NO `[cocoa]` extra — it
pulls pyobjc via darwin markers; cp314 wheels OK per Mac's pre-flight), and an EXPLICIT
native→browser fallback message. *(Can be pulled forward independent of STAGE C — WIN
may land it early; this is the small edit that unblocks Mac soonest.)*
**Steps:**
1. `git pull`; `pip install -r dev/requirements-desktop.txt` (confirm pyobjc resolves on this Py 3.14.5 via cp314 wheels — no source build).
2. Build a **TEST** dmg (NOT version-bumped, NOT uploaded) via `dev/build_dmg.sh`.
3. Launch the inner `YHWH.app`.
**Verify:** it opens its OWN native Cocoa window (NOT a browser tab) · dock icon =
`YHWH.icns` · window chrome + About show the app · then force the backend to fail and
confirm the EXPLICIT fallback message prints (not a silent browser open).
**Output:** pass/fail on the board. If the native window FAILS → diagnose the PyInstaller
bundling (which `hiddenimport` is missing) and hand WIN the exact module name (Guard #6).
Result = the de-risked recipe the M3 release dmg reuses.

## M2 — Device-QA verify of STAGE C
**GATE:** WIN lands + pushes the STAGE C EPUB (full-notes `ethiopian-tewahedo`, rebuilt).
**Steps:** sideload the rebuilt EPUB to Apple Books / the user's device.
**Verify each STAGE-C finding renders as intended** (on-device — paginated bugs are
invisible in a browser):
- **finding 3** — book title-page no longer bleeds onto the next page (WIN's fix: art
  `max-height:42vh` + frame `display:block; break-inside:avoid`).
- **finding 2** — "Your-Edition" stats page book-name column no longer clipped off-screen.
- **findings 1/1b/1c** — justify is the prose default (+ `hyphens:auto`); ToC/pills/
  headings/tables stay LEFT; the expandable pill ToC toggle (default ON); incipit chapter labels.
- **note-rehaul S1→S3a** — cascade verse→category→source→note; attribution de-dup
  (Ephrem ×3→×1); category→source grouping; tinted cards = enhancement only (backgrounds
  off → still clearly structured); the **legend popup** (Addendum A) pops same-file on tap.
- **finding 6** — desktop-app top-nav app-bar (if WIN ships it in C).
**Output:** per-finding pass/fail + screenshots. Any regression → straight back to WIN
(Guard #6) BEFORE the release; do not let it ride into F.

## M3 — v0.1.0 macOS release artifact
**GATE:** STAGE F — WIN bumped `VERSION`→`0.1.0` + created the `v0.1.0` GitHub release/tag.
**Steps:**
1. `git pull` (gets `VERSION=0.1.0`); build `dist/YHWH-0.1.0.dmg` (native-window deps +
   `.icns` baked, per M1's recipe).
2. Notarize + staple.
3. Upload to the `v0.1.0` release; merge its SHA256 line into `SHA256SUMS.txt`.
4. Point the website's macOS download button at `YHWH-0.1.0.dmg`.
**Verify:** `spctl -a -t exec` → accepted, source=Notarized Developer ID · `stapler
validate` ok · the release CDN URL is HTTP 200 at the exact byte size · also CONFIRM the
Win `.exe` / Linux AppImage native-window paths (master-plan cross-platform check).
**Output:** live v0.1.0 macOS artifact + checksum + site button, all verified.

## M4 — Final device confirm
**GATE:** full v0.1.0 shipped (all artifacts + site live).
**Steps:** smoke the SHIPPED dmg on the user's Mac (opens native window · loads the
console · builds one edition) + one final Apple Books pass on the shipped v0.1.0 EPUB.
**Output:** green sign-off, or a v0.1.x follow-up list.

---

## Dependency map (why the order)
```
WIN: …STAGE B✓ → STAGE C (note-rehaul+presentation, EPUB) → STAGE D (Win/Linux icons)
                    │                                         → STAGE E spec edit ──┐
                    │                                         → STAGE F (VERSION 0.1.0, release, site)
                    ▼                                                                │
MAC:   (M0 optional now) ── M1 native-window dmg de-risk ◀── STAGE E spec edit ◀─────┘
                              M2 device-QA verify ◀── STAGE C EPUB
                              M3 v0.1.0 dmg + upload + site ◀── STAGE F
                              M4 final confirm ◀── full ship
```
M1 unblocks as soon as WIN pushes the (small) STAGE E spec edit — WIN should land that
early so Mac can de-risk the HIGH finding-7 in parallel with STAGE C, not serially at
release. M2 needs the STAGE C EPUB. M3/M4 are release-time (STAGE F).

## Constraints (carried)
- Mac builds the dmg — only macOS can (`iconutil`/`build_dmg.sh`/notarize are macOS-only; Guard #4 parity).
- Never touch shared code WIN owns (Stages A–F impl) — Mac's surface is dmg-build + verify + (M0) NEW draft docs.
- Save cadence: local-commit during work, full push at each Mac milestone (M1/M2/M3 closes); `dev/save_mac.sh --before-push` radar-gates the protected-main push.
- No paid API / no rented VM; HDD-bound iMac → keep Chrome/render work light (one page at a time).
