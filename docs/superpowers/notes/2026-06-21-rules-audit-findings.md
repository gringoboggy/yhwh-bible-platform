# Rules-system deep-audit — verbatim findings (2026-06-21)

**Source:** the `rules-system-deep-audit` workflow (run `wav5b34sh`, 106 agents, each finding
adversarially verified). **47 confirmed findings** — radar-inconsistency 10 · cross-lane-parity 7 ·
redundancy 11 · loop-fit 10 · contradiction 3 · bloat 6.

This note preserves the synthesized proposal VERBATIM so the implementation session has the exact
edits. The sequenced execution plan is `docs/superpowers/plans/2026-06-21-rules-and-accuracy-consolidation.md`.

---

The key claims check out: §4:549-551 carries the "always wired on the background radar" leftover contradicting §4:528-537; HUMAN_DECISIONS.md does not exist; the backlog body has radar/no-stop residue contradicting its own header. I have enough to synthesize.

## Verdict

The rule system is in fundamentally good health — extraction discipline is working (~14 history pointers correctly off-loaded to RULES_HISTORY.md), the canonical-home pattern is mostly respected, and the de-bloat already landed. The radar story is **95% consistent**: every live surface (AGENTS.md, LANE_HANDOFF, PLAYBOOK, cc-hooks, §0, §4:528-537) now says seam-based, with exactly **one live holdout** — RULES §4:549-551 still says auto-pull is "always wired on the background radar," which self-contradicts the same bullet 14 lines above and is the single load-bearing fix; two lower-priority echoes remain in the v2 design spec (:49, :10) and AGENT_WORK_BACKLOG body. The work-phase loop slots in **cleanly as a pointer-only section** (the user-authored spec already exists in memory `feedback_autonomous_work_ladder`); it should consolidate-by-reference, never duplicate, and ship with a SAFEGUARD clause + the §4:550-551 fix in the same commit so step-1's pointer lands on clean text. The remaining drift is the predictable cost of stating the save cadence in 4-5 homes (it has already desynced — WIN memory + AGENTS.md carry the superseded pre-crash-safe model).

## Fix now (clear wins)

- [ ] **contradiction/radar (THE seed)** — `dev/CLAUDE_PROJECT_RULES.md:549-551`. Canonical = §4:528-537 (seam paragraph). Delete "via `lane_watch --auto-pull`, which is **always wired on the background radar** and checks…" → replace with: "the system **must** have already pulled at the seam — the save scripts' `lane_ping --before-push` + pull-rebase and `lane_watch.py --auto-pull` (the manual checker), when `HEAD..origin/main` > 0 and the tree is clean. There is NO background radar (removed 2026-06-20, per the §4 bullet above)." Keep :552-555 ("Agents must implement…/Partial or 'sometimes' implementations violate the rule") verbatim. Use "manual checker" to mirror AGENTS.md:110 exactly.
- [ ] **contradiction (save cadence digest)** — `AGENTS.md:346-347`. RULES §4:459-473 canonical. Rewrite first "Saving & git" bullet to the crash-safe cadence: local-commit micro-edits freely; after every coherent slice (gate green / truth-record edit / before risky-long job / before wrap) **SAVE = push both remotes autonomously, never ask, never wait**; never end with `ahead>0`; explicit save/push still forces immediate full sync. Reconcile with the (correct) :362-364 so the section is internally consistent. Keep "continue/proceed = advance not save" and the origin/github/E:/F: detail.
- [ ] **contradiction (E:/F: partial-save disposition)** — `dev/CLAUDE_PROJECT_RULES.md:168, :474, :505-509`. LANE_HANDOFF owns drive-location state. (a) §4 map row :168 + "WIN five-leg" :474 — append "(E:/F: legs STANDING-optional while drives are Mac-side — see LANE_HANDOFF; absence = expected skip, not partial)." (b) Reword :505-509 so a STANDING-deferred E:/F: drive is **not** "partial": "report as 'pushed to both remotes; E:/F: deferred (drives Mac-side)' — a complete save for the current state." (c) §3 quick-table :168 mark E:/F: conditional; §12 truth-gate :1412-1413 change "the git bundle exists on E:/F:" → "…WHEN drives are mounted (currently Mac-side → deferred; remotes-in-sync satisfies the backup claim)."
- [ ] **cross-lane-parity (Mac bootstrap baton model)** — `dev/cc-hooks/bootstrap-triad.sh:44, :54-57`. Canonical = RULES §4:512-527 (v2). Replace :44 "Baton rule: only the HOLDER pushes…" with the .ps1:29-30 v2 line: "Lane rule (v2): mode=parallel → file-disjoint, BOTH lanes push their own milestones; only the TRUTH_OWNER edits SESSION_STATE/IN_FLIGHT/CHANGELOG (see LANE_HANDOFF frontmatter)." Rewrite :54-57 heredoc to v2 framing, **keeping** /resume (picks up THIS lane's task; in parallel does NOT stop when truth_owner≠self), /handoff (transfers truth-ownership), /sync (milestone push). Leave the lane_handoff.py "incoming" references at .sh:76/.ps1:101 (truth_owner is the documented alias). Fix inverted .ps1:6 "Mirror of the Mac bootstrap-triad.sh" claim (.ps1 is now more current). Add a tiny lint/test asserting both hooks carry the same v2 substring ("v2" + "BOTH lanes" + "TRUTH_OWNER").
- [ ] **cross-lane-parity (Mac session-start ping missing)** — `dev/cc-hooks/bootstrap-triad.sh` (add after line 85) vs `bootstrap-triad.ps1:119-135`. Add a non-fatal, read-only, prints-only-when-BEHIND lane_ping block mirroring .ps1, using save_mac.sh:18's python resolution (`.venv/bin/python` then `python3`, NOT `py -3`). Rename both block headers `----- LANE SYNC RADAR -----` → `----- LANE SYNC PING (seam check) -----` and delete the stale .ps1:123-124 "hot-patched…never committed here" parenthetical (the block IS committed at HEAD). Until Mac re-installs + ACKs, mark the Mac session-start ping PENDING at LANE_HANDOFF.md:120 + spec:55 (Mac currently auto-pulls only at the save_mac.sh seam) — a rule must not assert what hasn't shipped.
- [ ] **radar (backlog body vs own header)** — `dev/AGENT_WORK_BACKLOG.md:20, :21, :23, :25`. Reconcile to the header (:3-7, already correct). :20 heading → "Recurring CHECKS (agent-run at seams, not daemonized)"; :21 delete "Trigger via radar or" (keep "post-work"); :22 delete "/self-gov"; :23 "(WIN always)" → "(WIN, on Mac-handoff)" and delete the "Windows…should always be looking…" quote; :25 delete "(no stop)"/auto-continue → "advance to the next §3-sequenced item, or stop clean if the session's real work is done" and "self-gov" → "rule-change". Keep :24 (rotate, a seam).
- [ ] **radar (v2 design spec)** — `docs/superpowers/specs/2026-06-08-lane-coordination-v2-design.md:49 and :10`. :49 "Mac uses `dev/save_mac.sh` + bg radar" → "Mac uses `dev/save_mac.sh --before-push` (one-shot `lane_ping` pull-rebase-on-BEHIND at the save seam; SessionStart `--quiet`) — no background radar." :10 "(`lane_ping.py` + `lane_watch.py --auto-pull`, always running)" → "…run at seams (save `--before-push` and session start, not a persistent process)"; "coordinated by the radar" → "coordinated by the seam checks." Live design-of-record; syncs on pull.
- [ ] **redundancy (auto-pull stated 5×, merge §4)** — `dev/CLAUDE_PROJECT_RULES.md:528-537 + 549-555`. Merge the two §4 auto-pull blocks into ONE: keep the tight seam statement at 528-537, fold in only the "user never utters the word / partial-implementations-violate" enforcement nuance, delete the duplicated trigger restatement. LANE_HANDOFF STANDING stays the both-lanes rule-of-record; AGENTS.md:106-110 + PLAYBOOK:19 already correct one-line pointers — leave them. (Same edit as the seed fix; do them together.)
- [ ] **redundancy (save cadence 4 homes → 1 + pointers)** — Canonical = RULES §4:459-509 (cadence doctrine + 5 legs). Reduce: LANE_HANDOFF:80-86 → keep only the cross-lane delta (E:/F:-with-Mac deferral, per-lane save commands) + "cadence doctrine = RULES §4"; PLAYBOOK §6.6:105-108 → keep order-of-ops + commands, cite §4 for triggers; AGENTS.md:346-364 → trim to digest one-liner + pointer (covered by the contradiction fix above). ~40-60 lines of duplicate trigger/leg prose collapsible.
- [ ] **redundancy (corpus count hard-coded, already rotted)** — `dev/SESSION_PLAYBOOK.md:11, :53, :82` + `dev/MATRIX_MAP.md:17-18, :136, :174` + `dev/REPO_MAP.md:36`. Canonical live count = SESSION_STATE.md (per RULES §1:299-307, which forbids hard-coding). Replace all "91,597 / 91,553" literals with "see dev/SESSION_STATE.md (source-corpus count; shipped = per-edition filter)". MATRIX_MAP:136 historical note → qualify as "as-of-2026-05-21 snapshot." (91,597 ≠ live 91,712.)
- [ ] **redundancy (pre-summary audit two shapes)** — `dev/CLAUDE_PROJECT_RULES.md:1393-1422` (5-point) canonical. PLAYBOOK §6.1:99 → replace re-enumerated "(a)-(d)" with "run the RULES §12 5-point audit"; fix the "§12 4-point" mislabel → "5-point"; **drop the hard-coded "≥7,667 collected"** (it will rot like the corpus count — use "test count matches the number the summary will claim"). PLAYBOOK §6.7:109 → merge back as point 5 by reference, keep only the Windows save-truth WARNING. AGENTS.md:362-364 = keep-but-sync to 5-point shape.
- [ ] **redundancy (clone-deletion gate, 2 homes)** — Canonical = `dev/SESSION_PLAYBOOK.md §6.5:104` (carries the executable git commands). Reduce `LANE_HANDOFF.md:74` to a pointer keeping only the user-directive provenance + per-lane mirror ACKs. Correct citation MEMORY.md:37 → :38.
- [ ] **redundancy (book-count cascade, 2 homes)** — Canonical = `dev/CLAUDE_PROJECT_RULES.md:141-158` (file paths). (1) ADD the missing "release notes / GitHub release body" surface to the RULES lettered list. (2) `AGENTS.md:282-285` keep the headline ("83; raw registry 87; never 87 publicly") but replace its parallel surface enumeration with "full list + paths: see RULES 'Corollary'."
- [ ] **bloat (frozen stats in every-session PLAYBOOK)** — `dev/SESSION_PLAYBOOK.md:11, :53, :82, :84`. Replace the ~7 rot-prone integers (91,597 ×3, 91,553, 32,263, 68 kinds, 15 categories) with structural invariant + SESSION_STATE pointer. **Scope-fence:** leave the structurally-stable "6 editions"/"6/6 schemas" (§5:78-80) and "83 shipped / 87 registry" untouched.
- [ ] **bloat (test-narrative history)** — `dev/SESSION_PLAYBOOK.md:62`. Move the ~290-word D.hang/D.slow + `_stub_exports_epubcheck` + retired-rule narrative to `dev/archive/RULES_HISTORY.md` (already mirrored in CHANGELOG mint-7 E2:1608). Replace with ONE live line on slow-tagged files + `-m "not slow"`; **avoid hard second-counts** (62/1608/1504 already mutually contradict).
- [ ] **bloat (plugin roster snapshot)** — `dev/CLAUDE_PROJECT_RULES.md:234-253`. Replace the 30-name roster (:236-247) + "16-official arithmetic slip" (:247-248) with the durable invariant (baseline 30 across 3 marketplaces; Mac 14; HARD BAR no telemetry/login plugins, gitkraken sanctioned; "the SessionStart ENV-HEALTH hook prints live `claude plugin list` — trust it"). Move/drop the slip sentence to RULES_HISTORY.md.
- [ ] **bloat (stale mini file-index)** — `dev/CLAUDE_PROJECT_RULES.md:1566-1575`. Delete the trailing file-index code block (duplicates REPO_MAP.md). **Precondition (same commit):** verify/add REPO_MAP entries for repo-root `HANDOFF_README_v7.md` and `dev/ROADMAP_FUTURE.md` first, then run `py dev/trace_repo.py` + the `repo_map_complete` lint.
- [ ] **loop-fit (placement — single canonical home)** — Add `§2.6 "Session work-phase loop"` to `dev/CLAUDE_PROJECT_RULES.md` after §2.5 (~30 lines, POINTER-ONLY). 8 one-line steps each ending in a §-pointer: (1) sync auto-pull-on-BEHIND at seam §4; (2) pick next advance §3 + IN_FLIGHT; (3) delegate cross-lane — WIN builds·Mac verifies `LANE_HANDOFF:122` + §2 principle 5 (≥2 lanes); (4) DEFER human-needs → `dev/HUMAN_DECISIONS.md` per guard #5(a); (5) advance autonomously guard #5; (6) marathon fallback (Phase-D Esther vision + Kings/Samuel) §2.5 + PLAYBOOK §7; (7) save crash-safe §4; (8) stop CLEAN — no-bg-runs-at-wrap. **SAFEGUARD clause (bold, mandatory):** "Runs ONCE per session, terminates at a clean quiescent stop — NOT continuous/self-restarting, no background watcher, no external poll, no auto-continue. Do NOT re-add agent_idle_radar.py / start_session_radars.ps1 / any always-on watcher (the removed Grok runaway pattern)." Add the §2.6 row to the Rules-map table (162-179). Sequence AFTER the §4:550-551 fix so step-1's pointer lands on clean text. Mirror as ONE pointer bullet in AGENTS.md prime directives + one pointer line at PLAYBOOK §4 WORKING (no step duplication). Collapse the inline ladder copies at MAC_WORK_QUEUE.md:31-34 + LANE_HANDOFF.md:7 to a §2.6 pointer.
- [ ] **loop-fit (create the DEFER sink)** — **CREATE `dev/HUMAN_DECISIONS.md`** (confirmed absent; already a dangling reference at MAC_WORK_QUEUE.md:33). Append-only table: date · what · why-needs-human · what-unblocks · raising-lane. Seed with current open human-decision items. Add it to REPO_MAP.md (the `dev/` dangling-path lint at lint_rules.py:1476-1483 hard-FAILs on the backtick path once §2.6/MAC_WORK_QUEUE cite it). Add a one-line pointer near guard #5(a) RULES:77. Clear the now-stale "loop is being added" notes at MAC_WORK_QUEUE.md:33-34 + LANE_HANDOFF.md:7 once it lands.
- [ ] **redundancy (MAC_WORK_QUEUE operating-model dup)** — `dev/MAC_WORK_QUEUE.md:3-8` restates "WIN builds·Mac verifies" verbatim from `LANE_HANDOFF.md:122`. Collapse :3-8 to a one-line pointer to the LANE_HANDOFF STANDING section; keep only Mac's current ≤3-item scope (:10-21).

## Mac self-audit

These touch Mac per-box memory (not readable from Windows) or require the Mac box to re-install/ACK. File each as a LANE_HANDOFF rule-change-parity task (LANE_HANDOFF.md:124):

- [ ] **Save cadence (HIGH, demonstrated desync):** Mac's `reference_save` / doctrine memory was mirrored from WIN's stale "local-commit-until-milestone" model — confirm it now states the crash-safe push-after-every-slice cadence (never end with unpushed work), NOT the superseded 2026-06-08 bandwidth-first model.
- [ ] **Lane-coordination model:** confirm no Mac per-box memory still encodes the single-baton "only the HOLDER pushes" model; it must carry v2 (mode=parallel, both-lanes-push, truth_owner).
- [ ] **Mac bootstrap re-install:** after the `bootstrap-triad.sh` v2-banner + lane_ping-block edits land in-repo, Mac pulls, `chmod +x`, re-runs SessionStart, verifies the printed banner + that the ping fires, ACKs in LANE_HANDOFF.
- [ ] **Radar-language sweep:** confirm no Mac per-box memory (auto-pull / lane_ping family) carries "background radar" / "always running" phrasing; align to seam-based wording.
- [ ] **§2.6 loop + HUMAN_DECISIONS.md + SAFEGUARD:** when §2.6 lands, Mac mirrors the unified work-phase loop + SAFEGUARD into per-box memory (consolidating its `feedback_autonomous_work_ladder` mirror) and re-points its memory at the now-existing `dev/HUMAN_DECISIONS.md`; ACK.
- [ ] **Stale-literal sweep:** when mirroring the de-bloated rules, confirm no Mac-side doc carries the stale 91,597/91,553 corpus literals.
- [ ] **Mac RAM-hygiene at session start (parity gap):** the WIN bootstrap prints a RAM-clear block (.ps1:52-60) the Mac (.sh) lacks — the Mac is the more constrained 8GB box. Add a Mac-tuned RAM-hygiene line to the `.sh` triad heredoc (browser MCP OFF with VS Code open per guard #6; end-task Chrome/Kindle between slices; one GUI app at a time) — do NOT copy the Windows PROTECT/KILL process list; record as a local OS-specific mechanism override.

## Propose to maintainer (judgment calls)

- **DEFER sink identity:** §2.6 step 4 needs a durable queue. Recommend creating `dev/HUMAN_DECISIONS.md` (already a dangling reference). Alternative: reuse IN_FLIGHT's deferred/HOLD items as the queue and only fix the dangling MAC_WORK_QUEUE:33 reference. Your call — a dedicated file is cleaner and answers the "what do you need from me?" check-in directly, but it's one more tracked artifact.
- **Regression guard for "background radar":** optional pre-commit `lint_rules.py` guard flagging the literal "background radar" outside a negation/"removed" context and outside `dev/archive/` + CHANGELOG.md. Cheap insurance against the seed regressing, but it's net-new lint surface — include only if you want the belt-and-suspenders.
- **Marathon fallback scope (step 6):** the memory ladder also wants step 6 to proactively stage Kobo `.kepub` + desktop device-test artifacts on a verified tree (it's the v1.0.0-gating QA path). Confirm whether that staging belongs in the loop's fallback or stays a separate queued item — it widens "fallback" beyond pure transcription.
- **Whether to ship the loop at all this pass:** every loop-fit finding is moot if §2.6 doesn't land. The consolidation fixes (auto-pull merge, save-cadence collapse) are prerequisites that make the loop a thin orchestrator over single-sourced rules. Recommend landing the radar/contradiction fixes + consolidations FIRST, then §2.6 in a follow-up commit on the cleaned base.
