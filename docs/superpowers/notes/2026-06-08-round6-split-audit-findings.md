<!-- Round-6 SPLIT deep-audit, merged on the N95 (win 4 dims + mac 14 dims, both on Opus).
     win 13 + mac 30 -> 43 unique survivors. bySeverity={'medium': 12, 'low': 26, 'info': 5}; bug=35 opt=8. 0 critical/high. now=2026-06-08.
     FINDINGS-ONLY artifact (the synthesized fix plan); fixes HELD pending the master plan. -->

# Round 6 Deep-Audit — Merged Fixes Plan (win + mac, 2026-06-08)

## Executive summary

The split audit (win lane = pytest/builds, mac lane = 14 read-only code-review dims) merged to **43 unique surviving findings: 12 medium, 26 low, 5 info** — 35 bug/correctness/security/debt/test/doc/rx items plus 8 optimization recommendations. The most serious are not shipped-output corruption but **a red test suite**: the v0.0.3 canon-fold (87→83/86) and the Phase-3 font-embed/lint/route changes landed in source without updating their assertions, so ~16 tests across 8 files now fail and three real-but-bounded latent defects exist (the `aes` coordinate-guard no-op, the `edition_stats` stale-cache twin of mint-9 #10, and the file-splitter orphaned "Notes" heading in the default shipping edition). **The 9-edition KJV program is functionally MINT** — every confirmed defect is either test-only, a latent guard that never fires on current data, a reader-cosmetic artifact, dev-tooling, or stale docs; no finding corrupts shipped bytes and none touches the marathon core. What stands between the repo and a green-suite, fully-honest MINT state is: (1) re-aligning the stale tests/docs to the shipped v0.0.3 reality, (2) closing the three latent runtime/promote-guard holes additively, and (3) one reader-visible file-split cosmetic fix with a byte-stability regen-diff. All fixes are additive or test/doc-only; none require touching `build_standalone.py`, `core/manuscript_*`, or `GAPS/`.

## Phased fixes

### Phase 0 — Add commit-time lint guards (foundational, additive, ship FIRST so later phases can't regress)
These add invariants that mechanically catch the recurring drift classes before touching any behavior.

- [ ] **(low, debt) Extend `check_atomic_writes` to flag `.write_text(`/`.write_bytes(` on `content/notes/` paths** — `scripts/lint_rules.py` `_find_open_write_calls`/`check_atomic_writes`. The current AST walk matches only `open(...,'w')`, which is exactly why the Phase-3 finding below slipped through. Guard: a lint test in `tests/test_lint_rules.py` planting a `.write_text` under `content/notes/` and asserting it fails. No build-path touch.
- [ ] **(low, data) Add a repo-wide candidate-extent lint** — assert no `pending` candidate coordinate in `content/candidates/*.json` fails `coord_in_canonical_extent`. Hosts the regression guard for the Phase 3 cleanup below so the 111-orphan class cannot silently re-accumulate. No build-path touch.
- [ ] **(low, tests) Couple the route-table count to its inventory** — fold the `/note-override` presence check into the same guard as the PUT-count pin (see Phase 1) using `any("/note-override" in r.pattern ...)` rather than a positional index. No build-path touch.

### Phase 1 — Test/doc re-alignment to shipped v0.0.3 reality (test-only, NO build-path, NO byte-stability obligation)
All of these are stale assertions/prose; the SOURCE is correct (the v0.0.3 canon fold, Phase-3 font embed, date-roll, route add are deliberate features). Do NOT touch `canons.yaml`, `editions.yaml`, `style_config.py`, `lint_rules._RETIRED_TERMS`, `gen_website_progress.py`, or any registry.

- [ ] **(medium) Canon book-count assertions hardcode pre-fold 87/76/78** — `tests/test_scripts.py:826-831,1958,2153-2154` + `tests/test_v1_console_polish.py:303-312`. Fix per verifier: catholic 76→75, orthodox/eastern-orthodox 78→77, ethiopian/coptic-orthodox 87→86, anglican-bcp 76→75; update line 2154 (ethiopian covers) to 86; fix the prose/docstrings at `test_v1_console_polish.py:304-307` and `test_scripts.py:1952,2146-2147`; add a self-documenting comment at `test_scripts.py:826` citing the aes-drop. **Do NOT change the `==87` registry assertions at `test_scripts.py:1389,1823`** (those track the raw 87-book `books.yaml` superset and correctly pass). Leave the 13 cascading `TestPsi7A` ERRORS alone (fixture-ordering artifact, separate).
- [ ] **(medium) `EMBED_FONT_PATHS == []` pins the retired Π.0 contract** — `tests/test_parallel_bible_pi0.py:305-319`, `pi1.py:565-570`, `delta1.py:416-420`. Migrate each `==[]` to assert the Cardo + Noto Serif Ethiopic families are present (mirror `phi1.py:332-352`), or rename to `..._populated_at_rx_phase3`. **Do NOT** touch the `monkeypatch.setattr(...,[])` sites in `phi1.py:214/262` (they legitimately exercise the empty path). Optional cosmetic: update stale `=[]` docstrings in `pi1.py:46`, `pi1b.py:60`, `delta1xa0.py:45`.
- [ ] **(medium) comm-reformation "filtered out of ethiopian-tewahedo" is stale** — `tests/test_scripts.py:1149-1160`. Switch `ed_id` to `"jewish-study"` (keep `kind="comm-reformation"`): it has potential==5, enabled==0, so the invariant is genuinely exercised (the verifier proved `comm-ai`/ethiopian would be a vacuous no-op). Update the docstring; optionally add an explicit `assert m.potential[ed_id][kind] > 0` precondition.
- [ ] **(medium) Date-rolled retired-term test** — `tests/test_lint_guardrails.py:179`. Remove `"2026-06-07"` from the expected set, leaving `>= {"hard deadline", "sonar"}`. Source (`lint_rules.py:1827`) is correct.
- [ ] **(medium) website-progress asserts 87, generator ships 83** (BOTH lanes flagged this — single fix) — `tests/test_website_progress.py:30,33,44,80`. Set the four literals to 83 / `83*2`; fix comments at lines 27/32. Optional robustness: derive `expected = len(config.load_books()) - len(gp._SUPERSET_EXCLUDE)` in-test. **Do NOT** touch `gen_website_progress.py`.
- [ ] **(low) PUT route table pinned at 10, now 11** — `tests/test_web_routetable.py:1404-1410`. Bump assertion 10→11; drop the stale "floor is 10" comment; add the `/note-override` presence assertion (couples to Phase 0 guard).
- [ ] **(low) corpus-index vs matrix equivalence assumes ethiopian is a perfect superset** — `tests/test_corpus_index_delta.py:286-328`. Relax line 328: either restrict both sides to the ethiopian canon book list, or assert `idx_count >= matrix_count` (index is a documented superset). Cite the aes drop (86 not 87; aes's 82 lang-hebrew notes = the delta) in the comment. **Do NOT** add aes to the ethiopian canon.
- [ ] **(low) 2es title assertion expects 'Sutu'** — `tests/test_parallel_bible_tau7xj.py:145`. Keep line 144 (`ch_count==16`); replace line 145 with `assert rec["title"] == "The Second Book of Esdras"` (or `"Esdras" in rec["title"]`). **Do NOT** assert a "native-name field" — none exists in the 2es record.
- [ ] **(low, docs) REPO_MAP website row stale** — `dev/REPO_MAP.md:26`. Rewrite: built by `website/build.mjs` (Node, stitches `src/*.html`+`partials/` → `dist/`, emits CNAME/.nojekyll/robots/sitemap; releases card upgrades via `releases.js`); deploys to **GitHub Pages** (`gringoboggy/yhwh-website`, www.yhwhyaway.com). Drop "planned `build_site.py`" + "GitLab Pages"; list actual root assets. Docs-only.
- [ ] **(low, docs) REPO_MAP calls 7 baked translations "gen.py only"** — `dev/REPO_MAP.md:35`. Reserve "gen.py only" for `lxx-brenton-{english,greek}`; mark wlc 39, jps 39, lxx-swete 50, byzantine 27, douay 74, vulgate 77, arabic 66 (cite MATRIX_MAP for per-source coverage). Fix `geez-tewahedo (33)`→`(36)`. **Do NOT** do the ex→exo rename (separately deferred).
- [ ] **(low, docs) REPO_MAP dead `.manifest.json` pointer** — `dev/REPO_MAP.md:34`. Delete the trailing `` + `.manifest.json`. `` clause; the 88-files / 91,733 totals are correct.
- [ ] **(info, docs) REPO_MAP prose counts stale** — `dev/REPO_MAP.md:17,19,43`. Refresh: 219 pytest files, 48 plans, 25 specs, 136 scripts. **Do NOT** change line 54 (`21 ship_scripts` is correct — the finder miscounted README.md). Better durable option: soften to `~`/ranges. Re-run `py dev/trace_repo.py` to confirm.

### Phase 2 — Latent guard / robustness holes (additive; one promote-time semantic change, NOT a build-path change)
These do not corrupt current output (current data is clean / not re-validated), but they defeat the guard's documented intent. All confined to promote-time / display-cache / authoring tooling — no shipped EPUB byte change.

- [ ] **(medium, data-validity) `aes` coordinate guard is a complete no-op** — `scripts/core/canonical_verse_counts.py:138-151`. PART 1: make `_book_shape_cached` robust to a non-1-start skeleton — scan `range(1,201)` and `continue` (not `break`) on empty chapters (prefer a single-read variant; `load_kjv_skeleton` re-reads per chapter). PART 2 (verifier-corrected, the finder's was wrong): change `html_chapter_count` to return `max(shape)` (==16 for aes), **NOT** `len(shape)` (==7, which would over-reject every real aes ch10-16); byte-identical for the 80 contiguous books. Leave `canonical_chapters` as `len()`. Guards: fix the false "scaffold-only" comment at `tests/test_canonical_verse_counts.py:36`; pin `canonical_book_shape('aes')` non-empty {10..16}; in `tests/test_promote_html_chapter_guard.py` assert `coord_in_canonical_extent('aes',99,1) is False`, `('aes',16,24) is True`, `html_chapter_count('aes')==16`, promote ch17 rejected / ch11 accepted. **No build-path touch** (promote-time only; aes notes never re-validated).
- [ ] **(medium, concurrency) `edition_stats` cache stale after a runtime note edit (mint-9 #10 twin)** — `scripts/core/notes_io.py` `_invalidate_corpus_index_if_notes_file`, immediately after `_matrix.compute_matrix.cache_clear()`: add `from scripts.core import edition_stats as _edition_stats; _edition_stats.cache_clear()` inside the same try/except (DAG-safe — edition_stats never imports notes_io). Guard: after `notes_io.atomic_write`/`write_book` to a `content/notes/<book>.py`, assert a prior `resolved_note_counts(ed)["total"]` recomputes without an explicit `cache_clear`; ideally also assert the in-process `build_one` bakes the fresh count. **No shipped-byte change** (build recomputes; EPUB cache key is separate).
- [ ] **(low, rx) Two re-ingest scripts write the notes store with bare `write_text`** (RULES §7) — `scripts/_reingest_greek_glosses.py:136,146` + `scripts/_reingest_torrey_topics.py:195,205`. Add `from scripts.core import notes_io` (mirror `_reingest_eastons.py:48`) and replace each bare write with `notes_io.ensure_backup(p); notes_io.atomic_write(p, new_text)`. The two source-store writes (136/195) are the real exposure; base writes (146/205) match for uniformity. Closed by the Phase 0 lint guard. No build-path touch.
- [ ] **(low, correctness) `prospect.discover_verses` truncates at first mid-chapter verse gap** (two findings, same site — single fix) — `scripts/prospect.py:63-74`. Replace `break` with hole-skipping; verifier-corrected to **stop after N consecutive misses** (e.g. `misses>=5`) rather than an unconditional 1..199 scan (each `find_verse_text` re-globs every HTML file). Verified real gaps in 1En (e.g. 1en 89 = {1-57,68-76}). Fix the stale "~150" comment (Psalm 119 = 176). Authoring-only — `content/candidates/` is not a build input; byte-identical candidate output on current contiguous base. `discover_chapters` verse-1-only fragility = note as secondary, leave as-is.
- [ ] **(low, cross-module) `NavesTopical/TorreyTopical.verses_for()` don't normalize stored book codes** (★BUGCLUSTER defense-in-depth gap) — `scripts/core/sources_lexicon.py:306,382`. Set `target_book=_normalize_book_code(r[0])` at both sites (helper already imported line 19; no-op on canonical data, mirrors `detectors.py:399`). Byte-stable on current clean data. No build-path touch.
- [ ] **(low, data-validity) 111 out-of-extent candidate artifacts in `content/candidates/`** — one-shot cleanup over all 1605 files (39 fully-orphan deletions + 43 partial in-place rewrites = 82 touched): drop every candidate failing `coord_in_canonical_extent`, rewrite `n_candidates`, re-serialize via `notes_io.atomic_write` with `json.dumps(indent=2, ensure_ascii=False)` to avoid churn, delete zero-candidate files. Guarded by the Phase 0 candidate-extent lint. **No shipped-Bible impact** (never promoted; promote-boundary already drops them).

### Phase 3 — Behavior-changing reader fix (TOUCHES BUILD PATH → byte-stability proof obligation)

- [ ] **(medium, rx) File-splitter leaves an orphaned "Notes" heading + `<hr>` shell, strips the heading from every per-piece notes block** — `scripts/build_edition.py:2222-2261` (default `reader_file_split=True`, ships). Two coordinated edits: (1) broaden `_EMPTY_NOTES_SECTION_RE` to also consume the leading `<hr class="notes-rule"/>` + `<h3 class="notes-heading">Notes</h3>` boilerplate; (2) add that boilerplate into `_NOTES_SECTION_OPEN` so each piece keeps its heading. Guard: add a SYNTH/NESTED fixture whose notes-section includes the real `<hr>`+`<h3>Notes</h3>` and assert (a) no piece has an orphan notes-section shell and (b) each piece with asides has exactly one `notes-heading`. **BYTE-STABILITY PROOF OBLIGATION:** regen the 9 KJV editions and `git diff` the split output — if a KJV edition has `reader_file_split` enabled, edit #2 correctly adds the heading to its pieces (accept that diff via the regen-diff invariant since it matches base boilerplate); editions opting out of file-split stay byte-identical. `epub_working/` is not mutated (splitter runs on the per-edition temp tree), so no nested-anchor base check needed.

### Phase 4 — Dist / website-deploy fixes (release tooling + public copy; no EPUB bytes)

- [ ] **(medium, website-deploy) Home page says beta "almost here"/"will post very soon"** (both lanes; live on production) — `website/src/index.html:25,330-331`. Rewrite to present-tense "live" copy matching `releases.html`; then `node website/build.mjs` to regenerate `dist/`. Copy-only.
- [ ] **(medium, dist) `gen_checksums.py` DEFAULT_EXTS omits `.epub`** — `scripts/gen_checksums.py:26`. Add `.epub` before the archive formats (matches both `.epub` and `.kepub.epub` via `endswith`). Guard: `tests/test_gen_checksums.py` adds `.epub`+`.kepub.epub` artifacts and asserts both are picked up. Also fixes the second caller `dev/notary_autofinish.sh:67` automatically.
- [ ] **(medium, dist) `notary_autofinish.sh` hardcodes the retired `YHWH-1.0.0-beta.1.dmg`** — `dev/notary_autofinish.sh:22`. Replace with `VERSION="$(head -n1 "$REPO/VERSION" | tr -d '[:space:]')"; DMG="$REPO/dist/YHWH-${VERSION}.dmg"` (mirror `build_dmg.sh:38-40`). NOTE: line 45 short-circuits on `STATE: DONE`, so re-running won't self-correct `NOTARIZATION_STATUS.md` — hand-correct the stale `Artifact:` line to `dist/YHWH-0.0.3.dmg` (or re-arm off DONE). macOS-only tooling.
- [ ] **(low, dist) `build-linux.yml` workflow_dispatch default tag is the retired `v1.0.0-beta.1`** — `.github/workflows/build-linux.yml:17`. Set `default: "v0.0.3"` (input stays `required: true`); refresh the stale "initial scaffold" header. CI-metadata only.
- [ ] **(low, website-deploy) EN back-translation row-count regex undercounts ruff-formatted stores** — `scripts/gen_website_progress.py:144`. Replace the line-regex with a structural `ast.parse` count of `len(VERSES)` (verifier prefers ast over importlib — no import side effects; the finder's alt-regex is fragile). Latent today (`en` not yet shipped/fetched). Guard: multi-line ruff-style fixture asserting a ≥50-row book lights EN and a <50-row book doesn't. Confined to the website-deploy script; KJV editions unaffected.

### Phase 5 — Dead-code / test-honesty / debt cleanups (test-only or off-build-path)

- [ ] **(low, tests) promote `html_chapter_count` guard is dead AND its tests tautological** — `scripts/promote.py:472-478,371-374`; `tests/test_promote_html_chapter_guard.py:42-114`. Recommended option (b): delete the redundant guard at both sites (coord_in_canonical_extent already subsumes it) and the 4 tautological tests; at minimum drop the never-fail `html_chapter_count('gen')==canonical_chapters('gen')` at line 42; add a one-line comment documenting coord_in_canonical_extent as the sole chapter-extent boundary. **NOTE:** this interacts with the Phase 2 aes fix — if Phase 2's max-based `html_chapter_count` is adopted, the guard becomes live for aes; sequence Phase 2 first and decide whether to keep the (now-functional) guard or rely solely on the coord check. Do NOT attempt the finder's option (a) real-extent-table this round.
- [ ] **(low, tests) ★BUGCLUSTER coord-guard sweep test is substring-only** — `tests/test_mint11_phase3.py:86-92`. Extract each driver's per-candidate extent decision into a tiny importable `_emit_extent_ok(book,chapter,verse)` helper and unit-test drop behavior (gen 1:1 kept, gen 99:1 / gen 23:24 dropped) for all 4 drivers; keep the substring check as a cheap secondary. Additive test-only.
- [ ] **(low, tests) coverage count-pattern test pins a private regex COPY** — `tests/test_inject_aside_id_high_chapter.py:82-105`. Add a module-level `_compile_injected_id_re(id_prefix)` to `scripts/coverage.py`, have `count_injected` (line 147) call it, import it in the test, delete `_coverage_pat`. STRONGER (do in addition): a behavioral test writing a synthetic `id="note-1e10001"` and asserting `count_injected` reports the ch-100 count. Off-build-path (coverage is a QA tool).
- [ ] **(low, rx-tests) Greek/Easton/Torrey lint guards lack a negative "fires-on-defect" test** — add per-guard negative+positive tests reusing `tests/test_lint_rules.py::TestNoReviewerScaffoldingDetection` (plant a synthetic notes store under tmp_path, `monkeypatch.setattr(lint_rules,"REPO",tmp_path)`). Prioritize `check_no_truncated_easton` (currently UNTESTED). Test-only; do not modify the guard predicates.
- [ ] **(low, debt) `run_hebrew_at_scale.py`/`run_greek_at_scale.py` ~95% clones** — hoist a `run_word_detector_for_book(...)` + parametrized `main()` into the existing `scripts/core/at_scale_base.py` (lazy-import translations/config; pass the detector INSTANCE so the leaf module never imports `detectors.py`). Keep thin CLI files; pin an identical-stats test; keep `test_corpus_chi1.py::TestNTBookLanguageInvariant` green. Offline tooling — gates nothing.
- [ ] **(low, debt) `run_ai_notes_at_scale.py`/`run_ai_xrefs_at_scale.py` near-clones** — hoist `run_ai_detector`, `build_ai_arg_parser`, `run_ai_driver_main` into the existing `at_scale_base.py`, parametrizing detector/client classes, `--kind` hint, the differing extra arg (`top_n`/`tradition`), `COST_PER_VERSE` (0.0023/0.0020), and `--min-confidence` default (0.7/0.65). Extend the injectable-core tests; verify parallel-output-identical + merge-not-clobber stay green. Offline tooling.
- [ ] **(low, debt) Dead helpers in `book_native_names.py`** — delete `native_name` (233-250) + `format_toc_entry` (365-377) **AND** the now-orphaned `_BOOK_CHAPTER_SEP` constant + comment (291-295, verifier addition). Keep `chapter_label_for` (live). Off-build-path; no test references these.
- [ ] **(info, correctness) `_append_cloned_edition` serializes numeric `0` as `""`** — `scripts/api/editions.py:168-169`. Keep the bool identity checks first, add an `isinstance(fval,(int,float))` branch emitting the bare literal before the empty-string branch. Latent (no numeric scalar field today). Note the finder's round-trip claim was wrong (`""` reads back as `""`, not None) — fix still correct. No live blast radius.
- [ ] **(info, data) `TEWAHEDO_DISTINCTIVE_NO_KJV` incomplete (omits 1cl/2en)** — `scripts/core/canonical_verse_counts.py:135`. Add `"1cl","2en"`; **MANDATORY companion (finder omitted):** update `tests/test_canonical_verse_counts.py:49` `==6`→`==8` (or convert to a partition-coverage assertion against `render_coverage._CANONICAL_BOOKS`). Update the doc comment at 36-38. Do NOT adopt the finder's filesystem-derive option. Stores are empty → deferring until 1cl/2en receive notes is also acceptable.
- [ ] **(low, render) `render_coverage --pretty` prints the two EN rows twice** — `scripts/render_coverage.py:281-292`. Delete the duplicate pair (287-292); add a one-line test asserting each `(back-translation)` label appears once. CLI-only.
- [ ] **(low, render) `render_coverage --pretty` mislabels Patrologia as "(ingest pending)"** — `scripts/render_coverage.py:296-298`. Change suffix to "books rendered from PDFs (own-versification re-ingest is a separate pending lane)". Optionally derive the count from actual render state. CLI-only.

## Optimization decisions

| Area | Verdict | Recommendation |
|---|---|---|
| All-9 cold build parallelism (`build_edition.py:4668-4677`) | **Change** | Switch `--all` cold path to `ProcessPoolExecutor` (build_one is process-safe; private tempdir + per-edition cache). Add `--jobs` defaulting `min(len(targets), os.cpu_count()//2 or 1)` = **2 on the N95** (honor soldered-16GB/OOM cap). Keep serial fallback for `--jobs 1` and **always when frozen** (PyInstaller can't spawn a Python child). Threads at `min(5)` over-subscribe a 4-core box. **Byte-stability proof:** regen `--all` (pool) vs `--all --no-parallel`, normalize `dc:date`/`dcterms:modified`/filename ts, compare per-member zip digests — must be byte-identical for all 9. No paid API. Effective speedup ~1.5–2× at 2 workers. |
| `compute_cache_key` default version `"v28a"` (`build_cache.py:242`) | **Change** | Make `version` a required keyword-only arg (sole production caller already passes it); update the ~30 test call sites to pass an explicit `version="v27"`. Turns a silent phantom-version cache MISS into an immediate TypeError. No byte-stability impact (key only selects a cache entry; a miss rebuilds correctly). |
| Torrey at-scale re-run stat double-count (`run_torrey_at_scale.py:108-115`) | **Change** | Mirror the 6 sibling drivers + `run_ethiopian` (89-97): count `candidates_written` only on a real write (`if out:`); dry-run keeps the pre-dedup figure. Operator-facing stat only; no corpus/byte change. |
| PO Patrologia vision: 1 transcriber + 1 reviewer (`2026-05-28-...-vision-plan.md:73,269-270`) | **Change (doc)** | Formalize the convergence model the lane already uses: 2 independent blind Opus passes → deterministic `converge_passes()` (reuse `manuscript_converge.py` UNCHANGED — pure/book-agnostic) → controller adjudicates only `divergent_loci`. Verifier addition: also re-verify load-bearing glyphs on AGREEING verses (note (v): two blind passes can converge on the same wrong glyph). Doc-only; Max agent path (no paid API); geez-tewahedo feeds only `build_standalone` so 9 KJV bytes untouched. |
| Standalone EN attachment alignment (`build_standalone.py:94-105`) | **Confirmed-optimal now; change-when-live (recommend-only)** | **Marathon-core off-limits → no edit now.** Current 4-book proof stores mirror Ge'ez order exactly (CONFIRM-OPTIMAL). When the standalone EN lane goes live, share one `_is_parallel`-style length+verse-sequence predicate between `build_standalone` and `gen_website_progress`, gating EN attachment per chapter and falling back to Ge'ez-only on mismatch. No paid API; byte-stable (9 KJV editions never route through `render_chapter_body`). |
| Per-step auto-commit + `/clear`-between-heavy-steps (`2026-05-17-kings-...md:12-13`) | **Confirmed-optimal — keep** | OOM is RAM/allocator (stacked multi-MB image buffers), not context length → 1M context does not remove it. On-disk accumulator + manifest + per-step LOCAL commit + MAX-1-heavy remain the right durable, deterministic-resume design (empirically recovered 2 crashed sessions). No change; composes with the local-commit-until-milestone cadence. |

## Constraints carried
- **Never touch the marathon core:** `build_standalone.py`, `core/manuscript_*`, `core/po_vision_store.py`, `content/manuscript/`, Patrologia source stores, `GAPS/`.
- **9 KJV editions MUST build byte-stable:** every behavior-changing fix that touches the build path (only Phase 3) carries an explicit regen + `git diff` per-member zip-digest proof obligation, normalizing `dc:date`/`dcterms:modified`/filename ts. Test/doc/CLI/promote-time/authoring fixes carry no byte obligation (verified non-build-path).
- **Additive schema only; KEEP PYTHON / no DB.** No registry edits (`canons.yaml`, `editions.yaml`, `books.yaml`, `kinds.yaml`, `style_config.py`) — the sources are correct; only stale tests/docs move.
- **Atomic writes:** all notes-store writes go through `notes_io.ensure_backup`+`atomic_write`; the Phase 0 lint extension enforces this for `.write_text`/`.write_bytes` too.
- **No paid API; no rented cloud VM** — all vision/build work stays on the two owned machines (N95 worker cap = 2; Mac read-only review lane).
- **5-leg save per phase milestone:** during work = LOCAL COMMIT ONLY; full push (local + GitLab + GitHub + E: + F: bundle via `save-all.ps1`, `lane_ping --before-push`) only at a milestone. Run `ruff format` on any regenerated stores before commit.
- **Single-user LOCAL app:** no CSRF/rate-limit/hosting findings raised. **Geez/Amharic standalones = a FUTURE update, out of scope** (the standalone-EN alignment item is recommend-only, deferred to when that lane goes live).
- **FINDINGS-ONLY this round:** this is the plan; no fixes applied.

## Completeness gaps (next-round finder lenses)
- **Font-embed seam: patch_opf_fonts (win-lane build path) × build_standalone.py + manuscript core (mac read-only marathon-boundary) — NO lane proved the v0.0.3 populated EMBED_FONT_PATHS builds byte-stable/epubcheck-clean on a REAL tree** — style_config.EMBED_FONT_PATHS was intentionally populated in Phase 3 (2026-06-05) with Cardo (3 .ttf faces) + NotoSerifEthiopic-Regular.woff2 (unicode_range U+1200-137F). It is consumed by patch_opf_fonts in BOTH scripts/build_edition.py:3652 (win lane: opt-build/byte-stability/rx-surfaces) AND scripts/build_standalone.py:275 (the OFF-LIMITS marathon core — mac-lane marathon-boundary dim ONLY runs `git log`/`git status`, it never builds). The actual byte-stability gate, tests/test_epub_zip_determinism.py:11, explicitly 'never touches the real epub_working/ tree and never runs a real build' — it only varies mtimes on a synthetic tree, so it can NOT observe (a) whether the woff2 font/font/woff2 media-type registers correctly, (b) whether @font-face has TWO divergent sources of truth — the hand-authored epub_working/stylesheet.css (lines 18-44, the live one) vs apply_style.py's generator with unicode-range support (lines 145-148, whose managed region style_config.py:90 says is STALE 'do NOT run'), (c) whether every @font-face url() is OPF-declared after the FULL post-pass chain (epubcheck RSC-008 undeclared-resource). A populated font list shipping in v0.0.3 means the 'byte-identical when empty' no-op comments throughout are no longer the live path, yet the only byte test still tests the empty path. The font bytes DO exist on disk (epub_working/fonts/), so this is about pipeline correctness, not missing assets.  _Lens: Actually BUILD eth + catholic-study + the 9 KJV editions twice each on the N95 SSD and diff the bytes (git diff / cmp); run epubcheck --jar on each to confirm zero undeclared-resource / RSC-008 from the woff2 + Cardo @font-face url()s; then DIFF epub_working/stylesheet.css's hand-authored @font-face block against what apply_style.py would emit from EMBED_FONT_PATHS — if they disagree on family names / unicode-range / which faces, the build ships one and validates the other (a latent device-only defect). Confirm the woff2 entry maps to media-type font/woff2 in the final OPF (not application/octet-stream)._
- **EN back-translation 1:1 alignment seam: build_standalone.en_occurrence_map (off-limits core, mac read-only) × web.py reader's alignment guard (win-lane testable) — neither lane RAN the standalone to observe a row-count mismatch drop** — The merged findings flag this only as 'OPTIMIZATION (recommend-only)': scripts/build_standalone.py:199-215 attaches EN by (verse, occurrence) key WITHOUT the 1:1 alignment guard the live website reader enforces. This is a true cross-lane blind spot: build_standalone.py is the off-limits marathon core (mac-lane marathon-boundary only greps git log, never executes it) AND it is a build (which is the win lane's job, but the win lane only builds the 9 KJV/Ethiopian editions, never the standalone — the standalone is the FUTURE out-of-scope update). So a chapter where geez-tewahedo-en has MORE or FEWER rows than the geez-tewahedo source silently attaches whatever matches by (v,occ) key and DROPS the unmatched EN/Ge'ez — exactly the silent-data-loss class the correctness dim hunts, but on a module no lane executed. The opt-render dim (mac) read it but read-only; the win lane never built it. The HaCohen Psalter dup-verse mis-numbering (Ps 36 verses mis-numbered 24/25/24/25, cited in the docstring) is precisely the misalignment-prone data.  _Lens: On the N95, RUN build_standalone for geez (read-only to the source, just observe) OR write a standalone harness that, per book/chapter, compares len(geez source verses) vs len(EN occurrence map) and reports every chapter where the (v,occurrence) keyspaces differ — count how many EN paras or Ge'ez verses would be silently dropped. Cross-reference against web.py's render_chapter_body alignment enforcement to confirm the web reader would have flagged/handled the same chapters the standalone silently drops. Recommend-only output (don't edit the core), but QUANTIFY the drop so the future-update plan knows the blast radius._
- **_book_shape_cached algorithm fragility (the aes no-op ROOT) is a CLASS, not just aes — break-on-first-missing-chapter + the early-return-True-on-empty-shape double-defeats the coord guard for ANY non-1-start or internally-gapped book** — scripts/core/canonical_verse_counts.py:138-151 enumerates a book's shape with `ch=1; while load_kjv_skeleton(book,ch): ...; break`. For aes the KJV skeleton starts at chapter 10 (verified: aes VERSES chapters = [10,11,12,13,14,15,16]), so load_kjv_skeleton('aes',1) is empty -> loop breaks immediately -> shape={} -> coord_in_canonical_extent returns True for ALL aes coords (the `if not shape: return True` at line 170). The merged finding correctly names aes as the instance, but per the project's own ★'fix the class not the instance' doctrine, the AUDIT under-covered the failure MODE: any book whose skeleton has an internal chapter gap (chapter N present, N+1 absent, N+2 present) is ALSO silently truncated at the gap — the same shape as the prospect.discover_verses 'stops at first missing verse' finding, but at the chapter granularity in the canonical-extent guard that is supposed to be the LAST line of defense against OCR/parse noise (Genesis 87:12 class). I verified aes is the only non-1-start today, but the guard's correctness must not depend on that staying true. Note load_kjv_skeleton lives in core/manuscript_collation.py (OFF-LIMITS) so the fix must be in _book_shape_cached (enumerate the actual chapter set from the already-loaded VERSES list), not the loader.  _Lens: Enumerate EVERY book's full chapter set directly from each content/translations/kjv/<book>.py VERSES literal (sorted(set(r[0] for r in VERSES))) and flag any book whose chapter set is non-contiguous OR doesn't start at 1 — that is the complete class _book_shape_cached silently mishandles. Then write the meta-test: assert coord_in_canonical_extent('aes', 1, 1) is False and ('aes', 9, 1) is False after the fix (today both wrongly return True). Confirm the data-validity dim's 111-candidate count is itself UNDERCOUNTED because aes_ch_010..016 candidates currently pass the broken guard._
- **Post-pass ORDERING × canon-filter × file-split triple-seam — the v0.0.3 chain (appendix-renumber -> superscriptions -> badge-merge -> enrich-nav -> file-split) was only source-read; no lane validated the BUILT output of a canon-filtered edition end-to-end** — build_edition.py runs patch_opf_fonts(4422) -> apply_appendix_demotion_and_renumber(4451, gapless 83-book renumber on canon-filtered editions) -> apply_superscriptions(4457) -> apply_badge_markers(4497, collapses note-refs to ONE badge + merges asides) -> enrich_nav_chapters(4542, nests chapter ol + navPoints) -> apply_file_split(4551, LAST: splits index_split_*.html, rewrites ~39.5K hrefs, REGENERATES OPF manifest+spine). The orphaned-Notes-heading-+-<hr> finding (win rx-surfaces) is in split_html_document, but the SEAM the split missed is the INTERACTION: file-split runs AFTER enrich_nav remaps nav/ncx, and apply_appendix_demotion renumbers eyebrows by scanning sorted(glob('index_split_*.html')) BEFORE the split — so on a canon-filtered edition (eth/catholic-study), a dropped book (e.g. the aes fold, which the corpus-index-vs-matrix finding says accounts for the exact 82-note mismatch) could leave a gap that the renumber claims to close, then file-split re-chunks those same files and must keep playOrder gapless + every badge->footnote target resolving across the NEW piece boundaries. test_epub_zip_determinism is synthetic and the rx-surfaces dim is build-heavy but the plan only mandated building eth+catholic-study — it's unclear the merge confirmed the badge-count==pre-collapse-marker-count invariant AND cross-piece href integrity AND gapless renumber all held SIMULTANEOUSLY on the same canon-filtered built artifact, vs each checked in isolation.  _Lens: On the N95, build catholic-study (canon-filtered, the gate-caught canon bug edition) and run a combined post-build scan on the SINGLE artifact: (1) parse content.opf spine playOrder for gaps; (2) grep every 'BOOK <roman>' eyebrow across the final pieces and assert the sequence is 1..N gapless; (3) for every verse-notes-badge, resolve its footnote/aside target across piece boundaries and assert no dead link; (4) assert no orphaned 'Notes' heading/<hr> shell in any piece tail (the known finding) survived into the canon-filtered build; (5) run check_nested_anchors --fix on the built tree (base-invariant). The point is to catch a defect that only emerges when canon-filter + renumber + badge-merge + file-split all act on the same edition — which isolated source-reading and the synthetic byte test both miss._
- **Real on-disk notes/translation STORES were never swept for out-of-extent coords — the 111-artifact finding is scoped to content/candidates/ only, but candidates are pre-promotion; the published stores are what ships** — The data-validity dim (mac, disk-light) flagged 111 out-of-extent artifacts confined to content/candidates/ — and the mint-10 cleanup deleted only 5. But content/candidates/ is the PRE-promote staging area (1,605 files); the coord guard is supposed to block bad coords at the promote boundary into content/notes/<book>.py (the published, shipping store). Because the aes guard is a proven no-op AND _book_shape_cached truncates at gaps, the question 'did any out-of-extent coord LEAK PAST promote into a published notes store' is the load-bearing one for the shipped Bible, and neither lane is documented to have scanned the ~83 published content/notes/*.py stores with the (now-known-buggy) guard. The win lane has the fast SSD to do a full corpus scan but ran tests/builds; the mac lane reviewed code but is HDD-bound and flagged only the candidates dir. My quick heuristic scan produced false positives (it matched byte-offset tuples in note text, e.g. jhn 1582,1609), which is itself the lesson: a naive grep is wrong — it needs the real notes_io loader to extract actual NOTE keys.  _Lens: On the N95, load each content/notes/<book>.py via the real notes store API (notes_io / the loader build_edition uses), extract the actual (chapter, verse) keys (NOT regex over the file text — that hits byte offsets/IDs in note bodies), and run coord_in_canonical_extent on each AFTER fixing the aes/_book_shape_cached bug. Report any published note whose coord is out-of-extent (that's a note that ships into the EPUB at an impossible coordinate). Separately confirm whether promote.py actually calls coord_in_canonical_extent on the WAY INTO the store, or only validates candidates — trace the exact boundary._
- **EN-flag row-count regex (gen/1sa/1ki undercount, website-deploy mac dim) × the ruff-format-before-save rule (a win-lane/build invariant) — the two halves of this bug live in different lanes** — scripts/gen_website_progress.py:144 counts back-translation rows with `re.match(r'\s*\(\d', line)` (>=50 to light the EN badge). The merged finding (website-deploy, mac) notes this undercounts ruff-formatted stores, suppressing the EN flag for gen/1sa/1ki. The SEAM: WHY are those stores ruff-formatted into a shape the regex misses? Because of the project's hard 'ruff format before save' rule (a recurring win-lane/CI invariant — the pre-commit hook runs ruff format --check). ruff reflows long VERSES tuples so the opening `(` is NOT at line start (it wraps after `(\n    1,` or aligns differently), defeating the per-line `^\s*\(\d` match. The mac lane saw the regex bug; the win lane owns the ruff-format reality that CAUSES it — but the merged plan treats it as a single website finding, missing that the fix must be robust to ruff's actual output (count real verse rows via ast, or via the same loader sources_base uses) AND that the SAME fragile 'count lines starting with (digit' pattern may be copied elsewhere (the at-scale drivers' candidate-count stats, the EN back-translation row-count guard cited in the over/under-claim finding).  _Lens: grep every site that counts verse/note rows via a line-anchored `\(\d` or similar regex (gen_website_progress, the EN back-translation guard, the at-scale driver stat reporters, render_coverage) and verify each against an ACTUAL ruff-formatted store (gen/1sa/1ki) — count the true row delta. Replace with ast.literal_eval of the VERSES/NOTES literal (the same way load_kjv_skeleton does it) so the count is ruff-format-invariant. This is the 'fix the class' move: one fragile counting idiom likely copied across the lanes' boundary._
- **Website count-consistency corollary partially verified by me but the social-card PNG regeneration + og/twitter meta tags were not confirmed swept — a count change must hit MORE than page HTML** — Per the deploy rule a book-count claim lives in page body + <meta>/og:/twitter: + the social card. I confirmed brand/sources/card.html (lines 37-38) and website/src/index.html (line 32) both say 83 — good. BUT the website-deploy dim returned only ONE count finding (the 87->83 stale-test in tests/test_website_progress.py) and the 'home page almost here / will post very soon' stale-copy finding. It did NOT report on whether brand/sources/card.html was actually RE-RENDERED to social-card.png after the count settled (the .html is the source; the .png is the shipped artifact — a stale PNG ships the old card), nor whether the og:image/twitter:image meta point at a regenerated card, nor whether the 4 folded books are excluded via _SUPERSET_EXCLUDE everywhere (geez-progress.html says '4 books Bible-ready of 83' — correct, but the exclude-set application across the generator wasn't surfaced). A dim returning only stale-test + stale-copy on a freshly-launched public surface is suspiciously thin on the artifact-vs-source rendering gap.  _Lens: Confirm the build chain that turns brand/sources/card.html -> social-card.png actually ran post-count-settle (compare mtimes / re-render and diff the PNG); grep website/src/*.html for og:image/twitter:image and confirm they reference the regenerated card; verify gen_website_progress applies _SUPERSET_EXCLUDE to the 4 folded books in EVERY count it emits (the bar 'of 83', the grid, the meta) not just the headline; and render the live pages via Chrome/Playwright MCP to confirm no page still renders a stale 87 or an old card image from cache._
