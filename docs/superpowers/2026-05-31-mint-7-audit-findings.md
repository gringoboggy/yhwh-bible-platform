# mint-7 audit — all findings

## [correctness] TSK JSON stores 5 books under legacy codes; CrossRefDetector silently returns [] for all verses in those books  (critical, conf 0.99, M)
- file: scripts\fetch_sources.py:153
- evidence: TSK_BOOK_REMAP maps 'Joel'→'jol', 'Ezek'→'ezk', 'Nah'→'nam', 'Phil'→'php', 'Jas'→'jas'. These legacy codes become top-level keys in tsk_xrefs.json. Tsk.refs_for() at sources_lexicon.py:201 does self._data.get(book, {}) with no normalization. CrossRefDetector.detect() is called with canonical codes (joe/eze/nah/phi/jam) from prospect.py and run_xref_at_scale.py, so refs_for() returns [] for all verses in those 5 books. Physical proof: 49 legacy-code candidate files already on disk — ezk_ch_001.json through ezk_ch_048.json, jol_ch_001.json through jol_ch_003.json (plus ch_008), nam_ch_001.json through nam_ch_003.json — all with 'book':'ezk'/'jol'/'nam' in their JSON. When batch_promote_xrefs.py runs, it tries to open NOTES_DIR/'ezk.py' (line 76) which does not exist → every candidate in those 49 files silently fails to promote. Additionally TskCrossRef.target_book carries legacy codes ('ezk','jol','nam','php','jas') which get embedded in note-body HTML as href='#vnote-ezk-...' — broken links since popup IDs use canonical codes ('eze' etc.).
- fix: Two-part fix: (1) At tsk_xrefs.json build time in TSK_BOOK_REMAP (fetch_sources.py:153-193), change the 5 legacy mappings to canonical: 'Joel'→'joe', 'Ezek'→'eze', 'Nah'→'nah', 'Phil'→'phi', 'Jas'→'jam'. Rebuild tsk_xrefs.json (python3 scripts/fetch_sources.py --force tsk). (2) In Tsk.refs_for() (sources_lexicon.py:201), add a _normalize_book_code call on the book parameter before the lookup as defense in depth. (3) Rename/merge the 49 legacy-code candidate files on disk: rename ezk_ch_*.json→eze_ch_*.json etc., updating the 'book' field inside each JSON. Verify with git diff after rebuild; byte-stable for all 9 KJV editions (no impact since no promoted notes use these broken refs yet).

## [correctness] KENYON_BOOK_NAME_TO_CODE maps 'joel'→'jol' and 'james'→'jas' — KenyonReferenceDetector routes Joel and James references to non-existent notes files  (high, conf 0.99, S)
- file: scripts\core\sources_lexicon.py:485
- evidence: KENYON_BOOK_NAME_TO_CODE at line 485 has 'joel': 'jol' and at lines 545-546 has 'jas': 'jas' and 'james': 'jas'. These legacy codes land in KenyonReference.book (line 656) and flow through iter_all_candidates() into the candidates JSON 'book' field. At promote time batch_promote_xrefs.py:76 tries NOTES_DIR/'jol.py' and NOTES_DIR/'jas.py' — neither exists (actual files are joe.py and jam.py). Additionally, the chapter-guard in KenyonText.references() uses ch_counts.get(book_code, 0) — for legacy code 'jol', ch_counts['jol']=0, so the guard condition (max_ch and chapter > max_ch) is always False → out-of-range Joel chapters are not rejected. The _BOOK_CODE_ALIASES in sources_base.py covers jas→jam but Kenyon does not call _normalize_book_code at all.
- fix: In KENYON_BOOK_NAME_TO_CODE fix 'joel': 'joe' (line 485), 'jas': 'jam' (line 545), 'james': 'jam' (line 546). Also fix 'phil'/'philippians' at lines 525-526: they currently map to 'php', change to 'phi'. Grep the full map for any other codes not in CANONICAL_BOOKS. After fixing, delete/regenerate any jol_ch_*.json or jas_ch_* Kenyon candidate files on disk. No impact on marathon core.

## [correctness] run_ai_xrefs_at_scale.py and run_ai_notes_at_scale.py use wrong YAML key 'chapters' instead of 'ch_count', silently capping all books at 50 chapters  (high, conf 0.99, S)
- file: scripts\run_ai_xrefs_at_scale.py:155
- evidence: Both run_ai_xrefs_at_scale.py:155 and run_ai_notes_at_scale.py:155 call book_meta.get('chapters', 50). books.yaml records the field as ch_count (confirmed at books.yaml:13). Since 'chapters' is never in the dict, get('chapters', 50) always returns 50. Books with ch_count > 50: Psalms (151), 1 Enoch (108), 1 Esdras (68), Sirach (65), Isaiah (66), Jeremiah (52). For contrast, run_hebrew_at_scale.py:146 and run_greek_at_scale.py:148 both correctly use 'ch_count'. Effect: any full-corpus AI xrefs/notes run silently skips Psalms ch 51-150 (100 chapters), Isaiah ch 51-66, Jeremiah ch 51-52, Sirach ch 51-65, etc. The per-chapter translations.get_chapter() call returns None for those chapters but no error is raised — pure silent coverage gap.
- fix: Change book_meta.get('chapters', 50) to book_meta.get('ch_count', 50) in both iter_target_verses() functions. One-line fix per file. Verify by dry-running: python3 scripts/run_ai_xrefs_at_scale.py --books psa --dry-run should now report 150 chapters instead of 50.

## [correctness] _normalize_book_code alias table is incomplete — missing jol→joe, ezk→eze, nam→nah, php→phi (4 of 7 legacy codes present in real data)  (high, conf 0.99, S)
- file: scripts\core\sources_base.py:54
- evidence: _BOOK_CODE_ALIASES dict (sources_base.py:54-65) maps only 3 aliases: joh→jhn, ps→psa, jas→jam. The TSK JSON and extract_torrey_ccel.py's _LEGACY_TO_CANON (lines 101-110) expose 4 more real legacy codes in circulation: jol (Joel), ezk (Ezekiel), nam (Nahum), php (Philippians). The central function _normalize_book_code is the documented single-source-of-truth for alias resolution (re-exported from sources.py) but callers relying on it (e.g. _CommentaryCorpus.for_verse, run_ethiopian_at_scale.py) would silently miss those 4 codes if any commentary JSON ever used them. The more urgent consequence is that _normalize_book_code is NOT called in Tsk.refs_for, NavesTopical.topics_for, or TorreyTopical.topics_for — so even a complete alias table wouldn't fix the TSK/Nave/Torrey lookup paths without also wiring the call.
- fix: Expand _BOOK_CODE_ALIASES with the 4 missing mappings: 'jol': 'joe', 'ezk': 'eze', 'nam': 'nah', 'php': 'phi'. Then add _normalize_book_code calls in: Tsk.refs_for() on the book param (sources_lexicon.py:201), NavesTopical.topics_for() (sources_lexicon.py:279), TorreyTopical.topics_for() (sources_lexicon.py:354). These are single-line additions. The commentary corpus already calls _normalize_book_code symmetrically at index-build and lookup (sources_commentary.py:316, 326) — model after that pattern.

## [correctness] 49 legacy-code candidate files on disk (ezk_ch_*, jol_ch_*, nam_ch_*) are permanently unpromotable — silently skipped by batch_promote_xrefs.py  (high, conf 0.99, S)
- file: content\candidates\ezk_ch_001.json:1
- evidence: Glob confirmed: 49 candidate files exist with legacy book codes — ezk_ch_001.json through ezk_ch_048.json plus ezk_ch_081.json (impossible ch 81 from OCR junk), jol_ch_001.json through jol_ch_003.json plus jol_ch_008.json, nam_ch_001.json through nam_ch_003.json. The 'book' field inside each is 'ezk'/'jol'/'nam'. batch_promote_xrefs.py:76 does book_path = NOTES_DIR / f'{book}.py' — 'ezk.py' does not exist, so every candidate in these files silently fails. The ezk_ch_081.json file contains an impossible coordinate (Ezekiel 81:11) from a Nave's OCR artifact that slipped through because _naves_coord_in_extent at fetch_sources.py:386-393 uses canonical_book_shape('ezk') — unknown code → True (bypass). This is separately a data quality issue.
- fix: After fixing TSK_BOOK_REMAP and rebuilding tsk_xrefs.json (finding #1), run a one-time migration script: for each legacy-code candidate file, rewrite 'book' field and rename file to canonical code (ezk→eze, jol→joe, nam→nah). Also delete ezk_ch_081.json (impossible chapter). After migration, re-run run_xref_at_scale.py to regenerate the now-canonical files. The rewrite is safe because no candidates in these files have been promoted yet (verified: no vnote-ezk/jol/nam links exist in content/notes/).

## [correctness] CrossRefDetector embeds legacy target_book codes in note-body HTML hrefs — cross-reference links to Ezekiel/Joel/Nahum/Philippians/James would be broken in any promoted note  (medium, conf 0.97, S)
- file: scripts\core\detectors.py:464
- evidence: CrossRefDetector.detect() at line 464 builds href='#vnote-{r.target_book}-{r.target_chapter}-{r.target_verse}'. TskCrossRef.target_book is the raw value from the TSK JSON tuple (fetched by Tsk.refs_for). Since TSK JSON stores target book as legacy codes (ezk/jol/nam/php/jas), any cross-reference pointing TO those books in a promoted note body would generate href='#vnote-ezk-...' — a broken link, because generate_verse_popups.py builds IDs using canonical codes: vid=f'vnote-{code}-{ch}-{vs}' (generate_verse_popups.py:32) where code comes from books.yaml canonical values. Currently 0 such notes are promoted (verified by grep), so no live EPUB damage yet — but any future promote of an xref-citation candidate pointing to Ezekiel/Joel/Nahum/Philippians/James would silently create a broken popup link.
- fix: In CrossRefDetector.detect() at line 464, normalize the target_book before building the href: from scripts.core.sources import _normalize_book_code; safe_target = _normalize_book_code(r.target_book). This requires the _normalize_book_code alias table to be expanded first (finding #4). Alternatively, normalize at TskCrossRef construction time in Tsk.refs_for() so the fix is centralized.

## [correctness] Nave's/Torrey/TSK lookup drivers (run_naves_at_scale.py, run_torrey_at_scale.py, run_xref_at_scale.py) iterate source JSON keys directly without normalizing — any legacy code in the JSON propagates to the candidates 'book' field  (medium, conf 0.95, S)
- file: scripts\run_naves_at_scale.py:100
- evidence: run_naves_at_scale.py:100 does naves._verses.get(book, {}) iterating books from naves._verses.keys(). run_torrey_at_scale.py:119 does torrey._verses.get(book, {}) iterating from torrey._verses.keys(). run_xref_at_scale.py:91 does raw.get(book, {}) iterating tsk_raw.keys(). If the underlying JSON contains a legacy key, the legacy code flows through unchanged into write_queue(book, ...) and becomes the 'book' field in the candidate JSON. The Nave's and Torrey JSONs are currently clean (built via CCEL extractors with canonical codes). The TSK JSON has 5 legacy keys confirmed above. This structural pattern is fragile: a future JSON rebuild from fetch_sources.py using the unfixed TSK_BOOK_REMAP would silently reintroduce legacy codes.
- fix: In each driver's main() book-list construction, apply _normalize_book_code to every book key before passing to run_*_for_book(). In run_xref_at_scale.py:140, books = [_normalize_book_code(b) for b in (args.books.split(',') if args.books else sorted(tsk_raw.keys()))]. In run_naves_at_scale.py:155-157 and run_torrey_at_scale.py:195, apply the same. This is a belt-and-suspenders defense so the at-scale drivers are self-correcting even if a source JSON is rebuilt with legacy codes.

## [code-debt] run_xref_at_scale.py write_queue overwrites instead of appending — data-loss when run after other drivers  (high, conf 0.97, S)
- file: scripts\run_xref_at_scale.py:62
- evidence: All 8 other at-scale drivers (run_naves, run_torrey, run_hebrew, run_greek, run_ethiopian, run_kenyon, run_ai_xrefs, run_ai_notes) implement append-not-overwrite semantics in write_queue, preserving candidates from prior driver runs. run_xref_at_scale.py's write_queue (lines 62-77) constructs a fresh payload and calls out_path.write_text() unconditionally with no read-existing logic. If run_hebrew_at_scale.py has already written content/candidates/gen_ch_001.json and run_xref_at_scale.py runs next, all Hebrew candidates are silently deleted. The ID numbering also resets to 1, potentially colliding with promoted entries.
- fix: Add the same append-merge pattern as run_naves_at_scale.py lines 74-88: read existing file if present, gather existing candidates list, number new candidates starting at len(existing)+1, write combined payload. This is a copy of ~15 lines from any of the 8 sibling drivers.

## [code-debt] catholic_commentaries.json uses legacy book code 'mar' (Mark) not covered by _normalize_book_code — 2 entries silently dropped on lookup  (high, conf 0.99, S)
- file: scripts\core\sources_base.py:54
- evidence: sources_base.py _BOOK_CODE_ALIASES maps only {joh→jhn, ps→psa, jas→jam}. catholic_commentaries.json lines 50 and 60 contain entries with 'book': 'mar'. _CommentaryCorpus.__init__ (sources_commentary.py:316) stores keys as (_normalize_book_code(obj.book), chapter, verse). With 'mar' not in the alias map, entries are indexed under key ('mar', ...). Callers that query for_verse('mrk', ...) will look for ('mrk', ...) and miss both entries. The fix is exactly the pattern of the joh→jhn fix that already exists — add 'mar': 'mrk' to _BOOK_CODE_ALIASES. extract_torrey_ccel.py line 100 already notes this gap: '_normalize_book_code covers only joh/ps/jas, so cover the full set here' and defines its own 7-entry _LEGACY_TO_CANON including mar→mrk.
- fix: Add 'mar': 'mrk' to the _BOOK_CODE_ALIASES dict in scripts/core/sources_base.py. Optionally add the other 4 codes from extract_torrey_ccel._LEGACY_TO_CANON that are not yet covered (jol→joe, ezk→eze, nam→nah, php→phi) as defensive entries — no current commentary JSON uses them but they prevent silent drops if new content is added. The 2 Catholic entries under 'mar' will then index and resolve correctly on next load.

## [code-debt] candidate_to_dict and the surrounding boilerplate are copy-pasted across 9 at-scale driver files with no shared base  (medium, conf 0.95, M)
- file: scripts\run_naves_at_scale.py:42
- evidence: candidate_to_dict (12-line function returning the same 13-key dict) is defined identically in all 9 at-scale drivers and in prospect.py — 10 definitions total (grep confirmed: run_greek_at_scale.py:72, run_hebrew_at_scale.py:71, run_naves_at_scale.py:42, run_torrey_at_scale.py:43, run_ethiopian_at_scale.py:51, run_xref_at_scale.py:42, run_kenyon_at_scale.py:47, run_ai_xrefs_at_scale.py:93, run_ai_notes_at_scale.py:90, prospect.py:148). The 27-element NT_BOOKS set is defined 4 times: detectors.py HebrewWordDetector (line 140), detectors.py GreekWordDetector (line 337), run_hebrew_at_scale.py (line 40), run_greek_at_scale.py (line 41). Also the ANSI color constants (GREEN, DIM, RESET) are defined in each of 9 files.
- fix: Extract to a new scripts/core/at_scale_base.py module: (1) candidate_to_dict(), (2) NT_BOOKS frozenset (and import it in detectors.py instead of re-defining twice), (3) ANSI color constants. Each driver then imports from scripts.core.at_scale_base. ~90 lines of dedup across the cluster. prospect.py can also import candidate_to_dict from at_scale_base. The write_queue implementations are NOT identical (each has driver-specific append/dedup/overwrite behavior) and should stay per-driver.

## [code-debt] Two legacy error-response paths in do_GET bypass _send_json and skip security headers (Content-Length, Cache-Control, X-Content-Type-Options, CSP, Referrer-Policy)  (medium, conf 0.98, S)
- file: scripts\web.py:1678
- evidence: At line 1678-1688 (/api/sample/ error path) and lines 1701-1712 (/api/backups error path), the code calls self.send_response(http_code), self.send_header('Content-Type', ...), self.end_headers(), self.wfile.write(...) directly — bypassing _send_json. Unlike _send_json, these paths do NOT send: Content-Length (HTTP/1.1 requirement), Cache-Control: no-store (may allow proxy caching of error responses), X-Content-Type-Options: nosniff (sniff-protection), Referrer-Policy, or Content-Security-Policy. _send_json handles all of these. The /api/verse-of-day.json error path (line 1522) correctly uses _send_json. The two manual paths are pre-table-migration legacy code.
- fix: Replace the two manual send_response/send_header/end_headers/wfile.write blocks with self._send_json({'error': ..., 'message': ...}, status=http_code) — 3 lines each instead of 7-10. For /api/sample/ the status-code is already computed (http_code); for /api/backups likewise. This is a direct call-site substitution with no API changes.

## [code-debt] Stale documentation comment in sources_base.py references a non-existent _BOOK_CODE_ALIASES_LONGFORM dict  (low, conf 0.99, S)
- file: scripts\core\sources_base.py:59
- evidence: Line 59 reads: '# _BOOK_CODE_ALIASES_LONGFORM dict (further down this file) maps'. A grep across the entire scripts/ tree returns exactly one hit: this comment itself. _BOOK_CODE_ALIASES_LONGFORM does not exist anywhere in the codebase. It was presumably in the original monolithic sources.py before the god-module split (2026-05-26) but was not migrated to sources_base.py. The comment instructs the reader to look 'further down this file' for something that is not there, which is actively misleading.
- fix: Delete lines 59-60 of sources_base.py (the stale comment). The rationale for jas→jam is already explained on the next line (63). No functional change.

## [code-debt] do_POST falls through to do_PUT (double auth-check + unintended route aliasing for 3 bespoke PUT routes)  (low, conf 0.85, S)
- file: scripts\web.py:2061
- evidence: do_POST at line 2022 calls _check_admin_auth() once. After exhausting _POST_ROUTES and _MULTIPART_ROUTES, it calls self.do_PUT() at line 2061. do_PUT calls _check_admin_auth() again at line 1903. This means for any POST that reaches the fallthrough, auth is checked twice — harmless but redundant. More structurally, the 3 bespoke PUT routes (/api/export/build/<id>, /api/build-all, /api/edition-meta/<id>/preview) become reachable via POST in addition to PUT, because do_PUT dispatches them with no method guard. The comment says 'front-end uses POST for create' justifying the fallthrough, but the bespoke build routes are mutation endpoints that should be PUT-only semantically. This creates an unintended POST→build-all path.
- fix: Extract the 3 bespoke routes from do_PUT into a shared _dispatch_bespoke_routes() helper called from both do_POST and do_PUT (explicitly, not via fallthrough), removing the do_POST → do_PUT tail-call. Move the _check_admin_auth() call to the shared helper or keep it at each method entry but skip the second check on fallthrough. Alternatively, accept the current behavior as intentional (it IS documented) and just add a comment clarifying that POST/PUT aliasing is deliberate for all routes including the bespoke build routes. Medium effort for the refactor, S effort for documentation.

## [dead-code] Built tested verse-popup-translation-swap feature not wired into any build path  (medium, conf 0.9, M)
- file: scripts/build_edition.py:600
- evidence: _replace_verse_popup_translation (Phase nu.2.5-B). grep across scripts shows only its own def at line 600, no production caller. Exercised by 5 tests (test_scripts.py:159,183,200,216,235) but never invoked by the build pipeline; ships dead.
- fix: WIRE-IN as a builder option (editions.yaml key via the per-edition resolver, call in popup-build step) or mark not-yet-wired. Do not delete: real tested feature.

## [dead-code] Obsolete one-shot _split_web_html.py still in scripts (split done, flagged 2026-05-23, never archived)  (low, conf 0.92, S)
- file: scripts/_split_web_html.py:1
- evidence: Docstring: One-shot extractor for the web.py split refactor. Split complete: scripts/templates holds 21 modules, web.py re-imports them. AUDIT_2026-05-23-DEEP.md:113 marked it C3a.splitweb dead/archive but it was never moved. No prod/test refs.
- fix: git mv scripts/_split_web_html.py dev/archive/ per the already-approved disposition.

## [dead-code] ebible-audit aggregator (audit_caches/dead_code/types/deps) not invoked by ci.py or gitlab CI  (medium, conf 0.85, M)
- file: scripts/ci.py:143
- evidence: ebible.py:307-314 wraps audit_caches/dead_code/types/deps as the documented automatic home. ci.py and .gitlab-ci.yml do not call ebible audit; ci.py runs vulture (149), mypy via audit_types (177), pip-audit inline (123), never audit_caches (only preflight.py:632). The audit_dead_code.py/audit_deps.py wrappers are never executed by the gate.
- fix: Have ci.py call ebible audit / run_audit_suite for the deadcode/types/deps/caches block instead of duplicating inline, or add a non-blocking ebible audit step to .gitlab-ci.yml.

## [dead-code] Orphaned helper _chapter_label in apply_style.py  (low, conf 0.9, S)
- file: scripts/apply_style.py:434
- evidence: Handles code-num/title-num TOC formats, but patch_nav (the only nav-label rewriter) explicitly skips those (docstring 452-459) and uses NAV_CHAPTER_RE. grep: only its def in scripts, 0 test refs.
- fix: Delete the function and its unused branch logic.

## [dead-code] Orphaned find_template_files in bulk_inject.py  (low, conf 0.88, S)
- file: scripts/bulk_inject.py:241
- evidence: Docstring claims the scaffolder is the caller, but scaffold_console.py has no reference; repo-wide grep shows only the def at line 241, 0 call sites, 0 test refs.
- fix: Delete; a future scaffolder can use sorted(templates_dir.glob('*.py')) inline.

## [security] Stored XSS in /api/sample/ HTML output — note body inserted without sanitize_html(); CSP nonce injection makes it exploitable  (high, conf 0.95, S)
- file: scripts\web_content.py:369
- evidence: Line 366: `body = str(n[7] or '')  # body is already HTML`. Line 369: `f'<li class="note"><span class="kind">{kind}</span> <strong>{title}.</strong> {body}</li>'` — body is interpolated verbatim into the HTML. Unlike preview.py lines 133-135 (which calls `from scripts.core.html_sanitize import sanitize_html; pieces.append(f'..{sanitize_html(body)}..')`), _render_sample_html has no sanitization call. _send_html in web.py then calls _inject_script_nonces(html, nonce) on the final composite string, so any <script> tag injected from a note body receives a valid per-request nonce and the nonce-based CSP (script-src 'self' 'nonce-{nonce}') will allow it to execute. The note body is stored verbatim by dict_to_tuple (web_helpers.py line 145: `d['body']`) with no sanitization at write time.
- fix: In _render_sample_html, replace `body = str(n[7] or '')` with `from scripts.core.html_sanitize import sanitize_html; body = sanitize_html(str(n[7] or ''))`. Mirror the pattern already used in preview.py:133-135. One-line fix, no architectural change needed.

## [security] api_download_export filename regex excludes .zip — 'Build All' combined ZIP download is permanently broken  (medium, conf 0.98, S)
- file: scripts\api\exports.py:406
- evidence: Line 406: `if not re.match(r'^Ethiopian_Bible_[a-z0-9-]+_[a-z0-9]+_[\dT\-:Z]+\.epub$', filename): return {'error': 'invalid export filename'}`. api_build_all_editions (line 377) produces filenames like `All_Editions_v28a_20260531T120000Z.zip` and emits `download_url = f'/api/export/download/{zip_name}'` (line 385). This filename never matches the regex (wrong prefix, wrong extension), so every Build All combined-ZIP download returns HTTP 400 `{"error": "invalid export filename"}`. Confirmed at web.py:1570 the route dispatches to api_download_export with the raw filename.
- fix: Extend the validation in api_download_export to also accept the zip pattern: add `or re.match(r'^All_Editions_[a-z0-9]+_[\dT]+Z\.zip$', filename)` as a second allowed shape. Alternatively, unify both patterns into a common allowlist. The EXPORTS_DIR containment (`path = EXPORTS_DIR / filename`) already prevents directory escape; the regex is purely a filename-shape allowlist.

## [security] content/auth.json (enrolled TOTP secret) not in .gitignore — base32 secret will be committed by save.ps1 on first 2FA enrollment  (medium, conf 0.97, S)
- file: scripts\core\auth.py:66
- evidence: auth.py line 66: `_REPO_ROOT / 'content' / 'auth.json'` is the persistence path for the enrolled TOTP state, which (per the schema comment at line 21) includes `'secret': '<base32>'`. .gitignore has no entry for content/auth.json — grep of .gitignore finds only `content/notes/.manifest.json`, `content/print_covers/*.pdf`, `content/.refactor_log.yaml`, and one PDF path; no auth.json or content/*.json wildcard. save.ps1 runs `git add -A` which will sweep any new file under content/ including auth.json. The file does not currently exist (no 2FA enrollment yet) so no secret is in the current commit, but the first enrollment + save cycle will commit it.
- fix: Add `content/auth.json` to .gitignore (and optionally `content/distribution.json`, `content/press_kit.json` for consistent treatment of runtime-state JSON that shouldn't be source-controlled). The auth module's atomic_write call already handles the file creation safely; the only missing piece is git exclusion.

## [security] Audit-log redaction of sensitive positional args is implicit (by dict-summary) not explicit — brittle if function signatures change  (low, conf 0.88, S)
- file: scripts\core\audit_log.py:284
- evidence: _summarize_args (line 284-295) redacts kwargs by name against _REDACT_KEYS (which includes 'secret', 'token', 'code', 'auth'). However, api_auth_totp_confirm (scripts/api/auth.py:110) receives `payload={'secret':..., 'code':...}` as a positional arg. The positional path (lines 286-287: `summary['args'] = [_short_repr(a) for a in args]`) calls _short_repr which for a dict returns `{'<dict>': 'N keys'}` — safe by accident because dicts are opaque. If a future refactor of api_auth_totp_confirm splits `payload` into `def api_auth_totp_confirm(secret: str, code: str, ...)`, both would become positional args whose values would be logged via _short_repr (strings are truncated to 200 chars, not redacted). The 'secret' and 'code' kwarg names ARE in _REDACT_KEYS so they would be caught if passed as kwargs.
- fix: No immediate code change needed for the current signatures (current behavior is safe). Document the implicit reliance in a comment near _summarize_args, or add a positional-arg inspection path that checks the decorated function's parameter names via inspect.signature() and redacts known-sensitive positional parameters. If api_auth_totp_confirm is ever refactored to named params, add a test asserting the audit-log entry for that endpoint contains no literal secret values.

## [doc-data-hygiene] Stale cross-reference in ROADMAP_FUTURE.md — archive path not updated  (low, conf 1, S)
- file: dev\ROADMAP_FUTURE.md:75
- evidence: Line 75 references `dev/SCOPE_2026-05-08-addendum-audio-epubs.md` without the `archive/` prefix. The file exists at `dev/archive/SCOPE_2026-05-08-addendum-audio-epubs.md` (committed during phase 3 archive sweep). This is a broken internal reference—the doc was moved but the citation was not updated.
- fix: Update line 75 from `dev/SCOPE_2026-05-08-addendum-audio-epubs.md` to `dev/archive/SCOPE_2026-05-08-addendum-audio-epubs.md`. This is the ONLY live doc referencing an archived SCOPE file without the prefix.

## [doc-data-hygiene] Plural references to obsolete PLAN files in live docs — minor drift  (medium, conf 0.95, M)
- file: dev\PROPOSAL_FEATURE_LANDSCAPE.md:1
- evidence: Several live docs (PROPOSAL_FEATURE_LANDSCAPE.md, marathon_reviews/README.md) cite `PLAN_2026-05-09.md`, `PLAN_2026-05-21.md`, or `PLAN_2026-05-24.md`. These are all archived in dev/archive/. The new canonical plan is `PLAN_2026-05-29-roadmap.md` (shipped 2026-05-31). These cites work via git history but represent outdated guidance — readers should use the new roadmap. The PROPOSAL doc still references an old cluster-matrix ledger location that was superseded.
- fix: Update PROPOSAL_FEATURE_LANDSCAPE.md §10 to point to `dev/PLAN_2026-05-29-roadmap.md` and remove the old ledger references. Optional: inline a note that PLAN_2026-05-09.md is archived and the new plan supersedes it. Marathon_reviews/README.md can add an archive pointer (informational only—no functional impact).

## [doc-data-hygiene] CHANGELOG size (38,530 lines) — consider optional month-roll archiving  (low, conf 0.8, M)
- file: dev\CHANGELOG.md:1
- evidence: CHANGELOG.md is 38,530 lines (2.3 MB), appended-only since inception. Phase 3 archived 56 dated docs but left the CHANGELOG intact—it's still the authoritative journal. The lint-rules check carries a pre-existing WARN for size ('changelog_size' guard). No functional issue; this is a documented optional cleanup. `check_untracked_phases` runs safely across the file size.
- fix: Optional: manually roll the oldest sessions (before 2026-05-15) into a `dev/archive/CHANGELOG_2026-05-01-to-2026-05-14.md` and truncate the live file to post-2026-05-15 entries. This is nice-to-have, not blocking. If done, update the lint-rules comment to note the archive exists. No test change required—the checker is size-agnostic.

## [tests] KENYON map yields non-canonical jol/php/jas; Joel/Phil/James witnesses silently drop  (high, conf 0.9, M)
- file: scripts/core/sources_lexicon.py:485
- evidence: Proven live: values minus books_by_code = jas/jol/php (no notes file). references() line 638 builds refs from these. test_scripts.py:8328 only spot-checks 9 codes.
- fix: Normalize to canonical; meta-test every alias value has a notes file.

## [tests] 8 of 26 active lint_rules checks have no unit test  (medium, conf 0.92, M)
- file: scripts/lint_rules.py:1956
- evidence: Untested+blocking: encode_decode(302), encoder_canonical_order(225), provenance_tier(1063), render_coverage(938), ephemeral_doc_pins(1401), plan_coherence(1194), freshness(422), manuscript_witnesses(1124).
- fix: Meta-test over ALL_CHECKS.keys() + tests for the 5 correctness checks.

## [tests] Two 23min files undeselectable (no slow marker); matrix re-walks corpus 10x uncached  (medium, conf 0.85, M)
- file: pyproject.toml:152
- evidence: Only serial marker defined. test_web_filesplit + test_matrix_psi35 ~23min unmarked. psi35 calls compute_matrix() 10x no session fixture.
- fix: Add slow marker; tag tests; session-scope compute_matrix.

## [tests] serial marker defined but never applied (dead config)  (low, conf 0.95, S)
- file: pyproject.toml:153
- evidence: Registered at 153; no test uses mark.serial. Races handled by --dist=loadfile.
- fix: Apply to shared-state tests or remove.

## [tests] No golden byte-stability gate for the 9 KJV editions  (medium, conf 0.7, L)
- file: tests/test_scripts.py:4818
- evidence: Only no-op tests (4818,4874)+build_one smoke. Goldens only for off-limits Geez core. No 9-edition fingerprint check.
- fix: Build 9 editions, assert golden hash over deterministic content; behind slow marker.


# VERIFIED critical/high

## [correctness] TSK JSON stores 5 books under legacy codes; CrossRefDetector silently returns [] for all verses in those books -> real=True sev=high
- CONFIRMED real with concrete evidence; recalibrated critical->high (it does not corrupt shipped output).

Verified facts:
1. TSK_BOOK_REMAP (scripts/fetch_sources.py:153,156,161,177,186) maps Ezek->ezk, Joel->jol, Nah->nam, Phil->php, Jas->jas (legacy codes).
2. tsk_xrefs.json top-level keys are ONLY legacy: direct JSON load shows ezk/jol/nam/php/jas present, canonical eze/joe/nah/phi/jam ABSENT. The index is keyed by SOURCE book (fetch_sources.py:237 index.setdefault(sb,...) where sb comes from TSK_BOOK_REMAP), so both source-bucket and target_book carry legacy codes.
3. Tsk.refs_for() at scripts/core/sources_lexicon.py:201 does self._data.get(book,{}) with NO _normalize_book_code call.
4. CrossRefDetector.detect() at scripts/core/detectors.py:456 passes book straight through.
5. Callers use canonical codes: notes files on disk are eze.py/joe.py/nah.py/phi.py/jam.py (legacy jol/ezk/nam/php/jas .py do NOT exist); content/books.yaml uses eze/joe/nah/phi. SMOKING GUN: canonical candidate file content/candidates/eze_ch_001.json (generated 2026-05-26) contains 45 candidates but ZERO xref-citation and ZERO topic-nave - physical proof refs_for('eze',...) returns []. The older legacy file ezk_ch_001.json (2026-05-09) DOES contain xref-citation candidates (built when detector was fed the legacy key that matches the JSON), with broken hrefs like #vnote-ezk-43-2.
6. 1,525 TSK source-verse entries are silently unreachable via canonical lookup across the 5 books (ezk 1194, jol 73, nam 47, php 104, jas 107).
7. Corroboration: the same class of bug was ALREADY FIXED for Nave's - naves_topical.json was rebuilt with CANONICAL keys (eze/joe/nah/phi/jam present, legacy absent), which is exactly why eze.py/joe.py/etc. have hundreds of promoted topic-nave notes but ZERO xref-citation notes. TSK's JSON was simply never rebuilt. This proves the drift is real and isolated to TSK.

Two REFINEMENTS where the finding slightly overstates:
- Broken-href impact: the #vnote-ezk-... hrefs exist ONLY in unpromoted legacy candidate files (content/candidates/ezk_ch_*.json etc.), never in promoted notes. Verified 0 occurrences of vnote-(ezk|jol|nam|php|jas)- across all content/notes/*. batch_promote_xrefs.py:77 skips them because ezk.py doesn't exist. So 'byte-stable, no shipped-EPUB impact' holds - no user-facing corruption today.
- The proposed fix-part-2 (add _normalize_book_code in refs_for) only helps James: _BOOK_CODE_ALIASES (sources_base.py:54-65) covers jas->jam (plus joh/ps) but NOT jol/ezk/nam/php. The real fix is fix-part-1: change TSK_BOOK_REMAP to canonical and rebuild tsk_xrefs.json (mirroring what was already done for Nave's), plus reconcile/rename the 49 stale legacy candidate files. Effort M.

Severity high not critical: real data-completeness bug in the editorial candidate-generation pipeline (1,525 TSK xrefs never surface for 5 books, and any future canonical-keyed promote drops them), but no shipped-output corruption, KJV editions byte-stable, no security impact, and all candidates are human-reviewed before promotion.

Effort: M. Confidence the underlying bug is real: ~0.97.

## [correctness] KENYON_BOOK_NAME_TO_CODE maps 'joel'→'jol' and 'james'→'jas' — KenyonReferenceDetector routes Joel and James references to non-existent notes files -> real=True sev=medium
- CONFIRMED REAL via direct code+data inspection. (1) KENYON_BOOK_NAME_TO_CODE in scripts/core/sources_lexicon.py maps to three non-canonical codes: line 485 'joel':'jol', lines 525-526 'phil'/'philippians':'php', lines 545-546 'jas'/'james':'jas'. (2) The actual notes files are content/notes/joe.py, jam.py, phi.py — NO jol.py, jas.py, or php.py exist (verified by Glob). (3) The Kenyon path never normalizes: KenyonText.references() sets book=book_code directly (lexicon:656); KenyonReferenceDetector indexes/emits key=(ref.book,...) and Candidate(book=book) with no _normalize_book_code call (detectors.py:688,714). _BOOK_CODE_ALIASES in sources_base.py:54-65 covers only joh/ps/jas — NOT jol or php — and _normalize_book_code is only invoked by sources_commentary.py and run_ethiopian_at_scale.py, never by Kenyon. (4) Chapter-guard bypass confirmed: ch_counts is built from config.books_by_code() (lexicon:624) keyed by canonical codes, so ch_counts.get('jol',0)=0; the guard at lexicon:646 `if chapter < 1 or (max_ch and chapter > max_ch)` has max_ch=0 → the out-of-range clause is dead → out-of-range chapters for these codes are NOT rejected. (5) Promote breakage confirmed: run_kenyon_at_scale.py:112 writes "book": book into the JSON and groups by c.book (line 154); batch_promote_xrefs.py:72 reads data["book"] and line 76 opens NOTES_DIR/f"{book}.py", line 77-78 prints "no notes file — skipping" when missing → candidates silently never promoted.

ON-DISK PROOF the dangling-code problem is live: content/candidates/jol_ch_001/002/003/008.json all carry "book":"jol", and jol_ch_008.json is verse jol-8-21 — an out-of-range chapter (Joel has 3-4 chapters), exactly the guard-bypass predicted. Canonical joe_ch_*.json, jam_ch_*.json, phi_ch_*.json ALSO exist, so the same book's candidates are split across two file-stems (joe vs jol, etc.) — a real data-drift defect.

REFINEMENTS that lower severity from the finding's 'high' to medium: The dangling jol_*.json files currently on disk were emitted by NaveTopicalDetector/CrossRefDetector (kind topic-nave / xref-citation), not Kenyon — so this same broken legacy-code convention is shared across the detector family (this is the ★BUGCLUSTER already noted in MEMORY's 'Book-code canonical vs legacy'). No jas_*/php_* candidate files exist right now, so the Kenyon-specific Joel/James/Phil corruption is latent/conditional on the source text. Impact is silently-dropped reference-note candidates (content-completeness loss, surfaced by an explicit 'skipping' warning) — NOT a crash, not security, and not corruption of any shipped artifact: the 9 KJV editions and the Geʽez marathon core are untouched. That is a genuine mint-quality/correctness+data-drift defect worth fixing, but its blast radius (3 books of optional study-note candidates) is medium, not high.

FIX (matches finding, plus systemic hardening): in sources_lexicon.py set 'joel':'joe' (485), 'phil':'phi' & 'philippians':'phi' (525-526), 'jas':'jam' & 'james':'jam' (545-546); grep the whole map for any other code not in the canonical set. Better: route the Kenyon path through _normalize_book_code and extend _BOOK_CODE_ALIASES with 'jol':'joe' and 'php':'phi' so the entire detector family stops splitting books. Then merge/regenerate the stale jol_ch_*.json (and any future jas_/php_) candidate files into their canonical joe_/jam_/phi_ stems. No impact on KJV byte-stability, notes_io atomic-write rule, or the marathon core. Effort S (point fix) to M (systemic normalize + on-disk candidate cleanup/merge). Confidence ~0.9 that the mapping+normalization bug is real; ~0.7 that Kenyon specifically has emitted Joel/James/Phil refs (the on-disk proof is from sibling detectors, so the Kenyon-specific manifestation is partly latent).

## [correctness] run_ai_xrefs_at_scale.py and run_ai_notes_at_scale.py use wrong YAML key 'chapters' instead of 'ch_count', silently capping all books at 50 chapters -> real=True sev=high
- CONFIRMED real and actionable. Verified directly against the code and data:

1. Bug present in both files: scripts/run_ai_xrefs_at_scale.py:155 and scripts/run_ai_notes_at_scale.py:156 both call book_meta.get("chapters", 50). (Minor correction: in the notes file the line is 156, not 155 as the finding stated; the xrefs file is exactly 155.)

2. books.yaml uses the field name ch_count, never chapters. Confirmed grep: line 13 ch_count:50, and no chapters key anywhere. config.get_book() (scripts/core/config.py:326-331) returns the raw parsed record from books_by_code() (line 308: {b["code"]: b for b in load_books()}), so the returned dict's chapter-count key is ch_count. Therefore get("chapters", 50) can never hit a real key and always returns the literal default 50.

3. Sibling drivers do it correctly: run_hebrew_at_scale.py:146 and run_greek_at_scale.py:148 both use book_meta.get("ch_count", 50) — proving the intended key is ch_count and these two AI drivers are the buggy outliers.

4. Real impact confirmed via books.yaml: books with ch_count > 50 are Psalms (151, line 350), 1 Enoch (108, line 200), 1 Esdras (68, line 213), Isaiah (66, line 416), Sirach (65, line 927), Jeremiah (52, line 427), plus a 51-chapter book (line 405). The loop is `for chapter in range(1, n_chapters + 1)` with n_chapters frozen at 50; chapters 51+ are never even passed to translations.get_chapter(), and the `if not verses: continue` guard means nothing errors. Pure silent coverage gap — e.g. Psalms 51-150 (100 chapters) is silently skipped on any full-corpus AI xrefs/notes run.

The fix sketch is correct and minimal: change "chapters" to "ch_count" in both iter_target_verses() functions (one line each).

Severity recalibrated to HIGH rather than the suggested level: it is a genuine silent-correctness bug causing large missed coverage, but it is confined to two optional at-scale AI-content generation drivers — it does not touch the 9 byte-stable KJV editions, the build/inject pipeline, the marathon core, data integrity, or security. The blast radius is "AI xrefs/notes generation under-covers high-chapter books," which is serious for content completeness but bounded and recoverable by a re-run after the one-line fix. Effort: S (two one-line edits; verify with a --dry-run / --books psa showing 151 chapters).

## [correctness] _normalize_book_code alias table is incomplete — missing jol→joe, ezk→eze, nam→nah, php→phi (4 of 7 legacy codes present in real data) -> real=True sev=medium
- VERDICT: REAL underlying defect, but the finding is MIS-FRAMED and its proposed fix is ACTIVELY HARMFUL. Recalibrated high->medium.

WHAT I CONFIRMED (concrete evidence):
1. tsk_xrefs.json (content/sources/) is keyed under LEGACY codes for 5 books: ezk(48 ch/13,659 verse-entries), jol(3/1,041), nam(3/582), php(4/2,227), jas(5/2,149). The canonical equivalents eze/joe/nah/phi/jam are NOT present as keys. The legacy codes also appear as cross-ref TARGETS inside the tuples: ezk x11,233, php x2,320, jas x2,592, jol x1,013, nam x558.
2. run_xref_at_scale.py iterates the ACTUAL data keys (books=sorted(tsk_raw.keys()) line 140; raw.get(book) line 91) and passes the legacy code straight to detector.detect(book,...) and write_queue(book,...). Result on disk: ezk_ch_*.json (49 files), jol/nam/php candidate files — all git-tracked — keyed under legacy codes, SEPARATE from the canonical eze_ch_*.json (48, from Hebrew/Torrey detectors).
3. CrossRefDetector (detectors.py:455-497) emits bodies linking to vnote-ezk-43-2 etc. (confirmed in ezk_ch_001.json) and display text "Ezk 43:2" — legacy anchors that won't match canonical eze-keyed verse anchors.
4. promote.py reads book=data["book"] (line 530, =legacy "ezk") and calls promote_candidate which does NOTES_DIR/f"{book}.py" (line 393) -> content/notes/ezk.py, which does NOT exist (only eze.py/joe.py/nah.py/phi.py/jam.py exist) -> silent return (False,"") at 394-395. So TSK xrefs for these 5 books are silently UN-promotable. (Verified no legacy vnote-ezk/jol/nam/php anchors leaked into promoted content/notes/ -- the loss is silent, not a corruption.)

WHY THE FINDING'S FRAMING/FIX IS WRONG:
- _normalize_book_code is symmetric-by-design ONLY where data is canonical and queries may be legacy (sources_commentary.py:316/326). For TSK the DATA ITSELF is legacy-keyed.
- The proposed fix (add _normalize_book_code(book) inside Tsk.refs_for, mapping query ezk->eze) would BREAK the currently-working path: the runner passes ezk, data is keyed ezk; normalizing the query to eze returns 0 results for all of Ezekiel. Backwards.
- Nave's/Torrey's JSON have NO legacy keys (extract_torrey_ccel.py:118 normalizes at extraction). So the finding's proposed additions to NavesTopical/TorreyTopical.topics_for are no-ops.
- Adding the 4 aliases jol/ezk/nam/php to _BOOK_CODE_ALIASES is harmless hygiene (sources_base.py:54-65 vs extract_torrey_ccel.py:101-110 _LEGACY_TO_CANON which already has all 8) but fixes nothing by itself.

CORRECT FIX (effort M): normalize TSK keys AND target codes to canonical at index-build time (mirror extract_torrey_ccel.py's extraction-time normalization and sources_commentary.py's stored-key normalization), regenerate tsk_xrefs.json + the legacy-coded candidate files, delete the orphan ezk_/jol_/nam_/php_/jas_ candidate JSONs. Optionally also expand _BOOK_CODE_ALIASES for completeness so the central function matches _LEGACY_TO_CANON. Do NOT normalize the query inside refs_for.

SEVERITY medium not high: real tracked data/code drift causing silent loss of TSK cross-ref candidates for 5 books, but confined to the candidate->promote pipeline (not the byte-stable 9 KJV editions, not security/crash), nothing corrupt leaked into promoted notes, and TSK promotion for these books may never have run.

## [correctness] 49 legacy-code candidate files on disk (ezk_ch_*, jol_ch_*, nam_ch_*) are permanently unpromotable — silently skipped by batch_promote_xrefs.py -> real=True sev=low
- VERDICT: REAL but materially mischaracterized and over-severe. Recalibrated HIGH -> LOW.

WHAT I CONFIRMED (mechanism is real):
- The legacy-coded candidate files exist and are git-tracked: 49 ezk_ch_*.json (001-048 + the impossible 081), 4 jol_ch_*.json (001-003 + 008), 3 nam_ch_*.json (001-003). Each has "book":"ezk"/"jol"/"nam" (e.g. content\candidates\ezk_ch_001.json:2).
- The notes files use CANONICAL codes: content\notes\eze.py / joe.py / nah.py exist; there is NO ezk.py / jol.py / nam.py.
- promote.py:393 `book_path = NOTES_DIR / f"{book}.py"` then :394 `if not book_path.is_file(): return False, ""` -> with book="ezk" it looks for ezk.py, misses, skips. batch_promote_xrefs.py:77-79 (by-book path) prints "no notes file (ezk.py) - skipping". Neither promote path calls _normalize_book_code, and that alias map (scripts\core\sources_base.py:54-65) only covers joh/ps/jas anyway, NOT ezk/jol/nam. So yes, these specific files are unpromotable as-is.
- Root cause is confirmed at fetch_sources.py:153 "Ezek":"ezk", :156 "Joel":"jol", :161 "Nah":"nam" (TSK_BOOK_REMAP, also inherited by NAVES_BOOK_REMAP at :249). Same bug also mis-maps :177 "Phil":"php" (notes file is phi.py) and "Jas":"jas" (notes file is jam.py) -- latent, though no php_/jas_ candidate files were materialized.
- ezk_ch_081.json (Ezekiel 81:11) is real OCR junk that bypassed _naves_coord_in_extent (fetch_sources.py:388 canonical_book_shape('ezk') -> unknown -> True at :391).

WHY THE FINDING IS WRONG ON IMPACT/SEVERITY (refutation):
- The finding's core implication ("permanently unpromotable" => Ezekiel/Joel/Nahum are missing their xref/topic notes, content loss) is FALSE. content\notes\eze.py contains 840 xref-citation/topic-nave notes, joe.py 50, nah.py 22 -- INCLUDING the exact WHEEL/CHEBAK topic-nave bodies that appear in ezk_ch_001.json. These were re-promoted under the canonical code by the 2026-05-09->2026-05-13 CCEL rebuild (commit 38b80d3a "rebuild Nave's clean from CCEL", and commit 13462720 title explicitly says "legacy book-code normalization"). The newer extract_torrey_ccel.py:103 has its own "ezk":"eze" remap with a comment that this is needed "else those notes land nowhere (the BUGCLUSTER class)."
- CANONICAL-coded candidate files ALREADY EXIST AND ARE TRACKED: 54 eze_ch_*.json plus joe_ch_*/nah_ch_* files. So the ezk/jol/nam files are STALE DUPLICATES from the superseded 2026-05-09 archive.org OCR ingest (commit f726f574), already replaced. They are dead data, not a missing-notes bug.
- The finding's proposed FIX is unsafe: it says rename ezk_ch_001.json -> eze_ch_001.json, but eze_ch_001.json already exists and is the live file -- the rename would collide/overwrite real current candidates. Correct fix is to DELETE the 56 stale legacy-coded files (and ezk_ch_081.json), NOT migrate them.
- The impossible-chapter problem is BROADER than ezk_ch_081: deu_ch_081/082/097, num_ch_081/082/084, 2ch_ch_080/081, pro_ch_080/081, gen_ch_085-089, isa_ch_080-087, jer_ch_082-087, job_ch_086, rev_ch_069 etc. are also out-of-extent OCR artifacts. Treating ezk_ch_081 as a one-off undersells it.

NET: This is genuine dead-data / hygiene debt (stale duplicate candidate files the pipeline silently no-ops on) plus a latent remap-code-smell (php/jas), but there is zero product/content impact -- the notes are present under canonical codes. That is LOW severity, not HIGH.

FIX SKETCH (effort S): (1) Delete the 56 superseded legacy-coded candidate files (ezk_/jol_/nam_*.json), git-tracked, since canonical eze_/joe_/nah_ equivalents already exist and were promoted. (2) Optionally sweep all out-of-extent *_ch_NNN.json (drop a coord-validity guard into a cleanup script). (3) Fix the remap source of truth in fetch_sources.py:153/156/161 (and the latent php/jas at :177/:186) to emit canonical codes so a future re-run never regenerates legacy-coded files. Confidence the issue is real: 0.9; confidence the finding's stated severity/impact ("permanently unpromotable", content gap) is accurate: ~0.1.

## [code-debt] run_xref_at_scale.py write_queue overwrites instead of appending — data-loss when run after other drivers -> real=True sev=medium
- CONFIRMED REAL. scripts/run_xref_at_scale.py:62-77 write_queue builds a fresh payload and calls out_path.write_text() unconditionally — no read-existing/append logic — and resets candidate ids to 1 each run (enumerate(..., start=1) at line 74). I verified all 8 named sibling drivers (hebrew, greek, torrey, naves, ethiopian, kenyon, ai_xrefs, ai_notes) and every one reads the existing per-chapter file and appends, numbering new ids from len(existing)+1; several docstrings (kenyon, ai_notes, greek, hebrew) explicitly state they "preserve prior detector output (TSK, ...)" — TSK/xref-citation being exactly what this driver writes. So the asymmetry is unmistakably unintentional: the siblings were deliberately written to avoid clobbering xref's output, but the xref driver reciprocally clobbers all of theirs. CHANGELOG.md:8854-8857 documents the canonical pattern as "append-not-overwrite per chapter; ids continue from existing tail to avoid collisions," retrofitted onto naves and every sibling; the xref driver (last touched in initial-arc commit b521fcca) was simply never updated. The data store is real and git-tracked: 1,666 content/candidates/*.json files, not gitignored. The id-collision sub-claim is also valid: ids are {book}-{chapter}-{verse}-{idx:03d} with idx chapter-file-wide sequential, and the in-repo kenyon/ai_xrefs comments themselves warn that "a fresh start at 1 would collide with already-promoted entries."\n\nRecalibrated high->medium. Mitigants the original finding understated: (a) candidates/ is git-tracked, so any clobber is visible in git diff and recoverable from history rather than being irreversible data loss; (b) these JSONs are an intermediate review-queue artifact (promote.py lifts accepted notes into the real notes store) not the final corpus; (c) the bug only triggers if at-scale drivers are re-run out of the conventional xref-first order. Still a genuine latent footgun — silently deletes other drivers' pending un-promoted candidates and is the lone deviation from a deliberately-established repo-wide contract.\n\nFix sketch (effort S, ~15 lines): replace the write_text-only body at run_xref_at_scale.py:62-77 with the naves/torrey merge pattern — read out_path if it exists, pull existing.get("candidates", []), number new candidates from len(existing)+1, write existing+new with n_candidates summed. No per-kind filter is needed because xref-citation is the base layer (use the torrey/naves variant, not the kind-filtered hebrew/greek/ai variant). OFF-LIMITS marathon core untouched; no schema change; atomic-write lint not implicated since the file already uses Path.write_text like every sibling.

## [code-debt] catholic_commentaries.json uses legacy book code 'mar' (Mark) not covered by _normalize_book_code — 2 entries silently dropped on lookup -> real=True sev=medium
- CONFIRMED REAL. Verified every link in the chain by reading the actual code.

1. scripts/core/sources_base.py:54-65 — _BOOK_CODE_ALIASES = {"joh":"jhn", "ps":"psa", "jas":"jam"}. 'mar' is NOT a key. _normalize_book_code('mar') therefore returns 'mar' unchanged (line 68-71).

2. content/sources/catholic_commentaries.json:50 and :60 — two entries with "book": "mar" (Bede on Mark 1:1; Gregory the Great on Mark 16:15). The four Gospels in this corpus use codes mat/mar/luk/joh; Mark is the only legacy code not aliased (joh IS aliased; mat/luk are canonical already).

3. scripts/core/sources_commentary.py:316 stores keys as (_normalize_book_code(obj.book), ch, vs) -> the two Mark entries are indexed under ('mar', 1, 1) and ('mar', 16, 15). for_verse (line 326) symmetrically normalizes the query code.

4. Production driver is scripts/prospect.py: line 275 takes a book-code arg, line 304 validates it via config.get_book (registry = content/books.yaml, which uses canonical 'mrk' at line 658 — there is no 'mar' code), line 84/68 discovers verses from the EPUB by id="v-{book}-..." (canonical codes), line 222 calls det.detect(book, ch, v, vtext). So prospecting Mark runs CatholicCommentaryDetector.detect('mrk', ...) (detectors.py:1321-1323) -> for_verse('mrk', ...) -> _normalize_book_code('mrk')='mrk' -> looks up ('mrk', ...) -> MISS. Both Mark Catena entries are silently dropped.

5. Corroboration the audit flagged: scripts/extract_torrey_ccel.py:100-110 explicitly documents 'the central _normalize_book_code covers only joh/ps/jas, so cover the full set here' and defines _LEGACY_TO_CANON with 'mar':'mrk' plus jol/ezk/nam/php/joh/jas/ps. The same mar->mrk mapping also exists in link_xrefs.py:159, extract_translation.py:123, versification.py:665, sources_lexicon.py:508, extract_eastons_ccel.py — the central resolver is the lone omission (the documented book-code ★BUGCLUSTER).

6. Empirical: grep found ZERO comm-catholic entries anywhere in content/notes/, and there is no content/notes/mar.py (only mrk.py). The Mark Catena entries never reached promotion — consistent with the silent-drop hypothesis.

REFUTATION ATTEMPTS that failed: (a) The sources_base.py docstring (line 59) references a '_BOOK_CODE_ALIASES_LONGFORM dict further down this file' that maps 'james'->'jas'; I checked — that dict does NOT exist in the file (file is 72 lines, ends at _normalize_book_code) and a grep for it found only that stale docstring reference, so it cannot rescue 'mar'. (b) run_ethiopian_at_scale.py iterates _by_verse keys and feeds them back verbatim, which WOULD process 'mar' keys — but that self-iterating runner is ethiopian-only; there is no run_catholic_at_scale.py, and the catholic detector is driven by prospect.py which uses canonical codes. (c) mat/luk entries are fine (canonical), joh resolves via the joh->jhn alias; only Mark is broken.

SEVERITY RECALIBRATED high->medium: it is a genuine correctness bug and trivial to fix, BUT the blast radius today is small/latent — the comm-catholic seed has not been promoted for any book yet (zero comm-catholic notes shipped), so no currently-built EPUB is missing content specifically because of this. It becomes an active silent content-loss the moment the chi.4 Catholic seed is prospected for Mark. Real, actionable, latent.

FIX: add "mar": "mrk" to _BOOK_CODE_ALIASES in scripts/core/sources_base.py (one line, S effort). Defensively also add jol->joe, ezk->eze, nam->nah, php->phi to fully mirror extract_torrey_ccel._LEGACY_TO_CANON and close the ★BUGCLUSTER class for any future commentary JSON. Effort S. (Tangential: also fix the stale _BOOK_CODE_ALIASES_LONGFORM / prospect-as-core docstring references, but those are separate doc-drift, not this finding.)

## [security] Stored XSS in /api/sample/ HTML output — note body inserted without sanitize_html(); CSP nonce injection makes it exploitable -> real=True sev=high
- CONFIRMED REAL — every link in the chain verified against the actual code.

SINK: scripts/web_content.py:366-369, `_render_sample_html`. Line 366 `body = str(n[7] or "")  # body is already HTML`; line 369 interpolates `{body}` verbatim into `<li class="note">...{body}</li>`. No sanitize_html call. Grep confirms web_content.py imports/calls no sanitizer anywhere.

DATA IS STORED RAW: web_helpers.py:135-147 `dict_to_tuple` writes `d["body"]` (line 145) unchanged into the NOTES tuple position 7. notes_io.load_notes returns it verbatim (web_content.py:273-274). A grep for sanitize_html across scripts/ returns only inject.py (build path), preview.py (preview render), detectors/sandbox/whitelist — NOT notes_io, web_helpers, or any ingest/promote path. So bodies are never sanitized at write time.

SIBLING IS HARDENED, THIS PATH MISSED IT: scripts/core/preview.py:125-135 (`_render_note_aside`) wraps the body in `sanitize_html(body)` with a comment (lines 130-132) explicitly stating the route is UNAUTHENTICATED and AI-authored bodies share the store so "publisher-trusted does not hold here." The /api/sample renderer is the same kind of unauthenticated render of the same store but was not given the same treatment — a clear, self-acknowledged inconsistency.

EXPLOITABILITY VIA NONCE INJECTION — VERIFIED AND SHARP: route web.py:1654-1676 sends the HTML via `self._send_html(result["html"])`. _send_html (web.py:1209-1219) generates a per-request nonce, then `_inject_script_nonces(html, nonce)` (line 1212). _inject_script_nonces (1117-1152) rewrites EVERY `<script` (regex line 1083) to `<script nonce="{nonce}"` (line 1137) — including a `<script>` smuggled in from a note body. The CSP applied is _csp_with_nonce (line 1168→1106): `script-src 'self' 'nonce-{nonce}' https://cdn.tailwindcss.com`, which drops 'unsafe-inline' but ALLOWS the now-noncified injected script. So the nonce machinery actively defeats the CSP for body-injected scripts. Confirmed exactly as described.

FIX VERIFIED CORRECT: html_sanitize.py:39-42 lists `script` (and iframe/style/on* handlers/javascript: URLs) among disallowed tags whose text is preserved but tag dropped, while whitelisting the legit apparatus tags (em/a/sup/strong/etc.). So `body = sanitize_html(str(n[7] or ""))` mirroring preview.py:133-135 is the right one-line fix and won't break legitimate inline markup. Effort S.

SEVERITY CALIBRATION: I keep HIGH but note the one mitigating factor pulling toward medium. This is a localhost single-user desktop app (html_sanitize.py:21-27 threat model), so the attacker model is not a remote multi-user web server — it requires a hostile/noisy note body to already be in the store (AI-generated note, imported scenario, restored backup, or a shared/imported content/notes/*.py file). That is a real and documented vector (the sibling's own comment cites AI-authored bodies), and once it fires, same-origin JS on the app origin can drive every 'self' API (backups list/restore, note write, edition mutation), so impact is meaningful. The original reporter's "CSP nonce injection makes it exploitable" claim is precisely correct rather than hand-waved. Net: a genuine, actionable, cheap-to-fix stored-XSS sink that the codebase already deemed worth hardening one function over. Real=true, high.

## [tests] KENYON map yields non-canonical jol/php/jas; Joel/Phil/James witnesses silently drop -> real=True sev=medium
- CONFIRMED REAL. Verified every link in the chain against the live code.

1. Non-canonical map values (the core claim): scripts/core/sources_lexicon.py KENYON_BOOK_NAME_TO_CODE maps "joel"->"jol" (line 485), "phil"/"philippians"->"php" (lines 525-526), "jas"/"james"->"jas" (lines 545-546). Live diff confirms: set(map.values()) - set(books_by_code()) == {'jas','jol','php'}. Canonical codes are 'joe','phi','jam' (content/books.yaml lines 536/751/842; covers/_book_defaults in editions.yaml also use joe/phi/jam). None of jas.py/jol.py/php.py exist under content/notes/ (joe.py/phi.py/jam.py do).

2. references() propagates the raw value: sources_lexicon.py:638 book_code = KENYON_BOOK_NAME_TO_CODE.get(key); :645 max_ch = ch_counts.get(book_code, 0) returns 0 for jol/php/jas, so the chapter-extent guard at :646 short-circuits (max_ch falsy) and the KenyonReference is created with book='jol'/'php'/'jas' (line 655). No drop here — it propagates downstream.

3. Detector + driver carry it forward: detectors.py:688 indexes on ref.book unchanged; run_kenyon_at_scale.py writes content/candidates/{book}_ch_NNN.json using the raw code (no _normalize_book_code call anywhere in that driver).

4. The silent drop happens at promote: promote.py:393-395 book_path = NOTES_DIR/f"{book}.py"; if not is_file(): return False, "" — fully SILENT for jol/php/jas. batch_promote_xrefs.py:76-78 prints a warning then skips (semi-silent). Either way the witness is dropped.

5. _normalize_book_code (sources_base.py:54-71) only covers joh->jhn, ps->psa, jas->jam — it does NOT map jol->joe or php->phi, and the Kenyon path never invokes it anyway, so even the one alias it does know (jas) is not applied here.

6. Test gap confirmed: tests/test_scripts.py:8328 test_book_code_map_covers_canonical_set only spot-checks 9 codes (gen,exo,psa,mat,rev,1sa,2ki,1co,rom) as VALUES present; it never asserts the reverse (every value is canonical / has a notes file), so the bug passes undetected.

SEVERITY recalibrated from 'high' to 'medium'. Impact is real data-loss for any Joel/Philippians/James textual-criticism witness Kenyon discusses, but it is currently LATENT: no Kenyon text-witness candidates exist on disk for these books yet (the jol_ch_*.json files present came from CrossRef/Nave detectors — a related but separate instance of the same canonical-code BUGCLUSTER, not Kenyon), no shipped KJV EPUB is corrupted, and the batch path emits a visible warning. It is part of the documented book_code_canonical BUGCLUSTER, so it is a genuine mint defect worth fixing, but not high-severity because nothing currently-built is wrong.

FIX (effort S): change the three map values to canonical (jol->joe, php->phi, jas->jam) in sources_lexicon.py, OR (more robust) route ref.book through a complete normalizer at sources_lexicon.py:638/655 and extend _BOOK_CODE_ALIASES to include jol->joe and php->phi. Then strengthen tests/test_scripts.py:8328 into a meta-test asserting every KENYON_BOOK_NAME_TO_CODE value is in books_by_code() AND has a content/notes/<code>.py file. Note this overlaps the broader BUGCLUSTER (CrossRef/Nave also emit jol) — a central _normalize_book_code applied at every detector/driver boundary, with the same meta-test generalized across all detectors, would be the durable mint-7 fix (effort M).


