# MacClaude — laundry list to v1.0.0 (verified 2026-06-14, WIN lane)

Built from a 5-dimension code-verified audit (not the roadmap text — several roadmap
rows were stale). Each item cites real evidence; `blocks_v1` is honest (most depth is
opportunistic). **mode=parallel ⇒ every Mac task below is file-disjoint from WIN's
active surfaces** (build pipeline / website / catalog / Phase-D Esther). Pull before
touching any both-lanes file (`build_edition.py`, `verify_kr2_build.py`).

---

## TL;DR — the honest v1.0.0 gap

The genuine v1.0.0 **blockers are almost all WIN-owned and already in hand**:
- **CI red on BOTH remotes — being fixed by WIN this turn:** GitHub fast-gate (`commercial_terms`: 5× `KDP` in `build_edition.py` comments, from the Mac turn-81 E999 commit `6a096772`) + 2 stale `test_scripts.py` pins; GitLab pipelines were `ci_quota_exceeded` (compute-minutes limit, ~Jun 6) → fixed by disabling per-push GitLab pipelines (`.gitlab-ci.yml workflow:rules`).
- **Stale pre-pivot public docs — WIN:** `dev/RELEASE_NOTES_v1.0.0.md` (buyer/ISBN/retail, 51,394/87/13 counts) + `HANDOFF_README_v7.md` (ONIX/BISAC/KDP/"goes to market") — both need a free-public rewrite.

**The Mac lane's contribution to v1.0.0 is narrow:** finish the Kindle distribution column (the E999 blocker is RESOLVED), re-notarize the DMG at tag time, and optionally pick up file-disjoint depth/polish. Nothing on the Mac list strictly *blocks* a professional first release except its half of the Kindle column ship-out.

---

## A. Distribution — finish the Kindle column (M4)  ·  Mac's marquee piece

★★ **GOAL CORRECTION (user, 2026-06-14) — read this before any Kindle work.** The
deliverable is that a normal user can take the free **kindle-target EPUB and
Send-to-Kindle it onto their own Kindle** (the free, personal, non-commercial path).
**KDP / kdp.amazon was only ever a TEST ORACLE, never the goal — we are NOT publishing
to the Kindle Store.** Turn-81 drifted into "make the EPUB pass KDP conversion" and
declared E999 RESOLVED on a *KDP* pass, while calling the actual user path
(Send-to-Kindle) "flaky/blind" and leaning on KDP instead. **So E999 is resolved only
in the KDP sense; Send-to-Kindle was never re-confirmed with the fixed artifact. M4 is
NOT done.** The strip-hidden fix itself is still correct (hidden content genuinely breaks
Amazon ingestion) — what was wrong is the success criterion.

★★ **CONFIRMED 2026-06-14 (user tested):** the shipped `FIXED.epub` was uploaded via
**Send-to-Kindle and FAILED.** So strip-hidden alone does NOT fix the real channel — **M4
is confirmed BLOCKED, not "nearly done."** Mac's turn-82 reconstruction (9-agent workflow
`wf_f714f284-c10`) further found the *entire* prior Kindle investigation was measured on
the **falsified KP3/KDP oracle** — the byte/element ceilings, `FILE_SPLIT_TARGET_KINDLE`
= 2 MB, and `MAX_POPUP_LANGUAGES_KINDLE` = 2 are all **unverified on Send-to-Kindle**, and
the one historical STK success (test-2, 06-10) is unrecoverable. **So the Kindle path must
RESTART against the real oracle (Send-to-Kindle on the user's device), not patch forward
on KP3/KDP-derived assumptions.** First moves: reproduce test-2's recipe from source (full
apparatus, display:none physically stripped) → STK *web uploader* re-test (on a
confirmed-up STK day); if it still fails, bisect against STK — file size, the popup
link-graph, and the split geometry are all back on the table as candidate causes.

1. **Rebuild ONE kindle-target edition first** — `build_edition <id> --target-reader kindle`
   so `apply_kindle_strip_hidden` runs; gate-5 (`dev/verify_kr2_build.py`) GREEN + epubcheck
   0/0/0/0. (Don't fan out to 9 until the device test below passes.)
2. **THE REAL ACCEPTANCE TEST = Send-to-Kindle onto a real Kindle** (user-assisted): send
   the fixed EPUB via Send-to-Kindle (email / app / web) and confirm it lands and opens
   with popups intact. The KDP dashboard is a *secondary diagnostic only*. **If
   Send-to-Kindle still throws E999 with the strip-hidden artifact, THAT is the real
   remaining Kindle blocker — re-open the investigation; do NOT close M4 on a KDP pass.**
3. **Only after Send-to-Kindle is device-proven:** rebuild all 9 kindle artifacts (gate-5 +
   epubcheck each) → hand them to WIN for the catalog regen + deploy (`gen_release_catalog`
   + `website/` are WIN surfaces; don't edit catalog.json / website from the Mac — collision).
4. **Update `dev/EREADERS.md`** Kindle row to the TRUE state — frame acceptance as
   Send-to-Kindle (not KDP); record the strip-hidden mechanism + whether Send-to-Kindle
   actually passed.
- Files (Mac side): `scripts/build_edition.py`, `scripts/build_format_matrix.py`, `dev/verify_kr2_build.py`, `dev/EREADERS.md`. Priority P2 · blocks_v1 **no** on the merits (catalog already ships 2 live format columns × 9 editions + 3 signed desktop binaries), but the Kindle column must NOT go "live" claiming Send-to-Kindle works until it is device-proven.

## B. Desktop — tag-time macOS DMG re-notarization  ·  Mac-only, mechanical

At the v1.0.0 version bump: re-run `dev/build_dmg.sh` with `CODESIGN_IDENTITY` +
`NOTARIZE_KEYCHAIN_PROFILE` → codesign (runtime+entitlements) → `notarytool submit
--wait` → `stapler` → upload to the release. (v0.1.0 DMG already notarized+stapled,
submission 27aedc8a — this is just the re-cut for the new tag.) Files: `dev/build_dmg.sh`,
`dev/YHWH.entitlements`, `dev/NOTARIZATION_STATUS.md`. P2 · blocks_v1 no.

## C. Content lanes — opportunistic, user-paced, file-disjoint from WIN's Esther

Corpus depth target is already met (83 books, ~91,733 notes, standalone Ge'ez 4 books
epubcheck 0/0/0/0). None of these block v1.0.0; all are different book files than WIN's `est`.

1. **LANE-M Kings/Samuel manuscript marathon** (highest-value Mac content) — remaining Ge'ez dual-witness transcription + collation; MAX-1 heavy agent, ≤1568px crops, folios by vision, per-step commits, **run slowly with user check-ins** (marathon-pacing). Files: `content/translations/geez-tewahedo/{1ki,1sa,2sa}.py`, `scripts/core/manuscript_collation.py`. P3.
2. **Phase-D batch own-vers** (1ch/2ch/ezr/neh/job + HaCohen sir/wis) — sequence AFTER WIN's Esther proves the path; `job` early (HaCohen cross-validates). Per-book calibrate-first GO/NO-GO → own-vers store → xref sidecar → `_STANDALONE_BOOKS` → rebuild. Different files than `est`. P3.
3. **Phase-D2 distinctive PD** (1 Enoch Charles 1906, Jubilees Charles 1895) — independent background lane, NOT gated on Esther; re-verify Meqabyan NO-GO + 4 Baruch per-book. P3.

## D. Polish / hygiene — either-lane; Mac takes to stay disjoint from WIN's doc rewrites

1. **Phase-E Clementine status-reconcile + render-QA** — data is ALREADY DONE (`vulgate-clementine/{man,1es,2es}.py` carry real Latin: 15/279/823 verses); the roadmap "phase-E open" row is stale. Verify they render via `vulgate_to_kjv` + reconcile the status. P3.
2. **Midjourney cover-licensing paragraph** — extend `content/sources/ATTRIBUTIONS.md` + `content/covers/templates/README.md` with the licensing basis (Midjourney ToS commercial-use grant for the paid tier / AI-art stance). Fonts already have a full OFL register; covers document origin but not the rights basis. One paragraph, no new assets. P3 · a clean public release wants this.
3. **audit_log positional-arg redaction** (latent defense-in-depth) — `_summarize_args` redacts kwargs only, not positional args; no live leak today (single-payload-dict endpoints summarize to a key-count) but `api_auth_totp_confirm`'s `secret` rides a positional payload. Bind `inspect.signature` + apply `_REDACT_KEYS` to positional params + TDD guard. Files: `scripts/core/audit_log.py`, `scripts/api/auth.py`. P1 (hardening).
4. **data-hygiene** — add missing `_meta.yaml` to `content/translations/geez-tewahedo-en/` + `amharic-tewahedo-en/`; confirm the single-book Brenton seeds don't surface as incomplete translations in any shipped edition. P3.
5. **reverify SHIFTS=0** — generalize the Douay/Vulgate versification check to WLC/LXX/Arabic/JPS. P3.

## E. Mac setup loose end (from turn-81)

Add the Kilo `chrome-devtools` MCP manually: `kilo mcp add` → name `chrome-devtools`,
cmd `npx`, args `-y chrome-devtools-mcp@latest` (the auto-mode classifier blocks the
`bash:allow` config edit; `kilo mcp add` is interactive-only).

---

## What WIN is handling (do NOT touch — collision)

- Red-CI fixes (this turn's push): the 5 KDP comment refs in `build_edition.py`, the 2 stale `test_scripts.py` pins, `.gitlab-ci.yml` per-push disable.
- The stale-doc rewrites: `dev/RELEASE_NOTES_v1.0.0.md` + `HANDOFF_README_v7.md` (free-public rewrite).
- M1/M2 (done, deployed), M3 Kobo column (gated on user taps), the catalog/website deploy step, Phase-D Esther vision marathon.

## ⚠ Parity / Guard #6 (PULL before your next build_edition / verify_kr2_build touch)

WIN edited (this push): `build_edition.py` (5 KDP→Amazon comment rewords — comments only,
no logic), `tests/test_scripts.py` (2 stale pins aligned to current behavior),
`.gitlab-ci.yml` (workflow:rules). All distinct from your turn-81 functions; git
auto-merges, but `git pull --rebase` first.

**ROOT-CAUSE FLAG for Mac (RULES §12):** the turn-81 KDP comments reached `main` even
though `commercial_terms` is a FAIL-enforced pre-commit gate — meaning that commit
bypassed the hook (either `--no-verify`, or the Mac's `.githooks` pre-commit isn't
installed / isn't running `lint_rules.py`). Please verify `git config core.hooksPath`
points at `.githooks` on the Mac and that a commit of a banned term actually blocks —
otherwise the next decommercialize breach will red-CI `main` again.
